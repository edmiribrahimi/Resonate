---
phase: 46-silent-failures-on-the-money-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/46-silent-failures-on-the-money-path/46-COPY.md
autonomous: false
requirements: [OBS-02, OBS-03, OBS-04]

must_haves:
  truths:
    - "REQUIREMENTS.md declares OBS-02, OBS-03 and OBS-04, and does not declare OBS-05"
    - "Every sentence this phase will ship exists in one list before any of it is merged into code"
    - "The owner approved that list in a single pass, not sentence by sentence"
    - "No traceability row created here cites an item that is out of this phase's perimeter"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "OBS-02/03/04 under a new `### Observable Failure on the Money Path` heading, three traceability rows, and the corrected DS-08 row"
      contains: "Observable Failure on the Money Path"
    - path: ".planning/phases/46-silent-failures-on-the-money-path/46-COPY.md"
      provides: "Every category and its one sentence, for every copy-bearing plan in this phase, plus the two decisions the owner must take in the same pass"
      min_lines: 80
  key_links:
    - from: ".planning/phases/46-silent-failures-on-the-money-path/46-COPY.md"
      to: "46-04, 46-05, 46-06, 46-07"
      via: "each sentence names the plan and the site it lands in"
      pattern: "46-0[4-7]"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 46"
      via: "traceability rows"
      pattern: "OBS-0[234] \\| Phase 46"
---

<objective>
Create the three requirements this phase is measured against, and produce — in one
document, approved in one pass — every sentence the phase will ship.

Purpose: `community-membership.md` states it as a rule, not a preference: *un rifiuto è
una comunicazione* and *il testo del rifiuto va scritto una volta, con cura, e usato
sempre lo stesso*. Sentences decided per-plan produce four registers on one product. This
plan is the gate that makes that impossible: **no copy-bearing plan in this phase merges a
sentence that is not on the approved list** (D-46-10a).

Output: `OBS-02`, `OBS-03`, `OBS-04` in `.planning/REQUIREMENTS.md` with their traceability
rows; `46-COPY.md` with the full sentence list; the owner's approval recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-silent-failures-on-the-money-path/46-CONTEXT.md
@.planning/phases/46-silent-failures-on-the-money-path/46-PATTERNS.md
@.planning/phases/46-silent-failures-on-the-money-path/46-VALIDATION.md
@.claude/rules/community-membership.md
@.claude/rules/meta-gates.md

<scope_limits>
Stated here so no plan downstream has to rediscover them, and so verification does not
find them by surprise:

- **`OBS-05` is NOT created.** It covered `DI-41.2-09` — the discount that does not survive
  registration — and that code is out of perimeter under D-46-11. Creating the ID and
  leaving it unmet would be worse than not creating it.
- **Roadmap success criterion 3** (*the one that charges the wrong amount is fixed first*)
  **is not met by this phase** and cannot be: its code is under review for deletion.
- **Roadmap success criterion 2's "five RSVP refusals"** is not met by this phase, same
  reason (`rsvp-actions.ts` requires an account).
- Criteria 1, 4 and 5 are met **within the perimeter of `46-CONTEXT.md` `<domain>`**, which
  is narrower than the roadmap's nine.
</scope_limits>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare OBS-02, OBS-03 and OBS-04, and repair the stale DS-08 row</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole file. Read the `### Venue Reveal`,
      `### Design System` and `### Production` sections (around `:112-149`) for the house
      style of a requirement line, and the `## Traceability` table (from `:178`) for the
      house style of a row. **Re-measure every line number in this task before editing:
      the file is edited by many phases and the numbers below are from 2026-08-14.**
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-VERIFICATION.md:415` and
      `:420` — the DS-08 finding and the traceability drift it recorded but did not fix.
      Read both before touching the DS-08 row; `:420` names the exact drift.
    - `.planning/phases/46-silent-failures-on-the-money-path/46-CONTEXT.md`
      `<phase_requirements>` — the amendment that removes `OBS-05`.
  </read_first>

  <action>
    Insert a new section `### Observable Failure on the Money Path` in the
    `## v1.5 Requirements` block, immediately after `### Production` and before
    `## Future Requirements`. Use the file's own line style: an unchecked box, the ID in
    bold, a colon, then the requirement.

    Three requirements, and no fourth:

    **OBS-02** — on a path that carries money, a failure produces an effect the person
    affected can see, or, where only staff can act on it, an effect staff can see. No
    failure on these paths is discharged by a log line: there is no error tracking
    (`OBS-01`), so a log is a place nobody looks.

    **OBS-03** — a failed read is never rendered as a legitimate value. *We could not read
    your drinks* is distinguishable from *you have no drinks*, and *we could not count*
    from *none sold*.

    **OBS-04** — a refusal with distinguishable causes says which one, wherever the next
    step differs. The wording for each cause is written once and used always, and it
    travels as a returned value, never as a thrown message — Next redacts the message of an
    error thrown out of a Server Action in a production build
    (`src/lib/capabilities/server.ts:58-63`).

    Do **not** create `OBS-05`. `OBS-01` stays where it is, in `## Future Requirements`,
    untouched.

    Then add three rows to the `## Traceability` table, in the same shape as the `DS-07`
    row, placed after the `RESP-04` row and before the `DS-04 | Phase 42` row so the table
    keeps its execution order:

    - `OBS-02` → Phase 46 → Pending, citing **by name** every in-perimeter site whose
      repair is the appearance of an effect where none existed: `DI-41.2-02` (the custody
      write, swallowed whole), `DI-41.2-04` (the failed fetch reporting `"unknown"`, and
      the poll bound that fires silently), `DI-41.2-06`, `DI-41.2-06b`, `DI-TODO-B`. A row
      that says "the in-perimeter sites" without naming them reads, to the next person, as
      a coverage hole — which is the same defect in a table that this phase is removing
      from the product.
    - `OBS-03` → Phase 46 → Pending, citing `DI-41.2-03`, `DI-41.2-08`, `F-46-01`,
      `DI-TODO-A`, `DI-TODO-B`.
    - `OBS-04` → Phase 46 → Pending, citing `DI-41.2-06` and `DI-41.2-06b`.

    **No row may cite `DEF-41.2-A`, `DI-41.2-01`, `DI-41.2-07`, `DI-41.2-09`, `DI-41.2-10`,
    `DI-41.2-11`, `DI-41.2-12` or `DI-41.2-20`** — all out of perimeter. A traceability row
    that cites work nobody is doing is a promise the table cannot keep.

    Repair the DS-08 row (at `:252` on 2026-08-14 — re-measure by matching the row text
    `| DS-08 |`). Its prose already records that `RevealVenueDialog` adopted the primitive
    in 41.1 and that the row was wrong; the **Phase cell itself was never corrected** and
    still reads two phases where three are involved. Set the Phase cell to the three-phase
    form used by the `DS-07` and `RESP-01` rows, and leave the row's existing prose exactly
    as it stands — it explains the correction and deleting it would make the fix look like
    a drift of its own.

    Finally, the coverage line under the table. It reads a figure that must now change, and
    the note beneath it already disagrees with it by two. **Count the rows mechanically**
    — do not add three to the printed figure — and set both the coverage line and the
    note's figure to the counted value, keeping the note's existing sentence about a count
    being corrected when it was wrong at the time it was written.
  </action>

  <verify>
    <automated>LC_ALL=C /usr/bin/grep -c 'Observable Failure on the Money Path' .planning/REQUIREMENTS.md</automated>
    <automated>LC_ALL=C /usr/bin/grep -cE '^\| OBS-0[234] \| Phase 46 \|' .planning/REQUIREMENTS.md</automated>
    <automated>LC_ALL=C /usr/bin/grep -c 'OBS-05' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'Observable Failure on the Money Path' .planning/REQUIREMENTS.md` → `1`
    - `LC_ALL=C /usr/bin/grep -cE '\*\*OBS-0[234]\*\*' .planning/REQUIREMENTS.md` → `3`
    - `LC_ALL=C /usr/bin/grep -c 'OBS-05' .planning/REQUIREMENTS.md` → `0`
    - `LC_ALL=C /usr/bin/grep -cE '^\| OBS-0[234] \| Phase 46 \|' .planning/REQUIREMENTS.md` → `3`
    - `LC_ALL=C /usr/bin/grep -nE '^\| OBS-0[234] \|' .planning/REQUIREMENTS.md | LC_ALL=C /usr/bin/grep -cE 'DEF-41\.2-A|DI-41\.2-(01|07|09|10|11|12|20)'` → `0`
    - The `| DS-08 |` row's Phase cell names three phases; the row's prose is byte-identical
      to what it was before the edit (`git diff` on that row shows a change inside the
      second cell only)
    - The coverage figure equals the number of data rows in the traceability table, counted
      by command and shown in the SUMMARY, and the note beneath it carries the same figure
    - `git diff --stat` lists `.planning/REQUIREMENTS.md` and nothing under `src/` or
      `supabase/`
  </acceptance_criteria>

  <done>
    `.planning/REQUIREMENTS.md` declares OBS-02, OBS-03 and OBS-04 with three traceability
    rows citing only in-perimeter items; `OBS-05` does not appear anywhere in the file; the
    DS-08 row's Phase cell is correct and its prose untouched; the coverage figure matches
    a mechanically counted row count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Draft every sentence this phase will ship, in one list</name>
  <files>.planning/phases/46-silent-failures-on-the-money-path/46-COPY.md</files>

  <read_first>
    - `src/lib/door/outcome.ts:278-302` — `DOOR_NIGHT_ERROR`, the three sentences and the
      docblock at `:288-294` arguing *why* the second one earns its own lookup. That
      argument is the standard the sentences below must reach.
    - `src/app/(admin)/admin/events/actions.ts:327-370` — `nightRefusalSentence`, ten
      sentences on a staff surface, written once and used always. Read the register.
    - `.planning/phases/46-silent-failures-on-the-money-path/46-PATTERNS.md` §3.1 §3.2 §3.3
      §3.4 §3.6 — the exact site each sentence lands on.
    - `.planning/phases/46-silent-failures-on-the-money-path/46-CONTEXT.md` `<decisions>` —
      D-46-05 through D-46-10c. In particular D-46-10 (the product speaks English),
      D-46-10b (two distinguishable outcomes for the menu-closing command) and D-46-10c
      (guest-facing half only for the drink receipts).
    - `.claude/rules/community-membership.md` — a refusal is a communication, and the
      wording does not explain more than we are willing to defend.
    - `.claude/rules/brand-visual-system.md` — the brand is written `re:sonate` with a
      normal e, everywhere, including in a sentence rendered in the app.
  </read_first>

  <action>
    Create `.planning/phases/46-silent-failures-on-the-money-path/46-COPY.md`.

    It is a table-per-plan document. For every category that plans 46-04, 46-05, 46-06 and
    46-07 will declare, give: the constant name, the plan, the `file:line` predicate it
    renders at, the cause in one clause, and **the sentence**. English (D-46-10). One
    sentence per cause. No sentence composed at run time from a tier name, a drink name or
    a party title.

    The categories to cover, and nothing beyond them:

    **46-04, the organizer's menu-closing command** (`menu/actions.ts`,
    `PartyDrinkMenu.tsx`) — three causes that D-46-10b requires to stay apart:
    nobody is signed in; this account may not set a closing time; the write itself failed
    and the closing time did not change. Plus the caller's fallback for a refusal that
    arrived carrying no category at all — modelled on `EventForm.tsx:619-630`, which says
    exactly that instead of collapsing into a shared *something went wrong*.

    **46-05, the guest's drink receipts** (`GuestTokenDisplay.tsx`) — the browser could not
    store this order, so this device may not be able to show it again; the browser could
    not be read, so *we cannot see what you bought here* rather than *you bought nothing*;
    the drinks are still arriving; we could not reach the server; the server refused to
    answer; and the poll stopped without an answer after its bound. Six causes.
    **Constraint on the register (O3):** none of these sentences may manufacture alarm
    about money that is safe. A storage failure does not mean the purchase failed — it
    means this browser cannot prove it — and the sentence must say which. Include what the
    guest should do next where an action exists, and say nothing where none does.
    **Stated limit, and it belongs in the document:** D-46-10c ships the guest-facing half
    only. There is no bar-side lookup, so *show this screen at the bar* is advice we can
    only give where the screen still renders something.

    **46-06, the public event page** (`(public)/events/[slug]/page.tsx`) — one cause: how
    many places remain for this night could not be checked. One sentence, used for both the
    event-level control and the per-party control. **It must not read as a refusal of the
    buyer** — the control stays live by the owner's standing decision, and the sentence's
    job is to stop a number the page could not read from being presented as a fact.

    **46-07, the refund cron** (`api/cron/refund-expired-tokens/route.ts`) — the report
    strings for: the run did everything it set out to do; the cleanup delete was refused;
    fewer rows were deleted than were asked for; and one or more refunds could not be
    issued. These are read in a hosting dashboard, not by a guest, so the register is
    operator-facing — but they are written once here for the same reason as all the others.

    **46-03 contributes no sentence, and the document says so out loud.** D-46-08 repairs
    the two permissive server reads as observability, not as a new refusal, and D-46-05
    keeps their direction permissive. Recording the absence is what keeps a later reader
    from thinking a sentence was forgotten.

    Then a final section, `## Two decisions inside this approval`, because the one-pass
    approval is the only pass:

    1. **Does a failed refund make the cron run go red?** D-46-06 decided that the *cleanup
       delete* failing makes the run terminate as failed. It did not say what happens when
       `refundErrors > 0` — money that should have gone back and did not. The route
       already counts them per item. Present both options with their cost (the accepted
       cost of red-as-wallpaper is already on the record in D-46-06) and ask for one.
    2. **What a guest is told when the browser cannot hold their receipt and there is no
       bar-side lookup to send them to.** Present the sentence and state plainly that this
       phase gives them no second route (D-46-10c), so the owner approves the sentence
       knowing what it does and does not promise.

    `.planning/` is public. No venue under negotiation, no unannounced date, no line-up, no
    contact, and roles rather than people, in every example sentence.
  </action>

  <verify>
    <automated>test -f .planning/phases/46-silent-failures-on-the-money-path/46-COPY.md && LC_ALL=C /usr/bin/grep -c '46-0[4-7]' .planning/phases/46-silent-failures-on-the-money-path/46-COPY.md</automated>
  </verify>

  <acceptance_criteria>
    - `46-COPY.md` exists and contains a row for every category named in this task's
      `<action>`: three for 46-04 plus the no-category fallback, six for 46-05, one for
      46-06, four for 46-07 — and an explicit statement that 46-03 contributes none
    - Every row carries a constant name, the owning plan id, and a `file:` reference
    - `LC_ALL=C /usr/bin/grep -c 'rɘ:sonate\|Rɘsonate' 46-COPY.md` → `0` (the reversed e
      lives only inside the logo)
    - The document contains a section headed `## Two decisions inside this approval` with
      exactly the two questions named above
    - No sentence in the document interpolates a tier name, a drink name or a party title
      (checked by reading: no `${` and no bracketed placeholder inside a sentence cell)
    - `git diff --stat` lists only files under `.planning/`
  </acceptance_criteria>

  <done>
    One document holds every sentence this phase will ship, each tied to its plan and its
    site, plus the two open decisions the owner must settle in the same pass. Nothing is
    merged into code yet.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The owner approves the sentence list in one pass</name>
  <files>.planning/phases/46-silent-failures-on-the-money-path/46-COPY.md</files>

  <read_first>
    - `.planning/phases/46-silent-failures-on-the-money-path/46-COPY.md` — the document
      Task 2 produced, read end to end before presenting it, so the presentation is a
      reading and not a summary.
    - `.planning/phases/46-silent-failures-on-the-money-path/46-CONTEXT.md` D-46-10a — the
      approval is **one pass over the whole list**, not sentence by sentence.
    - `.claude/rules/community-membership.md` — the standard the owner is being asked to
      judge the sentences against.
  </read_first>

  <action>
    Present `46-COPY.md` to the owner in full and **halt**. Do not merge any sentence into
    any source file, and do not start plans 46-04 … 46-07. If the owner amends sentences,
    amend the document, re-present the **whole list**, and halt again: the approval is one
    pass over a complete list, never a sequence of per-sentence agreements (D-46-10a).
    Record the answers to both open decisions verbatim in the SUMMARY.
  </action>

  <what-built>
    `.planning/phases/46-silent-failures-on-the-money-path/46-COPY.md` — every sentence
    this phase will ship, in one list, with the site each one lands on; and
    `.planning/REQUIREMENTS.md` carrying OBS-02, OBS-03 and OBS-04.
  </what-built>

  <how-to-verify>
    1. Read `46-COPY.md` end to end. It is meant to be read in one sitting: that is the
       point of the list existing at all (D-46-10a).
    2. For each sentence, the test is the one `outcome.ts:288-294` sets — does it tell the
       person something they can act on, or does it send them to find somebody else?
    3. Check the register against `community-membership.md`: a refusal does not explain
       more than we are willing to defend.
    4. Check the drink-receipt sentences against O3 in particular: none of them may make a
       guest think money is gone when it is not.
    5. Answer the two questions in `## Two decisions inside this approval`.
    6. Confirm the three requirements in `.planning/REQUIREMENTS.md` read the way you want
       to be measured — they are what `46-VERIFICATION.md` will be written against.
  </how-to-verify>

  <resume-signal>
    Type "approved" to release plans 46-04, 46-05, 46-06 and 46-07, **and answer the two
    questions**. Or give the edits sentence by sentence — the list is amended and
    re-presented as a list, never merged half-approved.
  </resume-signal>

  <verify>
    <human-check>The owner's answer is recorded verbatim in `46-01-SUMMARY.md`, and it
    includes an answer to each of the two open decisions.</human-check>
  </verify>

  <acceptance_criteria>
    - `46-01-SUMMARY.md` carries the owner's approval verbatim, dated
    - Both questions under `## Two decisions inside this approval` have a recorded answer;
      neither is left as "to be decided during execution"
    - `git diff --stat src/` is empty at the moment of approval: no sentence was merged
      before the list was approved
    - If sentences were amended, `46-COPY.md`'s final state is the approved state, and the
      SUMMARY says how many rounds it took
  </acceptance_criteria>

  <done>
    The full list is approved in one pass, both open decisions are answered and recorded,
    and no copy has been merged into any source file. Plans 46-04, 46-05, 46-06 and 46-07
    are released.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| written copy → any person reading a refusal | a sentence is the one thing the product says out loud; whatever it names becomes public to whoever triggered it |
| `.planning/` → the public internet | this repository is public and every commit is a publication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information disclosure | the sentences in `46-COPY.md` | mitigate | no sentence names another person's data, an order id, a membership code, an internal row value or a database code; the only run-time value any sentence may carry is a PostgREST error `code`, and only on the staff-facing menu-closing refusal, where the pattern already does it (`admin/events/actions.ts:368`) |
| T-46-02 | Information disclosure | `46-COPY.md` committed to a public repo | mitigate | roles never people; no venue under negotiation, no unannounced date, no line-up, no contact — the same rule `.claude/rules/venue-acquisition.md` puts on scouting material |
| T-46-03 | Repudiation | a refusal wording changed later, per-plan | mitigate | the list is the single source; a plan that wants a different sentence amends the list and re-presents it, which is why the approval is one pass |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no package; `git diff --stat package.json package-lock.json` must be empty at phase end, which is the assertion rather than a checkpoint |
</threat_model>

<verification>
- `LC_ALL=C /usr/bin/grep -c 'OBS-05' .planning/REQUIREMENTS.md` → `0`
- `git diff --stat` lists only files under `.planning/` — no `src/`, no `supabase/`, no
  `package.json`
- The checkpoint at Task 3 was answered, and its answer — including the two decisions — is
  recorded verbatim in this plan's SUMMARY
- **Never run `npm run verify`** in this phase (`scripts/rls-baseline.mjs:205-215` reaches
  the Supabase Management API against production)
</verification>

<success_criteria>
- Three requirements exist, `OBS-05` does not, and no traceability row cites an
  out-of-perimeter item
- One approved list holds every sentence plans 46-04 … 46-07 will merge
- The two open decisions are answered and recorded
- No source file was opened
</success_criteria>

<output>
Create `.planning/phases/46-silent-failures-on-the-money-path/46-01-SUMMARY.md` when done.
It must carry: the counted traceability row figure, the owner's verbatim answers to the two
decisions, and the approved list's location.
</output>
