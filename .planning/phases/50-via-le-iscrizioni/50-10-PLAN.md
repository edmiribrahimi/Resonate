---
phase: 50-via-le-iscrizioni
plan: 10
type: execute
wave: 7
depends_on: ["50-09"]
files_modified:
  - .planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md
  - .planning/phases/50-via-le-iscrizioni/50-ESITI.md
autonomous: false
requirements: [REG-01, REG-02, REG-04, REG-06]

must_haves:
  truths:
    - "Un anonimo non puo' creare un account nemmeno con la chiave anonima e curl: 422 signup_disabled (D-50-06, REG-04)"
    - "Con il signup spento, i tre percorsi di servizio creano ancora account: acquisto, guest list (D-50-07), creazione in-app — A1 smentita o confermata sul campo"
    - "Una prenotazione gratuita produce un ordine a zero, N biglietti, una mail e un account leggero con il suo nome — e rigiocata non ne conia altri (REG-06)"
    - "Un biglietto gratuito passa la porta con la radio spenta, come uno pagato"
    - "Ogni procedura e' stata PERCORSA e il suo esito e' scritto: in un repository senza test runner non esiste altra prova"
  artifacts:
    - path: ".planning/phases/50-via-le-iscrizioni/50-ESITI.md"
      provides: "gli esiti delle otto procedure, osservati e non attesi"
      contains: "P-50-"
  key_links:
    - from: "PATCH /v1/projects/{ref}/config/auth"
      to: "POST /auth/v1/signup"
      via: "disable_signup"
      pattern: "signup_disabled"
---

<objective>
Percorrere le otto procedure **sul laboratorio**, e scriverne gli esiti.

Purpose: `50-RESEARCH.md` §5.2 legge dal sorgente di `supabase/auth` che
`DisableSignup` compare in **due soli punti** — `signup.go:115` e
`external.go:343` — e **non** in `admin.go` (`adminUserCreate`) ne' in `mail.go`
(`adminGenerateLink`). Ma la versione di GoTrue dispiegata **non e' nota da
questo repository**: e' l'assunzione `A1`, e se fosse sbagliata **spegnere il
signup spegnerebbe l'acquisto da ospite**, cioe' l'unica cosa che oggi vende.
`P-50-5` e `P-50-6` la provano sul laboratorio **prima** della produzione.

E c'e' una cosa che il prodotto non puo' dimostrare da solo: la pagina cancellata
non dice niente sull'API. *Il middleware e' UX, il confine vero sta altrove* —
e qui l'altrove e' `curl` con la chiave anonima.

Output: `50-ESITI.md`, con cio' che si e' **osservato**, non cio' che ci si
aspettava.

**Nessuna scrittura in produzione.** Tutte e otto le procedure girano sul
laboratorio, e il laboratorio va riattivato dalla Management API se e' in pausa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/meta-gates.md
@.claude/rules/access-gating.md
@.claude/rules/checkin-offline.md
@.planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md
@.planning/phases/50-via-le-iscrizioni/50-VALIDATION.md
@.planning/phases/50-via-le-iscrizioni/50-RESEARCH.md
</context>

<decisions_declared_here>
**1. Le procedure tecniche le percorre Claude, non il proprietario.** Sette
delle otto si esercitano con una sessione, un `curl` e un browser pilotato: sono
verifiche tecniche, e girarle a chi possiede il prodotto significherebbe
chiedergli di fare il lavoro invece di decidere. **L'ottava no**: `P-50-8`
pretende un telefono vero con la radio spenta davvero, e resta un checkpoint.

**2. Si percorre tutto PRIMA di toccare la produzione.** L'ordine non e' una
preferenza: `A1` e' l'unica assunzione di questa fase che, se sbagliata, rompe
l'incasso — e si smentisce in laboratorio, dove non costa niente.

**3. Gli esiti si scrivono come osservazioni.** Il precedente della fase 49 e'
registrato: percorrendo le prove sono emersi **tre fallimenti veri**, trovati
perche' qualcuno guardava cosa succedeva invece di confermare cosa doveva
succedere.
</decisions_declared_here>

<tasks>

<task type="auto">
  <name>Task 1: Spegnere il signup sul laboratorio, e provare che i tre percorsi di servizio sopravvivono</name>
  <files>.planning/phases/50-via-le-iscrizioni/50-ESITI.md</files>
  <read_first>
    - .planning/phases/50-via-le-iscrizioni/50-RESEARCH.md §5 (il campo `disable_signup` nel corpo di `PATCH /v1/projects/{ref}/config/auth`; la tabella dei tre percorsi di creazione, tutti `auth.admin.createUser`; e `A1`)
    - .planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md (`P-50-5`, `P-50-6`)
    - scripts/lab-bootstrap.mjs (righe 40-70) — come si parla alla Management API, e il rifiuto del ref di produzione
    - src/lib/tickets/guest-identity.ts (righe 240-300), src/lib/guest-list/process-entry.ts (righe 230-250), src/app/(admin)/admin/members/actions.ts (`createAccount`) — i tre percorsi che devono sopravvivere
  </read_first>
  <action>
    **Solo sul laboratorio.** Verificare per prima cosa che il ref puntato sia
    `LAB_PROJECT_REF` e **non** quello di produzione.

    1. Riattivare il laboratorio se e' in pausa (succede dopo una settimana senza
       traffico).
    2. Leggere lo stato attuale di `disable_signup` con
       `GET /v1/projects/{ref}/config/auth` e **riportarlo**, poi applicare
       `PATCH` con `disable_signup: true`, poi **rileggere con una seconda
       `GET`** — mai fidarsi della risposta del `PATCH`: una misura presa con lo
       strumento che ha causato l'effetto e' un'eco.
    3. **`P-50-5`**: `POST {LAB_SUPABASE_URL}/auth/v1/signup` con la **chiave
       anonima** e un indirizzo `@lab.invalid` mai usato. Riportare il codice
       HTTP e il corpo **alla lettera**. Atteso: `422` con `signup_disabled`.
       **Se risponde qualcos'altro, non si conclude niente**: si riporta e ci si
       ferma, perche' e' la prova che REG-04 non e' chiuso.
    4. **`P-50-6`** e i due percorsi gemelli, tutti e tre con la chiave di
       servizio e con il signup **spento**:
       - `createAccount` dalla superficie amministrativa: crea, manda l'invito,
         restituisce il codice di membership;
       - un ordine da ospite sulla serata a pagamento del laboratorio, che
         arriva a `resolveGuestIdentity`;
       - una voce di guest list che genera un account.
       Riportare per ciascuno: l'account e' nato **si'/no**, il profilo e'
       comparso **si'/no**, e il tempo che ci ha messo.
       **Se uno dei tre fallisce, `A1` e' smentita**: si riporta, si **riaccende
       il signup sul laboratorio** per non lasciare il banco rotto, e si porta la
       cosa al proprietario prima di toccare la produzione — perche' in quel caso
       D-50-06 spegnerebbe l'unica cosa che vende.
    5. Scrivere nel runbook, come **riga datata**, che il signup del
       **laboratorio** e' stato spento, con l'ora UTC; la riga della produzione
       resta vuota fino al piano 50-11.

    Aprire `50-ESITI.md` e scriverci i due esiti, con ora UTC, comando alla
    lettera e risposta alla lettera. **Nessun indirizzo reale, nessun nome:** il
    file e' su un repository pubblico.
  </action>
  <acceptance_criteria>
    - `50-ESITI.md` esiste e riporta `P-50-5` con il codice HTTP e il corpo **alla lettera**
    - La lettura di `disable_signup` e' riportata **prima e dopo** il `PATCH`, con due `GET` distinte
    - `P-50-6` e i due percorsi gemelli sono riportati ciascuno con esito e tempo
    - Il runbook porta una riga datata per lo spegnimento sul **laboratorio**, e la riga della produzione e' ancora vuota
    - Il file dichiara esplicitamente che nessuna chiamata ha toccato il progetto di produzione
    - Nessun indirizzo di posta reale e nessun nome di persona nel file
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c "signup_disabled" .planning/phases/50-via-le-iscrizioni/50-ESITI.md</automated>
  </verify>
  <done>Con il signup spento sul laboratorio, un anonimo con la chiave anonima riceve un rifiuto e i tre percorsi di servizio continuano a creare account — oppure `A1` e' smentita e lo si sa prima di toccare la produzione.</done>
</task>

<task type="auto">
  <name>Task 2: Le superfici, la cancellazione, i media e l'ordine gratuito, percorsi</name>
  <files>.planning/phases/50-via-le-iscrizioni/50-ESITI.md</files>
  <read_first>
    - .planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md (`P-50-1`, `P-50-3`, `P-50-4`, `P-50-7`)
    - .planning/v1.6-LAB-DESIGN.md — `npm run dev:lab`, e `lab.resonatemotion.com` legato al ramo `lab`
    - .planning/phases/50-via-le-iscrizioni/50-01-SUMMARY.md — quali account e quali serate il laboratorio porta, e a cosa serve ciascuno
    - src/app/(admin)/admin/members/actions.ts (`deleteAccount` e i suoi codici di rifiuto) — per sapere **quale frase** ci si aspetta e riconoscerla quando arriva
  </read_first>
  <action>
    Percorrere quattro procedure **sul laboratorio**, con un browser pilotato e
    letture dal database fatte con la chiave di servizio. Per ciascuna, scrivere
    in `50-ESITI.md` **cio' che si e' osservato**, non cio' che ci si aspettava,
    e dove l'osservazione differisce dall'atteso **riportare la differenza per
    prima**.

    - **`P-50-1` superfici**, da anonimo, da `member` leggero e da organizer:
      `/register` → 404 su tutti e tre; **in piu'**, aprire `/registrati` da un
      profilo di browser che lo aveva gia' seguito, per esercitare la cache del
      redirect permanente; `/` → `/events` su tutti e tre, **compreso chi e'
      loggato**; la voce Account porta a `/login` da anonimo; nessun «Sign up»
      su `/login`, sul dialogo del venue segreto, sul banner del menu drink.
    - **`P-50-3` cancellazione**, da master: cancellare l'account seminato
      **senza tracce** — verificare dal catalogo che l'utente Auth e il profilo
      siano spariti e che la riga di `membership_acts` **resti** con
      `act = 'deleted'` e `subject_id` nullo; poi provare a cancellare
      **l'account con un biglietto** e **quello con una scansione alla porta**, e
      riportare **la frase che arriva**, alla lettera. Se e' generica, e'
      fallita: `meta-gates.md`, zero fallimenti silenziosi.
    - **`P-50-4` media**: dal `member` leggero il controllo di caricamento **non
      si vede**, e forzando l'azione arriva un rifiuto **nominato**; l'organizer
      carica; lo staff con assegnazione carica **sulla sua serata** e **non** su
      un'altra. Annotare, accanto all'esito, che il caricamento dei membri era
      **gia'** morto per il difetto del 2026-08-08: altrimenti l'esito si legge
      come un restringimento nuovo.
    - **`P-50-7` l'ordine gratuito, dal principio**, da anonimo sulla serata
      `free_rsvp` seminata: mettere nome e mail, prenotare, e verificare — **dal
      catalogo, non dalla superficie** — un ordine a totale zero con
      `sumup_checkout_id` **nullo**, N biglietti con lo stesso campo nullo e
      `issued_via = 'free_rsvp'`, `holder_label` **senza nome**, un profilo con
      il `full_name` giusto, **una** riga in `email_deliveries`. Poi: rigiocare
      la **stessa** azione e verificare che l'ordine precedente non riceva
      biglietti nuovi; superare il tetto e leggere il rifiuto; esaurire la
      capienza e leggere il rifiuto. Verificare infine che il **nome non
      sovrascriva** quello di un account gia' esistente, prenotando con lo
      stesso indirizzo e un nome diverso.
      La mail va a un indirizzo raggiungibile del proprietario: **e' l'unica
      prova che il QR e il link firmato funzionino davvero**.
  </action>
  <acceptance_criteria>
    - `50-ESITI.md` riporta i quattro esiti, ciascuno con i suoi passi numerati e cio' che si e' osservato
    - `P-50-1` riporta esplicitamente l'esito del tentativo su `/registrati` da un browser con la cache calda
    - `P-50-3` riporta **la frase alla lettera** dei due rifiuti, e il SUMMARY dichiara che non e' generica
    - `P-50-7` riporta letture **dal catalogo**: `sumup_checkout_id` nullo su ordine e biglietti, `issued_via = 'free_rsvp'`, `holder_label` privo di nome, `profiles.full_name` valorizzato, una sola riga in `email_deliveries`
    - `P-50-7` riporta che la seconda esecuzione **non** ha coniato biglietti nuovi sullo stesso ordine, e che il nome **non** ha sovrascritto quello esistente
    - Ogni esito non conforme e' riportato **per primo**, con cio' che si e' visto
    - Nessun indirizzo di posta reale e nessun nome di persona nel file
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -cE "P-50-1|P-50-3|P-50-4|P-50-7" .planning/phases/50-via-le-iscrizioni/50-ESITI.md</automated>
  </verify>
  <done>Le quattro procedure sono state percorse e i loro esiti scritti, con le letture prese dal catalogo e non dalla superficie che le ha prodotte.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: P-50-8 — la porta, con la radio spenta</name>
  <files>.planning/phases/50-via-le-iscrizioni/50-ESITI.md</files>
  <read_first>
    - .planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md (`P-50-8`)
    - .claude/rules/checkin-offline.md — *rifiutare un ospite valido e' peggio che ammetterne uno doppio*
    - .planning/phases/50-via-le-iscrizioni/50-01-SUMMARY.md — il codice firmato del biglietto seminato e l'account con `door.operate` sulla serata
  </read_first>
  <action>
    Fermarsi e far percorrere i sei passi qui sotto **sul laboratorio**, su un
    telefono vero, riportando cio' che si osserva in `50-ESITI.md`.

    E' l'unica delle otto procedure che nessun comando puo' eseguire al posto di
    una persona: pretende una radio spenta **davvero**, e la coda offline si
    comporta diversamente quando la rete e' assente rispetto a quando e'
    simulata assente. `checkin-offline.md` — *rifiutare un ospite valido e'
    peggio che ammetterne uno doppio* — e' la ragione per cui questa prova non si
    deduce.
  </action>
  <what-built>
    Un biglietto **gratuito**: una riga di `tickets` nata da un ordine a totale
    zero, con `issued_via = 'free_rsvp'`, `sumup_checkout_id` nullo e
    `amount_paid` a zero. La sua firma si costruisce come quella di ogni altro
    biglietto, e **lo scanner, la coda offline e il service worker non sanno la
    differenza** — che e' la proprieta' da verificare, non da dedurre.

    Tutto il resto e' automatizzato: l'emissione, la mail, il link firmato,
    l'account leggero. Questa e' l'unica cosa che nessun comando puo' dire al
    posto di una persona, perche' pretende una radio spenta davvero.
  </what-built>
  <how-to-verify>
    Sul **laboratorio** (`lab.resonatemotion.com`), con l'account di staff che
    ha `door.operate` sulla serata, **su un telefono**:

    1. Prenotare un posto sulla serata gratuita e ricevere la mail con il QR.
    2. Aprire lo scanner della porta sul telefono, con la rete accesa, e
       lasciarlo aperto il tempo che serve a precaricarsi.
    3. **Mettere il telefono in modalita' aereo.** Non spegnere solo il wi-fi:
       anche la rete dati.
    4. Scansionare il QR del biglietto **gratuito**. **Osservare:** viene
       accettato, con lo stesso esito visivo di un biglietto pagato, e finisce
       nella coda offline.
    5. Scansionare **due volte** lo stesso codice, sempre in modalita' aereo.
       **Osservare:** cosa dice la seconda volta.
    6. Riattivare la rete. **Osservare:** la coda si sincronizza, e nel database
       compare **una** convalida, non due.

    Riportare i sei esiti, con quello che si e' **visto** e non con quello che ci
    si aspettava. Se il quarto passo rifiuta, **e' un ospite valido respinto
    davanti a una fila**, ed e' il verso grave dell'errore: dirlo per primo.
  </how-to-verify>
  <acceptance_criteria>
    - I sei passi sono stati eseguiti e riportati uno per uno in `50-ESITI.md`
    - Al passo 4 il biglietto gratuito e' **accettato** in modalita' aereo, e l'esito visivo e' quello di un biglietto pagato
    - Al passo 6 il numero di convalide registrate per quel biglietto e' esattamente **1**, letto dal database
    - Il telefono era in modalita' aereo **vera** — non solo wi-fi spento — e il file lo dichiara
  </acceptance_criteria>
  <resume-signal>Scrivere «approvato» oppure descrivere quale dei sei passi non ha dato l'esito atteso</resume-signal>
  <verify>
    <automated>/usr/bin/grep -c "P-50-8" .planning/phases/50-via-le-iscrizioni/50-ESITI.md</automated>
  </verify>
  <done>La porta e' stata esercitata, non dedotta: un biglietto gratuito entra con la radio spenta come uno pagato, e una doppia lettura resta una convalida sola.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chiave anonima → `POST /auth/v1/signup` | e' il confine che la pagina cancellata **non** chiude |
| telefono senza rete → coda offline → database | fra la lettura e la sincronizzazione esiste una finestra in cui la verita' sta solo sul telefono |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-47 | Spoofing | iscrizione anonima sopravvissuta alla cancellazione della pagina | mitigate | `disable_signup` sul servizio Auth, **verificato con `curl` e la chiave anonima** (`P-50-5`), non dedotto dall'assenza della pagina |
| T-50-48 | Denial of service | `disable_signup` che spegne anche `admin.createUser` e quindi l'acquisto da ospite | mitigate | `A1` provata sul **laboratorio** prima della produzione, su tutti e tre i percorsi di servizio; se smentita, il signup si riaccende e la cosa torna al proprietario |
| T-50-49 | Denial of service (sul cliente) | un ospite valido respinto alla porta perche' il suo biglietto e' gratuito | mitigate | `P-50-8` con la radio spenta **davvero**; il verso grave dell'errore e' dichiarato nel passo 4 |
| T-50-50 | Tampering | un biglietto gratuito convalidato due volte da una coda offline | mitigate | doppia lettura in modalita' aereo e conteggio delle convalide **dal database** dopo la sincronizzazione |
| T-50-51 | Information disclosure | un indirizzo reale o un nome finiti in `50-ESITI.md` | mitigate | indirizzi `@lab.invalid` salvo l'unica mail vera al proprietario, che **non si trascrive**; ruoli, mai persone |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `50-ESITI.md` copre tutte e otto le procedure (`P-50-2` riportata dal piano 50-02)
- `P-50-5` riporta `422 signup_disabled` alla lettera, oppure la fase si ferma
- `P-50-6` e i due percorsi gemelli riportano account creati con il signup spento
- `P-50-8` riporta i sei passi, con la modalita' aereo dichiarata
- Nessuna chiamata di questo piano ha toccato il progetto di produzione
</verification>

<success_criteria>
Le otto procedure sono state percorse sul laboratorio e i loro esiti scritti.
`A1` e' confermata o smentita **sul campo**, e nel secondo caso la produzione non
viene toccata finche' il proprietario non ha deciso.
</success_criteria>

<output>
Create `.planning/phases/50-via-le-iscrizioni/50-10-SUMMARY.md` when done
</output>
