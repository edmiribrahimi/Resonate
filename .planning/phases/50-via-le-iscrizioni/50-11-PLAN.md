---
phase: 50-via-le-iscrizioni
plan: 11
type: execute
wave: 8
depends_on: ["50-10"]
files_modified:
  - .planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md
  - .planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md
autonomous: false
requirements: [REG-01, REG-02, REG-03, REG-04, REG-06]

must_haves:
  truths:
    - "Esiste una TERZA autorizzazione datata, con il perimetro scritto PRIMA di chiederla e i conteggi veri dentro (D-50-02, D-50-21)"
    - "Il codice raggiunge la produzione PRIMA della migration, e la finestra non rompe l'incasso (D-50-24)"
    - "Le due migration sono applicate alla produzione e rilette dal catalogo, mai dalla risposta del POST"
    - "Il signup pubblico e' spento anche sul progetto di produzione (D-50-06)"
    - "Gli account pending e rejected sono cancellati PER CHIAVE PRIMARIA, e chi ha biglietti o tracce non viene forzato ma riportato al proprietario (D-50-02, D-50-16)"
  artifacts:
    - path: ".planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md"
      provides: "la terza autorizzazione: perimetro, condizioni, registro d'uso compilato mentre si spende"
      contains: "perimetro"
  key_links:
    - from: "deploy Vercel"
      to: "POST /v1/projects/{ref}/database/migrations"
      via: "il verso invertito: codice prima, migration dopo"
      pattern: "codice prima"
---

<objective>
Portare la fase in produzione, nell'ordine che non rompe l'incasso, sotto
un'autorizzazione che si chiede **dopo** aver scritto cosa si tocchera'.

Purpose: `49-AUTHORISATION.md` chiude con *«da qui in poi questo documento non
autorizza piu' niente»*. Le due autorizzazioni della fase 49 sono **esaurite**
(2026-09-06 e 2026-09-08). Serve la **terza**, e `ai-engineering.md` dice come:
si consuma una volta, copre esattamente cio' che e' stato descritto quando e'
stata chiesta, e chi la riceve dichiara **quando l'ha usata e quando l'ha
esaurita**.

Il verso e' invertito rispetto alla regola del repository, e la ragione e'
misurata (§8.3): applicando la migration per prima, il codice in produzione
farebbe ancora `select("role, status")` e `update({status:'approved'})` e
solleverebbe `42703` sul webhook dei pagamenti, sulla pagina membri, su
`purchaseTicket` e sull'RSVP — **il percorso del denaro si romperebbe**.
Applicando il codice per primo, **niente si rompe**: `NOT NULL DEFAULT
'approved'` continua a soddisfarsi da solo sugli `INSERT`, e `has_capability`
continua a leggere una colonna che il codice ha smesso di guardare.

Output: due migration in produzione, il signup spento, gli account cancellati, e
un documento che dice cosa e' stato speso e quando.

**Il listing pubblico della prima serata in vendita e' atteso per lunedi' 28
settembre 2026.** La fase si chiude prima.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/meta-gates.md
@.claude/rules/ai-engineering.md
@.claude/rules/access-gating.md
@.planning/phases/49-comprare-senza-account/49-AUTHORISATION.md
@.planning/phases/50-via-le-iscrizioni/50-MEASURES.md
@.planning/phases/50-via-le-iscrizioni/50-ESITI.md
@.planning/phases/50-via-le-iscrizioni/50-RESEARCH.md
</context>

<decisions_declared_here>
**1. Il perimetro si scrive PRIMA di chiedere.** Una domanda del tipo *«mi
autorizzi ad applicare la fase?»* non e' un perimetro: e' una delega. Il
documento nomina le due migration per file, dice **quante righe si cancellano e
da quali tabelle**, e riporta le sei misure gia' prese dal piano 50-01.

**2. Il sottoinsieme non cancellabile NON si forza.** D-50-16, ultima riga: per
i `pending`/`rejected` vale la stessa regola degli altri — chi ha biglietti o
tracce di lavoro **non si cancella** e viene **riportato al proprietario**. Non
si cambia una cascata, non si cancellano righe collegate per fare spazio, non si
«pulisce» nulla per far riuscire una `DELETE`.

**3. `disable_signup` sulla produzione va DOPO le migration.** Il segnale che
ci si aspetta da `P-50-5` e' gia' stato visto sul laboratorio; sulla produzione
l'interruttore si tocca quando il resto e' a posto, perche' e' l'unico passo di
questo piano **reversibile con un secondo `PATCH`** ed e' quindi quello che
costa meno tenere per ultimo.
</decisions_declared_here>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: La terza autorizzazione datata</name>
  <read_first>
    - .planning/phases/49-comprare-senza-account/49-AUTHORISATION.md (intero) — **la forma**: la domanda posta alla lettera, il perimetro tabellato, le cinque condizioni, la finestra misurata, il registro d'uso compilato **mentre** si spende, e la riga finale che dichiara l'esaurimento
    - .planning/phases/50-via-le-iscrizioni/50-MEASURES.md — i conteggi veri, presi in sola lettura dal piano 50-01
    - .planning/phases/50-via-le-iscrizioni/50-RESEARCH.md §4.1 (le cascate), §4.3 (le sei query), §8.2 (cosa l'autorizzazione deve dichiarare)
  </read_first>
  <files>.planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md</files>
  <action>
    Scrivere `50-AUTHORISATION.md` **prima** di porre la domanda — perimetro,
    conteggi, condizioni, registro d'uso vuoto — poi fermarsi e porre al
    proprietario la domanda riportata qui sotto, con le tre strade nominate.

    Un'autorizzazione a scrivere in produzione **e' un atto, non un permesso**:
    si consuma una volta, copre esattamente cio' che e' stato descritto quando e'
    stata chiesta, e non si estende da se'. Le due della fase 49 sono
    **esaurite**, e una riga «applicata» trovata li' dentro e' una ricevuta, non
    un permesso.
  </action>
  <decision>
    Applicare alla **produzione** le due migration di questa fase, spegnere il
    signup pubblico sul progetto di produzione, e **cancellare** gli account
    oggi in `pending` o `rejected` — utente Auth e profilo.
  </decision>
  <context>
    Le due autorizzazioni della fase 49 sono **esaurite**. Questa scrittura e'
    **irreversibile** e questo repository **non ha PITR** — decisione del
    proprietario, registrata, non riaperta qui. Cambia cosa significa sbagliare,
    e per questo il perimetro conta.

    Prima di porre la domanda, **scrivere `50-AUTHORISATION.md`** con:

    - **Il perimetro, tabellato**, una riga per migration, con il nome del file e
      cosa tocca: `20260921120000_drop_status_and_referral.sql` (colonne `status`,
      `referred_by`, `approved_via`; `requires_approved`; quattro funzioni
      ridefinite; quattro policy sostituite; `get_user_status` droppata;
      `membership.active` cancellata dal catalogo; l'ottavo atto nel registro) e
      `20260921120100_free_order.sql` (checkout nullabile con indice unico
      parziale, `buyer_name`, il livello a zero per le serate gratuite —
      **l'unica istruzione di scrittura di dati delle due**).
    - **I conteggi**, dal piano 50-01: quanti profili per stato, quanti non
      approvati hanno biglietti, rsvp, media, voci di guest list, quanti hanno
      `referred_by`, e **per ciascuno dei sette vincoli bloccanti** quante righe
      li riferiscono. Piu' il conteggio delle righe di `rsvps` e quello per
      categoria di `email_deliveries`.
    - **Quante righe si cancellano e da quali tabelle**, derivato da quei
      conteggi — e **quante NON si cancellano**, con la ragione, se esiste un
      sottoinsieme bloccato.
    - **Le cinque condizioni** gia' dichiarate nella prima autorizzazione:
      istantanea prima, derivata da `pg_constraint` e **non ricordata**;
      rilettura **dal catalogo** e mai dalla risposta del `POST`; endpoint
      `POST /v1/projects/{ref}/database/migrations` e **mai** `/database/query`;
      **ci si ferma alla prima che fallisce**; **nessuna cascata nuova**.
    - **Le due proprieta' misurate dello strumento**, o qualcuno le riscoprira':
      l'endpoint conia la versione **con l'istante dell'applicazione** e non con
      il nome del file — successo sei volte su sei — e `CREATE OR REPLACE` su una
      firma identica **conserva l'ACL**.
    - **Il verso invertito del deploy**, con la ragione misurata, e il fatto che
      e' gia' scritto dentro la migration stessa.
    - **Un registro d'uso vuoto**, da compilare **mentre** si spende.

    Porre poi la domanda **alla lettera**, con le alternative reali sul tavolo —
    per esempio: tutto in un atto solo; oppure solo le due migration ora e le
    cancellazioni in un atto separato; oppure niente, e le migration restano al
    proprietario. **Le alternative vanno nominate**: un'autorizzazione con una
    sola strada e' una firma, non una decisione.
  </context>
  <options>
    <option id="tutto">
      <name>Tutto in un atto solo</name>
      <pros>La fase si chiude oggi, prima del listing di lunedi' 28. Una finestra sola.</pros>
      <cons>La cancellazione e' la parte irreversibile, e viene spesa nello stesso respiro delle migration.</cons>
    </option>
    <option id="migrazioni-prima">
      <name>Le due migration ora, le cancellazioni in un atto separato</name>
      <pros>Separa cio' che si puo' correggere in avanti da cio' che non torna. Il prodotto funziona comunque: senza la colonna, `pending` e `rejected` non esistono piu' come stati.</pros>
      <cons>Restano righe di profilo che non rappresentano piu' niente, e serve una seconda autorizzazione.</cons>
    </option>
    <option id="niente">
      <name>Nessuna scrittura: le migration restano al proprietario</name>
      <pros>Nessun rischio assunto per conto suo.</pros>
      <cons>La fase non si chiude, e il listing di lunedi' 28 arriva su un prodotto che ha ancora la pagina di iscrizione.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `50-AUTHORISATION.md` esiste **prima** che la domanda venga posta, con perimetro, conteggi, condizioni e registro d'uso vuoto
    - Il documento riporta i conteggi **presi**, con la data in cui sono stati presi, e non stime
    - Se esiste un sottoinsieme non cancellabile, il documento lo dichiara con il numero e la causa, e la domanda lo nomina
    - La domanda e' riportata **alla lettera**, con almeno tre strade nominate
    - Il documento dichiara `status: IN CORSO` e non `ESAURITA`
  </acceptance_criteria>
  <resume-signal>Scegliere: `tutto`, `migrazioni-prima`, oppure `niente` — o descrivere un perimetro diverso</resume-signal>
  <verify>
    <automated>test -f .planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md && /usr/bin/grep -c "perimetro" .planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md</automated>
  </verify>
  <done>Esiste un atto datato che dice esattamente cosa verra' toccato, con numeri veri, e il proprietario ha scelto fra strade che gli sono state nominate.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Il codice in produzione, PRIMA della migration</name>
  <files>.planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/50-via-le-iscrizioni/50-RESEARCH.md §8.3 — la tabella delle due finestre, e perche' questa volta il verso e' invertito
    - .planning/STATE.md (la sezione sul deploy della fase 49, 2026-09-07) — i gate eseguiti prima del push: build con artefatto fresco, `verify:persona`, controllo F, e il censimento dei file toccati
    - supabase/migrations/20260921120000_drop_status_and_referral.sql (il docblock) — il verso invertito e' dichiarato li' dentro, ed e' quello che va rispettato
  </read_first>
  <action>
    **I gate prima del push**, nell'ordine, e se uno e' rosso non si spinge:
    `npm run build` con artefatto fresco, `npm run verify`,
    `npm run verify:routes`, `npm run verify:conversion`,
    `npm run verify:capabilities`, `npm run verify:no-header-identity`.
    **`npm run verify:persona` NON si lancia qui**: si lancia nel piano 50-12,
    **dopo** la cancellazione delle superfici e la correzione dei moduli — il
    build non conosce i glob della persona, e lanciarlo prima misura un albero
    che non esiste piu'.

    **Il censimento dei file toccati**, come nella fase 49: ogni file nel push
    deve stare sotto `src/`, `.planning/`, `supabase/`, `scripts/`, `.claude/`,
    `public/` o `next.config.ts`. **Un file fuori da quell'insieme ferma il
    push**, e si riporta quale.

    **Controllo F prima di spingere**: `docs/` e `.firecrawl/` restano ignorati e
    **nessun file al loro interno risulta tracciato**. Il repository e'
    **pubblico**, e un commit e' una pubblicazione irreversibile.

    Poi spingere su `origin/main` e **attendere l'esito del deploy Vercel**.
    Verificare, dall'esterno e da anonimo:
    - `/events` risponde **200**;
    - `/register` risponde **404**;
    - `/` reindirizza a `/events`;
    - la pagina di una serata a pagamento si apre e mostra il modulo d'acquisto
      con i suoi due campi.

    **La migration NON si applica in questo task.** Se il deploy fallisce o uno
    dei quattro controlli non risponde come sopra, **ci si ferma qui**: con il
    codice non dispiegato, applicare la migration romperebbe il percorso del
    denaro, che e' esattamente cio' che il verso invertito esiste per evitare.

    Registrare nel runbook: l'intervallo di commit spinti, l'ora UTC del deploy,
    il suo stato, e i quattro esiti.
  </action>
  <acceptance_criteria>
    - Tutti i gate elencati sono verdi **prima** del push, e il SUMMARY li riporta uno per uno
    - `npm run verify:persona` **non** compare fra i comandi eseguiti in questo task
    - Il censimento dei file toccati e' riportato, e nessun file cade fuori dall'insieme dichiarato
    - Il controllo F e' verde: `docs/` e `.firecrawl/` ignorati e non tracciati
    - Il deploy Vercel e' in stato `success`, con l'ora UTC riportata
    - Da anonimo: `/events` → 200, `/register` → 404, `/` → `/events`, la pagina di una serata a pagamento si apre
    - Il SUMMARY dichiara che **nessuna migration e' stata applicata in questo task**
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run verify && npm run verify:routes && npm run verify:conversion && npm run verify:capabilities && npm run verify:no-header-identity</automated>
  </verify>
  <done>Il codice che non nomina piu' lo stato e' in produzione, e la colonna e' ancora li' senza che niente si rompa: e' la finestra che il verso invertito produce apposta.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Le due migration, il signup, e le cancellazioni</name>
  <files>.planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md, .planning/phases/50-via-le-iscrizioni/50-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md — il perimetro concesso: **non un byte oltre**
    - .planning/phases/49-comprare-senza-account/49-AUTHORISATION.md — le note d'uso dei piani 49-01, 49-03 e 49-05: la forma di un registro compilato mentre si spende, l'istantanea ri-derivata, e la deriva della versione
    - .planning/phases/50-via-le-iscrizioni/50-RESEARCH.md §4.1 (le cascate) e §4.3 (le sei query)
    - scripts/seed-lab-door.mjs (il docblock, le quattro regole della rimozione) — **sono le regole di questo task**, scritte dopo l'incidente delle 63 righe
  </read_first>
  <action>
    **Si esegue solo cio' che l'autorizzazione concede**, nell'ordine, e **ci si
    ferma alla prima cosa che fallisce**.

    **(a) L'istantanea, prima.** Enumerare leggendo `pg_constraint` — **non
    ricordando** — ogni tabella raggiungibile per cascata dalle righe che si
    toccheranno, e contarle. Il gate e' scritto dopo l'incidente delle 63 righe:
    *un'istantanea prima copre cio' che si tocca, non cio' che si crea*. Il
    totale delle righe e' la misura che conta, e va ripreso **dopo**.

    **(b) Le due migration**, in ordine di timestamp, con
    `POST /v1/projects/{ref}/database/migrations` — **mai** `/database/query`,
    cosi' la storia delle migration resta veritiera. Dopo **ciascuna**,
    rileggere **dal catalogo** gli stessi oggetti gia' verificati sul laboratorio
    dai piani 50-02 e 50-03: `information_schema.columns`, `pg_policies`,
    `pg_proc` (con `proacl` **prima e dopo**), `pg_constraint`, `pg_indexes`, e
    le tre query di §1.5(a) a **zero**.
    Registrare, per ciascuna: l'ora UTC, **la versione coniata dall'endpoint** —
    che non e' il timestamp del file, ed e' una deriva misurata sei volte su sei
    — e l'esito della rilettura. **Compilare il registro mentre si spende, non
    dopo.**

    **(c) Le cancellazioni, per chiave primaria.** Quattro regole, e sono codice
    altrove in questo repository:
    1. **La lista degli id si cattura PRIMA**, con una `select` che li porta su
       disco, e la cancellazione legge **quella lista**;
    2. si cancella **per chiave primaria**, mai per stato, mai con un `where` che
       **descrive** invece di nominare. Il verso dell'errore e' il punto: un
       selettore largo cancella **piu'** del dovuto; una cancellazione per id che
       sbaglia **non trova nulla**;
    3. **prima di ogni id**, ricontare le otto famiglie di tracce di §4.1 su
       quell'id: se una non e' zero, **quell'id si salta** e finisce nella lista
       dei non cancellabili, che va nel documento e al proprietario;
    4. la conferma si chiede a una **fonte diversa** da quella con cui si e'
       cancellato — si cancella via Management API e si riconta via PostgREST, o
       viceversa. Una misura presa con lo strumento che ha causato l'effetto e'
       un'eco.
    Dopo: verificare che le righe di `membership_acts` dei soggetti cancellati
    **esistano ancora**, con `subject_id` nullo — e' la proprieta' che rende la
    cancellazione tracciata invece che silenziosa.

    **(d) Il signup sulla produzione.** `GET /v1/projects/{ref}/config/auth` per
    leggere lo stato, `PATCH` con `disable_signup: true`, **seconda `GET`** per
    rileggere. Poi `POST /auth/v1/signup` sulla produzione con la **chiave
    anonima** e un indirizzo che non esiste: atteso **422 `signup_disabled`**.
    Registrare nel runbook la **riga datata** che D-50-06 chiede, con l'ora UTC.
    E' l'unico passo di questo task reversibile con un secondo `PATCH`, ed e'
    per questo che sta per ultimo.

    **(e) Il ri-conteggio.** Riprendere il totale di (a) e riportare la
    differenza, spiegata riga per riga. Poi chiudere `50-AUTHORISATION.md` con
    lo stato **ESAURITA**, l'ora, e la riga che dice che da li' in poi quel
    documento non autorizza piu' niente.

    **Fuori perimetro, esplicitamente:** ogni altra scrittura — righe seminate,
    spunte, sessioni coniate, altre rimozioni, cambi di cascata, `GRANT` o
    `REVOKE` non contenuti nelle due migration. Per quelle serve un atto nuovo,
    con la sua data.
  </action>
  <acceptance_criteria>
    - L'istantanea e' **ri-derivata** da `pg_constraint` prima e ripresa dopo, e la differenza e' spiegata riga per riga
    - Le due migration risultano applicate con l'endpoint delle migration; **nessuna chiamata a `/database/query` per applicarle**, e il SUMMARY lo dichiara
    - Per ciascuna migration: ora UTC, versione coniata dall'endpoint (dichiarata diversa dal nome del file), e rilettura **dal catalogo** riportata
    - Le tre query di §1.5(a) restituiscono **0** sulla produzione, incollate nel SUMMARY
    - `proacl` di `reserve_ticket_order` e delle funzioni ridefinite e' **identico** prima e dopo
    - Le cancellazioni sono avvenute **per chiave primaria** da una lista catturata prima, e il numero cancellato coincide con quello dichiarato nell'autorizzazione
    - La lista dei **non cancellabili**, se non vuota, e' nel documento con la causa per ciascuno, e **nessuno di essi e' stato forzato**
    - Le righe di `membership_acts` dei soggetti cancellati esistono ancora con `subject_id` nullo
    - `POST /auth/v1/signup` sulla produzione risponde **422 `signup_disabled`**, riportato alla lettera
    - `50-AUTHORISATION.md` e' chiuso con stato `ESAURITA`, l'ora, e il registro d'uso compilato
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c "ESAURITA" .planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md</automated>
  </verify>
  <done>La produzione ha lo schema della fase, il signup spento e nessun account in uno stato che non esiste piu'. Cio' che non si poteva cancellare e' scritto, non forzato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Management API → database di produzione | una chiamata sbagliata non torna indietro, e questo repository non ha PITR |
| finestra fra deploy e migration | per il tempo che dura, codice e schema descrivono due mondi |
| chiave anonima → `POST /auth/v1/signup` in produzione | e' il confine vero, e la pagina cancellata non lo chiude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-52 | Denial of service | `42703` sul percorso del denaro per aver applicato la migration prima del codice | mitigate | **verso invertito**, dichiarato dentro la migration e imposto dall'ordine dei task: il task 3 non parte se il task 2 non ha visto il deploy `success` e i quattro controlli |
| T-50-53 | Tampering | una cancellazione che prende piu' righe del dovuto | mitigate | lista di id catturata **prima**, cancellazione **per chiave primaria**, mai per stato; il verso dell'errore e' scelto apposta — un id sbagliato non trova nulla |
| T-50-54 | Repudiation | un account cancellato senza traccia | mitigate | `membership_acts` e' append-only con `subject_id ON DELETE SET NULL`, e l'esistenza delle righe si verifica **dopo** la cancellazione |
| T-50-55 | Tampering | un profilo bloccato «sbloccato» cancellando le sue righe collegate | mitigate | **vietato esplicitamente**: chi ha tracce si salta e si riporta al proprietario; nessun cambio di cascata, nessuna pulizia preparatoria |
| T-50-56 | Spoofing | iscrizione anonima ancora possibile in produzione | mitigate | `disable_signup` applicato e **riletto con una seconda `GET`**, poi esercitato con `curl` e la chiave anonima |
| T-50-57 | Information disclosure | materiale di produzione pubblicato con il push | mitigate | controllo F **prima** del push: `docs/` e `.firecrawl/` ignorati **e** non tracciati; censimento dei file toccati, e un file fuori insieme ferma il push |
| T-50-58 | Repudiation | un'autorizzazione usata oltre il suo perimetro | mitigate | il perimetro e' scritto **prima** della domanda; il registro si compila **mentre** si spende; il documento si chiude `ESAURITA` con la riga che nega ogni uso successivo |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | nessun pacchetto installato |
</threat_model>

<verification>
- I gate prima del push tutti verdi, `verify:persona` **escluso** e rimandato al piano 50-12
- Deploy Vercel `success`, e i quattro controlli da anonimo riportati
- Due migration applicate con l'endpoint delle migration, rilette dal catalogo
- Le tre query di §1.5(a) a zero **sulla produzione**
- `422 signup_disabled` sulla produzione
- `50-AUTHORISATION.md` chiuso `ESAURITA` con il registro compilato
</verification>

<success_criteria>
In produzione: nessun profilo ha uno stato, nessuno puo' iscriversi da solo
nemmeno con `curl`, una serata gratuita puo' emettere biglietti a totale zero, e
il percorso del denaro non ha avuto un solo istante di `42703`.
</success_criteria>

<output>
Create `.planning/phases/50-via-le-iscrizioni/50-11-SUMMARY.md` when done
</output>
