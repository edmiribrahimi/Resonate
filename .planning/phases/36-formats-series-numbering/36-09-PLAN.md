---
phase: 36-formats-series-numbering
plan: 09
type: execute
wave: 7
depends_on: ["36-08"]
files_modified:
  - src/app/(admin)/admin/(work)/formats/page.tsx
  - src/app/(admin)/admin/formats/FormatsCatalogue.tsx
  - src/app/(admin)/admin/formats/RetireFormatDialog.tsx
  - src/lib/routes/staff-tabs.ts
  - .planning/phases/36-formats-series-numbering/deferred-items.md
autonomous: true
requirements: [FMT-05]

must_haves:
  truths:
    - "A person holding catalogue.manage can see every format and series, including the retired ones"
    - "A person without it is sent away by the page as well as by the middleware"
    - "Retiring is confirmed, says what it does and does not do, and opens on Cancel"
    - "No format can be deleted from this surface"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/formats/page.tsx"
      provides: "The catalogue route: guard, query, and the two sections"
      contains: "CAP.CATALOGUE_MANAGE"
    - path: "src/app/(admin)/admin/formats/RetireFormatDialog.tsx"
      provides: "The only destructive confirmation in this phase"
      contains: "Retire"
  key_links:
    - from: "src/app/(admin)/admin/(work)/formats/page.tsx"
      to: "src/lib/routes/capability-routes.ts"
      via: "the page guard asks the same key the map binds to this address"
      pattern: "CAP.CATALOGUE_MANAGE"
---

<objective>
Build the catalogue surface: the one place a person can see what formats and
series exist, change a name or a colour, list a format, and retire one.

Purpose: FMT-05 says labels and colours come from data so that a colour changes
without a deploy and a retired sigla cannot appear. This surface is where the data
changes. It is also the first page in this repository with a page-level create
affordance, which the pattern map recorded as a gap rather than pointing at an
approximate analog.

Output: one route file, one client shell, one destructive confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-formats-series-numbering/36-UI-SPEC.md
@.planning/phases/36-formats-series-numbering/36-PATTERNS.md
@.planning/phases/36-formats-series-numbering/36-08-SUMMARY.md

<interfaces>
The guard every work page uses, `(work)/venues/page.tsx:52-58`:

  const { capabilities } = await getAccessContext();
  if (!capabilities.has(CAP.ORGANIZER_ACCESS)) {
    redirect("/dashboard");
  }

Never a role list. **The key asked here must be the key the map binds to this
address** — for `/admin/formats` that is `CAP.CATALOGUE_MANAGE` (plan 36-06), not
`CAP.ORGANIZER_ACCESS`, or the middleware and the page can disagree.

The row surface already in the tree, `(work)/venues/page.tsx:81`:

  rounded-xl border border-card-border bg-card p-3 hover:bg-card/80 transition-colors

The destructive treatment, the only `red-500/10` + `red-400` pair in the tree
(`CreateVenueModal.tsx:169-171`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The route file</name>
  <files>src/app/(admin)/admin/(work)/formats/page.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/venues/page.tsx` — the whole file: imports, guard, query, layout, empty state, row markup
    - `src/app/(admin)/admin/(work)/artists/page.tsx:8-44` — the docblock form worth copying, where a surface explains its own merge reasoning
    - `.planning/phases/36-formats-series-numbering/36-UI-SPEC.md` § *S5 — The catalogue surface*
    - `src/lib/routes/capability-routes.ts` — the entry plan 36-06 wrote, to confirm which key opens this address
  </read_first>
  <action>
    Create the route file under `(work)` and nothing else there: R-WORK-ROUTES keeps
    route files alone in that segment, and the client shell and the dialog live one
    level out at `src/app/(admin)/admin/formats/`, imported with
    `@/app/(admin)/admin/formats/…`.

    Guard with `CAP.CATALOGUE_MANAGE` and redirect to `/dashboard` otherwise. Copy
    the comment from the sibling pages explaining that `getAccessContext()` is
    resolved once per request by the layout and `cache()`-scoped, so the second ask
    costs no round trip, and that the page keeps its own guard because the
    middleware and the page give the same verdict only by reading the same entry
    (D-34-09). Add one sentence of your own: this page asks `catalogue.manage`
    rather than `organizer.access`, unlike its two sibling catalogue surfaces,
    because `catalogue.manage` is `requires_approved = true` and a pending organizer
    is better refused at the address than shown a page where every button refuses
    them.

    Query the catalogue with the cookie client. Read the formats with their columns
    and their series, ordered by `sort_order` then `name`, **without** filtering on
    `retired_at` or `listed` — this surface shows everything, which is what makes it
    the place a retirement can be undone. A caller reaching this query holds
    `catalogue.manage`, so the `formats_select_catalogue_manage` and
    `party_series_select_catalogue_manage` arms admit the rows; a caller who does
    not hold it never reaches the query because the guard sent them away, and would
    see only the listed formats if they somehow did. Both statements are true at
    once and both are worth a line in a comment.

    Render two sections. **Active** — one row per format with `retired_at` null:
    the `FormatMarker` from plan 36-06 for the swatch and the name, then the code in
    the Body role with `text-muted` and `tabular-nums`, then the number of series it
    holds. A count is fine here and only here: this is an internal surface behind a
    capability, and rule 1 of the UI spec governs public surfaces. Say so in a
    comment, because the rule is otherwise easy to over-apply and this count is the
    only thing that makes the row informative. Beneath each format, its series, each
    with its public name and its code. **Retired** — its own section below the
    active list, so the working list stays the list you can work with. A retired row
    keeps its swatch at `opacity .5` **and** carries the literal word `Retired` as
    text, never opacity alone.

    Empty state: `No formats yet` with `A night cannot be saved without a format.
    Add the first one.`

    Everything interactive — the `Add format` button, the modals, the listing
    toggle, the retire trigger — is delegated to the client shell in task 2. This
    file fetches and lays out; it holds no state.
  </action>
  <verify>
    <automated>npm run build && npm run verify:routes</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CAP.CATALOGUE_MANAGE" src/app/\(admin\)/admin/\(work\)/formats/page.tsx` returns at least 1, and `grep -c "CAP.ORGANIZER_ACCESS" <file>` returns 0
    - `grep -Ec "role ===|role\.includes" <file>` returns 0 — no role list anywhere
    - The query filters neither `retired_at` nor `listed`
    - `grep -c "Retired" <file>` returns at least 1 — the word is rendered as text, not implied by opacity
    - `grep -c '"use client"' <file>` returns 0
    - `npm run verify:routes` exits 0 — the page under `(admin)` has its pattern in the map
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The catalogue is visible to exactly the people who may change it, retired rows included, with the count that only an internal surface may carry.</done>
</task>

<task type="auto">
  <name>Task 2: The client shell</name>
  <files>src/app/(admin)/admin/formats/FormatsCatalogue.tsx</files>
  <read_first>
    - `src/components/venues/CreateVenueModal.tsx:37-50`, `:289-306` — the open/close plumbing and the button pair
    - `.planning/phases/36-formats-series-numbering/36-UI-SPEC.md` § *S5* and § *Copywriting Contract*
    - `.planning/phases/36-formats-series-numbering/36-PATTERNS.md` § *No Analog Found*, the `Add format` row — no page in the tree has a page-level create affordance, so its shape is designed here rather than copied
    - the exports of `src/app/(admin)/admin/formats/actions.ts` from plan 36-07
  </read_first>
  <action>
    A client component receiving the already-fetched rows as props and owning every
    piece of interaction on this surface: the `Add format` primary action, the
    `Add series` secondary action per format, the edit triggers, the listing toggle
    and the retire trigger. It mounts `CreateFormatModal`, `CreateSeriesModal` and
    `RetireFormatDialog` from plans 36-08 and task 3.

    The `Add format` button has no precedent in this repository — neither
    `(work)/venues/page.tsx` nor `(work)/artists/page.tsx` has any create affordance
    at all, and creation for those two happens only through modals mounted inside
    `EventForm`. Build it from the ingredients that do exist: the primary button
    styling at `CreateVenueModal.tsx:291-297` — the `rounded-full bg-accent` fill —
    and the `<dialog>` open/close plumbing at `:37-50`. Accent is correct here: the
    spec reserves it for, among other things, the primary button on this surface,
    and it is barred only from the format identification channel.

    The listing control is a distinct affordance with its own words, not a checkbox
    labelled `listed`. It calls `setFormatListed` and it says what it does: turning
    it on is what puts a chip on `/events` for every visitor. Turning it off removes
    the chip and leaves everything else alone. A format created here arrives
    unlisted, which is D-36-17, and this control is the deliberate second act.

    Refusals arrive as `CatalogueRefusal` values and are rendered as their own
    sentences, in the error box treatment, never as a shared failure string.

    Nothing on this surface deletes. There is no delete button, no delete action to
    call, and the foreign keys would refuse it anyway — the absence is structural and
    the comment should say so rather than leaving a reader to wonder whether it was
    forgotten.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"use client"' <file>` returns 1
    - `grep -Ec "\.from\(|createClient|getServiceClient" <file>` returns 0 — the shell fetches nothing; it receives props
    - `grep -c "setFormatListed" <file>` returns at least 1 and the control's label names the effect on `/events` rather than the column
    - `grep -Eci "delete" <file>` returns 0 outside a comment explaining the absence
    - `grep -Ec "Something went wrong|An error occurred" <file>` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Every catalogue act has a control with words that say what it does, and listing a format is visibly a separate decision from creating one.</done>
</task>

<task type="auto">
  <name>Task 3: RetireFormatDialog</name>
  <files>src/app/(admin)/admin/formats/RetireFormatDialog.tsx</files>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-UI-SPEC.md` § *S5*, *Retiring*, and § *Copywriting Contract*, the destructive confirmation row
    - `src/components/venues/CreateVenueModal.tsx:37-50`, `:140-152`, `:169-171` — the `<dialog>` shell, the open/close effect, and the destructive treatment
    - `.planning/phases/36-formats-series-numbering/36-PATTERNS.md` § *No Analog Found*, the first row — there is no confirmation dialog for a destructive action anywhere in `src/`, so the two-button confirm, the focus trap and the default focus are new
  </read_first>
  <action>
    The only destructive confirmation in this phase, and it is worth building
    properly because it has no analog: the repository has no confirmation dialog at
    all today.

    Copy the `<dialog>` shell and the open/close effect from `CreateVenueModal`.
    The body carries the copy verbatim from the contract:

    heading `Retire {Name}?`, then `New nights can no longer be assigned to it.
    Nights already recorded under it keep their name and stay where they are.`,
    then the pair `[ Cancel ] [ Retire format ]`.

    `Retire format` uses the destructive treatment — the `red-500/10` fill and
    `red-400` ink, the only place in the tree those two co-occur, reserved for this
    dialog and its error box and nothing else. **`Cancel` takes the default focus**,
    which `CreateVenueModal` does not do for anything, so it has to be done
    deliberately here. The dialog traps focus while open.

    Reuse the same component for restoring, with its own copy, or write a sibling —
    either is fine, but restoring must have its own confirmation and must not read
    as an undo. `production-calendar.md` holds that a retired sigla is not cited
    again, so bringing a name back is a decision of its own.

    Record in a comment what this dialog is **not**: retiring publishes nothing and
    is reversible. Unlike `venue_reveal_sent` it is not a monotone switch, and this
    phase adds none. The monotone guard the phase must not break is the series
    number, which is held in the database by storing it rather than deriving it.
  </action>
  <verify>
    <automated>npm run build && grep -c "Nights already recorded under it keep their name" src/app/\(admin\)/admin/formats/RetireFormatDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Nights already recorded under it keep their name" <file>` returns 1 — the copy is verbatim
    - `grep -c "New nights can no longer be assigned to it" <file>` returns 1
    - The `Cancel` button receives focus when the dialog opens — assert the focus call or `autoFocus` is present on the Cancel branch and not on the destructive one
    - `grep -c "red-400" <file>` returns at least 1 and `grep -c "red-400" src/app/\(admin\)/admin/formats/CreateSeriesModal.tsx` is unaffected by this task
    - `grep -Eci "\bdelete\b" <file>` returns 0 outside a comment
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Retiring a format asks first, defaults to not doing it, and says in the same breath what it does not touch.</done>
</task>

<task type="auto">
  <name>Task 4: riaccendi la scheda Formats, che il piano 36-06 ha dovuto rinviare qui</name>
  <files>src/lib/routes/staff-tabs.ts</files>
  <read_first>
    - `src/lib/routes/staff-tabs.ts` — la riga scritta e commentata accanto a `Venues`, con le opzioni scartate accanto
    - `.planning/phases/36-formats-series-numbering/deferred-items.md` — la voce che registra il rinvio e la sua ragione
    - `.planning/phases/36-formats-series-numbering/36-06-SUMMARY.md` — l'errore di build misurato che ha reso il rinvio necessario
    - `src/app/(admin)/admin/(work)/formats/page.tsx` — il file che il Task 1 di questo piano ha creato, e che e' la ragione per cui adesso si puo'
  </read_first>
  <action>
    **Aggiunto il 2026-08-10, durante l'onda 3.** Il piano 36-06 ha dovuto
    lasciare la scheda `Formats` scritta ma commentata, e l'errore era reale e
    misurato:

    ```
    Type error: Type '"/admin/formats"' is not assignable to type 'Route'.
    src/lib/routes/staff-tabs.ts:92:5
    ```

    `StaffTab.href` e' di tipo `Route`, e un indirizzo statico entra
    nell'unione generata **solo dopo che un `page.tsx` lo serve**. Il Task 1 di
    questo piano lo serve. Quindi qui, e non prima, la riga si puo' riaccendere
    **senza un cast e senza allargare il tipo** — che erano le due strade
    scartate, e vanno lasciate scartate: un `as Route` sopravvive alla settimana
    in cui serviva, e allargare il tipo spegnerebbe `typedRoutes` per tutte e
    sette le schede esistenti.

    Togli il commento dalla riga `Formats` accanto a `Venues`, con la stessa
    capability con cui il Task 1 ha legato l'indirizzo. Rimuovi le opzioni
    scartate solo se sono commenti di lavoro; **lascia la frase che spiega
    perche' `href` e' `Route` e non `string`**, perche' e' la ragione per cui un
    menu non puo' promettere un indirizzo che nessuno serve.

    Chiudi la voce in `deferred-items.md` dichiarandola chiusa, con la data.
    Un rinvio che resta aperto in un file dopo essere stato fatto e' rumore che
    il prossimo lettore deve smaltire.
  </action>
  <verify>
    <automated>npm run build && npm run verify:routes</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "formats" src/lib/routes/staff-tabs.ts` mostra la voce **non**
      commentata
    - `grep -c "as Route" src/lib/routes/staff-tabs.ts` restituisce 0 — nessun
      cast e' stato introdotto
    - Il tipo di `StaffTab.href` e' ancora `Route` e non e' stato allargato a
      `string`
    - `npm run build` esce 0 — che qui prova qualcosa di reale, perche' e'
      esattamente il controllo che aveva fallito in 36-06
    - `npm run verify:routes` esce 0
    - La voce in `deferred-items.md` e' marcata chiusa con la data
  </acceptance_criteria>
  <done>La scheda esiste nel menu perche' adesso esiste la pagina, e il tipo che l'ha impedita prima e' intatto — era lui ad avere ragione.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request → `/admin/formats` | Two independent verdicts must agree: the middleware's and the page's |
| catalogue rows → the rendered page | The surface shows unlisted and retired rows a public page must never show |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09-01 | Elevation of Privilege | the page guard disagreeing with the map | mitigate | The page asks `CAP.CATALOGUE_MANAGE`, the key the map binds to this address; a role list is forbidden and checked by an acceptance criterion |
| T-36-09-02 | Information Disclosure | unlisted formats rendered on this surface | accept | Deliberate and correct: this surface is behind `catalogue.manage`, and the whole point of an unlisted row is that somebody can see it before deciding to publish it. The public surfaces filter, and they are a different plan |
| T-36-09-03 | Information Disclosure | the series count on a row | accept | Internal surface, behind a capability. The count rule of the UI spec governs public surfaces, and over-applying it here would remove the only informative field on the row |
| T-36-09-04 | Tampering | an accidental retirement | mitigate | A confirmation dialog that names the format, states what changes and what does not, and opens on `Cancel` |
| T-36-09-05 | Tampering | deleting a catalogue row and orphaning an archive | mitigate | No delete affordance and no delete action; `ON DELETE RESTRICT` on both foreign keys makes it structural |
| T-36-09-SC | Tampering | npm/pip/cargo installs | accept | No package is installed; every component is hand-written against existing patterns |
</threat_model>

<verification>
- `npm run build` and `npm run verify:routes` green
- The page asks the same capability the map binds
- Retired rows carry the word, not only the opacity
</verification>

<success_criteria>
A colour or a label can be changed from this page and a reload of `/events` shows
it — no deploy — and a format can be taken out of circulation without anything in
the archive changing.
</success_criteria>

<output>
Create `.planning/phases/36-formats-series-numbering/36-09-SUMMARY.md` when done.
</output>
</content>
