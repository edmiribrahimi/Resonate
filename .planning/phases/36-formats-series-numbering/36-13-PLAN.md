---
phase: 36-formats-series-numbering
plan: 13
type: execute
wave: 8
depends_on: ["36-09", "36-10", "36-12"]
files_modified:
  - .planning/phases/36-formats-series-numbering/36-13-SUMMARY.md
autonomous: false
requirements: [FMT-06, FMT-04]

must_haves:
  truths:
    - "An unannounced night exists on purpose, so the filter can be put to the test at least once"
    - "The anonymous key returns no series that only an unpublished night references"
    - "The anonymous key returns no unlisted format"
    - "The rendered source of a filtered list carries no count, label, attribute or JSON key naming the seeded night"
    - "The same filter, on a format that does have a published night, does show it"
  artifacts:
    - path: ".planning/phases/36-formats-series-numbering/36-13-SUMMARY.md"
      provides: "The V3 record: what was seeded, what each probe returned, what was observed, and the date"
      contains: "V3"
  key_links:
    - from: "the anonymous key"
      to: "public.party_series"
      via: "PostgREST, the exit path the interface does not draw"
      pattern: "rest/v1/party_series"
---

<objective>
Run V3 — the procedure that decides this phase — and record what it returned.

Purpose: `36-VALIDATION.md` says it plainly. FMT-06 has no automated proof
available here, because no mechanism in this repository can assert the **absence**
of a channel. The closest thing to a proof is asking the anonymous key directly and
reading the rendered source, and both have to be done rather than evoked.

It also has a precondition nobody can skip: both production events are published,
so there is no unannounced night for the probe to run against. One must be created
on purpose, or the filter has not been put to the test even once.

Output: a dated record of the observations, and a production database left as it
was found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-formats-series-numbering/36-VALIDATION.md
@.planning/phases/36-formats-series-numbering/36-05-SUMMARY.md
@.planning/phases/36-formats-series-numbering/36-12-SUMMARY.md

`/events` is not a valid probe of a gate, and that page has said so since phase 33
(`events/page.tsx:42-57`): it reports "no difference" because RLS refuses
unpublished rows to `anon` regardless of what the page decides. The filter inherits
the problem exactly. That is why this plan asks the **anonymous key** directly, and
why the browser half reads the **source** rather than the rendering.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the unannounced night, through the real surface</name>
  <files>.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-VALIDATION.md` § *V3*
    - `.planning/phases/36-formats-series-numbering/36-10-SUMMARY.md` — the work surface that now carries the three fields
  </read_first>
  <action>
    Create, in production, one event that is **not published**, holding one night
    with a format, a series and a number. Create it through the ordinary work
    surface at `/admin/events/new`, not with the service client: a probe that
    bypasses the path being tested proves something about a different path.

    Choose the format deliberately and record which one, because every later step
    filters on it. Prefer a format that **also** has at least one published night,
    so the admission half of the procedure can run on the same format without
    publishing anything: filtering to it must show the published night and must not
    show the seeded one. That is what turns a refusal nobody has seen fire into a
    refusal seen not to fire when it should not — and it does so without ever
    putting a test event on the live site, which is this plan's version of V3's
    final step.

    Give the event a title that reads as internal, so that if anything goes wrong
    and it does become visible it says what it is. Do not name a venue, do not use a
    date that belongs to the production calendar, and put nothing in the line-up.

    Record in the SUMMARY the event id, the night id and the format — and read
    `is_published` back from the database rather than assuming the form saved what
    it displayed.
  </action>
  <verify>
    <automated>set -a; . ./.env.local; set +a; curl -s "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/events?select=id,is_published&is_published=eq.false" -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"</automated>
  </verify>
  <acceptance_criteria>
    - The service-key read returns at least one event with `is_published: false`
    - That event has exactly one night, and that night carries a non-null `format_id`, `series_id` and `number`
    - The event was created through `/admin/events/new`, not by a direct insert
    - The SUMMARY records the event id, the night id and the chosen format
    - The chosen format also has at least one published night, or the SUMMARY says why not and how the admission half will be run instead
  </acceptance_criteria>
  <done>There is something to hide, and it was created the way a real night is created.</done>
</task>

<task type="auto">
  <name>Task 2: Ask the anonymous key, which is where the interface stops counting</name>
  <files>.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-VALIDATION.md` § *V3*, point 5
    - `.planning/phases/36-formats-series-numbering/36-RESEARCH.md` § *Findings outside scope*, V1 — the shape of an anonymous-key probe, and the defect it found that is **not** this phase's
    - `.planning/phases/36-formats-series-numbering/36-05-SUMMARY.md` — the policy predicates as the database renders them
  </read_first>
  <action>
    Three requests, with `NEXT_PUBLIC_SUPABASE_ANON_KEY` and no session. This is the
    layer where the interface stops mattering: whatever the page chooses to draw,
    PostgREST answers for every row a policy admits.

    Ask `public.formats` for everything. The answer must contain only the formats a
    person has listed. An unlisted format appearing here means `formats_select_listed`
    was applied as an unconditional read, and a format nobody has announced is
    readable by anyone.

    Ask `public.party_series` for everything. The answer must contain only the
    series that a **published** night references. A series that only the seeded
    draft points at appearing here is the tautology of Pitfall 3 having shipped —
    the whole series catalogue, venue names and all, public.

    Ask `public.event_parties` for its columns. The seeded night must be absent
    entirely, refused by `event_parties_select_published`, which this phase did not
    touch and must not have touched.

    Record the row counts and the verdicts in the SUMMARY as figures, not as
    reassurances. If any probe returns something it should not, stop: the repair is a
    new migration replacing the policy, never an edit to the applied one, and the
    phase does not close until the probe is re-run and returns what it should.

    Do not paste any returned series name or venue into the SUMMARY. Counts and
    verdicts only — `.planning/` is tracked and this repository is public.
  </action>
  <verify>
    <automated>set -a; . ./.env.local; set +a; for t in formats party_series event_parties; do echo "== $t"; curl -s "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/$t?select=*" -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY" -H "Authorization: Bearer $NEXT_PUBLIC_SUPABASE_ANON_KEY"; echo; done</automated>
  </verify>
  <acceptance_criteria>
    - The `formats` response contains no row whose `listed` is false — in particular, not the deliberately-retired fallback format
    - The `party_series` response contains no series referenced only by the seeded unpublished night
    - The `event_parties` response does not contain the seeded night's id
    - The three row counts are written into the SUMMARY as numbers
    - No series name, venue or address from the responses is copied into the SUMMARY
  </acceptance_criteria>
  <done>The exit path the interface does not draw has been asked directly, and its answers are recorded as figures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The browser half — the source, not the rendering</name>
  <files>.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md</files>
  <what-built>
    A public events list with a format filter in the address, a chip row built from
    the catalogue, and an empty state that is the same sentence for every format.
    An unpublished night exists in production under a chosen format, created through
    the ordinary work surface. The anonymous key has already been asked directly and
    its answers are in the SUMMARY.
  </what-built>
  <how-to-verify>
    In a private window, with no session:

    1. Open `/events`. Then open it again in a window where you are signed in as
       someone who can see drafts. **Is the chip row identical** — same chips, same
       order, same appearance? A difference here is D-36-16 broken.
    2. Open `/events?format=<the seeded format>`. The list must not show the seeded
       night. Note the empty-or-short state you get, then open the same address for
       two other formats: **is the wording identical**? A sentence that varies by
       format tells a visitor which formats have something hidden.
    3. **Read the source, not the page.** Use `view-source:` or `curl` on that same
       address and search it for: any number that could be a count; any
       `aria-label` or `title` containing a number; any JSON payload key naming the
       seeded night, its title, its series or its number. This step is the closest
       thing to a proof that exists here, and it is the one most easily replaced by
       a glance at the rendering, which proves nothing.
    4. Copy the address, open it on a different device with no session: same result?
    5. The admission half, on the **same** format: does a night that IS published
       under it appear in that filtered list? A refusal that has never been seen to
       admit anything has not been tested.

    Then remove the seeded event. It was created to be probed, and leaving an
    unexplained draft in production is how a later reader learns to distrust the
    archive.
  </how-to-verify>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-VALIDATION.md` § *V3*
    - the figures recorded by task 2
  </read_first>
  <action>
    Walk the five steps with the owner, or hand them the five steps and wait. Record
    each observation in the SUMMARY with what was actually seen — not "passed". Where
    a step could not be run, say which and why; a step recorded as skipped is
    honest, a step recorded as passed without being run is not.

    Delete the seeded event afterwards and confirm the deletion by reading the count
    of unpublished events back.
  </action>
  <verify>
    <human-check>Five observations recorded, each with what was seen, and the seeded event removed.</human-check>
  </verify>
  <acceptance_criteria>
    - The chip row is recorded as identical between an anonymous window and a drafts-visible window, or the difference is recorded and the phase does not close
    - The empty-state wording is recorded as identical across at least three formats
    - The page source was read and the SUMMARY names what was searched for and what was found
    - The shared-link check was run on a second device, or its absence is recorded
    - The admission half was observed: a published night under the same format does appear
    - The seeded event no longer exists, confirmed by a read-back
    - The SUMMARY is dated
  </acceptance_criteria>
  <resume-signal>Reply with the five observations, or with what you saw that you did not expect.</resume-signal>
  <done>FMT-06 has the only kind of evidence it can have in this repository: a written procedure, run, with what was observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous key → PostgREST | The layer where the interface stops mattering |
| a seeded draft → the live site | A test event exists in the production database for the duration of this plan |
| observations → a public file | `.planning/` is tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-13-01 | Information Disclosure | the seeded event becoming visible | mitigate | It is never published; its title reads as internal; it names no venue, no real date and no line-up; it is deleted when the procedure ends and the deletion is read back |
| T-36-13-02 | Information Disclosure | probe output copied into a tracked file | mitigate | Counts and verdicts only. No series name, venue or address is written into the SUMMARY |
| T-36-13-03 | Information Disclosure | a count surviving in the rendered source | mitigate | Step 3 reads the source and searches for numbers, `aria-label`, `title` and JSON keys — the only place a count with no visual form could be caught |
| T-36-13-04 | Repudiation | a procedure recorded as passed without being run | mitigate | Each step is recorded with what was seen; a skipped step is recorded as skipped |
| T-36-13-05 | Tampering | probing through the service client instead of the real path | mitigate | The night is created through `/admin/events/new`; the probes use the anonymous key, which is what a visitor holds |
| T-36-13-SC | Tampering | npm/pip/cargo installs | accept | No package is installed |
</threat_model>

<verification>
- The three anonymous-key probes, with their counts recorded
- The five browser observations, each with what was seen
- The seeded event removed, confirmed by a read-back

**What this plan still cannot prove:** that the gate refuses a *role*. No tool in
this repository can authenticate as one — the 32 open `human_needed` entries across
phases 43, 35 and 34. This phase neither consumes that debt nor worsens it, and it
builds public surfaces on top of a permission model nobody has yet watched refuse
anybody. Say so in the SUMMARY.
</verification>

<success_criteria>
Someone can read the SUMMARY in six months and know exactly what was asked, what
answered, and what was still unproven on the day the phase shipped.
</success_criteria>

<output>
Create `.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md` when done.
</output>
</content>
