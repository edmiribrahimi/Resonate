---
phase: 36-formats-series-numbering
plan: 14
type: execute
wave: 9
depends_on: ["36-13"]
files_modified:
  - .planning/phases/36-formats-series-numbering/36-VALIDATION.md
  - .planning/phases/36-formats-series-numbering/36-14-SUMMARY.md
autonomous: false
requirements: [FMT-01, FMT-02, FMT-04, FMT-05]

must_haves:
  truths:
    - "One event holds two nights of different formats, and each night shows its own"
    - "Deleting a night does not renumber another, and does not lower the next proposal"
    - "The filter survives navigation, a reload and a share, and an unknown value shows the complete list"
    - "A colour changes without a deploy, and a retired format keeps its name on the nights that ran under it"
    - "The validation record says which requirements are proved and which are only observed"
  artifacts:
    - path: ".planning/phases/36-formats-series-numbering/36-VALIDATION.md"
      provides: "The signed-off validation record, with nyquist_compliant left false on purpose"
      contains: "V5"
  key_links:
    - from: "36-VALIDATION.md"
      to: "the phase's closing state"
      via: "the sign-off checklist, each box answered with what was observed"
      pattern: "Validation Sign-Off"
---

<objective>
Run the four remaining written procedures and close the validation record
honestly.

Purpose: five of this phase's six requirements have no automated proof available
in this repository, and `36-VALIDATION.md` keeps `nyquist_compliant: false`
deliberately for that reason. The correct close is not to find a way to mark them
green; it is to run the procedures, write what was seen, and leave the flag where
it is.

Output: four procedures with their observations, and a sign-off that describes the
phase rather than flattering it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-formats-series-numbering/36-VALIDATION.md
@.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md
@.planning/phases/36-formats-series-numbering/36-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: V1 — the double bill — and V2 — the number that does not move</name>
  <files>.planning/phases/36-formats-series-numbering/36-14-SUMMARY.md</files>
  <what-built>
    A night carries a format, a series and a stored number. The public card
    aggregates the formats of an event's nights in `sort_order` and shows one marker
    each; the detail page shows each night's own. The number arrives suggested from
    a watermark that only rises and is saved exactly as typed.
  </what-built>
  <how-to-verify>
    **V1 — the double bill.** With an account holding `catalogue.manage`: create an
    event with two nights of different formats, save it, open `/events`. There must
    be **one card with two markers**, in `sort_order`, joined by `×`. Open the
    detail: each night shows its own marker, and neither shows a number or a code.

    **V2 — the number does not recalculate.** Give two nights of the same series the
    numbers *n* and *n+1*. **Delete the one carrying *n*.** The other must still
    carry *n+1* and must not have become *n*. Then open the form for a new night in
    that series: the suggestion must be **at least *n+2***. If it offers *n*, the
    suggestion is coming from a count and a number already on a poster is about to
    be handed out twice.

    Delete anything created for these two procedures afterwards, and say so.
  </how-to-verify>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-VALIDATION.md` § *V1* and § *V2*
    - `.planning/phases/36-formats-series-numbering/36-10-SUMMARY.md`
  </read_first>
  <action>
    Walk both procedures with the owner, or hand them the steps and wait. Record what
    was actually observed for each — the numbers seen, not the word "correct". V2's
    last step is the one that matters: write down the number the form proposed.
  </action>
  <verify>
    <human-check>Both procedures run, with the observed values written down, and the test rows removed.</human-check>
  </verify>
  <acceptance_criteria>
    - V1 is recorded with the number of cards seen and the number of markers on the card
    - V2 is recorded with the two numbers assigned, the one deleted, the number the surviving night still carries, and the number the form proposed afterwards
    - The proposal after the deletion is greater than the highest number ever assigned in that series
    - Anything created for these procedures has been removed, confirmed by a read-back
  </acceptance_criteria>
  <resume-signal>Reply with the observed values for V1 and V2.</resume-signal>
  <done>FMT-01 and FMT-02 have been observed rather than assumed, with numbers on the record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: V4 — the filter in the address — and V5 — data and not code</name>
  <files>.planning/phases/36-formats-series-numbering/36-14-SUMMARY.md</files>
  <what-built>
    The format filter and the Upcoming/Past choice both live in the address; the
    chip row is built from the catalogue; a format's label and colour are catalogue
    data, and retiring one removes it from the forward-looking surfaces without
    touching the archive.
  </what-built>
  <how-to-verify>
    **V4 — the filter in the address.** Choose a format, navigate into an event,
    come back: is the filter still applied? Copy the address, open it in a new
    window: same filter? Choose `Past` **and then** a format: is the tab still
    `Past`? Type `?format=` with a value that does not exist: the list must be
    **complete** and `All` current — not an empty list, not an error, not a redirect.
    Swipe between the panels: does the panel move at once, without waiting?

    **V5 — data and not code.** Change a format's colour from the catalogue and
    reload `/events` **without deploying anything**: the swatch has changed. Retire
    a format: its chip disappears from `/events`, its entry disappears from the
    select for a new assignment, **and a night in the archive that carried it still
    shows it**. Restore it and confirm the chip comes back only after listing.
  </how-to-verify>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-VALIDATION.md` § *V4* and § *V5*
    - `.planning/phases/36-formats-series-numbering/36-12-SUMMARY.md`
    - `.planning/phases/36-formats-series-numbering/36-09-SUMMARY.md`
  </read_first>
  <action>
    Walk both procedures. For V5, note explicitly that no deploy happened between the
    colour change and the reload — that is the whole content of the claim, and it can
    only be proved by not doing it.

    For the retirement half, use a format that has no nights if one exists, or the
    fallback catalogue row, so nothing real is disturbed. If a retirement has to be
    performed on a format that carries nights, restore it afterwards and record both
    acts.
  </action>
  <verify>
    <human-check>Both procedures run, each step recorded with what was seen, including the unknown-slug behaviour.</human-check>
  </verify>
  <acceptance_criteria>
    - V4 records all five observations, and the unknown-slug step explicitly records "complete list, `All` current, no error, no redirect"
    - V4 records that the tab survived choosing a format, and that the format survived choosing a tab
    - V5 records that no deploy occurred between the colour change and the reload
    - V5 records that a retired format's chip disappeared **and** that an archived night still shows it
    - Anything retired for the procedure has been restored, and both acts are recorded
  </acceptance_criteria>
  <resume-signal>Reply with the observations for V4 and V5.</resume-signal>
  <done>FMT-04 and FMT-05 have been observed on a running system, including the two claims — "survives a share" and "without a deploy" — that only running can settle.</done>
</task>

<task type="auto">
  <name>Task 3: Close the validation record, and say what is still owed</name>
  <files>.planning/phases/36-formats-series-numbering/36-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-VALIDATION.md` — the whole file, including its opening paragraph on why `nyquist_compliant` is false on purpose
    - `.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md` and this plan's earlier tasks
  </read_first>
  <action>
    Fill in the per-task verification map with what actually exists now: FMT-03 and
    the composite key have automated constraint probes and the command that runs
    them; everything else is a written procedure with a date and an observation.

    Tick the Wave 0 boxes that were completed and leave any that were not, with a
    sentence. Answer each box of the sign-off with what was observed rather than a
    tick alone.

    **Leave `nyquist_compliant: false`.** It is the correct description of a phase in
    which five requirements of six are proved only by hand, and changing it would be
    a lie that somebody later uses to close something.

    Record the debt this phase leaves and the debt it did not touch:
    - No tool in this repository can authenticate as a role, so nothing here has
      watched the gate refuse anybody. That is the 32 open `human_needed` entries
      across phases 43, 35 and 34; this phase neither consumed nor worsened them, and
      it has now built public surfaces on top of that model.
    - The secret venue address remains readable with the anonymous key through
      `venues_select_public`; it is recorded in
      `.planning/todos/pending/secret-venue-address-readable-by-anon.md` and assigned
      to phase 37. It is named here because a reader of this record should not have
      to discover it.
    - `production-calendar.md` still carries the gate *progressivo per sede o per
      format* as open, and D-36-07 closed it. Updating that module is
      `ai-engineering.md` work — version, changelog, `npm run verify:persona` — and
      `36-CONTEXT.md` defers it to when the phase ships. Record it as owed, do not do
      it here.
    - The migration history drift measured at eighteen entries is the owner's call
      and is unchanged by this phase.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: false" .planning/phases/36-formats-series-numbering/36-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nyquist_compliant: false" <file>` returns 1
    - Every row of the per-task verification map is answered, with a command where one exists and with a procedure name where none does
    - Every sign-off box carries an observation, not only a tick
    - The four items of owed work are recorded, each naming where it belongs
    - No requirement is marked proved on the strength of `npm run build`
  </acceptance_criteria>
  <done>The validation record describes the phase as it is: two constraints proved by a command, four behaviours observed by a person, and a permission model nobody has yet watched refuse anyone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test data → the production archive | V1 and V2 create and delete real nights |
| the validation record → a future decision | Somebody will close something on the strength of this file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14-01 | Tampering | test nights left in the archive | mitigate | Everything created for V1 and V2 is deleted and the deletion is read back; anything retired for V5 is restored and both acts recorded |
| T-36-14-02 | Tampering | a number burned by a test | accept | The watermark is monotone by design, so a test number cannot be reissued. That is the behaviour V2 exists to observe, and the cost is one number in a test series |
| T-36-14-03 | Repudiation | a record that flatters the phase | mitigate | `nyquist_compliant` stays false; every sign-off box carries an observation; no requirement may be marked proved on the strength of a build |
| T-36-14-04 | Information Disclosure | a retirement performed on a live format | mitigate | Prefer a format with no nights or the fallback row; if a live one must be used, restore it and record both acts |
| T-36-14-SC | Tampering | npm/pip/cargo installs | accept | No package is installed |
</threat_model>

<verification>
- Four procedures run, each with observations rather than verdicts
- `36-VALIDATION.md` complete, `nyquist_compliant: false`
- The four owed items recorded with where each belongs
</verification>

<success_criteria>
The phase closes with a written record of what was proved, what was observed, and
what nobody has been able to test yet — and none of the three is disguised as
another.
</success_criteria>

<output>
Create `.planning/phases/36-formats-series-numbering/36-14-SUMMARY.md` when done.
</output>
</content>
