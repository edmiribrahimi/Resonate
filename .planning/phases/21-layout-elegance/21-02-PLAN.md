---
phase: 21-layout-elegance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/events/loading.tsx
  - src/app/(public)/events/[slug]/loading.tsx
  - src/app/(public)/events/[slug]/menu/loading.tsx
  - src/app/(public)/gallery/loading.tsx
  - src/app/(members)/dashboard/loading.tsx
  - src/app/(members)/membership-card/loading.tsx
  - src/app/(admin)/admin/members/loading.tsx
  - src/app/(admin)/admin/events/loading.tsx
  - src/app/(admin)/admin/finance/loading.tsx
  - src/app/(organizer)/organizer/events/loading.tsx
  - src/app/(organizer)/organizer/members/loading.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Navigating to any major page shows skeleton placeholders while data loads instead of blank space"
    - "Skeleton cards match the visual structure of the actual content (similar heights, widths, spacing)"
    - "Skeletons use animate-pulse for visual feedback that loading is in progress"
  artifacts:
    - path: "src/app/(public)/events/loading.tsx"
      provides: "Skeleton for events listing page"
      contains: "SkeletonCard"
    - path: "src/app/(public)/events/[slug]/loading.tsx"
      provides: "Skeleton for event detail page"
      contains: "SkeletonLine"
    - path: "src/app/(public)/events/[slug]/menu/loading.tsx"
      provides: "Skeleton for drink menu page"
      contains: "SkeletonCard"
    - path: "src/app/(public)/gallery/loading.tsx"
      provides: "Skeleton for gallery page"
      contains: "animate-pulse"
    - path: "src/app/(members)/dashboard/loading.tsx"
      provides: "Skeleton for member dashboard"
      contains: "SkeletonCard"
    - path: "src/app/(admin)/admin/members/loading.tsx"
      provides: "Skeleton for admin members page"
      contains: "SkeletonAvatar"
    - path: "src/app/(admin)/admin/events/loading.tsx"
      provides: "Skeleton for admin events page"
      contains: "SkeletonCard"
    - path: "src/app/(admin)/admin/finance/loading.tsx"
      provides: "Skeleton for finance page"
      contains: "SkeletonCard"
  key_links:
    - from: "src/app/(public)/events/loading.tsx"
      to: "src/components/ui/Skeleton.tsx"
      via: "import SkeletonCard"
      pattern: "from.*@/components/ui/Skeleton"
---

<objective>
Create Next.js loading.tsx skeleton files for all major route groups so async data loads show placeholder content instead of blank screens.

Purpose: Eliminate layout shift and blank flashes during page navigation. Users see a structured skeleton that mirrors the page layout while data loads.

Output: 11 loading.tsx files across public, member, admin, and organizer route groups.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-layout-elegance/21-RESEARCH.md

Note: This plan runs in parallel with Plan 01 (Wave 1). The Skeleton.tsx primitives (SkeletonLine, SkeletonCard, SkeletonAvatar) are created by Plan 01 Task 2. If executing before Plan 01 completes, create inline skeleton elements using raw Tailwind animate-pulse classes. If Plan 01 has completed, import from @/components/ui/Skeleton.

<interfaces>
<!-- Skeleton primitives from Plan 01 (if available) -->
From src/components/ui/Skeleton.tsx (created by Plan 01):
```typescript
export function SkeletonLine({ className }: { className?: string }): JSX.Element;
export function SkeletonCard({ className }: { className?: string }): JSX.Element;
export function SkeletonAvatar({ size?: "sm" | "md" | "lg" }): JSX.Element;
```

If Plan 01 has not completed yet, use inline Tailwind:
```typescript
// Inline skeleton pattern:
<div className="animate-pulse rounded-2xl border border-card-border bg-card p-5">
  <div className="h-3 w-1/3 rounded-lg bg-card-border/50 mb-3" />
  <div className="h-5 w-2/3 rounded-lg bg-card-border/50 mb-2" />
  <div className="h-3 w-1/2 rounded-lg bg-card-border/50" />
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton loading states for public routes</name>
  <files>src/app/(public)/events/loading.tsx, src/app/(public)/events/[slug]/loading.tsx, src/app/(public)/events/[slug]/menu/loading.tsx, src/app/(public)/gallery/loading.tsx</files>
  <action>
Before creating files, read the actual page.tsx files to understand the layout structure each skeleton must mirror:
- Read src/app/(public)/events/page.tsx (events listing)
- Read src/app/(public)/events/[slug]/page.tsx (event detail)
- Read src/app/(public)/events/[slug]/menu/page.tsx (drink menu)
- Read src/app/(public)/gallery/page.tsx (gallery)

Import SkeletonCard, SkeletonLine, SkeletonAvatar from "@/components/ui/Skeleton" if the file exists. If it doesn't exist yet (Plan 01 not complete), use inline Tailwind animate-pulse divs instead.

All loading.tsx files are Server Components (no "use client" needed). All must use min-h-dvh and pb-24 (for MobileNav space).

1. `src/app/(public)/events/loading.tsx`:
   - Mirror events page structure: header area with title skeleton, then 3 SkeletonCards in a vertical stack with gap-4
   - Include padding matching the actual events page (px-6 pt-12 pb-6 for header, px-6 for cards)

2. `src/app/(public)/events/[slug]/loading.tsx`:
   - Mirror event detail: hero image skeleton (aspect-video w-full rounded-2xl), then event title (h-8 w-2/3), date/venue info lines (h-4 w-1/2), description block (3 lines), action button area
   - Match the actual page's padding and spacing

3. `src/app/(public)/events/[slug]/menu/loading.tsx`:
   - Mirror drink menu: header with event name skeleton, then grid of 4-6 drink card skeletons
   - Drink card skeleton: rounded-2xl with image placeholder (h-32) + name line + price line

4. `src/app/(public)/gallery/loading.tsx`:
   - Mirror gallery grid: header skeleton, then a 2-column grid (sm:3-column) of square image placeholders (aspect-square animate-pulse rounded-xl bg-card-border/50)
   - 6-9 grid items
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npm run build</automated>
  </verify>
  <done>4 loading.tsx files exist for public routes. Each mirrors the real page layout with animate-pulse skeleton elements. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create skeleton loading states for authenticated routes</name>
  <files>src/app/(members)/dashboard/loading.tsx, src/app/(members)/membership-card/loading.tsx, src/app/(admin)/admin/members/loading.tsx, src/app/(admin)/admin/events/loading.tsx, src/app/(admin)/admin/finance/loading.tsx, src/app/(organizer)/organizer/events/loading.tsx, src/app/(organizer)/organizer/members/loading.tsx</files>
  <action>
Before creating files, read the actual page.tsx files to understand the layout structure:
- Read src/app/(members)/dashboard/page.tsx (member dashboard)
- Read src/app/(members)/membership-card/page.tsx (membership card)
- Read src/app/(admin)/admin/members/page.tsx (admin members list)
- Read src/app/(admin)/admin/events/page.tsx (admin events list)
- Read src/app/(admin)/admin/finance/page.tsx (finance dashboard)
- Read src/app/(organizer)/organizer/events/page.tsx (organizer events)
- Read src/app/(organizer)/organizer/members/page.tsx (organizer members)

Import SkeletonCard, SkeletonLine, SkeletonAvatar from "@/components/ui/Skeleton" if available.

1. `src/app/(members)/dashboard/loading.tsx`:
   - Mirror dashboard: greeting area skeleton (h-8 w-1/2), quick stats row (2-3 skeleton cards in a row), then main content cards
   - Include the drink tokens area with 2-3 small card skeletons

2. `src/app/(members)/membership-card/loading.tsx`:
   - Mirror membership card page: centered card skeleton with QR code placeholder (square, ~200x200), name line, membership code line

3. `src/app/(admin)/admin/members/loading.tsx`:
   - Mirror members list: header + search bar skeleton, then list of 8-10 member row skeletons (avatar circle + name line + email line)

4. `src/app/(admin)/admin/events/loading.tsx`:
   - Mirror events management: header skeleton + 3 event card skeletons (image + title + date)

5. `src/app/(admin)/admin/finance/loading.tsx`:
   - Mirror finance dashboard: summary cards row (3 stat cards), then transaction list skeletons (6-8 rows)

6. `src/app/(organizer)/organizer/events/loading.tsx`:
   - Mirror organizer events list: header + 3 event card skeletons (same structure as admin events)

7. `src/app/(organizer)/organizer/members/loading.tsx`:
   - Mirror organizer members: header + member list skeletons (same structure as admin members but simpler)

All files use min-h-dvh pb-24 pattern. Match the padding of the actual pages. No "use client" directive needed.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npm run build</automated>
  </verify>
  <done>7 loading.tsx files exist for authenticated routes. Each mirrors the real page layout. Total: 11 loading.tsx files across the app. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `find src/app -name "loading.tsx" | wc -l` shows 11 files
3. `grep -r "animate-pulse" src/app/*/loading.tsx src/app/*/*/loading.tsx` confirms all use pulse animation
4. No loading.tsx file contains "use client" directive
</verification>

<success_criteria>
- Every major route group has a loading.tsx file
- Skeletons use animate-pulse and match the visual structure of actual page content
- No layout shift visible when navigating between pages (skeleton fills the space)
- All files are Server Components (no "use client")
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/21-layout-elegance/21-02-SUMMARY.md`
</output>
