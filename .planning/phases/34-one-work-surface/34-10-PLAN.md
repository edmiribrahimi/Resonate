---
phase: 34-one-work-surface
plan: 10
type: execute
wave: 3
depends_on: ["34-05", "34-06", "34-07"]
files_modified:
  - src/app/(admin)/admin/(work)/members/page.tsx
  - src/app/(admin)/admin/(work)/members/loading.tsx
  - src/app/(admin)/admin/(work)/members/actions.ts
  - src/app/(admin)/admin/(work)/members/CreateAccountForm.tsx
  - src/app/(admin)/admin/(work)/members/MemberActionNotice.tsx
  - src/app/(admin)/admin/(work)/members/growth/page.tsx
  - src/app/(admin)/admin/(work)/members/growth/loading.tsx
  - src/app/(admin)/admin/(work)/members/register/page.tsx
  - src/app/(organizer)/organizer/members/page.tsx
  - src/app/(organizer)/organizer/members/loading.tsx
autonomous: true
requirements: [STAFF-01, STAFF-03]

must_haves:
  truths:
    - "`/admin/members` exists once, opens to `organizer.access`, and shows the account-creation form and the way into the register"
    - "`/admin/members/register` opens to `register.read` — an organizer holding it reaches it, without any grant being edited"
    - "`/admin/members/growth` stays master-only on `admin.access`"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/members/page.tsx"
      provides: "The single members surface, with the creation form and the register link"
      contains: "ORGANIZER_ACCESS"
    - path: "src/app/(admin)/admin/(work)/members/register/page.tsx"
      provides: "The register, at the address the map binds to register.read"
      contains: "REGISTER_READ"
  key_links:
    - from: "src/app/(admin)/admin/(work)/members/page.tsx"
      to: "/admin/members/register"
      via: "the Membership acts link, drawn unconditionally"
      pattern: "members/register"
---

<objective>
Collapse the largest divergence — the `members` pair, 131 changed lines — and move the whole
`members` tree under the work layout, including `growth` and `register`.

Purpose: this plan closes the folded todo. **It is the smallest true test of the whole phase.**
An organizer holds `register.read` and cannot reach `/admin/members/register` today, because
`middleware.ts` judges everything under `/admin` with `admin.access`, granted to the master
alone. The executor of plan 43-14 deliberately did not loosen the middleware, writing that the
collapse belonged here. **If the collapse is real, that defect disappears without anyone
editing a permission.** If closing it requires touching a grant, the collapse did not happen —
something was special-cased instead.

**The grant that decides each part of the merge, as D-34-05 requires.** The organizer file lacks
three things the admin file has:
- **`<CreateAccountForm />`** — decided by **D-20 of Phase 43** (an organizer may create an
  account directly as organizer) and by the action's own gate: `createAccount` re-asks
  `staff.manage`, which an organizer holds. Resolving **towards more** is permitted here because
  the grant already says so (D-34-06). No grant changes.
- **the `Membership acts →` link to the register** — decided by
  `('organizer','register.read',true)`. Granted, and unreachable today only because of the
  prefix rule this phase dissolves.
- **`AnimatedSection`** — cosmetic.

Output: one members surface, the register reachable by an organizer, and the whole tree under
the layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-06-SUMMARY.md
@.planning/phases/43-role-model-account-creation/43-CONTEXT.md
@.planning/todos/pending/register-read-unreachable-for-organizers.md
@CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/community-membership.md
@.claude/rules/meta-gates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the members tree under the layout and merge the pair</name>
  <files>src/app/(admin)/admin/(work)/members/**, src/app/(organizer)/organizer/members/**</files>
  <read_first>
    - src/app/(admin)/admin/members/page.tsx (read in full — the `admin.access` guard, `<CreateAccountForm />`, the `Membership acts →` link and the comment block explaining why the link is drawn unconditionally)
    - src/app/(organizer)/organizer/members/page.tsx (read in full — the same `MemberTable` with the same props, and the three things it lacks)
    - src/app/(admin)/admin/members/actions.ts — read the guards only: `createAccount`'s `staff.manage` re-check, and the `verifyMaster` sites. Do **not** read all 2600 lines; grep for `assertStaffManage`, `verifyMaster`, `ownsOrIsMaster` and read those blocks
    - .planning/phases/34-one-work-surface/34-06-SUMMARY.md (the route-group arrangement to follow)
    - src/lib/routes/capability-routes.ts (`organizer.access` → `/admin/members`; `admin.access` → `/admin/members/growth`; `register.read` → `/admin/members/register`)
  </read_first>
  <action>
    `git mv` the entire `src/app/(admin)/admin/members/` directory into
    `src/app/(admin)/admin/(work)/members/`, following the arrangement recorded in
    `34-06-SUMMARY.md`. That is `page.tsx`, `loading.tsx`, `actions.ts`, `CreateAccountForm.tsx`,
    `MemberActionNotice.tsx`, `growth/{page,loading}.tsx` and `register/page.tsx` — the whole
    tree in one move, which is why no other plan owns any part of it.

    Merge the pair into the moved `page.tsx`:
    - Guard changes from `CAP.ADMIN_ACCESS` to `CAP.ORGANIZER_ACCESS`. That is the key the map
      binds to `/admin/members` and the key the organizer twin already asked.
    - **Keep `<CreateAccountForm />`.** The grant is D-20 of Phase 43 and `createAccount`'s own
      `staff.manage` re-check; both are cited in the SUMMARY.
    - **Keep the `Membership acts →` link, and keep it unconditional.** Here is the edge that
      has to be handled honestly rather than tidied: `register.read` carries
      `requires_approved = true`, so a **`pending` organizer** reaching `/admin/members` will
      see a link that leads to a refusal. That is today's behaviour on the master side too — the
      admin page draws the link unconditionally and its comment block says why. **Keep it
      unconditional and keep the comment.** Making it conditional would be a navigation change
      with no matching server change, which is the inverse of STAFF-03 and worse: a link that
      vanishes tells the holder of a granted capability that they do not hold it.
    - `AnimatedSection` is cosmetic; keep the admin file's version and say so.
    - Delete the nav mounts, their imports and the `role as UserRole` / `status as UserStatus`
      casts. The layout provides both.
    - The `<h1>` takes the surface's own name.

    `growth/page.tsx` keeps `CAP.ADMIN_ACCESS` — it is master-only and the map binds it to
    `admin.access`. `register/page.tsx` keeps `CAP.REGISTER_READ`. Neither guard changes. Delete
    only their nav mounts and casts.

    Delete `src/app/(organizer)/organizer/members/page.tsx` and its `loading.tsx`.

    Do **not** change any `revalidatePath` argument in `actions.ts`. Sixteen of the phase's 26
    calls live in that file as five matched `/admin/…` + `/organizer/…` pairs plus four
    singletons, and collapsing them is plan 34-16's — **deleting the wrong half of a pair is a
    one-character mistake with no detector**, and doing it in the same plan as a 131-line merge
    is how it happens.

    Do not restyle. Phases 40 and 41.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)" -path '*members*' -name page.tsx | wc -l` returns 3 — members, growth, register
    - `src/app/(organizer)/organizer/members/` no longer exists
    - The merged `members/page.tsx` guards on `ORGANIZER_ACCESS`, renders `<CreateAccountForm />`, and contains a link whose href is the register's route pattern
    - The register link is drawn unconditionally: no `capabilities.has(...REGISTER_READ)` wraps it, and the comment explaining why survives
    - `growth/page.tsx` still guards on `ADMIN_ACCESS`; `register/page.tsx` still guards on `REGISTER_READ`
    - `grep -rc "StaffNav\|MobileNav\|as UserRole\|as UserStatus" "src/app/(admin)/admin/(work)/members"` returns 0 for every file
    - `git diff -M` shows **no** hunk changing a `revalidatePath` argument, and the call count in `actions.ts` is unchanged — record it in the SUMMARY
    - `git diff -M` shows no hunk touching `assertStaffManage`, `verifyMaster` or `ownsOrIsMaster`
    - `rm -rf .next && npm run build` exits 0
  </acceptance_criteria>
  <done>One members surface, the whole tree under the layout, three guards unchanged in meaning, and every `revalidatePath` left for its own plan.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the folded todo closed by construction, and that nothing was special-cased to close it</name>
  <files>.planning/todos/pending/register-read-unreachable-for-organizers.md</files>
  <read_first>
    - .planning/todos/pending/register-read-unreachable-for-organizers.md (including its own warning that a new rule inserted carelessly reproduces the scanner hazard)
    - src/lib/routes/capability-routes.ts (`register.read`'s binding)
    - src/lib/supabase/middleware.ts (as rewritten by plan 34-03 — confirm there is no branch naming `register` and no branch naming a prefix)
    - supabase/migrations/20260808002000_membership_register.sql (the `('organizer','register.read',true)` grant row — read it, do not quote it from another document)
  </read_first>
  <action>
    Establish, by reading and by command, that the defect closed **by construction** and not by
    an exception:

    1. `git diff` across the whole phase so far contains **no change under `supabase/`**. This
       phase writes no migration and edits no grant. Run
       `git diff --name-only <phase-base>..HEAD -- supabase/` and confirm it is empty. Paste the
       command and its empty output into the SUMMARY.
    2. `private.role_capabilities` was not edited. The grant `('organizer','register.read',true)`
       is read from `20260808002000_membership_register.sql`, where it already was.
    3. The middleware contains no special case for the register: `grep -ci "register"
       src/lib/supabase/middleware.ts` returns 0.
    4. `register.read`'s entry in the map binds exactly one route,
       `/admin/members/register`, and resolves by specificity against `/admin/members`
       (`organizer.access`) and `/admin` (`organizer.access`) — the three-way nesting the
       research named as the concrete reason declaration order would have been wrong.

    Then move the todo file from `.planning/todos/pending/` to `.planning/todos/done/` — or to
    whatever the repository's convention is; check `ls .planning/todos/` first and follow it —
    adding a closing note that names the mechanism (D-34-02 dissolved the prefix rule; the map
    binds the route to `register.read`) and the date, and states explicitly that **no permission
    was edited**.

    If any of the four checks fails, do **not** close the todo. Record what was special-cased
    and stop: the collapse did not happen, and closing the todo would record a success that was
    not achieved.
  </action>
  <verify>
    <automated>test -z "$(grep -ci 'register' src/lib/supabase/middleware.ts | grep -v '^0$')" &amp;&amp; rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` over the phase shows no file under `supabase/`
    - `grep -ci "register" src/lib/supabase/middleware.ts` returns 0
    - `register.read`'s `routes` array in the map has exactly one element
    - The todo file has moved out of `pending/`, carrying a dated closing note that names the mechanism and states that no permission was edited
    - Behaviour (manual, owed to plan 34-17): **M-2** must observe an `organizer`/`approved` account rendering `/admin/members/register`, and **M-3** must observe an `organizer`/`pending` account being **refused** there — `register.read` requires approved
  </acceptance_criteria>
  <done>The defect is closed, and the evidence shows it closed because the prefix rule dissolved, not because somebody made room for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| organizer → member record | Approvals, rejections and account creation are privileged operations reachable from this surface |
| client → `members/actions.ts` | Every action is a public POST endpoint with its own re-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-47 | Elevation of Privilege | the merged members surface admitting somebody the organizer twin refused | mitigate | `organizer.access` is the twin's own key; the `CreateAccountForm` addition rests on D-20 and on `createAccount`'s `staff.manage` re-check, both cited |
| T-34-48 | Elevation of Privilege | `growth` opened to organizers by the collapse | mitigate | `growth` keeps `admin.access` and the map binds it there; longest-literal-match makes `/admin/members/growth` beat `/admin/members`, proved by mutation in 34-01 |
| T-34-49 | Elevation of Privilege | the register opened by editing a grant instead of by the collapse | mitigate | Task 2 asserts an empty `supabase/` diff across the phase and no `register` string in the middleware |
| T-34-50 | Information Disclosure | a members list that stops refreshing after an approval | transfer | Deliberately left to plan 34-16 and procedure M-8. This plan asserts the `revalidatePath` count unchanged so no half-pair edit can hide here |
| T-34-51 | Repudiation | a nav link hidden from somebody who holds the capability | mitigate | The register link stays unconditional, with its comment; an acceptance criterion forbids wrapping it in a capability check |
| T-34-52 | Information Disclosure | a member named in a published planning document | mitigate | `.planning/` is published; the SUMMARY records roles only |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; audit not applicable for this phase |
</threat_model>

<verification>
- `rm -rf .next && npm run build` exits 0
- `git diff --name-only` over the phase shows nothing under `supabase/`
- `npm run verify:persona` exits 0
- **Not claimed:** that an organizer can now open the register. The type system cannot see that. **M-2** observes it, and until M-2 runs, the todo's closure is a construction argument, not an observation — the SUMMARY must say so in those words.
</verification>

<success_criteria>
1. `/admin/members` exists once, on `organizer.access`, with the creation form and an unconditional register link
2. `growth` keeps `admin.access` and `register` keeps `register.read`
3. No migration, no grant and no `revalidatePath` argument was touched
4. The folded todo is closed with a dated note naming the mechanism, or left open with the special case recorded
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-10-SUMMARY.md` when done.
</output>
