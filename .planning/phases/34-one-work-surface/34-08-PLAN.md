---
phase: 34-one-work-surface
plan: 08
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - scripts/verify-organizer-redirects.sh
  - scripts/verify-routes.mjs
  - package.json
autonomous: true
requirements: [STAFF-02, STAFF-03]

must_haves:
  truths:
    - "Every row of the redirect table can be walked mechanically, against a local dev server, without deploying"
    - "A `revalidatePath` call naming an address that no route serves fails a committed check"
    - "Both checks are proved by mutation, with the mutation confirmed applied before its result is read"
  artifacts:
    - path: "scripts/verify-organizer-redirects.sh"
      provides: "The fifteen-row redirect walk, status and Location per row"
      min_lines: 30
    - path: "scripts/verify-routes.mjs"
      provides: "The only mechanism in the repository that can see a revalidatePath argument"
      min_lines: 40
  key_links:
    - from: "scripts/verify-routes.mjs"
      to: "src/lib/routes/capability-routes.ts"
      via: "reads the declared route patterns and asserts every revalidatePath argument is one of them"
      pattern: "capability-routes"
---

<objective>
Write the two mechanical checks this phase needs and does not have: the redirect-table walk,
and the `revalidatePath` check.

Purpose: STAFF-02 has no automated instrument otherwise — the redirect table is fifteen rows of
prose until something requests all fifteen. And `revalidatePath` is the phase's **silent-failure
category**: 26 calls, untyped in every option, invisible to `typedRoutes` (whose augmentation
covers `next/link`, `next/navigation` and `next/form`, never `next/cache`) and invisible to
`npm run build`. Its failure mode is a members list that stops refreshing after an approval —
a member-facing consequence of a routing edit, in a product with **no error tracking** to
report it (D-34-16).

A shell loop is re-runnable; a prose procedure is not. Both of these are committed scripts for
that reason.

Output: `scripts/verify-organizer-redirects.sh`, `scripts/verify-routes.mjs`, two `package.json`
entries, and two mutation proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-VALIDATION.md
@.planning/phases/34-one-work-surface/34-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/meta-gates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The redirect walk</name>
  <files>scripts/verify-organizer-redirects.sh, package.json</files>
  <read_first>
    - src/lib/routes/organizer-redirects.ts (written by 34-01 — the fifteen rows and `REDIRECT_STATUS`)
    - scripts/verify-persona.mjs:1-40 (the house style for a verification script: a docblock that states what it checks and, explicitly, what it does **not**)
    - package.json (the existing `verify:*` entries — follow their naming)
  </read_first>
  <action>
    Write `scripts/verify-organizer-redirects.sh`. It takes a base URL, defaulting to
    `http://localhost:3000`, parses the fifteen `[from, to]` rows out of
    `src/lib/routes/organizer-redirects.ts` — do not re-type them into the script, a second copy
    of the table is the drift this phase removes — and for each row issues
    `curl -sS -o /dev/null -D - -w '%{http_code}'` against `from`, printing a pass/fail column
    with the observed status and `Location`.

    Substitute a fixed placeholder for the `[id]` segment so a dynamic row is requestable.

    A row passes when the status matches the `REDIRECT_STATUS` exported by the table (307 while
    the phase is in flight, 308 after plan 34-17 flips it — read it, do not hardcode it) **and**
    the `Location` path equals the expected destination with the placeholder substituted. Exit 1
    naming every failing row; exit 0 only when all fifteen pass.

    Two assertions the script makes beyond the rows, because they are the fence:
    - No response `Location` contains `/scanner`.
    - Requesting `/admin/scanner` directly returns something that is **not** a redirect to
      another address — the door does not move, and this script is where that becomes a repeated
      observation rather than a remembered rule.

    macOS/BSD: `grep -E` for extended regex, `sed -i ''` if editing in place. Every command must
    run on macOS.

    Docblock, in the house style: state that this proves the **address translation** and nothing
    about who may then see the destination; that it needs a running dev server and therefore
    does not run in `npm run build`; and that there is **no CI in this repository**, so it runs
    when a person runs it.

    Add `"verify:redirects": "bash scripts/verify-organizer-redirects.sh"` to `package.json`.
    Do not add a `test` script. Do not add a dependency.
  </action>
  <verify>
    <automated>bash -n scripts/verify-organizer-redirects.sh &amp;&amp; node -e "const p=require('./package.json'); if(!p.scripts['verify:redirects']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-organizer-redirects.sh` exits 0 (syntax)
    - The script parses the rows from `src/lib/routes/organizer-redirects.ts`; `grep -c "/organizer/artists" scripts/verify-organizer-redirects.sh` returns 0 — the table is not re-typed
    - `package.json` gains exactly one script entry, `verify:redirects`, and no dependency
    - Behaviour, with `npm run dev` running: `npm run verify:redirects` walks 15 rows and prints a status and a `Location` for each
    - **Mutation proof:** point one row's `to` at a wrong destination in `organizer-redirects.ts`, run `git diff --stat` and paste it into the SUMMARY to confirm the mutation applied, run the script, confirm it names that row and exits non-zero, revert
    - `git status --porcelain` shows no mutation residue after the task
  </acceptance_criteria>
  <done>The fifteen rows can be walked in one command against a local server, and the walk has been proved to fail on a wrong row.</done>
</task>

<task type="auto">
  <name>Task 2: The revalidatePath check — the only mechanism that can see this category</name>
  <files>scripts/verify-routes.mjs, package.json</files>
  <read_first>
    - The output of `grep -rEn 'revalidatePath\(\s*"' src/` — 26 calls naming `/admin` or `/organizer` were measured on 2026-08-09, 16 of them in `admin/members/actions.ts` as five matched pairs plus four singletons; there are also calls to public paths such as `/events` and `` `/events/${slug}` ``
    - src/lib/routes/capability-routes.ts (the declared route patterns)
    - scripts/verify-capabilities.mjs:26-60 (the house style, and the proof-by-mutation rule this script inherits)
  </read_first>
  <action>
    Write `scripts/verify-routes.mjs`, zero dependencies, Node only.

    It reads every `.ts` / `.tsx` file under `src/`, finds every `revalidatePath(` call whose
    first argument is a **string literal** or a template literal with a static prefix, and
    asserts the path is either (a) a route pattern declared in `CAPABILITY_ROUTES`, or (b) a
    member of a small, explicit, in-script allow-list of public routes that the map deliberately
    does not gate — `/events`, `/events/[slug]`, `/gallery`, `/artists/[slug]`,
    `/venues/[slug]`, `/dashboard` and any other the measurement turns up. Write that allow-list
    with a one-line reason per entry; an unexplained allow-list is where a hole hides.

    Extract the route patterns from `capability-routes.ts` by reading and matching the quoted
    `/...` literals in the file. It is a `.mjs` reading a `.ts`, so this is a parse, not an
    import — say so in the docblock, and say what that parse cannot see (a route built by
    concatenation, a call whose argument is a variable). **Name what was NOT measured.** That is
    this repository's house style, and it is the difference between a check and a claim.

    Exit 1 naming every offending file:line and its argument. Exit 0 otherwise.

    Add `"verify:routes": "node scripts/verify-routes.mjs"` to `package.json`.

    The script must **pass** on the tree as it stands at the end of this plan. It will not: 16
    calls in `admin/members/actions.ts` and 11 elsewhere still name `/organizer/...`, which no
    route serves after plan 34-03's redirect. That is correct and expected — it is exactly the
    finding plan 34-16 exists to close. So: run it, record its full failing output in the
    SUMMARY as the **worklist plan 34-16 must drive to zero**, and do **not** weaken the script
    to make it green. Weakening a check to make a phase pass is the one move this project
    treats as worse than the defect.
  </action>
  <verify>
    <automated>node scripts/verify-routes.mjs; test $? -le 1</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-routes.mjs` runs to completion and exits 1 with a list of offending file:line entries — the pre-sweep state
    - That list is pasted verbatim into the SUMMARY as plan 34-16's worklist, with its total count
    - The public allow-list in the script has a one-line reason per entry
    - The docblock names what the parse **cannot** see: a concatenated path and a variable argument
    - `package.json` gains exactly one script entry, `verify:routes`, and no dependency
    - **Mutation proof:** temporarily point one `/admin/members` call at `/admin/nonexistent`, confirm with `git diff --stat` that the mutation applied, run the script, confirm it names that exact file:line, revert
    - `git status --porcelain` shows no mutation residue after the task
    - No test framework was installed and no `test` script was added
  </acceptance_criteria>
  <done>The one category `npm run build` cannot see now has a committed check, it has been proved to catch a bad path, and its current failures are recorded as a worklist rather than silenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → dev server | The walk requests every legacy address; it sends no credential and asserts no identity |
| script → source | Both scripts parse source rather than importing it, so what they cannot see must be declared |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-38 | Repudiation | a check weakened to make the phase pass | mitigate | `verify:routes` is required to **fail** at the end of this plan, with its output recorded as 34-16's worklist; an acceptance criterion forbids silencing it |
| T-34-39 | Repudiation | a check that reports green on something it cannot see | mitigate | The docblock must name the two blind spots (concatenated paths, variable arguments), and both scripts are proved by mutation with the mutation confirmed applied first |
| T-34-40 | Information Disclosure | the walk run against production | mitigate | The base URL defaults to `http://localhost:3000` and the script sends no cookie; it proves address translation, which has no subject |
| T-34-41 | Tampering | the redirect table re-typed into the script and drifting | mitigate | The script parses the table; `grep` asserts no row literal appears in the script |
| T-34-42 | Denial of Service *at a queue* | a check that could report the door as redirectable | mitigate | The script asserts no `Location` contains `/scanner` and that `/admin/scanner` is not redirected — the fence becomes a repeated observation |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | Both scripts are zero-dependency; `package.json` gains two script entries and no dependency, asserted |
</threat_model>

<verification>
- `bash -n scripts/verify-organizer-redirects.sh` exits 0
- `node scripts/verify-routes.mjs` runs and exits 1 with the expected pre-sweep worklist
- `npm run verify:redirects` walks all fifteen rows against `npm run dev`
- Both mutation proofs recorded with their `git diff --stat` evidence
- **Not claimed:** that a green `verify:routes` means every `revalidatePath` is correct. It means every *statically visible literal* names a declared route. The docblock says which cases it cannot see.
</verification>

<success_criteria>
1. `scripts/verify-organizer-redirects.sh` walks all fifteen rows from the table itself, asserts the status and `Location`, and asserts the door is never a destination
2. `scripts/verify-routes.mjs` finds every statically visible `revalidatePath` literal and checks it against the map plus a reasoned public allow-list
3. Both are proved by mutation, each mutation confirmed applied before its result is read
4. `verify:routes`'s current failures are recorded as plan 34-16's worklist, not silenced
5. No test framework, no dependency
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-08-SUMMARY.md` when done.
</output>
