---
phase: 34-one-work-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CAP-02, STAFF-01]

must_haves:
  truths:
    - "A pre-phase row-level permission snapshot exists and can be compared after the phase"
    - "The catalogue↔CAP link of the CAP-02 chain has been run once, by hand, and its output recorded"
    - "The door's behaviour with the network off is recorded BEFORE any middleware edit"
  artifacts:
    - path: ".planning/phases/34-one-work-surface/34-02-SUMMARY.md"
      provides: "The pre-34 baseline location, the verify:capabilities output, and the M-9 before observation with its date"
      min_lines: 40
  key_links:
    - from: "34-02-SUMMARY.md"
      to: "34-17-PLAN.md"
      via: "the post-34 comparison and the M-9 after run read this record"
      pattern: "pre-34"
---

<objective>
Capture the three observations that stop being capturable the moment the phase starts: the
row-level permission baseline, the one link of the CAP-02 chain that needs a live database,
and the door's behaviour with the network off.

Purpose: this phase edits **no migration**, so the baseline comparison in plan 34-17 is very
nearly guaranteed to be green — which is exactly why the "before" must exist, and exactly why
`34-VERIFICATION.md` must later say what that green does **not** prove. And Pitfall 8 is the
reason M-9 runs twice: `/admin/scanner` keeps its address, but the code that judges it is
being rewritten, and *"assuming the door is untouched because nobody edited it"* is the named
trap.

Output: a `pre-34` baseline artefact on disk and a SUMMARY carrying the two observations with
their dates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-VALIDATION.md
@CLAUDE.md
@.claude/rules/meta-gates.md
@.claude/rules/checkin-offline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-34 container baseline and run the CAP-02 chain's first link</name>
  <files>.planning/ (baseline artefacts only — no source file is modified)</files>
  <read_first>
    - scripts/rls-baseline-container.mjs (its usage block at :26-38 — it reads no environment variable and connects only to a container it starts itself, which is why it is the safe target)
    - scripts/rls-baseline-compare.mjs (usage at :19-22)
    - scripts/verify-capabilities.mjs:26-60 and :1030-1053 (side 4 and its stated hand-off to Phase 34's CAP-02)
    - .planning/phases/34-one-work-surface/34-VALIDATION.md § "The container baseline"
  </read_first>
  <action>
    Run `npm run baseline:container -- --phase-point=pre-34`. Use the **container**, never
    `npm run baseline:rls`: B3 writes, and on production it refuses without
    `--i-know-this-writes`, while the container script is safe by construction. If an artefact
    for `pre-34` already exists the script aborts with exit 1 naming `--overwrite` — do **not**
    pass `--overwrite`; a captured artefact is never overwritten. Report the collision instead
    and stop.

    Record in the SUMMARY the exact artefact directory the script printed. Plan 34-17 needs it
    as `--before-dir`.

    Then run `npm run verify:capabilities` and paste its full output into the SUMMARY,
    including the side-4 warning verbatim. This is the link of the CAP-02 chain that needs a
    live database, and there is **no CI in this repository** (`.github/` is absent;
    `package.json` has no test or CI script), so it runs only when a person runs it. Recording
    the pre-phase state here is what makes plan 34-16's re-pointed side-4 message reviewable
    against something.

    If `.env.local` is missing or the database is unreachable, do **not** substitute a green:
    record the failure, its cause, and stop the task. A missing measurement recorded as
    missing is worth more than an assumed one.
  </action>
  <verify>
    <automated>ls -d .planning/**/*pre-34* 2>/dev/null || npm run baseline:container -- --phase-point=pre-34</automated>
  </verify>
  <acceptance_criteria>
    - A baseline artefact directory carrying `pre-34` exists on disk, and its absolute path is written in the SUMMARY
    - The SUMMARY contains the full stdout of `npm run verify:capabilities`, including side 4's warning verbatim
    - No source file under `src/`, `supabase/` or `scripts/` was modified: `git status --porcelain -- src supabase scripts` is empty
    - No migration was applied and `private.role_capabilities` was not edited
  </acceptance_criteria>
  <done>The `pre-34` artefact exists and the catalogue↔`CAP` link has been run once, with its output recorded rather than summarised.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: M-9 before — the door, with the network off</name>
  <files>.planning/phases/34-one-work-surface/34-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/34-one-work-surface/34-VALIDATION.md § "Manual-Only Verifications", row M-9
    - .claude/rules/checkin-offline.md
    - src/app/(admin)/admin/scanner/page.tsx (the gate's own docblock — the coarse role-or-assignment test, and why a stricter test here would be a second refusal in front of the door)
  </read_first>
  <action>
    Execute procedure M-9 exactly as written under `how-to-verify` below, on a real door device,
    and transcribe every observation into `34-02-SUMMARY.md` with today's date. Modify no source
    file. If the door cannot be exercised — no device, no seeded ticket — record that and name
    what therefore becomes unverifiable in plan 34-03. Do not report a pass for something that
    was not observed.
  </action>
  <what-built>
    Nothing yet. This is the baseline observation that plan 34-03's rewrite of the middleware
    will be compared against. It is deliberately the second task of the first wave, before any
    line of routing code changes.
  </what-built>
  <how-to-verify>
    Procedure **M-9 (before)**, from `34-VALIDATION.md`:

    1. On the door device, with the application installed, sign in as a role that holds
       `door.operate` — `master` or `organizer`. Roles only; do not record who.
    2. Open `/admin/scanner`. Confirm it renders and a night can be selected.
    3. Turn the network **off** — aeroplane mode, not just Wi-Fi.
    4. Scan a ticket. Record the outcome shown, and whether the entry queues.
    5. Record: does the bottom navigation render? Which entries?
    6. Record the exact URL in the address bar, including whether anything redirected.

    Write each observation into the SUMMARY with today's date. Roles only, never people. No
    venue, no unannounced date, no line-up.
  </how-to-verify>
  <verify>
    <automated>MISSING — no automated instrument can observe a device with its radio off. The proof is procedure M-9, executed by a person and recorded with its date in 34-02-SUMMARY.md.</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains a dated M-9 (before) record with: the page rendered yes/no, the scan outcome observed, whether the entry queued, the nav entries present, and the final URL
    - The record names a **role**, never a person
    - If the door could not be exercised (no device available, no seeded ticket), the SUMMARY says so explicitly and names what is therefore unverifiable in plan 34-03 — it does not report a pass
  </acceptance_criteria>
  <done>The door's offline behaviour before any routing change is written down, with a date, in terms that plan 34-03's after-run can be compared against line for line.</done>
  <resume-signal>Type "approved" with the observations, or describe what could not be exercised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator device → server | The door's scan path, exercised here with the network deliberately absent |
| local shell → database | `verify:capabilities` and the baseline scripts read privileged catalogues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Tampering | `baseline:rls` run against production instead of the container | mitigate | Only `npm run baseline:container` is used; it reads no environment variable and starts its own throwaway postgres. `baseline:rls` is not invoked in this phase |
| T-34-09 | Tampering | an existing baseline artefact overwritten | mitigate | `--overwrite` is forbidden by this plan; a collision aborts the task and is reported |
| T-34-10 | Information Disclosure | a person named in a published planning document | mitigate | `.planning/` is published. The SUMMARY records roles only, and the acceptance criteria assert it |
| T-34-11 | Repudiation | a green claimed where no measurement was taken | mitigate | The plan requires the failure to be recorded rather than substituted, in both tasks |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed. `34-RESEARCH.md` § Package Legitimacy Audit records the audit as not applicable for this phase |
</threat_model>

<verification>
- The `pre-34` baseline directory exists
- `git status --porcelain -- src supabase scripts` is empty — this plan changes no product code
- The SUMMARY carries both dated observations
- **Not claimed:** a green baseline does not prove that who-reaches-which-address is unchanged. B1 dumps `pg_policies`, B2/B3 are persona matrices, B5 is the advisor — **none of them can see a route.** That sentence belongs in `34-VERIFICATION.md` and is owed by plan 34-17.
</verification>

<success_criteria>
1. A `pre-34` container baseline artefact exists, and its path is recorded for plan 34-17
2. `npm run verify:capabilities`'s full output, including side 4's warning, is recorded verbatim
3. M-9 (before) is executed and its observations recorded with a date, naming roles only
4. No product source file, migration or grant was touched
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-02-SUMMARY.md` when done.
</output>
