---
phase: 34-one-work-surface
plan: 11
type: execute
wave: 3
depends_on: ["34-05", "34-06", "34-07"]
files_modified:
  - src/app/(admin)/admin/(work)/events/page.tsx
  - src/app/(admin)/admin/(work)/events/loading.tsx
  - src/app/(admin)/admin/(work)/events/actions.ts
  - src/app/(admin)/admin/(work)/events/new/page.tsx
  - src/components/events/EventList.tsx
  - src/components/events/EventForm.tsx
  - src/app/(organizer)/organizer/events/page.tsx
  - src/app/(organizer)/organizer/events/loading.tsx
  - src/app/(organizer)/organizer/events/new/page.tsx
autonomous: true
requirements: [STAFF-01, STAFF-03]

must_haves:
  truths:
    - "`/admin/events` and `/admin/events/new` each exist once and open to `organizer.access`"
    - "`EventList` has no `basePath` prop and links only into `/admin/events`"
    - "The master-only controls on the events list are drawn by capability, not by a role string"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/events/page.tsx"
      provides: "The single events surface"
      contains: "ORGANIZER_ACCESS"
    - path: "src/components/events/EventList.tsx"
      provides: "One caller, one value, no prop"
  key_links:
    - from: "src/components/events/EventList.tsx"
      to: "/admin/events/[id]/..."
      via: "six per-event hrefs, now typed as Route"
      pattern: "/admin/events"
---

<objective>
Collapse the `events` and `events/new` pairs, and delete `EventList`'s `basePath` prop.

Purpose: D-34-05 pairs D4 and one of D5..D12. The `events` pair is 118 changed lines; the
organizer version carries a `master.manage` branch for conditional rendering while both mount
`EventList`. And `EventList` takes `basePath?: string` defaulting to `"/organizer/events"`,
which the admin page overrides — **after the collapse the prop has one caller and one value:
delete it and hardcode `/admin/events`.** Its six per-event hrefs at `:163-198` are among the
sites `typedRoutes` flagged in plan 34-01.

Output: two surfaces where four were, and a shared list component with no branch left in it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-06-SUMMARY.md
@CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/venue-secrecy.md
@.claude/rules/meta-gates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff both pairs and classify every difference against a grant</name>
  <files>(no file is modified in this task)</files>
  <read_first>
    - src/app/(admin)/admin/events/page.tsx (its guard and its `EventList basePath` override at :84)
    - src/app/(organizer)/organizer/events/page.tsx (its `organizer.access` + `master.manage` combination)
    - src/app/(admin)/admin/events/new/page.tsx and src/app/(organizer)/organizer/events/new/page.tsx
    - .planning/phases/34-one-work-surface/34-RESEARCH.md § entry D4 and assumption A5 (each pair diffed individually, never assumed from another)
    - .planning/phases/34-one-work-surface/34-09-SUMMARY.md (the classification format plan 34-09 set)
  </read_first>
  <action>
    `diff` both pairs and classify every difference as **drift**, **guard** or **cosmetic**,
    exactly as plan 34-09 did, naming the `private.role_capabilities` row for every drift.

    The one difference that needs a decision rather than a classification is the organizer
    version's `master.manage` branch. `master.manage` is a `scope: "table"` key in the map — it
    gates rows and operations (deleting an event, deleting an artist or a venue, changing
    another member's role), not reachability. So the merged page reaches `organizer.access` and
    **draws** the master-only controls on `master.manage`, held from the same resolved capability
    set. That is the naming rule `keys.ts` states — a key named after its question, never after
    its predicate — applied here: *may this account reach the events surface* and *may this
    account delete an event* are two questions and stay two.

    D-34-06 is the fence: if either merge would need a new grant or a `requires_approved` flip,
    the merged surface takes the more restrictive behaviour and the case becomes a finding.
    Pitfall 6's forbidden sentence — *"the admin version is more complete, so use it"* without a
    grant — must not appear.

    Cross-check `venue-secrecy.md`: the events list renders event data. Confirm by reading that
    neither page nor `EventList` writes `venue_reveal_sent` or renders a venue address for an
    unrevealed night, and record the file:line you read. This surface is staff-only either way,
    but "staff-only so it cannot leak" is an assumption and this project records checks.
  </action>
  <verify>
    <automated>diff "src/app/(admin)/admin/events/page.tsx" "src/app/(organizer)/organizer/events/page.tsx"; diff "src/app/(admin)/admin/events/new/page.tsx" "src/app/(organizer)/organizer/events/new/page.tsx"; true</automated>
  </verify>
  <acceptance_criteria>
    - Every difference in both pairs is classified in the SUMMARY as drift, guard or cosmetic
    - The `master.manage` branch has an explicit decision recorded: reachability on `organizer.access`, control visibility on `master.manage`, both from the same resolved set
    - The venue-secrecy cross-check is recorded with the file:line read
    - No file was modified: `git status --porcelain` is empty
  </acceptance_criteria>
  <done>Both pairs are measured and every difference has a verdict backed by a grant or by a naming rule.</done>
</task>

<task type="auto">
  <name>Task 2: Merge both pairs and delete EventList's basePath</name>
  <files>src/app/(admin)/admin/(work)/events/**, src/components/events/{EventList,EventForm}.tsx, src/app/(organizer)/organizer/events/{page.tsx,loading.tsx,new/page.tsx}</files>
  <read_first>
    - The Task 1 classification
    - src/components/events/EventList.tsx:18-35 (the `basePath?: string` prop and its `/organizer/events` default) and :160-200 (the six per-event hrefs)
    - src/components/events/EventForm.tsx (its imports of the artists and venues actions, rewritten by plan 34-07 — confirm they point at `(admin)` before changing anything else)
    - src/app/(admin)/admin/(work)/layout.tsx
    - .planning/phases/34-one-work-surface/34-06-SUMMARY.md (the route-group arrangement)
  </read_first>
  <action>
    `git mv` `src/app/(admin)/admin/events/page.tsx`, `loading.tsx`, `actions.ts` (put there by
    plan 34-07) and `new/page.tsx` into `src/app/(admin)/admin/(work)/events/`, following the
    recorded arrangement. Leave `events/[id]/` alone — plans 34-12, 34-13 and 34-14 own its
    children, and 34-06 already placed `assignments` and `review`.

    In the merged `events/page.tsx`:
    - Guard on `CAP.ORGANIZER_ACCESS`.
    - Draw the master-only controls on `capabilities.has(CAP.MASTER_MANAGE)`, taken from the
      resolved set the page already has. Not on a role string, and not on a second query.
    - Delete the nav mounts, their imports and the `role as UserRole` / `status as UserStatus`
      casts.
    - Drop the `basePath` argument from the `<EventList>` call.

    In the merged `events/new/page.tsx`: guard on `CAP.ORGANIZER_ACCESS`, delete the mounts and
    the casts.

    In `EventList.tsx`: delete the `basePath` prop from the interface, from the destructuring
    and from the default, and replace every `${basePath}` in the six hrefs with the literal
    `/admin/events`. Type the hrefs as `Route` so `typedRoutes` holds them — that is the
    documented pattern and it turns a future stale link into a build error rather than a 404.
    `EventList` has exactly one caller after this plan; a prop with one caller and one value is
    an option nobody has, and options nobody has are where the next divergence starts.

    In `EventForm.tsx`: nothing changes except confirming plan 34-07's two rewritten import
    specifiers are correct. If they are not, fix them and say so — the file is on the boundary
    between two plans and is the one most likely to be half-done.

    Delete `src/app/(organizer)/organizer/events/page.tsx`, its `loading.tsx`, and
    `new/page.tsx`.

    Do **not** touch `actions.ts`'s `revalidatePath` calls — seven of them name
    `/organizer/events`, `/admin/events`, `/events` and `` `/events/${slug}` ``, and they are
    plan 34-16's, together with the sixteen in the members actions. Deleting the wrong half of a
    pair has no detector, and `scripts/verify-routes.mjs` (plan 34-08) is the worklist.

    No restyle. Phases 40 and 41.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)" -path '*events/page.tsx'` returns one path, and likewise for `events/new/page.tsx`
    - `src/app/(organizer)/organizer/events/page.tsx`, its `loading.tsx` and `new/page.tsx` no longer exist
    - `grep -c "basePath" src/components/events/EventList.tsx` returns 0
    - `grep -c "/organizer" src/components/events/EventList.tsx` returns 0
    - Both merged pages guard on `ORGANIZER_ACCESS`; the master-only controls are conditioned on `MASTER_MANAGE` and not on a role string: `grep -c 'role === "master"' <merged events page>` returns 0
    - `grep -rc "StaffNav\|MobileNav\|as UserRole\|as UserStatus"` returns 0 in both merged pages
    - `git diff -M` shows no hunk changing a `revalidatePath` argument in `events/actions.ts`
    - `rm -rf .next && npm run build` exits 0
    - Behaviour: `curl -sI http://localhost:3000/organizer/events` against `npm run dev` redirects to `/admin/events`
  </acceptance_criteria>
  <done>Two surfaces exist once each, the shared list has one destination and no prop, and the master-only controls are drawn by the key that governs them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → page | Same key, same address (D-34-09) |
| page → event actions | `master.manage` re-checks inside the actions are the real boundary for deletion |
| staff surface → venue data | An events surface is a place a reveal could be advanced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-53 | Elevation of Privilege | master-only controls drawn for an organizer | mitigate | Drawn on `capabilities.has(CAP.MASTER_MANAGE)` from the resolved set, not on a role string; the deleting actions re-check `master.manage` themselves and are untouched |
| T-34-54 | Elevation of Privilege | reachability conflated with deletion rights | mitigate | Two keys, two questions: `organizer.access` opens the address, `master.manage` draws the control. `keys.ts`'s naming rule cited in the SUMMARY |
| T-34-55 | Information Disclosure | a collapsed event surface advancing a venue reveal | mitigate | Task 1 records the check with the file:line read; neither page nor `EventList` writes `venue_reveal_sent` |
| T-34-56 | Information Disclosure | a stale `/organizer/events` link surviving in the shared list | mitigate | `basePath` deleted, hrefs typed as `Route`, `grep -c "/organizer"` asserted 0 |
| T-34-57 | Repudiation | a members/events list that stops refreshing | transfer | `revalidatePath` deliberately untouched here; owned by plan 34-16 and observed in M-8 |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; audit not applicable for this phase |
</threat_model>

<verification>
- `rm -rf .next && npm run build` exits 0
- `npm run verify:persona` exits 0
- Old addresses redirect, checked with `curl -sI`
- **Not claimed:** that an organizer sees the right controls. M-1 and M-2 observe that, in plan 34-17.
</verification>

<success_criteria>
1. `events` and `events/new` each exist once, on `organizer.access`
2. `EventList` has no `basePath` and no `/organizer` literal, and its hrefs are typed
3. Master-only controls are conditioned on `master.manage`, never on a role string
4. Every difference in both pairs is classified against a grant in the SUMMARY
5. No `revalidatePath` argument changed
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-11-SUMMARY.md` when done.
</output>
