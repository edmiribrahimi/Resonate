---
phase: 34-one-work-surface
plan: 09
type: execute
wave: 4
depends_on: ["34-05", "34-06", "34-07"]
files_modified:
  # Route files only — R-WORK-ROUTES. `admin/{artists,venues}/actions.ts` stay where plan
  # 34-07 put them, outside `(work)`, and are not touched by this plan at all.
  - src/app/(admin)/admin/(work)/artists/page.tsx
  - src/app/(admin)/admin/(work)/venues/page.tsx
  - src/app/(organizer)/organizer/artists/page.tsx
  - src/app/(organizer)/organizer/venues/page.tsx
autonomous: true
requirements: [STAFF-01]

must_haves:
  truths:
    - "`/admin/artists` and `/admin/venues` each exist as one file and open to `organizer.access`"
    - "An organizer reaches both, exactly as it reached `/organizer/artists` and `/organizer/venues` before"
    - "Neither surface advances a venue reveal"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/artists/page.tsx"
      provides: "The single artists surface"
      contains: "ORGANIZER_ACCESS"
    - path: "src/app/(admin)/admin/(work)/venues/page.tsx"
      provides: "The single venues surface"
      contains: "ORGANIZER_ACCESS"
  key_links:
    - from: "src/app/(admin)/admin/(work)/artists/page.tsx"
      to: "src/lib/routes/capability-routes.ts"
      via: "the guard asks the same key the map binds to /admin/artists"
      pattern: "ORGANIZER_ACCESS"
---

<objective>
Collapse the first two of the twelve duplicated pairs — `artists` and `venues` — into one file
each, at `/admin/artists` and `/admin/venues`, opening to `organizer.access`.

Purpose: D-34-05 and the first two entries of the research's divergence table. Both pairs are
byte-identical except for import order, the function name, the capability in the guard, the
`<h1>`, the `StaffNav context` prop and one comment — 31 changed lines each, **no body
difference**. They are the cleanest test of the collapse and they go first for that reason.

**The grant that decides each merge, named as D-34-05 requires:**
- **D1 — `artists` → `organizer.access`.** Decided by the row `('organizer','organizer.access',false)`
  in `private.role_capabilities`. This is **not a widening**: an organizer reaches the identical
  page today at `/organizer/artists`, and the diff proves the two files render the same body.
- **D2 — `venues` → `organizer.access`.** Same row, same reasoning. The admin file carries a
  comment warning that `catalogue.manage` *"would additionally widen ADDRESS visibility to every
  approved organizer"* — but the diff proves the organizer page already renders the same
  component, so **an organizer already sees venue addresses today**. Collapsing widens nothing.
  This was verified as neutral, not assumed.

Output: two files where four were, and the two `(organizer)` originals deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-06-SUMMARY.md
@CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/venue-secrecy.md
@.claude/rules/meta-gates.md
</context>

<phase_rule>
**R-WORK-ROUTES — only Next.js route files enter `(work)`.** Declared in plan 34-07, Task 1,
and cited here rather than re-derived. `page.tsx` and `loading.tsx` move into
`src/app/(admin)/admin/(work)/…`; `actions.ts` and co-located client components **stay** at
their `src/app/(admin)/admin/…` path, outside the group, exactly where plan 34-07 put them.
A moved page reaches its former siblings with an absolute `@/app/(admin)/admin/…` specifier —
an edit inside the page, owned by this plan and by no other.

The rule is what keeps this wave parallel. A route group governs routing and nothing else, so
a non-route module gains nothing by entering `(work)`, while moving it changes its specifier
for **every external importer** — and those importers are files owned by sibling plans in this
same wave. In parallel worktrees a sibling's `git mv` is invisible to `grep`, so the outcome
is either a merge conflict or a clean merge pointing at a file that no longer exists.
</phase_rule>

<tasks>

<task type="auto">
  <name>Task 1: Diff both pairs before merging either, and record what each difference was</name>
  <files>(no file is modified in this task — it is the measurement the merge rests on)</files>
  <read_first>
    - src/app/(admin)/admin/artists/page.tsx (all of it — the `ADMIN_ACCESS` guard at :20 and the comment at :14-19 explaining why it is not `CATALOGUE_MANAGE`)
    - src/app/(organizer)/organizer/artists/page.tsx
    - src/app/(admin)/admin/venues/page.tsx
    - src/app/(organizer)/organizer/venues/page.tsx
    - .planning/phases/34-one-work-surface/34-RESEARCH.md § "What actually differs, pair by pair, and the grant that decides it" (entries D1 and D2)
  </read_first>
  <action>
    Run `diff` on each pair and read the output in full. Research assumption A5 is explicit that
    each pair must be diffed **individually**, not assumed from another pair, and this is the
    plan that sets the precedent for the five that follow.

    For each of the two pairs, write into the SUMMARY one line per difference, and for each
    difference one of exactly three verdicts:
    - **drift** — the two versions disagree and neither is intentional; the merged file takes
      the behaviour the grant supports, and the grant row is named.
    - **guard** — the difference is the capability asked; the merged file asks the one the map
      binds to that address.
    - **cosmetic** — heading, function name, import order, `context` prop.

    D-34-06 is the fence on this task: a divergence may only be resolved **towards more** when an
    existing grant already says so. If either pair turns out to hold a difference that would need
    a new grant or a `requires_approved` flip to merge, the merged surface takes **the more
    restrictive** of the two behaviours and the case goes into the SUMMARY as a finding.
    Widening a policy to make a collapse pass is forbidden. Pitfall 6's warning sign is a plan
    line that says *"the admin version is more complete, so use it"* without naming a grant —
    do not write that line.

    Cross-check `venue-secrecy.md` explicitly and record the result rather than the intent: the
    venues surface renders `venues.address`. Confirm by reading that neither file touches
    `venue_reveal_sent` and that neither is a public surface, so no reveal can be advanced by
    this merge. Record it as **checked**, with the file:line you read.
  </action>
  <verify>
    <automated>diff "src/app/(admin)/admin/artists/page.tsx" "src/app/(organizer)/organizer/artists/page.tsx"; diff "src/app/(admin)/admin/venues/page.tsx" "src/app/(organizer)/organizer/venues/page.tsx"; true</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains, per pair, every difference from the `diff` output classified as drift, guard or cosmetic
    - Every difference classified as drift names the `private.role_capabilities` row that decided it
    - The SUMMARY records the venue-secrecy cross-check with the file:line read, as a checked result
    - No file was modified by this task: `git status --porcelain` is empty
  </acceptance_criteria>
  <done>Both pairs are measured, every difference has a verdict, and no verdict rests on "the fuller version wins".</done>
</task>

<task type="auto">
  <name>Task 2: Merge both pairs onto organizer.access under the work layout</name>
  <files>src/app/(admin)/admin/(work)/{artists,venues}/page.tsx, src/app/(organizer)/organizer/{artists,venues}/page.tsx</files>
  <read_first>
    - The SUMMARY notes from Task 1
    - src/app/(admin)/admin/(work)/layout.tsx (what it now provides — both navs and the resolved context)
    - src/app/(admin)/admin/{artists,venues}/actions.ts (moved here by plan 34-07 — read them only to confirm their exported names; they stay at this path and this plan does not edit them)
    - .planning/phases/34-one-work-surface/34-06-SUMMARY.md (which route-group arrangement the compiler accepted — follow it)
    - src/lib/routes/capability-routes.ts (`organizer.access`'s binding for `/admin/artists` and `/admin/venues`)
  </read_first>
  <action>
    `git mv` **`src/app/(admin)/admin/artists/page.tsx` and
    `src/app/(admin)/admin/venues/page.tsx` only**, into
    `src/app/(admin)/admin/(work)/{artists,venues}/`, following the arrangement recorded in
    `34-06-SUMMARY.md`.

    **`admin/artists/actions.ts` and `admin/venues/actions.ts` do not move** — R-WORK-ROUTES.
    They are not routes, so `(work)` governs nothing about them, and moving them would change
    the specifier in six files this plan does not own, measured 2026-08-09:
    `components/artists/{CreateArtistModal.tsx:5,EditArtistButton.tsx:5}`,
    `components/venues/{CreateVenueModal.tsx:5,EditVenueButton.tsx:5}` and
    `components/events/EventForm.tsx:11-12` — and `EventForm.tsx` belongs to plan 34-11, in
    this same wave. Plan 34-07 already pointed all six at `@/app/(admin)/admin/…`, and under
    this rule those six lines are correct and final.

    Rewrite the moved pages' relative `./actions` imports, if any, to
    `@/app/(admin)/admin/{artists,venues}/actions`.

    In each merged page:
    - Change the guard from `CAP.ADMIN_ACCESS` to `CAP.ORGANIZER_ACCESS`. Replace the comment
      above it: the current one argues why `ADMIN_ACCESS` and not `CATALOGUE_MANAGE`, and after
      the collapse that argument is about a capability the page no longer asks. Write the new
      one to the measured truth — `organizer.access` is what the map binds to this address, an
      organizer already reached the identical page at the `/organizer` twin, and
      `catalogue.manage` is still the key the **actions** re-ask, which is a different question
      from reachability.
    - Delete the `<StaffNav>` and `<MobileNav>` mounts, their imports, the
      `role as UserRole` / `status as UserStatus` casts and the `@/types/database` import if
      nothing else uses it. The layout provides both. `capabilities/server.ts:200-208` names
      Phase 34 as the owner of removing `role` and `status` from the payload's consumers —
      **record in the SUMMARY which of the two you did**: dropped them from the consumers here,
      or left the SQL payload's keys alone (removing those is a migration, and this phase writes
      none).
    - Set the `<h1>` to the surface's own name rather than the tree's. The heading collapse
      (`Admin` versus `Organizer`) is decided here by necessity; the vocabulary question belongs
      to Phases 40 and 41 and must not be pre-empted with a redesign.

    Delete `src/app/(organizer)/organizer/artists/page.tsx` and
    `src/app/(organizer)/organizer/venues/page.tsx`.

    No visual change beyond the heading: no token, no typography, no spacing, no component
    restyle. There is deliberately no `UI-SPEC.md` for this phase.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build &amp;&amp; npm run verify:persona</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)" -path '*artists*' -name page.tsx | wc -l` returns 1, and likewise for venues
    - `src/app/(admin)/admin/artists/actions.ts` and `src/app/(admin)/admin/venues/actions.ts` still exist at those exact paths — outside `(work)` — and no `(work)/…/actions.ts` was created for either
    - `src/components/events/EventForm.tsx` is **not** in this plan's diff: `git diff --name-only` does not list it. It is plan 34-11's file, in this same wave, and under R-WORK-ROUTES nothing this plan does can make it stale
    - `src/app/(organizer)/organizer/artists/` and `.../venues/` no longer contain a `page.tsx`
    - Both merged pages guard on `ORGANIZER_ACCESS`: `grep -c "ORGANIZER_ACCESS"` returns at least 1 in each
    - `grep -rc "StaffNav\|MobileNav\|as UserRole\|as UserStatus"` returns 0 in both merged pages
    - Both `actions.ts` files are unchanged by this plan **in contents and in path**: `git diff -M --stat` shows neither a hunk nor a rename for either. (The earlier form of this criterion said only "unchanged", which was true of the contents and false of the module graph — under R-WORK-ROUTES both are now true)
    - `rm -rf .next && npm run build` exits 0 and `npm run verify:persona` exits 0
    - Behaviour, against `npm run dev`: `curl -sI http://localhost:3000/organizer/artists` redirects to `/admin/artists`
    - Behaviour (manual, owed to plan 34-17): **M-2** must observe an `organizer`/`approved` account rendering both surfaces
  </acceptance_criteria>
  <done>Two surfaces exist once each, open to the capability the map binds, mount nothing by hand, and their actions are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → page | Both must ask the same key for the same address (D-34-09) |
| page → catalogue actions | `catalogue.manage` re-checks inside the actions are the real write boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-43 | Elevation of Privilege | the merged surface bound wider than either original | mitigate | `organizer.access` is the key the organizer twin already asked; the grant row is named in the SUMMARY; D-34-06 forbids resolving towards more without one |
| T-34-44 | Information Disclosure | venue addresses reaching a wider audience | mitigate | Verified by diff that the organizer twin already renders the same component, so the audience is unchanged; neither file touches `venue_reveal_sent`; recorded as checked with a file:line |
| T-34-45 | Elevation of Privilege | an action's `catalogue.manage` check weakened while merging | mitigate | Both `actions.ts` files asserted unchanged in the diff |
| T-34-46 | Repudiation | a divergence resolved by preference rather than by grant | mitigate | Task 1 classifies every difference and names a grant row for each drift; Pitfall 6's forbidden sentence is named |
| T-34-46b | Denial of Service | the catalogue actions made unimportable for six files owned by sibling plans | mitigate | R-WORK-ROUTES: `admin/{artists,venues}/actions.ts` do not enter `(work)`. Asserted by `git diff --name-only` not listing `EventForm.tsx` and by both action files showing no rename |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; audit not applicable for this phase |
</threat_model>

<verification>
- `rm -rf .next && npm run build` exits 0
- `npm run verify:persona` exits 0 — `src/app/(organizer)/**` still matches remaining files, so check A is not yet due
- Old addresses redirect, checked with `curl -sI`
- **Not claimed:** that an organizer can use these surfaces. That is M-2, and it is owed to plan 34-17.
</verification>

<success_criteria>
1. `artists` and `venues` each exist as one page, bound to `organizer.access`
2. Every difference between each pair is classified in the SUMMARY, with a grant row named for every drift
3. Neither merged page mounts a nav or casts a role, and neither `actions.ts` moved
4. The venue-secrecy cross-check is recorded as a checked result, with the line read
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-09-SUMMARY.md` when done.
</output>
