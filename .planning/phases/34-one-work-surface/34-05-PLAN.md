---
phase: 34-one-work-surface
plan: 05
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/app/(admin)/admin/(work)/layout.tsx
  - src/app/(admin)/admin/page.tsx
  - src/app/(admin)/admin/analytics/page.tsx
  - src/app/(admin)/admin/analytics/loading.tsx
  - src/app/(admin)/admin/analytics/compare/page.tsx
  - src/app/(admin)/admin/analytics/compare/loading.tsx
  - src/app/(admin)/admin/analytics/members/page.tsx
  - src/app/(admin)/admin/analytics/members/loading.tsx
  - src/app/(admin)/admin/newsletter/page.tsx
  - src/app/(admin)/admin/newsletter/actions.ts
  - src/app/(admin)/admin/newsletter/NewsletterClient.tsx
  - src/app/(admin)/admin/newsletter/ComposeForm.tsx
  - src/app/(admin)/admin/newsletter/BroadcastList.tsx
  - src/app/(admin)/admin/newsletter/FailureNotice.tsx
  - src/app/(admin)/admin/finance/page.tsx
  - src/app/(admin)/admin/finance/loading.tsx
  - src/app/(admin)/admin/finance/actions.ts
autonomous: true
requirements: [STAFF-01, STAFF-03]

must_haves:
  truths:
    - "A work surface resolves the viewer's access context once for the whole tree, not once per page"
    - "`/admin` refuses a visitor who holds no work-surface capability, before its redirect runs"
    - "The door is outside the layout and its page file is untouched"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/layout.tsx"
      provides: "One access-context resolution and both navs for every work surface"
      contains: "getAccessContext"
    - path: "src/app/(admin)/admin/(work)/page.tsx"
      provides: "The `/admin` root, now with a gate of its own (finding F2)"
  key_links:
    - from: "src/app/(admin)/admin/(work)/layout.tsx"
      to: "src/lib/capabilities/server.ts"
      via: "getAccessContext(), cache()-scoped per request"
      pattern: "getAccessContext"
    - from: "src/app/(admin)/admin/(work)/layout.tsx"
      to: "src/components/staff/StaffNav.tsx"
      via: "capabilities passed as props"
      pattern: "StaffNav"
---

<objective>
Create the layout for the collapsed work surface — one access-context resolution, both navs
mounted once — and move the four master-only surfaces under it. Give `/admin` a gate of its
own.

Purpose: D-34-07. There is no `layout.tsx` in either route group today, so every page repeats
the mount and the `role as UserRole` cast the code already flags as this phase's to remove.
And finding F2: `(admin)/admin/page.tsx` is a bare `redirect()` with **no gate at all**, held
up solely by the `/admin/*` prefix rule that D-34-02 dissolves — closing that is not optional,
it is a consequence of the collapse.

**Why a nested `(work)` route group and not `admin/layout.tsx`.** A layout at
`src/app/(admin)/admin/layout.tsx` would wrap `/admin/scanner` too, and would put a tab bar
and a second bottom nav on the door — which means editing
`src/app/(admin)/admin/scanner/page.tsx` to remove its own mount. This phase does not touch
the door's page. A route group does not affect the URL, so
`src/app/(admin)/admin/(work)/analytics/page.tsx` still serves `/admin/analytics`, and
`scanner/` stays outside the group, byte-identical. It also pre-positions Phase 39: when the
door leaves for its own address, it is already structurally separate.

Output: `(work)/layout.tsx`, four surfaces moved under it with their hand mounts removed, and
`/admin` bound to `organizer.access`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/nextjs-architecture.md
@.claude/rules/access-gating.md
@.claude/rules/meta-gates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The work-surface layout, and /admin gets a gate of its own</name>
  <files>src/app/(admin)/admin/(work)/layout.tsx, src/app/(admin)/admin/(work)/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/page.tsx (all 4 lines — a bare `redirect("/admin/events")` with no gate; this is finding F2)
    - src/lib/capabilities/server.ts:255-345 (`AccessContextResult`, `ANONYMOUS_CONTEXT`, and `getAccessContext` — note it is `cache()`-scoped, so a layout resolving once costs one round trip for the whole tree, and note the `capabilities.resolve_failed` throw at :338)
    - src/lib/capabilities/guards.ts:161-192 (`assertStaffManage` — the existing page-guard shape to follow)
    - src/lib/routes/capability-routes.ts (written by 34-01)
    - src/app/(admin)/admin/artists/page.tsx:11-42 (the per-page pattern being replaced: guard, two casts, two hand mounts)
  </read_first>
  <action>
    Create `src/app/(admin)/admin/(work)/layout.tsx` as a Server Component. It calls
    `getAccessContext()` **once** and renders `<StaffNav capabilities={…} />`,
    `{children}` and `<MobileNav role={…} status={…} />`.

    Two things about that call that must be stated in the layout's docblock rather than
    assumed:
    - `getAccessContext` is `cache()`-scoped per request, so a page below that also calls it
      costs **no second round trip**. That matters because the same middleware path serves the
      door, and this project counts round trips on bad networks.
    - It **throws** `capabilities.resolve_failed: <code>` and that must stay a throw
      (D-34-08 state 3). **Do not wrap it in `try/catch`.** An infrastructure fault dressed as
      a permission denial is a silent failure with an alibi, and there is no error tracking
      here to contradict it.

    `MobileNav` keeps its `role` / `status` props — plan 34-04 deliberately did not change its
    signature, because it is mounted on 44 pages including the door's. The layout supplies them
    from the resolved context. `StaffNav` takes the capability key array, as rewritten by
    34-04.

    The layout does **not** contain a capability guard. Each page keeps its own — D-34-09: the
    middleware and the page give the same verdict because they read the same entry, and a
    layout-level guard would be a third place asking a question that already has two askers.
    Say so in the docblock.

    Then `git mv src/app/(admin)/admin/page.tsx "src/app/(admin)/admin/(work)/page.tsx"` and
    give it the gate it has never had: resolve the access context, refuse with
    `redirect("/dashboard")` when `organizer.access` is absent, then `redirect("/admin/events")`
    as before. `organizer.access` is the binding plan 34-01 gave `/admin` in the map, and it is
    the least of the capabilities any collapsed surface needs — an unentitled visitor is
    refused before the redirect runs. Cite finding F2 in a comment: this page was held up
    solely by `middleware.ts`'s `/admin/*` rule, and that rule no longer exists.

    Do not restyle anything. No token, no typography, no spacing, no component restyle — Phases
    40 and 41. The layout's markup is the two mounts and `{children}`, in the order the pages
    used.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(admin)/admin/(work)/layout.tsx` exists and calls `getAccessContext()` exactly once
    - `grep -c "try {" "src/app/(admin)/admin/(work)/layout.tsx"` returns 0
    - `src/app/(admin)/admin/page.tsx` no longer exists; `src/app/(admin)/admin/(work)/page.tsx` does
    - `(work)/page.tsx` refuses when `organizer.access` is absent, and its comment cites finding F2
    - `src/app/(admin)/admin/scanner/page.tsx` is not in this plan's diff: `git diff --name-only` does not list it
    - The layout's docblock states that `getAccessContext` is `cache()`-scoped, that it throws rather than degrading, and that the page guard is not replaced by the layout
    - `rm -rf .next && npm run build` exits 0, and `/admin` still resolves (route groups do not affect the URL)
  </acceptance_criteria>
  <done>One resolution and one pair of navs serve the whole work surface, and `/admin` refuses somebody it used to admit by accident.</done>
</task>

<task type="auto">
  <name>Task 2: The four master-only surfaces move under the layout and stop mounting their own navigation</name>
  <files>src/app/(admin)/admin/analytics/**, src/app/(admin)/admin/newsletter/**, src/app/(admin)/admin/finance/**</files>
  <read_first>
    - src/app/(admin)/admin/analytics/page.tsx and its `loading.tsx` (the `<StaffNav … context="admin" />` mount in both)
    - src/app/(admin)/admin/finance/page.tsx, loading.tsx, actions.ts
    - src/app/(admin)/admin/newsletter/page.tsx and its four co-located client files
    - src/app/(admin)/admin/analytics/compare/{page,loading}.tsx and analytics/members/{page,loading}.tsx
    - src/app/(admin)/admin/(work)/layout.tsx (written in Task 1 — what it now provides)
  </read_first>
  <action>
    `git mv` these four surfaces, with every co-located file, into
    `src/app/(admin)/admin/(work)/`:
    `analytics/` (page, loading, `compare/`, `members/`), `newsletter/` (page, `actions.ts`,
    `NewsletterClient.tsx`, `ComposeForm.tsx`, `BroadcastList.tsx`, `FailureNotice.tsx`),
    `finance/` (page, loading, `actions.ts`). Use `git mv` so the history follows the file.

    The addresses do not change: a route group does not affect the URL. `/admin/analytics`,
    `/admin/analytics/compare`, `/admin/analytics/members`, `/admin/newsletter` and
    `/admin/finance` are all still exactly those. Confirm it after the move rather than
    assuming it.

    In each moved `page.tsx` and each moved `loading.tsx`, delete the `<StaffNav …>` and
    `<MobileNav …>` mounts, their imports, and the `role as UserRole` / `status as UserStatus`
    casts and their `@/types/database` import where nothing else uses them. The layout provides
    both now. **Keep each page's own capability guard** — all four gate on `admin.access` today
    and all four keep it: D-34-09 wants the page and the middleware to agree, not for one of
    them to stop asking.

    Do **not** move `members/`. Its whole tree — `page.tsx`, `actions.ts`,
    `CreateAccountForm.tsx`, `MemberActionNotice.tsx`, `loading.tsx`, `growth/`, `register/` —
    moves in one piece in plan 34-10, which is the plan that collapses the `members` pair. It
    would otherwise be split across two plans that share a directory, and `members/register` is
    the folded todo's address.

    Do **not** move `events/` or `artists/` or `venues/` or `scanner/`. The first three move
    with their collapse plans in Wave 3; the door does not move at all.

    Change nothing visual: no token, no typography, no spacing, no component restyle.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)/admin/(work)" -name page.tsx | wc -l` returns 6 (`/admin`, analytics ×3, newsletter, finance)
    - `grep -rc "StaffNav\|MobileNav" "src/app/(admin)/admin/(work)"/{analytics,newsletter,finance} --include='*.tsx'` returns 0 for every file
    - `grep -rc "as UserRole\|as UserStatus" "src/app/(admin)/admin/(work)"/{analytics,newsletter,finance}` returns 0
    - Every moved page still contains its `admin.access` guard: `grep -rl "ADMIN_ACCESS" "src/app/(admin)/admin/(work)"` lists all six pages
    - `src/app/(admin)/admin/members` and `src/app/(admin)/admin/events` still exist unmoved, and `src/app/(admin)/admin/scanner` is untouched
    - `git log --follow --oneline -1 -- "src/app/(admin)/admin/(work)/finance/page.tsx"` shows history from before the move — the move used `git mv`
    - `rm -rf .next && npm run build` exits 0
    - Behaviour, against `npm run dev`: `curl -sI http://localhost:3000/admin/finance` returns a redirect to `/login` (unauthenticated), not a 404 — the address survived the group
  </acceptance_criteria>
  <done>Four master-only surfaces sit under the layout, at the same addresses, mounting nothing by hand, and still asking `admin.access` for themselves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout → page | The layout resolves the context; the page still decides admission |
| route group → URL | A group that silently changed an address would break every already-sent link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-23 | Elevation of Privilege | `/admin` admitting an unentitled visitor | mitigate | `(work)/page.tsx` gains a guard on `organizer.access`, closing finding F2 before D-34-02 removes the prefix rule holding it up |
| T-34-24 | Elevation of Privilege | a page losing its own guard because the layout "handles it" | mitigate | Every moved page keeps its `admin.access` guard; asserted by `grep -rl "ADMIN_ACCESS"` listing all six |
| T-34-25 | Denial of Service *at a queue* | a layout wrapping the door | mitigate | The door sits outside the `(work)` group; `scanner/page.tsx` is asserted absent from the diff |
| T-34-26 | Repudiation | a resolution failure turned into a refusal by the layout | mitigate | No `try/catch` in the layout; asserted by `grep` |
| T-34-27 | Information Disclosure | an address changed by the route group without anyone noticing | mitigate | A `curl -sI` behaviour assertion per moved surface, plus the `find … | wc -l` census |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; audit not applicable for this phase |
</threat_model>

<verification>
- `rm -rf .next && npm run build` exits 0
- `npm run verify:persona` still green — this plan deletes no glob (`src/app/(admin)/**` still matches)
- Every moved address still answers, checked with `curl -sI` against `npm run dev`
- **Not claimed:** that the layout protects anything. It resolves and it draws. The middleware is UX and RLS is the boundary.
</verification>

<success_criteria>
1. `src/app/(admin)/admin/(work)/layout.tsx` exists, resolves the access context once, mounts both navs, and wraps no `try/catch` around the resolution
2. `/admin` has a capability guard of its own for the first time, bound to `organizer.access`
3. Six pages sit under `(work)/` with no hand-mounted navigation and no `UserRole` cast, each keeping its own `admin.access` guard
4. Every moved address is unchanged, proved by request and not by inspection
5. `scanner/`, `members/`, `events/`, `artists/` and `venues/` are untouched by this plan
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-05-SUMMARY.md` when done.
</output>
