---
phase: 34-one-work-surface
plan: 12
type: execute
wave: 3
depends_on: ["34-05", "34-06", "34-07"]
files_modified:
  - src/app/(admin)/admin/(work)/events/[id]/edit/page.tsx
  - src/app/(admin)/admin/(work)/events/[id]/drinks/page.tsx
  - src/app/(admin)/admin/(work)/events/[id]/drinks/DrinkMenuManager.tsx
  - src/app/(organizer)/organizer/events/[id]/edit/page.tsx
  - src/app/(organizer)/organizer/events/[id]/drinks/page.tsx
autonomous: true
requirements: [STAFF-01]

must_haves:
  truths:
    - "`/admin/events/<id>/edit` and `/admin/events/<id>/drinks` each exist once and open to `organizer.access`"
    - "The drinks menu's closing time and token behaviour are unchanged by the merge"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/events/[id]/edit/page.tsx"
      provides: "The single event edit surface"
      contains: "ORGANIZER_ACCESS"
    - path: "src/app/(admin)/admin/(work)/events/[id]/drinks/page.tsx"
      provides: "The single drinks surface"
      contains: "ORGANIZER_ACCESS"
  key_links:
    - from: "src/app/(admin)/admin/(work)/events/[id]/drinks/DrinkMenuManager.tsx"
      to: "src/app/(admin)/admin/(work)/events/actions.ts"
      via: "the drink menu actions at their post-move path"
      pattern: "admin/events/actions"
---

<objective>
Collapse the `events/[id]/edit` and `events/[id]/drinks` pairs.

Purpose: two of the eight `events/[id]/*` pairs D-34-05 covers, grouped because they are the two
smallest and because `drinks` carries a shared client component (`DrinkMenuManager`) that a
public surface also mounts — `(public)/events/[slug]/menu/PartyDrinkMenu.tsx` — which makes it
the one in this group that can reach a paying visitor if it is merged carelessly.

Output: two surfaces where four were.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-06-SUMMARY.md
@.planning/phases/34-one-work-surface/34-09-SUMMARY.md
@CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/ticketing-payments.md
@.claude/rules/meta-gates.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diff both pairs and classify every difference against a grant</name>
  <files>(no file is modified in this task)</files>
  <read_first>
    - src/app/(admin)/admin/events/[id]/edit/page.tsx and src/app/(organizer)/organizer/events/[id]/edit/page.tsx
    - src/app/(admin)/admin/events/[id]/drinks/page.tsx and src/app/(organizer)/organizer/events/[id]/drinks/page.tsx
    - src/app/(admin)/admin/events/[id]/drinks/DrinkMenuManager.tsx (moved there by plan 34-07 — and note its other mount, `(public)/events/[slug]/menu/PartyDrinkMenu.tsx`)
    - .planning/phases/34-one-work-surface/34-RESEARCH.md § D5..D12 and assumption A5 — diff sizes 32–140 lines, each pair holding page-specific drift that must be read pair by pair, never assumed from `artists`/`venues`
    - .planning/phases/34-one-work-surface/34-09-SUMMARY.md (the classification format)
  </read_first>
  <action>
    `diff` both pairs. Classify every difference as **drift**, **guard** or **cosmetic**, naming
    the `private.role_capabilities` row for every drift. Assumption A5 is explicit: these are
    **not** the `artists` shape, and the plan that assumes they are is the plan that ships a
    behavioural divergence as a heading change.

    Pay specific attention on the `drinks` pair to anything touching money or a token: the drink
    menu's closing time (`menu_closes_at` on `event_parties`), the token grace period after
    close, and the redeem path. `ticketing-payments.md` governs those, and a difference there is
    not a divergence to resolve by preference — it is a behavioural difference that must be
    named, decided against a grant, and recorded. If either version differs in *what happens to
    a token*, stop and report rather than merging: that is a money path, and money paths are
    Critical.

    D-34-06 fence unchanged: resolve towards more only where an existing grant already says so;
    otherwise take the more restrictive behaviour and record a finding.
  </action>
  <verify>
    <automated>diff "src/app/(admin)/admin/events/[id]/edit/page.tsx" "src/app/(organizer)/organizer/events/[id]/edit/page.tsx"; diff "src/app/(admin)/admin/events/[id]/drinks/page.tsx" "src/app/(organizer)/organizer/events/[id]/drinks/page.tsx"; true</automated>
  </verify>
  <acceptance_criteria>
    - Every difference in both pairs is classified in the SUMMARY as drift, guard or cosmetic, with a grant row named for each drift
    - The SUMMARY states explicitly whether the `drinks` pair differs in any token or closing-time behaviour, and if so, the plan is stopped rather than merged
    - No file was modified: `git status --porcelain` is empty
  </acceptance_criteria>
  <done>Both pairs are measured on their own evidence, and the money path has been looked at rather than assumed identical.</done>
</task>

<task type="auto">
  <name>Task 2: Merge both pairs under the work layout</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/{edit,drinks}/**, src/app/(organizer)/organizer/events/[id]/{edit,drinks}/page.tsx</files>
  <read_first>
    - The Task 1 classification
    - src/app/(admin)/admin/(work)/layout.tsx
    - .planning/phases/34-one-work-surface/34-06-SUMMARY.md (the route-group arrangement)
    - src/lib/routes/capability-routes.ts (`organizer.access` binds both addresses)
  </read_first>
  <action>
    `git mv` `src/app/(admin)/admin/events/[id]/edit/` and
    `src/app/(admin)/admin/events/[id]/drinks/` — including `DrinkMenuManager.tsx` — into
    `src/app/(admin)/admin/(work)/events/[id]/`, following the recorded arrangement. Then rewrite
    the one importer that names `DrinkMenuManager` by path from outside:
    `src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx`. Re-measure with `grep -rn
    "DrinkMenuManager" src/` rather than trusting that there is only one.

    In both merged pages:
    - Guard on `CAP.ORGANIZER_ACCESS` — the key the map binds and the key each organizer twin
      already asked.
    - Delete the nav mounts, their imports and the `role as UserRole` / `status as UserStatus`
      casts.
    - `<h1>` takes the surface's own name.
    - Apply every **drift** verdict from Task 1, one line of SUMMARY per resolved divergence
      naming the capability that decided it (D-34-05).

    Delete `src/app/(organizer)/organizer/events/[id]/edit/page.tsx` and
    `.../drinks/page.tsx`.

    Change nothing in the drinks money path: not the closing time, not the grace period, not the
    redeem confirmation. Not one line. If the merge appears to require it, that is Task 1's stop
    condition arriving late.

    No restyle. Phases 40 and 41.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)" -path '*events/\[id\]/edit/page.tsx'` returns one path, and likewise for drinks
    - `src/app/(organizer)/organizer/events/[id]/{edit,drinks}/page.tsx` no longer exist
    - Both merged pages guard on `ORGANIZER_ACCESS`
    - `grep -rc "StaffNav\|MobileNav\|as UserRole\|as UserStatus"` returns 0 in both merged pages
    - `grep -rn "DrinkMenuManager" src/` shows every importer pointing at the new path, and 0 matches for `(organizer)`
    - `git diff -M` on `DrinkMenuManager.tsx` shows **no** hunk touching a token, a closing time or a redeem call
    - `rm -rf .next && npm run build` exits 0
    - Behaviour: `curl -sI http://localhost:3000/organizer/events/abc/drinks` against `npm run dev` redirects to `/admin/events/abc/drinks`
  </acceptance_criteria>
  <done>Two surfaces exist once each, on the bound capability, with the drinks money path provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → page | Same key, same address |
| staff surface ↔ public menu | `DrinkMenuManager` is mounted by both a staff page and a public party menu |
| client → drink actions | Purchase, redeem and refund are money paths with their own re-checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-58 | Elevation of Privilege | either surface bound wider than its organizer twin | mitigate | `organizer.access` is the twin's own key; every drift is decided against a named grant row |
| T-34-59 | Tampering | a token or closing-time behaviour changed inside a routing merge | mitigate | Task 1 stops the plan if the pair differs there; Task 2 asserts by diff that no hunk touches a token, a closing time or a redeem call |
| T-34-60 | Denial of Service | the public party menu losing its manager component | mitigate | `grep -rn "DrinkMenuManager"` asserted to show every importer at the new path, and `npm run build` is the type gate |
| T-34-61 | Repudiation | a divergence resolved by preference | mitigate | Task 1's three-verdict classification, with a grant named for every drift |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; audit not applicable for this phase |
</threat_model>

<verification>
- `rm -rf .next && npm run build` exits 0
- `npm run verify:persona` exits 0
- Old addresses redirect, checked with `curl -sI`
- **Not claimed:** that a drink token still redeems. There is no test runner for this product; what is claimed is that no line on that path changed, proved by diff.
</verification>

<success_criteria>
1. `events/[id]/edit` and `events/[id]/drinks` each exist once, on `organizer.access`
2. Every difference in both pairs is classified with a grant named for each drift
3. No line of the drinks money path changed
4. Every `DrinkMenuManager` importer resolves at the new path
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-12-SUMMARY.md` when done.
</output>
