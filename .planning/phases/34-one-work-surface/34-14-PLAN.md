---
phase: 34-one-work-surface
plan: 14
type: execute
wave: 4
depends_on: ["34-05", "34-06", "34-07"]
files_modified:
  # Route files only — R-WORK-ROUTES. `guest-list/{actions.ts,GuestListClient.tsx}` stay
  # outside `(work)`, and `SalesDashboard.tsx` needs nothing from this plan: plan 34-13 leaves
  # `RefundActions.tsx` where 34-07 put it, so its specifier is already final.
  - src/app/(admin)/admin/(work)/events/[id]/sales/page.tsx
  - src/app/(admin)/admin/(work)/events/[id]/guest-list/page.tsx
  - src/app/(admin)/admin/(work)/events/[id]/analytics/page.tsx
  - src/app/(admin)/admin/(work)/events/[id]/analytics/loading.tsx
  - src/app/(admin)/admin/(work)/events/[id]/media/page.tsx
  - src/app/(organizer)/organizer/events/[id]/sales/page.tsx
  - src/app/(organizer)/organizer/events/[id]/guest-list/page.tsx
  - src/app/(organizer)/organizer/events/[id]/analytics/page.tsx
  - src/app/(organizer)/organizer/events/[id]/analytics/loading.tsx
  - src/app/(organizer)/organizer/events/[id]/media/page.tsx
autonomous: true
requirements: [STAFF-01, STAFF-03]

must_haves:
  truths:
    - "`sales`, `guest-list` and `analytics` each exist once and open to `organizer.access`"
    - "`/admin/events/<id>/media` has a capability check of its own for the first time — `staff.manage`"
    - "No guest-list check-in behaviour changed"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/events/[id]/media/page.tsx"
      provides: "The media review surface, with the gate finding F1 says it never had"
      contains: "STAFF_MANAGE"
    - path: "src/app/(admin)/admin/(work)/events/[id]/sales/page.tsx"
      provides: "The single sales surface"
      contains: "ORGANIZER_ACCESS"
  key_links:
    - from: "src/app/(admin)/admin/(work)/events/[id]/media/page.tsx"
      to: "src/lib/routes/capability-routes.ts"
      via: "the page asks staff.manage, the key the map binds to this address"
      pattern: "STAFF_MANAGE"
---

<objective>
Collapse the last four pairs — `sales`, `guest-list`, `analytics` and `media` — and give the
media surface the gate it has never had.

Purpose: this plan closes **finding F1**, which the CONTEXT does not record.
`src/app/(admin)/admin/events/[id]/media/page.tsx` contains **no capability check at all** —
only `if (!user) redirect("/login")` at line 17. It is held up **solely** by
`middleware.ts`'s `/admin/*` → `admin.access` rule, and D-34-02 dissolved that rule in plan
34-03. Closing this is not optional; it is a consequence of the collapse, and leaving it would
ship a media-moderation surface reachable by any signed-in account. The gate is not invented:
its organizer twin already gates on `staff.manage` at `media/page.tsx:63`, so taking the twin's
gate is giving a page its own check, not a widening.

`sales` is the second of the two pairs the research flagged as most likely to hold real
behavioural drift (77 changed lines). It gets its own task.

Output: four surfaces where seven files were, and one page that stops being naked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-06-SUMMARY.md
@.planning/phases/34-one-work-surface/34-09-SUMMARY.md
@CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/checkin-offline.md
@.claude/rules/media-and-storage.md
@.claude/rules/meta-gates.md
</context>

<phase_rule>
**R-WORK-ROUTES — only Next.js route files enter `(work)`.** Declared in plan 34-07, Task 1,
and cited here rather than re-derived. `page.tsx` and `loading.tsx` move into
`src/app/(admin)/admin/(work)/…`; `actions.ts` and co-located client components **stay** at
their `src/app/(admin)/admin/…` path, outside the group, exactly where plan 34-07 put them.
A moved page reaches its former siblings with an absolute `@/app/(admin)/admin/…` specifier —
an edit inside the page, owned by this plan and by no other.

The rule is what keeps this wave parallel. A route group governs routing and nothing else, so
a non-route module gains nothing by entering `(work)`, while moving it changes its specifier
for **every external importer** — and those importers are files owned by sibling plans in this
same wave. In parallel worktrees a sibling's `git mv` is invisible to `grep`, so the outcome
is either a merge conflict or a clean merge pointing at a file that no longer exists.
</phase_rule>

<tasks>

<task type="auto">
  <name>Task 1: The media surface gets its own gate — finding F1, closed</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/media/page.tsx, src/app/(organizer)/organizer/events/[id]/media/page.tsx</files>
  <read_first>
    - src/app/(admin)/admin/events/[id]/media/page.tsx (all of it — its only guard is `if (!user) redirect("/login")`; that is the whole finding)
    - src/app/(organizer)/organizer/events/[id]/media/page.tsx (its `staff.manage` gate around :63 — this is the gate being taken)
    - src/lib/capabilities/guards.ts:161-192 (`assertStaffManage`, the existing guard shape)
    - src/lib/routes/capability-routes.ts (`staff.manage` → `/admin/events/[id]/media`, `alsoGatesTables: true`)
    - .planning/phases/34-one-work-surface/34-RESEARCH.md § finding F1
  </read_first>
  <action>
    `diff` the pair and classify every difference in the format plan 34-09 set. The headline
    difference is the guard, and it is not a divergence to weigh: one file has one and the other
    does not.

    `git mv` `src/app/(admin)/admin/events/[id]/media/` into
    `src/app/(admin)/admin/(work)/events/[id]/media/`, following the arrangement recorded in
    `34-06-SUMMARY.md`. Merge the organizer twin's content into it and delete the twin.

    Give the merged page the `staff.manage` gate, using the same shape the twin used — resolve
    the access context and refuse when the capability is absent. `staff.manage` is granted to
    master and organizer with `requires_approved = false`, so this does **not** narrow the page
    against a pending organizer, and it does **not** widen it: nobody who could not already
    reach the organizer twin gains anything.

    Write a comment above the gate recording, in one short paragraph, that this page had no
    capability check of its own and was held up by the `/admin/*` prefix rule alone; that the
    rule was dissolved by D-34-02; and that the gate was taken from the organizer twin rather
    than chosen. A gate whose reason is not written is a gate somebody removes as redundant —
    which is the trap Phase 43's D-06 recorded for `door.operate`.

    Delete the nav mounts if present, their imports and the casts. `<h1>` takes the surface's own
    name.

    `media-and-storage.md` cross-check, recorded rather than intended: confirm the merge changes
    nothing about what is uploaded, what is stripped, or what `media.upload` gates —
    `media.upload` is a `scope: "table"` key in the map with the reason *"gates a Route Handler;
    the guard is `src/lib/media/may-upload.ts`"*, and this plan does not touch that handler.

    `venue-secrecy.md` cross-check: the media surface renders uploaded media for a night.
    Confirm by reading that it does not render a venue address and does not touch
    `venue_reveal_sent`. Record the file:line read.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)" -path '*events/\[id\]/media/page.tsx'` returns one path; the organizer twin no longer exists
    - `grep -c "STAFF_MANAGE" <merged media page>` returns at least 1
    - `grep -c "if (!user)" <merged media page>` — the bare login redirect is either replaced by the capability guard or accompanied by it; the SUMMARY states which and why
    - The comment above the gate names finding F1, the dissolved prefix rule, and that the gate came from the twin
    - The `media-and-storage` and `venue-secrecy` cross-checks are recorded with the file:line read
    - `rm -rf .next && npm run build` exits 0
    - Behaviour (manual, owed to plan 34-17): **M-4** must observe a `staff`/`approved` account being refused at this address
  </acceptance_criteria>
  <done>The one page in the tree with no capability check has one, taken from its twin, with its reason written down.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse sales, guest-list and analytics</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/{sales,guest-list}/page.tsx, src/app/(admin)/admin/(work)/events/[id]/analytics/{page,loading}.tsx, src/app/(organizer)/organizer/events/[id]/{sales,guest-list,analytics}/**</files>
  <read_first>
    - src/app/(admin)/admin/events/[id]/sales/page.tsx and its organizer twin (77 changed lines — the research names this as one of the two most likely to hold real behavioural drift)
    - src/app/(admin)/admin/events/[id]/guest-list/page.tsx and its organizer twin
    - src/app/(admin)/admin/events/[id]/guest-list/{actions.ts,GuestListClient.tsx} (moved there by plan 34-07; **they stay there** — read the check-in-related guards; the guest list has a door-adjacent path)
    - src/app/(admin)/admin/events/[id]/analytics/{page,loading}.tsx and their organizer twins
    - src/components/events/SalesDashboard.tsx (**read only, do not edit.** It mounts `RefundActions` at `:30`. Under R-WORK-ROUTES plan 34-13 leaves `RefundActions.tsx` at `src/app/(admin)/admin/events/[id]/tickets/`, where 34-07 put it, so this specifier is already final and no coordination is needed — which is the point, because in parallel worktrees this plan cannot see 34-13's tree and the coordination the earlier draft asked for was not something the execution mechanism permits)
    - .claude/rules/checkin-offline.md (the guest-list check-in columns are door state)
  </read_first>
  <action>
    `diff` all three pairs. Classify every difference as **drift**, **guard** or **cosmetic**,
    naming the `private.role_capabilities` row for every drift. Assumption A5 applies: each pair
    on its own evidence.

    For `sales`, ask the same money question plan 34-13 asks of `tickets`: does either version
    show a refund control, a takings figure or a payout detail the other does not, and if so
    which grant permits it. `SalesDashboard` mounts `RefundActions`, so a difference here reaches
    the same control.

    For `guest-list`, ask the door question: does either version differ in what it can do to a
    check-in — mark, unmark, add at the door? `checkin-offline.md`'s asymmetry applies — refusing
    a valid guest is worse than admitting a duplicate — and a difference in that behaviour is not
    a divergence to resolve by preference. If either differs, take the more restrictive
    **reachability** and record the behavioural difference as a finding rather than picking one.

    Then `git mv` the **route files** of the three `(admin)` directories into
    `src/app/(admin)/admin/(work)/events/[id]/`, following the recorded arrangement — that is
    `sales/page.tsx`, `guest-list/page.tsx`, `analytics/page.tsx` and `analytics/loading.tsx`.
    **`guest-list/actions.ts` and `guest-list/GuestListClient.tsx` do not move** (R-WORK-ROUTES);
    they stay at `src/app/(admin)/admin/events/[id]/guest-list/`, and the moved page reaches
    them with an absolute `@/app/(admin)/admin/events/[id]/guest-list/…` specifier. Then merge:
    - Guard each on `CAP.ORGANIZER_ACCESS`, the key the map binds and the key each organizer twin
      already asked.
    - Delete the nav mounts, their imports and the `role as UserRole` / `status as UserStatus`
      casts from the pages **and** from `analytics/loading.tsx`, which mounts `StaffNav` too.
    - `<h1>` takes each surface's own name.
    - Apply the drift verdicts.

    Delete the three organizer twins and `organizer/events/[id]/analytics/loading.tsx`.

    `guest-list/actions.ts` and `GuestListClient.tsx` are not moved and not edited. Their
    internal capability re-checks are the boundary, and a routing plan does not touch them.

    **`SalesDashboard.tsx` is not edited by this plan.** Its `RefundActions` specifier at `:30`
    was rewritten by plan 34-07 in Wave 2 and, because 34-13 does not move `RefundActions.tsx`,
    it stays correct. Confirm by reading and record the line in the SUMMARY. Change nothing —
    this file was the collision between this plan and 34-13, and R-WORK-ROUTES is what removes
    it. If the specifier is somehow stale, that is a failure of 34-13's acceptance criteria:
    **stop and report**, do not repair it here.

    No restyle. Phases 40 and 41.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)" -path '*events/\[id\]/sales/page.tsx'` returns one path, and likewise for guest-list and analytics
    - `src/app/(organizer)/organizer/events/[id]/{sales,guest-list,analytics}/` no longer exist
    - All three merged pages guard on `ORGANIZER_ACCESS`
    - `grep -rc "StaffNav\|MobileNav\|as UserRole\|as UserStatus"` returns 0 in all three merged pages **and** in the moved `analytics/loading.tsx`
    - `src/app/(admin)/admin/events/[id]/guest-list/{actions.ts,GuestListClient.tsx}` still exist at those exact paths, outside `(work)`; `git diff -M --stat` shows **neither a rename nor a hunk** for either — the door-adjacent path is unchanged in content and in the module graph
    - `src/components/events/SalesDashboard.tsx` is **not** in this plan's diff: `git diff --name-only` does not list it
    - The SUMMARY answers the sales money question and the guest-list door question explicitly
    - `rm -rf .next && npm run build` exits 0
    - Behaviour: `curl -sI` on `/organizer/events/abc/{sales,guest-list,analytics}` redirects to the `/admin` twin in each case
  </acceptance_criteria>
  <done>Three more surfaces exist once each, the guest-list door path is provably untouched, and the sales money question has an answer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signed-in account → media moderation | Until this plan, only `if (!user)` stood in front of it |
| staff surface → check-in state | The guest list writes door state |
| staff surface → takings and refunds | `SalesDashboard` reaches the refund control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-67 | Elevation of Privilege | media moderation reachable by any signed-in account after the prefix rule dissolves | mitigate | The merged page takes the organizer twin's `staff.manage` gate; asserted by `grep`; observed by M-4 in plan 34-17. **This is finding F1 and it is the reason this plan exists** |
| T-34-68 | Tampering | a check-in behaviour changed inside a routing merge | mitigate | R-WORK-ROUTES: `guest-list/actions.ts` and `GuestListClient.tsx` neither move nor change — asserted by `git diff -M --stat` showing no rename and no hunk. The door question is answered explicitly before any edit |
| T-34-68b | Denial of Service | `SalesDashboard.tsx` merged clean with a specifier pointing at a `RefundActions.tsx` that a sibling worktree moved | mitigate | R-WORK-ROUTES removes the move in 34-13; this plan does not edit the file and asserts it stays out of the diff |
| T-34-69 | Elevation of Privilege | a takings or refund detail shown to a new audience | mitigate | The sales money question requires a named grant for any control appearing for a new audience; D-34-06 otherwise forces the more restrictive behaviour |
| T-34-70 | Information Disclosure | a media or analytics surface advancing a venue reveal | mitigate | Cross-check recorded with the file:line read; neither surface touches `venue_reveal_sent` |
| T-34-71 | Repudiation | a gate added without its reason, later removed as redundant | mitigate | The comment above the media gate names F1, the dissolved rule and the twin it came from — the same failure mode Phase 43's D-06 recorded for `door.operate` |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; audit not applicable for this phase |
</threat_model>

<verification>
- `rm -rf .next && npm run build` exits 0
- `npm run verify:persona` exits 0
- All four old addresses redirect, checked with `curl -sI`
- **Not claimed:** that a `staff` account is refused at the media surface. That is M-4, owed to plan 34-17. Until then the gate is a source assertion, not an observation.
</verification>

<success_criteria>
1. `sales`, `guest-list`, `analytics` and `media` each exist once
2. The media surface carries `staff.manage`, with finding F1 and the twin it came from written above it
3. The guest-list door path and the guest-list actions neither moved nor changed, and `SalesDashboard.tsx` was not touched
4. The sales money question and the guest-list door question are answered in the SUMMARY
5. All twelve pairs of the phase are now collapsed
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-14-SUMMARY.md` when done.
</output>
