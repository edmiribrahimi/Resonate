---
phase: 34-one-work-surface
plan: 13
type: execute
wave: 4
depends_on: ["34-05", "34-06", "34-07"]
files_modified:
  # Route files only — R-WORK-ROUTES. `tickets/{actions.ts,RefundActions.tsx}` stay outside
  # `(work)`, so `SalesDashboard.tsx`, the four `components/tickets/*` and `TierSelection.tsx`
  # need no edit and are not listed.
  - src/app/(admin)/admin/(work)/events/[id]/tickets/page.tsx
  - src/app/(organizer)/organizer/events/[id]/tickets/page.tsx
autonomous: true
requirements: [STAFF-01]

must_haves:
  truths:
    - "`/admin/events/<id>/tickets` exists once and opens to `organizer.access`"
    - "The master-only ticket controls are drawn on `master.manage`, not on a role string"
    - "No refund path, tier action or discount-code action changed"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/events/[id]/tickets/page.tsx"
      provides: "The single tickets surface"
      contains: "ORGANIZER_ACCESS"
  key_links:
    - from: "src/app/(admin)/admin/(work)/events/[id]/tickets/page.tsx"
      to: "src/app/(admin)/admin/events/[id]/tickets/RefundActions.tsx"
      via: "an absolute specifier into the non-route module, which stays outside (work) with the refund path unchanged"
      pattern: "admin/events/\\[id\\]/tickets/RefundActions"
---

<objective>
Collapse the `events/[id]/tickets` pair — **alone**, because it is the largest divergence in the
phase.

Purpose: the research measured **140 changed lines** between the two files, 365 source lines
against 413, and named `tickets` and `sales` as the two pairs *"most likely to hold a real
behavioural divergence"* which *"should not be merged in the same plan as anything else"*.
This is that plan. It has one pair and nothing else, and its first task is a diff, not an edit.

And this pair sits on a money path. `RefundActions.tsx`, `actions.ts` (tiers and discount
codes) and the four `components/tickets/*` files all hang off it, and `(public)/events/[slug]/TierSelection.tsx`
imports `validateDiscountCode` from the same action file. *"Il denaro non si fida di chi lo
annuncia"* is not decorative here: a divergence resolved by preference in this file is a refund
rule changed inside a routing phase.

Output: one tickets surface, with a written account of every one of the 140 lines' worth of
difference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-one-work-surface/34-CONTEXT.md
@.planning/phases/34-one-work-surface/34-RESEARCH.md
@.planning/phases/34-one-work-surface/34-06-SUMMARY.md
@.planning/phases/34-one-work-surface/34-09-SUMMARY.md
@CLAUDE.md
@.claude/rules/ticketing-payments.md
@.claude/rules/access-gating.md
@.claude/rules/meta-gates.md
</context>

<phase_rule>
**R-WORK-ROUTES — only Next.js route files enter `(work)`.** Declared in plan 34-07, Task 1,
and cited here rather than re-derived. `page.tsx` and `loading.tsx` move into
`src/app/(admin)/admin/(work)/…`; `actions.ts` and co-located client components **stay** at
their `src/app/(admin)/admin/…` path, outside the group, exactly where plan 34-07 put them.
A moved page reaches its former siblings with an absolute `@/app/(admin)/admin/…` specifier —
an edit inside the page, owned by this plan and by no other.

The rule is what keeps this wave parallel. A route group governs routing and nothing else, so
a non-route module gains nothing by entering `(work)`, while moving it changes its specifier
for **every external importer** — and those importers are files owned by sibling plans in this
same wave. In parallel worktrees a sibling's `git mv` is invisible to `grep`, so the outcome
is either a merge conflict or a clean merge pointing at a file that no longer exists.
</phase_rule>

<tasks>

<task type="auto">
  <name>Task 1: Diff the pair line by line and classify all 140 changed lines</name>
  <files>(no file is modified in this task)</files>
  <read_first>
    - src/app/(admin)/admin/events/[id]/tickets/page.tsx (read in full — it is the larger of the two at ~413 lines, and it carries the `master.manage` branch)
    - src/app/(organizer)/organizer/events/[id]/tickets/page.tsx (read in full — ~365 lines)
    - src/app/(admin)/admin/events/[id]/tickets/actions.ts (moved there by plan 34-07; **it stays there** — read its guards: which actions re-check `master.manage`, which re-check `staff.manage`)
    - src/app/(admin)/admin/events/[id]/tickets/RefundActions.tsx (**stays there** — and note its other mount, `src/components/events/SalesDashboard.tsx:30`, which is plan 34-14's file in this same wave)
    - .claude/rules/ticketing-payments.md
    - .planning/phases/34-one-work-surface/34-RESEARCH.md § D5..D12 and assumption A5
  </read_first>
  <action>
    `diff` the pair and read every hunk. Classify each as **drift**, **guard** or **cosmetic**,
    naming the `private.role_capabilities` row for every drift, in the format plan 34-09 set.
    140 changed lines is enough that a summary sentence would hide something; the SUMMARY needs
    a line per hunk, grouped, not a paragraph.

    Three specific questions this pair must answer explicitly, because they are the ones that
    can change what money does:

    1. **Does either version show a refund control the other does not?** If yes, name the grant
       that permits it. `RefundActions` is mounted from both this page and
       `components/events/SalesDashboard.tsx`; a control that appears for a new audience is a
       widening and needs a grant, per D-34-06.
    2. **Does either version differ in what it passes to `RefundActions`** — a different event
       scope, a different set of tickets, a different confirmation? A prop difference here is a
       behavioural difference.
    3. **Does either version differ in tier or discount-code handling?** Those actions are
       reachable as public POSTs and re-check themselves, but the surface can still show
       different controls.

    If the answer to any of the three is a difference that would need a new grant or a
    `requires_approved` flip to merge, **stop**: the merged surface takes the more restrictive
    behaviour and the case goes into the SUMMARY as a finding. Widening a policy to make a
    collapse pass is forbidden. Pitfall 6's forbidden sentence — *"the admin version is more
    complete, so use it"* — is especially tempting here, because the admin version genuinely is
    48 lines longer.
  </action>
  <verify>
    <automated>diff "src/app/(admin)/admin/events/[id]/tickets/page.tsx" "src/app/(organizer)/organizer/events/[id]/tickets/page.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY classifies every hunk of the diff as drift, guard or cosmetic, grouped, with a grant row named for every drift
    - The SUMMARY answers all three money questions explicitly, each with a yes/no and, where yes, the grant or the finding
    - No file was modified: `git status --porcelain` is empty
  </acceptance_criteria>
  <done>The largest divergence in the phase is written down hunk by hunk, and every difference that touches money has an answer rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the pair under the work layout, with the money path provably untouched</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/tickets/page.tsx, src/app/(organizer)/organizer/events/[id]/tickets/page.tsx</files>
  <read_first>
    - The Task 1 classification and the three money answers
    - src/app/(admin)/admin/(work)/layout.tsx
    - .planning/phases/34-one-work-surface/34-06-SUMMARY.md (the route-group arrangement)
    - src/lib/routes/capability-routes.ts (`organizer.access` binds `/admin/events/[id]/tickets`)
  </read_first>
  <action>
    `git mv` **`src/app/(admin)/admin/events/[id]/tickets/page.tsx` only**, into
    `src/app/(admin)/admin/(work)/events/[id]/tickets/`, following the recorded arrangement.

    **`actions.ts` and `RefundActions.tsx` do not move** — R-WORK-ROUTES. They stay at
    `src/app/(admin)/admin/events/[id]/tickets/`, where plan 34-07 put them. Measured
    2026-08-09, moving them would force this plan to rewrite specifiers in six files it does not
    own: `src/components/events/SalesDashboard.tsx:30` — **plan 34-14's file, in this same
    wave** — the four `src/components/tickets/{AddTierForm.tsx:4,TierCard.tsx:8,AddDiscountCodeForm.tsx:4,DiscountCodeCard.tsx:8}`,
    and `(public)/events/[slug]/TierSelection.tsx`, which is a public purchase surface. In
    parallel worktrees this plan cannot see 34-14's tree and 34-14 cannot see this one, so a
    coordinated rewrite of `SalesDashboard.tsx` is not something the execution mechanism
    permits. The rule removes the need for it: nothing outside this directory changes.

    Rewrite the moved page's two relative imports (`./actions`, `./RefundActions`) to
    `@/app/(admin)/admin/events/[id]/tickets/…`. That edit is inside a page this plan owns.

    In the merged page:
    - Guard on `CAP.ORGANIZER_ACCESS`.
    - Draw the master-only controls on `capabilities.has(CAP.MASTER_MANAGE)` from the resolved
      set — the same two-questions rule plan 34-11 applied: reachability and deletion are not the
      same question, and `master.manage` is a `scope: "table"` key in the map precisely because it
      gates operations rather than addresses.
    - Delete the nav mounts, their imports and the `role as UserRole` / `status as UserStatus`
      casts.
    - Apply every drift verdict from Task 1.

    Delete `src/app/(organizer)/organizer/events/[id]/tickets/page.tsx`.

    **`actions.ts` and `RefundActions.tsx` are not edited at all.** Not a guard, not
    a refund rule, not an idempotency check, not a SumUp call. `ticketing-payments.md` and the
    webhook's own rule — never trust an announcement, always verify — are not this plan's to
    revisit, and a routing phase that touched a refund path would be the exact cross-domain
    failure `meta-gates.md` exists to prevent.

    No restyle. Phases 40 and 41.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `find "src/app/(admin)" -path '*events/\[id\]/tickets/page.tsx'` returns one path
    - `src/app/(organizer)/organizer/events/[id]/tickets/page.tsx` no longer exists
    - The merged page guards on `ORGANIZER_ACCESS`, and `grep -c 'role === "master"'` returns 0
    - `grep -rc "StaffNav\|MobileNav\|as UserRole\|as UserStatus"` returns 0 in the merged page
    - `src/app/(admin)/admin/events/[id]/tickets/{actions.ts,RefundActions.tsx}` still exist at those exact paths, outside `(work)`; `git diff -M --stat` shows **neither a rename nor a hunk** for either — the refund path is not merely unchanged in content, it is unchanged in the module graph
    - `src/components/events/SalesDashboard.tsx`, the four `src/components/tickets/*` files and `src/app/(public)/events/[slug]/TierSelection.tsx` are **not** in this plan's diff: `git diff --name-only` lists none of the six
    - `grep -rn "tickets/actions\|RefundActions" src/` shows 0 matches for `(organizer)` and 0 for `(work)`; every importer names `@/app/(admin)/admin/events/[id]/tickets/…`
    - `rm -rf .next && npm run build` exits 0
    - Behaviour: `curl -sI http://localhost:3000/organizer/events/abc/tickets` redirects to `/admin/events/abc/tickets`
  </acceptance_criteria>
  <done>One tickets surface, on the bound capability, with the refund and tier code proved unchanged by a zero-line rename.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public browser → tier and discount actions | `validateDiscountCode` is reachable from a public event page |
| staff surface → refund | The refund control is the highest-consequence control in the tree |
| middleware → page | Same key, same address |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-62 | Tampering | a refund rule changed inside a routing merge | mitigate | R-WORK-ROUTES: `actions.ts` and `RefundActions.tsx` neither move nor change — asserted by `git diff -M --stat` showing no rename and no hunk. Task 1's three money questions must be answered before any edit |
| T-34-62b | Denial of Service | `SalesDashboard.tsx` — plan 34-14's file in this same wave — left pointing at a `RefundActions.tsx` that moved in a worktree it cannot see | mitigate | R-WORK-ROUTES removes the move. Asserted by `git diff --name-only` listing none of the six external importers |
| T-34-63 | Elevation of Privilege | a refund control drawn for an audience that did not have it | mitigate | Task 1 question 1 requires a named grant for any control appearing for a new audience; D-34-06 otherwise forces the more restrictive behaviour |
| T-34-64 | Elevation of Privilege | master-only ticket controls drawn for an organizer | mitigate | Drawn on `capabilities.has(CAP.MASTER_MANAGE)`; `grep` asserts no role-string condition |
| T-34-65 | Denial of Service | a public event page losing `validateDiscountCode` | mitigate | Importer sweep asserted by `grep -rn`, and `npm run build` is the type gate |
| T-34-66 | Repudiation | 140 changed lines summarised instead of classified | mitigate | The SUMMARY is required to carry a line per hunk, grouped, not a paragraph |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed; audit not applicable for this phase |
</threat_model>

<verification>
- `rm -rf .next && npm run build` exits 0
- `git diff -M --stat` shows two pure renames with zero changed lines
- Old address redirects, checked with `curl -sI`
- **Not claimed:** that a refund still works. No test runner exists. What is claimed is that no line of the refund path changed, and the diff proves it.
</verification>

<success_criteria>
1. `events/[id]/tickets` exists once, on `organizer.access`
2. Every hunk of the 140-line diff is classified, with a grant named for each drift
3. The three money questions are answered explicitly in the SUMMARY
4. `actions.ts` and `RefundActions.tsx` did not move and did not change, and none of their six external importers was edited
</success_criteria>

<output>
Create `.planning/phases/34-one-work-surface/34-13-SUMMARY.md` when done.
</output>
