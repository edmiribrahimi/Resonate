---
phase: 41.1-work-surface-conversion
plan: 04
type: execute
wave: 1
depends_on: ["41.1-01"]
files_modified:
  - scripts/verify-conversion.mjs
  - scripts/conversion-manifest.mjs
autonomous: true
requirements: [DS-07, RESP-02]
must_haves:
  truths:
    - "No header in verify-conversion advertises a guard the code cannot deliver"
    - "A permitted navigation-property site is keyed on where it is, not only on what it says"
    - "A declared surface's route-adjacent files are scanned, so a loading.tsx cannot pass by absence"
    - "The manifest says in writing why /admin is not a declared surface"
  artifacts:
    - path: "scripts/verify-conversion.mjs"
      provides: "checks A-E over a scanned set that includes route-adjacent files"
      contains: "loading.tsx"
    - path: "scripts/conversion-manifest.mjs"
      provides: "the declared list, with the /admin decision written into its docblock"
  key_links:
    - from: "scripts/verify-conversion.mjs"
      to: "a declared surface's loading.tsx"
      via: "the route-adjacent extension of the scanned set"
      pattern: "loading\\.tsx"
---

<objective>
Repair the two `DEF-41-07` defects that live in `verify-conversion.mjs`, extend
the scanned set so criterion 3 stops being provable only by hand, and settle the
`/admin` index page **before** check D's first red run.

Purpose: seven `loading.tsx` files under `(work)` carry **149 legacy-token
utilities and 45 hand-rolled pulse blocks** and **no gate in this phase can see
them** — a route-level loading file is reached by the router, not by an import,
and the word `loading` appears zero times in this gate. Criterion 3 says *no file
under the work surface*, so as the gates stand it is a claim. And an exemption
discovered on a red run is an exemption nobody trusts (§0 rule 3, D-41-16).

Output: one advertisement retired, one digest keyed on position, a scanned set
that grows, and a manifest that explains its own count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
</context>

<plan_rules>
- This plan is the **only** plan in wave 1 that may edit these two files.
  Check E's replacement is **not** here — it belongs to plan 41.1-05, in wave 2,
  because the assertion and the thing it asserts are one decision.
- `CONVERTED` gains no entry here. Declaring a surface belongs to the wave that
  converted it, through that wave's reconciliation plan (D-41.1-22).
- The two frozen digests must stay valid. `PageShell.tsx` is not edited by any
  plan in this phase; if that ever stops being true, both digests are re-frozen in
  the same commit with the measurement that justified it.
</plan_rules>

<tasks>

<task type="auto">
  <name>Task 1: DEF-41-07 item 2 — retire the advertisement rather than make the branch reachable</name>
  <files>scripts/verify-conversion.mjs</files>
  <read_first>
    - `scripts/verify-conversion.mjs` lines 1917-1934 — the `GATE CANNOT READ` refusal for a focus-root render site outside the frozen window, and its three-conjunct guard
    - `scripts/verify-conversion.mjs` lines 318-320 — the header sentence that advertises the guard
    - `scripts/verify-conversion.mjs` line 1432 — the marker constant the advertisement uses
    - `scripts/verify-dialogs.mjs` lines 55-61 — the fence vocabulary (*UNMEASURED, not approved*)
    - `.planning/phases/41-shared-primitives-three-tier-layout/deferred-items.md` DEF-41-07 item 2
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §4.6 row 2 — why the third conjunct is unsatisfiable when the first two hold
  </read_first>
  <action>
The refusal branch at `:1917-1934` cannot fire: matching the frozen shape requires
the window to contain the render line, so the third conjunct is unsatisfiable
whenever the first two hold. **D-41.1-10 decides the direction: close it by
retiring the advertisement, not by making the branch reachable.** A header that
promises a guard the code cannot deliver is a false header sentence, and this
phase has found three; deleting the sentence adds no code and removes the lie,
while making the branch reachable would add both.

Delete the advertisement at `:318-320` **and** the unreachable branch and its
marker constant at `:1432` — both, because leaving dead code beside a deleted
sentence is what "adds no code" does not mean, and the plan is asked to decide
and say which it did. In their place put one sentence in the fence vocabulary
this gate family already uses: name what is now **UNMEASURED, not approved** — a
focus-root render site outside the frozen window is not detected — and name the
frozen digest that does the work the branch pretended to do.

Record the retirement in the docblock in the house shape (`PageShell.tsx:42-46`):
the sentence that used to be there, and the measurement that retired it, side by
side. `DEF-41-07` item 2 is closed by this task and the entry says so.
  </action>
  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-conversion.mjs` exits 0, prints `CONVERSION_OK`, 5
      checks, 8 surfaces, 53 files scanned — unchanged from the 41.1-01 baseline
    - `grep -n "GATE CANNOT READ" scripts/verify-conversion.mjs` no longer returns
      the focus-root refusal branch
    - The marker constant formerly at `:1432` is gone:
      `grep -c` on its exact identifier returns 0
    - The docblock contains the retired sentence quoted, the measurement that
      retired it, and the words `UNMEASURED, not approved`
    - The plan's SUMMARY states explicitly that both the sentence and the branch
      were deleted, and why deleting only one would have been the worse half
  </acceptance_criteria>
  <done>The advertisement and its dead branch are gone, and the gap is declared in the fence vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: DEF-41-07 item 3 — a permitted site keyed on position, and the existsSync class in the manifest</name>
  <files>scripts/verify-conversion.mjs, scripts/conversion-manifest.mjs</files>
  <read_first>
    - `scripts/verify-conversion.mjs` lines 2042-2053 — `NAV_PROPERTY_SITE_DIGESTS`
    - `scripts/verify-conversion.mjs` line 2089 — the consumption, hashing the trimmed line text alone
    - `scripts/verify-conversion.mjs` lines 2063-2064 — `SHELL_CODE_OUTSIDE_WINDOW_DIGEST`, the other frozen digest, and why it stays valid
    - `scripts/conversion-manifest.mjs` lines 410, 426, 436 — the three `existsSync` calls carrying the same case-sensitivity exposure
    - `.planning/phases/41-shared-primitives-three-tier-layout/deferred-items.md` DEF-41-07 items 2 and 3
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §4.6 rows 3 and 4
  </read_first>
  <action>
**Item 3.** A permitted navigation-property site is keyed on a line's **text**
alone: the digest is taken over the trimmed line, so the same text anywhere in
any file is permitted. Key it on `path` and line number **together with** the
digest, so a permitted site is a place as well as a string. Re-freeze the
constant with the current positions measured, and write the measuring command
into the comment above it so the next re-freeze is reproducible rather than
re-derived.

**The `existsSync` class, named in plan 41.1-02 and paid here.** The same
case-sensitivity exposure lives at `conversion-manifest.mjs:410,426,436`: on the
house APFS volume `existsSync` is case-insensitive while every declared path is
compared case-exact elsewhere, so one case typo produces two verdicts depending
on the filesystem. Make each of the three compare case-exactly — read the
directory and match the entry byte-for-byte, or normalise both sides through a
single helper — and write beside the helper that this is the second half of a
class whose first half was repaired in `verify-dialogs.mjs`.

Neither change may move a verdict on today's tree. Both frozen digests stay
valid because no plan in this phase edits `PageShell.tsx`; state that in the
comment above the re-frozen constant, with the condition under which it stops
being true.
  </action>
  <verify>
    <automated>node scripts/verify-conversion.mjs && node -e "import('./scripts/conversion-manifest.mjs').then(m=>console.log('MANIFEST_LOADS'))"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-conversion.mjs` exits 0 with the same five-check,
      8-surface, 53-file line as the baseline
    - The permitted-site constant carries a path and a line number per entry, not
      a bare digest — paste the constant into the SUMMARY
    - **Mutation proof for item 3:** copy a permitted line's exact text into a
      second file, run the gate, and record that it is now **rejected** where
      before it would have been permitted. Remove the probe and re-run to green
    - **Mutation proof for the `existsSync` class:** introduce a case typo in a
      declared manifest path, run `checkManifest`, record that it is refused, and
      record that the refusal message names the path. Restore and re-run
    - `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>A permitted site is a place, and the case-sensitivity class is closed on both sides.</done>
</task>

<task type="auto">
  <name>Task 3: The scanned set gains route-adjacent files, and /admin is settled in writing</name>
  <files>scripts/verify-conversion.mjs, scripts/conversion-manifest.mjs</files>
  <read_first>
    - `scripts/verify-conversion.mjs` around line 1358 — where a declared surface's file set is assembled from `importClosure()`
    - `scripts/verify-conversion.mjs` lines 2562-2564 — check D, and how a surface enters `pagesWithoutShell` when its page file does not import `PageShell`
    - `scripts/conversion-manifest.mjs` lines 5-16 — the docblock's first paragraph, where *whole* is defined
    - `scripts/conversion-manifest.mjs` lines 206-214 — the neighbouring list that already refuses a claim; the nearest precedent for declining to declare something
    - `src/app/(admin)/admin/(work)/page.tsx` — 39 lines, zero `className`, ending in a redirect
    - `src/app/(admin)/admin/(work)/members/loading.tsx` — the one converted loading file, and the shape the other seven take
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §1.5 and §1.6
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §1e and §4.1
  </read_first>
  <action>
**D-41.1-21 — extend the scanned set.** For each declared surface, after the
import closure is assembled, add the **route-adjacent files that sit beside its
page file**: `loading.tsx`, and `error.tsx` / `not-found.tsx` where they exist.
One `existsSync` per surface per name, using the case-exact helper from task 2.
Measured today, `(work)` holds zero `error.tsx` and zero `not-found.tsx`, so the
second and third names are prevention; say so in the comment rather than letting
a later reader think they were guessed.

**The failure direction is the safe one and must be stated:** the set grows, so
the gate finds **more**, and a file that was never measured stops passing by
absence. Write that sentence above the extension. Also write what it still does
not reach — a file the router mounts that is neither a page nor route-adjacent —
in the fence vocabulary (*UNMEASURED, not approved*).

Checks A and B now run over the enlarged set. On today's tree that changes
nothing, because the only declared surface with a `loading.tsx` is
`/admin/members` and its loading file is clean — which is itself the evidence
that the work is real and that nothing would have caught its absence. Assert
that: run the gate and record that the scanned-file count rises by exactly the
number of route-adjacent files the eight declared surfaces have, and that the
verdict is unchanged.

**§1.5 — `/admin` is not a declared surface, and the manifest says why.**
`src/app/(admin)/admin/(work)/page.tsx` has no markup and ends in a redirect, so
it can never import `PageShell` and check D would push it into
`pagesWithoutShell` — a red on a correct file, which is the failure that gets a
gate switched off. Take resolution (1): do not declare it, and write the phrase
into the manifest's docblock so the next reader does not count 24 pages and find
23. The docblock sentence names the file, states that criterion 1 reads *every
page that renders a surface*, and cites `conversion-manifest.mjs:206-214` as the
neighbouring precedent for refusing a claim. **This is decided now, before check
D's first red run** — an exemption discovered on a red run is an exemption
nobody trusts.
  </action>
  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"; grep -c "loading.tsx" scripts/verify-conversion.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-conversion.mjs` exits 0 and `CONVERSION_OK` still
      reports 5 checks and 8 surfaces; the scanned-file count is 53 plus exactly
      the number of route-adjacent files the eight declared surfaces carry —
      state that number and how it was counted in the SUMMARY
    - `grep -n "loading.tsx" scripts/verify-conversion.mjs` returns the extension
      site, and the surrounding comment contains the sentence that the set grows
      so the gate finds more
    - **Mutation proof:** add a legacy token utility to
      `src/app/(admin)/admin/(work)/members/loading.tsx`, run the gate, record
      that check A now **fails** naming that file, then revert the file and re-run
      to green. Before this task the same mutation produced a green
    - `grep -n "renders a surface" scripts/conversion-manifest.mjs` returns the
      docblock sentence that settles `/admin`
    - `grep -n "(work)/page.tsx" scripts/conversion-manifest.mjs` returns the
      sentence naming the file that is deliberately not declared
    - `/admin` is absent from `CONVERTED`
    - `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>Route-adjacent files are measured, the growth direction is declared, and `/admin` is settled in writing before any red run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → gate | a declared entry makes the gate assert the right thing about a named file; a wrong entry asserts it about the wrong one |
| router → file | a route-adjacent file is mounted without an import, so it is invisible to a closure walk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-10 | Repudiation | criterion 3 | mitigate | the scanned set gains route-adjacent files, so 149 legacy utilities in seven loading files stop passing by absence; proved by the mutation on `members/loading.tsx` |
| T-41.1-11 | Spoofing | a permitted navigation-property site | mitigate | the permit is keyed on path and line number as well as digest; proved by copying a permitted line into a second file and recording the rejection |
| T-41.1-12 | Repudiation | a false header sentence | mitigate | the advertisement and its unreachable branch are both deleted and the gap is declared *UNMEASURED, not approved* |
| T-41.1-13 | Spoofing | a declared path with a case typo | mitigate | the three manifest `existsSync` calls compare case-exactly; one verdict on any filesystem |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `node scripts/verify-conversion.mjs` exits 0, `CONVERSION_OK`, 5 checks, 8 surfaces
- `npm run verify` exits 0 with no refusal
- Three recorded mutation proofs: the copied permitted line, the manifest case typo, the legacy token planted in `members/loading.tsx`
</verification>

<success_criteria>
`verify-conversion.mjs` no longer advertises a guard it cannot deliver, permits a
site by place rather than by phrase, scans the files the router mounts beside a
declared page, and the manifest explains in writing why the work surface has 24
pages and 23 declarations.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-04-SUMMARY.md` when done,
with the three mutation proofs, the new scanned-file count and how it was derived,
and the manifest docblock sentence quoted.
</output>
