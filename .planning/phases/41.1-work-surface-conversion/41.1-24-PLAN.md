---
phase: 41.1-work-surface-conversion
plan: 24
type: execute
wave: 9
depends_on: ["41.1-21", "41.1-22", "41.1-23"]
files_modified:
  - scripts/conversion-manifest.mjs
  - src/app/(admin)/admin/(work)/layout.tsx
  - scripts/verify-tables.mjs
  - scripts/verify-dialogs.mjs
  - scripts/verify-breakpoints.mjs
autonomous: false
requirements: [DS-07, DS-09, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "Every page under the work surface that renders a surface is declared converted"
    - "The (work) layout shim is deleted, not left behind — one element remains and it only declares"
    - "The table gate's REMAINING list is empty"
    - "The dialogs and breakpoints gates report zero for the work group"
    - "A person has looked at the last five surfaces at three widths"
    - "Wave 7's five event-form satellites were observed inside wave 7 by plan 41.1-19, not accumulated here"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/layout.tsx"
      provides: "the navigation-mounting layout — declaring the clearance, no longer applying it, no inner element"
    - path: "scripts/verify-tables.mjs"
      provides: "the table gate at zero"
      contains: "WORK GROUP REMAINING"
  key_links:
    - from: "src/app/(admin)/admin/(work)/layout.tsx"
      to: "src/components/ui/PageShell.tsx"
      via: "the layout declares the clearance and the shell consumes it — D-41-03's original design, reached"
      pattern: "nav-inset-inline-start"
---

<objective>
Close the phase: declare the last five surfaces, delete the layout shim in the
same commit, empty the work group's debt lists on the tree's authority, and put a
person in front of the five surfaces waves 7 and 8 converted.

Purpose: criterion 1 has two halves — *every page under the work surface has
converted whole*, **and** *the shim Phase 41 wrote to protect the unconverted ones
is deleted, not left behind*. D-41.1-17 named this plan at planning time rather
than leaving it to be discovered, because the shim's own docblock already declares
its exit and criterion 1 makes its removal a condition of the phase closing, so it
cannot be inherited by Phase 41.2.

Output: 28 declared surfaces, one layout element that only declares, three gates
reporting zero for the work group, and the phase's four criteria each proved by
the command that proves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-VALIDATION.md
@.planning/phases/41.1-work-surface-conversion/41.1-15-SUMMARY.md
@.planning/phases/41.1-work-surface-conversion/41.1-21-SUMMARY.md
@.planning/phases/41.1-work-surface-conversion/41.1-22-SUMMARY.md
@.planning/phases/41.1-work-surface-conversion/41.1-23-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The last five declarations and the shim's second step — one commit, because the ordering rule requires it</name>
  <files>scripts/conversion-manifest.mjs, src/app/(admin)/admin/(work)/layout.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/layout.tsx` — as left by plan 41.1-05 at **state 1**: the outer element declares the column clearance at the `md` tier **and** applies it as leading padding; the inner element still redeclares the variable as zero for its subtree. Lines 83-87 carry the docblock that already declares this exit; lines 138-141 carry the measured warning about an abbreviated arbitrary property
    - `src/components/ui/PageShell.tsx` lines 151-168 — the consumer. **It is not edited, and that is what keeps the two frozen digests in `verify-conversion.mjs` valid**
    - `scripts/conversion-manifest.mjs` — `CONVERTED` with its 23 entries, and the held `/admin/finance` entry left in a comment by plan 41.1-16
    - `scripts/conversion-manifest.mjs` lines 417-422 — `checkManifest()`
    - The four SUMMARYs in context, for the entry texts they reported
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §6.2 (the three states, written out) and **§6.3 (what breaks if the two steps are done in the wrong order)**
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.8
  </read_first>
  <action>
**These two edits are one commit. The ordering rule is: *the layout may stop
padding only in the same commit that declares the last page converted, and may
not stop declaring at all.*** Splitting them produces one of two loud failures,
both measured in advance: remove the inner element early and every converted page
gets 448 pixels of leading clearance at tablet width and up, squeezing content
into 320 pixels on a 768-pixel screen; remove the outer padding early and every
still-unconverted page sits under the navigation column, which is the exact
failure the shim was written to prevent, reintroduced at the end.

**Add the last five `CONVERTED` entries**, taking each text from the SUMMARY that
reported it: `/admin/finance` **wide** (the held entry, now releasable because the
refund dialog was converted in plan 41.1-17), `/admin/events/[id]/sales` **wide**,
`/admin/events/[id]/tickets` **wide**, `/admin/events/new` **default**,
`/admin/events/[id]/edit` **default**. Each reason carries the plan number and
what *whole* meant, the width with its reason, and the no-behaviour-change
sentence.

That brings `CONVERTED` to **28**: the 8 Phase 41 declared plus the **20** work
pages this phase converted. Twenty-four page files exist under the work surface;
`/admin` is not among them because it renders nothing and the manifest docblock
says so in writing — **state the arithmetic in the SUMMARY** so nobody counts 24
and finds 23 and reads it as a gap.

**The shim reaches state 2.** The inner element goes; the outer element **stops
padding and keeps declaring**. What remains is one element carrying the
`md`-tier declaration of the column clearance, the navigation mount, and the
children. Every page below now renders the shell, which reads the declared value
and applies the clearance itself — D-41-03's original design, reached at last.

Write the utility whole, never abbreviated: the file's own comment records that an
abbreviated arbitrary property emits a malformed rule and a build warning, measured
on the first build. Describe it in prose in any comment; never spell it.

Update the docblock: the exit condition it declared is now spent, and the
paragraph says so with the plan that spent it, in the house shape where the
superseded sentence stays visible beside its measurement.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /dev/stderr | grep -ci "malformed\|warn" ; node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-conversion.mjs` exits 0, reports **28 surfaces**, all
      five checks passing, and **check E still names the two-file responsive-mount
      set** — the layout must still be one of them, because it may not stop
      declaring
    - `checkManifest()` raises no width disagreement
    - `grep -c "nav-inset-inline-start" "src/app/(admin)/admin/(work)/layout.tsx"`
      returns exactly **1** — the declaration. The applied padding and the inner
      element's zeroing declaration are both gone
    - `grep -c "ps-\[var(--nav-inset-inline-start)\]" "src/app/(admin)/admin/(work)/layout.tsx"`
      returns 0
    - The layout has **one** element around the children where it had two — quote
      the before and after
    - `git show --stat HEAD` shows the manifest and the layout **in the same
      commit**; paste it
    - `npm run build` exits 0 and its output contains **no** Tailwind malformed-rule
      warning
    - `git diff --name-only` shows no change to `src/components/ui/PageShell.tsx`,
      and `verify-conversion.mjs`'s two frozen digests are untouched
    - The SUMMARY states the arithmetic: 24 page files, 1 not a surface, 23
      declared work pages, 28 entries in total
  </acceptance_criteria>
  <done>Twenty-eight surfaces declared and the shim reduced to a single declaring element, in one commit.</done>
</task>

<task type="auto">
  <name>Task 2: The final reconciliation — entries diffed against the tree, and three work-group counts at zero</name>
  <files>scripts/verify-tables.mjs, scripts/verify-dialogs.mjs, scripts/verify-breakpoints.mjs</files>
  <read_first>
    - `scripts/verify-tables.mjs` — `REMAINING` with its last entry, and the exemption constants at lines 355-380 which are **not** on the list and must stay that way
    - `scripts/verify-tables.mjs` lines 424-430 — the model comment for a paid entry
    - `scripts/verify-dialogs.mjs` — `REMAINING` as left by plan 41.1-11, thirteen entries with their group tags
    - `scripts/verify-breakpoints.mjs` — `REMAINING` as left by plan 41.1-16
    - The SUMMARYs of plans 41.1-17, 41.1-19, 41.1-20, 41.1-21 and 41.1-22, for the entries each reported emptied
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §9 P1 — the four recorded mechanisms by which a count falls for the wrong reason
  </read_first>
  <action>
**Diff entries, not counts — for the last time and over every entry on all three
gates.** For each, re-derive from the tree whether it is still owed by grepping
the named file for the needle the gate counts. Produce the three-column table and
put it in the SUMMARY. Only then delete.

Expected, from the wave 7 and 8 SUMMARYs:

- **Tables:** the sales dashboard, paid by plan 41.1-21. `REMAINING` reaches
  **0**. The gate must print its unmissable line saying the work group is empty.
- **Dialogs:** the refund dialog and the refund actions (plan 41.1-17), the reveal
  dialog (plan 41.1-20), and the two creation modals (plan 41.1-19). The work
  group reaches **0**; the list keeps its `public-member-money` entries, which are
  Phase 41.2's.
- **Breakpoints:** the sales dashboard, paid by plan 41.1-21. The work group
  reaches **0**; the five non-work entries stay.

**These are expectations, not instructions.** If the tree and a SUMMARY disagree,
**the tree wins and the disagreement is recorded** with the plan that claimed it.
That is the whole reason this task exists, and it is the last chance the phase has
to catch a count that fell for the wrong reason.

Leave a comment where each deleted entry was, naming the plan and task that paid
it and what the numbers went from and to.

**Two negative obligations, and the first one is the phase's most fragile.** The
permanently exempt diagnostic grid is **not** tidied onto any list: a file that
will never convert on a list that can only shrink guarantees the number never
reaches zero, and a number that cannot reach zero is a number that lies. Assert
its absence by grep after the edit. And **no entry is moved between groups** to
make a count come out right.

**Do not remove the four legacy token names.** Emptying them of consumers is this
phase's; removing them is a bonus and was declared never to become the schedule
(D-41-13). Report the check-D figure as it stands.
  </action>
  <verify>
    <automated>node scripts/verify-tables.mjs && node scripts/verify-dialogs.mjs && node scripts/verify-breakpoints.mjs && npm run verify; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-tables.mjs` exits 0 with `REMAINING = 0` and prints the
      unmissable line that the work group is empty
    - `node scripts/verify-dialogs.mjs` exits 0 with `WORK GROUP REMAINING = 0`
      and its `public-member-money` entries intact
    - `node scripts/verify-breakpoints.mjs` exits 0 with
      `WORK GROUP REMAINING = 0`, its five non-work entries intact, and check A's
      count of the two larger prefixes still zero across all files
    - The three-column entry diff table covers **every** entry on all three gates
    - Each deletion left a comment naming the plan and task that paid it
    - `grep -n "ReviewListClient" scripts/verify-tables.mjs` shows it only in the
      exemption constants
    - `node scripts/verify-tokens.mjs` exits 0; the check-D figure is reported as
      it stands and **no legacy token name is removed**
    - `npm run verify` exits 0 with no refusal; `npm run build` exits 0
  </acceptance_criteria>
  <done>Three gates report zero for the work group, every deletion is backed by the tree, and the exemption is still deliberately absent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: H41-1 for waves 7 and 8, and the phase's four criteria proved by command</name>
  <action>
Stop here. This is a **blocking** checkpoint: nothing in this plan or in the next
wave proceeds until a person has performed the observation below and recorded its
rows. `41.1-VALIDATION.md` states the rule — **an unticked row is the correct
state until a person has looked**, and a row inferred from a neighbouring one is
not an observation. D-41.1-18 puts this per wave rather than accumulated to the
end, because a batch looked at once, at the end, is a batch that gets skipped.
Present the steps below verbatim, collect the answers, and write them into the
SUMMARY as rows — ticked, or `pending` with the reason it could not be performed.
  </action>
  <read_first>
    - `.planning/phases/41.1-work-surface-conversion/41.1-VALIDATION.md` — the six manual observations, the sign-off list, and the rule that an unticked row is the correct state
    - `.planning/phases/41.1-work-surface-conversion/41.1-17-SUMMARY.md` and `41.1-20-SUMMARY.md` — the refund and reveal observations already recorded, so they are not repeated blind
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-CR01-PASS.md` — the row format; a headless render is not a device render
  </read_first>
  <what-built>
The phase is complete in the tree: twenty work pages converted, five tables on one
column declaration each, seven loading placeholders on the skeleton, one new
primitive with four adopters, the three knots converted with their guards held,
the ambient navigation clearance replaced by a declared one, ten gate scripts on a
single proven comment stripper, and the layout shim reduced to a single declaring
element.
  </what-built>
  <how-to-verify>
**Part A — H41-1 for waves 7 and 8: five pages, and five only.** At **390px, 768px
and 1440px** on `/admin/finance`, `/admin/events/{id}/sales`,
`/admin/events/{id}/tickets`, `/admin/events/new` and `/admin/events/{id}/edit`.
**Wave 7's five event-form satellites are deliberately not in this list** — the two
creation modals, the two autocompletes and the venue profile prompt were observed
inside wave 7 by plan 41.1-19's task 3, which is where D-41.1-18 requires them. Part
D checks that those rows exist; it does not re-open them with a glance from here.

1. Nothing scrolls sideways; nothing sits under the navigation column.
2. **No doubled clearance.** This is the first pass after the shim's inner element
   was removed, so it is the pass that would catch 448 pixels of leading padding —
   content squeezed into 320 pixels on a 768-pixel tablet. Look for it
   deliberately.
3. Every control is thumb-reachable, and tabbing shows a visible focus ring.

**Part B — the shim's removal, on a page from every wave.** Open one converted
page from wave 3, one from wave 5 and one from wave 8, at 768px, and confirm each
clears the column by exactly one column's width. Then open `/gallery` — the other
responsive mount, which this phase edited in wave 2 and has not touched since —
and confirm it is unchanged.

**Part C — the four criteria, each by its command.** Run and paste:
- criterion 1: the conversion gate, exit 0 with 28 surfaces; plus the grep showing
  the layout carries one clearance reference and no applied padding;
- criterion 2: the table gate, `REMAINING = 0`;
- criterion 3: the conversion gate's checks A and B over the enlarged scanned set,
  which now includes each declared surface's route-adjacent files;
- criterion 4: the breakpoint gate, `WORK GROUP REMAINING = 0` and the two larger
  prefixes at zero across all files.

**Part D — what is owed and not closed, written down rather than assumed.**
- **RESP-01 is advanced, not closed.** It says *every* surface, so it closes in
  Phase 41.2 and only by a written human pass. Do not tick it.
- **RESP-03's device row** stays `human_needed` unless a large touch screen was
  actually used. The gate estimates from a class string and cannot see a flex
  stretch, an icon height or an ancestor's padding.
- Any step recorded `pending` in plans 41.1-17, 41.1-18, 41.1-20, 41.1-21 or
  41.1-22 — the money and reveal observations that would have required a real
  send or a real refund — stays `pending`, listed by plan. **Deferred is not
  verified**, and this project has already written that down.
- **Plan 41.1-19's H41-1 rows must exist.** Open `41.1-19-SUMMARY.md` and confirm
  it carries rows for the two creation modals, the two autocompletes and the venue
  profile prompt, at three widths. Carry them forward **by reference**, ticked or
  `pending` as they stand. If they are missing, say so here as a missing wave-7
  observation — do not substitute a look from this plan, which would be the
  accumulation D-41.1-18 forbids, one wave late.

Record one row per surface per width for part A, one row per page for part B, the
four command outputs for part C, and the owed list for part D.
  </how-to-verify>
  <resume-signal>Type "approved" with the rows, the four command outputs and the owed list, or name the surface and width where something was wrong.</resume-signal>
  <done>Every row below is recorded in the SUMMARY — ticked, or left `pending` with its reason. None is assumed, and none is inferred from another row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout → every page below it | one element now decides the clearance for twenty-three pages |
| debt list → the phase's exit condition | three counts reaching zero is what closes the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-103 | Denial of service | a doubled or absent column clearance across the whole work surface | mitigate | the two edits land in one commit as the ordering rule requires; check E still asserts the layout declares; a person looks at one page per wave at 768px in part B |
| T-41.1-104 | Repudiation | a `REMAINING` count reaching zero for the wrong reason | mitigate | every entry on all three gates is re-derived from the tree and tabulated; the tree wins over any SUMMARY and disagreements are recorded |
| T-41.1-105 | Tampering | the permanent exemption tidied onto a list to make a number look complete | mitigate | asserted absent by grep after the edit; this is the phase's most fragile negative obligation and it is stated twice |
| T-41.1-106 | Repudiation | a requirement ticked that no observation closed | mitigate | part D: RESP-01 is claimed **advanced, not closed**; RESP-03's device row stays `human_needed` unless a device was used; every `pending` step is listed by plan |
| T-41.1-108 | Repudiation | wave 7's five satellites passing for observed because the closing pass enumerates pages | mitigate | part A says in writing that it is five pages and that the satellites are not among them; part D requires plan 41.1-19's rows to be found and carried by reference, or reported missing |
| T-41.1-107 | Repudiation | the legacy token names removed because the lists are empty | mitigate | D-41-13: emptying is this phase's, removal was declared never to become the schedule; the check-D figure is reported as it stands |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed across the entire phase |
</threat_model>

<verification>
- `npm run verify` exits 0 with no refusal; `npm run build` exits 0 with no Tailwind warning
- `verify-conversion` reports 28 surfaces and check E still names the two-file set
- `verify-tables` `REMAINING = 0`; `verify-dialogs` and `verify-breakpoints` work group 0
- The manifest and the layout are in one commit; `git show --stat` pasted
- Part D's owed list recorded, with RESP-01 explicitly not ticked
</verification>

<success_criteria>
Criterion 1: twenty-eight surfaces declared and the shim deleted rather than left
behind. Criterion 2: the table gate empty. Criterion 3: checks A and B green over
a scanned set that now includes route-adjacent files. Criterion 4: the breakpoint
gate empty for the work group. Criterion 5: the three knots converted with their
domain gates held, each by its own plan with a written impact analysis, each
exercised by a person or explicitly left `pending` with its reason.

**And RESP-01 is recorded as advanced, never as closed.**
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-24-SUMMARY.md` when done,
with the declaration arithmetic, the before/after layout element, the
`git show --stat` proving one commit, the full entry diff table for all three
gates, the four criteria command outputs, and part D's owed list.
</output>
