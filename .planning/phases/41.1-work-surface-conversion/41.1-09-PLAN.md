---
phase: 41.1-work-surface-conversion
plan: 09
type: execute
wave: 3
depends_on: ["41.1-05"]
files_modified:
  - src/app/(admin)/admin/(work)/events/[id]/media/page.tsx
  - src/components/media/MediaReviewGrid.tsx
  - src/app/(admin)/admin/(work)/events/[id]/review/page.tsx
  - src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx
autonomous: true
requirements: [DS-07, DS-09, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The media review surface renders inside the page shell and its grid gains the middle step it skipped"
    - "The night review surface renders inside the page shell at the wide maximum"
    - "The copy-out diagnostic grid is still a table, is still exempt, and is still not on any REMAINING list"
    - "The diagnostic grid's eleven-pixel type size is gone"
  artifacts:
    - path: "src/components/media/MediaReviewGrid.tsx"
      provides: "the media review grid, converted, on the tier boundaries this system has"
    - path: "src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx"
      provides: "the copy-out diagnostic grid — still a table, at the label size"
  key_links:
    - from: "src/app/(admin)/admin/(work)/events/[id]/review/page.tsx"
      to: "src/components/ui/PageShell.tsx"
      via: "the shell, at the wide maximum"
      pattern: "PageShell width=\"wide\""
---

<objective>
Convert the media review surface and the night review surface — and, on the
review surface, honour a **negative** obligation that belongs to the planner
rather than to a diff: the copy-out diagnostic grid stays a table.

Purpose: `ReviewListClient.tsx` is exempt **permanently**, declared before the
gate's first run in three places, and deliberately **not** on `REMAINING` because
a file that will never convert on a list that can only shrink guarantees the
number never reaches zero — and a number that cannot reach zero is a number that
lies. Its own copy says it exists to be pasted into a diagnostic tool. Cards
would destroy the thing it is for. **The obligation here is to convert everything
around it and to leave the grid a grid** (D-41.1-14, D-41-16).

Output: two surfaces converted whole, one exemption intact, and the one line the
exemption explicitly does **not** cover finally paid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). In particular it does
   **not** touch `verify-tables.mjs`'s exemption constants — those are plan
   41.1-03's territory and were deliberately left untouched there too. Report the
   entry texts; the reconciliation plan makes the edits.
2. **No behaviour change:** *"no query changed, no column added, no capability
   check touched, no action payload altered"*. Grep and paste.
3. **Never spell a Tailwind class string inside a comment.**
4. **`sm:` is a map, not a rename** — §2.3, per class.
5. **`FOCUS_RING` is imported**, never re-spelled.
6. **A `Badge` that is a link or a button is a `Chip`.**
7. **Stop condition 3 applies.** Neither page touches money or the venue reveal.
8. **The review surface renders door evidence.** `checkin-offline.md`: a conflict
   is recorded against a ticket, never as a label on a member, and the technical
   detail carries identifiers rather than names or email addresses. **A visual
   conversion changes none of that** — it does not promote an identifier into a
   name, does not reorder the classification, and does not change which role sees
   the technical detail.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: /admin/events/[id]/media and the media review grid</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/media/page.tsx, src/components/media/MediaReviewGrid.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/media/page.tsx` — the route file; its only local import is the grid
    - `src/components/media/MediaReviewGrid.tsx` — 148 lines, 17 class attributes; it is a named entry on `verify-breakpoints.mjs`'s `REMAINING` and this plan is its sole owner
    - **The analog, exact match on the grid axis:** `src/app/(public)/gallery/page.tsx` — the converted grid surface, and note it is the other responsive navigation mount, edited by plan 41.1-05
    - `src/app/(public)/gallery/loading.tsx` — how the same grid is placeholdered
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §1c, §2.1, §2.5
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §2.1 (the two tier boundaries), §2.3 (the per-class map), §4 (the media surface is **not** on the closed wide list), §6.1
    - `.claude/rules/media-and-storage.md` if present, and `.claude/rules/venue-secrecy.md` — a media item can carry an address in its frame
  </read_first>
  <action>
The page is **`default`** — not on §4's closed `wide` list. Apply the four
deletions from the shell-adoption pattern: the page root with its minimum height
and bottom padding; the gutter and top rhythm and every repeated gutter; the
header's bottom padding becoming a margin; the top-level heading becoming
`PageTitle`.

`MediaReviewGrid.tsx` carries the small-prefix debt this plan pays. Read the map
per class: a grid declared at three columns from the small prefix becomes two
columns at `md` and three at `lg`, gaining the middle step it skipped. Take the
gallery grid as the specimen for the column progression, because it is the
converted surface with the same axis.

Every approve and reject control is interactive and therefore reaches the touch
floor. On a review grid the controls sit on top of an image, so **contrast is a
control-boundary question, not a preference**: use the primitives, whose boundary
token measures against the specified minimum, rather than a border colour chosen
against a photograph.

**Nothing about which media a role may review changes.** The queries, the
capability check and the action payloads are untouched, and a media item that is
hidden today is hidden after.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-breakpoints.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the media page file>` returns at least 1 with
      `width="default"`
    - `grep -c "sm:" src/components/media/MediaReviewGrid.tsx` returns 0, and the
      SUMMARY carries a before/after table of every class that moved, so the
      reviewer can see the map was read per class
    - Across both files: zero raw palette utilities, zero legacy token utilities,
      zero `focus:outline-none`, zero `font-medium`, zero `max-w-`
    - `node scripts/verify-breakpoints.mjs` exits 0 and still reports 19 entries
    - `node scripts/verify-touch-targets.mjs` exits 0; ledger pasted in
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean
  </acceptance_criteria>
  <done>The media surface is on the shell and its grid has the middle step it was missing.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/events/[id]/review — convert around the exempt grid, and pay the one line the exemption does not cover</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/review/page.tsx, src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/review/page.tsx` — the route file
    - `src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx` — 423 lines, 35 class attributes; **line 205 carries the eleven-pixel type size**
    - `scripts/verify-tables.mjs` lines 355-380 — the exemption: the path constant, the reason constant, the sentence saying it is deliberately **not** on `REMAINING`, and the explicit statement of what the exemption does **not** cover
    - **The analog, role match:** `src/app/(admin)/admin/(work)/members/register/page.tsx` — a converted work page that is a read-only audit list, at the register width; and `src/app/(admin)/admin/(work)/members/page.tsx` line 159 for the wide call
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.2's closing paragraph — the negative obligation, in the pattern map's own words
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 (the review surface **is** on the closed wide list), §7 (the type scale, and that there is no eleven-pixel step), §8.8
  </read_first>
  <action>
The page is **`wide`** — it is named on §4's closed list. Write the width
explicitly and say so in the reported reason.

Apply the four deletions to the page. Convert `ReviewListClient.tsx`'s class
strings, its controls, its empty state and its error region — **and leave its
table a table.** Do not adopt the data-table primitive here, do not add a card
branch, and do not add this file to any debt list. Its own copy says it exists to
be pasted into a diagnostic tool, and cards would destroy the thing it is for.

**The one debt the exemption explicitly does not cover is the type size at line
205.** The exemption covers the *table*, not the type. Eleven pixels is not a step
in §7's scale; take the label and data size instead, with the accepted
consequence that the grid scrolls slightly more. That consequence is written into
the exemption's own reason, so it is a decision already made — implement it, and
record in the SUMMARY that the debt named at `verify-tables.mjs:109-116` is now
paid.

The night's review list is door evidence. Do not change the classification, do
not promote an identifier into a name, and do not change which role sees the
technical detail — those are `checkin-offline.md`'s, not this plan's.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tables.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the review page file>` returns at least 1 with
      `width="wide"`
    - `grep -c "text-\[11px\]" src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx`
      returns 0
    - `grep -c "<table" src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx`
      returns at least 1 — **the table survives**
    - `grep -c "DataTable" src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx`
      returns 0
    - `grep -n "ReviewListClient" scripts/verify-tables.mjs` shows it **only**
      inside the exemption constants and never inside `REMAINING` — assert this
      after the conversion, because the failure mode here is a later tidy
    - `node scripts/verify-tables.mjs` exits 0 with `REMAINING = 5` and
      `WORK GROUP REMAINING = 5` — unchanged; this plan pays no table entry
    - Across both files: zero raw palette utilities, zero legacy token utilities,
      zero `focus:outline-none`, zero `font-medium`, zero `max-w-`, zero `sm:`
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean, and the SUMMARY states that no
      classification, identifier or role visibility on the review list changed
  </acceptance_criteria>
  <done>The review surface is converted around a grid that is still a grid, and the type-size debt the exemption did not cover is paid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review list → door evidence | the technical detail carries identifiers, and which role sees it is an access decision |
| media grid → a photograph | an uploaded frame can carry an address |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-29 | Information disclosure | the review list's technical detail | mitigate | no classification, no identifier-to-name promotion and no role visibility changes; asserted in the SUMMARY and grepped in the diff |
| T-41.1-30 | Information disclosure | a media item's frame | accept | this plan changes no visibility predicate and no query; which items reach the review grid is unchanged and is `media-and-storage.md`'s question, not this conversion's |
| T-41.1-31 | Repudiation | the permanent table exemption | mitigate | the negative obligation is asserted by grep **after** the conversion: the exempt file appears only in the exemption constants, never on a debt list |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run verify` exits 0 with no refusal; `verify-tables` still `REMAINING = 5`
- The exempt file is asserted absent from every `REMAINING` list after the conversion
- The eleven-pixel type size is gone and the debt is recorded as paid
</verification>

<success_criteria>
Two surfaces converted whole, one grid deliberately left a grid with its
exemption intact, and the single line that exemption never covered finally paid.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-09-SUMMARY.md` when done,
with the small-prefix before/after table, the behaviour-change grep, the two
`CONVERTED` entry texts, the `REMAINING` entries emptied, and the grep proving the
exemption survived.
</output>
