---
phase: 41.1-work-surface-conversion
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/lib/comments.mjs
  - scripts/verify-comment-stripper.mjs
  - scripts/verify-breakpoints.mjs
  - scripts/verify-conversion.mjs
  - scripts/verify-dialogs.mjs
  - scripts/verify-media-strip.mjs
  - scripts/verify-no-viewport-read.mjs
  - scripts/verify-semantic-separation.mjs
  - scripts/verify-sunset-gradient.mjs
  - scripts/verify-tables.mjs
  - scripts/verify-tokens.mjs
  - scripts/verify-touch-targets.mjs
  - scripts/verify-all.mjs
autonomous: true
requirements: [DS-07]
must_haves:
  truths:
    - "One comment stripper exists and ten gate scripts import it; no script carries a private copy"
    - "The stripper is correct on all seven measured comment shapes and refuses an unterminated comment"
    - "Running the ten gates after the extraction produces the same verdicts as before it"
  artifacts:
    - path: "scripts/lib/comments.mjs"
      provides: "stripLeadingComments, liveLinesFrom, liveLines — the single stripper"
      exports: ["stripLeadingComments", "liveLinesFrom", "liveLines"]
    - path: "scripts/verify-comment-stripper.mjs"
      provides: "the mutation harness — seven shapes plus the unterminated refusal"
  key_links:
    - from: "scripts/verify-conversion.mjs"
      to: "scripts/lib/comments.mjs"
      via: "ESM import"
      pattern: "from \"\\./lib/comments\\.mjs\""
    - from: "scripts/verify-all.mjs"
      to: "scripts/verify-comment-stripper.mjs"
      via: "the aggregate gate list"
      pattern: "verify-comment-stripper"
---

<objective>
Extract the comment stripper every gate copies into one module, prove it against
the seven comment shapes that have each blinded or false-reddened a gate, and
point all ten copies at it.

Purpose: **D-41.1-08, the one ordering constraint in this phase that is not
negotiable at plan time.** Every `REMAINING` deletion in 41.1 is a claim these
gates measure, and round 5 measured a money-domain entry
(`src/components/admin/RefundDialog.tsx`) going invisible through exactly this
hole, with the gate printing *"converted; remove this entry"* and exiting 0.
Converting first and repairing after means deleting entries on the authority of a
mechanism known to be blind.

Output: `scripts/lib/comments.mjs`, `scripts/verify-comment-stripper.mjs`, ten
gates importing the module, and a before/after `npm run verify` diff showing no
gate reddened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/deferred-items.md
</context>

<plan_rules>
1. **The probes never enter the repository.** `git status --porcelain` is captured
   before and after every harness run and asserted identical (D-41.1-09).
2. **The mutation is asserted applied before its result is read.** A substitution
   that did not land produces a green that means nothing
   (`.claude/rules/ai-engineering.md`, *prova per mutazione*).
3. **It stays a line-shape state machine and does not become a tokeniser**
   (D-41.1-07). `40-REVIEW.md` WR-07 records that a real comment parser written
   in this repository was unsound.
4. No new package. D-41-20.
</plan_rules>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/lib/comments.mjs — one stripper, seven shapes</name>
  <files>scripts/lib/comments.mjs</files>
  <read_first>
    - `scripts/verify-conversion.mjs` lines 487-595 — family B, the span logic and the JSX multi-line state; this is the strongest existing base
    - `scripts/verify-touch-targets.mjs` lines 302-358 — family D, the block-comment multi-line state (DEF-41-06's G5 fix)
    - `scripts/verify-tables.mjs` lines 216-261 — family C, for the whole-line blanking it does and the state it lacks
    - `scripts/verify-breakpoints.mjs` lines 369-381 — family A, `isCommentLine`, the four-line function with six byte-identical copies (digest `35d258011314`)
    - `scripts/rls-baseline.mjs` — the house precedent for a non-executable module imported by a gate
    - `scripts/verify-breakpoints.mjs` lines 1-130 — the six-movement docblock shape this module's own docblock copies
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §4.1-§4.4 — the four families, the shape matrix, the merged design and the no-redden measurement
    - `.planning/phases/41-shared-primitives-three-tier-layout/deferred-items.md` DEF-41-06 and DEF-41-07
  </read_first>
  <action>
Create the directory `scripts/lib/` and the module `scripts/lib/comments.mjs`.
A subdirectory rather than a third non-executable beside the executables, because
`scripts/` today holds executables plus `conversion-manifest.mjs` and
`rls-baseline.mjs`, and a third at the same level invites the next reader to run
it (Claude's discretion under D-41.1, settled here).

Export exactly three names and nothing else:

- `stripLeadingComments(text)` returning an object with `text` and `unclosed`,
  where `unclosed` is the string `jsx`, the string `block`, or `null`.
- `liveLinesFrom(rawLines)` taking an **array of raw lines** and returning an
  object with `lines` and `unterminated`, where `unterminated` is `null` or an
  object carrying `kind` and `lineNo`. Taking an array is not a convenience: it
  is WR-01's fix, and it is what lets `verify-dialogs.mjs`'s self-check exercise
  the multi-line state instead of half the pipeline.
- `liveLines(absPath)` returning the same shape, reading the file.

Take family B's span logic as the base, and add, each named in a comment beside
the branch it fixes:

- family D's **block-comment multi-line state**, so a multi-line `/* … */` with
  prose body lines does not hand its body back as code (shape W3, and DEF-41-06's
  S2 for a block comment inside a JSX opening tag);
- **CR-01's terminating-line span** — the multi-line branch resumes
  span-stripping after the closer instead of pushing an empty string, so live
  code on the line that closes a multi-line JSX comment stays visible;
- **CR-02's whitespace-tolerant closer** — the closer is a run-time-assembled
  regular expression matching the block-comment terminator followed by optional
  whitespace and then the closing brace, never the exact three-character token.
  Assemble it at run time from its pieces rather than writing the literal, the
  same discipline `MAX_WIDTH_RE` (`verify-conversion.mjs:2546`) and
  `MIN_HEIGHT_RE` (`:2656`) already use and for the reason stated at `:2654-2655`;
- **WR-04's per-opener `searchFrom`** — the JSX opener searches for its closer
  from offset 2 so the degenerate `{/*/}` still blanks whole; the block opener
  searches from offset 2 so `/*/` is not read as already closed; the docblock
  continuation star searches from offset 0.

Signal an unterminated comment through the returned `unterminated` / `unclosed`
fields. **The module never calls `process.exit`** — refusing is the caller's
decision and the caller owns its own exit code.

Write the module docblock in the house six-movement shape read from
`verify-breakpoints.mjs:1-130`: the title in the imperative; `WHAT IT ASSERTS, in
one sentence`; the decision it encodes by ID (D-41.1-07, D-41.1-08, D-41.1-09)
and the four families it replaces; `WHY A STRUCTURAL CHECK AND NOT A TEST` naming
Guardrail 1; `WHAT A GREEN DOES NOT MEAN` — and there the stated error direction
is inherited verbatim from family D: **a line whose first characters are a block
opener inside a string blanks more than it should**, because this is a line-shape
state machine with no notion of strings, template literals or regex literals; and
`THE SHAPES`, listing S1, S2, S3, S4, S5, W3, W4 and U1 each with the review
entry that found it.

Do not spell any Tailwind class string in a comment anywhere in this module
(`PageShell.tsx:64-67`; DEF-41-01 is open and `.planning/` is in Tailwind's scan
path).
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./scripts/lib/comments.mjs').then(m=>{const k=Object.keys(m).sort().join(',');if(k!=='liveLines,liveLinesFrom,stripLeadingComments')throw new Error('exports: '+k);console.log('EXPORTS_OK '+k)})"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/comments.mjs` exists and the command above prints
      `EXPORTS_OK liveLines,liveLinesFrom,stripLeadingComments` — exactly three
      exports, no more
    - `grep -c "process.exit" scripts/lib/comments.mjs` returns 0
    - `grep -n "unterminated" scripts/lib/comments.mjs` returns at least one line
      inside `liveLinesFrom`
    - The closing-token regex is assembled at run time:
      `grep -cF '*/}' scripts/lib/comments.mjs` returns 0
    - The docblock contains the literal strings `WHAT IT ASSERTS`,
      `WHY A STRUCTURAL CHECK AND NOT A TEST` and `WHAT A GREEN DOES NOT MEAN`
    - The docblock names `D-41.1-07`, `DEF-41-06`, `CR-01`, `CR-02`, `WR-04`
  </acceptance_criteria>
  <done>The module exists, exports three names, refuses nothing itself, and carries the house docblock.</done>
</task>

<task type="auto">
  <name>Task 2: The mutation harness — seven shapes, the refusal, and no file left behind</name>
  <files>scripts/verify-comment-stripper.mjs, scripts/verify-all.mjs</files>
  <read_first>
    - `scripts/lib/comments.mjs` — the module written in task 1
    - `scripts/verify-dialogs.mjs` lines 816-846 and 1234-1261 — `MATCHER_PROBES`, the self-check pattern this harness generalises: probes run before anything is pointed at the tree, a disagreement **refuses** rather than fails
    - `scripts/verify-breakpoints.mjs` lines 338-342 — `refuse()`, exit 2, *"a refusal is not a failure: it means the measurement did not happen"*
    - `scripts/verify-all.mjs` — the aggregate, and how it counts refusal separately from failure
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §4.2 (the shape matrix and what BLIND / FALSE-RED mean) and §4.5 (the harness's six properties)
  </read_first>
  <action>
Create `scripts/verify-comment-stripper.mjs`, an executable gate that proves the
module of task 1 against the eight probes and leaves nothing behind.

**Where the probes live.** Create a fresh directory per run with
`fs.mkdtempSync` under `os.tmpdir()` — outside the repository by construction —
and remove it in a `finally`. Capture `git status --porcelain` before the first
write and after the removal and **refuse** (exit 2) if the two differ. D-41.1-09
and round 5's own reproduction discipline.

**The eight probes**, each written as a file, each with the review entry that
found it in its label:

- S1 — a multi-line JSX comment with prose body lines (DEF-41-02). Needle inside
  the comment. Expect hidden.
- S2 — a block comment **inside a JSX opening tag** with a prose body
  (DEF-41-06). Needle inside the comment. Expect hidden.
- S3 — a single-line closed JSX comment with **live code after the closer**
  (round 5). Needle is real code. Expect visible.
- S4 — the **terminating line** of a multi-line JSX comment carrying live code
  (CR-01). Needle is real code. Expect visible.
- S5 — a JSX comment whose closer has whitespace before the closing brace
  (CR-02); without the fix this blinds to end of file. Needle is real code on a
  later line. Expect visible.
- W3 — a multi-line **block** comment with prose body lines (WR-03). Needle
  inside the comment. Expect hidden.
- W4 — the degenerate opener that is one comment, not two (WR-04). Needle inside
  the comment. Expect hidden.
- U1 — a comment opened and never closed. Expect `unterminated` to be non-null
  and to report line 1.

**Assert the mutation applied before reading its result.** After writing each
probe, read it back from disk and assert it is byte-equal to what was written.
If the read-back differs, `process.exit(2)` — *refuse* — and print that nothing
was measured. A green from a substitution that did not land is a green that means
nothing.

**Assert in both directions.** Every shape declares whether its needle is real
code (expect visible) or inside a comment (expect hidden). A harness that only
checked "hidden" would pass a stripper that blanks everything, so both
directions are asserted and a one-directional pass is a failure.

**Print the pre-fix families beside the merged one.** Extract the four incumbent
family functions into local copies inside the harness (family A from
`verify-breakpoints.mjs:369-381`, B from `verify-conversion.mjs:487-595`, C from
`verify-tables.mjs:216-261`, D from `verify-touch-targets.mjs:302-358`), run them
on the same probes, and print the matrix with the merged column beside them. That
is what makes the table evidence rather than a claim, and it is the record that
survives after the four copies are deleted in task 3.

Exit 1 on any disagreement between expectation and measurement for the merged
module. Exit 2 on a read-back mismatch or a dirty `git status`. Exit 0 with the
matrix printed loudly, in the house shape.

Then register the new gate in `scripts/verify-all.mjs` alongside the existing
ones, so it becomes visible to someone who is not the person who wrote it.
  </action>
  <verify>
    <automated>node scripts/verify-comment-stripper.mjs; echo "exit=$?"; git status --porcelain | grep -E "^\?\? scripts/|^ M scripts/lib" || true</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-comment-stripper.mjs` exits 0 and its output contains a
      row for each of S1, S2, S3, S4, S5, W3, W4 and U1
    - The printed matrix shows the merged module correct on all eight, and shows
      at least one incumbent family incorrect on S2, S4, S5 and W3 — the defect
      and its repair on the same page
    - `git status --porcelain` is byte-identical before and after the run
      (capture both into the SUMMARY)
    - Mutating the module to always return the raw text makes the harness exit
      non-zero — run it, record the output, and restore the module. This is the
      proof the harness is not vacuous
    - `grep -n "verify-comment-stripper" scripts/verify-all.mjs` returns a line
    - `npm run verify` runs the new gate and reports it, and the aggregate still
      distinguishes refusal (exit 2) from failure (exit 1)
  </acceptance_criteria>
  <done>The harness proves all eight shapes, refuses on a failed mutation, leaves no file behind, and is in the aggregate.</done>
</task>

<task type="auto">
  <name>Task 3: Point all ten gates at the module, and prove nothing reddened</name>
  <files>scripts/verify-breakpoints.mjs, scripts/verify-conversion.mjs, scripts/verify-dialogs.mjs, scripts/verify-media-strip.mjs, scripts/verify-no-viewport-read.mjs, scripts/verify-semantic-separation.mjs, scripts/verify-sunset-gradient.mjs, scripts/verify-tables.mjs, scripts/verify-tokens.mjs, scripts/verify-touch-targets.mjs</files>
  <read_first>
    - `scripts/lib/comments.mjs` — the module
    - `scripts/verify-conversion.mjs` lines 487-595 and the `liveSource()` that joins them
    - `scripts/verify-dialogs.mjs` lines 279-387
    - `scripts/verify-tables.mjs` lines 216-261
    - `scripts/verify-touch-targets.mjs` lines 302-358
    - `scripts/verify-breakpoints.mjs`, `scripts/verify-no-viewport-read.mjs`, `scripts/verify-semantic-separation.mjs`, `scripts/verify-tokens.mjs`, `scripts/verify-media-strip.mjs`, `scripts/verify-sunset-gradient.mjs` — the six `isCommentLine` copies at digest `35d258011314`
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §5.1 — the measurement that D-41.1-07's eight is ten, and the three options for the two unnamed scripts
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §4.4 — zero lines become live anywhere; the one line that becomes blank and carries a needle is `src/components/formats/FormatMarker.tsx:128`, which does not match any check today
  </read_first>
  <action>
Capture `npm run verify` **before** any edit and save its full output to the
session scratchpad (not to the repository). This is the baseline the no-redden
claim is diffed against; an inference is not a diff.

Then replace every private stripper with an import of `scripts/lib/comments.mjs`.
**Ten consumers, and the count is a decision, not a copy of a list** — D-41.1-07's
original eight was measured with a grep for the state-machine identifiers and
missed the two scripts carrying only the line-shape half. Take option 1 of
`41.1-PATTERNS.md` §5.1: extend to ten, because the alternative is closing a
phase that says *"one module"* while two byte-identical copies survive in the same
directory, which is the shape DEF-41-06 recorded and this phase exists to close.

Per consumer:

- `verify-conversion.mjs` and `verify-dialogs.mjs` — delete the local
  `stripLeadingComments` / `liveLines` span implementations and import the module;
  keep each gate's own per-run cache `Map`, which belongs to the gate and not to
  the module.
- `verify-tables.mjs` and `verify-touch-targets.mjs` — delete the local
  `liveLines` and import.
- `verify-breakpoints.mjs`, `verify-no-viewport-read.mjs`,
  `verify-semantic-separation.mjs`, `verify-tokens.mjs`, `verify-media-strip.mjs`,
  `verify-sunset-gradient.mjs` — delete `isCommentLine` and the local `liveLines`
  and import. **Before deleting an `isCommentLine` export, grep for importers of
  it**; two of these scripts export it, and an export removed while something
  reads it is a refusal at load time, not a green.

**Every consumer refuses (exit 2) on `unterminated`.** A file with an unterminated
comment is a file the stripper cannot measure, and a gate that keeps going has
produced a green about nothing. Today the refusal fires on zero of the 263 files
under `src/`, so this is prevention rather than a wave-0 blocker — say so in the
refusal message.

Where a consumer's docblock describes its own stripper, correct the paragraph
rather than deleting it, in the house shape read from `PageShell.tsx:42-46`: the
superseded sentence stays visible with the measurement that superseded it,
because a decision undone without its measurement reads as a slip to the next
person.

Finally re-run `npm run verify` and diff it against the captured baseline.
  </action>
  <verify>
    <automated>npm run verify; echo "verify_exit=$?"; grep -c "isCommentLine" scripts/*.mjs | grep -v ":0" || echo "NO_PRIVATE_ISCOMMENTLINE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "stripLeadingComments\|isCommentLine\|liveLines" scripts/*.mjs`
      lists ten files and **every one of them** also matches
      `grep -l "lib/comments.mjs"` — no script defines its own
    - `grep -n "function isCommentLine" scripts/*.mjs` returns nothing
    - `grep -c "unterminated" <each of the ten>` returns at least 1 for each — the
      refusal is wired, not assumed
    - The before/after `npm run verify` outputs are diffed and every gate's
      verdict line is unchanged: `TABLES_OK` `REMAINING = 5`;
      `BREAKPOINTS_OK` 19 files / 37 uses; `DIALOGS_OK` `REMAINING = 14`;
      `CONVERSION_OK` 5 checks / 8 surfaces / 53 files; `TOKENS_OK` 7 checks /
      31 declared / 23 exposed / 8 under check D;
      `SEMANTIC_SEPARATION_OK` 5 checks / 2 exemptions;
      `NO_VIEWPORT_READ_OK` 263 files / 3 needles at zero;
      touch targets `PASSED` 9 elements / 27 files. Paste both into the SUMMARY
    - `npm run verify` exits 0 and reports no refusal
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Ten gates import one module, none carries a copy, every gate refuses on an unterminated comment, and the verdict lines are unmoved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → filesystem | the harness writes files; if it writes them inside the repository it corrupts the tree it is measuring |
| gate → tree | a gate's verdict authorises a `REMAINING` deletion later in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-01 | Repudiation | the ten gates | mitigate | the merged stripper is proved on all eight shapes by asserted mutation before any `REMAINING` entry in this phase is deleted (D-41.1-08) |
| T-41.1-02 | Tampering | the repository working tree | mitigate | probes are written under `os.tmpdir()` via `mkdtempSync`, removed in a `finally`, and `git status --porcelain` is asserted identical before and after |
| T-41.1-03 | Denial of service | `npm run verify` | mitigate | every consumer refuses (exit 2) on an unterminated comment rather than producing a verdict about a file it could not read; the aggregate already counts refusal separately from failure |
| T-41.1-SC | Tampering | npm installs | mitigate | **zero packages are proposed or installed by this plan.** D-41-20 forbids them; `41.1-RESEARCH.md` §10 records the audit did not run because there is nothing to vet |
</threat_model>

<verification>
- `node scripts/verify-comment-stripper.mjs` exits 0 with the eight-shape matrix printed
- `npm run verify` exits 0, no refusal, every verdict line identical to the captured baseline
- `npm run build` exits 0
- `git status --porcelain` shows no file outside `scripts/` touched by this plan
</verification>

<success_criteria>
One stripper module, ten consumers, zero private copies, eight shapes proved by
asserted mutation, and a before/after `npm run verify` diff showing no gate
reddened. **After this plan the phase's later `REMAINING` deletions rest on a
mechanism that has been shown not to be blind.**
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-01-SUMMARY.md` when done.
Record in it: the before/after `npm run verify` outputs in full, the eight-shape
matrix, the two `git status --porcelain` captures, and the output of the
deliberate mutation that proved the harness is not vacuous.
</output>
