---
phase: 41.1-work-surface-conversion
plan: 12
type: execute
wave: 5
depends_on: ["41.1-11"]
files_modified:
  - src/app/(admin)/admin/(work)/analytics/members/page.tsx
  - src/app/(admin)/admin/(work)/analytics/members/loading.tsx
  - src/components/analytics/MemberSpendTable.tsx
  - src/components/analytics/ReferralChainTable.tsx
  - src/components/analytics/RepeatAttendeeCard.tsx
  - src/components/analytics/GuestConversionCard.tsx
autonomous: true
requirements: [DS-07, DS-09, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The member-spend table and the referral chain render as cards on a phone from one column declaration"
    - "The figure that decides money on each of those two tables is a mark, never buried in the meta line"
    - "The member analytics placeholder renders the same maximum as the page it precedes"
  artifacts:
    - path: "src/components/analytics/MemberSpendTable.tsx"
      provides: "the member-spend table, on DataTable, with a precomputed rank"
      contains: "DataColumn"
    - path: "src/components/analytics/ReferralChainTable.tsx"
      provides: "the referral chain, on DataTable, with its disclosure as the expansion apparatus"
      contains: "expansion"
  key_links:
    - from: "src/components/analytics/MemberSpendTable.tsx"
      to: "src/components/ui/DataTable.tsx"
      via: "the table-to-card primitive"
      pattern: "from \"@/components/ui/DataTable\""
---

<objective>
Convert `/admin/analytics/members` and the two tables it owns onto `DataTable`,
so seven columns and a nested disclosure read as cards on a phone from one column
declaration instead of two hand-written branches.

Purpose: DS-09 — a dense table becomes cards on a phone rather than scrolling
sideways. Today both files maintain **two hand-written lists of columns**, and the
primitive exists precisely because two lists drift, and the drift renders,
compiles and looks correct on the machine of whoever added it.

Output: two of the five tables paid, one placeholder adopted, and the entries
reported for wave 6's reconciliation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). Report the entry
   texts and the `REMAINING` entries emptied; wave 6's plan makes the edits.
2. **No behaviour change:** *"no query changed, no column added, no capability
   check touched, no action payload altered"*. Grep and paste.
3. **Never spell a Tailwind class string inside a comment.**
4. **`sm:` is a map, not a rename** — §2.3, per class.
5. **`FOCUS_RING` is imported**, never re-spelled.
6. **D-41.1-13 is the floor:** on a surface that carries money, a figure that
   decides money is a **`mark`**, never `meta`. The `none` slot is permitted only
   where the card carries the fact another way, and the money exception is
   refused outright.
7. **Stop condition 3 applies.** These surfaces read money but move none: no
   status transition, no refund amount, no idempotency key and no webhook path is
   anywhere in this closure. If one appears, halt.
8. **`.planning/` is public.** Nothing in a SUMMARY names a member, an email
   address or a spend figure from real data. Describe the shape, never the row.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: MemberSpendTable onto DataTable — seven columns, a precomputed rank, and the total as the mark</name>
  <files>src/components/analytics/MemberSpendTable.tsx</files>
  <read_first>
    - `src/components/analytics/MemberSpendTable.tsx` — 79 lines, **7 columns** counted from its own header cells; lines 20-76 are the two hand-written branches, line 37 renders the rank from the array index, lines 56-77 are the card branch
    - **The analog, exact match:** `src/components/admin/MemberTable.tsx` — the only existing adopter. Lines 1052-1084 (the column array, verbatim), lines 1394-1442 (the call with all three optional apparatus wired), line 1183 and 1348 (the checkbox sites)
    - `src/components/ui/DataTable.tsx` — lines 22-39 (why two trees and not a display override), lines 41-48 (why one column declaration), line 101 and 206-207 (the slot union and its default), line 109 (`cell` receives only the row — **never its index**), lines 284 and 391 (the two branch roots), lines 408-460 (what each slot renders on the card), lines 77-79 (what the primitive declines by name)
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.2 — the before/after diff that **is** the pattern, and the five axes that differ per table
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §2.2 gap G-a, §2.3 and §2.4
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §8.8
  </read_first>
  <action>
Delete **both** hand-written branches whole — the desktop table with its header
row, its per-cell classes and its row component, and the card branch with its own
container and its own empty state. What replaces them is one array and one call.

**The slot assignment**, which is a judgement about which fact is a row's
identity and is settled here rather than at execution time:

| Column | Slot |
|---|---|
| the rank | `title` prefix, or `none` |
| name | `title` |
| email | `subtitle` — the primitive already truncates it |
| **total spend** | **`mark`** — D-41.1-13; it is the figure the surface exists to read |
| tickets | `meta`, marked as a figure |
| drinks | `meta`, marked as a figure |
| events | `meta`, marked as a figure |

**Gap G-a and its resolution.** The primitive's cell renderer receives only the
row, never its index, so the rank cannot be derived inside a cell. **Precompute
the rank into the row object before passing it** — it is data, and it survives a
re-sort honestly, whereas widening the primitive's cell signature would change a
published contract that already has an adopter. State the choice and its reason
in the file.

**The mark slot gives position, not weight** (gap G-c). The primitive renders a
mark inside a bare span, stacked at the end; the card's title carries the weight.
So the money column's own cell renderer supplies its emphasis. That needs no
contract edit and the renderer already exists.

Every string in the deleted excerpt is retired rather than migrated: the
small-prefix branch switch, the legacy border and ground token names, the
weight-500 utility that this system does not have. The primitive writes the
replacements.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tables.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from \"@/components/ui/DataTable\"" src/components/analytics/MemberSpendTable.tsx`
      returns 1
    - `grep -c "<table" src/components/analytics/MemberSpendTable.tsx` returns 0
    - `grep -c "sm:" src/components/analytics/MemberSpendTable.tsx` returns 0
    - `grep -c "font-medium" src/components/analytics/MemberSpendTable.tsx` returns 0
    - Zero legacy token utilities and zero raw palette utilities in the file
    - The column array declares a slot for **every** column including any `none`,
      and the total-spend column's slot is `mark` — paste the array into the
      SUMMARY
    - The rank is a property of the row object, not derived inside a cell:
      `grep -cE "\bi \+ 1\b|index \+ 1" src/components/analytics/MemberSpendTable.tsx`
      returns 0
    - `node scripts/verify-tables.mjs` exits 0 and still reports `REMAINING = 5` —
      the entry is paid in the tree and reported, not deleted here
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>One column declaration, seven slots, the money figure as a mark, and the rank as data.</done>
</task>

<task type="auto">
  <name>Task 2: ReferralChainTable — the disclosure becomes the expansion apparatus, and the file crosses to the client</name>
  <files>src/components/analytics/ReferralChainTable.tsx</files>
  <read_first>
    - `src/components/analytics/ReferralChainTable.tsx` — 109 lines, **3 columns** plus a nested per-member list; line 33 is the single cell spanning all three columns, lines 36-50 are the flex row inside the summary that the three header cells never align to, lines 38-59 and 73-98 are the native disclosure
    - `src/components/ui/DataTable.tsx` lines 157-164 — the expansion apparatus: it is a **controlled** API and therefore needs state
    - `src/components/admin/MemberTable.tsx` lines 1394-1442 — the expansion wired at a real call site, including the accessible label per row
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §2.2 gap G-b and §2.3's entry for this file — including that adopting the primitive makes this a real table for the first time, which is an improvement **and** a visible change
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.2's per-table axis table — the client-boundary row
    - `.claude/rules/nextjs-architecture.md` — the gate on what a client boundary drags into the bundle
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §8.8
  </read_first>
  <action>
This file is barely a table today: its body holds one row per chain whose single
cell spans all three columns and contains a native disclosure, and the three
header cells label columns that no cell aligns to. **Adopting the primitive makes
it a real table for the first time** — the three become actual cells. Say that in
the file and in the SUMMARY: it is an improvement and it is a visible change, and
a visible change discovered by a reader is worse than one announced by the plan.

Slots: referrer is the `title`; referred is `meta`, marked as a figure; **chain
spend is the `mark`** (D-41.1-13). The nested per-member list becomes the
expansion's render.

**Gap G-b is load-bearing here and it is a declared decision, not a side effect.**
The expansion apparatus is controlled — it takes an is-expanded predicate and a
toggle — so it needs state, and this file is a server component today. Adopting
it makes the file a client component. The referral chain carries member names and
per-member spend; that data is **already rendered to HTML today**, so no new
audience is created, but it becomes props in the server-component payload, and
`nextjs-architecture.md`'s gate on secrets in the bundle asks that such a move be
**stated in the plan rather than discovered in a diff.** It is stated here, and
the file's own docblock records it with that reasoning.

The alternative — keeping the native disclosure inside a cell renderer — was
considered and is refused: it would put the disclosure in one column of the table
branch and nowhere sensible in the card branch, which is exactly the two-lists
drift the primitive exists to prevent.

Give each expandable row an accessible label naming **the row**, not the column,
in the shape the existing adopter uses.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tables.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-no-viewport-read.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from \"@/components/ui/DataTable\"" src/components/analytics/ReferralChainTable.tsx`
      returns 1
    - `head -1 src/components/analytics/ReferralChainTable.tsx` shows the client
      directive, and the file's docblock states the G-b decision with its reason
      and with the sentence that the data is already rendered to HTML today
    - `grep -cE "<details|<summary" src/components/analytics/ReferralChainTable.tsx`
      returns 0 — the native disclosure is replaced, not wrapped
    - The chain-spend column's slot is `mark`; the column array is pasted into the
      SUMMARY
    - `grep -c "sm:" src/components/analytics/ReferralChainTable.tsx` returns 0
    - Zero legacy token utilities, zero raw palette utilities, zero
      `focus:outline-none`, zero `font-medium` in the file
    - `node scripts/verify-no-viewport-read.mjs` exits 0 with its three needles
      still at zero — the client boundary must not become an excuse to decide the
      tier in JavaScript
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean and the SUMMARY names no member and no
      figure from real data
  </acceptance_criteria>
  <done>The referral chain is a real table with a declared expansion, and the client-boundary crossing is stated rather than discovered.</done>
</task>

<task type="auto">
  <name>Task 3: The page, its two cards, and its placeholder</name>
  <files>src/app/(admin)/admin/(work)/analytics/members/page.tsx, src/app/(admin)/admin/(work)/analytics/members/loading.tsx, src/components/analytics/RepeatAttendeeCard.tsx, src/components/analytics/GuestConversionCard.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/analytics/members/page.tsx` — the route file; it imports the two tables and the two cards
    - `src/app/(admin)/admin/(work)/analytics/members/loading.tsx` — 78 lines, **29 legacy token hits, 6 hand-rolled pulse blocks, one small-prefix use**
    - `src/components/analytics/RepeatAttendeeCard.tsx` (31 lines, 6 class attributes) and `src/components/analytics/GuestConversionCard.tsx` (37 lines, 5)
    - **The page analog, exact match:** `src/app/(admin)/admin/(work)/members/page.tsx` line 159 — the wide shell call
    - **The placeholder analog, exact match:** `src/app/(admin)/admin/(work)/members/loading.tsx` — the whole call shape
    - `src/components/ui/Skeleton.tsx` lines 19-27 (a line colour used as a ground is a category error), 20-28 (every count is a literal and stands for nothing), 29-34 (the width must equal the page's), 44-53 (do not append a radius — DEF-41-05, open)
    - `src/components/ui/Card.tsx` line 47
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.1, §2.4, §2.5
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 — this surface **is** on the closed wide list
  </read_first>
  <action>
The page is **`wide`** — named on §4's closed list. Apply the four deletions from
the shell-adoption pattern and convert the two cards onto `Card` and the type
scale.

The placeholder adopts `PageShell` and `Skeleton` at **`wide`**, matching the
page. Three constraints from its analog that a plan must not lose: the width must
equal the page's, or content jumps sideways the moment data lands, which is the
defect a skeleton exists to prevent; every count is a literal and stands for
nothing, because no query runs before a loading file renders — this binds hardest
here, where the real page shows counts; and no radius is appended to a skeleton
line, because the container radius is written after the pill radius in the emitted
sheet and a caller's radius is a line of code that does nothing.

Delete all six hand-rolled pulse blocks and every line-colour-as-ground fill. The
primitive's own fill is deliberately near-invisible, because a placeholder must
read as absent content, not as content.

The placeholder's small-prefix use is mapped per class, not renamed.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-conversion.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the page file>` and `<the loading file>` each return at
      least 1, and **both pass `width="wide"`** — state the matched pair in the
      SUMMARY
    - `grep -c "animate-pulse" <the loading file>` returns 0
    - `grep -c "sm:" <the loading file>` returns 0, and the before/after class map
      is in the SUMMARY
    - Across all four files: zero raw palette utilities, zero legacy token
      utilities, zero `focus:outline-none`, zero `font-medium`, zero `max-w-`
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean
    - The SUMMARY reports the `CONVERTED` entry text and the `REMAINING` entries
      emptied: two on the tables gate, three on the breakpoints gate (the two
      tables and this loading file)
  </acceptance_criteria>
  <done>The member analytics surface is on the wide shell and its placeholder matches its maximum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server component → client bundle | adopting the controlled expansion moves rendered data into the payload as props |
| analytics surface → member spend | this page reads money without moving any |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-41 | Information disclosure | member names and spend crossing into the client payload | mitigate | the crossing is **declared** in the plan and in the file's docblock with the reason (`nextjs-architecture.md`, secrets in the bundle); no new audience is created because the same data is already rendered to HTML |
| T-41.1-42 | Repudiation | a card that drops a column the table has | mitigate | one column declaration, every column naming its slot including `none`; D-41.1-13 refuses the money exception |
| T-41.1-43 | Information disclosure | planning material in a public repository | mitigate | conversion rule 8: no member, no email address and no real spend figure appears in any SUMMARY |
| T-41.1-44 | Tampering | a money figure buried in the meta line | mitigate | the total spend and the chain spend are `mark` slots and their cells supply their own emphasis, since the primitive gives position and not weight |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run verify` exits 0 with no refusal; `verify-tables` still `REMAINING = 5`
- `verify-no-viewport-read` still reports its three needles at zero across all files
- The behaviour-change grep is clean and pasted in
</verification>

<success_criteria>
Two of the five tables read as cards on a phone from one column declaration, the
figure that decides money on each is a mark, the client-boundary crossing is a
declared decision, and the placeholder renders the page's maximum.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-12-SUMMARY.md` when done,
with both column arrays, the behaviour-change grep, the `CONVERTED` entry text,
and the `REMAINING` entries emptied by gate.
</output>
