---
phase: 41.1-work-surface-conversion
plan: 13
type: execute
wave: 5
depends_on: ["41.1-11"]
files_modified:
  - src/app/(admin)/admin/(work)/events/[id]/analytics/page.tsx
  - src/app/(admin)/admin/(work)/events/[id]/analytics/loading.tsx
  - src/components/analytics/DrinkSalesBreakdown.tsx
  - src/components/analytics/RevenueCard.tsx
  - src/components/analytics/AttendanceCard.tsx
  - src/components/analytics/TokenLifecycleCard.tsx
  - src/components/analytics/TicketVelocityChart.tsx
  - src/components/analytics/DrinkPopularityChart.tsx
  - src/components/analytics/MarketInsightsCard.tsx
  - src/components/analytics/PurchaseFunnelChart.tsx
autonomous: true
requirements: [DS-07, DS-09, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The drink sales breakdown reads as cards on a phone from one column declaration"
    - "The revenue figure on that table is a mark, and the redeemed and refunded counts carry semantic tokens rather than raw palette"
    - "The event analytics placeholder renders the same maximum as the page it precedes"
  artifacts:
    - path: "src/components/analytics/DrinkSalesBreakdown.tsx"
      provides: "the drink sales table, on DataTable"
      contains: "DataColumn"
    - path: "src/app/(admin)/admin/(work)/events/[id]/analytics/loading.tsx"
      provides: "the event analytics placeholder, on Skeleton and the wide shell"
      contains: "PageShell"
  key_links:
    - from: "src/components/analytics/DrinkSalesBreakdown.tsx"
      to: "src/components/ui/DataTable.tsx"
      via: "the table-to-card primitive"
      pattern: "from \"@/components/ui/DataTable\""
---

<objective>
Convert `/admin/events/[id]/analytics`: the drink sales table onto `DataTable`,
its eight cards and charts onto the primitives, and its placeholder onto the
skeleton at the page's own maximum.

Purpose: DS-09 for the third of the five tables — and this file carries **four
live raw palette hits** that check A will fail on the moment the surface is
declared, which is exactly the shape a conversion is supposed to remove rather
than recolour.

Output: one table paid, ten files converted, one placeholder adopted, and the
entries reported for wave 6's reconciliation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). Report the entry
   texts and the `REMAINING` entries emptied.
2. **No behaviour change:** *"no query changed, no column added, no capability
   check touched, no action payload altered"*. Grep and paste.
3. **Never spell a Tailwind class string inside a comment.**
4. **`sm:` is a map, not a rename** — §2.3, per class.
5. **`FOCUS_RING` is imported**, never re-spelled.
6. **D-41.1-13 is the floor:** the figure that decides money is a **`mark`**.
7. **Stop condition 3 applies.** This surface reads money and moves none: no
   status transition, no refund amount, no idempotency key, no webhook path. If
   one appears in the closure, halt.
8. **Colour is never the only channel** (`41-UI-SPEC.md` §10). A redeemed count
   and a refunded count that today differ only by hue must still differ by their
   label after the conversion.
9. **`.planning/` is public.** No real figure, no member, no venue and no date
   from real data appears in any SUMMARY.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: DrinkSalesBreakdown onto DataTable — five columns, revenue as the mark, four raw palette hits deleted</name>
  <files>src/components/analytics/DrinkSalesBreakdown.tsx</files>
  <read_first>
    - `src/components/analytics/DrinkSalesBreakdown.tsx` — 71 lines, **5 columns** counted from its own header cells; **lines 38 and 60 carry a green-family text utility and lines 41 and 64 a red-family one** — four live raw palette hits
    - **The analog, exact match:** `src/components/admin/MemberTable.tsx` lines 1052-1084 (the column array) and 1394-1442 (the call)
    - `src/components/ui/DataTable.tsx` — lines 101 and 206-207 (the slot union and its default), lines 408-460 (what each slot renders on the card), lines 284 and 391 (the two branch roots), lines 185-187 (one empty node, one caption)
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §2.3's entry for this file and §2.4's slot summary
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.2
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §5 (the four semantic tokens and the separation), §8.8, §10
  </read_first>
  <action>
Delete both hand-written branches whole and replace them with one column array and
one call.

**The slot assignment**, settled here:

| Column | Slot |
|---|---|
| name | `title` |
| **revenue** | **`mark`** — D-41.1-13 |
| quantity | `meta`, marked as a figure |
| redeemed | `meta`, marked as a figure |
| refunded | `meta`, marked as a figure |

**The four raw palette hits are deleted, not recoloured.** The green-family text
utility on the redeemed figure takes the semantic token for a completed state;
the red-family utility on the refunded figure takes the semantic token for a
critical one. Read `41-UI-SPEC.md` §5 for the two token names and their literal
values on both sides of the separation, and take them from the spec rather than
guessing a near equivalent.

**Colour is not the only channel** (§10). Both figures already carry a label in
the meta row, and the meta slot renders the label with the value — confirm that
survives, because with the hue carrying less weight the label is now the primary
distinction and not a redundancy.

The mark slot renders inside a bare span and gives position, not weight, so the
revenue cell supplies its own emphasis in its renderer.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tables.mjs && node scripts/verify-semantic-separation.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from \"@/components/ui/DataTable\"" src/components/analytics/DrinkSalesBreakdown.tsx`
      returns 1
    - `grep -c "<table" src/components/analytics/DrinkSalesBreakdown.tsx` returns 0
    - `grep -cE "text-(green|red)-[0-9]" src/components/analytics/DrinkSalesBreakdown.tsx`
      returns 0
    - `grep -c "sm:" src/components/analytics/DrinkSalesBreakdown.tsx` returns 0
    - Zero legacy token utilities and zero `font-medium` in the file
    - The column array is pasted into the SUMMARY, every column names a slot, and
      revenue's is `mark`
    - `node scripts/verify-semantic-separation.mjs` exits 0 with its five checks
      and its exemption count unchanged
    - `node scripts/verify-tables.mjs` exits 0 and still reports `REMAINING = 5`
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>One column declaration, revenue as the mark, and the four raw palette hits replaced by the semantic tokens rather than by nearer hues.</done>
</task>

<task type="auto">
  <name>Task 2: The page and its eight cards and charts</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/analytics/page.tsx, src/components/analytics/RevenueCard.tsx, src/components/analytics/AttendanceCard.tsx, src/components/analytics/TokenLifecycleCard.tsx, src/components/analytics/TicketVelocityChart.tsx, src/components/analytics/DrinkPopularityChart.tsx, src/components/analytics/MarketInsightsCard.tsx, src/components/analytics/PurchaseFunnelChart.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/analytics/page.tsx` — the route file; it imports the eight components plus the table
    - each of the seven card and chart components, in full — they are small
    - **The analog, exact match:** `src/app/(admin)/admin/(work)/members/page.tsx` line 159 — the wide shell call, and the four deletions its diff shows
    - `src/components/ui/Card.tsx` line 47 — the card contract
    - `src/components/ui/Typography.tsx` line 53 (`PageTitle`) and line 116 (`SectionHeading`); and the section-heading class string written out at `src/app/(admin)/admin/(work)/formats/page.tsx:179-183`
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.1, §2.5
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 (this surface **is** on the closed wide list), §7 (the type scale — there is no weight-500 step), §2.3 (the per-class breakpoint map)
  </read_first>
  <action>
The page is **`wide`** — named on §4's closed list. Apply the four deletions the
analog's diff shows: the page root with its minimum height and hard-coded bottom
padding; the gutter and top rhythm on the header **and every repeated gutter on
inner sections** — this page has several, so count them; the header's bottom
padding, which becomes a margin; and the hand-written top-level heading, which
becomes `PageTitle`.

Each of the eight becomes a `Card` where it is a bordered rounded container, and
each section heading becomes either the heading primitive or the one class string
written out in the analog — ten spellings collapsed to one is the point.

**The charts are the place to be careful.** A chart that draws with inline styles
or with a colour constant is not reachable by a class-string scanner, so its
palette is checked **by reading the file**, not by trusting a green. If a chart
carries a hard-coded colour that belongs to a format's identity or to the sunset
gradient, that is a `brand-visual-system.md` question and not a conversion:
**halt and report** rather than substituting. The sunset gradient is SunSet's
alone, and a format identification colour is data on a row, never a CSS token.

Any small-prefix use on the page or in a chart is mapped per class: a
three-column grid becomes two columns at `md` and three at `lg`.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-conversion.mjs && node scripts/verify-sunset-gradient.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the page file>` returns at least 1 with `width="wide"`
    - Across the eight files and the page: zero raw palette utilities, zero legacy
      token utilities, zero `focus:outline-none`, zero `font-medium`, zero
      `max-w-`, zero `sm:`
    - Every chart file was **read** for inline styles and colour constants; the
      SUMMARY lists, per chart, what colour source it uses and whether it is a
      token, a semantic token or a hard-coded value. A hard-coded value that
      belongs to a format identity or to the gradient is reported as a halt, not
      substituted
    - `node scripts/verify-sunset-gradient.mjs` exits 0
    - `node scripts/verify-breakpoints.mjs` exits 0 and still reports 17 entries
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean
  </acceptance_criteria>
  <done>The event analytics surface is on the wide shell, its eight components are on the primitives, and every chart's colour source is written down.</done>
</task>

<task type="auto">
  <name>Task 3: The placeholder — 41 legacy hits and 8 pulse blocks, at the page's own maximum</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/analytics/loading.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/analytics/loading.tsx` — 84 lines, **41 legacy token hits, 8 hand-rolled pulse blocks, one small-prefix use**; the heaviest of the seven
    - **The analog, exact match:** `src/app/(admin)/admin/(work)/members/loading.tsx`
    - `src/components/ui/Skeleton.tsx` — its four exports, and lines 19-27, 20-28, 29-34, 36-42, 44-53
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.4 — the whole call shape and the three constraints
  </read_first>
  <action>
Adopt `PageShell` at **`wide`**, matching the page converted in task 2, and
`Skeleton` for every placeholder shape.

The three constraints from the analog, none of which may be lost:

1. **The width equals the page's.** A placeholder at a different maximum makes the
   content jump sideways the moment the data lands, which is the defect a skeleton
   exists to prevent. This page is `wide`; so is its placeholder.
2. **Every count is a literal and stands for nothing.** No query runs before a
   loading file renders, so a placeholder that looked like it knew how many cards
   or rows there will be would be saying something it cannot know. This file
   mirrors an eight-component page, so the literals are chosen to mirror the
   **regions**, not the data.
3. **No radius is appended to a skeleton line.** The container radius is written
   after the pill radius in the emitted sheet, so a caller's radius is a line of
   code that does nothing — DEF-41-05, open, and not this phase's to close.

Delete all eight hand-rolled pulse blocks and every line-colour-as-ground fill:
that is the category error the primitive's own docblock records, a line colour
used as a ground. The primitive's fill is deliberately near-invisible, because a
placeholder must read as absent content and not as content.

The small-prefix use is mapped per class, not renamed.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the loading file>` returns at least 1 with
      `width="wide"`, matching the page — state the pair in the SUMMARY
    - `grep -c "animate-pulse" <the loading file>` returns 0
    - `grep -c "from \"@/components/ui/Skeleton\"" <the loading file>` returns 1
    - `grep -cE "\b(bg|text|border|divide)-(card-border|card|background|foreground)\b" <the loading file>`
      returns 0 — all 41 hits gone
    - `grep -c "sm:" <the loading file>` returns 0, with the before/after class in
      the SUMMARY
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The SUMMARY reports the `CONVERTED` entry text and the `REMAINING` entries
      emptied: one on the tables gate, and the breakpoints entries for this
      loading file and for the drink sales table
  </acceptance_criteria>
  <done>The heaviest placeholder in the phase is on the primitive, at the page's maximum, with no literal that claims to know a count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics surface → event revenue | this page reads money without moving any |
| chart → colour source | a chart can carry a colour a class-string scanner cannot see |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-45 | Tampering | a money figure buried in the meta line | mitigate | revenue is a `mark` and its cell supplies its own emphasis (D-41.1-13) |
| T-41.1-46 | Information disclosure | a chart's hard-coded colour that belongs to a format identity or to the sunset gradient | mitigate | every chart is read rather than scanned; a hard-coded identity colour is a halt and a report, not a substitution (`brand-visual-system.md`) |
| T-41.1-47 | Repudiation | redeemed and refunded distinguished only by hue | mitigate | the semantic tokens replace the raw palette and the meta slot keeps the label, so colour is not the only channel (§10) |
| T-41.1-48 | Information disclosure | planning material in a public repository | mitigate | no real figure, member, venue or date in any SUMMARY |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run verify` exits 0 with no refusal; `verify-tables` still `REMAINING = 5`
- Every chart's colour source is written down in the SUMMARY
- The behaviour-change grep is clean
</verification>

<success_criteria>
The third table reads as cards, its four raw palette hits are replaced by the
semantic tokens rather than by nearer hues, the eight components are on the
primitives with their colour sources recorded, and the phase's heaviest
placeholder is on the skeleton at the page's own maximum.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-13-SUMMARY.md` when done,
with the column array, the per-chart colour-source table, the behaviour-change
grep, the `CONVERTED` entry text, and the `REMAINING` entries emptied by gate.
</output>
