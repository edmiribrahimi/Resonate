---
phase: 41.1-work-surface-conversion
plan: 02
type: execute
wave: 1
depends_on: ["41.1-01"]
files_modified:
  - scripts/verify-dialogs.mjs
autonomous: true
requirements: [DS-07]
must_haves:
  truths:
    - "verify-dialogs' three never-opened branches each guard their own existence claim"
    - "The matcher self-check exercises the multi-line stripper state, not half of it"
    - "Every dialogs REMAINING entry carries a group tag and the gate prints a per-group count"
  artifacts:
    - path: "scripts/verify-dialogs.mjs"
      provides: "the dialog gate with repaired refusals, a real self-check, and per-group debt"
      contains: "WORK GROUP REMAINING"
  key_links:
    - from: "scripts/verify-dialogs.mjs"
      to: "scripts/lib/comments.mjs"
      via: "liveLinesFrom in the matcher self-check"
      pattern: "liveLinesFrom"
---

<objective>
Repair `verify-dialogs.mjs`: two of `DEF-41-07`'s four refusal defects, the
matcher self-check that cannot see the multi-line state it claims to test, the
counter printed before the walk it is supposed to describe, and the group tag
that makes criterion 2 readable off a gate instead of eyeballed.

Purpose: **a criterion that cannot be read off a gate is a claim** (D-41.1-11),
and a refusal branch that fires on a typo depending on the filesystem is a gate
that says two different things on two machines.

Output: one gate, repaired, still green, with `WORK GROUP REMAINING` printed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
</context>

<plan_rules>
- This plan is the **only** plan in wave 1 that may edit `scripts/verify-dialogs.mjs`.
  D-41.1-15's partition, applied to gate files.
- No entry is added to or removed from any `REMAINING` list here. This plan tags
  entries; **paying** an entry belongs to the wave that converts its surface, and
  the deletion belongs to that wave's reconciliation plan (D-41.1-22).
- A refusal is exit 2 and means nothing was measured; a failure is exit 1.
</plan_rules>

<tasks>

<task type="auto">
  <name>Task 1: DEF-41-07 items 1 and 4 — the guard that covers one branch of three, and the typo that has two verdicts</name>
  <files>scripts/verify-dialogs.mjs</files>
  <read_first>
    - `scripts/verify-dialogs.mjs` lines 1183-1210 — `neverOpenedReason()`, the guard at `:1191`, the `NEVER_MEASURED_BY_B` branch at `:1199-1200` and the fence branch at `:1201-1208`
    - `scripts/verify-dialogs.mjs` lines 55-61 — the fence vocabulary (*UNMEASURED, not approved*) this repair speaks in
    - `.planning/phases/41-shared-primitives-three-tier-layout/deferred-items.md` DEF-41-07, items 1 and 3
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §4.6 — both defects re-located on today's tree
    - `CLAUDE.md` Guardrail 6 — macOS/BSD; the house volume is APFS and case-insensitive
  </read_first>
  <action>
**Item 1.** `neverOpenedReason()` has three branches and only the first is
guarded by an existence check. At `:1191` the condition combines "not in the
walked set" with `existsSync`; the `NEVER_MEASURED_BY_B` branch at `:1199-1200`
and the fence branch at `:1201-1208` each return a reason **unconditionally** —
so a path that does not exist gets a confident explanation of why it was never
opened. Give all three branches the same existence obligation: a path that is not
on disk produces a **refusal** naming the path, not a reason.

**Item 4, and it is a class rather than a line.** `existsSync` on the house APFS
volume is case-insensitive while the walk's output is case-exact, so a
case-typo in a declared path **refuses** here and **fails** on a case-sensitive
volume — one typo, two verdicts, depending on the filesystem. Fix it by comparing
against the walk's case-exact `Set` **before** trusting `existsSync`: membership
in `walked` is the authority, and `existsSync` becomes the second opinion, not
the first. State in the repaired comment that this is a class and that
`conversion-manifest.mjs:410,426,436` carry the same exposure — plan 41.1-04
owns those three, and naming the sibling here is what keeps the class from being
re-derived.

Record both repairs in the docblock in the house shape read from
`PageShell.tsx:42-46`: the superseded sentence stays visible beside the
measurement that superseded it.
  </action>
  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-dialogs.mjs` exits 0 and prints `DIALOGS_OK` with
      `REMAINING = 14`, 263 walked, 7 never opened, 256 opened — unchanged from
      the 41.1-01 baseline
    - All three branches of `neverOpenedReason()` are reached only after an
      existence obligation is satisfied; grep the function and paste it into the
      SUMMARY with the three guards visible
    - The `walked` set is consulted before `existsSync` at the repaired site:
      `grep -n "walked.has" scripts/verify-dialogs.mjs` shows the membership test
      preceding the `existsSync` call on the same condition
    - **Mutation proof:** temporarily add a declared path with a deliberate case
      typo, run the gate, and record that it produces the **same** verdict
      (refusal, naming the path) as it would on a case-sensitive volume; then
      remove the probe and re-run to green. Record both outputs
    - The docblock names `DEF-41-07` items 1 and 3 and points at
      `conversion-manifest.mjs:410,426,436` as the same class
  </acceptance_criteria>
  <done>All three branches guard existence, the case-exact set is the authority, and the class is named.</done>
</task>

<task type="auto">
  <name>Task 2: The self-check rebuilt on liveLinesFrom, and the counter moved below the walk</name>
  <files>scripts/verify-dialogs.mjs</files>
  <read_first>
    - `scripts/verify-dialogs.mjs` lines 816-846 — `MATCHER_PROBES`, the probe table
    - `scripts/verify-dialogs.mjs` lines 1234-1261 — the self-check, and `:1247` where a single-line string is fed through `stripLeadingComments` alone under a comment claiming it is *"the path a real line takes"*
    - `scripts/verify-dialogs.mjs` line 1293 — `jsxCommentLinesBlanked`, printed **before** check B walks `src/`
    - `scripts/lib/comments.mjs` — `liveLinesFrom(rawLines)`, the array-taking export written for exactly this
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §4.3 (WR-01) and §4.7 (WR-02)
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.6e — the three properties of the self-check to preserve
  </read_first>
  <action>
**WR-01.** Turn each `MATCHER_PROBES` row's `line` into a **line array** and run
it through `liveLinesFrom` from `scripts/lib/comments.mjs`, so the probe takes
the path a real line takes — including the multi-line state, which lives in
`liveLines` and which the current single-string path is structurally unable to
reach. Keep the three properties the existing shape already has and that make it
worth copying: the probes run **before** anything is pointed at the tree; a
disagreement between a probe's declared verdict and its measurement **refuses**
(exit 2) rather than fails; and the refusal message says in words that nothing was
measured and that no check-B verdict follows.

Add at least two probe rows that only a multi-line array can express: the
terminating line of a multi-line JSX comment carrying a live overlay utility
(CR-01, expect a match), and a JSX comment whose closer has whitespace before the
closing brace followed on a later line by a live overlay utility (CR-02, expect a
match). Before the module landed these two rows were inexpressible; that is the
evidence WR-01 is closed rather than described.

**WR-02.** `jsxCommentLinesBlanked` is printed at `:1293`, which runs before
check B walks `src/`, so the number describes `Dialog.tsx` and not the run — it
cannot report a blindness spike, which is the one thing a counter like this is
for. Split it into two counters with distinct names — one counting comment
**spans** stripped, one counting whole **lines** blanked — and print both
**below** the walk, with the file count they were measured over. In a repository
with no error tracking, a printed counter is one of the few observables a gate
has (`meta-gates.md`), and this one currently observes the wrong thing.
  </action>
  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "liveLinesFrom" scripts/verify-dialogs.mjs` shows the self-check
      calling it; `grep -n "stripLeadingComments(p.line)" scripts/verify-dialogs.mjs`
      returns nothing
    - The probe table contains at least two rows whose `line` field is an array of
      more than one line, and the gate exits 0 with them
    - **Mutation proof:** revert the stripper import in the self-check to the
      single-line path, run the gate, and record that the two new multi-line rows
      disagree and the gate **refuses** (exit 2). Restore and re-run to green.
      This is what proves the self-check can now see a defect it could not see
      before
    - The two counters are printed after the line reporting the number of files
      walked — paste the tail of the output into the SUMMARY showing the order
    - `node scripts/verify-dialogs.mjs` exits 0, `DIALOGS_OK`, `REMAINING = 14`
  </acceptance_criteria>
  <done>The self-check runs the real path and can see both multi-line shapes; the counters describe the run.</done>
</task>

<task type="auto">
  <name>Task 3: The group tag on all fourteen entries, the per-group print, and the two wrong reason strings</name>
  <files>scripts/verify-dialogs.mjs</files>
  <read_first>
    - `scripts/verify-dialogs.mjs` lines 975-1058 — `REMAINING`, all fourteen entries with their reasons
    - `scripts/conversion-manifest.mjs` line 80 and its docblock — `[path, state, reason]`, the third field as a load-bearing column; the tag is the same construction
    - `scripts/verify-breakpoints.mjs` lines 196-217 — the four rules that travel with a `REMAINING` constant
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §5.1 (the fourteen entries classified, with the two wrong reasons) and §5.2 (the closed vocabulary and why a third field beats a per-group list)
  </read_first>
  <action>
Add a **group tag as an additional tuple field** on every one of the fourteen
`REMAINING` entries — not a separate list per group. Three reasons, and the
first is the decisive one: the reason must travel with the entry
(`verify-routes.mjs:130-152`'s established form), and splitting into per-group
lists puts the group in the list **name**, leaving an entry able to sit in the
wrong one silently. Two of these entries are already mislabelled in prose; a
per-group list would have made them structurally wrong instead of merely wrong.

**The vocabulary is closed at four values:** `work`, `public-member-money`,
`phase-42`, `exempt`. Validate at load: an unknown or missing tag is a
**refusal** (exit 2), because an untagged entry makes the per-group count
meaningless and a meaningless count is worse than no count.

**Assign the fourteen tags from the measured importers, not from the path
prefix.** `work`: `components/venues/EditVenueButton.tsx` only.
`public-member-money`: the six `(public)/**` entries, `components/media/MyMediaSection.tsx`,
`components/artists/EditArtistButton.tsx`, `components/venues/CreateVenueModal.tsx`,
`components/artists/CreateArtistModal.tsx`, and the three work-reached knots
`admin/events/[id]/tickets/RefundActions.tsx`, `components/admin/RefundDialog.tsx`
and `admin/events/[id]/reveal/RevealVenueDialog.tsx` — those three are reached
from the work surface and are converted in this phase's last wave, so tag them
`work` and say in the reason that they are reached through a work page's import
closure and are paid by the knot plans (D-41.1-19). Deriving a tag from a glob is
exactly what would have misfiled `EditArtistButton.tsx`, whose only importer is
`src/app/(public)/artists/[slug]/page.tsx:5` despite the path reading like an
admin component.

**Correct the two wrong reason strings.** `:1026` says
*"the artists work surface"* for `EditArtistButton.tsx` — no work page reaches
it. `:1044` says *"the venues work surface and the event form"* for
`CreateVenueModal.tsx` — its only importer is `src/components/events/EventForm.tsx:10`.
Re-derive each corrected reason from `grep -rln` on the importer and write the
command's result into the reason. Record the class beneath the constant: five
reason strings across two gates named the wrong surface, each true of an
intention and never of the tree — a reason travels with an entry **so that it
stays true**, and five that are not is a pattern, not five typos.

**Print the per-group counts**, and print, on its own unmissable line, the count
criterion 2 and criterion 4 are read off — the work-group total — using the same
loud treatment `STALE` already gets. When the work count reaches zero, say so in
one line that cannot be missed.
  </action>
  <verify>
    <automated>node scripts/verify-dialogs.mjs | grep -E "REMAINING|WORK GROUP"; echo "exit=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-dialogs.mjs` exits 0 and its output contains a line
      matching `WORK GROUP REMAINING = 4` (EditVenueButton plus the three
      work-reached knots) and a per-group breakdown summing to 14
    - Every entry in `REMAINING` carries a tag from the closed set; the gate
      **refuses** if one does not — prove it by temporarily setting one tag to an
      invented value, recording exit 2 and the message, then restoring
    - `grep -n "the artists work surface" scripts/verify-dialogs.mjs` returns
      nothing; the corrected reason for `EditArtistButton.tsx` names
      `src/app/(public)/artists/[slug]/page.tsx` as its only importer
    - `grep -n "the venues work surface" scripts/verify-dialogs.mjs` returns
      nothing; the corrected reason for `CreateVenueModal.tsx` names
      `src/components/events/EventForm.tsx` as its only importer
    - `REMAINING` still holds exactly 14 entries — this plan tags, it does not pay
    - `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>Fourteen tagged entries, a refusing validator, a per-group print, and two reasons that are true of the tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → reader | the printed debt is what a later plan deletes an entry on the authority of |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-04 | Repudiation | `verify-dialogs.mjs` self-check | mitigate | the self-check runs the real multi-line path via `liveLinesFrom`, and a disagreement refuses rather than fails |
| T-41.1-05 | Spoofing | a declared path with a case typo | mitigate | the case-exact `walked` set is the authority; `existsSync` is the second opinion — one verdict on any filesystem |
| T-41.1-06 | Information disclosure | the corrected reason strings | accept | they name file paths already public in this repository; no person, no venue, no date |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `node scripts/verify-dialogs.mjs` exits 0, `DIALOGS_OK`, `REMAINING = 14`, `WORK GROUP REMAINING = 4`
- `npm run verify` exits 0 with no refusal
- Three recorded mutation proofs: the case typo, the reverted self-check path, the invalid tag
</verification>

<success_criteria>
`verify-dialogs.mjs` refuses where it used to explain, tests what it claims to
test, counts what it claims to count, and prints the work-group debt as a number
a criterion can be read off.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-02-SUMMARY.md` when done,
with the three mutation proofs and the gate's full output pasted in.
</output>
