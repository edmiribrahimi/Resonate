---
phase: 41.1-work-surface-conversion
plan: 08
type: execute
wave: 3
depends_on: ["41.1-05"]
files_modified:
  - src/app/(admin)/admin/(work)/events/[id]/guest-list/page.tsx
  - src/app/(admin)/admin/events/[id]/guest-list/GuestListClient.tsx
  - src/components/guest-list/GuestListUnavailable.tsx
  - src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx
  - src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx
autonomous: false
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The guest list renders inside the page shell at the wide maximum"
    - "The assignments surface renders inside the page shell at the default maximum"
    - "A toast raised on the guest list at 768px has been looked at by a person"
    - "No small-prefix breakpoint survives in the assignments client"
  artifacts:
    - path: "src/app/(admin)/admin/events/[id]/guest-list/GuestListClient.tsx"
      provides: "the guest list, converted — and the tree's only toast consumer"
      contains: "PageShell"
    - path: "src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx"
      provides: "the per-night capability assignment surface, converted"
  key_links:
    - from: "src/app/(admin)/admin/events/[id]/guest-list/GuestListClient.tsx"
      to: "src/components/toast/ToastContext.tsx"
      via: "the only useToast consumer in the tree"
      pattern: "useToast"
---

<objective>
Convert the guest list and the per-night assignments surface — and, on the guest
list, make a person raise a toast at 768px and look at it.

Purpose: `GuestListClient.tsx:73` is the **only** consumer of `useToast` in the
whole tree, and plan 41.1-05 moved the toast's inline offset to a fixed value
with a measured 32px overlap of the navigation column at exactly 768px. That
residue was shipped as a recorded loss, not as a substitution, and **this is the
plan where someone finds out whether the loss is acceptable.** If it is not,
`D-41.1-03` reopens on evidence rather than on preference.

Output: two surfaces converted whole, and one human observation that no script in
this repository can make.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). Report the
   `CONVERTED` entry texts and the `REMAINING` entries emptied.
2. **No behaviour change:** *"no query changed, no column added, no capability
   check touched, no action payload altered"*. Grep the diff for `select(`,
   `CAP.`, `.eq(`, `.filter(`, `capabilities.has`, `redirect(` and paste it.
3. **Never spell a Tailwind class string inside a comment.**
4. **`sm:` is a map, not a rename** — `41-UI-SPEC.md` §2.3, per class.
5. **`FOCUS_RING` is imported from `src/components/ui/Button.tsx:71`.**
6. **A `Badge` that is a link or a button is a `Chip`** (§8.5).
7. **Stop condition 3 applies** — no progressivo is renumbered, re-sorted or
   re-derived. Neither page touches money.
8. **The guest list is a gating surface.** `community-membership.md`: every way in
   that bypasses approval is an exception to the gating mechanism, not a
   convenience feature. A visual conversion **changes no entry path, no
   attribution and no count** — if a layout change would drop the record of who
   added an entry, that is a halt, not a styling decision.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: /admin/events/[id]/guest-list — the wide shell, and the tree's only toast consumer</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/guest-list/page.tsx, src/app/(admin)/admin/events/[id]/guest-list/GuestListClient.tsx, src/components/guest-list/GuestListUnavailable.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/guest-list/page.tsx` — the route file; it imports the client and the unavailable state
    - `src/app/(admin)/admin/events/[id]/guest-list/GuestListClient.tsx` — 330 lines, 38 class attributes; **line 5 and line 73 are the `useToast` import and call**
    - `src/components/guest-list/GuestListUnavailable.tsx` — 44 lines, 4 class attributes
    - **The analog, role match:** `src/app/(admin)/admin/(work)/members/page.tsx` line 159 (`PageShell width="wide"`) and `src/app/(admin)/admin/formats/FormatsCatalogue.tsx` — a work client component holding its own state and dialogs one directory out
    - `src/components/toast/ToastContainer.tsx` — as left by plan 41.1-05, including the docblock recording the 32px overlap at 768px and the 112px centre shift at 1440px
    - `src/components/ui/Input.tsx` lines 216, 247, 279 — the field boundary token
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.1, §2.5, §3.5, §3.6
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 (the guest list is **on** the closed `wide` list), §8.11, §11
  </read_first>
  <action>
The guest list is **`wide`** — it is named on §4's closed list. Write the width
explicitly and state in the reported manifest reason that it is on the closed
list, so the width is checkable rather than chosen.

Apply the four deletions the analog's diff shows: the page root with its minimum
height and hard-coded bottom padding; the gutter and top rhythm on the header and
every repeated gutter on inner sections; the header's bottom padding becoming a
margin; the hand-written top-level heading becoming `PageTitle`.

`GuestListClient.tsx` carries 38 class attributes and 42 raw palette hits across
this closure — the highest palette density of any of the five cheap surfaces.
Walk the control ladder element by element: bordered fields become the `Input`,
`Textarea` and `Select` primitives; accent-fill controls become the primary button
variant; interactive pills become chips; bordered rounded containers become
`Card`. Every empty state takes the class contract at §8.11 — a heading plus one
body sentence naming the next step — and never an error tone.

**Do not change what the toast says or when it is raised.** The toast call sites
are behaviour; this plan converts the surface around them. If a toast's copy
collapses two distinct causes into one sentence, record it as a finding carried
forward rather than rewriting it — §11 says of existing messages *"none
introduced"*, and there is no error tracking here, so a message is one of the few
observables the product has.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs && node scripts/verify-semantic-separation.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the guest-list page file>` returns at least 1 with
      `width="wide"`
    - Across the three files: zero raw palette utilities (the seven colour
      families with a numeric step, in `bg-`, `text-`, `border-`, `divide-`),
      zero legacy token utilities (the four names), zero `focus:outline-none`,
      zero `font-medium`, zero `max-w-`, zero `sm:`
    - `grep -c "useToast" src/app/(admin)/admin/events/[id]/guest-list/GuestListClient.tsx`
      still returns the same count as before the conversion — the call sites are
      untouched
    - `node scripts/verify-touch-targets.mjs` exits 0; the exemption ledger is
      pasted in
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean, and the SUMMARY states that no entry
      path, attribution or count on the guest list changed
  </acceptance_criteria>
  <done>The guest list is on the wide shell with no page-owned maximum, gutter or palette, and its toast behaviour is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/events/[id]/assignments — the default shell and the small-prefix breakpoint</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx, src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx` — the route file
    - `src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx` — 326 lines, 19 class attributes; it is a named entry on `verify-breakpoints.mjs`'s `REMAINING` and this plan is its sole owner
    - **The analog, role match:** `src/app/(admin)/admin/formats/FormatsCatalogue.tsx`
    - `src/app/(admin)/admin/(work)/formats/page.tsx` line 147 — the default-width shell call
    - `src/components/ui/Checkbox.tsx` line 96 — the 44×44 hit area around a 16px box
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.1, §2.5
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §2.3 (the per-class map for every small-prefix use), §6.1, §6.2
  </read_first>
  <action>
The page is **`default`** — not on §4's closed `wide` list. Apply the four
deletions.

`AssignmentsClient.tsx` is on the breakpoints debt list, so its small-prefix uses
are the point of this task. **Read §2.3 per class, never per prefix.** A
three-column grid declared at the small prefix becomes two columns at `md` and
three at `lg`, gaining the middle step it skipped; a layout that merely switches
direction moves to `md`. Renaming the prefix without reading the class is
pitfall P6 and produces three columns in 544px on a tablet.

Every checkbox becomes the `Checkbox` primitive, which keeps the 16px drawn box
and gains the 44×44 hit area — this surface grants a capability for one night, so
a mis-hit here is an access decision made by accident.

**Nothing about who may assign what changes.** This is an `access-gating` surface:
the capability keys, the guards and the server-action payloads are untouched, and
the diff must show it.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-breakpoints.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the assignments page file>` returns at least 1 with
      `width="default"`
    - `grep -c "sm:" src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx`
      returns 0
    - For each small-prefix class removed, the SUMMARY records the class it became
      — a table of before and after, so the reviewer can check the map was read
      per class rather than per prefix
    - Across both files: zero raw palette utilities, zero legacy token utilities,
      zero `focus:outline-none`, zero `font-medium`, zero `max-w-`
    - `node scripts/verify-breakpoints.mjs` exits 0 and **still reports 19
      entries** — the entry is paid in the tree and reported; the deletion belongs
      to the reconciliation plan
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean and the SUMMARY states that no capability
      key, guard or action payload changed
  </acceptance_criteria>
  <done>The assignments surface is on the default shell, its small-prefix uses are mapped per class, and its checkboxes reach the floor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: H41-1 on the guest list — and the toast at 768px, which is the observation plan 41.1-05 owes</name>
  <action>
Stop here. This is a **blocking** checkpoint: nothing in this plan or in the next
wave proceeds until a person has performed the observation below and recorded its
rows. `41.1-VALIDATION.md` states the rule — **an unticked row is the correct
state until a person has looked**, and a row inferred from a neighbouring one is
not an observation. D-41.1-18 puts this per wave rather than accumulated to the
end, because a batch looked at once, at the end, is a batch that gets skipped.
Present the steps below verbatim, collect the answers, and write them into the
SUMMARY as rows — ticked, or `pending` with the reason it could not be performed.
  </action>
  <read_first>
    - `src/components/toast/ToastContainer.tsx` — the docblock plan 41.1-05 wrote: the 32px overlap at 768px, the 112px centre shift at 1440px, the three priced options, and the condition under which `D-41.1-03` reopens
    - `.planning/phases/41.1-work-surface-conversion/41.1-VALIDATION.md` — the six manual observations; an unticked row is the correct state until a person has looked
  </read_first>
  <what-built>
The guest list and the assignments surface are converted. The guest list is the
only page in the product that raises a toast, and the toast's inline offset was
changed in wave 2 with a measured residue.
  </what-built>
  <how-to-verify>
Start the dev server and open `/admin/events/{an event}/guest-list` as a role that
can see it.

**At 390px, 768px and 1440px, for both converted surfaces:**
1. The content clears the navigation without sitting under it, and without a
   doubled clearance.
2. Every control can be hit with a thumb — in particular the checkboxes on the
   assignments surface and the row controls on the guest list.
3. Nothing scrolls sideways.

**And then, at 768px exactly — this is the row that plan 41.1-05 owes:**
4. **Raise a toast.** Perform whichever guest-list action produces one, and
   watch where it appears. Record: does it overlap the 224px navigation column,
   and by how much does it look like it does? The measured prediction is a 32px
   overlap. Record what you actually see, not what was predicted.
5. Raise one at 1440px too, and record whether the off-centre placement reads as
   deliberate or as a defect.

**If the overlap is unacceptable**, say so in plain words. That answer reopens
`D-41.1-03` on evidence — the `:has()` mechanism was refused on a performance
argument made when the alternative was believed to be free, and it turned out not
to be free. Do not approve a residue you would not want to live with.

Record each observation as a row: surface, width, what was seen. Leave a row
unticked rather than inferring it.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded rows, or describe the toast's placement at 768px and whether it is acceptable.</resume-signal>
  <done>Every row below is recorded in the SUMMARY — ticked, or left `pending` with its reason. None is assumed, and none is inferred from another row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest list → the gating mechanism | a guest-list entry is a way into a night that does not pass approval |
| assignments → capability grant | this surface grants a capability for one night |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-25 | Elevation of privilege | the per-night capability assignment | mitigate | no capability key, guard or action payload changes; the diff is grepped for `CAP.` and `capabilities.has` and the result pasted in |
| T-41.1-26 | Repudiation | who added a guest-list entry | mitigate | conversion rule 8: attribution and counts are not dropped by a layout change; if one would be, the plan halts |
| T-41.1-27 | Tampering | a mis-hit checkbox granting a capability | mitigate | every checkbox becomes the primitive with the 44×44 hit area; confirmed by a person in task 3, because the gate estimates from a class string and cannot see a flex stretch |
| T-41.1-28 | Denial of service | the toast overlapping the navigation at 768px | mitigate | the residue is recorded in the file, and task 3 makes a person raise a toast at exactly that width; an unacceptable observation reopens `D-41.1-03` on evidence |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run verify` exits 0 with no refusal and no verdict line moved
- The before-and-after table of small-prefix classes is recorded
- Task 3's rows recorded — including the toast at 768px, ticked or left `pending`, never assumed
</verification>

<success_criteria>
Both surfaces converted whole, the assignments file's small-prefix debt paid per
class rather than per prefix, and a person has raised a toast at 768px and said
in writing whether the recorded residue is acceptable.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-08-SUMMARY.md` when done,
with the behaviour-change grep, the small-prefix before/after table, the two
`CONVERTED` entry texts, the `REMAINING` entries emptied by gate, and task 3's
observation rows including the toast answer.
</output>
