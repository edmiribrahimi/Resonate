---
phase: 41.1-work-surface-conversion
verified: 2026-08-14T00:17:48Z
status: human_needed
lab_sitting_4: >
  2026-08-19, `.planning/v1.5-LAB-SITTING-4.md` — La conferma della rivelazione OSSERVATA: il fuoco atterra su Cancel dentro il dialogo, visibile, e Invio subito non rivela.
score: 5/5 success criteria verified in their mechanical half — 0 failures, 0 gaps; every remaining claim is an observation no script in this repository can make
overrides_applied: 0
re_verification: null
gaps: []
deferred:
  - truth: "RESP-01 — every surface usable at phone, tablet and desktop"
    addressed_in: "Phase 41.2"
    evidence: "REQUIREMENTS.md:254 — 'says *every* surface, so it closes only in 41.2, and by a written human pass; no script can close it'. ROADMAP.md:861-863 repeats it as a phase-level note."
  - truth: "DS-07 — adoption of the shared pattern completes"
    addressed_in: "Phase 41.2"
    evidence: "REQUIREMENTS.md:251 — 'layer in 41, adoption completes in 41.2'"
  - truth: "RESP-02 / RESP-03 — inherited by every surface as it converts; the size measured on a device"
    addressed_in: "Phase 41.2"
    evidence: "REQUIREMENTS.md:255-256"
  - truth: "DEF-41-01 — Tailwind's source detection scans .planning/, compiling a dead rule out of prose into the shipped stylesheet"
    addressed_in: "its own plan, deferred with a written reason"
    evidence: "41.1-CONTEXT.md defers it; deferred-items.md DI-41.1-01 records one live instance and why chasing individual harvested strings closes none of it"
human_verification:
  - test: "H41-1 — every surface this phase converted, at 390px / 768px / 1280px"
    expected: "nothing clipped, nothing stretched, no horizontal scroll; the navigation column clears the content at 768px and up"
    why_human: "every gate here reads class strings and import clauses; none renders a pixel"
  - test: "H41-3 — the six card-rendering tables on a phone"
    expected: "the card carries the columns that mattered; no sideways scroll at 390px"
    why_human: "whether a seven-column table is comprehensible as a card is a judgement, not a class string"
  - test: "H41-4 — the smallest interactive control, on a LARGE touch screen, measured"
    expected: "at least 44px in both axes as rendered"
    why_human: "the gate estimates from a class string and cannot see a flex stretch, an icon height, an ancestor's padding, or a class built by concatenation — it says so in its own header"
  - test: "The venue-reveal confirmation still refuses the accidental path"
    expected: "opening the dialog puts focus on Cancel visibly; Enter performs nothing; the confirming control must be pressed deliberately"
    why_human: "monotone guard. The code path is verified below; that focus actually LANDS and is VISIBLE is an observation, and this is the one act in the product that cannot be undone"
  - test: "A refund control's confirmation is unchanged in observable behaviour"
    expected: "same two answers, same refusals, the reason field still optional, the amount unchanged"
    why_human: "no gate reads a status transition; the diff is verified clean below but the running behaviour is not"
  - test: "The toast clears the navigation column at 768px on a work surface"
    expected: "a raised toast does not sit under the column"
    why_human: "ToastContainer is mounted at the root as a sibling of the route tree, so no wrapper's declared value reaches it — verify-conversion records this as an UNMEASURED loss, not an approval"
---

# Phase 41.1: Work-Surface Conversion — Verification Report

**Phase Goal:** *"The 21 remaining work pages convert onto the layer Phase 41 built, and the gates that shipped as ratchets stop being ratchets for the work group."*
**Verified:** 2026-08-14T00:17:48Z
**Status:** `human_needed` — no gap, no failure, and nothing a script here can still add
**Re-verification:** No — initial verification

**Method.** Every number below was re-derived in this session by running the command or reading the file. Where a SUMMARY's claim was accepted rather than re-run, it is said so and why. Two files were mutated to test whether the gates still look; both were restored and `git status --porcelain` was confirmed byte-identical before and after each run — it carries the same two untracked planning directories at the start and at the end of this verification and nothing else.

---

## The finding an independent reader will want first — the shared stripper

Phase 41's verification is `gaps_found` at its **fifth** consecutive round, and its open truth is a lexer blind spot shared by `verify-conversion.mjs` and `verify-dialogs.mjs`: two ordinary JSX comment shapes blanked the rest of the file, so debt went invisible **while the gate printed a green**. Round 5 measured it live on a money-domain file. Plan 41.1-01 claims to have replaced this with one shared module.

**It is closed. Independently reproduced, not accepted.**

`scripts/lib/comments.mjs` was imported directly into a throwaway probe under the session scratchpad and handed the two shapes verbatim:

| Round-5 shape | Round-5 behaviour | Measured now | Evidence |
|---|---|---|---|
| A two-line JSX comment with live code after the closer on the terminating line (CR-01) | terminating line came back blanked whole; the code after `*/}` invisible | **live code returned** on both the closing line and after | `comments.mjs:270-285` — the resume branch strips the span and continues instead of pushing an empty line |
| `{/* x */ }` — whitespace before the closing brace (CR-02) | `insideJsxComment` never cleared; blanked to end of file | **closer recognised, file measured to the end** | `comments.mjs:147` — `JSX_COMMENT_CLOSE_RE` is a run-time-assembled regex `\*\/\s*\}`, not the exact three-character token |

**And the fix is on the gate path, not only in the module.** The decisive test: a real work-surface page was mutated with a violation placed *after* a CR-02-shaped comment — the exact configuration that was invisible at round 5 — and every relevant gate reddened on it.

```
src/app/(admin)/admin/(work)/artists/page.tsx:110 (injected, restored)
  {/* probe */ }
  <div className="2xl:hidden sm:block bg-red-500 text-card-border" />

verify-breakpoints  ✗ A  2xl: — 1 occurrence(s)          BREAKPOINTS_FAIL
                    ✗ B  1 file(s) carry sm: and are not on REMAINING
verify-conversion   ✗ A  bg-red-500       (reached from /admin/artists)
                    ✗ B  text-card-border (reached from /admin/artists)   CONVERSION_FAIL
```

All eleven gates that read source now import the one module — `verify-conversion.mjs:421`, `verify-dialogs.mjs:283`, `verify-breakpoints.mjs:214`, `verify-tokens.mjs:303`, `verify-tables.mjs:198`, `verify-touch-targets.mjs:308`, `verify-semantic-separation.mjs:188`, `verify-media-strip.mjs:104`, `verify-sunset-gradient.mjs:129`, `verify-no-viewport-read.mjs:171`, and the harness `verify-comment-stripper.mjs:110`. The per-gate `liveLines(relPath)` functions that remain are thin wrappers that delegate and own the refusal — e.g. `verify-breakpoints.mjs:613-619`. No private copy of the old heuristic survives.

`node scripts/verify-comment-stripper.mjs` → **exit 0**, and its matrix prints the four incumbent families beside the merged module, so the defect and its repair read on one page: family A false-reds on S1/S2/W3, families B/C/D are BLIND on S3/S4/S5. That is the argument for the extraction, stated in measurements rather than in "ten copies is a lot".

### Is there a shape N+1?

Round 5's standing warning was that *closing shape N reliably surfaces shape N+1*. Four further legal shapes were probed. **No blind shape was found.** Every one lands in a direction that cannot produce a false green:

| Shape | Behaviour now | Direction |
|---|---|---|
| `{ /* … */ }` — a space **after** the opening brace, multi-line | not recognised as a comment; the body comes back as live code | **FALSE-RED** — a needle inside a comment would be flagged. Nothing is hidden |
| a closer token inside a string on a comment body line | comment closed early; remainder returned live | **FALSE-RED** — declared at `comments.mjs:54-57` ("a line-shape state machine and not a tokeniser") |
| `*/` on one line and `}` on the next | reported `unterminated: jsx`; the gate **refuses** | **REFUSAL** — see below |
| two JSX comments then live code on one line | correct | — |

The refusal was confirmed on a real file rather than argued: the shape was injected into `(work)/artists/page.tsx` and `verify-conversion.mjs` exited **2** with `NOTHING WAS MEASURED`, not 0. That is the difference from round 5 that matters most — round 5's two shapes returned a green about nothing; every residual shape found today either over-reports or says out loud that it measured nothing.

**The one cost worth naming:** the false-red direction is not free. This repository's own `deferred-items.md` records the rule — *a gate that reddens on correct code is a gate somebody switches off*. Writing `{ /* … */ }` with a space after the brace in any `src/` file would produce a red on correct code. Zero such lines exist today.

---

## Goal Achievement

### Observable Truths — the five Success Criteria

| # | Success Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Every page under the work surface converted whole; the `(work)` layout shim **deleted**, not left behind | ✓ VERIFIED | below |
| 2 | The five analytics tables and the finance transaction list read as cards on a phone; G3's `REMAINING` empty for the work group | ✓ VERIFIED (mechanical half) · H41-3 owed | below |
| 3 | No file under the work surface reads a legacy token name or carries a raw palette colour | ✓ VERIFIED | below |
| 4 | G6's `REMAINING` empty for the work group — no `sm:`, `xl:`, `2xl:` there | ✓ VERIFIED | below |
| 5 | The three knots converted with their domain gates **held**, each its own plan, each with a written impact analysis before its diff | ✓ VERIFIED | below, in its own section |

**Score: 5/5.** No failure, no gap. The score is a claim about what commands and code can settle; §*What a green does NOT mean* states its boundary.

---

### Criterion 1 — the pages, and the shim

**The pages.** 24 `page.tsx` files live under `src/app/(admin)/admin/(work)/`. **23 render `PageShell`**, enumerated one by one this session. The 24th is `src/app/(admin)/admin/(work)/page.tsx`, which renders nothing: it resolves the access context, refuses a visitor without `organizer.access` (`:35-37`), and ends in `redirect("/admin/events")` (`:39`). A page that renders nothing cannot carry a shell, and the manifest documents the exemption in writing rather than discovering it on a red run.

**The shim.** `src/app/(admin)/admin/(work)/layout.tsx` still exists — it must, it is the layout Phase 34 built and the file that **declares** the column clearance. The shim was never the layout: it was the *pairing* the layout wrapped `{children}` in — an outer element that **applied** 224px of leading padding, plus an inner element that re-declared the value as `0px` so a converted page did not add a second 224px on top.

Both are gone, in commit `b4bc6a8`:

```
- <div className="ps-[var(--nav-inset-inline-start)] md:[--nav-inset-inline-start:14rem]">
+ <div className="md:[--nav-inset-inline-start:14rem]">
-   <div style={{ "--nav-inset-inline-start": "0px" } as CSSProperties}>
-     {children}
-   </div>
+   {children}
```

Current state: `layout.tsx:176` — one element, declaration only. The ordering rule the file wrote for itself **before** taking it (`layout.tsx:94-105`) was honoured: `git show b4bc6a8 --stat` shows the shim removal and the last five surface declarations in **one commit**, which is what the rule required in both directions.

And the layout may not stop *declaring*: `verify-conversion.mjs` check E asserts that the files declaring the clearance at the `md` tier are **exactly** the files mounting the responsive navigation form, and it fails in both directions. Green this session, naming the two files (`(work)/layout.tsx`, `(public)/gallery/page.tsx`).

---

### Criterion 2 — the tables

`node scripts/verify-tables.mjs` → **exit 0**:

```
REMAINING = 0      work 0 · public-member-money 0 · phase-42 0 · exempt 0
WORK GROUP REMAINING = 0
the table branch     expected 1, measured 1
the card branch      expected 1, measured 1
✓ B  the primitive switches at md and at no other breakpoint, in itself and in
     its 6 importer(s), and no table part is transformed
```

The six importers, printed by the gate: `MemberTable.tsx`, `TransactionList.tsx` (the finance list), `DrinkSalesBreakdown.tsx`, `MemberSpendTable.tsx`, `ReferralChainTable.tsx`, `SalesDashboard.tsx`.

**Two trees, not one filtered by width** — read directly rather than taken from the gate: `src/components/ui/DataTable.tsx:284-285` is `hidden … md:block` wrapping a real `<table>`; `:391-396` is `space-y-3 md:hidden` wrapping real `Card`s; `:486` is the empty state. Each branch is removed at the other tier by `display`.

**What is still owed:** whether the card carries the columns that mattered. That is H41-3, a person holding a phone, and the gate says so itself in the sentence it prints under the star.

---

### Criterion 3 — no legacy token, no raw palette

`verify-conversion.mjs` checks A and B, green over the 157 files reached by the 28 declared surfaces:

```
check A — raw palette utilities:  palette families matched : 24
  ✓ A  no raw palette utility in 157 file(s) under 28 converted surface(s)
check B — legacy token utilities: legacy names matched : 4
  ✓ B  no legacy token utility in 157 file(s) across 28 converted surface(s)
```

**Re-derived independently, and wider than the gate's own scope.** The gate scans an import closure; a file under the work surface that nothing imports would be missed. So all **65** files under `src/app/(admin)/admin/` were walked in this session using the same shared stripper, matched against the same 22 palette families and the same four legacy names (`card-border`, `card`, `background`, `foreground`):

- **Zero** palette utilities and **zero** legacy token utilities anywhere under `(work)/`, or in the co-located non-route components the work surfaces import.
- Every hit — 56 palette, 42 legacy — is in exactly one file: `src/app/(admin)/admin/scanner/ScannerClient.tsx`. **That is the door, and the door is deliberately outside `(work)`** (`nextjs-architecture.md`: *"La porta sta fuori, di proposito"*). It is not a work surface and criterion 3 does not reach it. It remains unconverted, which is a fact this report states rather than a debt this phase owed.

**Two things my own scan flagged that are not violations:**

- `(work)/artists/page.tsx:133`, `(work)/venues/[slug]/page.tsx:228,265,350`, `(work)/venues/page.tsx:147` — these matched a six-hex-digit pattern but are **HTML numeric character references** (`&#127925;` etc.), not colours. A false positive of my regex, recorded so the next reader does not re-derive it.
- `src/app/(admin)/admin/formats/ColorSwatchPicker.tsx:118-123` — six literal hexes, and they are the brand palette. This is a **declared exemption** in the manifest (*"a brand hex that is data on a row, not a token"*) and it is correct: those values are the six colours `brand-visual-system.md` fixes.

**One declared blindness, with a live instance.** `src/app/(admin)/admin/newsletter/ComposeForm.tsx:88-97` carries raw hexes inside an **email HTML template string** (`background-color:#0a0a0a`, `border:1px solid #262626`, …). Check A matches palette *utilities*, not literals — it says so in its own footer: *"it is blind to an inline hex"*. The instance is legitimate on its merits (an email body cannot read a CSS custom property, and inline style is the only thing mail clients honour), but criterion 3's words are *"carries a raw palette colour"* and no gate in this repository would have caught it if it had not been. Recorded as a coverage boundary, not a gap.

---

### Criterion 4 — the breakpoints

`node scripts/verify-breakpoints.mjs` → **exit 0**:

```
check A: ·  0  xl:      ·  0  2xl:
  ✓ A  xl: and 2xl: have zero occurrences in 264 file(s) under src/
check B by group (measured):
    work                   0 file(s)    0 use(s)
    public-member-money    5 file(s)   16 use(s)
    phase-42               0 file(s)    0 use(s)
    exempt                 0 file(s)    0 use(s)
  WORK GROUP REMAINING = 0      (0 sm: use(s))
```

The five files still carrying `sm:` were checked by name — all are under `src/app/(public)/events/…`. The group tag is not being used to move work-surface debt into another bucket.

**A reading nuance, measured on the mutation.** `WORK GROUP REMAINING` counts *declared* `REMAINING` entries by group. When a `sm:` was injected into a work page that carries no declaration, the star line still printed `WORK GROUP REMAINING = 0` — while **check B failed loudly** and named the file and the line. So the criterion's sentence is satisfied and the debt did not go quiet, but the headline number alone is not the safety: **check B's verdict is.** The gate's own printed warning says the same thing, and it is worth repeating here because criterion 4 is phrased as a number.

---

## The three knots

Criterion 5 is the one where a green would be worth the least, so it was checked against diffs rather than prose.

### The single strongest finding: the enforcement surface is byte-identical

The complete set of `src/` and `supabase/` files changed by **the whole of phase 41.1** was enumerated (`git diff --name-only` from the phase's first execution commit to HEAD): **80 files, every one of them presentation.**

Filtering that set against the paths where the guards actually live returns **nothing**:

```
src/app/(admin)/admin/events/[id]/reveal/actions.ts   unchanged
src/lib/venue-reveal/                                 unchanged
src/app/api/            (webhooks, cron, tickets)     unchanged
src/lib/sumup.ts · src/lib/rbac/ · src/lib/capabilities/
src/lib/supabase/ · src/middleware.ts                 unchanged
supabase/                                             unchanged

every *actions.ts, every route.ts, every .sql touched by 41.1:  NONE
```

`venue_reveal_sent` is enforced on the server. This phase did not open the server. That is not the whole of the monotone answer, but it is the load-bearing half of it, and it is a fact rather than an assurance.

### 41.1-20 — the venue reveal · **the monotone guard held**

Two files, both client: `VenueRevealPanel.tsx` and `RevealVenueDialog.tsx`.

**Did anything make a reveal reachable with fewer clicks, a weaker predicate, a default-open control, or a removed confirmation? No.** Each asked separately:

- **Weaker predicate** — the panel's diff was filtered to every changed line that is not a comment. What changed: three imports, `<section>`→`Card`, `<button>`→`Button`, and colour tokens (`text-red-400`→`text-sem-crit`, `border-card-border`→`border-line`). **Not one conditional line changed.** The condition deciding whether the manual reveal is offered at all is byte-identical.
- **Fewer clicks** — the confirmation still stands between the control and the send. `RevealVenueDialog.tsx:429-437`.
- **Default-open** — no. The dialog opens only from the panel's control.
- **Removed confirmation** — no. The eleven refusals are still rendered from a *total* `Record`, so a new refusal added upstream turns the file red at build time rather than rendering a blank at two in the morning.
- **Enter-to-confirm** — checked in the primitive, not in the prose. `src/components/ui/Dialog.tsx` contains **no `<form>`, no `method="dialog"`, no `onSubmit`, no key handler**, and `src/components/ui/Button.tsx:236` renders `<button type="button">` with the type written **before** the prop spread is not the point — no call site passes a type. Enter cannot perform the act.
- **Focus** — `RevealVenueDialog.tsx:432`: `data-initial-focus` appears **exactly once in the file**, on `Cancel`, which is first in the DOM. `Dialog.tsx:251-258` focuses that element **after** `showModal()`, with the fallback being the close marker — never the confirming control. The conversion replaced two mechanisms (one of them inert) with one declared marker, and made the placement visible for the first time.

**The one direction that changed is the safe one.** The confirming control moved to the destructive rung. A heavier confirming control makes the act harder to press by accident, never easier. The file quotes its own previous reasoning rather than deleting it.

**Impact analysis, written before the diff:** `41.1-20-PLAN.md:69` — *"## Written before the diff"* — classifies the work **Critical**, names `venue-secrecy` primary with `comms-analytics` and `ticketing-payments` secondary, states what is opened and what is explicitly **not** (the cron route, the reveal library, the email template, every server action), and carries the three stop conditions verbatim at `:102`. The plan file is dated 2026-08-13 15:51; the first diff commit is 2026-08-14 01:16. The order is real, not asserted.

**Still owed:** that the focus visibly lands on Cancel, tried by hand. Listed in `human_verification`.

### 41.1-17 — the refunds · **money untouched**

`RefundActions.tsx` and `RefundDialog.tsx`, both client. The full non-comment diff is: imports; `useId`; a `closeConfirm()` helper; a hand-rolled `fixed inset-0 z-[60]` overlay replaced by the `Dialog` primitive; hand-rolled buttons replaced by `Button`; a raw `<textarea>` replaced by `Textarea`; `text-green-400 Done` replaced by `<Badge>Done</Badge>`.

- **No status transition, no amount, no idempotency key, no webhook path appears in the diff.** The server action file was not opened at all — see the enumeration above.
- The confirmation survives, with `data-initial-focus` on Cancel (`RefundActions.tsx:132`, `RefundDialog.tsx:161`) and the confirming control on the destructive rung.
- One genuine improvement rather than a neutral move: the reason field previously had **no programmatic name** — a screen reader announced an editable region and nothing else. It now carries `aria-label="Reason (optional)"`, the placeholder's own words, so nothing a person reads changed.
- The refusal moved out of the scrolling body into the primitive's `status` region (`Dialog.tsx:316-325`), so it can no longer appear below the fold. In a project with **no error tracking**, that is the difference between an error a person sees and one nobody does.

**Impact analysis:** `41.1-17-PLAN.md:66`, same shape, same three stop conditions at `:104`.

### 41.1-18 / 41.1-19 — the event form · **the secrecy control's semantics preserved**

`src/components/events/EventForm.tsx` and five satellites. The form carries the `venue_secret` control, so it is a venue-secrecy file whatever else it is.

Checked line by line rather than by summary:

- **Toggle semantics not inverted.** The three toggle tracks moved from hand-rolled `role="switch"` markup to the `Switch` primitive. The diff is `aria-checked={value}` → `checked={value}`, `aria-checked={subEvent.venue_reveal_on_purchase}` → `checked={…}`, `aria-checked={mainVenueRevealOnPurchase}` → `checked={…}`, and the `!x` toggle handlers are unchanged. `Switch.tsx:177-178` is a real `<input type="checkbox" role="switch">`, so the checked state is the platform's.
- **No default flipped.** `venue_secret: false` (`EventForm.tsx:128`, `:246`) and `venue_reveal_on_purchase: true` (`:131`) are both pre-existing; the diff adds a *comment* documenting the first and changes neither.
- **The reveal-hours floor survives.** `min={DEFAULT_VENUE_REVEAL_HOURS}` is present at **both** sites — `EventForm.tsx:1136` and `:1499`. The only change to those lines is indentation. Removing that floor would let the address mail leave after the party had started, because the reveal cron runs once a day; the field's help text says so at `:84-86`.
- **Field order preserved.** venue → the secrecy control → the fields the secrecy control gates. The form's own docblock names this as a `venue-secrecy.md` invariant rather than a layout preference, and the diff does not move it.

**Impact analyses:** `41.1-18-PLAN.md:107`, `41.1-19-PLAN.md:84`.

---

## Requirements Coverage

**None of the five closes in this phase, and REQUIREMENTS.md already says so.** A report that closed any of them here would contradict the requirement register.

| Requirement | Status | Reason |
|---|---|---|
| **DS-07** — a recurring pattern is a shared component, adopted as a page converts | **PARTIAL** | `verify-conversion.mjs` checks A, B, C green over 28 surfaces / 157 files; 16 of 16 published exports have an importer, no orphan. But `REQUIREMENTS.md:251` routes it *"Phase 41 → 41.1 → 41.2 — adoption completes in 41.2"*. Advanced, not closed |
| **DS-09** — a dense table becomes cards on a phone | **PARTIAL** | Mechanical half closed: `WORK GROUP REMAINING = 0`, two real trees at `DataTable.tsx:284,391`, six importers. Judgement half is **H41-3**, unticked. `REQUIREMENTS.md:253` ends this requirement's trajectory at 41.1, so H41-3 is the only thing between it and closure |
| **RESP-01** — every surface usable at three widths | **OPEN — and must not be closed here** | Says *every* surface. `REQUIREMENTS.md:254` and `ROADMAP.md:861-863` both bind it to 41.2 and to a written human pass. Advanced by this phase; not closed by it |
| **RESP-02** — content stops widening on large screens | **PARTIAL** | Check D green: the shell declares §4's three maxima and only those, and all 28 converted surfaces declare the width their mounted navigation calls for, with no page writing its own. `REQUIREMENTS.md:255` routes the close to 41.2 |
| **RESP-03** — touch targets stay large where the input is a finger | **PARTIAL** | `verify-touch-targets.mjs` exit 0. But the gate reads class strings and cannot see a rendered box, and it now forgives five call sites on a declared reason rather than a measurement. **H41-4 remains the only thing in this repository that proves anything is 44px**, and it is unticked. `REQUIREMENTS.md:256` routes the close to 41.2 |

**No orphaned requirements.** No requirement ID is mapped to Phase 41.1 in `REQUIREMENTS.md` that no plan claimed.

---

## The deferred red, and how it was closed

`deferred-items.md` records **DEF-41.1-24-01**: at the phase's own final reconciliation, `npm run verify` did **not** exit 0 — `verify:touch-targets` failed on nine correct elements in five files, and the item argued at length that fixing it at phase close under a single-writer discipline was the move that discipline exists to prevent.

It has since been closed by plan **41.1-25**, an unplanned closure repair with a SUMMARY and no PLAN. Because this is precisely the shape of a gate being taught to stop looking, it was re-derived rather than accepted.

**It is a contract edit, not a widening.** `82d25a9` changes exactly two files: `41-UI-SPEC.md` (§13, entries 7 and 8, dated and attributed to an owner authorisation) and `scripts/verify-touch-targets.mjs`. **No product element was edited to satisfy the parser, and no threshold moved** — 44px is still 44px, confirmed by reading the constants at `src/components/ui/AutocompleteInput.tsx:89,96`, which still carry the minimum.

**Exemption 8's premise check was proved by mutation.** The exemption forgives five call sites because their minimum lives in a shared module constant. The obvious failure mode is somebody shrinking that constant and all five being forgiven in silence. So the constant's floor was removed in the tree and the gate re-run:

```
sed -i '' '89s/min-h-11 //' src/components/ui/AutocompleteInput.tsx
node scripts/verify-touch-targets.mjs   →  exit 1
  src/components/ui/AutocompleteInput.tsx:162  <input>   CONTROL — DOES NOT DECLARE IT
```

**Exit 1, a failure — not a refusal, and not a silent pass**, which is what the exemption's docblock promised. File restored; `git status --porcelain` identical; gate back to exit 0.

**Current state, re-run this session:** `npm run verify` → **VERIFY_OK, 16 gates passed, 0 failed, 0 refused**, 1 not run (`verify:redirects`, which needs a dev server and says so in its own header). `verify:capabilities` passes here because this checkout has credentials; it refused in the worktree, which was its honest state there. `npm run build` → **exit 0** (this is the typecheck; there is no test runner).

---

## Anti-Patterns Found

All 80 files this phase touched were scanned for `TODO`, `FIXME`, `XXX`, `TBD`, `HACK`, `PLACEHOLDER`, `not implemented`, `coming soon`, `mock`, `stub` — **through the shared stripper, so a marker inside a comment could not hide and a marker in live code could not be mistaken for prose.**

| File | Line | Match | Severity | Finding |
|---|---|---|---|---|
| `src/components/events/AutocompleteTagInput.tsx` | 75, 118 | `placeholder` | ℹ️ Info | the HTML prop name and its default. Not a stub |
| `src/components/ui/AutocompleteInput.tsx` | 73, 104 | `placeholder` | ℹ️ Info | same |
| `src/components/tickets/TierCard.tsx` | 226 | `Coming soon` | ℹ️ Info | a real conditional badge: `tier.starts_at && new Date(tier.starts_at) > new Date()`. A designed state for a future-dated tier, not an unfinished screen |

**Zero `TODO`, zero `FIXME`, zero `XXX`, zero `TBD`, zero `HACK`, zero `mock`, zero `stub` in live code.** No debt marker gate is tripped, so no blocker arises from this scan.

---

## What a green does NOT mean

The honest boundary of this verification.

1. **Nothing here was rendered.** Every gate reads class strings, import clauses and declarations. No pixel was measured, no browser was opened, no surface was seen. A work page can pass all sixteen gates and be unusable at 390px.
2. **There is no test runner for this product.** `package.json` declares no `test` script and no `*.test.*` or `*.spec.*` exists. Nothing below was verified "because tests pass", because there are none. `npm run build` is the typecheck and nothing more.
3. **No product code was executed.** No route was requested, no server action was called, no refund was attempted, no reveal was triggered. The refund and reveal findings are findings about **diffs and code paths**, not about running behaviour.
4. **No production data was read or written.** Per the standing constraint and the recorded D12 incident, this verification performed no Supabase write, no migration and no seeding, and did not attempt any.
5. **The import-closure scope is not the whole tree.** `verify-conversion.mjs` checks A and B walk what a declared surface reaches. The independent 65-file walk in criterion 3 was run precisely because that scope could hide an unimported file — and it found none.
6. **The stripper is not a tokeniser.** It has no notion of strings, template literals or regex literals, and it says so at `comments.mjs:54-57`. Its residual error direction is *more blanking, never less* — which can under-count, but cannot hand a comment back as code.
7. **`WORK GROUP REMAINING = 0` is a statement about the declared list, not about the tree.** The mutation above showed the star line reading zero while check B failed. Read the verdict, not the number.
8. **Four SUMMARY claims were accepted rather than re-run**, for budget: the per-wave H41-1 observations, the twenty focus reconciliations of plan 41.1-22, the entry-level wave reconciliations of plans 41.1-11/16/24, and the byte-hash family measurements quoted in `comments.mjs`'s docblock. Everything load-bearing for the five criteria and the three knots was re-derived.

---

## Human Verification Required

Six rows, consolidated and deduplicated against `41.1-VALIDATION.md`'s manual-only table. They belong to the existing end-of-v1.5 batch and are **not failures and not passes** — they are unticked, which is the correct state until a person has looked.

### 1. H41-1 — every surface this phase converted, at three widths
**Test:** open each surface at 390px, 768px and 1280px.
**Expected:** nothing clipped, nothing stretched, no horizontal scroll; from 768px up the navigation column clears the content.
**Role:** an account holding `organizer.access`; the analytics and finance surfaces additionally need the capabilities that gate them.
**Why human:** no gate renders anything. This is RESP-01's evidence and RESP-01 closes in 41.2.

### 2. H41-3 — the six card-rendering tables on a phone
**Test:** at 390px, open member spend, referral chain, drink sales, the sales dashboard, the member table and the finance transaction list.
**Expected:** each card carries the columns that mattered; no sideways scroll.
**Role:** as above.
**Why human:** whether a seven-column table is comprehensible as a card is a judgement. **This is the only thing between DS-09 and closure.**

### 3. H41-4 — the smallest control, on a LARGE touch screen, measured
**Test:** on a large touch screen (not a phone), measure the smallest interactive control on a converted work surface.
**Expected:** at least 44px in both axes as rendered.
**Why human:** the gate estimates from a class string and cannot see a flex stretch, an icon height, an ancestor's padding, or a class built by concatenation. Five call sites are now forgiven on a declared reason rather than measured. **This is the only thing in the repository that proves anything is 44px.**

### 4. The venue-reveal confirmation refuses the accidental path — **Critical**
**Test:** on the event edit surface, open the reveal confirmation. Observe where focus lands and whether it is visible. Press Enter. Then cancel.
**Expected:** focus is on **Cancel**, visibly. **Enter performs nothing.** The act requires a deliberate press of the confirming control.
**Role:** an account entitled to the reveal.
**Why human:** the code path is verified above — one `data-initial-focus`, on Cancel, focused after `showModal()`, no form and no key handler anywhere in the primitive. That focus **lands** and is **seen** is an observation. This is the one act in the product that cannot be undone.

### 5. A refund control's confirmation is unchanged in observable behaviour
**Test:** open a refund confirmation on the tickets surface and on the finance surface. Cancel both.
**Expected:** the same two answers, the same refusals, the reason field still optional, the amount unchanged. **Do not complete a refund to test this.**
**Why human:** no gate reads a status transition. The diff greps clean of status, amount, idempotency key and webhook path, and no server action was opened — but the running behaviour was not exercised.

### 6. The toast clears the navigation column at 768px on a work surface
**Test:** on the guest-list surface at 768px, raise a toast.
**Expected:** the toast does not sit under the navigation column.
**Why human:** `ToastContainer` is mounted at the root as a sibling of the route tree, below neither file that declares the clearance, so no wrapper's declared value can reach it. `verify-conversion.mjs` records this explicitly as an **UNMEASURED loss, not an approval** — a textual pairing cannot see it.

---

## Gaps Summary

**None.** All five Success Criteria are met in the half a command or a file can settle, and the mechanism that made phase 41's greens untrustworthy for five consecutive rounds has been independently confirmed closed — on the module, on the gate path, and on a real work-surface file with the violation placed behind the exact comment shape that used to hide it.

Two things deserve to be carried forward without being called gaps:

1. **The false-red direction is a live cost.** `{ /* … */ }` with a space after the opening brace is legal JSX that the stripper does not recognise, so a needle written inside such a comment would produce a red on correct code. Zero instances today. This repository's own record says a gate that reddens on correct code is a gate somebody switches off — which is how the *previous* defect became invisible.
2. **The door is still unconverted**, and it is the only file under `src/app/(admin)/admin/` carrying legacy tokens and raw palette steps. That is correct — the door sits outside `(work)` by design and is not a work surface — but it is 98 hits in one file, and the next reader should know where they live before concluding the admin tree is clean.

Status is `human_needed` rather than `passed` for one reason: **six observations are owed and none of them has been made.** A phase whose remaining evidence is entirely a person's is not a phase that has passed; it is a phase waiting on a person. Recording it as passed would let a deferral read as a closure, which is the failure this repository has already paid for once.

---

*Verified: 2026-08-14T00:17:48Z*
*Verifier: Claude (gsd-verifier)*
*Repository state before and after this verification: `git status --porcelain` identical. Two files were mutated to test whether the gates still look — `src/app/(admin)/admin/(work)/artists/page.tsx` and `src/components/ui/AutocompleteInput.tsx` — each restored and confirmed byte-identical to a copy taken before the mutation. All probe files were written under the session scratchpad, never inside the repository.*
