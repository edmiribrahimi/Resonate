---
phase: 41.1-work-surface-conversion
plan: 07
type: execute
wave: 3
depends_on: ["41.1-05"]
files_modified:
  - src/app/(admin)/admin/(work)/venues/[slug]/page.tsx
  - src/components/venues/EditVenueButton.tsx
  - src/app/(admin)/admin/(work)/events/page.tsx
  - src/components/events/EventList.tsx
  - src/app/(admin)/admin/(work)/events/loading.tsx
autonomous: true
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The venue detail surface and the events listing render inside the page shell at the default maximum"
    - "The venue edit modal is the Dialog primitive, with escape, focus trap and the top layer coming from the platform"
    - "The events loading placeholder renders the same maximum as the page it precedes"
    - "No control in the events listing row is smaller than the touch floor"
  artifacts:
    - path: "src/components/venues/EditVenueButton.tsx"
      provides: "the venue edit form, on the Dialog primitive"
      contains: "Dialog"
    - path: "src/app/(admin)/admin/(work)/events/loading.tsx"
      provides: "the events placeholder, on Skeleton and the shell"
      contains: "PageShell"
  key_links:
    - from: "src/components/venues/EditVenueButton.tsx"
      to: "src/components/ui/Dialog.tsx"
      via: "the extracted dialog shell"
      pattern: "from \"@/components/ui/Dialog\""
    - from: "src/app/(admin)/admin/(work)/events/loading.tsx"
      to: "src/components/ui/Skeleton.tsx"
      via: "the placeholder primitive"
      pattern: "from \"@/components/ui/Skeleton\""
---

<objective>
Convert the venue detail surface with its edit modal, and the events listing with
its row controls and its loading placeholder.

Purpose: `EditVenueButton.tsx` is the **one** dialogs `REMAINING` entry that is
unambiguously the work group — its only importer is the venue detail page — so
this plan is the first entry that phase 41.1 actually pays on that gate. And
`EventList.tsx:191-226` is `41-UI-SPEC.md` §6.4's six-in-a-row 28px case: six
controls in one row, none of them reaching the touch floor.

Output: two surfaces converted whole, one dialog entry paid, one placeholder
adopted, and the entries reported for the wave's reconciliation plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). Report the
   `CONVERTED` entry texts and the `REMAINING` entries emptied; the wave's
   reconciliation plan makes every gate edit.
2. **No behaviour change:** *"no query changed, no column added, no capability
   check touched, no action payload altered"*. Grep the diff for `select(`,
   `CAP.`, `.eq(`, `.filter(`, `capabilities.has`, `redirect(` and paste the
   result.
3. **Never spell a Tailwind class string inside a comment** (DEF-41-01 open).
4. **`sm:` is a map, not a rename** — read `41-UI-SPEC.md` §2.3 per class.
5. **`FOCUS_RING` is imported from `src/components/ui/Button.tsx:71`**, never
   re-spelled; `focus-visible:`, never `focus:`.
6. **A `Badge` that is a link or a button is a `Chip`** (§8.5).
7. **Stop condition 3 applies:** a series progressivo is already on a poster — no
   conversion renumbers, re-sorts or re-derives one. The events listing renders
   series and format identifiers; **it may reorder nothing and derive no number.**
   Neither page touches money or the venue reveal.
8. **Venue secrecy:** `/admin/venues/[slug]` renders a venue's address to staff.
   The conversion changes no visibility predicate and no query. If a class change
   would surface an address earlier — for example by removing a conditional
   wrapper — **halt**: that is `venue-secrecy.md`, not a styling decision.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: /admin/venues/[slug] and the venue edit modal onto the Dialog primitive</name>
  <files>src/app/(admin)/admin/(work)/venues/[slug]/page.tsx, src/components/venues/EditVenueButton.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/venues/[slug]/page.tsx` — 328 lines, 22 class attributes; its only local import is the edit button
    - `src/components/venues/EditVenueButton.tsx` — 278 lines, 27 class attributes; lines 155-162 (the hand-rolled overlay, its panel and its own heading) and lines 165-171 (the hand-rolled success and error boxes, four raw palette hits)
    - **The analog, exact match:** `src/app/(admin)/admin/formats/CreateFormatModal.tsx` lines 365-399 — the whole call shape; line 126 and lines 373-380 for the form-owner attribute
    - `src/components/ui/Dialog.tsx` — line 89 (actions below the scroller), lines 316-322 (the `status` region and its two roles and two tokens), lines 246-256 (the imperative initial focus), lines 124-132 (why the React autofocus prop never reaches the DOM)
    - `src/app/(admin)/admin/formats/FormatsCatalogue.tsx` lines 193-198 and 385-386 — the parent holds the open state, never the dialog
    - `src/app/(admin)/admin/(work)/formats/page.tsx` line 147 — the shell call at the default maximum
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.1, §2.3a, §2.5
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §8.3 (the dialog and its closed size list), §8.6, §11
  </read_first>
  <action>
The page is **`default`** width — not on §4's closed `wide` list. Apply the four
deletions from the analog's diff: the page root with its minimum height and
hard-coded bottom padding; the gutter and top rhythm on the header and every
repeated gutter on inner sections; the header's bottom padding, which becomes a
margin; the hand-written top-level heading, which becomes `PageTitle`.

`EditVenueButton.tsx` adopts `Dialog`. Three things go outright: the fixed
full-screen overlay with its own stacking index and blur; the panel with its own
width cap, scroll cap, radius, border, ground and padding; and the modal's own
heading element — the primitive owns the shell and the title prop **is** the
heading.

Two things from the analog are not obvious and must be copied:

- **The buttons live outside the form**, in the actions region, and reach it
  through HTML's form-owner attribute. The primitive puts the actions region
  below the scroller, so a submit control inside the form would scroll away.
- **The refusal is the `status` prop, not a hand-rolled box.** The primitive
  renders an alert role for the critical tone and a status role otherwise, in the
  two semantic tokens. That prop deletes the four raw palette hits at
  `EditVenueButton.tsx:165-171` outright — a red-family border, ground and text
  for the failure, a green-family set for the success.

The open state lives in the parent, never in the dialog; the dialog's own close
resets its local refusal and calls the close callback. Size comes from §8.3's
closed list, chosen by field count.

**The error copy keeps its causes distinct.** `RetireFormatDialog.tsx:167-200` is
the pattern: one sentence per cause, each opening the same way, and a default arm
where an unforeseen cause **identifies itself on screen** rather than borrowing a
sentence written for something else. There is no error tracking in this project,
so a refusal a person cannot read is a refusal nobody ever reads.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-dialogs.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" "src/app/(admin)/admin/(work)/venues/[slug]/page.tsx"`
      returns at least 1 with `width="default"`
    - `grep -c "from \"@/components/ui/Dialog\"" src/components/venues/EditVenueButton.tsx`
      returns 1
    - `grep -cE "fixed inset-0|z-\[60\]|backdrop-blur" src/components/venues/EditVenueButton.tsx`
      returns 0 — the hand-rolled overlay is gone, not styled
    - `grep -cE "(bg|text|border)-(red|green)-[0-9]" src/components/venues/EditVenueButton.tsx`
      returns 0
    - Across both files: zero raw palette utilities, zero legacy token utilities,
      zero `focus:outline-none`, zero `font-medium`, zero `max-w-`, zero `sm:`
    - `node scripts/verify-dialogs.mjs` exits 0 and **still reports
      `REMAINING = 14`** — this plan pays the entry in the tree and reports it;
      the deletion belongs to the reconciliation plan
    - `node scripts/verify-touch-targets.mjs` exits 0
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean and pasted in, and the SUMMARY states
      that no visibility predicate on the venue address changed
  </acceptance_criteria>
  <done>The venue surface is on the shell and its modal is the primitive, with the four raw palette hits deleted by the status prop rather than recoloured.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/events, its listing rows, and its loading placeholder</name>
  <files>src/app/(admin)/admin/(work)/events/page.tsx, src/components/events/EventList.tsx, src/app/(admin)/admin/(work)/events/loading.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/page.tsx` — 167 lines, 11 class attributes
    - `src/components/events/EventList.tsx` — 269 lines, 23 class attributes; **lines 191-226 are §6.4's six-in-a-row 28px case**
    - `src/app/(admin)/admin/(work)/events/loading.tsx` — 38 lines, 8 legacy token hits, 3 hand-rolled pulse blocks
    - **The loading analog, exact match:** `src/app/(admin)/admin/(work)/members/loading.tsx` — the whole call shape, and note it renders the **same width** as the page it precedes
    - `src/components/ui/Skeleton.tsx` — lines 19-27 (the category error: a line colour used as a ground), lines 29-34 (the width-must-match rule), lines 20-28 (every count is a literal and stands for nothing), lines 36-42 (the fill and why it is deliberately near-invisible), lines 44-53 (DEF-41-05: do not append a radius)
    - `src/components/admin/MemberTable.tsx` lines 1109-1118 — the composed control shape including the imported focus expression
    - `src/components/ui/Chip.tsx` line 119 (interactive, 44px, current marking) and line 227 (non-interactive, no minimum height)
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.1, §2.4, §2.5
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §6.1, §6.3 (the shrink allow-list, closed at one item), §6.4
  </read_first>
  <action>
The page is **`default`** width. Apply the four deletions.

`EventList.tsx` is the row renderer and its six-control row is the reason this
file is named in the spec. Every one of the six is a link or a button, so **every
one of them is a `Chip`, not a `Badge`** — that single sentence is the difference
between a correct file and six 20px targets in a row. The shrink allow-list is
closed at one item and this row is not on it, so none of the six shrinks below the
floor. Where the six no longer fit on one line at phone width, they wrap; do not
reintroduce a small-prefix breakpoint to hide the problem.

`events/loading.tsx` adopts `PageShell` and `Skeleton` and takes three constraints
from its analog that a plan must not lose:

1. **The width must equal the page's width** — `default` here, matching the page
   converted in this same task. A placeholder at a different maximum makes the
   content jump sideways the moment the data lands, which is the defect a skeleton
   exists to prevent.
2. **Every count is a literal and stands for nothing.** No query runs before a
   loading file renders, so a placeholder that looked like it knew a count would
   be saying something it cannot know.
3. **Do not append a radius to a skeleton line.** The container radius is written
   after the pill radius in the emitted sheet, so a caller's radius is a line of
   code that does nothing — DEF-41-05, still open and not this phase's to close.

Delete the three hand-rolled pulse blocks and the line-colour-as-ground fill;
the primitive's own fill is deliberate and reads as absent content rather than as
content.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-touch-targets.mjs && node scripts/verify-conversion.mjs && node scripts/verify-breakpoints.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" "src/app/(admin)/admin/(work)/events/page.tsx"` returns
      at least 1 with `width="default"`
    - `grep -c "PageShell" "src/app/(admin)/admin/(work)/events/loading.tsx"`
      returns at least 1 and **its width is `default`**, identical to the page's —
      state the pair explicitly in the SUMMARY
    - `grep -c "animate-pulse" "src/app/(admin)/admin/(work)/events/loading.tsx"`
      returns 0
    - `grep -c "from \"@/components/ui/Skeleton\"" "src/app/(admin)/admin/(work)/events/loading.tsx"`
      returns 1
    - `src/components/events/EventList.tsx` lines around the six-control row: each
      of the six is a `Chip` or a `Button`; `grep -c "Badge" src/components/events/EventList.tsx`
      returns 0 for any of the six, and any surviving `Badge` is provably
      non-interactive — name each in the SUMMARY
    - Across all three files: zero raw palette utilities, zero legacy token
      utilities, zero `focus:outline-none`, zero `font-medium`, zero `max-w-`,
      zero `sm:`
    - `node scripts/verify-touch-targets.mjs` exits 0; its exemption ledger is
      pasted into the SUMMARY with how many times each exemption applied — if
      exemption 4 (a wrapper marking a child as the target) began applying here,
      say so, because that was a predicted-but-unconfirmed assumption
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The behaviour-change grep is clean and the SUMMARY states that no series
      identifier was renumbered, re-sorted or re-derived
  </acceptance_criteria>
  <done>The events surface, its six-control row and its placeholder are converted, and the placeholder's maximum matches the page's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff → venue address | the venue detail page renders an address that the public path does not |
| page → capability check | both pages resolve a capability before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-21 | Information disclosure | the venue address on the detail page | mitigate | conversion rule 8: no visibility predicate and no query changes; the diff is grepped and the SUMMARY states it explicitly. If a class change would surface an address earlier, the plan halts |
| T-41.1-22 | Tampering | the series progressivo on the events listing | mitigate | stop condition 3 stated in the rules and asserted in the SUMMARY: no renumber, no re-sort, no re-derive |
| T-41.1-23 | Denial of service | the six-control row at phone width | mitigate | each control becomes an interactive chip at the 44px floor and the row wraps; the shrink allow-list is closed and this row is not on it |
| T-41.1-24 | Tampering | the venue edit modal's confirmation | mitigate | the dialog primitive supplies escape, focus trap and background inertness by specification; the initial focus is placed imperatively after the modal opens because the React autofocus prop never reaches the DOM |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run verify` exits 0 with no refusal and no verdict line moved
- `verify-dialogs` still reports `REMAINING = 14` — the entry is paid in the tree and reported, not deleted here
- The behaviour-change grep is clean and pasted in
</verification>

<success_criteria>
Two surfaces on the shell, one hand-rolled overlay replaced by the platform's
modal, four raw palette hits deleted by a prop rather than recoloured, six 28px
controls raised to the floor, and one placeholder rendering the same maximum as
its page.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-07-SUMMARY.md` when done,
with the closure output per page, the behaviour-change grep, the touch-target
exemption ledger, the two `CONVERTED` entry texts, and the `REMAINING` entries
emptied by gate.
</output>
