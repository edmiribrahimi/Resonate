---
phase: 41.1-work-surface-conversion
plan: 03
type: execute
wave: 1
depends_on: ["41.1-01"]
files_modified:
  - scripts/verify-tables.mjs
  - scripts/verify-breakpoints.mjs
autonomous: true
requirements: [DS-09, RESP-01]
must_haves:
  truths:
    - "verify-tables prints the work-group table debt as its own number"
    - "verify-breakpoints prints the work-group sm: debt as its own number"
    - "The three table reason strings name the surface that actually imports the file"
    - "The copy-out diagnostic grid is still exempt and is still not on REMAINING"
  artifacts:
    - path: "scripts/verify-tables.mjs"
      provides: "the table gate with tagged debt and corrected reasons"
      contains: "WORK GROUP REMAINING"
    - path: "scripts/verify-breakpoints.mjs"
      provides: "the breakpoint gate with tagged debt"
      contains: "WORK GROUP REMAINING"
  key_links:
    - from: "scripts/verify-tables.mjs"
      to: "the phase's criterion 2"
      via: "the printed work-group count"
      pattern: "WORK GROUP REMAINING"
---

<objective>
Give `verify-tables.mjs` and `verify-breakpoints.mjs` the group tag and the
per-group print, and correct the three table reason strings that name a surface
which does not import the file.

Purpose: criteria 2 and 4 both say *"empty for the work group"* and the lists are
flat today, so the phase's own exit condition is eyeballed. **A criterion that
cannot be read off a gate is a claim** (D-41.1-11). And a plan that assigned
tables by reading the current reasons would have put three of the five in the
wrong place (D-41.1-22).

Output: two gates printing a work-group number, five entries tagged in one gate
and nineteen in the other, three reasons re-derived from the importer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
</context>

<plan_rules>
- This plan is the **only** plan in wave 1 that may edit these two files.
- **No entry is added to or removed from either `REMAINING`.** Tagging is not
  paying. Deletions belong to the reconciliation plan of the wave that converts
  the surface (D-41.1-22).
- **The negative obligation:** `ReviewListClient.tsx` is exempt permanently and is
  **deliberately not** on `REMAINING`, because a file that will never convert on a
  list that can only shrink makes the number lie. Nothing in this plan adds it,
  and nothing tidies it in (D-41.1-14, D-41-16).
</plan_rules>

<tasks>

<task type="auto">
  <name>Task 1: verify-tables — the group tag, the per-group print, and the three wrong reasons</name>
  <files>scripts/verify-tables.mjs</files>
  <read_first>
    - `scripts/verify-tables.mjs` lines 423-453 — `REMAINING`, the five entries in the `[path, reason, target]` shape
    - `scripts/verify-tables.mjs` lines 355-380 — `REVIEW_GRID_FILE` and `REVIEW_GRID_REASON`, the exemption declared before the gate's first run, including the sentence about why it is not on `REMAINING`
    - `scripts/verify-tables.mjs` lines 183-186 — `refuse()`, exit 2
    - `scripts/conversion-manifest.mjs` line 80 and its docblock — the third field as a load-bearing column
    - `scripts/verify-breakpoints.mjs` lines 196-217 — the four rules that travel with a `REMAINING` constant
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §5.1 (the five entries and the three wrong reasons at `:438`, `:443`, `:453`) and §5.2 (the closed vocabulary)
  </read_first>
  <action>
Add the group tag as an additional tuple field on each of the five `REMAINING`
entries, keeping the existing `[path, reason, target]` fields in place. Validate
at load against the closed vocabulary `work`, `public-member-money`, `phase-42`,
`exempt`; an unknown or missing tag is a **refusal** (exit 2), not a failure.

**All five are `work`.** Say that in a sentence beneath the constant, and say
what follows from it: criterion 2 for this gate is therefore *"`REMAINING`
reaches 0"* and the tag adds nothing here **except honesty about that** — which
is worth the field, because the same tag on the sibling gate is load-bearing.

**Correct the three reason strings by re-deriving each from `grep -rln` on the
importer, and put the command's result in the reason:**

- `:438` — `analytics/DrinkSalesBreakdown.tsx` says *"the /admin/analytics
  surface"*. Its only importer is
  `src/app/(admin)/admin/(work)/events/[id]/analytics/page.tsx:12`.
- `:443` — `analytics/ReferralChainTable.tsx` says *"the /admin/analytics
  surface"*. Its only importer is
  `src/app/(admin)/admin/(work)/analytics/members/page.tsx:6`.
- `:453` — `admin/TransactionList.tsx` says *"the event tickets work surface"*.
  Its only importer is `src/app/(admin)/admin/(work)/finance/page.tsx:2`.

Record the class beneath the constant in the house shape (`PageShell.tsx:42-46`):
the superseded sentence stays visible with the measurement that superseded it,
because a decision undone without its measurement reads as a slip. Add the
sentence that a reason travels with an entry **so that it stays true**, and that
five wrong ones across two gates is a pattern rather than five typos.

Print the per-group counts and, on its own unmissable line, the work-group total
— the line criterion 2 is read off. When it reaches zero, say so in one line
that cannot be missed, in the same loud treatment `STALE` already gets.

**Touch nothing at `:355-380`.** The exemption's constants and its reason are
untouched by this plan, and the reason's own statement of what it does **not**
cover — `ReviewListClient.tsx:205`'s eleven-pixel type size — stays exactly as
written, because plan 41.1-09 owes that line and the reason is where the debt
is recorded.
  </action>
  <verify>
    <automated>node scripts/verify-tables.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-tables.mjs` exits 0, prints `TABLES_OK`,
      `REMAINING = 5` and a line matching `WORK GROUP REMAINING = 5`
    - `grep -c "the /admin/analytics surface" scripts/verify-tables.mjs` returns 0
    - `grep -c "the event tickets work surface" scripts/verify-tables.mjs` returns 0
    - The reason for each of the three corrected entries contains the exact path of
      its measured importer as listed in the action
    - `grep -n "ReviewListClient" scripts/verify-tables.mjs` shows it only inside
      the exemption constants, never inside `REMAINING`
    - `grep -c "text-\[11px\]" scripts/verify-tables.mjs` returns at least 1 — the
      exemption still records the debt it does not cover
    - **Refusal proof:** set one tag to an invented value, record exit 2 and the
      message, restore, re-run to green
  </acceptance_criteria>
  <done>Five tagged entries, three true reasons, a work-group line, and the exemption untouched.</done>
</task>

<task type="auto">
  <name>Task 2: verify-breakpoints — the group tag on nineteen entries, assigned by importer</name>
  <files>scripts/verify-breakpoints.mjs</files>
  <read_first>
    - `scripts/verify-breakpoints.mjs` lines 219-333 — `REMAINING`, nineteen entries in the `[path, count, reason]` shape
    - `scripts/verify-breakpoints.mjs` lines 196-217 — the four rules that travel with the constant
    - `scripts/verify-breakpoints.mjs` lines 338-342 — `refuse()`
    - `scripts/verify-breakpoints.mjs` lines 594-604 — how the debt is printed loudly on a green
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §5.1 — the measured split: **14 work files / 21 uses**, **5 non-work files / 16 uses**, and the fifth non-work file
  </read_first>
  <action>
Add the group tag as an additional tuple field on each of the nineteen entries,
validated against the same closed vocabulary, with an unknown or missing tag a
**refusal**.

**Assign every tag from the file's measured importer, never from its path
prefix.** The measured split is 14 `work` / 5 `public-member-money`:

- `work` (14 files, 21 uses): the six route files under `(work)/analytics*` and
  `(work)/events/[id]/analytics*`, plus `KPIDashboard`, `MediaReviewGrid`,
  `DrinkSalesBreakdown`, `MemberSpendTable`, `ReferralChainTable`,
  `SalesDashboard`, `TransactionList`, `AssignmentsClient`.
- `public-member-money` (5 files, 16 uses): `RedeemConfirmationModal`,
  `SumUpCheckoutModal`, `GuestLoginBanner`, `GuestTokenDisplay`, and
  **`src/components/media/MediaUpload.tsx`**.

Write beside `MediaUpload.tsx` why it carries the tag it does: it is not under
`(public)/events/[slug]/**` at all — its only importer is
`src/app/(public)/events/[slug]/MediaGallerySection.tsx:8` — and **a tag derived
from a glob would have missed it.** That single file is the argument for tagging
by decision. Correct the figure while you are there: `D-41.1-11` estimated
*"roughly fifteen work-group and four public"*; the measurement is 14 and 5, and
the correction is recorded with its command rather than silently applied.

Print the per-group counts of both files and uses, and print the work-group total
on its own unmissable line — the line criterion 4 is read off. Add the caution
the constant's rules do not yet carry: **a group count that falls is subject to
the same defect as a total that falls**, so the per-wave reconciliation diffs
entries, never counts (D-41.1-16).
  </action>
  <verify>
    <automated>node scripts/verify-breakpoints.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-breakpoints.mjs` exits 0, prints `BREAKPOINTS_OK`, 19
      files / 37 uses, and a line matching `WORK GROUP REMAINING = 14` with its
      use count 21
    - The per-group print also reports `public-member-money` at 5 files / 16 uses
    - Every entry carries a tag from the closed set; **refusal proof** recorded as
      in task 1
    - `grep -n "MediaGallerySection" scripts/verify-breakpoints.mjs` returns the
      line where `MediaUpload.tsx`'s importer is named in its reason
    - Check A's `xl:` / `2xl:` count is still zero across all files
    - `REMAINING` still holds exactly 19 entries
    - `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>Nineteen tagged entries assigned by importer, a work-group line, and the estimate corrected with its measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → criterion | the printed work-group number is what closes criteria 2 and 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-07 | Repudiation | the per-group counts | mitigate | an unknown or missing tag is a refusal (exit 2), so a count is never computed over a partially tagged list |
| T-41.1-08 | Repudiation | a falling group count | mitigate | the caution is written into the constant's rules: the reconciliation diffs entries, not counts (D-41.1-16) |
| T-41.1-09 | Tampering | the permanent exemption | mitigate | the negative obligation is stated in this plan's rules and asserted by grep: `ReviewListClient.tsx` appears only in the exemption constants |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `node scripts/verify-tables.mjs` exits 0 with `WORK GROUP REMAINING = 5`
- `node scripts/verify-breakpoints.mjs` exits 0 with `WORK GROUP REMAINING = 14`
- `npm run verify` exits 0 with no refusal
- Two recorded refusal proofs, one per gate
</verification>

<success_criteria>
Both gates print the number their criterion is read off, every entry carries a tag
assigned from a measured importer, three reasons are true of the tree, and the
permanent exemption is exactly where it was.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-03-SUMMARY.md` when done,
with both gates' full output and the two refusal proofs.
</output>
