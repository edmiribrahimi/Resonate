---
phase: 41.1-work-surface-conversion
plan: 16
subsystem: gates
status: "COMPLETE ON TASKS 1-2 AND THE FOUR HAND-OFFS — task 3's checkpoint is DEFERRED to the end-of-phase batch, every row unticked, none self-approved and none inferred"
tags: [reconciliation, gates, conversion-manifest, entry-diff, chart-colour-blind-spot, h41-1, h41-3]
requires:
  - "41.1-12 · 41.1-13 · 41.1-14 · 41.1-15 — the four wave-5 plans, each of which reported an entry text and edited nothing under scripts/"
provides:
  - "five declared CONVERTED surfaces, so the closure-walking gates now scan 126 files over 23 surfaces instead of 93 over 18"
  - "fifteen REMAINING entries deleted on the authority of the tree, each departure named, each leaving a comment"
  - "the finance surface's declaration held for plan 41.1-24, with its reason and an arity correction sitting beside the list"
  - "the chart-colour blind spot counted at eight values across three files — a note, not a check, and D-41.1-30 left open"
affects:
  - "plan 41.1-17 and wave 7 — the finance closure is theirs to unblock"
  - "plan 41.1-21 and wave 8 — the one surviving entry on both debt gates is the sales dashboard"
  - "plan 41.1-24 — the held entry text and its arity correction are written where that plan will look"
tech-stack:
  added: []
  patterns:
    - "an entry diff re-derived with a second instrument that does not use the gates' own comment stripper"
    - "a paid debt leaves a comment naming the plan, the task and the figures"
    - "a sentence whose figure a deletion falsifies is restated with its withdrawn wording quoted, never deleted in silence"
key-files:
  created: []
  modified:
    - scripts/conversion-manifest.mjs
    - scripts/verify-tables.mjs
    - scripts/verify-breakpoints.mjs
    - scripts/verify-tokens.mjs
decisions:
  - "the fourth breakpoints entry on the member-analytics surface was found by counting on the tree, not by trusting any plan's number"
  - "/admin/finance stays undeclared, and the held text's three-element arity is recorded rather than corrected in the quotation"
  - "D-41.1-30's second half was NOT answered: the blind spot is counted, no mechanism was built and no colour was chosen"
  - "the acceptance criterion counting a literal string was measured, reported and NOT reached by editing a reported entry text"
metrics:
  duration: "one session"
  completed: 2026-08-14
---

# Phase 41.1 Plan 16: Wave-5 Reconciliation Summary

The five analytics surfaces wave 5 converted are declared and the gates now walk
them — **126 files over 23 surfaces, up from 93 over 18** — fifteen debt entries
left two lists because the tree changed rather than because a count moved, the
finance surface is still deliberately undeclared with its reason written beside
the list, and the chart-colour hole two plans refused to fill is now **counted**
instead of merely disclaimed. **The human observation is deferred, not done**,
and this file says so in those words.

---

## 0. What this plan is, in one sentence

D-41.1-16's reconciliation, arriving after four parallel plans that each reported
what they had emptied and **edited nothing under `scripts/`** — because
D-41.1-22 makes this plan the single declared owner of every gate edit in the
wave, which is the only way D-41.1-15's partition rule is satisfiable at all when
a `REMAINING` list is one file four plans would all want to touch.

**Three commits, in order:**

| # | Commit | What |
|---|---|---|
| 1 | `d6df6ea` | five `CONVERTED` entries; the sixth held with its reason and an arity correction |
| 2 | `1da1bfa` | fifteen `REMAINING` entries deleted across two gates, each leaving a comment |
| 3 | `a090c79` | the chart-colour blind spot counted in the palette gate — a note, not a check |

---

## 1. Task 1 — the five surfaces, and what the gates can now see

### 1.1 Before and after, measured on both sides

| | before | after |
|---|---|---|
| `CONVERTED` surfaces | 18 | **23** |
| files scanned by checks A, B and D | 93 | **126** |
| elements measured by `verify-touch-targets` | 24 across 49 files | **28 across 56 files** |

**Per surface, straight off the gate:**

```
/admin/analytics/members       [wide]  17 file(s)
/admin/events/[id]/analytics   [wide]  22
/admin/analytics               [wide]  13
/admin/analytics/compare       [wide]  15
/admin/members/growth          [wide]  13
```

Those figures overlap heavily — the spine and the primitives are reached by
nearly all of them — so they do not sum to 33. The number that is derived rather
than added is the union: **126 files, up from 93, a rise of 33**, and it is the
gate's own count of the distinct files checks A, B and D opened.

**This is the moment four SUMMARYs were waiting for and each of them said so.**
Every wave-5 plan recorded that its own gate runs proved nothing about its own
work, because the gates walk *declared* closures and none of these five was
declared. From commit `d6df6ea` the statement covers the diff, and it is green.

### 1.2 The touch-target gate needed no new exemption, and that is plan 41.1-14's doing

Wave 3's reconciliation had to add an exemption when its declarations widened
this gate's scope. **This one did not**, and the reason is worth naming because
it is the right direction taken under pressure: plan 41.1-14 declared its own
surfaces on a **disposable copy of `scripts/` outside the repository**, found a
red on a call site that had genuinely declared the 44px floor — hidden inside an
interpolated module constant the gate cannot resolve — and **fixed the element
rather than the gate**. The gate's blind spot is unchanged and is reported at
that plan's §7.3 as belonging to whoever owns `scripts/`; the red it produced is
gone because the element moved, not because the matcher was narrowed.

Measured here after the five declarations landed: **28 elements across 56 files,
exit 0, no exemption added.**

### 1.3 The entry texts came from the plans that did the work

Each reason is the text its plan reported, **taken rather than re-composed**. The
only editorial change is joining a reason a SUMMARY printed across several lines
into the one string this list's shape requires. The plan that walked the closure
is the one that knows what *whole* turned out to mean; a reconciliation rewriting
those sentences would be asserting about files it never opened.

All five routes are **named on §4's closed wide list** — `verify-conversion.mjs`
`WIDE_ROUTES` at `:1223`–`:1227` — so `checkManifest()` and check D compare the
declared width against the contract rather than agree with it. **No width
disagreement was raised.**

### 1.4 `/admin/finance` is still not declared, and that is the point

Check A walks a declared surface's import closure. This one runs through
`src/components/admin/TransactionList.tsx` into
`src/components/admin/RefundDialog.tsx`, **which plan 41.1-17 converts in wave
7**. An entry written today would be a claim about a file nobody has opened, and
check A would redden on that file's palette hits — correctly.

`grep -c '"/admin/finance"'` inside `CONVERTED` returns **0**. The held entry
text plan 41.1-15 drafted sits in a comment beside the list, marked as held for
**plan 41.1-24**, with its one-line reason.

**One measured caution was added, and it is why the text is quoted rather than
pasted.** The drafted entry is **three elements**; this list's shape is **four**
— `[route, pageFile, width, reason]`. Pasted as written, the width slot would
receive the reason string and the page-file slot would receive the width.
`checkManifest()` does catch it, but it catches it as *"not on disk under that
exact name"* — a refusal whose sentence points at a missing file rather than at a
missing field, which is a long way from the actual defect and would cost the next
reader an hour. The page file measured on this tree is
`src/app/(admin)/admin/(work)/finance/page.tsx`, and `/admin/finance` is already
on the closed wide list at `verify-conversion.mjs:1218`.

**The correction is recorded beside the quotation, not applied inside it.** A
reported entry text is taken as reported; editing somebody else's reported
sentence is precisely the move this block's own opening paragraph refuses.

### 1.5 The acceptance criterion whose arithmetic could not be met

> `grep -c "no query changed" scripts/conversion-manifest.mjs` returns at least 17.

**Measured: 9.** Case-insensitively, 15. **No entry text was edited to reach 17**,
and the criterion is not reachable at all — the maximum available under the
reported texts is 15, before case is even considered:

| Reason it falls short | Entries |
|---|---|
| the sentence is capitalised, because it begins a sentence | `/admin/artists`, `/admin/newsletter`, and **four of the five entered here** — `/admin/events/[id]/analytics`, `/admin/analytics`, `/admin/analytics/compare`, `/admin/members/growth` |
| the surface grants a **capability** rather than running a query, so its plan wrote *"no capability key, guard or action payload changed"* | `/admin/events/[id]/assignments` |
| written before the sentence became the required form | `/admin/formats`, `/admin/members/register` |
| the surface has no query to speak of | the four focus screens, `/gallery` |

The sibling clauses were measured too and are better instruments: **`action
payload` appears 16 times** and **`no capability check touched` 16**.

This is the third recurrence of one defect in this phase family — **a criterion
counting a string where it means a property** — and plan 41.1-11 §1.5 recorded
the first, plan 41.1-15's deviation 2 the second. Editing four reported sentences
so a `grep -c` reaches 17 would have moved the metric without moving an inch of
work, which is the behaviour D-41.1-16 names.

---

## 2. Task 2 — the entry diff, all 22 rows

**Diffed by entry, never by count**, and with a **second instrument**: a
read-only script under the session scratchpad that greps each declared path for
the needle its gate counts and **deliberately does not use the gates' shared
comment stripper**. Asking the gate what it thinks is not a check on the gate.

> **The asymmetry that makes a raw count sound for a deletion.** A comment can
> only *lower* a live count, never raise it. So a raw needle count of **0** is
> proof the entry is not owed, whatever any stripper thinks. For an entry that
> **stays**, raw > 0 is necessary but not sufficient, so the gate's own live
> count is quoted beside it.

### 2.1 `verify-tables.mjs` — 5 entries

Needle: the literal table element opener.

| Entry | Gate's verdict | What the tree says |
|---|---|---|
| **`components/analytics/MemberSpendTable.tsx`** | **STALE** — *"converted; remove this entry"* | raw **0** → **NOT owed** → **DELETED** (plan 41.1-12 task 1) |
| **`components/analytics/DrinkSalesBreakdown.tsx`** | **STALE** | raw **0** → **NOT owed** → **DELETED** (plan 41.1-13) |
| **`components/analytics/ReferralChainTable.tsx`** | **STALE** | raw **0** → **NOT owed** → **DELETED** (plan 41.1-12 task 2) |
| `components/events/SalesDashboard.tsx` | on the list, counted | raw **1** → **owed** — plan 41.1-21, wave 8 |
| **`components/admin/TransactionList.tsx`** | **STALE** | raw **0** → **NOT owed** → **DELETED** (plan 41.1-15 tasks 1–2) |

**Four deletions.** Declared entries **5 → 1**; work group **5 → 1**.

### 2.2 `verify-breakpoints.mjs` — 17 entries

Needle: the small-tier prefix. Declared count / re-derived count.

| Entry | Declared | Tree | Verdict |
|---|---|---|---|
| **`(work)/analytics/loading.tsx`** | 2 | **0** | **NOT owed** → **DELETED** (41.1-14) |
| **`(work)/analytics/members/loading.tsx`** | 1 | **0** | **NOT owed** → **DELETED** (41.1-12) |
| **`(work)/analytics/members/page.tsx`** | 1 | **0** | **NOT owed** → **DELETED** (41.1-12) — *the fourth entry, §2.4* |
| **`(work)/analytics/page.tsx`** | 1 | **0** | **NOT owed** → **DELETED** (41.1-14) |
| **`(work)/events/[id]/analytics/loading.tsx`** | 1 | **0** | **NOT owed** → **DELETED** (41.1-13) |
| **`(work)/events/[id]/analytics/page.tsx`** | 1 | **0** | **NOT owed** → **DELETED** (41.1-13) |
| **`components/analytics/KPIDashboard.tsx`** | 1 | **0** | **NOT owed** → **DELETED** (41.1-14) |
| `components/media/MediaUpload.tsx` | 1 | 1 | owed — 41.2's |
| `(public)/events/[slug]/RedeemConfirmationModal.tsx` | 4 | 4 | owed — 41.2's |
| `(public)/events/[slug]/SumUpCheckoutModal.tsx` | 4 | 4 | owed — 41.2's |
| `(public)/events/[slug]/menu/GuestLoginBanner.tsx` | 3 | 3 | owed — 41.2's |
| `(public)/events/[slug]/menu/GuestTokenDisplay.tsx` | 4 | 4 | owed — 41.2's |
| **`components/analytics/DrinkSalesBreakdown.tsx`** | 2 | **0** | **NOT owed** → **DELETED** (41.1-13) |
| **`components/analytics/MemberSpendTable.tsx`** | 2 | **0** | **NOT owed** → **DELETED** (41.1-12) |
| **`components/analytics/ReferralChainTable.tsx`** | 2 | **0** | **NOT owed** → **DELETED** (41.1-12) |
| `components/events/SalesDashboard.tsx` | 2 | 2 | owed — plan 41.1-21, wave 8 |
| **`components/admin/TransactionList.tsx`** | 3 | **0** | **NOT owed** → **DELETED** (41.1-15) |

**Eleven deletions.** Declared entries **17 → 6**, declared uses **35 → 18**; the
work group's declared figures **12 → 1 file** and **19 → 2 uses**.

### 2.3 `verify-dialogs.mjs` — untouched at 13, and that is a measurement

`REMAINING = 13`, `WORK GROUP REMAINING = 3`, no stale entry, exit 0 — identical
before and after. **Wave 5 paid no dialog entry**, and the tree agrees. Stated
rather than skipped, because "we did not look" and "we looked and nothing moved"
are different claims and only one of them is this one.

### 2.4 The fourth entry — found by counting, not by reading a number

Plan 41.1-12's own acceptance text names **three** entries on the breakpoints
gate for the member-analytics surface: the two tables and the loading file. **The
page file carries one too**, declared count 1, for the same KPI grid the loading
file mirrors. That plan measured the discrepancy itself, printed the command that
produced it, and **reported it rather than quietly satisfying the smaller
number.**

Confirmed here independently by counting on the tree before any deletion: four
entries name that surface, all four raw-count 0, all four gone.

**A reconciliation working from the plan's figure would have removed three and
left a fourth asserting something that had stopped being true** — which is
precisely how a debt list starts to lie, and it is the whole reason this task
diffs entries instead of counts.

### 2.5 Two numbers, by design — and neither of them moved the way a reader expects

Both gates print **two different `REMAINING` figures**, and a reader who
conflates them will read commit `1da1bfa` as work vanishing.

| Gate | The constant — `REMAINING entries declared` | The live count — `REMAINING` |
|---|---|---|
| `verify-tables` | **5 → 1** | **1 → 1**, unmoved |
| `verify-breakpoints` | **17 → 6** entries, **35 → 18** uses | **6 → 6** files, **18 → 18** uses, unmoved |

**The live counts did not move because they had already fallen.** Every one of
the fifteen files stopped carrying its needle in its own plan's commit, and both
gates had been printing all fifteen as STALE ever since — *"converted; remove
this entry"*. **The notice is what makes these deletions a response rather than a
tidy.** Declared and measured now agree exactly, which is the state a
reconciliation exists to restore.

This also settles an acceptance criterion honestly rather than by arithmetic: the
plan asks that `verify-tables` exit 0 with `REMAINING = 1` and `WORK GROUP
REMAINING = 1`. **Both were already true before this plan touched anything**,
because that gate's printed `REMAINING` is *declared minus missing minus stale* —
the count of entries still **owed**. Plan 41.1-15's deviation 2 recorded the same
property from the other side when it printed 4 rather than 5. What this plan
moved is the **constant**, and the constant is the thing a stale entry lives in.

### 2.6 The three negative obligations, asserted after the edit

A textual diff was not trusted for this. Both versions of both lists were
**parsed** — the array literal sliced out of the source text of each version and
evaluated as data, which makes the comparison blind to every comment added, since
a comment is not an entry:

```
verify-breakpoints:  17 entries -> 6
  DELETED (11) · ADDED (0) · GROUP TAG MOVED (0) · SURVIVING ENTRY ALTERED IN ANY FIELD (0)
verify-tables:        5 entries -> 1
  DELETED (4)  · ADDED (0) · GROUP TAG MOVED (0) · SURVIVING ENTRY ALTERED IN ANY FIELD (0)
```

- **No entry was added.** A raw `git diff` shows one added path line —
  `SalesDashboard.tsx` on the breakpoints gate — and it is an artefact of
  restructuring the block around it. The parsed diff settles it: that entry was
  present before, is present after, and **is byte-identical in all four fields**.
  This is exactly why the obligation was checked with a parser and not a grep.
- **No entry moved between groups.** Zero tags touched on either list.
- **`ReviewListClient.tsx` is on no debt list.** Asserted by grep **after** the
  edits, because the failure mode on that file is a tidy rather than a decision:

```
$ command grep -n "ReviewListClient" scripts/verify-tables.mjs
101:  * `src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx` — the   ← docblock, the exemption
419:  * `ReviewListClient.tsx` stays a table for ever (D-41-16, D-41.1-14)       ← docblock
426:    'src/app/(admin)/admin/events/[id]/review/ReviewListClient.tsx';         ← REVIEW_GRID_FILE

$ command grep -c "ReviewListClient" scripts/verify-breakpoints.mjs
0
```

Three occurrences, all inside the exemption constants. **Zero inside
`REMAINING`.** D-41-16 stands: that grid stays a table for ever, and its
exemption survives this phase untouched.

### 2.7 Tree against SUMMARY: no disagreement

Four plans reported fifteen emptied entries. The tree, asked with a second
instrument, says the same fifteen and no others. **Nothing had to be refused, and
no plan's claim had to be named as unsupported.** That is worth stating precisely
because the procedure exists for the case where it is false — and because on this
list the case where it is false has a name: four distinct mechanisms in this
phase family have already made a count fall for the wrong reason.

### 2.8 Four sentences a deletion falsified, restated rather than left standing

A sentence describing owed work, left standing after the work is done, is the
same defect as a `REMAINING` entry left standing after its debt is paid: the next
person either redoes it or concludes the file is not maintained. Four were caught
and all four are restated **with the withdrawn wording quoted**, which is this
file family's own habit (`src/components/ui/PageShell.tsx:42-46`):

| File | What it said | What it says now |
|---|---|---|
| `verify-tables.mjs` header | *"Five tables still rendering their own dual-render is a green"* | one table, with the old figure and the reason it changed |
| `verify-tables.mjs` header | *"Four of the five switch at a breakpoint §2.1 does not have"* | the one that remains does, as four of the original five did |
| `verify-tables.mjs` `REMAINING` docblock | *"All five entries are `work`"* | every entry is `work` — five then, one now |
| `verify-breakpoints.mjs` `GROUP_TAGS` docblock | *"14 of its 19 entries are the work surface and 5 are not"* | 1 of 6, **with the fact that makes it an argument rather than a figure**: every one of the thirteen entries that has left this list was `work`, and not one `public-member-money` entry has moved at all |

The last is the only one where restating changed what the paragraph *proves*. Its
argument is about a **ratio** — why the group tag is load-bearing on this gate
and not on its sibling — and a ratio quoted from a list that has since halved is
an argument the next reader cannot check. Written out, the ratio now says
something better than it did: the criterion is working, not the tag drifting.

---

## 3. The gates, after everything

| Gate | Exit | The numbers |
|---|---|---|
| `verify-conversion` | **0** | `CONVERSION_OK` — five checks over **23** declared surfaces, **126** files scanned, 264 walked under `src/` |
| `verify-tables` | **0** | `TABLES_OK` — declared **1**, `REMAINING = 1`, `WORK GROUP REMAINING = 1`, **no stale entry** |
| `verify-breakpoints` | **0** | `BREAKPOINTS_OK` — declared **6** / **18** uses, measured 6 / 18, `WORK GROUP REMAINING = 1` (2 uses), **no stale entry** |
| `verify-dialogs` | **0** | `DIALOGS_OK` — `REMAINING = 13`, `WORK GROUP REMAINING = 3`, unchanged |
| `verify-touch-targets` | **0** | 28 elements measured across 56 files, no new exemption |
| `verify-tokens` | **0** | all seven checks, unchanged by §5's note |
| `npm run build` | **0** | `✓ Compiled successfully`. The one warning is the pre-existing workspace-root inference, a lockfile-ancestry artefact of running inside a worktree |
| `npm run verify` | **2** | 16 run — **15 passed, 0 FAILED, 1 REFUSED** |

Breakpoints by group, measured:

```
work                   1 file(s)    2 use(s)
public-member-money    5 file(s)   16 use(s)
phase-42               0 file(s)    0 use(s)
exempt                 0 file(s)    0 use(s)
```

### 3.1 The refusal, recorded as unmet

`verify:capabilities` **refuses (exit 2)**: it needs `SUPABASE_ACCESS_TOKEN` and
`NEXT_PUBLIC_SUPABASE_URL`, `.env.local` is gitignored, and no worktree holds it.
**Nothing about the capability model was measured. A refusal is not a pass**, and
no credential was copied in to make the number look better — doing so would have
manufactured a measurement instead of taking one. `npm run verify` therefore
exits **2** and not 0, which is the honest state of this tree and the same state
waves 2, 3 and 5 recorded. Closing it belongs to the orchestrator on the main
checkout.

**The plan's verification line asks for `npm run verify` exit 0 with no refusal.
That is not obtainable here and was not manufactured.**

### 3.2 What none of it says

Every one of those gates reads class strings, import clauses and declarations.
Not one renders a pixel. `verify-tables`' own closing paragraph says it about the
question this plan owes: *"whether a seven-column table is comprehensible as a
card"* is **H41-3, a person holding a phone**, and no tick stands in for it.
**There is no test runner for this product**, and nothing above is a passing test.

---

## 4. The two decisions this plan did NOT close

### 4.1 D-41.1-29 — the semantic pair, and a consequence sitting in the tree

D-41.1-29 measured `--sem-done` against `--sem-crit` at **1.23 : 1**, against
WCAG 1.4.11's 3 : 1 for distinguishing a UI component, and closed wave 5's one
blocking row with a **no**. It is not re-opened here, and **nothing in this
commit range implies a colour pair was available and simply not used** — the
semantic set cannot carry two distinguishable outcome fills, full stop.

**But the decision has a consequence that is in the tree right now, and it splits
in two**, which matters because the plan's own checkpoint text conflates them:

| Site | How the pair is used | Verdict from D-41.1-29 |
|---|---|---|
| `src/components/analytics/DrinkSalesBreakdown.tsx:86,94` | as **ink**, on two figures in **two different columns**, each under its own header | **fine** — the pair works as ink, at 5.99 and 7.36 on the ground, and the channel separating the two figures is the **column**, not the hue |
| `src/components/analytics/TokenLifecycleCard.tsx:59,65` | as **two adjacent fills** on one stacked meter | **this is the measured case** — two segments of one bar, 1.23 : 1 apart |

The plan's checkpoint row 6 points at the table and asks *"can you still tell
them apart"*. On the table the answer is structurally yes. **The exposure is the
meter**, and the H41-3 row at §6.3 is written to point there instead.

**The architecture around the meter is already correct and that is why this is a
report and not a defect:** the legend's two dots are `aria-hidden`, each sits
beside its own **word** and its own figure, and the card's headline reads the two
rates in words. Nothing is lost to a screen reader. What a person glancing at the
**bar** sees is the open question, and only a person can answer it.

**No colour was chosen.** There is no second distinguishable fill inside the
semantic set to substitute — that is what D-41.1-29 measured — so any fix is
either a new token or a non-colour channel, and both are the owner's.

### 4.2 D-41.1-30 — counted, and left open in both halves

Two wave-5 plans reached this independently and **both refused to fill it**. This
plan refuses too, and adds the one thing it can add without deciding anything:
**the invisibility is now visible and counted.**

Measured on 2026-08-14 — **eight raw palette values reach the screen from three
files the palette gate READS and is silent about**, because not one of them is a
class attribute:

| Site | How many | How the colour travels |
|---|---|---|
| `src/lib/analytics/event-queries.ts:354-356` | 3 | a fill on a **data row returned by a query function** |
| `src/components/analytics/EventComparisonChart.tsx:58-60` | 3 | a module constant fed to a **rendering prop** |
| `src/components/analytics/MemberGrowthChart.tsx:108-109` | 2 | fill and stroke **props** |

The first is the sharpest: **the colour of a chart segment is part of a query's
result.** Every gate under `scripts/` reads class strings and import clauses, so
a colour that never becomes a class is outside the whole family's reach — the
same species of blind spot as the route-adjacent placeholder gap D-41.1-21
closed, met from the other direction. Two of the four-step palettes also open
with the accent token, which **is** visible — and a chart series is on neither
§5.1's reserved-for list nor its never-list, so the token already filling bars on
two surfaces is outside its own list.

The note went into `verify-tokens.mjs`'s existing *"the hole that remains"*
paragraph, with the command that regenerates the count, because a general
disclaimer is a sentence nobody can check and a specific dated inventory is one
anybody can.

> **The values themselves are deliberately NOT spelled in this file.** Writing
> four hex values into a published planning document is one short step from their
> being read as *the* chart palette, which is exactly the rule nobody has decided
> — and `.planning/` is tracked and this repository is public. The file and line
> are given instead; anyone who needs the values reads them from the source.

**What was deliberately NOT done:** no constant was added, no exit code can
change because of the note, and no mechanism was built. **D-41.1-30 has two
halves and both are the owner's** — whether a chart series gets its own scale,
and whether this gate family should be able to see a colour that never becomes a
class. Building the mechanism here would answer the second half **by precedent,
inside a reconciliation commit**, in the same wave in which two agents declined
to answer the first half inside a component. **Counting is not deciding.**

---

## 5. Task 3 — H41-1 and H41-3 for wave 5: **DEFERRED, and deferred is not done**

> **Every row below is `pending`. Not one was performed. Not one was
> self-approved. Not one may be inferred from a neighbouring row** —
> `41.1-VALIDATION.md` is explicit that an unticked row is the correct state
> until a person has looked, and `41-CR01-PASS.md` is explicit that a headless
> render is not a device render.

### 5.1 Why this is deferred rather than performed, stated plainly

The plan carries this checkpoint as `gate="blocking"` and D-41.1-18 puts H41-1
per wave rather than accumulated, on the argument that *a batch looked at once,
at the end, is a batch that gets skipped.* **The owner has directed that human
verifications are batched to the end of the phase.**

So these rows go into the end-of-phase batch, carried by the orchestrator. **The
tension with D-41.1-18 is real and is not being papered over:** that decision
exists precisely to stop this batching, and the owner's direction supersedes it
for scheduling while leaving its reason standing. The mitigation is that every
row below is an **executable procedure** — which route, which width, how the
observer reaches the control, and what must be true — because the thing that
makes a late batch get skipped is a batch that has to be re-derived before it can
be performed.

**And the honest constraint underneath.** Every one of these surfaces needs an
authenticated session holding `organizer.access`, and two panels on the per-event
analytics surface need `master`. No tool in this repository can mint one, and
wave 2 already recorded that obtaining one would require credential extraction
that was never authorised. That is a reason the rows are hard, not a reason they
are optional.

### 5.2 Preconditions for every row

Start the dev server. Sign in as `master`, or as an `organizer` who owns the
event in question — `master` is required for rows on the per-event analytics
surface, which carries two panels gated on `admin.access`. Set the viewport to
exactly **390px, 768px and 1440px**; resizing between rows is fine, **inferring
between them is not**. Where a row names an event, use one with real content: at
least three drink items with some tokens redeemed and some refunded, at least two
members with more than one purchase, and at least one member who arrived through
a referral.

**The four things every width row asks, on every surface:** nothing scrolls
sideways; no content sits under the navigation column at 768px and above; **no
doubled clearance** — content must not start 448px in; every control can be hit
with a thumb.

### 5.3 H41-1 — the five surfaces at three widths

| # | Route | Width | What must be true, and how to reach it | State |
|---|---|---|---|---|
| 1 | `/admin/analytics` | 390 | no leading gap; the KPI tiles are **one column**; each figure renders in the data face and is legible without zooming | ⬜ `pending` |
| 2 | `/admin/analytics` | 768 | content clears the 224px column and starts **once**, not twice. **The KPI grid must show TWO columns, not three** — this grid declared its axis from the small tier before wave 5 and putting three columns into the 544px a portrait tablet leaves is the exact failure the migration exists to fix | ⬜ `pending` |
| 3 | `/admin/analytics` | 1440 | single clearance; three columns; content stops widening at the **wide** maximum rather than running edge to edge | ⬜ `pending` |
| 4 | `/admin/analytics/compare` | 390 | no leading gap; the event picker rows stack; **each picker row's box is a full finger-sized target, not just the 16px drawn box** | ⬜ `pending` |
| 5 | `/admin/analytics/compare` | 768 | single clearance; the two mode controls sit as chips and neither is clipped; **no grid shows three columns** | ⬜ `pending` |
| 6 | `/admin/analytics/compare` | 1440 | single clearance; **wide** maximum; up to four series read side by side without the legend wrapping onto the chart | ⬜ `pending` |
| 7 | `/admin/analytics/members` | 390 | no leading gap; both tables render as **cards**, not as sideways-scrolling tables; the two KPI tiles are one column | ⬜ `pending` |
| 8 | `/admin/analytics/members` | 768 | single clearance; **the tables have switched to their table branch and no grid shows three columns**. This is the width where the table primitive's single breakpoint and the grid axis's middle step are visible in the same screenful | ⬜ `pending` |
| 9 | `/admin/analytics/members` | 1440 | single clearance; **wide** maximum; the seven-column table is not stretched to the window edge | ⬜ `pending` |
| 10 | `/admin/events/[id]/analytics` | 390 | no leading gap; every chart fits its card without sideways scroll; the drink breakdown renders as cards | ⬜ `pending` |
| 11 | `/admin/events/[id]/analytics` | 768 | single clearance; **no grid shows three columns**; the two master-only panels render (sign in as `master`) and clear the column like the rest | ⬜ `pending` |
| 12 | `/admin/events/[id]/analytics` | 1440 | single clearance; **wide** maximum; the funnel and the attendance meter are readable at full width | ⬜ `pending` |
| 13 | `/admin/members/growth` | 390 | no leading gap; the chart fits without sideways scroll; **the heading reads the surface's own name and not the address prefix** — this is the second instance of that correction | ⬜ `pending` |
| 14 | `/admin/members/growth` | 768 | single clearance; **no grid shows three columns**; the summary card and the chart do not overlap | ⬜ `pending` |
| 15 | `/admin/members/growth` | 1440 | single clearance; **wide** maximum | ⬜ `pending` |

### 5.4 H41-1 — the placeholder match, one row per surface

Throttle the network and reload. **Watch for a sideways jump when the data
lands.** Both the page and its placeholder declare `wide` on all five, so a jump
is a real finding and not a mis-set expectation: it means a placeholder's maximum
does not match its page's.

| # | Route | What must be true | State |
|---|---|---|---|
| 16 | `/admin/analytics` | no sideways jump; the placeholder's shapes match what arrives (a KPI grid and a feed, not a list) | ⬜ `pending` |
| 17 | `/admin/analytics/compare` | no sideways jump; the placeholder predicts a picker and a chart | ⬜ `pending` |
| 18 | `/admin/analytics/members` | no sideways jump; the placeholder predicts two tables and two tiles | ⬜ `pending` |
| 19 | `/admin/events/[id]/analytics` | no sideways jump; the placeholder predicts cards and charts | ⬜ `pending` |
| 20 | `/admin/members/growth` | no sideways jump; the placeholder predicts a chart and a summary card | ⬜ `pending` |

### 5.5 H41-1 on `/admin/finance` — cross-referenced, NOT restated

The plan's checkpoint names `/admin/finance` among the H41-1 surfaces even though
it is not declared. **Plan 41.1-15 already wrote those rows as executable
procedures** — its §6.2 rows 1–12 cover 390, 768, **820**, 1440, the throttled
reload, the disclosure target, the keyboard path, the failure state, two empty
states, the missing-buyer case and the status filter's accessible name; and its
§6.1 rows M1–M6 cover the pagination stack forward and back, the search branch,
the amount's position, the status words, and the two refund paths.

**They are referenced rather than repeated**, because a row written twice in two
files is a row two people each assume the other performed. **All eighteen remain
`pending` and belong in the same end-of-phase batch as the rows above.**

### 5.6 H41-3 — four tables, on a phone, held in a hand

**This is the observation no script can make**, and `verify-tables`' own closing
paragraph says so about itself. **DS-09 has no other evidence.**

| # | Table | Surface, and how to reach it | What must be true | State |
|---|---|---|---|---|
| **T1** | **member spend — seven columns as a card** | `/admin/analytics/members` at 390px | Can you tell, **at a glance**, who this row is and what they spent? **Is the rank still legible** — it stopped being an array index and became a property of the row, and dropping it from the card was refused because list position is not the fact. **Is the total the thing your eye lands on**, or is it lost among the smaller figures? The total is a mark slot and not a meta detail (D-41.1-13), and only a person can say whether that reads | ⬜ `pending` |
| **T2** | **referral chain — the expansion** | `/admin/analytics/members` at 390px, on a member who referred at least two others | Open a disclosure **on the phone**. Does the nested list read, or does it **push the row's own figures off the screen**? Then check the fact that changed rather than moved: the body used to hold ONE cell spanning all three columns, so three headers labelled columns no cell aligned to. **The three are actual cells now — do the card's three facts line up with the three headers the table branch shows at 768px?** | ⬜ `pending` |
| **T3** | **the two outcomes — and this row is NOT about the table** | `/admin/events/[id]/analytics`, any width | **Read §4.1 before performing this row.** On the **breakdown table** the two figures sit in two different columns under their own headers, and the pair works as ink — that part is not in doubt. **The row is about the stacked meter on the token lifecycle card**, where the same two semantics are two **adjacent fills**, and D-41.1-29 has already measured them at **1.23 : 1**. So the question is **not** "can you tell the fills apart" — that is answered, and the answer is no. It is: **with the fills effectively indistinguishable, do the legend's words and figures and the headline's two rates carry the meaning on their own?** Record what is seen. **Do not propose a colour** | ⬜ `pending` |
| **T4** | **finance transaction list — the amount first** | `/admin/finance` at 390px | Is the **amount** the first thing you read on a card? D-41.1-13 exists for this row and only a person can settle it. Then open a row's detail and page forward and back once. **Plan 41.1-15's M1, M3 and M5 are the same observation written in more detail — perform those, and treat this row as satisfied by them rather than as a second sitting** | ⬜ `pending` |

### 5.7 The rows whose outcome would change what a LATER plan writes

Flagged separately, because the others would change only whether a surface
already shipped is judged acceptable.

| Row | Why it is different |
|---|---|
| **T3** — the meter's two fills | It is **spine evidence**. D-41.1-25 left one case open — *does a result grade a person, or report a machine?* — and this is a machine outcome on a meter. If the words do not carry it, that is a case for putting the open question back to the owner, and **every remaining conversion that grades two outcomes inherits the answer.** It also feeds D-41.1-30, since the alternative to a second fill is a scale nobody has decided |
| **Rows 2, 5, 8, 11, 14** — the 768px three-column check | **Every one of the five surfaces' grid rules was mapped per class rather than renamed by prefix**, and waves 7 and 8 use the same mapping. If any grid still shows three columns at 768px, the mapping is wrong and the next two waves are about to repeat it. This is the specific failure a bare prefix rename produces |
| **T1** — the rank and the total | It is the first judgement on the published cell signature. Widening that signature for one table was **refused** by plan 41.1-12, and the remaining tables adopt the same primitive. If the seven-column card does not carry the facts that mattered, the refusal is the thing to revisit — in a plan, not in a call site |

### 5.8 What is NOT claimed

This plan does **not** claim H41-1 ran for wave 5, and does **not** claim H41-3
ran at all. It does not claim any of the five surfaces is workable. **RESP-01
does not close here and does not close in this phase** — it says *every* surface.
**DS-09 does not close either**, and T1–T4 are its only evidence. **Deferred and
done are different states**, and the phase's exit condition should read this
section as *owed*.

---

## 6. Deviations from plan

### 1. `[criterion arithmetic — not reachable without falsifying the work]` `grep -c "no query changed"` returns 9, not 17

- **Found during:** Task 1
- **Issue:** the criterion counts a literal string across at least seventeen
  entries. Four of the five reported texts capitalise it because it begins a
  sentence; eight of the twenty-three entries never carried it at all, for
  reasons that are properties of those surfaces and not oversights.
- **Fix:** **none applied to the entries.** The measurement and its full
  breakdown are at §1.5, alongside the two sibling clauses which measure 16 and
  16. The maximum reachable under the reported texts is 15.
- **Committed in:** `d6df6ea`

### 2. `[Rule 2 — a held entry that cannot be pasted is a trap for the plan it is held for]` the drafted finance entry's arity is recorded

- **Found during:** Task 1
- **Issue:** plan 41.1-15's drafted `/admin/finance` entry is three elements;
  `CONVERTED` takes four. Pasted as written it would be caught by
  `checkManifest()` — but caught as *"not on disk under that exact name"*, which
  points at a missing file rather than at a missing field.
- **Fix:** the quotation is **verbatim and unedited**; the correction, the
  measured page file and the wide-list citation sit **beside** it. §1.4.
- **Committed in:** `d6df6ea`

### 3. `[scope, declared]` a fourth file was edited that the plan's `files_modified` does not name

- **Found during:** Task 3's preparation
- **Issue:** the plan names three files. This plan also edited
  `scripts/verify-tokens.mjs`.
- **Why:** D-41.1-22 makes this plan the single declared owner of every gate edit
  in the wave, so if a gate edit is warranted it is this plan's and no other's.
  The edit is a **comment**: no constant, no check, no reachable change of exit
  code, verified by running the gate before and after (exit 0, seven checks, both
  times). It exists because the prompt asked whether the chart-colour
  invisibility could be made visible, and a counted inventory is the largest
  answer available that does not decide D-41.1-30. §4.2.
- **Committed in:** `a090c79`

### 4. `[Rule 1 — a checkpoint row pointed at the wrong element]` H41-3's outcome row was rewritten to point at the meter

- **Found during:** Task 3
- **Issue:** the plan's row 6 asks whether the two outcomes are tellable apart on
  the **drink sales breakdown**, where they are ink in two separate columns and
  the question is structurally answered. The site D-41.1-29 actually measured is
  the **stacked meter** on the token lifecycle card, where the same two
  semantics are adjacent fills at 1.23 : 1.
- **Fix:** row **T3** points at the meter, states that the fills question is
  already answered *no*, and asks the only question left open — whether the words
  and figures carry it. **The row is written so that it cannot be read as
  implying a colour pair was available.** §4.1, §5.6.
- **Verification:** the two sites read at
  `DrinkSalesBreakdown.tsx:86,94` and `TokenLifecycleCard.tsx:59,65`.

### 5. `[Rule 3 — the environment could not run the build]` `node_modules` was absent

- **Found during:** setup
- **Issue:** the worktree had no `node_modules`, so `npm run build` could not run.
- **Fix:** `npm install` from the existing lockfile — restoring declared
  dependencies, **not adding a package**, so the package-legitimacy checkpoint
  does not apply. It left `package-lock.json` modified as a side effect; that was
  reverted before any commit, and the lockfile is **not** in any commit of this
  plan.
- **Verification:** `git status --short` checked clean of it before every commit.

### 6. `[stated, not a fix]` `npm run verify` exits 2 and cannot exit 0 here

- **Found during:** verification
- **Issue:** the plan's verification asks for exit 0 with no refusal.
  `verify:capabilities` refuses inside a worktree because `.env.local` is
  gitignored.
- **Fix:** **none.** Recorded as unmet at §3.1. No credential was copied in. A
  refusal is not a pass and was not made to look like one.

**Total deviations:** 6. One is a criterion counting a string where it means a
property; one is a trap defused for a later plan; one is declared scope; one is a
checkpoint row corrected against a measurement; one is a lockfile restore; one is
an environment refusal recorded rather than resolved.

---

## 7. Decisions made

1. **The reported entry texts are authoritative and were not edited** — not to
   satisfy a grep, and not to correct an arity. §1.3, §1.4, §1.5.
2. **The fourth breakpoints entry was found by counting on the tree**, not by
   trusting any plan's number, including the number this plan's own text carried.
   §2.4.
3. **The three negative obligations were checked with a parser, not a grep**,
   because a block restructure makes a textual diff say a re-added entry is a new
   one. §2.6.
4. **`/admin/finance` stays undeclared**, and the absence carries its reason in
   the file rather than only in this SUMMARY. §1.4.
5. **D-41.1-30's second half was not answered.** The blind spot is counted; no
   mechanism was built, no constant added, no colour chosen. §4.2.
6. **The hex values are not spelled in `.planning/`**, because a published
   planning document listing four chart colours is one step from being read as
   the palette nobody decided. §4.2.
7. **Every checkpoint row is written as an executable procedure**, and the three
   whose outcome would change a later plan are flagged separately. §5.7.

---

## 8. Deferred

- **`DEF-41-01`** — Tailwind scanning `.planning/`. Untouched, and it governs
  every line of this file: **no utility class is spelled anywhere in it**, in the
  prose or in the tables. Needles are named by what they are — *the small-tier
  prefix*, *the literal table element opener* — never written out.
- **`DEF-41-04`** — whether the typographic measure returns to the format
  catalogue's paragraph and the venue bio. Untouched.
- **`DEF-41-05`** — the skeleton's radius opt-out. Untouched.
- **`D-41.1-30`, both halves** — whether a chart series gets its own scale, and
  whether the gate family should be able to see a colour that never becomes a
  class. **Open, the owner's, and now measured at eight values across three
  files.** §4.2.
- **`D-41.1-25`'s one open case** — *does a result grade a person or report a
  machine?* Row **T3** is the observation that informs it. **Not decided inside a
  reconciliation commit.**
- **The touch-target gate cannot resolve an interpolated floor** — named by plan
  41.1-14 §7.3, unchanged here. It produced a red on a call site that had
  genuinely declared the floor; that plan fixed the element, which is the right
  direction. The matcher's blind spot belongs to whoever next opens `scripts/`
  with a mandate to change a check, and this plan had no such mandate.
- **A capacity figure beside the growth series** — plan 41.1-14 refused to add
  one and wrote the refusal into the page. `community-membership.md` is explicit
  that growth is only meaningful next to how many seats a night has, and the
  target rooms hold 150–300. **That is a product question and it stays one.**

---

## 9. Known stubs

None. No hardcoded empty value, no placeholder string, no `TODO`, no `FIXME`, and
no component receiving mock data. The one list this plan shrank —
`verify-tables.mjs`'s `REMAINING`, now a single entry — is a measurement, and its
single entry is owed by a named plan in a named wave.

---

## 10. Threat register — the five entries this plan owns

| ID | Disposition | What was done |
|---|---|---|
| **T-41.1-59** — a falling `REMAINING` count | mitigate | all 22 entries on both gates re-derived from the tree with a second instrument **before** any deletion, tabulated at §2.1 and §2.2; the two-numbers-by-design property written out at §2.5 so the unmoved live counts cannot be read as work vanishing; tree and SUMMARYs agreed, so no claim had to be refused |
| **T-41.1-60** — a card that lost a column the table has | **NOT mitigated** | H41-3 is deferred. §5.6. Four rows, all `pending`, and **DS-09 has no other evidence.** No gate renders a pixel and this plan does not pretend otherwise |
| **T-41.1-61** — `/admin/finance` declared before its closure is converted | mitigate | deliberately not declared; `grep -c` inside `CONVERTED` returns 0; the held text, its reason, its arity correction and its measured page file sit beside the list, addressed to plan 41.1-24 |
| **T-41.1-62** — the permanent exemption tidied onto a list | mitigate | `ReviewListClient.tsx` asserted absent from every debt list by grep **after** the edits — 3 occurrences in `verify-tables.mjs`, all inside the exemption constants, 0 in `verify-breakpoints.mjs`. §2.6 |
| **T-41.1-SC** — npm installs | mitigate | zero packages proposed or installed; `npm install` restored the existing lockfile and the lockfile is in no commit |

**No threat flags raised.** No new network endpoint, no new auth path, no new
file-access pattern, no schema change at a trust boundary. Two of the five
declared surfaces carry a caution in their own manifest entry — the member-spend
and referral figures as money read but never moved, and the growth surface as
where the community's growth is read — and in both cases the entry states that no
query, column, capability check or action payload moved. **This plan declared
those surfaces; it did not convert them, and it opened none of their files** —
the only product files it read at all were the two it needed to write §4.1's row
accurately, and it wrote neither.

---

## 11. Self-check

Files claimed modified, checked on disk:

```
FOUND: scripts/conversion-manifest.mjs
FOUND: scripts/verify-tables.mjs
FOUND: scripts/verify-breakpoints.mjs
FOUND: scripts/verify-tokens.mjs
```

Commits claimed, checked in `git log`:

```
FOUND: d6df6ea  feat(41.1-16)  five surfaces declared, the sixth held
FOUND: 1da1bfa  chore(41.1-16) fifteen entries deleted, one kept
FOUND: a090c79  docs(41.1-16)  the chart-colour blind spot counted
```

Both scratchpad instruments — the raw-needle diff and the list parser — ran on
copies under the session scratchpad, never inside the repository, and
`git status --porcelain` was checked after each and showed only the intended
edits. No file was created under `src/`. No `git clean`, no `git stash`, no
blanket reset.

## Self-Check: PASSED

---

*Plan 41.1-16. Contains no venue under negotiation, no unannounced date, no
line-up and no personal name: `.planning/` is tracked and this repository is
public. Roles, never people.*
