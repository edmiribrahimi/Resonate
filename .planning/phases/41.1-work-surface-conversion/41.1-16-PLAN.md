---
phase: 41.1-work-surface-conversion
plan: 16
type: execute
wave: 6
depends_on: ["41.1-12", "41.1-13", "41.1-14", "41.1-15"]
files_modified:
  - scripts/conversion-manifest.mjs
  - scripts/verify-tables.mjs
  - scripts/verify-breakpoints.mjs
autonomous: false
requirements: [DS-07, DS-09, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The five analytics surfaces wave 5 converted are declared, and check A walks their closures"
    - "Four of the five tables are off the table gate's debt list, each deleted because the tree changed"
    - "A person has held a phone and read each converted table as cards"
    - "The finance surface is still undeclared, and the reason is written down"
  artifacts:
    - path: "scripts/verify-tables.mjs"
      provides: "the table gate with four entries paid and one remaining"
      contains: "WORK GROUP REMAINING"
  key_links:
    - from: "scripts/conversion-manifest.mjs"
      to: "scripts/verify-conversion.mjs"
      via: "the declared list"
      pattern: "CONVERTED"
---

<objective>
Reconcile wave 5 against the tree: declare the five analytics surfaces, delete the
debt entries the wave actually paid, and put a person in front of every converted
table holding a phone.

Purpose: D-41.1-16 and D-41.1-22. **A debt tracked by a proxy metric is closed by
anything that moves the metric**, so this task diffs entries and never counts —
and it is the single declared owner of every gate edit in wave 5. And **whether a
seven-column table is comprehensible as a card is a comprehension question**: the
table gate reads class strings and import clauses, and its own closing paragraph
says so. That is H41-3, and it is owed here.

Output: five surfaces declared, the table gate down to one entry, and two sets of
human observation rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-VALIDATION.md
@.planning/phases/41.1-work-surface-conversion/41.1-12-SUMMARY.md
@.planning/phases/41.1-work-surface-conversion/41.1-13-SUMMARY.md
@.planning/phases/41.1-work-surface-conversion/41.1-14-SUMMARY.md
@.planning/phases/41.1-work-surface-conversion/41.1-15-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the five analytics surfaces — and deliberately not the sixth</name>
  <files>scripts/conversion-manifest.mjs</files>
  <read_first>
    - `scripts/conversion-manifest.mjs` — `CONVERTED` as left by plan 41.1-11, with its eighteen entries
    - `scripts/conversion-manifest.mjs` lines 417-422 — `checkManifest()` and the width comparison
    - The four wave-5 SUMMARYs, for the entry texts they reported — including plan 41.1-15's **held** entry for the finance surface
    - `.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md` §2.7 — the three things every reason must carry
  </read_first>
  <action>
Add five `CONVERTED` entries, taking each text from the plan that reported it:
`/admin/analytics/members` **wide** · `/admin/events/[id]/analytics` **wide** ·
`/admin/analytics` **wide** · `/admin/analytics/compare` **wide** ·
`/admin/members/growth` **wide**. All five are named on the closed wide list, so
`checkManifest()` must raise no width disagreement.

Each reason carries the plan number and what *whole* turned out to mean; the width
with the sentence that it is on the closed list; and the no-behaviour-change
sentence. Carry forward the cautions the plans reported: the client-boundary
crossing on the referral chain, the per-chart colour sources, and the refusal to
add a capacity figure to the growth surface.

**`/admin/finance` is deliberately not added.** Check A walks through the
transaction list into `RefundDialog.tsx`, which plan 41.1-17 converts in wave 7,
so declaring it now would fail on that file's palette hits — correctly. Put the
held entry text into a comment beside `CONVERTED`, marked as held for plan
41.1-24, with its one-line reason. **An absence with a written reason is a
decision; an absence without one is read as an oversight**, and this manifest
already carries the neighbouring precedent for refusing a claim in writing.
  </action>
  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-conversion.mjs` exits 0 and reports **23 surfaces**
      (18 + 5), all five checks passing
    - `checkManifest()` raises no width disagreement
    - `grep -c "\"/admin/finance\"" scripts/conversion-manifest.mjs` returns 0
      inside `CONVERTED`, and the held entry appears in a comment naming plan
      41.1-24 and its reason
    - `grep -c "no query changed" scripts/conversion-manifest.mjs` returns at
      least 17
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Five surfaces declared, one deliberately held with its reason in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Delete only what was paid — thirty entries diffed against the tree</name>
  <files>scripts/verify-tables.mjs, scripts/verify-breakpoints.mjs</files>
  <read_first>
    - `scripts/verify-tables.mjs` — `REMAINING` as left by plans 41.1-03 and 41.1-11, five entries, all tagged `work`
    - `scripts/verify-tables.mjs` lines 424-430 — the model comment for a **paid** entry: which plan and task paid it, what the number went from and to, and that nothing else moved
    - `scripts/verify-tables.mjs` lines 355-380 — the permanent exemption, which is **not** on `REMAINING` and must stay that way
    - `scripts/verify-breakpoints.mjs` — `REMAINING` as left by plan 41.1-11, seventeen entries
    - `scripts/verify-breakpoints.mjs` lines 196-217 — the four rules, including that a lowered count is `STALE` and never a failure
    - The four wave-5 SUMMARYs, for the entries each reported emptied
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §9 P1
  </read_first>
  <action>
**Diff entries, not counts.** For every entry on both lists, re-derive from the
tree whether it is still owed by grepping the named file for the needle the gate
counts. Produce a three-column table — entry, gate's verdict, what the tree says
— covering **all** entries on both gates, and put it in the SUMMARY. Only then
delete.

Wave 5 is expected to pay four table entries — the member-spend table, the
referral chain, the drink sales breakdown and the finance transaction list —
leaving **one**, the sales dashboard, which plan 41.1-21 converts in wave 8. On
the breakpoints gate it is expected to pay every remaining work entry **except**
the sales dashboard.

**Those are expectations, not instructions.** If the tree and a SUMMARY disagree,
**the tree wins and the disagreement is recorded** with the plan that claimed it.
That is the entire reason this task exists: four distinct mechanisms in this
phase family have already made a count fall for the wrong reason.

Leave a comment where each deleted entry was, naming the plan and task that paid
it and what the numbers went from and to.

**Three negative obligations.** No entry is added; no entry is moved between
groups; and the permanently exempt diagnostic grid is **not** tidied onto any
list. Assert its absence by grep after the edit.
  </action>
  <verify>
    <automated>node scripts/verify-tables.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-dialogs.mjs && npm run verify; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-tables.mjs` exits 0 with `REMAINING = 1` and
      `WORK GROUP REMAINING = 1`, the surviving entry being the sales dashboard —
      or, if the tree disagrees, the actual number with the disagreement recorded
      entry by entry
    - `node scripts/verify-breakpoints.mjs` exits 0 with `WORK GROUP REMAINING = 1`
      and the five non-work entries intact
    - `node scripts/verify-dialogs.mjs` exits 0, still `REMAINING = 13` — wave 5
      paid no dialog entry
    - The three-column entry diff table covers every entry on both gates
    - Each deletion left a comment naming the plan and task that paid it
    - `grep -n "ReviewListClient" scripts/verify-tables.mjs` shows it only in the
      exemption constants
    - `npm run verify` exits 0 with no refusal; `npm run build` exits 0
  </acceptance_criteria>
  <done>Four table entries and every remaining work breakpoint entry deleted on the tree's authority, with a comment where each was.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: H41-1 and H41-3 for wave 5 — five surfaces at three widths, and four tables read as cards on a phone</name>
  <action>
Stop here. This is a **blocking** checkpoint: nothing in this plan or in the next
wave proceeds until a person has performed the observation below and recorded its
rows. `41.1-VALIDATION.md` states the rule — **an unticked row is the correct
state until a person has looked**, and a row inferred from a neighbouring one is
not an observation. D-41.1-18 puts this per wave rather than accumulated to the
end, because a batch looked at once, at the end, is a batch that gets skipped.
Present the steps below verbatim, collect the answers, and write them into the
SUMMARY as rows — ticked, or `pending` with the reason it could not be performed.
  </action>
  <read_first>
    - `.planning/phases/41.1-work-surface-conversion/41.1-VALIDATION.md` — the six manual observations; an unticked row is the correct state until a person has looked
    - `.planning/phases/41.1-work-surface-conversion/41.1-15-SUMMARY.md` — the pagination procedure that was recorded, so it is not repeated blind
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-CR01-PASS.md` — the row format
  </read_first>
  <what-built>
Five analytics surfaces converted onto the wide shell with their placeholders, and
four of the five tables moved onto one column declaration each: the member-spend
table, the referral chain with its disclosure now the expansion apparatus, the
drink sales breakdown with its semantic tokens, and the finance transaction list
with its two renderings sharing a declaration.
  </what-built>
  <how-to-verify>
**H41-1 — five surfaces, three widths.** At **390px, 768px and 1440px**, on
`/admin/analytics`, `/admin/analytics/compare`, `/admin/analytics/members`,
`/admin/events/[id]/analytics` and `/admin/members/growth`, plus
`/admin/finance` even though it is not yet declared:

1. Nothing scrolls sideways; nothing sits under the navigation column.
2. **No grid shows three columns at 768px.** That is the specific failure a bare
   prefix rename produces, and it is why every grid rule was mapped per class.
3. Reload each with the network throttled and watch for a sideways jump when the
   data lands — a jump means a placeholder's maximum does not match its page's.

**H41-3 — four tables, on a phone, held in a hand.** This is the observation no
script can make: whether the card carries the facts that mattered.

4. **Member-spend table** — seven columns as a card. Can you tell, at a glance,
   who this row is and what they spent? Is the rank still legible? Is the total
   the thing your eye lands on, or is it lost among the smaller figures?
5. **Referral chain** — open a disclosure on the phone. Does the nested list read,
   or does it push the row's own figures off the screen?
6. **Drink sales breakdown** — redeemed and refunded now differ by a semantic
   token rather than by a raw hue. **Can you still tell them apart**, and does the
   label do the work if you look away from the colour?
7. **Finance transaction list** — is the **amount** the first thing you read on a
   card? D-41.1-13 exists for this row and only a person can settle it. Then open
   a row's detail, and page forward and back once.

Record one row per surface per width for H41-1, and one row per table for H41-3.
Leave a row unticked rather than inferring it.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded rows, or name the table and the fact that got lost.</resume-signal>
  <done>Every row below is recorded in the SUMMARY — ticked, or left `pending` with its reason. None is assumed, and none is inferred from another row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debt list → reader | a deleted entry is a claim that work happened |
| card branch → an operator's first read | on a money surface the card's first line decides what is read first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-59 | Repudiation | a falling `REMAINING` count | mitigate | every entry on both gates is re-derived from the tree and tabulated before any deletion; the tree wins over a SUMMARY and the disagreement is recorded |
| T-41.1-60 | Repudiation | a card that lost a column the table has | mitigate | H41-3 in task 3, per table, on a phone — the gate reads class strings and cannot answer a comprehension question |
| T-41.1-61 | Repudiation | `/admin/finance` declared before its closure is converted | mitigate | it is deliberately not declared, and the held entry with its reason sits beside the list |
| T-41.1-62 | Tampering | the permanent exemption tidied onto a list | mitigate | asserted absent by grep after the edit |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run verify` exits 0 with no refusal; `npm run build` exits 0
- `verify-conversion` reports 23 surfaces; `verify-tables` 1; `verify-dialogs` 13
- The full entry diff table is recorded
- Task 3's H41-1 and H41-3 rows recorded, ticked or `pending`, never assumed
</verification>

<success_criteria>
Five surfaces declared, one held with a written reason, four of the five tables
off the debt list on the tree's authority, and a person has read each converted
table as cards on a phone and said whether the facts that mattered survived.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-16-SUMMARY.md` when done,
with the entry diff table, all four gates' output, and task 3's two sets of rows.
</output>
