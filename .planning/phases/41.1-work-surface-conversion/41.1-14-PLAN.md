---
phase: 41.1-work-surface-conversion
plan: 14
type: execute
wave: 5
depends_on: ["41.1-11"]
files_modified:
  - src/app/(admin)/admin/(work)/analytics/page.tsx
  - src/app/(admin)/admin/(work)/analytics/loading.tsx
  - src/app/(admin)/admin/(work)/analytics/compare/page.tsx
  - src/app/(admin)/admin/(work)/analytics/compare/loading.tsx
  - src/app/(admin)/admin/(work)/members/growth/page.tsx
  - src/app/(admin)/admin/(work)/members/growth/loading.tsx
  - src/components/analytics/KPIDashboard.tsx
  - src/components/analytics/RecentActivityFeed.tsx
  - src/components/analytics/EventSelector.tsx
  - src/components/analytics/EventComparisonChart.tsx
  - src/components/analytics/MemberGrowthChart.tsx
  - src/components/analytics/GrowthSummaryCard.tsx
autonomous: true
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The three analytics dashboards render inside the page shell at the wide maximum"
    - "Each of their three placeholders renders the same maximum as the page it precedes"
    - "No small-prefix breakpoint survives in the six analytics components or the three placeholders"
  artifacts:
    - path: "src/components/analytics/KPIDashboard.tsx"
      provides: "the KPI grid, on the tier boundaries this system has"
    - path: "src/app/(admin)/admin/(work)/analytics/loading.tsx"
      provides: "the analytics placeholder, on Skeleton and the wide shell"
      contains: "PageShell"
  key_links:
    - from: "src/app/(admin)/admin/(work)/analytics/page.tsx"
      to: "src/components/ui/PageShell.tsx"
      via: "the shell, at the wide maximum"
      pattern: "PageShell width=\"wide\""
---

<objective>
Convert the three dashboard surfaces that carry no table: the analytics overview,
the event comparison, and the member growth page — with their six components and
their three placeholders.

Purpose: these three carry **six of the work group's twenty-one small-prefix uses
and eight of its route files' legacy-token debt**, and every one of those uses is
a grid rule. They are the surfaces where pitfall P6 bites hardest: renaming the
prefix rather than reading the class gives a tablet three columns in 544px.

Output: three surfaces converted whole, three placeholders adopted, six
breakpoint entries paid, and the entries reported for wave 6's reconciliation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). Report the entry
   texts and the `REMAINING` entries emptied.
2. **No behaviour change:** *"no query changed, no column added, no capability
   check touched, no action payload altered"*. Grep and paste.
3. **Never spell a Tailwind class string inside a comment.**
4. **`sm:` is a map, not a rename.** This plan's whole risk is here.
5. **`FOCUS_RING` is imported**, never re-spelled.
6. **Stop condition 3 applies.** These surfaces read money and move none.
7. **A chart's colour is read, not scanned.** A colour set in an inline style or a
   constant is invisible to a class-string gate. If a chart carries a hard-coded
   colour belonging to a format's identity or to the sunset gradient, **halt and
   report** — that is `brand-visual-system.md`, not a conversion.
8. **`.planning/` is public.** No real figure, member, venue or date in any SUMMARY.
9. **The authorized file list is closed, and a file outside it is a halt**
   (D-41.1-19, standing rule, 2026-08-13). This phase converts *whole* closures, so
   an execution can walk into a file nobody planned for. If a closure this plan
   declares reaches a file that is neither in this plan's `files_modified`, nor
   already converted, nor on D-41.1-19's authorized list — `EventForm.tsx`,
   `RefundDialog.tsx`, `RefundActions.tsx`, `VenueRevealPanel.tsx`,
   `RevealVenueDialog.tsx`, `EventQRCode.tsx` — **stop and report it. Do not
   convert it, and do not conclude that it is safe.** The list grows **by owner
   decision or not at all**, however small the file and however well measured. The
   sixth entry is the precedent: it was measured, the measurement was right, and
   the inclusion was still refused until the owner decided it. Record the file, its
   measurements and the surface whose closure reaches it in the SUMMARY as a
   question owed to the owner, in the same shape the knot plans use for their stop
   conditions.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: /admin/analytics — the KPI grid and the activity feed, with every grid rule mapped per class</name>
  <files>src/app/(admin)/admin/(work)/analytics/page.tsx, src/app/(admin)/admin/(work)/analytics/loading.tsx, src/components/analytics/KPIDashboard.tsx, src/components/analytics/RecentActivityFeed.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/analytics/page.tsx` — the route file; 48 legacy hits across its closure and no palette
    - `src/app/(admin)/admin/(work)/analytics/loading.tsx` — 73 lines, **30 legacy hits, 8 pulse blocks, two small-prefix uses**
    - `src/components/analytics/KPIDashboard.tsx` — 51 lines, 10 class attributes; a named entry on the breakpoints debt list
    - `src/components/analytics/RecentActivityFeed.tsx` — 76 lines, 13 class attributes
    - **The page analog, partial match:** `src/app/(admin)/admin/(work)/members/page.tsx` line 159 — the wide shell call and the four deletions its diff shows. It is *partial* because that page's primary object is a table and this one's is a KPI grid; take the shell adoption from it, not the layout
    - **The placeholder analog, exact match:** `src/app/(admin)/admin/(work)/members/loading.tsx`
    - `src/components/ui/Skeleton.tsx` lines 19-27, 20-28, 29-34, 44-53
    - `src/components/ui/Card.tsx` line 47; `src/components/ui/Typography.tsx` lines 53 and 116
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §2.1 (the two tier boundaries and why they are Tailwind's defaults), **§2.3 (the per-class map — the section this task is built on)**, §4 (this surface **is** on the closed wide list)
  </read_first>
  <action>
The page is **`wide`** — named on §4's closed list; so is its placeholder. Apply
the four deletions from the shell-adoption pattern to the page.

**Every grid rule is read per class.** `41-UI-SPEC.md` §2.3 is a map, not a rename
table: a three-column grid declared from the small prefix becomes **two columns at
`md` and three at `lg`**, gaining the middle step it skipped. Renaming the prefix
gives a 768px tablet three columns in 544px, which is pitfall P6 and the exact
failure this phase exists to stop repeating. Produce a before/after row for every
class that moves — there are two in the placeholder and at least one in the KPI
grid — and put the table in the SUMMARY so a reviewer can check the map was read
rather than the prefix swapped.

`KPIDashboard.tsx` and `RecentActivityFeed.tsx` take `Card` where they are
bordered rounded containers, `SectionHeading` or the one written-out class string
where they carry a section heading, and the type scale everywhere — there is no
weight-500 step in this system.

The placeholder adopts `PageShell` at `wide` and `Skeleton` for every shape, and
carries the three constraints: the width equals the page's; every count is a
literal that stands for nothing, because no query runs before a loading file
renders; and no radius is appended to a skeleton line.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-breakpoints.mjs && node scripts/verify-tokens.mjs && node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <page>` and `<loading>` each return at least 1 and
      **both pass `width="wide"`** — state the matched pair
    - `grep -c "sm:" <each of the four files>` returns 0
    - The before/after class table is in the SUMMARY, one row per moved class, and
      **no row is a bare prefix rename** — every three-column rule gains a middle
      step
    - `grep -c "animate-pulse" <loading>` returns 0 and the skeleton import is
      present
    - Across the four files: zero raw palette utilities, zero legacy token
      utilities, zero `focus:outline-none`, zero `font-medium`, zero `max-w-`
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The analytics overview and its placeholder are on the wide shell, and every grid rule gained the step it was missing.</done>
</task>

<task type="auto">
  <name>Task 2: /admin/analytics/compare — the selector and the comparison chart</name>
  <files>src/app/(admin)/admin/(work)/analytics/compare/page.tsx, src/app/(admin)/admin/(work)/analytics/compare/loading.tsx, src/components/analytics/EventSelector.tsx, src/components/analytics/EventComparisonChart.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/analytics/compare/page.tsx` — the route file
    - `src/app/(admin)/admin/(work)/analytics/compare/loading.tsx` — 51 lines, **15 legacy hits, 6 pulse blocks**
    - `src/components/analytics/EventSelector.tsx` — 105 lines, 10 class attributes; it is the interactive control on this surface
    - `src/components/analytics/EventComparisonChart.tsx` — 135 lines, 4 class attributes; **a chart, so rule 7 applies**
    - **Analogs:** `src/app/(admin)/admin/(work)/members/page.tsx` line 159 (shell, wide) and `src/app/(admin)/admin/(work)/members/loading.tsx` (placeholder)
    - `src/components/ui/Input.tsx` lines 216, 247, 279 — the field boundary token and its measured contrast; `src/components/ui/Chip.tsx` line 119 — the interactive pill at the floor
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 (on the closed wide list), §6.1, §8.6
  </read_first>
  <action>
The page is **`wide`**; so is its placeholder. Apply the four deletions.

`EventSelector.tsx` is the one interactive control here and it is what a person
actually touches on this surface: every select, every field and every pill takes
its primitive, and each reaches the 44px floor. An event selector that is hard to
hit on a tablet is the whole surface being hard to use, because nothing else on
the page is interactive.

`EventComparisonChart.tsx` is a chart: **read it**, do not scan it. Record in the
SUMMARY what its colour source is — a design token, a semantic token, or a
hard-coded value — and if a hard-coded value belongs to a format's identity or to
the sunset gradient, halt and report rather than substituting. A format
identification colour is data on a row and never a CSS token, and the gradient is
SunSet's alone.

The placeholder adopts the shell at `wide` and the skeleton for all six shapes,
with the three constraints intact.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-sunset-gradient.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <page>` and `<loading>` each return at least 1 with
      `width="wide"` — state the matched pair
    - `grep -c "animate-pulse" <loading>` returns 0
    - Across the four files: zero raw palette utilities, zero legacy token
      utilities, zero `focus:outline-none`, zero `font-medium`, zero `max-w-`,
      zero `sm:`
    - Every interactive element in `EventSelector.tsx` is a primitive or declares
      the floor; `node scripts/verify-touch-targets.mjs` exits 0 and the ledger is
      pasted in
    - The chart's colour source is recorded in the SUMMARY
    - `node scripts/verify-sunset-gradient.mjs` exits 0
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The comparison surface is on the wide shell, its selector reaches the floor, and its chart's colour source is written down.</done>
</task>

<task type="auto">
  <name>Task 3: /admin/members/growth — the growth chart, the summary card, and the pulse-heaviest placeholder</name>
  <files>src/app/(admin)/admin/(work)/members/growth/page.tsx, src/app/(admin)/admin/(work)/members/growth/loading.tsx, src/components/analytics/MemberGrowthChart.tsx, src/components/analytics/GrowthSummaryCard.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/members/growth/page.tsx` — the route file
    - `src/app/(admin)/admin/(work)/members/growth/loading.tsx` — 43 lines, **15 legacy hits and 10 hand-rolled pulse blocks**, the highest pulse count of the seven
    - `src/components/analytics/MemberGrowthChart.tsx` — 68 lines, 2 class attributes; **a chart, so rule 7 applies**
    - `src/components/analytics/GrowthSummaryCard.tsx` — 39 lines, 9 class attributes
    - **Analogs:** `src/app/(admin)/admin/(work)/members/page.tsx` line 159 and `src/app/(admin)/admin/(work)/members/loading.tsx`
    - `src/components/ui/Skeleton.tsx` — its four exports and the four docblock passages
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 (on the closed wide list)
    - `.claude/rules/community-membership.md` — because this surface is where growth is read, and the module's own gate says growth is looked at **next to how many seats a night actually has**
  </read_first>
  <action>
The page is **`wide`**; so is its placeholder. Apply the four deletions.

`GrowthSummaryCard.tsx` takes `Card` and the type scale.
`MemberGrowthChart.tsx` is a chart: read it, record its colour source, and halt
rather than substitute if it carries an identity colour.

The placeholder is the pulse-heaviest of the seven — ten hand-rolled blocks —
and every one is deleted in favour of the skeleton primitive. Its fill is
deliberately near-invisible against the surface, because a placeholder must read
as absent content and not as content; a line colour used as a ground is the
category error the primitive's docblock records, and that is exactly what these
ten blocks do today.

**One thing this conversion deliberately does not do.** The growth surface shows
a number rising; the community module's own gate says growth is only meaningful
next to how many seats a night has, because a member who never gets in is an ex
member. **That is a product question, not a conversion**, and this plan does not
add a capacity figure, a ratio or a caption implying one. If it is worth raising,
it is raised as a finding in the SUMMARY.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-conversion.mjs && node scripts/verify-sunset-gradient.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <page>` and `<loading>` each return at least 1 with
      `width="wide"` — state the matched pair
    - `grep -c "animate-pulse" <loading>` returns 0 and the skeleton import is
      present
    - `grep -cE "\b(bg|text|border|divide)-(card-border|card|background|foreground)\b" <loading>`
      returns 0 — all 15 hits gone
    - Across the four files: zero raw palette utilities, zero legacy token
      utilities, zero `focus:outline-none`, zero `font-medium`, zero `max-w-`,
      zero `sm:`
    - The chart's colour source is recorded
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The SUMMARY reports the three `CONVERTED` entry texts and every `REMAINING`
      entry emptied — six on the breakpoints gate across the three surfaces
    - The behaviour-change grep is clean, and no capacity figure or ratio was
      added to the growth surface
  </acceptance_criteria>
  <done>The growth surface is on the wide shell, its ten pulse blocks are gone, and no product claim was added under cover of a conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart → colour source | a colour set in an inline style is invisible to a class-string gate |
| dashboard → a product claim | a caption added during a conversion is a claim nobody reviewed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-49 | Information disclosure | a format identity colour or the sunset gradient hard-coded in a chart | mitigate | every chart is read, its colour source recorded, and an identity colour is a halt rather than a substitution (`brand-visual-system.md`) |
| T-41.1-50 | Denial of service | a three-column grid at 544px on a tablet | mitigate | every grid rule is mapped per class with a before/after row; a bare prefix rename is rejected in the acceptance criteria |
| T-41.1-51 | Repudiation | a product claim added inside a conversion commit | mitigate | task 3 states the one temptation by name and refuses it; findings go in the SUMMARY, not into the surface |
| T-41.1-52 | Information disclosure | planning material in a public repository | mitigate | no real figure, member, venue or date in any SUMMARY |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run verify` exits 0 with no refusal
- Three matched width pairs recorded (page and placeholder)
- The before/after grid-class table recorded, with no bare prefix rename
- Three chart colour sources recorded
</verification>

<success_criteria>
Three dashboard surfaces and their three placeholders on the wide shell, twenty-four
hand-rolled pulse blocks replaced by the primitive, every grid rule gaining the
middle step it skipped, and every chart's colour source written down.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-14-SUMMARY.md` when done,
with the three matched width pairs, the before/after grid-class table, the three
chart colour sources, the behaviour-change grep, the three `CONVERTED` entry
texts, and the `REMAINING` entries emptied.
</output>
