---
phase: 41.1-work-surface-conversion
plan: 21
type: execute
wave: 8
depends_on: ["41.1-17"]
files_modified:
  - src/app/(admin)/admin/(work)/events/[id]/sales/page.tsx
  - src/components/events/SalesDashboard.tsx
autonomous: false
requirements: [DS-07, DS-09, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The buyer table reads as cards on a phone from one column declaration"
    - "The dashboard's three empty states stay three, and a filtered-to-nothing table does not claim there were no sales"
    - "The search still filters, and no status transition, refund amount, idempotency key or webhook path changed"
  artifacts:
    - path: "src/components/events/SalesDashboard.tsx"
      provides: "the event sales dashboard, with its buyer table on DataTable"
      contains: "DataColumn"
  key_links:
    - from: "src/components/events/SalesDashboard.tsx"
      to: "src/app/(admin)/admin/events/[id]/tickets/RefundActions.tsx"
      via: "the row actions apparatus, converted as spine in wave 7"
      pattern: "RefundActions"
---

<objective>
Convert `/admin/events/[id]/sales` and the sales dashboard — the fifth and last of
the five tables.

Purpose: DS-09's final entry, and the one place where D-41.1-13 is satisfied by
the **surface** rather than by the row: the money figure on this dashboard is the
event total, which is a card above the table and not a column, so the table's
`mark` is the tier pill. That is the one instance of the rule being met somewhere
other than where it is written, and the plan says so rather than leaving a reader
to wonder whether the rule was skipped.

This plan runs after the refund control was converted as spine in wave 7, which is
**why it is a separate plan from the tickets surface** instead of merged with it:
the shared file was taken out of the partition problem rather than around it.

Output: the last table paid, one surface converted, and the entries reported for
the final reconciliation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-work-surface-conversion/41.1-CONTEXT.md
@.planning/phases/41.1-work-surface-conversion/41.1-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<impact_analysis>
## Written before the diff — this surface renders a refund control

**Classification: Structured, at the boundary of Critical.** The dashboard reads
an event's money and hosts the per-row refund control. It does not move money: the
control itself was converted in plan 41.1-17 with its own impact analysis and is
**not opened here**.

**Domain:** `ticketing-payments` primary; `nextjs-architecture` secondary.

**What is opened:** the sales route file and `SalesDashboard.tsx` (297 lines, 63
class attributes). **What is not:** `RefundActions.tsx`, the refund server action,
the two refund emails, the SumUp webhook and every cron.

## The three stop conditions, verbatim

1. **`venue_reveal_sent` is monotone.** Nothing in this closure reaches the
   reveal; asserted by grep, not assumed.
2. **A payment reaching `completed` corrects forward.** **No conversion changes a
   status transition, a refund amount, an idempotency key or a webhook path.** A
   plan that finds it cannot convert without doing so **halts and returns to the
   owner.**
3. **A series progressivo is already on a poster.** No conversion renumbers,
   re-sorts or re-derives one — including the tier ordering, which is what a buyer
   chose and not a display preference.
</impact_analysis>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22).
2. **No behaviour change:** grep the diff for `select(`, `CAP.`, `.eq(`,
   `.filter(`, `capabilities.has`, `redirect(`, `status`, `refund`, `amount`,
   `idempot`, `webhook`, and quote every match with a sentence.
3. **Never spell a Tailwind class string inside a comment.**
4. **`sm:` is a map, not a rename.**
5. **`FOCUS_RING` is imported**, never re-spelled.
6. **The copy does not change.** §11: *none introduced*.
7. **`.planning/` is public.** No buyer, email address, amount or date in the
   SUMMARY.
8. **The authorized file list is closed, and a file outside it is a halt**
   (D-41.1-19, standing rule, 2026-08-13). This phase converts *whole* closures, so
   an execution can walk into a file nobody planned for. If a closure this plan
   declares reaches a file that is neither in this plan's `files_modified`, nor
   already converted, nor on D-41.1-19's authorized list — `EventForm.tsx`,
   `RefundDialog.tsx`, `RefundActions.tsx`, `VenueRevealPanel.tsx`,
   `RevealVenueDialog.tsx`, `EventQRCode.tsx` — **stop and report it. Do not
   convert it, and do not conclude that it is safe.** The list grows **by owner
   decision or not at all**, however small the file and however well measured. The
   sixth entry is the precedent: it was measured, the measurement was right, and
   the inclusion was still refused until the owner decided it. Record the file, its
   measurements and the surface whose closure reaches it in the SUMMARY as a
   question owed to the owner, in the same shape the knot plans use for their stop
   conditions.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: The buyer table onto DataTable — six columns, three empty states, and a search that still filters</name>
  <files>src/components/events/SalesDashboard.tsx</files>
  <read_first>
    - `src/components/events/SalesDashboard.tsx` — 297 lines, 63 class attributes, in full. Four regions: a revenue summary card, a per-tier breakdown with a progress bar, the buyer table, and a discount-code summary. **Only the third is the primitive's business.** Lines 44-52 (the date formatter), 62-72 (the client-side search over name, email and tier — it filters the rows **before** the primitive sees them), 110 (a sold-out pill on a raw red family), 133 (a progress bar on a raw red family), 143-244 (the buyer table), 154 (a search input with its own focus suppression, at a height below the floor), 159-166 (**the three empty states**), 199 and 236 (discount pills on a raw green family)
    - **The analog, exact match:** `src/components/admin/MemberTable.tsx` lines 1052-1084 and 1394-1442
    - `src/components/ui/DataTable.tsx` — lines 77-79 (filtering and search are declined by name and stay at the call site), lines 185-187 (**one empty node, one caption** — the caller chooses which node to pass, per render), lines 166 and 361 (the actions apparatus and the `dense` argument, true only in the table branch), lines 408-460
    - `src/app/(admin)/admin/events/[id]/tickets/RefundActions.tsx` — as converted in plan 41.1-17; the actions apparatus renders it
    - `.planning/phases/41.1-work-surface-conversion/41.1-RESEARCH.md` §2.2 gap G-d, §2.3's entry for this file, §2.4 and its note about where the money `mark` lives here
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §5, §6.1, §8.8, §8.11
  </read_first>
  <action>
Replace the buyer table's hand-written branches with one column array and one
call. **The other three regions stay hand-written and are converted as ordinary
markup** — the revenue card becomes a `Card`, the tier breakdown keeps its
structure with its progress bar recoloured onto tokens, the discount summary
becomes a `Card`.

**The slot assignment**, settled here:

| Column | Slot |
|---|---|
| buyer name | `title` |
| email | `subtitle` |
| tier | `mark` — already a pill, and it fits the slot as rendered |
| discount | `meta` — conditional; the cell returns nothing when there is none |
| date | `meta` |
| the refund control | the actions apparatus, not a column |

**Where D-41.1-13 is met.** The figure that decides money on this surface is the
event total, which is a card **above** the table and not a column. So the table's
`mark` is the tier pill, and the rule is satisfied by the surface rather than by
the row. **State that in the file and in the SUMMARY** — it is the one place in
the phase where the rule is met somewhere other than where it is written, and an
unexplained tier-as-mark reads like the rule was skipped.

**Gap G-d: three empty states must stay three.** No tiers, no buyers, and **no
results for a search** — and the third is the one that matters, because a
filtered-to-nothing table saying *no tickets sold yet* is a lie about the data.
The primitive takes one empty node per render, so the caller chooses which to pass
based on which condition holds. **Write that selection explicitly**; today the
distinction lives in the markup and it will be lost if it is not carried
deliberately.

The search stays at the call site — the primitive declines filtering by name —
and its input becomes the field primitive, which raises it above the touch floor
and removes its hand-written focus suppression.

Four raw palette hits go: the sold-out pill, the progress bar, and the two
discount pills. They take semantic tokens, not nearer hues, and **colour is never
the only channel** — each already carries a word, and with the hue carrying less
weight that word is now the primary distinction.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tables.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-semantic-separation.mjs && node scripts/verify-breakpoints.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from \"@/components/ui/DataTable\"" src/components/events/SalesDashboard.tsx`
      returns 1; `grep -c "<table" <the file>` returns 0
    - The column array is pasted into the SUMMARY; every column names a slot; the
      tier's is `mark` and the file carries the sentence explaining where the money
      figure lives instead
    - **The three empty states are three**: quote the selection logic into the
      SUMMARY and name which node is passed under which condition. In particular,
      a search that matches nothing must not render the no-sales wording
    - `grep -cE "(bg|text|border)-(red|green)-[0-9]" <the file>` returns 0
    - `grep -c "focus:outline-none" <the file>` returns 0;
      `grep -c "sm:" <the file>` returns 0
    - Zero legacy token utilities, zero `font-medium`, zero `max-w-`
    - **The money grep is quoted line by line** for the five terms
    - `grep -rn "venue_reveal_sent\|RevealVenueDialog" <the file>` returns nothing
    - `node scripts/verify-tables.mjs` exits 0 and still reports `REMAINING = 1` —
      the last entry is paid in the tree and reported, not deleted here
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The last table is one column declaration, its three empty states are still three, and the money rule's one exception is written down.</done>
</task>

<task type="auto">
  <name>Task 2: The sales page onto the wide shell</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/sales/page.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/sales/page.tsx` — the route file; its only local import is the dashboard
    - **The analog:** `src/app/(admin)/admin/(work)/members/page.tsx` line 159 — the wide shell call and the four deletions its diff shows
    - `src/components/ui/Typography.tsx` line 53
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 — this surface **is** on the closed wide list
  </read_first>
  <action>
The page is **`wide`** — named on §4's closed list. Apply the four deletions: the
page root with its minimum height and hard-coded bottom padding; the gutter and
top rhythm on the header and every repeated gutter on inner sections; the header's
bottom padding, which becomes a margin; the hand-written top-level heading, which
becomes `PageTitle`.

There is no loading file for this route today, so there is none to convert; say so
in the SUMMARY rather than leaving the absence to be checked twice.

Report the `CONVERTED` entry text. The declaration is made by the final
reconciliation, plan 41.1-24 — this plan writes nothing under `scripts/`.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-conversion.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PageShell" <the page file>` returns at least 1 with `width="wide"`
    - Zero raw palette utilities, zero legacy token utilities, zero
      `focus:outline-none`, zero `font-medium`, zero `max-w-`, zero `sm:`
    - `ls "src/app/(admin)/admin/(work)/events/[id]/sales/"` is recorded in the
      SUMMARY, showing whether a loading file exists
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
    - The SUMMARY reports the `CONVERTED` entry text and the one table
      `REMAINING` entry emptied
  </acceptance_criteria>
  <done>The sales surface is on the wide shell and its entry is reported for the final reconciliation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The dashboard on a phone — the three empty states, and the refund control still reachable</name>
  <action>
Stop here. This is a **blocking** checkpoint: nothing in this plan or in the next
wave proceeds until a person has performed the observation below and recorded its
rows. `41.1-VALIDATION.md` states the rule — **an unticked row is the correct
state until a person has looked**, and a row inferred from a neighbouring one is
not an observation. D-41.1-18 puts this per wave rather than accumulated to the
end, because a batch looked at once, at the end, is a batch that gets skipped.
Present the steps below verbatim, collect the answers, and write them into the
SUMMARY as rows — ticked, or `pending` with the reason it could not be performed.
  </action>
  <read_first>
    - `.planning/phases/41.1-work-surface-conversion/41.1-VALIDATION.md` — H41-3 and the manual row for a refund control's confirmation
    - `.planning/phases/41.1-work-surface-conversion/41.1-17-SUMMARY.md` — the refund observation already recorded, so it is not repeated blind
  </read_first>
  <what-built>
The event sales dashboard: the buyer table now reads as cards on a phone from one
column declaration, its search still filters at the call site, its three empty
states are still three, and its per-row refund control is the one converted in
wave 7.
  </what-built>
  <how-to-verify>
At **390px, 768px and 1440px** on `/admin/events/{an event}/sales`:

1. Nothing scrolls sideways; nothing sits under the navigation column.
2. **H41-3, on a phone:** read a buyer card. Can you tell who bought, on which
   tier, and when — at a glance? Is anything you would have wanted missing?
3. **Type a search term that matches nothing.** The empty state must say the
   search found nothing, **not** that no tickets have been sold. That is the one
   of the three that matters, and only a person can see which sentence appeared.
4. **Look at an event with no tiers**, and one with tiers but no buyers, if such
   an event exists. Each must show its own sentence. If neither exists, record the
   step as `pending` with the reason rather than approximating it.
5. **The sold-out pill and the discount pill** now use semantic tokens rather than
   raw hues. Can you still tell them apart, and does the word do the work if you
   look away from the colour?
6. **Open the refund control from a row** and confirm it still opens and that
   Cancel is focused. **Do not perform a refund.** If observing further would
   require a real refund, record the step as `pending`.

Record one row per step per width where widths apply.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded rows, or name which empty state appeared where the wrong one was expected.</resume-signal>
  <done>Every row below is recorded in the SUMMARY — ticked, or left `pending` with its reason. None is assumed, and none is inferred from another row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard row → refund control | a row hosts a control that moves money out |
| empty state → a claim about data | a filtered-to-nothing table saying "no sales" is a false statement about the night |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-88 | Tampering | a status transition, refund amount, idempotency key or webhook path | mitigate | stop condition 2 verbatim; the diff is filtered for all five terms and quoted line by line; the refund control itself is not opened by this plan |
| T-41.1-89 | Repudiation | the search empty state collapsed into the no-sales one | mitigate | gap G-d handled explicitly: the selection logic is quoted and a person types a non-matching search in task 3 step 3 |
| T-41.1-90 | Repudiation | a card that drops a fact the table had | mitigate | one column declaration with every slot named, plus H41-3 on a phone |
| T-41.1-91 | Repudiation | sold-out and discount distinguished only by hue | mitigate | semantic tokens plus the existing word; colour is never the only channel, and a person checks in task 3 step 5 |
| T-41.1-92 | Information disclosure | planning material in a public repository | mitigate | no buyer, email address, amount or date in the SUMMARY |
| T-41.1-SC | Tampering | npm installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run verify` exits 0 with no refusal
- `verify-tables` still reports `REMAINING = 1` — paid in the tree, reported, not deleted here
- The money grep is quoted line by line
- Task 3's rows recorded, ticked or `pending` with a reason
</verification>

<success_criteria>
The fifth table reads as cards, its three empty states survive as three, the one
place D-41.1-13 is met by the surface rather than the row is written down, no
money path moved, and a person has typed a search that matches nothing and read
the sentence that came back.
</success_criteria>

<output>
Create `.planning/phases/41.1-work-surface-conversion/41.1-21-SUMMARY.md` when done,
with the column array, the empty-state selection logic, the money grep, the
`CONVERTED` entry text, the table entry emptied, and task 3's rows.
</output>
