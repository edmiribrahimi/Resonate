---
phase: 27-guest-navigation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/rbac/roles.ts
  - src/components/layout/MobileNav.tsx
autonomous: true
requirements:
  - GNAV-01
  - GNAV-02

must_haves:
  truths:
    - "Guest non autenticato vede 3 tab nella bottom nav: Home, Events, Gallery"
    - "Tab Home linka alla landing page (/) con CTA di registrazione"
    - "Utente autenticato (member/organizer/master) NON vede il tab Home"
    - "Navigazione autenticata resta invariata: member vede Events/Gallery/Account, staff vede Events/Gallery/Check-in/Account"
  artifacts:
    - path: "src/lib/rbac/roles.ts"
      provides: "NavItem Home con requireAuth=false e logica di esclusione per utenti autenticati"
      contains: "Home"
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Icona home nella mappa icons"
      contains: "home"
  key_links:
    - from: "src/lib/rbac/roles.ts"
      to: "src/components/layout/MobileNav.tsx"
      via: "getVisibleNavItems returns Home item, MobileNav renders home icon"
      pattern: "icons\\[.*home"
---

<objective>
Aggiungere un tab "Home" nella bottom navigation per i guest non autenticati che linka alla landing page (/) con CTA di registrazione. Gli utenti autenticati non devono vedere questo tab.

Purpose: I guest che navigano /events o /gallery non hanno modo di tornare alla landing page per registrarsi. Il tab Home risolve questo problema di discoverability.
Output: MobileNav mostra Home/Events/Gallery per guest, navigazione autenticata invariata.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/rbac/roles.ts
@src/components/layout/MobileNav.tsx
</context>

<interfaces>
<!-- Contratti esistenti che l'executor deve conoscere -->

From src/lib/rbac/roles.ts:
```typescript
export interface NavItem {
  href: string;
  label: string;
  icon: string;
  roles: UserRole[] | null;
  requireApproved: boolean;
  requireAuth: boolean;
}

export function getVisibleNavItems(
  role: UserRole | null,
  status: UserStatus | null
): NavItem[];
```

From src/components/layout/MobileNav.tsx:
```typescript
const icons: Record<string, React.ReactNode> = {
  calendar: (/* SVG */),
  image: (/* SVG */),
  qrcode: (/* SVG */),
  user: (/* SVG */),
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere Home nav item e icona</name>
  <files>src/lib/rbac/roles.ts, src/components/layout/MobileNav.tsx</files>
  <action>
1. In `src/lib/rbac/roles.ts`:
   - Aggiungere un nuovo campo `hideWhenAuth: boolean` all'interfaccia `NavItem` (default false per tutti gli item esistenti)
   - Aggiungere un nuovo NavItem "Home" come PRIMO elemento dell'array `NAV_ITEMS`:
     ```
     { href: "/", label: "Home", icon: "home", roles: null, requireApproved: false, requireAuth: false, hideWhenAuth: true }
     ```
   - Nella funzione `getVisibleNavItems`, aggiungere un check all'inizio del filter:
     ```
     if (item.hideWhenAuth && isAuthenticated) return false;
     ```
   - Aggiornare il commento JSDoc della funzione per includere:
     `Unauthenticated (role=null): Home, Events, Gallery (3 tabs)`

2. In `src/components/layout/MobileNav.tsx`:
   - Aggiungere l'icona `home` nella mappa `icons` (Heroicons outline home):
     ```
     home: (
       <svg className="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
         <path strokeLinecap="round" strokeLinejoin="round" d="m2.25 12 8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
       </svg>
     ),
     ```
   - ATTENZIONE alla logica `isActive` per l'href `/`: la condizione attuale `pathname.startsWith(item.href)` matcherebbe SEMPRE per `/`. Aggiornare la logica isActive:
     ```typescript
     const isActive =
       item.href === "/"
         ? pathname === "/"
         : item.href === "/dashboard"
           ? pathname === "/dashboard" || pathname.startsWith("/dashboard")
           : pathname.startsWith(item.href);
     ```
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Guest non autenticato vede Home/Events/Gallery (3 tab) nella bottom nav
    - Tab Home linka a "/" (landing page con CTA "Join" per registrazione)
    - Home tab si evidenzia come attivo solo quando pathname === "/"
    - Utente autenticato non vede il tab Home
    - Navigazione per member (Events/Gallery/Account) e staff (Events/Gallery/Check-in/Account) resta invariata
    - TypeScript compila senza errori
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compila senza errori
2. Dev server: guest non autenticato su /events vede 3 tab (Home, Events, Gallery)
3. Dev server: click su Home porta a / con bottone "Join" visibile
4. Dev server: utente autenticato NON vede Home, vede la sua nav standard
</verification>

<success_criteria>
- Bottom nav per guest mostra esattamente: Home, Events, Gallery (3 tab)
- Bottom nav per member autenticato mostra: Events, Gallery, Account (3 tab, nessun Home)
- Bottom nav per staff autenticato mostra: Events, Gallery, Check-in, Account (4 tab, nessun Home)
- Nessun errore TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/27-guest-navigation-fix/27-01-SUMMARY.md`
</output>
