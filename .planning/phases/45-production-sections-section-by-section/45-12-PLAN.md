---
phase: 45-production-sections-section-by-section
plan: 12
type: execute
wave: 5
depends_on: ["45-11"]
files_modified:
  - src/app/(admin)/admin/manifesto/SectionStateBadge.tsx
  - src/app/(admin)/admin/manifesto/OpenQuestionNotice.tsx
  - src/app/(admin)/admin/(work)/manifesto/page.tsx
  - src/app/(admin)/admin/(work)/manifesto/loading.tsx
  - src/lib/production/sections/tokens.ts
  - src/app/(admin)/admin/visual/PaletteSwatches.tsx
  - src/app/(admin)/admin/(work)/visual/page.tsx
  - src/app/(admin)/admin/(work)/visual/loading.tsx
  - src/lib/routes/capability-routes.ts
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "A section in each of the three states renders differently, and the not-decided one names what is missing and whose call it is"
    - "An open question warns on the piece it bears on and blocks nothing"
    - "The palette is read from the token file at runtime, and no brand hex literal exists anywhere under `src/` outside the two existing exemptions"
    - "A format with no palette declares the void and shows the interim rule; its identification colour is never drawn as one"
  artifacts:
    - path: "src/app/(admin)/admin/manifesto/SectionStateBadge.tsx"
      provides: "the three-state badge, with no branch that renders nothing"
    - path: "src/lib/production/sections/tokens.ts"
      provides: "the runtime reader of globals.css's :root — the resolution of D-45-09's collision with check B"
    - path: "src/app/(admin)/admin/visual/PaletteSwatches.tsx"
      provides: "the palette, drawn from values it read rather than values it carries"
  key_links:
    - from: "src/lib/production/sections/tokens.ts"
      to: "src/app/globals.css"
      via: "a runtime parse of the :root block — a value, never a literal, and invisible to a grep over source"
      pattern: "globals.css"
    - from: "src/lib/routes/capability-routes.ts"
      to: "/admin/manifesto and /admin/visual"
      via: "both bindings moved to the routes branch in the same commit as their pages"
      pattern: "\"/admin/manifesto\""
---

<objective>
The two authored sections: a body of rules that can say it is not written, a register of
open questions that warns without blocking, and a palette that has exactly one home.

Purpose: this is success criterion 3 — *a section whose content is not yet written declares
the emptiness instead of filling it, and no surface implies a decision the owner has not
taken.* It is also where the phase's second collision is resolved rather than discovered:
the capitolato needs hex values and `verify:semantic-separation` forbids them.
Output: two surfaces, three components, one runtime token reader, two bindings moved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-CONTEXT.md
@.planning/phases/45-production-sections-section-by-section/45-PATTERNS.md
@.claude/rules/sound-manifesto.md
@.claude/rules/brand-visual-system.md
@src/app/(admin)/admin/calendar/StageBadge.tsx
@src/app/(admin)/admin/(work)/calendar/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: The manifesto surface — three states, and a register that warns</name>
  <files>src/app/(admin)/admin/manifesto/SectionStateBadge.tsx, src/app/(admin)/admin/manifesto/OpenQuestionNotice.tsx, src/app/(admin)/admin/(work)/manifesto/page.tsx, src/app/(admin)/admin/(work)/manifesto/loading.tsx, src/lib/routes/capability-routes.ts</files>
  <read_first>
    - `src/components/production/StageBadge.tsx` — all of it, relocated in plan 45-07. The
      three-state badge takes its **exact** shape: no colour, **no branch that produces
      nothing**, and `normal-case` declared on the element because `text-transform` inherits.
    - `.claude/rules/sound-manifesto.md` — the two opposite errors this surface exists to
      prevent: *inventato non è una risposta* and *non-scritto non vuol dire non-vincolato*.
      And *il fatto negativo si cita*: exclusions are decisions as much as inclusions, and
      they are reported rather than re-derived.
    - `scripts/verify-semantic-separation.mjs:35-39` — `--sem-warn` **is** `--amber` **is**
      SunSet's identification colour. **Anything amber carries text.**
    - `src/app/(admin)/admin/(work)/calendar/page.tsx:60-73`, `:103-113` — the cookie-bound
      read and the page guard.
    - `45-CONTEXT.md` D-45-14, D-45-15, and `<specifics>` — the register's real first
      entries, all of them already declared open in the domain modules and all of them
      criteria rather than candidates.
  </read_first>
  <action>
    **`SectionStateBadge.tsx`.** Three states, one badge, no colour, no early return.
    Write the three paragraphs the analog writes, adapted:
    · **why no colour** — grading *written* against *not decided* on a four-semantic token
      layer would settle in CSS a question `sound-manifesto.md` says is settled by whoever
      owns the brand;
    · **the badge never disappears** — a blank reads as *fine*, and *fine* is exactly the
      claim that cannot be made about a manifesto nobody has written;
    · **three states and not two** — the middle one, *coordinates declared*, is the whole
      reason this component exists. A two-state model makes it read as free, and the domain
      names both errors: inventing where a rule exists, and answering *not decided* where a
      coordinate has been declared.

    **`OpenQuestionNotice.tsx`.** A warning that **blocks nothing** (D-45-15, inheriting
    D-44-16: a block that fires under deadline is a block somebody routes around). It renders
    the question and the decision owner, and it is **a word, not a colour** — the amber
    token is SunSet's identification colour, so an amber mark alone cannot tell a reader
    *caution* from *this format*. Nothing on it disables anything.

    **`page.tsx`.** Guard on `CAP.PRODUCTION_MANIFESTO_MANAGE`, then the **cookie-bound**
    client. Read `production_section` where `section = 'manifesto'` with its format, and
    `production_open_question` where the section is `'manifesto'` **or null** — the null
    ones are questions about the brand rather than about one body of rules, and dropping
    them would hide the register's most general entries. Check each embed against its
    foreign keys and write the check as a comment: `production_section → formats` is one FK,
    `production_open_question → formats` is one FK. An ambiguity is `PGRST201` and it fails
    **silently** through this client.

    Render, per section row: the title, the state badge, and then —
    · `written`: the body;
    · `coordinates_declared`: the body, which is where the declared coordinates and the
      **explicit negative** live. There is no `negatives` column, deliberately: a separate
      column invites a surface that shows the positives and drops them;
    · `not_decided`: **what is missing and whose call it is**, both, drawn as the content of
      the panel and not as a subtitle under an empty one. This is criterion 3's sentence in
      pixels, and the difference between *not yet decided* and *failed to load* is what
      procedure P3 exists to judge.

    Open questions render **beside the piece they bear on**, not in a footer nobody reaches.

    **`loading.tsx`** on the calendar's model: a literal skeleton, no count.

    **`capability-routes.ts`.** Move `production.manifesto.manage` to the routes branch with
    `routes: ["/admin/manifesto"]` and `alsoGatesTables: true`, and delete the discharged
    obligation text. **No tab** — plan 45-18.
  </action>
  <acceptance_criteria>
    - The badge has no branch that renders nothing: `grep -c "return null" src/app/(admin)/admin/manifesto/SectionStateBadge.tsx` returns 0, and the three paragraphs are present.
    - The notice disables nothing: `grep -cE "disabled|aria-disabled" src/app/(admin)/admin/manifesto/OpenQuestionNotice.tsx` returns 0.
    - The notice carries text, not only a hue: `grep -cE "sem-warn|amber" …` returns 0, **or** every match is on a line that also renders a word — state which.
    - The null-section register entries are read: the query in `page.tsx` contains an `is null` arm, and the comment says why.
    - The `not_decided` branch renders both fields: `grep -c "missing" "src/app/(admin)/admin/(work)/manifesto/page.tsx"` returns 1 or more and the same for `decision_owner`.
    - No service client: `grep -c "supabase/service" "src/app/(admin)/admin/(work)/manifesto/page.tsx"` returns 0.
    - `npm run build` exits 0; `npm run verify:routes` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:routes</automated>
  </verify>
  <done>Three states render as three different things, a void names its gap and its owner, an open question warns in words and blocks nothing, and the manifesto binding is on the routes branch.</done>
</task>

<task type="auto">
  <name>Task 2: The palette has one home — src/lib/production/sections/tokens.ts and PaletteSwatches.tsx</name>
  <files>src/lib/production/sections/tokens.ts, src/app/(admin)/admin/visual/PaletteSwatches.tsx</files>
  <read_first>
    - `scripts/verify-semantic-separation.mjs:60-62` and `:193-203` — **check B**: no file
      under `src/`, other than **two exact-path exemptions**, contains any hex declared in
      the token file's `:root`. The exemptions are
      `src/app/(admin)/admin/formats/ColorSwatchPicker.tsx` and `src/app/layout.tsx`.
    - The same file `:190-215` — the `:root` reader it already implements. It is a **script**,
      and this module is a runtime one; the parse is the same shape, the location is not.
    - `src/app/globals.css:213-219` — the six brand hexes and `--grad-sunset`.
    - `supabase/migrations/20260810120000_formats_and_series.sql:118-124`, `:173` —
      `formats.color` is `NOT NULL`, matches `^#[0-9A-Fa-f]{6}$`, and the migration's own
      prose calls it *the IDENTIFICATION colour — the dot on a chip … and not the palette of
      the materials, which is a different thing that shares a word.*
    - `.claude/rules/brand-visual-system.md` — *il colore non si eredita*: the sunset
      gradient is SunSet's **exclusive** signature; one format uses a flat orange; one has
      **no palette at all** and its materials stay neutral.
    - `45-CONTEXT.md` D-45-09 and D-45-16.
  </read_first>
  <action>
    **`src/lib/production/sections/tokens.ts`.** Parse `src/app/globals.css`'s `:root` block
    at runtime and return the brand custom properties it finds, by name. Server-side only —
    `import "server-only"` on **line 1, above the docblock**, because the check that greps
    for it reads the first lines and a file written docblock-first would fail while
    satisfying the rule.

    **This is D-45-09's resolution and it is not optional.** A capitolato page containing
    `#FF5C93` as a literal turns `verify:semantic-separation` **red**, and that is the gate
    working. A runtime value is invisible to a grep over source, which is the point:
    **do not add a third exemption path.** An exemption granted to the surface whose whole
    job is to publish the palette would make check B meaningless for the one file it most
    needs to cover — and `verify-tokens.mjs:154` already records why a check must not be
    able to go green on the sentence describing its own prohibition.

    Return names **and** values, and let the caller decide what to draw. **Do not restate the
    gradient**: `npm run verify:sunset-gradient` is a separate gate, and the gradient is one
    format's exclusive signature rather than a palette entry.

    If the file cannot be read or the block cannot be parsed, **return a declared failure**,
    not an empty list — an empty palette rendered as a palette is a void nobody declared.

    **`PaletteSwatches.tsx`.** Draws what the reader returned. Three rules, each written:

    1. **A format with no palette declares the void and shows the interim rule.** Not a
       blank, not a placeholder swatch, and not a colour: the words that say no palette has
       been decided and that the materials stay neutral until one is. This is the format
       that has none today, and `brand-visual-system.md` calls borrowing the sunset to fill
       that gap *the way a format loses its identity before having one*.
    2. **`formats.color` is never drawn as a palette.** It is the dot on a chip — the size of
       a marker, beside a name, and never a panel. What no automatic check can assert is
       **size**: a swatch at 4 px and at 200 px are the same source line. Say so in the
       component's docblock and name procedure **P4** as the gate that actually holds it.
    3. **`--grad-sunset` is not chrome.** A surface showing "the palette" must not spend one
       format's exclusive signature on its own background.

    Every swatch carries its **token name** as text. A colour identified only by itself is a
    colour somebody will re-type into a design tool from memory.
  </action>
  <acceptance_criteria>
    - `head -3 src/lib/production/sections/tokens.ts | grep -c "server-only"` returns 1.
    - No brand hex literal is added anywhere: `npm run verify:semantic-separation` exits 0, and the exemption list is **unchanged** — `grep -c "EXEMPT_PATHS" scripts/verify-semantic-separation.mjs` and the array's contents are identical to their pre-change values, recorded in the SUMMARY.
    - `npm run verify:sunset-gradient` exits 0.
    - The reader fails loudly: `grep -cE "return \[\]|return \{\}" src/lib/production/sections/tokens.ts` returns 0.
    - The no-palette branch renders words: `grep -ci "neutral\|not been decided\|no palette" src/app/(admin)/admin/visual/PaletteSwatches.tsx` returns 1 or more.
    - `grep -c "grad-sunset" src/app/(admin)/admin/visual/PaletteSwatches.tsx` returns 0, or exactly 1 inside the paragraph refusing it.
    - Every swatch carries a name: the component renders the token name as text beside each value.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:semantic-separation &amp;&amp; npm run verify:sunset-gradient</automated>
  </verify>
  <done>The palette is drawn from values read at runtime, the gate that forbids literals stayed green without a third exemption, and the format with no palette says so in words.</done>
</task>

<task type="auto">
  <name>Task 3: The visual surface — the capitolato beside the material</name>
  <files>src/app/(admin)/admin/(work)/visual/page.tsx, src/app/(admin)/admin/(work)/visual/loading.tsx, src/lib/routes/capability-routes.ts</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/manifesto/page.tsx` — task 1's file. This one is its
      sibling and shares its guard, its client and its embed discipline.
    - `.claude/rules/brand-visual-system.md` — what the capitolato holds: what is fixed and
      what is variable, the grid-safe square, the date legible in a thumbnail, the
      publication order and its inversion in the grid, the typography and its declared
      degradation, `@ Secret Venue`, and *lo spazio non acquisito non si nomina*.
    - `src/app/(admin)/admin/visual/PaletteSwatches.tsx` — task 2's component.
    - `45-CONTEXT.md` D-45-08 — the rules **and** the material together, including the dj
      photo archive. The archive's upload arrives in plan 45-17; this page leaves a place for
      it and does not build one.
  </read_first>
  <action>
    Guard on `CAP.PRODUCTION_VISUAL_MANAGE`, then the cookie-bound client. Read
    `production_section` where `section = 'visual'` with its format, the open questions for
    `'visual'` and for null, and `production_visual_asset` — **listed by kind, artist and
    date, with no image rendered yet**, because the bytes have no read path until plan 45-17
    mints one. A broken image on a gated surface is indistinguishable from an empty archive,
    so render a count and a list, and say in a comment that the thumbnails arrive with their
    signed-URL path and not before.

    The same three-state rendering as the manifesto, from the same badge — **do not write a
    second one**. `PaletteSwatches` sits inside the capitolato's own section.

    Two things this page must **not** do, each written where it would be added:
    · **it names no space.** The capitolato is the document that leaves the perimeter, and
      `venue-secrecy.md` calls it an exit route; a venue name on it is a negotiation
      published. The page reads no venue table and has no venue field;
    · **it alludes to no sound.** Where a format's identity is unwritten, materials carry no
      genre, no scene reference and no adjective that sounds like a promise
      (`sound-manifesto.md`, *gate la grafica non anticipa il suono*). This page shows the
      manifesto's **state**, not a description invented to fill it.

    **`loading.tsx`** on the same model, a literal skeleton.

    **`capability-routes.ts`.** Move `production.visual.manage` to the routes branch with
    `routes: ["/admin/visual"]` and `alsoGatesTables: true`. Delete the discharged obligation
    text. **No tab** — plan 45-18. After this task, **no key remains on the table-only branch
    that this phase put there**; confirm that by reading the file and record it.
  </action>
  <acceptance_criteria>
    - The guard asks the visual key and no service client is constructed.
    - No second state badge exists: `/usr/bin/grep -rln "SectionStateBadge" src` shows one definition and two or more importers.
    - The page reads no venue table: `grep -cE "\.from\(\"venues\"|\.from\(\"event_parties\"|\.from\(\"production_space" "src/app/(admin)/admin/(work)/visual/page.tsx"` returns 0.
    - No image is rendered from the archive yet: `grep -cE "<img|next/image" "src/app/(admin)/admin/(work)/visual/page.tsx"` returns 0, and the comment naming plan 45-17 is present.
    - `capability-routes.ts` has no remaining `scope: "table"` entry added by this phase — verify by diffing the entry list against plan 45-05's SUMMARY and recording the result.
    - `npm run build` exits 0; `npm run verify:routes` exits 0; `npm run verify` exits 0.
    - `node scripts/verify-section-surface.mjs` now measures all six scoped directories. Record its exit code and every check's verdict; a REFUSED here would mean a directory is still missing.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify &amp;&amp; node scripts/verify-section-surface.mjs</automated>
  </verify>
  <done>Both authored sections render behind their own keys, all four bindings are on the routes branch, and the surface gate can finally see every directory it was written for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a rendered colour → a decision somebody believes was taken | `formats.color` is `NOT NULL`, so **every** format has one, including the one with no palette |
| a token value → a second home | Six colours written twice are six colours that diverge |
| the capitolato surface → whoever receives the document | It is built to leave the perimeter, so what it can name is what will leave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-13 | Spoofing | a `formats.color` swatch, and an empty section | mitigate | the identification colour is drawn at marker size beside a name and never as a panel; a format with no palette gets **words**; the state badge has no branch that produces nothing. **Size is not machine-checkable** — that is procedure P4, named in the component's own docblock |
| T-45-09 | Information disclosure | the capitolato surface | mitigate | it reads no venue table and has no venue field; the export that carries it is proved narrow by the gate of plan 45-06 and built in plan 45-16 |
| T-45-14 | Information disclosure | the palette's second home | mitigate | read at runtime from the token file; **no third exemption** added to check B, and the exemption array is asserted unchanged |
| T-45-01 | Information disclosure | the archive listing | mitigate | no image is rendered until a signed-URL path exists; an artist name in an unannounced line-up is material in the same sense a space under negotiation is |
| T-45-04 | Elevation of privilege | the two binding moves | mitigate | each moves in the same commit as its page; `Unbound` is the compiler error that catches the reverse |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | no package is installed |
</threat_model>

<verification>
- `npm run build`, `npm run verify:routes`, `npm run verify:semantic-separation`,
  `npm run verify:sunset-gradient` and `npm run verify` all exit 0.
- `node scripts/verify-section-surface.mjs` measures all six directories for the first time;
  its verdicts are recorded check by check.
- **What a green does NOT mean:** check D of the surface gate says of itself that it is
  weak, because no assertion sees size. And no assertion can tell *not yet decided* from
  *failed to load* on a screen. Procedures **P3** and **P4** are what close those, and both
  are still `pending`.
</verification>

<success_criteria>
- Three states rendering as three different things, with the void naming what is missing and whose call it is.
- An open-question register that warns beside the piece it bears on and blocks nothing, in words rather than a hue.
- A palette read from the token file at runtime, with the exemption list provably unchanged and both colour gates green.
- A format with no palette declaring the void and showing the interim rule, and its identification colour drawn as a marker and never as a panel.
- All four section bindings on the routes branch, none left on the table-only branch, and no tab added.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-12-SUMMARY.md` when done.
</output>
