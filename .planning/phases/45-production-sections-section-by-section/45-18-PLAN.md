---
phase: 45-production-sections-section-by-section
plan: 18
type: execute
wave: 9
depends_on: ["45-09", "45-10", "45-14", "45-16", "45-17"]
files_modified:
  - src/lib/routes/staff-tabs.ts
  - .planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md
autonomous: false
requirements: [PROD-02]

must_haves:
  truths:
    - "Four section tabs exist, each pointing at an address its own key opens, checked by the module-load loop at build time"
    - "Every automatic gate in this repository is green, and the two that can only refuse have been run under their authorisations"
    - "The refusal instrument asserts a real pair on a section table that holds rows, for the first time"
    - "Every written procedure carries an observation instead of `pending`, or is recorded as unrun with its reason"
    - "What this phase cannot prove is written down as such, in its own words, and not left implied"
  artifacts:
    - path: "src/lib/routes/staff-tabs.ts"
      provides: "the four section tabs"
    - path: ".planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md"
      provides: "four results, each an observation or a recorded non-run"
  key_links:
    - from: "src/lib/routes/staff-tabs.ts"
      to: "src/lib/routes/capability-routes.ts"
      via: "the module-load loop, which throws by name when a tab and the map disagree"
      pattern: "resolveRoute"
---

<objective>
The four sections become reachable from the navigation, every gate is run, and the four
things only a person can see are seen.

Purpose: the tab is the last step of the binding and it is last **because the order is
forced, not chosen** — `StaffTab.href` is `Route`, and a static address enters the generated
union only once a `page.tsx` serves it. And a phase whose evidence is written procedures is
not finished when the code compiles; it is finished when the procedures carry observations.
Output: four tabs, one full sweep, four results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-CONTEXT.md
@.planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md
@.planning/phases/45-production-sections-section-by-section/45-02-SUMMARY.md
@.planning/phases/45-production-sections-section-by-section/45-08-SUMMARY.md
@src/lib/routes/staff-tabs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The four tabs</name>
  <files>src/lib/routes/staff-tabs.ts</files>
  <read_first>
    - `src/lib/routes/staff-tabs.ts:108-131` — why the tab lands in a **later plan** than the
      page, and the two workarounds — widening `href`, asserting the type on one entry —
      **rejected in writing** at `:117-126`. They stay rejected.
    - The same file `:132-153` — the Calendar entry and the **hiding this tab protects
      nothing** paragraph: what this surface holds **is** the secret, and three things refuse
      an unentitled account, none of which is the absence of a link — the middleware entry,
      the page's own guard, and the row-level policies.
    - The same file `:173-198` — the module-load loop and its two failure sentences.
  </read_first>
  <action>
    Three new entries beside the calendar's — Location, Manifesto, Visual — each with its
    address and **its own key**, and the calendar's entry already moved in plan 45-05.

    Copy the *hiding protects nothing* paragraph **per section**, and make it specific rather
    than generic: for location, what the surface holds is a list of spaces under negotiation
    with their addresses, and the three things that refuse an unentitled account are named
    individually. A paragraph repeated without being re-derived is a paragraph the next
    reader skips.

    Order the tabs so the four production sections sit together, and say why in one line — a
    navigation is read as a grouping whether or not it was meant as one.

    The module-load loop is what checks this, and it throws **by name** when a tab claims a
    key the map binds to another. That check runs during `next build`, because staff pages
    are prerendered — so a mistake here is a red build, which is the safe direction. The
    unsafe direction is the reverse, and it was handled in plans 45-11 and 45-12: a map entry
    whose page is not on disk is silent.
  </action>
  <acceptance_criteria>
    - Four production tabs exist: `grep -cE "/admin/(calendar|location|manifesto|visual)" src/lib/routes/staff-tabs.ts` returns 4.
    - Each names a different key: extract the four `capability:` values and record that they are four distinct members of `CAP`.
    - The paragraph is per section, not copied verbatim four times: record the four and confirm each names what its own surface holds.
    - `npm run build` exits 0 — which is the module-load loop passing.
    - `npm run verify:routes` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:routes</automated>
  </verify>
  <done>Four tabs, four keys, four addresses, checked by the loop that throws by name.</done>
</task>

<task type="auto">
  <name>Task 2: The full sweep, and the refusal instrument with something to measure</name>
  <files>(no repository file changes)</files>
  <read_first>
    - `45-02-SUMMARY.md` and `45-08-SUMMARY.md` — the two earlier runs of the refusal
      instrument, and what each could and could not measure.
    - `scripts/verify-all.mjs:26-36` — *a refusal is not a failure*, and why collapsing the
      two turns *nothing was measured* into a tick.
    - `45-VALIDATION.md` § Per-Task Verification Map — the eleven rows, and which command
      closes each.
  </read_first>
  <action>
    Run, and record each transcript with its exit code:

    · `npm run build`
    · `npm run verify` — the aggregate, with its four lists printed
    · `npm run verify:capabilities` — **green**, seventeen keys
    · `npm run verify:routes`, `verify:semantic-separation`, `verify:sunset-gradient`,
      `verify:dialogs`, `verify:media-strip`, `verify:touch-targets`, `verify:tokens`
    · `node scripts/verify-section-surface.mjs` — **every check with a verdict**
    · `npm run verify:section-export` — both halves, the closure and the reachability census

    Then, under an authorisation for **this sitting** — the mint of plan 45-02 was spent, and
    an authorisation covers what it described and nothing more — run `npm run verify:refusal`
    a third time.

    **This run is the one the phase was built for.** The location section now holds rows, so
    for the first time the instrument can assert a **pair** on a section table: a master
    reads them, a member reads none, and neither answer is the byte-identical `[]` that made
    the earlier runs refuse. Record, per table:
    · the master's count and the member's count;
    · the anonymous floor's count;
    · and, for the tables still holding zero rows, **REFUSED — not measured**, unchanged and
      unrepaired.

    **Write both sentences, and do not let the first stand in for the second:**
    · *the four section policies ask four different keys* — read from `pg_policies`, which is
      criterion 1's structural half;
    · *a signed-in subject holding none of the four keys reads no rows from any of them* —
      which is criterion 4, and which this run closes for the first time in this project's
      history.

    And the third sentence, which is a limit: **nothing here proves that a holder of one
    section is refused another.** D-45-03 grants all three keys to both roles, so no
    production subject exists for whom that is true, and **no plan in this phase fabricated
    one** (D-45-23). That is procedure P1, in a throwaway environment, and it is task 3.
  </action>
  <acceptance_criteria>
    - Every command above has its transcript and exit code recorded in the SUMMARY.
    - `npm run build`, `npm run verify` and `npm run verify:capabilities` all exit 0.
    - `node scripts/verify-section-surface.mjs` exits 0 with no check reporting REFUSED.
    - `npm run verify:section-export` exits 0 on both halves, and the reachability census lists the one expected edge — `promoted_venue_id → venues` — and no other.
    - The refusal instrument asserts at least one **real pair** on a section table: master count greater than zero, member count zero, anonymous count zero.
    - Every minted session is provably revoked, with the token re-read and no longer resolving.
    - The three sentences are written in the SUMMARY, in that order, with the third not softened.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify &amp;&amp; npm run verify:capabilities &amp;&amp; node scripts/verify-section-surface.mjs &amp;&amp; npm run verify:section-export</automated>
  </verify>
  <done>Every automatic gate in the repository is green, the reachability census enumerates one edge and no other, and the first genuine refusal evidence in this project's history is recorded with what it does and does not say.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The four procedures, performed and recorded</name>
  <what-built>
    Four production sections are reachable from the navigation, each behind its own key. The
    location section holds the seeded archive at stage `mapped`; the manifesto and visual
    sections hold whatever has been authored. Every automatic gate is green and the refusal
    instrument has recorded a real pair.

    `45-PROCEDURES.md` holds four procedures, and **every one of them still reads
    `Result: pending`** — which means unrun, not verified-by-inspection.
  </what-built>
  <how-to-verify>
    Perform the four procedures as written, in `45-PROCEDURES.md`, and record what was
    observed. A `Result` that repeats the procedure's own expectation is not an observation.

    **P1 — a holder of one section is refused the others.** Criterion 1, and it is the one
    that cannot be done in production: D-45-03 grants all three keys to both roles, so there
    is no subject. **In a throwaway environment**, with a hand-made account holding one
    section key: open each of the four addresses, record which render and which refuse, and
    record **where the refusal came from** — the middleware, the page guard, or the row-level
    policy. The criterion names the third and says *not from the navigation hiding a link*,
    so the interesting observation is what happens when the address is typed rather than
    clicked.

    **P2 — the stage is visible wherever the space is named.** With rows at each of the four
    stages: is a non-`acquired` stage unmistakable at a glance, in the list **and** in the
    detail? A source assertion proved the badge is in the tree; only a person can say it
    reads as a stage rather than as decoration.

    **P3 — the void reads as declared, not as broken.** Open a section in each of the three
    states. Does the not-decided one name what is missing and whose call it is, and does it
    read as *nobody has decided this yet* rather than as *this failed to load*?

    **P4 — an unanswered attribute reads as a question, and a format colour is not a
    palette.** Open a space with several attributes unanswered: does each read as a question
    nobody has asked, rather than as missing data? Then open the visual section on the format
    that has **no palette**: is its identification colour drawn as a marker, at marker size,
    beside a name — and is the void declared in words? **Record the size at which the colour
    is drawn**, because no assertion in this repository can see size, and that is the whole
    reason this half of P4 exists.

    **If a procedure cannot be run**, record it as unrun **with its reason**. An unrun
    procedure is an honest state; a `Result` invented from the code is not, and this
    repository has a written precedent for a SUMMARY that claimed a passing build on a runner
    that never started.
  </how-to-verify>
  <read_first>
    - `.planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md` — the four
      procedures and the four rules of the document.
    - `.planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md` — the model,
      and the `human_needed` shape a result takes when it cannot be run.
    - `45-CONTEXT.md` D-45-03 and D-45-23 — the two sentences that must not be softened.
  </read_first>
  <acceptance_criteria>
    - Each of the four `Result:` lines carries either an **observation** — what was seen, and where — or **unrun, with a reason**. No line still reads `pending` without one of the two.
    - P1's result names **where** each refusal came from, and states plainly that it was performed in a throwaway environment and never in production.
    - P4's result records the pixel size at which the identification colour is drawn.
    - No venue name, address, date or line-up appears in the file: `grep -cE "[Vv]ia |[Cc]orso |[Pp]iazza |20[0-9]{2}-[0-9]{2}-[0-9]{2}" 45-PROCEDURES.md` returns 0 except for the frontmatter's own dates.
    - The three authorisation ledger lines A1, A2 and A3 all carry their dates.
    - The document's closing paragraph states, unsoftened, that **a green build is not a proof of refusal**, and which criterion closes how.
  </acceptance_criteria>
  <files>.planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md</files>
  <action>Perform the four procedures, record each result as an observation or as an unrun with its reason, fill the three authorisation dates, and change nothing else in the document.</action>
  <verify>
    <automated>MISSING — these four exist precisely because no assertion in this repository can hold them. The evidence is the recorded observations in 45-PROCEDURES.md.</automated>
  </verify>
  <done>Four results, each an observation or an honest non-run, with P1 performed outside production and P4 carrying a measured size.</done>
  <resume-signal>Type "verified" with the four results, or describe what could not be run and why.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a navigation link → an access verdict | The tab is neither, and the paragraph beside each says so |
| a green transcript → a claim about a person's perception | Four things in this phase close on a person, and three of them look like they could be automated |
| a throwaway environment → production | P1's account exists only in the former, and nothing in this phase created one in the latter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Elevation of privilege | the four tabs | mitigate | each names its own key and the module-load loop throws by name when a tab and the map disagree; **hiding a tab protects nothing**, and the three things that do are named per section |
| T-45-21 | Repudiation | a `Result` invented from the code | mitigate | a pending Result is an **unrun** procedure; an unrun one is recorded with its reason; the recorded precedent of a SUMMARY claiming a green from a runner that never started is cited in the document |
| T-45-04c | Elevation of privilege | criterion 1's subject | mitigate | the hand-made account exists **only in a throwaway environment**; D-45-23 forbids fabricating a role grant in production, and a criterion made green by changing the thing it measures is worse than an open criterion |
| T-45-03 | Information disclosure | the procedures document | mitigate | roles never names, steps never observations of content; the grep for street, date and line-up tokens |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | this phase installed no package, and the audit table in `45-RESEARCH.md` records that there was nothing to check |
</threat_model>

<verification>
- `npm run build` and `npm run verify` exit 0; `npm run verify:capabilities` exits 0.
- `node scripts/verify-section-surface.mjs` exits 0 with no REFUSED.
- `npm run verify:section-export` exits 0 on both halves.
- `npm run verify:refusal` recorded, with a real pair on a section table.
- Four procedure results recorded.
- **What a green does NOT mean, and this is the sentence the phase's verification document
  inherits:** the build passes, every gate is green, and **none of that is a proof of
  refusal**. `verify:capabilities` reads through a role that bypasses RLS. The one instrument
  that does not is `verify:refusal`, and even it cannot reach criterion 1, because D-45-03
  left it no subject. That is P1's job, in a throwaway environment, and the honest phrasing
  of what it closes is *a viewer holding one section was refused the others **there***.
</verification>

<success_criteria>
- Four tabs, four keys, four addresses, and a per-section paragraph that was re-derived rather than copied.
- Every automatic gate in the repository green, with the two that can only refuse run under their authorisations.
- The first real refusal pair on a section table, with the three sentences written and the third unsoftened.
- Four procedure results, each an observation or an honest unrun, and three authorisation dates recorded.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-18-SUMMARY.md` when done.
</output>
