---
phase: 45-production-sections-section-by-section
plan: 06
type: execute
wave: 2
depends_on: ["45-02", "45-03"]
files_modified:
  - scripts/verify-section-surface.mjs
  - scripts/verify-section-export.mjs
  - scripts/verify-all.mjs
  - package.json
  - .planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "No component can render a space's name without its stage in the same subtree, and a gate says so by reading the source"
    - "No component can render a computed score without its provenance"
    - "No export module can reach a table holding an address or an unannounced date, and the gate proves it by walking the import closure rather than by reading one query string"
    - "No scouting table has an edge — foreign key, view or function — into the one public road to an address, and the gate enumerates the absence from the catalogues"
    - "Every manual procedure this phase owes exists in writing, with its Result at `pending`"
  artifacts:
    - path: "scripts/verify-section-surface.mjs"
      provides: "the mechanical half of criteria 2 and 3, plus D-45-18's log-shape assertion"
      contains: "what a green does not mean"
    - path: "scripts/verify-section-export.mjs"
      provides: "D-45-17's and D-45-21's structural proof — a closure walk and a catalogue reachability census"
    - path: ".planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md"
      provides: "the four written procedures and the three authorisation ledger lines"
      contains: "Result: pending"
  key_links:
    - from: "scripts/verify-section-export.mjs"
      to: "public.venue_for_parties"
      via: "a catalogue census of pg_constraint, pg_views and pg_proc asserting no edge exists"
      pattern: "venue_for_parties"
    - from: "both gates"
      to: "scripts/lib/comments.mjs"
      via: "every scanned file read through the one comment stripper"
      pattern: "comments.mjs"
---

<objective>
The three things this phase must be able to check with a machine, and the four it can only
check with a person — written **before** the surfaces they measure, so that the file paths
they assert are a contract the surfaces satisfy rather than a description of whatever got
built.

Purpose: these three surfaces are outside `verify:conversion`'s walk — the conversion
manifest ends at Phase 41 and the calendar surface was never added to it. **If these gates
are not built, nothing automatic looks at these files at all.**
Output: two gates, two registrations, and one procedures document with every result at
`pending`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-RESEARCH.md
@.planning/phases/45-production-sections-section-by-section/45-VALIDATION.md
@.planning/phases/45-production-sections-section-by-section/45-03-SUMMARY.md
@.planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md
@scripts/verify-calendar-surface.mjs
@scripts/verify-conversion.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify-section-surface.mjs — five checks over named directories</name>
  <files>scripts/verify-section-surface.mjs</files>
  <read_first>
    - `scripts/verify-calendar-surface.mjs:1-70` — the whole header, and copy it in
      substance: the **what a green does not mean** section (`:10-30`), the scope named as
      directories and never as `src/` (`:32-39`), every file read through
      `scripts/lib/comments.mjs` (`:41-55`), the script not matching itself (`:57-65`), and
      the exit codes `0` / `1` / **`2` = REFUSED, and a refusal is not a failure**.
    - `src/app/(admin)/admin/calendar/PieceDate.tsx:9-16` — the **single-renderer rule**,
      asserted as check U5 of the calendar's gate. It is what makes a source assertion
      about a surface checkable at all.
    - `src/app/(admin)/admin/calendar/actions.ts:740-746` — D-45-18's exact log form.
    - `scripts/verify-semantic-separation.mjs:35-39` and `:193-203` — `--sem-warn` **is**
      `--amber` **is** SunSet's identification colour, and the two-path exemption list.
    - `45-RESEARCH.md` §E4 — the five checks and their stated limits.
  </read_first>
  <action>
    Scope: `src/app/(admin)/admin/location/`, `src/app/(admin)/admin/manifesto/`,
    `src/app/(admin)/admin/visual/`, `src/app/(admin)/admin/(work)/location/`,
    `src/app/(admin)/admin/(work)/manifesto/`, `src/app/(admin)/admin/(work)/visual/` and
    `src/lib/production/sections/`. **Named directories, never `src/`** — widening the scan
    reddens files that never agreed to these rules, and a gate that goes red on a correct
    tree is a gate somebody disables.

    A directory that does not exist yet is a **REFUSAL** for the checks that read it, not a
    pass. The run exits `2` and says which directory was missing, so a green can never be
    obtained by not having written the surface.

    Five checks, each with its own name and its own limit written beside it:

    **A — stage beside the name.** No file in scope renders `space.name` (or the prop that
    carries it) without `StageBadge` appearing in the same JSX subtree. Criterion 2 in its
    own words: *a stage that is not `acquired` is visible as such **wherever** the space is
    named*. Limit: this is a string assertion over source; it proves the badge is in the
    tree, not that a person can read it — that half is procedure P2.

    **B — provenance beside the value.** No file in scope renders a computed score without
    the provenance of its inputs in the same subtree, and no file renders an attribute
    value without the marker that says whether it was asked. Enforce it the way the
    calendar does: **a single renderer** — one component owns the score and one owns the
    attribute, and the check asserts that no other file in scope names them.

    **C — the void is declared.** The `not_decided` branch renders `missing` **and**
    `decision_owner`; there is no branch in scope that renders a section with no body as an
    empty panel. Limit: it cannot tell *declared* from *broken* on a screen — that is
    procedure P3.

    **D — a format colour is never a palette.** No file in scope applies a large-swatch
    utility to a value derived from `formats.color`. **State in the check's own text that
    this is weak**: a swatch at 4 px and at 200 px are the same source line, and size is
    the thing that turns an identification colour into a palette. The real gate is
    procedure P4.

    **E — the log shape.** Every `console.error` in scope matches
    `code=… message=…` and no `console.*` in scope receives an error object or a space's
    name or address. This is D-45-18, promoted to Critical by D-45-21: what a whole-error
    log line can now leak is the address of a space under negotiation, into logs nobody
    watches, because this project has no error tracking. **Do not spell PostgREST's third
    field in the sentence forbidding it** — `formats/actions.ts:58-63` records why: a grep
    whose only match is its own prohibition is a grep that gets ignored the third time it
    goes red.

    Every scanned file is read through `scripts/lib/comments.mjs`, and a file whose comment
    never closes is a file this gate **cannot measure** — it REFUSES rather than reporting a
    green about nothing.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-section-surface.mjs` exits **2** on the current tree, and its transcript names each missing directory. A `0` at this moment would mean the gate measured nothing and said so was fine.
    - The header carries the mandatory section: `grep -ci "what a green does not mean" scripts/verify-section-surface.mjs` returns 1.
    - The scope is directories, not `src/`: `grep -vE "^\s*(\*|//)" scripts/verify-section-surface.mjs | grep -c "'src/'" ` returns 0.
    - The stripper is used: `grep -c "comments.mjs" scripts/verify-section-surface.mjs` returns 1 or more.
    - Check D declares its own weakness: `grep -ci "weak" scripts/verify-section-surface.mjs` returns 1 or more.
    - PostgREST's third field name does not appear: `grep -ci "details" scripts/verify-section-surface.mjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-section-surface.mjs; test $? -eq 2</automated>
  </verify>
  <done>Five named checks over named directories, refusing honestly on a tree that has not built the surfaces yet, with each check's limit written beside it.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/verify-section-export.mjs — the closure walk and the reachability census</name>
  <files>scripts/verify-section-export.mjs</files>
  <read_first>
    - `scripts/verify-conversion.mjs:792-824` — `importClosure`, the only import-closure walk
      in the repository, **and the sentence about why the walk passes THROUGH an excluded
      file rather than stopping at it**: stopping would be a narrowing in the direction that
      produces a green.
    - `scripts/verify-conversion.mjs:75` and `scripts/probe-forged-identity.sh:44-46` — a
      check that has never been shown to fire proves nothing. The positive half is not
      decoration.
    - `supabase/migrations/20260810161000_venues_read_narrowed.sql:371-397` — the one public
      road to an address, its `FROM` clause, and the three dangerous shapes named at
      `45-PATTERNS.md` §D3 so they are recognised if proposed.
    - `supabase/migrations/20260807000000_capability_model.sql:127-160` — the best-written
      argument in this repository for **structural unreachability**, and its own limit:
      those tables are unreachable by everyone, which `production_space` cannot be.
    - `scripts/rls-baseline.mjs:215-216`, `:262-288` — how a catalogue read is performed here,
      and that it connects with a role that **bypasses RLS**, so what it can prove is
      *structure*, never *refusal*.
    - `45-RESEARCH.md` §E2 — checks A to E, in the researcher's words.
  </read_first>
  <action>
    Two halves, one script, both refusing rather than passing when they cannot measure.

    **Half one — source, offline.** Entry files: `src/lib/production/export/manifesto.ts`
    and `src/lib/production/export/capitolato.ts` (plan 45-16).

    · **A — closure.** Walk the transitive import closure from each entry and assert that
      no reached module queries `production_plan`, `production_piece`,
      `production_commitment`, `production_checklist_item`, `production_import_run`,
      `production_space`, `production_space_attribute`, `production_visual_asset`,
      `event_parties`, `events` or `venues`. Pass **through** excluded files; do not stop
      at them.
    · **B — no forbidden `.from()`** in the entry modules themselves, read through the
      comment stripper.
    · **C — the positive half.** Each export module **names its own tables**, so the check
      is not vacuous the day somebody deletes the query. A negative-only assertion goes
      green on a deleted file.
    · **D — the forbidden list is derived, not typed.** Build it from the set of tables
      declared in `supabase/migrations/**` that carry a column named `address`, `venue*`,
      `date` or `start_time`, so a seventh production table added later is covered by
      construction. Print the derived list on every run; a list that shrank is a finding.
    · **E — no service client.** No reached module imports the service client. An export
      that bypasses the policy of the section it exports is an export that can carry
      another section's rows.

    **The location section has no export**, and this gate is where that is structural
    rather than remembered: there is no third entry file, and check A's forbidden list
    contains the two location tables. If a `src/lib/production/export/location*.ts` ever
    appears, the gate **fails by name** rather than quietly walking it.

    **Half two — the reachability census, catalogue, credentials required.** This is the
    new work `45-PATTERNS.md` §No Analog Found #1 names: nothing in this repository reads
    `pg_constraint` / `pg_views` / `pg_proc` and asserts that a named table has no edge
    into a named public road. Assert, with a **pre-registered expectation** in the shape of
    `verify-capabilities.mjs:190-200`:

    · **no foreign key** from any column of `public.venues`, `public.events` or
      `public.event_parties` to `production_space` or `production_space_attribute` — the
      dangerous direction; `production_space.promoted_venue_id → venues` is the safe one
      and is expected, by name, so its presence is a pass and its absence is a finding;
    · **no view in `public`** whose definition names both a `production_space*` table and
      any of `venues`, `events`, `event_parties`. PostgREST would serve it;
    · **no function in `public`** whose body names both, other than the promotion path if
      one is ever added, which would be declared here by name.

    Without `SUPABASE_ACCESS_TOKEN` this half **REFUSES with exit 2** and prints *nothing
    about reachability was measured — that is its honest state, and it is not a pass*.

    And the closing sentence, mandatory: this census reads structure through a role that
    **bypasses RLS**. It proves no road exists. It proves nothing about who may walk one.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-section-export.mjs` exits **2** on the current tree — the entry files do not exist and no token is set — and its transcript distinguishes *missing entry file* from *missing credentials*.
    - The closure walk is present: `grep -c "importClosure\|closure" scripts/verify-section-export.mjs` returns 2 or more, and the walk passes through excluded files (state it in a comment).
    - The forbidden list is derived: `grep -ci "derived from" scripts/verify-section-export.mjs` returns 1 or more, and the run prints the list it derived.
    - The positive half exists: check C fails when an export module's own `.from()` is removed. **Demonstrate it** — comment out one line in a scratch copy, run, record the red, restore. A gate never shown to fire proves nothing.
    - `grep -c "venue_for_parties" scripts/verify-section-export.mjs` returns 1 or more.
    - `grep -ci "location" scripts/verify-section-export.mjs` returns 1 or more, in the paragraph stating that the location section has no export **and** that the gate fails by name if one appears.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-section-export.mjs; test $? -eq 2</automated>
  </verify>
  <done>The export's narrowness has a walk that can prove it and a census that can enumerate the absence, both refusing honestly until there is something to measure, and check C has been shown to fire.</done>
</task>

<task type="auto">
  <name>Task 3: Registration, and 45-PROCEDURES.md with every Result at pending</name>
  <files>package.json, scripts/verify-all.mjs, .planning/phases/45-production-sections-section-by-section/45-PROCEDURES.md</files>
  <read_first>
    - `scripts/verify-all.mjs:220-267` — the `OFFLINE` list and the reason-string shape.
    - `scripts/verify-all.mjs:26-36` — *a refusal is not a failure*, in the aggregate too.
    - `.planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md:1-38` — the
      frontmatter with its authorisation split, and the four rules: every `Result` reads
      `pending` and a pending Result is an **unrun** procedure; **roles, never names**, and
      no venue, no date and no line-up anywhere in the file; why these procedures and not
      more; and why a writing procedure is separated on purpose.
    - `45-VALIDATION.md` § Manual-Only Verifications — the four rows, with the reason each
      is manual written in the researcher's words.
    - `45-CONTEXT.md` D-45-03 and D-45-23 — why criterion 1 has no subject in production,
      and that **no plan may fabricate a role grant in production to make it provable**.
  </read_first>
  <action>
    **Registration.** Add `verify:section-surface` and `verify:section-export` to
    `package.json`, and add both to `verify-all.mjs`'s **`OFFLINE`** list — the surface gate
    is genuinely offline; the export gate's second half needs credentials and **refuses**
    without them, which is the same honest state `verify:capabilities` already carries and
    the reason string says so in the same words.

    **`45-PROCEDURES.md`.** Frontmatter on 44's model: phase, date, `status: all pending`,
    what it closes, the accounts it uses expressed as **roles, never names**, and the
    authorisation split. This phase has **three** authorisations, not one, and each is an
    act consumed once for exactly what was described:
    · **A1** — the session mint of plan 45-02 (already spent once by the time this is read;
      a second sitting is a second authorisation);
    · **A2** — the migration application of plan 45-08 and the retirement of plan 45-09;
    · **A3** — the scouting seed run of plan 45-10.

    Four procedures, each with steps a person performs and a `Result: pending`:

    · **P1 — a holder of one section is refused the others.** Criterion 1. **Hand-made
      account, in a throwaway environment**, never in production: D-45-03 grants all three
      keys to both roles, so no production subject exists for whom the sentence is true,
      and making one would be an access change rather than a test row. Steps: create the
      account with a single section key; open each of the four section addresses; record
      which render and which refuse; and record **where the refusal came from** — the
      middleware, the page guard, or the row-level policy — because the criterion names
      the third and says *not from the navigation hiding a link*.
    · **P2 — the stage is visible wherever a space is named.** Criterion 2. Open the
      location section with rows at each of the four stages; confirm a non-`acquired` stage
      is unmistakable at a glance, in the list **and** in the detail.
    · **P3 — the void reads as declared, not as broken.** Criterion 3. Open a section in
      each of the three states; confirm the not-decided one names what is missing and whose
      call it is, and that it does not read as a failed load.
    · **P4 — an unanswered attribute reads as a question nobody asked, and a format colour
      is not a palette.** Two judgements no assertion can hold: a blank cell and an unasked
      question are the same pixel; and a swatch at 4 px and at 200 px are the same source
      line. Confirm both, on a format that **has no palette**, and record the size at which
      the identification colour is drawn.

    **The four rules, restated in this file's own voice.** In particular rule (b): this
    document is tracked and the repository is public, so a step says *open the location
    section and read the first row*, never what that row says.

    And a closing paragraph the verification document will need: **a green build is not a
    proof of refusal.** It is the same shape as Phase 44's criterion 4, and it is why
    D-45-18 and D-45-19 exist.
  </action>
  <acceptance_criteria>
    - `grep -c "verify:section-surface\|verify:section-export" package.json` returns 2.
    - `npm run verify` exits 0, and its transcript shows both new gates **refusing** (exit 2) with a reason, and the aggregate not treating a refusal as a failure.
    - `grep -c "Result: pending" 45-PROCEDURES.md` returns 4.
    - `grep -ci "throwaway" 45-PROCEDURES.md` returns 1 or more, in P1.
    - No candidate material: `grep -cE "[Vv]ia |[Cc]orso |[Pp]iazza |20[0-9]{2}-[0-9]{2}-[0-9]{2}" 45-PROCEDURES.md` returns 0 except for the frontmatter's own `written:` date.
    - The three authorisations are listed by name with their plans.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <done>Both gates are registered and refuse honestly inside the aggregate, and the four procedures exist in writing with nothing claimed as done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an export module → whoever receives the document | The manifesto goes to whoever plays; the capitolato goes to the external designer. Both **leave the perimeter** by design |
| the scouting table → any public road | One function, one `FROM` clause. The census is what turns *there is no road* from an argument into an enumeration |
| a written procedure → a public repository | The steps are published; what they observe is not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09 | Information disclosure | the export path | mitigate | closure walk + derived forbidden list + the positive half, shown to fire; no service client in any reached module; the location section has no entry file and the gate fails by name if one appears |
| T-45-01 | Information disclosure | `production_space.address` | mitigate | the catalogue census over `pg_constraint`, `pg_views` and `pg_proc`, with a pre-registered expectation naming `promoted_venue_id → venues` as the one expected edge and its **direction** as the reason it is safe |
| T-45-02 | Information disclosure | server logs | mitigate | check E of the surface gate, and the field name deliberately unspelled so the grep is not reddened by its own prohibition |
| T-45-13 | Spoofing | a `formats.color` swatch drawn large | accept, with a named observer | no automatic check sees size; check D says so about itself, and P4 is the real gate |
| T-45-03 | Information disclosure | `45-PROCEDURES.md` | mitigate | roles never names; steps never observations; the grep for street and date tokens |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | no package is installed |
</threat_model>

<verification>
- `node scripts/verify-section-surface.mjs` exits 2 and names the missing directories.
- `node scripts/verify-section-export.mjs` exits 2 and distinguishes the two reasons.
- Check C of the export gate has been **shown to fire**, and the demonstration is recorded.
- `npm run verify` exits 0 with both refusals reported as refusals.
- **What a green does NOT mean:** at the end of this plan **nothing has been measured** by
  either gate. Two exit-2s are the correct outcome and the honest one; a 0 here would mean
  a gate went green on an absence.
</verification>

<success_criteria>
- Two gates exist, scoped to named directories and entry files, reading every file through the one comment stripper, refusing rather than passing when there is nothing to measure.
- The export gate can enumerate the absence of a road, not merely assert it, and its positive half has been shown to fire.
- Both gates are registered in `package.json` and in the aggregate, whose transcript treats a refusal as a refusal.
- `45-PROCEDURES.md` exists with four procedures, three named authorisations, every `Result: pending`, and no candidate anywhere in it.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-06-SUMMARY.md` when done.
</output>
