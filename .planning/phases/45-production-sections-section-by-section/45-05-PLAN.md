---
phase: 45-production-sections-section-by-section
plan: 05
type: execute
wave: 2
depends_on: ["45-03"]
files_modified:
  - src/lib/capabilities/keys.ts
  - src/lib/routes/capability-routes.ts
  - src/lib/routes/staff-tabs.ts
  - scripts/verify-capabilities.mjs
  - src/app/(admin)/admin/calendar/actions.ts
  - src/app/(admin)/admin/(work)/calendar/page.tsx
  - src/app/(admin)/admin/(work)/calendar/[id]/page.tsx
  - src/app/(admin)/admin/(work)/calendar/loading.tsx
  - src/app/(admin)/admin/(work)/calendar/[id]/loading.tsx
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "The four section keys exist in `CAP`, each with a description byte-identical to the migration's"
    - "The calendar's page guard, its actions and its tab all ask the calendar's section key, and none of the nine files still names `production.read`"
    - "The capability gate's pre-registered expectation counts seventeen keys, sixty-eight pairs, thirty-six grants and thirty-two refusals"
    - "`npm run build` passes, and `npm run verify:capabilities` is RED until the migrations are applied — declared here, in advance"
  artifacts:
    - path: "src/lib/capabilities/keys.ts"
      provides: "four CAP members and four CAP_DESCRIPTIONS entries"
      contains: "production.location.manage"
    - path: "src/lib/routes/capability-routes.ts"
      provides: "one binding per new key: the calendar keeps its two routes, the three new keys enter on the table-only branch"
    - path: "scripts/verify-capabilities.mjs"
      provides: "the pre-registered expectation, moved as a decision rather than to make a run pass"
  key_links:
    - from: "src/lib/capabilities/keys.ts"
      to: "supabase/migrations/20260817120000_production_section_keys.sql"
      via: "four description strings, byte for byte"
      pattern: "production\\.(calendar|manifesto|visual|location)\\.manage"
    - from: "src/lib/routes/staff-tabs.ts"
      to: "src/lib/routes/capability-routes.ts"
      via: "the module-load loop, which throws when a tab and the map disagree"
      pattern: "resolveRoute\\(tab.href\\)"
---

<objective>
The code half of the key split: four keys declared, four bindings, the calendar's own
sites moved off the old string, and the capability gate's pre-registered expectation moved
to what the new model will hold.

Purpose: this is the commit the deploy of plan 45-08 carries. Until it is live, the
retirement migration of plan 45-03 may not be applied — and this plan is where that
dependency becomes a code fact rather than a note.
Output: nine edited files and one declared red gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-RESEARCH.md
@.planning/phases/45-production-sections-section-by-section/45-PATTERNS.md
@.planning/phases/45-production-sections-section-by-section/45-03-SUMMARY.md
@supabase/migrations/20260817120000_production_section_keys.sql
@src/lib/capabilities/keys.ts
@src/lib/routes/capability-routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: keys.ts — four keys, four descriptions, and the reasons that must not be lost</name>
  <files>src/lib/capabilities/keys.ts</files>
  <read_first>
    - `src/lib/capabilities/keys.ts:110-159` — the docblock plan 44-04 wrote for the
      fourteenth key. It is the template for each of the three new ones and for the
      calendar's rename, including the **four rejected reuses** at `:118-143`, each with
      the *direction* of its mistake.
    - `src/lib/capabilities/keys.ts:38-45` — **named by the question, not by the
      predicate**, which is why four keys resolving to the same two grants are still four
      keys.
    - `src/lib/capabilities/keys.ts:214-236` — the three things the compiler holds, and the
      one it cannot: that these strings match the rows in `private.capabilities`. That is
      `verify-capabilities.mjs`'s job, and it needs a live database.
    - `supabase/migrations/20260817120000_production_section_keys.sql` — the four
      description strings. They are copied **byte for byte**, in this commit.
  </read_first>
  <action>
    Remove `PRODUCTION_READ` from `CAP` and its `CAP_DESCRIPTIONS` entry; add
    `PRODUCTION_CALENDAR_MANAGE`, `PRODUCTION_MANIFESTO_MANAGE`,
    `PRODUCTION_VISUAL_MANAGE` and `PRODUCTION_LOCATION_MANAGE`, with the four strings
    settled in plan 45-03.

    Each gets its own docblock, on the fourteenth key's model:
    · **what question it answers**, not what predicate it resolves to;
    · the **four rejected reuses** restated for that key with the direction of each
      mistake — `organizer.access`, `catalogue.manage`, `admin.access`, `staff.manage`;
    · `requires_approved = false` and **the bet**, restated in the same words the migration
      uses. A bet that is written in one of the two places is a bet that is lost when that
      place is edited.

    Write, once and where the next reader will meet it, the sentence this split exists for:
    the four keys resolve to the same two grants **today**, and they are four keys so that
    one of them can be taken away without the others — which a single `production.read`
    could not be. That is `keys.ts:38-45` applied to this phase, and it is the whole of
    D-45-04's justification.

    `CAP_DESCRIPTIONS` is a **total** `Record` over the union, so a key added without a
    description is a build error, and a description for a key that no longer exists is one
    too. Do not weaken that type to land the change in two steps.
  </action>
  <acceptance_criteria>
    - `grep -c "production.read" src/lib/capabilities/keys.ts` returns 0.
    - `grep -cE "production\.(calendar|manifesto|visual|location)\.manage" src/lib/capabilities/keys.ts` returns 8 (four in `CAP`, four in `CAP_DESCRIPTIONS`).
    - The descriptions match the migration byte for byte: for each of the four keys, the description string extracted from `keys.ts` is found verbatim in `supabase/migrations/20260817120000_production_section_keys.sql`. Record the four comparisons in the SUMMARY.
    - `grep -ci "bet on the signup path" src/lib/capabilities/keys.ts` returns 4.
    - `npm run build` fails at this point **only** with errors naming `CAPABILITY_ROUTES` — the totality error firing for a key with no binding. That is the compiler doing its job and it is repaired by task 2, not by weakening the type.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — the build is expected RED between this task and task 2. The evidence is the recorded error text, which must name the missing bindings and nothing else.</automated>
  </verify>
  <done>Four keys are declared with their reasons, the old key is gone from this file, and the only build error is the one the totality type exists to produce.</done>
</task>

<task type="auto">
  <name>Task 2: The bindings and the calendar's own sites</name>
  <files>src/lib/routes/capability-routes.ts, src/lib/routes/staff-tabs.ts, src/app/(admin)/admin/calendar/actions.ts, src/app/(admin)/admin/(work)/calendar/page.tsx, src/app/(admin)/admin/(work)/calendar/[id]/page.tsx, src/app/(admin)/admin/(work)/calendar/loading.tsx, src/app/(admin)/admin/(work)/calendar/[id]/loading.tsx</files>
  <read_first>
    - `src/lib/routes/capability-routes.ts:520-600` — the `PRODUCTION_READ` entry and its
      whole docblock, including the **branch change** paragraph: plan 44-04 landed the key
      on `scope: "table"` because the page was not on disk yet, and plan 44-09 moved it.
      That is exactly the move the three new keys make in reverse order, in plans 45-11 and
      45-12.
    - `src/lib/routes/capability-routes.ts:154-166` — `alsoGatesTables` is **optional on
      that branch**, so omitting it produces no error at all. The comment's count of *six
      of the fourteen* moves in this phase and must be re-counted, not adjusted.
    - `src/lib/routes/capability-routes.ts:604-651` — `Unbound = Exclude<StaffRoute,
      Listed>`. **A page on disk with no entry here is a build error; an entry whose page
      is not on disk is silent.** That asymmetry is what decides the order of this phase's
      surface plans.
    - `src/lib/routes/staff-tabs.ts:132-153` — the Calendar tab and the *hiding this tab
      protects nothing* paragraph; `:173-198` — the module-load loop that throws when a tab
      and the map disagree.
    - `45-RESEARCH.md` §A2 — the table of all 22 sites in 9 files.
  </read_first>
  <action>
    **`capability-routes.ts`.** Move the calendar's entry to
    `[CAP.PRODUCTION_CALENDAR_MANAGE]`, unchanged in substance: the same two routes, and
    `alsoGatesTables: true` carried across — it is optional on that branch, so omitting it
    is a declaration that lies by omission.

    Add three entries on the **`scope: "table"` branch**, each with its mandatory one-line
    `reason`, for `production.manifesto.manage`, `production.visual.manage` and
    `production.location.manage`. Their pages are not on disk yet, and a page bound to a
    `scope: "table"` key is unreachable **for everyone** with no build error and nothing in
    a log, because `resolveRoute` returns `null` and the middleware fails closed. Write
    that trap into each entry, and write **which plan moves it** — 45-11 for location,
    45-12 for manifesto and visual — so the obligation is discharged rather than remembered.

    Re-derive, do not inherit, the ambiguity paragraph, using the pattern list recorded in
    `45-03-SUMMARY.md`, and re-count `alsoGatesTables` by reading the file rather than by
    adding to the printed figure.

    **`staff-tabs.ts`.** One line: the Calendar tab's `capability:` becomes
    `CAP.PRODUCTION_CALENDAR_MANAGE`. **No new tab is added here** — `StaffTab.href` is
    `Route`, a static address enters the generated union only once a `page.tsx` serves it,
    and the two workarounds are rejected in writing at `:117-126` and stay rejected. The
    three new tabs are plan 45-18.

    **The calendar's seven remaining sites.** `assertProductionRead()` and its two log
    strings in `admin/calendar/actions.ts`; the two page guards and their docblock
    references; the two `loading.tsx` docblocks. Rename the helper to match the key it now
    asks — a helper called `assertProductionRead` that asks
    `production.calendar.manage` is a name that will be believed over the code it sits on.
    Keep the two **different** throw categories exactly as they are: a refusal on the
    merits and an unresolvable identity, which *is not a refusal on the merits*.

    **Change nothing else in those files.** This is a rename with a declared reach, and a
    second change inside it would make the reach unauditable.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -rn "production\.read\|PRODUCTION_READ" --include='*.ts' --include='*.tsx' src` returns 0 lines.
    - `grep -c "scope: \"table\"" src/lib/routes/capability-routes.ts` increases by exactly 3 against its pre-change value, and each new entry carries a `reason` string.
    - The calendar binding keeps both routes and the flag: `grep -A3 "PRODUCTION_CALENDAR_MANAGE" src/lib/routes/capability-routes.ts` shows `"/admin/calendar"`, `"/admin/calendar/[id]"` and `alsoGatesTables: true`.
    - No new tab: `grep -c "href:" src/lib/routes/staff-tabs.ts` is unchanged.
    - `npm run build` exits 0.
    - `npm run verify:routes` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run verify:routes</automated>
  </verify>
  <done>Four keys have bindings, the calendar's whole reach has moved to its section key, no tab was added, and both the compiler and the route gate agree.</done>
</task>

<task type="auto">
  <name>Task 3: The pre-registered expectation — scripts/verify-capabilities.mjs</name>
  <files>scripts/verify-capabilities.mjs</files>
  <read_first>
    - `scripts/verify-capabilities.mjs:190-200` — `EXPECTED_KEY_COUNT` and the instruction
      beside it: **if this trips, look at the capability model, not at this constant.**
    - `scripts/verify-capabilities.mjs:500-549` — side 5, the declared refusals, the
      arithmetic, and the recorded shape of the failure this constant exists to catch:
      *lowering a total to make a run pass*, which mutation C of plan 43-02 did in two steps.
    - `45-RESEARCH.md` §A5 — the four constants, today's values and the values after the
      split, and the **two intervals in which this gate is RED**.
  </read_first>
  <action>
    Move the four constants to the values the split produces, and write **beside each one**
    why it moved — a capability was added or removed as a design decision, with the plan
    number, in the shape the file already uses for the move from 13 to 14.

    `EXPECTED_KEY_COUNT` 14 → 17. `EXPECTED_PAIR_COUNT` 56 → 68 (17 × 4).
    `EXPECTED_GRANT_COUNT` 30 → 36 (30 − 2 + 8). `EXPECTED_REFUSAL_COUNT` 26 → 32 (68 − 36).
    **Recompute each from the `ROLE_GRANTS` table rather than from the arithmetic above**,
    and if a number disagrees, the table is the fact and this paragraph is the error.

    In `ROLE_GRANTS`: remove the `production.read` row; add four rows, each granting
    `master` and `organizer` and declaring `staff` and `member` **REFUSED**, with the
    refusal paragraph the file already writes for the calendar — a member holding one of
    these keys would read every space under negotiation the moment it was typed, and
    `PROJECT.md` says the gating mechanism **is** the product.

    **Declare the red, in the file and in the SUMMARY.** This gate needs a live database.
    From this commit until the additive migration is applied (plan 45-08) it is red because
    the database holds 14 keys and this file declares 17; between that application and the
    retirement (plan 45-09) it is red because the database holds 18. Both reds are the gate
    doing its job. **Neither is repaired by editing a constant** — that is the exact failure
    the file's own comment names.
  </action>
  <acceptance_criteria>
    - `grep -c "EXPECTED_KEY_COUNT = 17" scripts/verify-capabilities.mjs` returns 1, and the same for 68 / 36 / 32 on their constants.
    - `grep -c "production.read" scripts/verify-capabilities.mjs` returns 0 outside a comment recording the removal; verify with `grep -vE "^\s*(\*|//)" scripts/verify-capabilities.mjs | grep -c "production.read"` returning 0.
    - Four new refusal declarations exist: `grep -cE "'production\.(calendar|manifesto|visual|location)\.manage'" scripts/verify-capabilities.mjs` returns 8 or more.
    - `npm run verify` exits 0 — the aggregate registers `verify:capabilities` as OFFLINE-needing-credentials and its refusal is not a failure.
    - `npm run verify:capabilities` exits **2** (no credentials) or **1** (credentials, model not yet applied). Record which, and record that a `0` here at this moment would mean the database already holds seventeen keys — which would itself be a finding.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run verify</automated>
  </verify>
  <done>The pre-registered expectation describes the model this phase is building, every moved number carries its reason, and both red intervals are declared in advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the declared key set → the applied key set | The compiler holds two directions of the declaration and cannot hold the third: that these strings exist as rows. Only a live catalogue read can |
| `capability-routes.ts` → the middleware bundle | The module-load throw fires on the first request after a deploy, not at `npm run build` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Denial of service | the calendar surface between this commit and the deploy | mitigate | the additive migration leaves `production.read` granted, so nothing is refused at any instant; the retirement waits for the deploy and its own gate in plan 45-09 |
| T-45-05 | Denial of service | the middleware bundle | mitigate | the ambiguity paragraph is **re-derived** from the pattern list recorded in plan 45-03, not inherited; `npm run verify:routes` is an acceptance criterion; the deploy rule — a day without a night, and make the first request yourself — is carried into plan 45-08 |
| T-45-04b | Elevation of privilege | the three new bindings | mitigate | they land on the table-only branch with a mandatory `reason`, and each names the plan that moves it; a page bound to a table-only key is unreachable for **everyone** with no error, and that trap is written into each entry |
| T-45-12 | Repudiation | the pre-registered expectation | mitigate | every moved number carries its reason and its plan; the recorded failure shape — *lowering a total to make a run pass* — is named beside them |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | no package is installed |
</threat_model>

<verification>
- `npm run build` exits 0 at the end of task 2 and stays 0.
- `npm run verify:routes` exits 0.
- `npm run verify` exits 0.
- `/usr/bin/grep -rn "production\.read" --include='*.ts' --include='*.tsx' src` returns nothing.
- **What a green does NOT mean:** the build and the route gate both pass against a database
  that still holds one key called `production.read` and none of the four declared here. The
  types come from a hand-written file, not from the live database. `verify:capabilities` is
  the one automatic check that can see the difference, and it is **red on purpose** from
  this commit until plan 45-09.
</verification>

<success_criteria>
- Four keys in `CAP` with descriptions byte-identical to the migration's, and the four rejected reuses restated per key.
- One binding per key; the calendar keeps its two routes and its `alsoGatesTables`; the three new keys wait on the table-only branch, each naming the plan that moves it.
- No occurrence of `production.read` left under `src/`.
- The capability gate's four constants moved with their reasons, and both red intervals declared before either arrives.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-05-SUMMARY.md` when done.
</output>
