---
phase: 45-production-sections-section-by-section
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260817120000_production_section_keys.sql
  - supabase/migrations/20260817120500_production_read_retire.sql
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "Four section keys exist under one naming schema, and the calendar is one of the four — three new ones for the three sections D-45-01 admits (manifesto, visual, location), and no key for a section outside them"
    - "D-45-02 is what these keys ARE: entitlement granted to the role, not to the person — there is no per-subject grant table anywhere in this file, and adding one later would be a model change, not an extension"
    - "The six SELECT policies on the calendar's tables ask the calendar's own section key, with the `(SELECT …)` wrapper intact"
    - "The two roles that hold the calendar before the split hold it after — the grants neither narrow nor widen"
    - "At no instant of the sequence is an entitled reader refused anything: the additive file leaves `production.read` granted"
    - "The retirement removes the old key **by key**, never by a prefix selector"
  artifacts:
    - path: "supabase/migrations/20260817120000_production_section_keys.sql"
      provides: "four capability rows, eight grants, six rewritten SELECT policies, in one transaction"
      contains: "production.calendar.manage"
    - path: "supabase/migrations/20260817120500_production_read_retire.sql"
      provides: "the retirement of production.read, by key, idempotent"
      contains: "WHERE key = 'production.read'"
  key_links:
    - from: "20260817120000_production_section_keys.sql"
      to: "private.role_capabilities"
      via: "eight INSERTs, four keys x {master, organizer}, ON CONFLICT DO NOTHING"
      pattern: "INSERT INTO private.role_capabilities"
    - from: "the six rewritten policies"
      to: "private.has_capability"
      via: "the InitPlan wrapper, which is what makes the call once-per-statement"
      pattern: "\\(SELECT private.has_capability\\("
---

<objective>
The key split, as SQL: one additive migration that mints four section keys and moves the
six calendar policies onto one of them **while leaving the old key granted**, and one
small retirement migration that removes the old key afterwards.

Purpose: **this is the phase's only edit to something already live**, and it is written in
its own plan so the rest of the phase is neither blamed for it nor blocked by it. The
sequence exists because both single-transaction orders leave a window in which an entitled
master is refused, and this one leaves none.
Output: two migration files, applied by nobody yet, plus the four key names and the four
addresses settled and recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-RESEARCH.md
@.planning/phases/45-production-sections-section-by-section/45-CONTEXT.md
@.planning/phases/45-production-sections-section-by-section/45-PATTERNS.md
@supabase/migrations/20260815120100_production_calendar_access.sql
@supabase/migrations/20260815120200_production_checklist_tick_revoke.sql
@src/lib/capabilities/keys.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settle the four key names and the four addresses, against the pattern list</name>
  <files>(no repository file changes — this task settles two naming decisions and records them)</files>
  <read_first>
    - `src/lib/capabilities/keys.ts:38-45` — **named by the question, not by the
      predicate**. Three existing keys resolve to the same predicate and are deliberately
      three keys, because a key named after its predicate cannot later be taken away on
      its own.
    - `src/lib/capabilities/keys.ts:194` — `membership.card.view`, the existing
      three-segment precedent.
    - `src/lib/routes/capability-routes.ts:553-569` — the ambiguity paragraph, and **when**
      the throw fires: at module load **inside a middleware bundle**, so a tie is a 500 on
      the payments webhook and on the door's scan path, not a broken page.
    - `.planning/todos/pending/module-load-throws-500-the-whole-middleware-surface.md` — it
      is about this exact file.
  </read_first>
  <action>
    **The keys.** Adopt the three-segment schema `production.<section>.manage`:
    `production.calendar.manage`, `production.manifesto.manage`,
    `production.visual.manage`, `production.location.manage`. It keeps the grouping prefix,
    it is precedented by `membership.card.view`, and `manage` is honest about D-45-06 —
    one key covers read **and** write, so `read` would under-claim.

    **The addresses.** `/admin/calendar` and `/admin/calendar/[id]` are unchanged.
    The three new ones are `/admin/location`, `/admin/location/[id]`, `/admin/manifesto`
    and `/admin/visual`.

    **Check them before anything writes them anywhere.** Run
    `node scripts/verify-routes.mjs --print-patterns`, read the list, and confirm in
    writing, pattern by pattern, that none of the four new addresses ties with an existing
    one: a tie is decided segment by segment, and the loop compares literal against
    dynamic. If a tie exists, **stop the plan and report it** — do not rename around it
    silently, because the address is about to be written into four files and a written
    procedure.

    Record both decisions in the SUMMARY, with the printed pattern list, so every later
    plan copies rather than re-derives.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-routes.mjs --print-patterns` exits 0 and its output is recorded verbatim in `45-03-SUMMARY.md`.
    - The SUMMARY states, for each of `/admin/location`, `/admin/location/[id]`, `/admin/manifesto`, `/admin/visual`, which existing patterns share its segment count and why there is no tie.
    - The four key strings are recorded exactly as they will be written into the migration and into `keys.ts`, character for character.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-routes.mjs --print-patterns</automated>
  </verify>
  <done>Four key names and four addresses are settled, the pattern list is recorded, and no tie exists.</done>
</task>

<task type="auto">
  <name>Task 2: The additive migration — 20260817120000_production_section_keys.sql</name>
  <files>supabase/migrations/20260817120000_production_section_keys.sql</files>
  <read_first>
    - `supabase/migrations/20260815120100_production_calendar_access.sql` — **in full,
      including the prose.** It is both the analog and the subject. In particular `:16-22`
      and `:52` (why one transaction), `:76-81` (the capability row), `:83-115` (the grants
      and the four ⚠ paragraphs), `:117-138` (the declared refusals and where they are
      asserted), `:154-160` and `:176-181` (the policy arm and why the `(SELECT …)` wrapper
      is load-bearing), `:236-244` (the uniformity paragraph), `:253-284` (the paragraph
      declaring the absent write policies a decision).
    - `supabase/migrations/20260807000000_capability_model.sql:177-184` — `EXPLAIN`
      disproved the belief that `STABLE` makes the call once per statement. **It is the
      InitPlan wrapper that does.** 26 policies in this repo were written unwrapped on the
      strength of the wrong comment; these six were not.
    - `src/lib/capabilities/keys.ts:118-143` — the four rejected reuses, each with the
      **direction** of its mistake.
    - `45-RESEARCH.md` §A1 (the measured live state), §A3 (how a key resolves), §A4 (why
      additive-then-retire), §A6 (the naming schema and what the prose must carry).
    - `45-CONTEXT.md` D-45-04 and **D-45-20**.
  </read_first>
  <action>
    One transaction. Copy the *why one transaction* reasoning, not just the `BEGIN`: a
    half-applied version is worse than none of it, because a capability row without its
    grants resolves `false` for **everyone**, the master included.

    **Four capability rows**, `INSERT … ON CONFLICT (key) DO NOTHING`. Each description
    string is written here **and byte-identically in `CAP_DESCRIPTIONS`** in plan 45-05;
    say so in the file, as the analog does at `:58-65`.

    **Eight grants**, four keys × {`master`, `organizer`}, all `requires_approved = false`,
    `ON CONFLICT (role, capability) DO NOTHING`. Carry the four ⚠ paragraphs across:

    · The value is **the owner's call and not the planner's** — D-45-20 for the three new
      keys, D-44-27 for the calendar's, which constraint 3 forbids changing.
    · **It is a bet, written down here rather than left to be discovered.** Restate the
      sentence verbatim in substance: *that is a BET on the signup path staying closed —
      reopen a path that can create a `pending` organizer and this flag is reconsidered in
      the same commit.*
    · It must **not** be defended by pointing at `door.operate` or `staff.manage`: those
      two carry `false` to keep a person from being refused in front of a queue, and
      nobody is standing in a queue in front of a manifesto.
    · The grants **do not narrow and do not widen**. Master and organizer hold the
      calendar today and hold it after. A key rename that changed who can read would be
      two changes in one diff, auditable against neither question.

    **The declared refusals as prose**, one paragraph per key: `staff` refused (and it is
    not one of the four a per-night assignment may carry —
    `20260809000000_party_assignments.sql:340-342`); `member` refused, nothing grants it;
    `anon` refused by construction, because `private.has_capability` answers false for a
    null `auth.uid()` (`20260807000000_capability_model.sql:55-57`). Name **where** they
    are asserted rather than believed: `scripts/verify-capabilities.mjs`, side 5, in plan
    45-05's commit.

    **The four rejected reuses**, restated for the section keys with the direction of each
    mistake: `organizer.access`, `catalogue.manage`, `admin.access`, `staff.manage`. A
    rewrite that carries the policies and drops the reasons is how the next reader
    "repairs" the gap with one `CREATE POLICY`.

    **Six policies rewritten**, on `production_plan`, `production_piece`,
    `production_commitment`, `production_checklist_item`, `production_import_run` and
    `production_pipeline_rule`: `DROP POLICY IF EXISTS` then `CREATE POLICY` asking
    `production.calendar.manage`, **each wrapped as `((SELECT private.has_capability(…)))`**.
    A rewritten policy whose `qual` in `pg_policies` no longer begins `( SELECT ` has lost
    the InitPlan, and the read-back in plan 45-08 is where that is caught.

    Carry across, in substance rather than by reference, the two paragraphs that would
    otherwise be lost: the **uniformity** paragraph (a single table readable without the
    key would be the one somebody joins the others onto, and *this table is harmless on its
    own* is how a read path is opened by degrees), and the **absent write policies**
    paragraph, ending on its own sharpest sentence — *the service client is not a boundary;
    it is the absence of one*, and what decides who may write is the guard inside the
    server action, which saying so here does not enforce.

    **`production.read` and its two grants stay in place in this file.** That is the entire
    reason the sequence has no window: during the interval between this migration and the
    deploy, the old code's guard asks `production.read` (still granted → true) and the six
    policies ask the calendar key (granted to the same two roles → true). Nobody is refused
    anything. Write that paragraph; it is the file's justification.

    **NO MATERIAL.** No venue, no date, no line-up, and no candidate.
  </action>
  <acceptance_criteria>
    - One transaction: `grep -c "^BEGIN;" …` returns 1, `grep -c "^COMMIT;" …` returns 1.
    - Four keys: `grep -cE "'production\.(calendar|manifesto|visual|location)\.manage'" …` returns 18 or more (four rows, eight grants, six policies).
    - Eight grants, all `false`: the `INSERT INTO private.role_capabilities` block contains exactly eight value tuples and no `true`.
    - Six policies, all wrapped: `grep -c "CREATE POLICY" …` returns 6 and `grep -c "(SELECT private.has_capability(" …` returns 6.
    - **No DELETE anywhere in this file:** `grep -ci "delete" …` returns 0.
    - The bet is restated: `grep -ci "bet on the signup path" …` returns 4 (one per key description).
    - `grep -c "DROP POLICY IF EXISTS" …` returns 6 — the six existing arms are replaced, not doubled. A second permissive policy beside an existing one widens rather than replaces.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this file is applied in plan 45-08 and no runner reads SQL here. The mechanical evidence is the greps above; the applied evidence is the `pg_policies` read-back.</automated>
  </verify>
  <done>Four keys, eight grants, six rewritten and wrapped policies, the old key untouched, and every reason the old file carried carried forward rather than deleted.</done>
</task>

<task type="auto">
  <name>Task 3: The retirement migration — 20260817120500_production_read_retire.sql</name>
  <files>supabase/migrations/20260817120500_production_read_retire.sql</files>
  <read_first>
    - `supabase/migrations/20260815120200_production_checklist_tick_revoke.sql:5-52` — the
      whole file. It is the repository's smallest complete example of *a correction that
      travels forward*: the applied migration is **not** edited, because rewriting a file
      that has already run makes the history a description of something that never
      happened. And its idempotence sentence is the model.
    - `.claude/rules/ai-engineering.md`, *gate una rimozione si fa per chiave*.
    - `45-RESEARCH.md` §A4, the retirement paragraph: this is the file an auditor reads to
      answer *did the reach change*, and the answer is read off two `INSERT` blocks and one
      `DELETE`, none of which touch a role list.
  </read_first>
  <action>
    A small, separate, auditable migration. In one transaction:

    · `DELETE FROM private.role_capabilities WHERE (role, capability) = ('master',
      'production.read')` and the same for `organizer` — **by the pair**, one statement
      each, never a set-returning predicate.
    · `DELETE FROM private.capabilities WHERE key = 'production.read'` — **by key.**

    **Never `LIKE 'production%'`.** After this phase that pattern matches the four new
    keys, and the harmful repair is named here so it is recognised if it is ever proposed.

    Grants first, then the row: the reverse order leaves a grant referencing a key that no
    longer exists for the length of the statement, and the foreign key would refuse it
    anyway — say which of the two it is, having read the constraint, rather than guessing.

    **Idempotent:** a `DELETE` of an absent row is a no-op, so running this twice changes
    nothing the second time. Write the sentence.

    **The precondition, in the header and in capitals:** this file may only be applied
    **after** the deploy that carries `keys.ts`, `capability-routes.ts`, `staff-tabs.ts`
    and the new pages is live. Applied before it, the old bundle asks `production.read`,
    `has_capability` answers false with no error and no log line, and `/admin/calendar`
    becomes a redirect to `/dashboard` for the master — a surface that is down for the
    length of a deploy in a repository with **no error tracking**, which nobody would learn
    about except by opening it.

    **NO MATERIAL.**
  </action>
  <acceptance_criteria>
    - `grep -c "WHERE key = 'production.read'" …` returns 1.
    - No prefix selector: `grep -ci "like 'production" …` returns 0 outside the paragraph that forbids it — verify with `grep -vE "^\s*--" … | grep -ci "like"` returning 0.
    - Exactly three `DELETE` statements: `grep -vE "^\s*--" … | grep -c "DELETE FROM"` returns 3.
    - The precondition appears in the header: `grep -ci "after the deploy" …` returns 1 or more.
    - The file names no table other than `private.capabilities` and `private.role_capabilities`: `grep -vE "^\s*--" … | grep -c "public\." ` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — applied in plan 45-09, under its own gate. The mechanical evidence is the greps above.</automated>
  </verify>
  <done>The old key is retired by key and by pair, idempotently, with the deploy precondition written where whoever applies it will read it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a capability key string → the live access verdict | A key that does not exist resolves `false` with no error, no log line and nothing in this product that would report it |
| the migration file → the deployed bundle | The two hold the same strings, and the interval between them is the only place a window can open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Denial of service | the six calendar policies and the calendar page guard | mitigate | the additive file leaves `production.read` granted, so both the old guard and the new policies answer true throughout the window; the retirement is a separate file with its precondition in its header |
| T-45-04b | Elevation of privilege | the eight grants | mitigate | the same two roles on all four keys, eight tuples, no `true`; the refusals of `staff`, `member` and `anon` are declared in prose here and **asserted** in `verify-capabilities.mjs` side 5 in plan 45-05 |
| T-45-10 | Elevation of privilege | the six rewritten policies | mitigate | `DROP` then `CREATE`, never a second permissive policy beside an existing one; the uniformity paragraph carried across so nobody reasons *this table is harmless on its own* |
| T-45-05 | Denial of service | the four addresses | mitigate | `verify-routes.mjs --print-patterns` read **before** any file names an address; a tie throws at module load inside the middleware bundle, which is the payments webhook and the door |
| T-45-03 | Information disclosure | these two files, tracked and public | mitigate | NO MATERIAL headers; the greps |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | no package is installed |
</threat_model>

<verification>
- `node scripts/verify-routes.mjs --print-patterns` exits 0, output recorded.
- The greps of tasks 2 and 3, run and recorded.
- `npm run build` exits 0 — it proves only that nothing else broke: **no build reads a
  `.sql` file**, and neither does any test, because there is no test runner.
- **What a green does NOT mean:** nothing here has been applied. `verify:capabilities` is
  still green against the *old* model at this point, because the database still holds
  fourteen keys and `keys.ts` still declares fourteen. It goes red in plan 45-05 and
  returns green only in plan 45-09. That is the gate working, declared in advance.
</verification>

<success_criteria>
- Four key names and four addresses settled, with the printed pattern list recorded and no tie.
- One additive migration: four rows, eight grants, six wrapped policies, the old key left in place, every reason carried across.
- One retirement migration: three deletes, by key and by pair, idempotent, with the deploy precondition in its header.
- Neither file contains a candidate, an address, a date or a line-up.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-03-SUMMARY.md` when done.
</output>
