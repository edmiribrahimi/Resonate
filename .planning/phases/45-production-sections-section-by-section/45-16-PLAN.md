---
phase: 45-production-sections-section-by-section
plan: 16
type: execute
wave: 7
depends_on: ["45-06", "45-15"]
files_modified:
  - src/lib/production/export/manifesto.ts
  - src/lib/production/export/capitolato.ts
  - src/app/(admin)/admin/manifesto/export-actions.ts
  - src/app/(admin)/admin/visual/export-actions.ts
  - src/app/(admin)/admin/manifesto/ExportPanel.tsx
  - src/app/(admin)/admin/(work)/manifesto/page.tsx
  - src/app/(admin)/admin/(work)/visual/page.tsx
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "Pressing export on the manifesto yields the manifesto; pressing it on the capitolato yields the capitolato"
    - "Neither export can carry an address or an unannounced date, because neither module reads a table that holds one — proved by a closure walk, not by care"
    - "The location section has no export, and the gate fails by name if one appears"
    - "The capitolato's palette values are read from the token file at runtime, and no brand hex literal enters `src/`"
    - "`npm run verify:section-export` is green for the first time"
  artifacts:
    - path: "src/lib/production/export/manifesto.ts"
      provides: "the manifesto document, with its readable tables declared at the top of the file"
    - path: "src/lib/production/export/capitolato.ts"
      provides: "the capitolato, palette included, values read rather than carried"
  key_links:
    - from: "src/lib/production/export/capitolato.ts"
      to: "src/lib/production/sections/tokens.ts"
      via: "the runtime :root read, so no literal exists for check B to find"
      pattern: "from \"@/lib/production/sections/tokens\""
    - from: "scripts/verify-section-export.mjs"
      to: "the two entry modules"
      via: "the import closure, walked from each entry"
      pattern: "src/lib/production/export"
---

<objective>
The two documents built to leave the perimeter, and the narrowness that is structural
rather than careful.

Purpose: D-45-17 says the export *cannot carry an address or an unannounced date because it
does not read those tables, not because whoever presses it is careful*, and that a plan
proves it **by showing what it cannot reach**. The gate that does the showing was written in
plan 45-06 and has been refusing ever since. This plan gives it something to measure and
turns it green.
Output: two serialisation modules, two thin action arms, one panel, two pages wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-CONTEXT.md
@.planning/phases/45-production-sections-section-by-section/45-06-SUMMARY.md
@.claude/rules/venue-secrecy.md
@.claude/rules/sound-manifesto.md
@.claude/rules/brand-visual-system.md
@scripts/verify-section-export.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: The two serialisation modules — declared tables, and nothing else reachable</name>
  <files>src/lib/production/export/manifesto.ts, src/lib/production/export/capitolato.ts</files>
  <read_first>
    - `scripts/verify-section-export.mjs` (plan 45-06) — **read it before writing these two
      files.** Its five checks are the contract: the closure contains no module that queries
      a forbidden table; the entries contain no forbidden `.from()`; each entry **names its
      own tables**, which is the positive half; the forbidden list is derived rather than
      typed; and no reached module imports the service client.
    - `.claude/rules/sound-manifesto.md` — *gate il brief esce dal perimetro*: the manifesto
      is written for whoever goes to the console, so before it is handed over it must not
      carry secret venues, unannounced dates or other line-ups.
    - `.claude/rules/brand-visual-system.md` — what the capitolato carries: fixed and
      variable, the grid-safe square, the date legible in a thumbnail, the publication order
      and its inversion in the grid, the typography and its **declared degradation**,
      `@ Secret Venue`, and *lo spazio non acquisito non si nomina*.
    - `.claude/rules/venue-secrecy.md` — *il capitolato è un percorso di uscita*. This is that
      exit route, built once and built tight.
    - `src/lib/production/sections/tokens.ts` (plan 45-12) — the runtime `:root` reader.
    - `45-RESEARCH.md` § Don't Hand-Roll — **no PDF dependency exists and none is added.**
      Markdown, or a print-ready view. A new package is supply-chain surface bought for a
      document the owner can print.
  </read_first>
  <action>
    Two modules, **Markdown output**, each with a **declared table list at the top of the
    file** — a comment block naming exactly what it reads, which is the sentence the gate's
    positive check reads back:

    · `manifesto.ts` — reads `production_section` where `section = 'manifesto'`, and
      `formats` for the name and code. **Nothing else.**
    · `capitolato.ts` — reads `production_section` where `section = 'visual'`, `formats`, and
      the **token file**, through the runtime reader. **Nothing else.**

    Each takes an already-constructed **cookie-bound** client as an argument, or performs its
    own read with one — and **imports no service client**, which is check E: an export that
    bypasses the policy of the section it exports is an export that can carry another
    section's rows.

    **What the manifesto document contains and what it must not.** The three states, rendered
    as three different things: a written manifesto in full; a `coordinates_declared` section
    as its declared coordinates **including the explicit negative**, because exclusions are
    decisions as much as inclusions and a brief that dropped them would let a selector
    re-derive them wrongly; a `not_decided` section as **what is missing and whose call it
    is** — because the document's honest answer is *not yet defined*, and inventing one would
    write the brand at the place of whoever owns it.

    Open questions travel with it, marked as open. They warn and block nothing, and a brief
    that hid them would let one resolve itself by habit.

    **What the capitolato contains.** The rules, and the palette. The palette's values come
    from `tokens.ts` at runtime — **there is no hex literal in this file**, and there is no
    third exemption added to `verify:semantic-separation`. A format with **no palette** is
    rendered as the void plus the interim rule: the materials stay neutral. Its
    identification colour does not appear.

    **Two prohibitions, written where they would be violated:**
    · **no venue name and no address.** The capitolato goes to an external designer;
      `@ Secret Venue` is what it says instead, and a space not acquired is not named at all.
      The module has no query that could reach one, which is what makes this structural.
    · **no unannounced date.** The document carries rules, not a calendar. If it ever needs
      an anchor, it names the anchor by its rule — *the listing goes out on the Tuesday* —
      and never a date.

    **And the location section has no export.** No third module. Say it here, once, so the
    absence is a decision in the reader's hands, and let the gate fail by name if a
    `location*.ts` ever appears beside these two.
  </action>
  <acceptance_criteria>
    - `npm run verify:section-export` exits 0 — **its first green**, and its transcript shows check C passing on the positive half rather than vacuously.
    - No forbidden `.from()`: the transcript's check B lists what it looked for and found nothing.
    - No service client: `grep -c "supabase/service" src/lib/production/export/*.ts` returns 0.
    - No hex literal: `npm run verify:semantic-separation` exits 0 and the exemption array is unchanged.
    - No PDF dependency was added: `git diff package.json` is empty for `dependencies`.
    - Each file's declared table list is present as a comment block and matches what its queries actually name.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:section-export &amp;&amp; npm run verify:semantic-separation</automated>
  </verify>
  <done>Two documents can be produced, each reading only its own section's tables, neither able to reach an address or a date, and the gate that says so is green for the first time.</done>
</task>

<task type="auto">
  <name>Task 2: The two action arms and the panel that presses them</name>
  <files>src/app/(admin)/admin/manifesto/export-actions.ts, src/app/(admin)/admin/visual/export-actions.ts, src/app/(admin)/admin/manifesto/ExportPanel.tsx, src/app/(admin)/admin/(work)/manifesto/page.tsx, src/app/(admin)/admin/(work)/visual/page.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/manifesto/actions.ts` and `src/app/(admin)/admin/visual/actions.ts`
      (plan 45-15) — the gate shape. These arms are **separate files** for the same reason
      those modules are separate: every export of a `"use server"` module is a public
      endpoint, and one shared export module would publish the manifesto's export to a holder
      of the visual key.
    - `src/app/(admin)/admin/calendar/actions.ts:50-60` — refusals are **returned values**;
      Next redacts a message thrown out of a Server Action in a production build.
    - `src/components/ui/Button.tsx`, `Card.tsx` — the primitives.
  </read_first>
  <action>
    **Two thin arms.** Each asks **its own section's key first and once**, through its own
    unexported gate, then calls the matching serialisation module and returns
    `{ ok: true, filename, markdown }` or a named refusal. The arm contains no query of its
    own — the module owns the read, so that the closure the gate walks is the whole story.

    **The filename carries no date and no venue**: it names the document and the format,
    nothing else. A filename is a string somebody pastes into a message.

    **`ExportPanel.tsx`.** One control, one document. It renders the Markdown into a
    downloadable blob **on the client**, from the string the action returned — no new route,
    no new endpoint, and no third address for `capability-routes.ts` to bind.

    Beside the control, one sentence the person pressing it needs to read, from
    `sound-manifesto.md`'s own gate: **this document leaves the perimeter.** Before handing
    it over, check it carries no secret venue, no unannounced date and no other line-up. The
    module cannot reach the first two by construction; **the third can be typed into a body
    by an author**, and that is why the sentence is a sentence and not a claim of proof.

    The outcome — produced, or refused with its cause — is reported **in the panel**, not in a
    transient notification.

    Mount the panel on both pages, passing each page's own arm as a prop. One component, two
    callers.
  </action>
  <acceptance_criteria>
    - Two separate arms, each asking one key: `grep -c "CAP.PRODUCTION_VISUAL_MANAGE" src/app/(admin)/admin/manifesto/export-actions.ts` returns 0, and the mirror.
    - Neither arm queries: `grep -cE "\.from\(" src/app/(admin)/admin/*/export-actions.ts` returns 0.
    - No new route was added: `git diff src/lib/routes/capability-routes.ts` is empty, and `npm run verify:routes` exits 0.
    - The warning sentence is present beside the control: `grep -ci "leaves the perimeter" src/app/(admin)/admin/manifesto/ExportPanel.tsx` returns 1.
    - No transient notification: `grep -cE "toast|Toast" src/app/(admin)/admin/manifesto/ExportPanel.tsx` returns 0.
    - One component, two callers: `/usr/bin/grep -rln "ExportPanel" src` shows one definition and two importers.
    - `npm run build` exits 0; `npm run verify` exits 0; `npm run verify:dialogs` exits 0; `npm run verify:touch-targets` exits 0.
    - `npm run verify:section-export` still exits 0 — the arms are inside the closure now, and a query added to one of them would redden it.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify &amp;&amp; npm run verify:section-export</automated>
  </verify>
  <done>Each section exports itself and only itself, through its own key, with no new address, and the person pressing the control reads what the document is about to do.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the export module → whoever receives the document | The manifesto goes to whoever plays; the capitolato to the external designer. Both leave the perimeter **by design**, which is why narrowness has to be a property of the code |
| an author's prose → the document | The one channel the closure walk cannot close: a line-up typed into a body travels with the body |
| a `"use server"` export → the public internet | Two arms, not one shared module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09 | Information disclosure | the export path | mitigate | declared table lists; a closure walk from each entry; a derived forbidden list; the positive half shown to fire; no service client in any reached module; **no third entry for the location section**, and the gate fails by name if one appears |
| T-45-01 | Information disclosure | an address in a document that leaves | mitigate | structural — neither module reads a table holding one, and neither arm queries at all. `@ Secret Venue` is what the capitolato says instead |
| T-45-18 | Information disclosure | a line-up typed into a body | accept, with a named human step | no code path can see it; the panel carries the sentence from `sound-manifesto.md` requiring the brief be checked before it is handed over. Stated as a limit rather than claimed as a proof |
| T-45-14 | Information disclosure | a second home for a brand hex | mitigate | values read at runtime from the token file; the exemption array asserted unchanged |
| T-45-04 | Elevation of privilege | the two arms | mitigate | one key each, gates unexported, no shared module |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | **no package is installed**, and the temptation is named: a PDF library was weighed and refused. If a later plan proposes one, the legitimacy gate runs first and the install sits behind a blocking human checkpoint |
</threat_model>

<verification>
- `npm run verify:section-export` exits 0 — the first green, with check C non-vacuous.
- `npm run verify:semantic-separation` exits 0 with the exemption array unchanged.
- `npm run build`, `npm run verify`, `npm run verify:routes` all exit 0.
- **What a green does NOT mean:** the gate proves what the code **can reach**. It cannot
  read prose. A line-up an author typed into a manifesto body will travel with it, and the
  panel's sentence is the only thing standing there — stated in the SUMMARY as a limit, not
  as a residual risk somebody will remember.
</verification>

<success_criteria>
- Two Markdown documents, each declaring the tables it reads and reading no others, proved by a closure walk with a positive half that has been shown to fire.
- No location export, no third entry file, and a gate that fails by name if one appears.
- The capitolato's palette read at runtime, with no hex literal and no new exemption.
- No new route, no new dependency, and one panel with two callers.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-16-SUMMARY.md` when done.
</output>
