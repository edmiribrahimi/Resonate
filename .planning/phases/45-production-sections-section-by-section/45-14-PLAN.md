---
phase: 45-production-sections-section-by-section
plan: 14
type: execute
wave: 6
depends_on: ["45-13"]
files_modified:
  - src/app/(admin)/admin/location/actions.ts
  - src/app/(admin)/admin/location/PromoteSpaceDialog.tsx
  - src/app/(admin)/admin/(work)/location/[id]/page.tsx
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "A space that is not `acquired` cannot be promoted, and the refusal carries the stage back while naming no space"
    - "Promoting twice is refused: the link written on the scouting row makes the second press idempotent"
    - "The act asks the location section key AND `catalogue.manage`, because working the location section is not the same question as creating a venue"
    - "If the act writes an address, its confirmation says so in the same breath"
    - "A container created and then abandoned is removed by primary key, on an id this call captured"
  artifacts:
    - path: "src/app/(admin)/admin/location/PromoteSpaceDialog.tsx"
      provides: "the one bridge from an internal list to something the public may eventually see"
    - path: "src/app/(admin)/admin/location/actions.ts"
      provides: "promoteSpace, on announceNight's nine-step shape"
      contains: "already_promoted"
  key_links:
    - from: "promoteSpace"
      to: "public.venues"
      via: "the service client, after two capability checks, writing name and slug and deciding the address explicitly"
      pattern: "from\\(\"venues\"\\)"
    - from: "public.production_space.promoted_venue_id"
      to: "the created venue"
      via: "the link written last, which is what makes the second press idempotent"
      pattern: "promoted_venue_id"
---

<objective>
The one crossing that exists: an acquired space becomes a venue, deliberately, once.

Purpose: D-45-10 keeps the scouted list separate from `venues` because a scouted row inside
`venues` sits in the picker from which a night's venue is chosen, and one wrong selection
puts it on a night — from where `venue_for_parties` serves its name and address **to the
public**. A space under negotiation named on a public page is a negotiation closed badly,
and it does not un-publish. This act is where that crossing is made on purpose instead.
Output: one Server Action, one dialog, one control on the detail page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-CONTEXT.md
@.planning/phases/45-production-sections-section-by-section/45-PATTERNS.md
@.claude/rules/venue-secrecy.md
@.claude/rules/venue-acquisition.md
@src/app/(admin)/admin/calendar/actions.ts
@src/app/(admin)/admin/calendar/AnnounceNightDialog.tsx
@supabase/migrations/20260810161000_venues_read_narrowed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: promoteSpace — nine steps, and what it deliberately does not carry</name>
  <files>src/app/(admin)/admin/location/actions.ts</files>
  <read_first>
    - `src/app/(admin)/admin/calendar/actions.ts:598-932` — `announceNight`, whole. The nine
      steps and what each buys: the gate asked first and once (`:598-606`); the **idempotence
      check before anything else** (`:653-656`, *announcing twice is not idempotent — it
      spends a second number*); the refusals named one by one and the stage gate carrying the
      stage back while naming no space (`:657-676`); the count that never gates (`:678`); the
      slug derived once with a suffix on collision and never an overwrite (`:686-711`); **the
      paragraph saying what the write deliberately does not carry** (`:748-770`); the orphan
      cleanup **by primary key, on the id this call just created** (`:799-820`); and the link
      written last (`:748-770`).
    - `supabase/migrations/20260810161000_venues_read_narrowed.sql:371-397` — `venue_for_parties`,
      its `FROM` clause, and that it walks `event_parties → events → venues` and reads
      nothing else. That is why `promoted_venue_id → venues` is safe and why the reverse
      direction would not be.
    - `supabase/migrations/20260807010000_policies_to_capabilities.sql:401-417` — the three
      `venues` write policies: `master.manage` for delete, `catalogue.manage` for insert and
      update, **and `catalogue.manage` requires an approved status**.
    - `.claude/rules/venue-secrecy.md` — the monotone guard: a location can only be revealed,
      never re-hidden.
    - `45-RESEARCH.md` §D2 — the refusal table.
  </read_first>
  <action>
    `promoteSpace(spaceId)`, in this order, with each step's reason written beside it.

    1. **Two gates, asked first and once.** The location section key **and**
       `catalogue.manage`. This is a decision, not an inheritance: *may this subject work the
       location section* is not *may this subject create a venue*, and `keys.ts:38-45`'s
       naming rule applies to a bridge exactly as it applies to a surface. Write which key
       refused, as two different refusal members.
    2. **Validate the argument's shape** before any query, with the existing `UUID_PATTERN`.
    3. **Construct the service client after both gates**, never before.
    4. **The idempotence check first.** `promoted_venue_id IS NOT NULL` →
       `already_promoted`. A second press must not create a second venue; the link is what
       makes that true, and it is written last for the same reason `linked_party_id` is.
    5. **The refusals, named one by one and never collapsed:** `not_acquired` — carrying the
       stage back, **naming no space**; `no_agreement_evidence` — belt and braces against the
       CHECK, because the constraint is the boundary and the code is the sentence;
       `space_exited`; `slug_taken`; `write_failed`. Each gets a JSDoc saying why it is not
       folded into its neighbour.
    6. **The slug, derived once at creation with a suffix on collision, never an overwrite.**
       A venue slug is an address somebody may send to somebody else, and overwriting one
       breaks a link that already exists in a message.
    7. **What the write carries, and the paragraph saying what it does not.** `name`,
       `slug`, `created_by`. **The address is a decision this act takes explicitly and
       records:** D-45-21 consequence 4 names this as the only crossing where the address
       becomes subject to the reveal machinery. Carry it — the venue is the place the night
       will be at, and a venue with no address is a venue somebody will fill in by hand from
       a message — **and say so in the confirmation**, which is task 2's obligation.
       Carry **nothing else** by default, and write the list of what is left out and why.
    8. **The orphan cleanup, by primary key, on the id this call just created and captured.**
       If the venue row is created and the link-back fails, remove the venue **by its
       primary key** — never by a selector over a list (`ai-engineering.md`, *gate una
       rimozione si fa per chiave*). This repository lost 63 production rows to the other
       shape and has no point-in-time recovery. Log the orphan with `code=… message=…` and
       the id, and nothing else.
    9. **The link written last:** `promoted_venue_id = <new id>` on the scouting row.

    **What this act must not touch, stated so it is recognised if it is ever proposed:** it
    creates no event, no party and no reveal. It does not set `venue_reveal_on_purchase` and
    it does not clear `venue_reveal_sent` — the reveal is monotone and nothing here may bring
    one forward. A promotion is a catalogue act; announcing a night is a different act, in a
    different section, and it already exists.

    Every log line: `[location.promote_<cause>] space=<id> venue=<id>: code=… message=…`.
    No name. No address. No slug — a slug is a name with the spaces removed.
  </action>
  <acceptance_criteria>
    - Both gates are asked, once each, before the client is constructed: read the export top-to-bottom and record the order in the SUMMARY.
    - The idempotence check precedes every other query on the row.
    - Every refusal is a returned value: `grep -c "throw new Error" …` is unchanged from plan 45-13's count of 2.
    - The cleanup is by primary key: `grep -cE "\.delete\(\)\.eq\(\"id\"" …` returns 1, and no other `.delete(` exists.
    - No reveal column is written: `grep -cE "venue_reveal_sent|venue_reveal_on_purchase" …` returns 0, or exactly the count inside the paragraph forbidding it.
    - No name, address or slug in a log: `grep -cE "console\.error\([^)]*(name|address|slug)" …` returns 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>An acquired space can become a venue exactly once, through two gates, refusing five named causes, cleaning up by key, and touching no reveal.</done>
</task>

<task type="auto">
  <name>Task 2: PromoteSpaceDialog — the confirmation that says what is leaving</name>
  <files>src/app/(admin)/admin/location/PromoteSpaceDialog.tsx, src/app/(admin)/admin/(work)/location/[id]/page.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/calendar/AnnounceNightDialog.tsx:22-90` — the pattern: the act is
      **consequential and not destructive**, so the trigger does not take the destructive
      rung; cancel first, cancel focused, no Enter-to-confirm, using the primitive's
      **declared marker** rather than React's `autofocus` prop, which `Dialog.tsx:117-148`
      records as inert; the refusal drawn in the body with the confirming control disabled,
      so the reason is readable **before** the press; and the outcome reported in the
      dialog's own panel.
    - **And the one point that inverts.** `AnnounceNightDialog` point 3 says *this dialog
      does not name the venue*, because the act moves no address. **The promotion does.** The
      precedent it follows is `RevealVenueDialog`, which names the place **because the place
      is exactly what is leaving**.
    - `.claude/rules/venue-secrecy.md` — why a crossing is irreversible.
  </read_first>
  <action>
    A confirmation whose body says, in the domain's words, what this act does and what it
    cannot undo:

    · **it creates a venue** in the catalogue, from which a night may later be built;
    · **it carries the address across**, and from a venue an address can reach the public
      through the reveal machinery. That road exists on purpose and this is its entrance;
    · **it cannot be undone by pressing again.** The link is written on the scouting row and
      a second press is refused as *already promoted*;
    · **the space stays in the location section.** Nothing is deleted, nothing is moved out
      of the list, and the promotion is a link rather than a migration.

    **The dialog names the space**, deliberately, inverting the calendar's silence: the name
    and the address are what is leaving, and a confirmation that hid them would be asking for
    consent to something it did not describe.

    **When the space is not `acquired`, the confirming control is disabled and the reason is
    drawn in the body**, with the stage named. The act refuses again on the server — that is
    where the guarantee lives — and the constraint refuses after that.

    The outcome is reported in the dialog's own panel, never a transient notification;
    `verify:dialogs` check C asserts the absence.

    Mount the trigger on the detail page, visible only where it can be pressed
    — and note in a comment that **hiding it protects nothing**: what this surface holds is
    the secret, and the three things that refuse an unentitled subject are the middleware
    entry, the page guard and the row-level policies, none of which is the absence of a
    button.
  </action>
  <acceptance_criteria>
    - `npm run verify:dialogs` exits 0.
    - The body names all four consequences: `grep -ci "address" src/app/(admin)/admin/location/PromoteSpaceDialog.tsx` returns 1 or more, and the four bullets are present.
    - The confirming control is disabled when the stage is not `acquired`, and the stage is named in the body.
    - No transient notification: `grep -cE "toast|Toast" …` returns 0.
    - Cancel is first and focused through the primitive's declared marker: `grep -c "autoFocus" …` returns 0.
    - `npm run build` exits 0; `npm run verify` exits 0; `npm run verify:touch-targets` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:dialogs</automated>
  </verify>
  <done>The one irreversible crossing in this phase asks for consent while describing exactly what leaves, refuses before the press when it would refuse after it, and reports its outcome where the press happened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the scouted list → `public.venues` | The only crossing that exists, and it is one-way |
| `public.venues` → the public | `venue_for_parties`, `SECURITY DEFINER`, granted to `anon` and `authenticated`, walking `event_parties → events → venues` |
| a press → an irreversible publication path | Not the publication itself, but the road to it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information disclosure | the address crossing into `venues` | mitigate | the crossing is an explicit act behind two capability gates and a stage that means *in writing*; the confirmation names what is leaving; **the act creates no event and no party**, so the address is not yet on any road |
| T-45-16 | Tampering | the monotone reveal guard | mitigate | nothing here writes `venue_reveal_sent` or `venue_reveal_on_purchase`; the greps assert their absence. `meta-gates.md` allows a change only in the direction that makes a reveal **harder** |
| T-45-08 | Tampering | an orphaned venue row | mitigate | removal by **primary key**, on an id captured at creation, never by a selector. D12 is the recorded precedent and there is no point-in-time recovery |
| T-45-04 | Elevation of privilege | the two gates | mitigate | `catalogue.manage` is a **different key** from the location section key and both are asked; the refusals are distinguishable, so a person refused knows which door was shut |
| T-45-02 | Information disclosure | the promotion's log lines | mitigate | identifiers only; no name, no address, and no slug, which is a name with the spaces removed |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | no package is installed |
</threat_model>

<verification>
- `npm run build`, `npm run verify`, `npm run verify:dialogs`, `npm run verify:touch-targets` all exit 0.
- `node scripts/verify-section-export.mjs` — re-run and record. The promotion is a **write**
  path and is not an export, so it must not appear in any export module's closure; if it
  does, the closure has grown a road.
- **What a green does NOT mean:** no promotion has been performed against production. Whether
  the CHECK refuses what the action refuses is exercised by a person, and the SUMMARY must
  say that it has not been exercised here.
</verification>

<success_criteria>
- One act, two gates, five named refusals, idempotent on a link written last.
- The address crosses deliberately and the confirmation says so; the calendar dialog's silence is inverted with the reason written down.
- No reveal column touched, no event or party created, nothing deleted except an orphan by primary key.
- The trigger disabled with its reason readable before the press, and the outcome reported in the dialog's own panel.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-14-SUMMARY.md` when done.
</output>
