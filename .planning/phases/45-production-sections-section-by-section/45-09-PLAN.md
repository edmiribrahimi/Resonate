---
phase: 45-production-sections-section-by-section
plan: 09
type: execute
wave: 4
depends_on: ["45-08"]
files_modified: []
autonomous: false
requirements: [PROD-02]

must_haves:
  truths:
    - "`production.read` no longer exists as a capability row or as a grant, and it was removed by key and by pair, never by a prefix"
    - "The deploy carrying the four section keys was live before the retirement was applied, and that is recorded as an observation rather than an assumption"
    - "`npm run verify:capabilities` is green for the first time since plan 45-05's commit"
    - "The same two roles read the calendar after the split as before it, read from `private.role_capabilities` and not from memory"
  artifacts: []
  key_links:
    - from: "20260817120500_production_read_retire.sql"
      to: "private.capabilities"
      via: "DELETE by key, and by (role, capability) pair — never by a LIKE selector"
      pattern: "WHERE key = 'production.read'"
---

<objective>
Retire the old key, once the code that no longer asks for it is live, and close the second
of the two red intervals this phase declared in advance.

Purpose: this is the file an auditor reads to answer *did the reach change* — and the answer
is read off two `INSERT` blocks in plan 45-03 and three `DELETE`s here, none of which touch
a role list. It is a separate plan from the application because it has a precondition no
script can check: **a deploy has happened.**
Output: one version in the migration history, one green capability gate, and one recorded
before-and-after of who can read the calendar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-08-SUMMARY.md
@.planning/phases/45-production-sections-section-by-section/45-05-SUMMARY.md
@supabase/migrations/20260817120500_production_read_retire.sql
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: The deploy is live, observed rather than assumed</name>
  <what-built>
    Plan 45-08 applied five migrations. The database now holds eighteen capability keys:
    the four section keys, granted to master and organizer, **and** `production.read`, still
    granted, deliberately. Plan 45-05's commit — `keys.ts`, `capability-routes.ts`,
    `staff-tabs.ts`, `verify-capabilities.mjs` and the calendar's own seven sites — no longer
    names `production.read` anywhere under `src/`.
  </what-built>
  <how-to-verify>
    The retirement may only be applied **after** that commit is live in production. Applied
    before it, the deployed bundle asks a key that no longer exists, `has_capability` answers
    `false` with no error and no log line, and `/admin/calendar` becomes a redirect to
    `/dashboard` for the master — in a repository with **no error tracking**, a surface
    nobody would learn was down except by opening it.

    Three things to observe, not to assume:

    1. **The deploy is live.** Confirm the deployment carrying that commit has completed.
    2. **The first request has been made by hand.** `capability-routes.ts`'s module-load
       assertion fires on the first request after a deploy, **inside a middleware bundle** —
       which covers the payments webhook and the door's scan path. Open one gated address
       yourself and confirm a page, not a 500.
    3. **The calendar still opens for a master.** Both halves are live now: the old key is
       granted and the new one is too, so this is the interval in which nothing is refused.
       Confirming it here is what makes the retirement a removal of something unused rather
       than a removal of something load-bearing.

    **If any of the three is not true, this plan stops.** There is no partial version of it:
    the file deletes a key, and a key that is deleted while something still asks for it is
    the exact window this whole sequence was built to avoid.
  </how-to-verify>
  <read_first>
    - `supabase/migrations/20260817120500_production_read_retire.sql` — its header states this
      precondition in capitals, and it states it there because that is where whoever applies
      it will read it.
    - `.planning/STATE.md` — the deploy rule: ship on a day without a night, and make the
      first request yourself.
  </read_first>
  <acceptance_criteria>
    - The SUMMARY records, with the date and time: the deployment identifier, the address opened by hand and the outcome observed, and that a master could open the calendar **before** the retirement was applied.
    - If the deploy has not happened, the plan stops here and the SUMMARY says so. Deferred is not applied.
  </acceptance_criteria>
  <files>(no repository file changes — this task obtains an observation and a permission)</files>
  <action>Confirm the three points with the owner, record the observations, and send no request until all three are recorded. The authorisation of plan 45-08 covered five migrations by name; this is the sixth, so confirm it is inside the scope the owner granted or obtain a new line for it.</action>
  <verify>
    <automated>MISSING — no automated check can observe a deploy from this repository. The evidence is the recorded observation and the deployment identifier.</automated>
  </verify>
  <done>The deploy is live, the first request was made by hand, the calendar opens for a master, and all three are recorded — or the plan is stopped.</done>
  <resume-signal>Type "deployed" with the deployment identifier and the address you opened, or describe what is not yet true.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the retirement, read back, and close the declared red</name>
  <files>(no repository file changes — this task changes production)</files>
  <read_first>
    - `supabase/migrations/20260817120500_production_read_retire.sql` — in full.
    - `.planning/STATE.md` — the migrations endpoint, never `/database/query`, never `PUT`.
    - `45-08-SUMMARY.md` — the pre-snapshot and the eighteen-key read-back, which are what
      the numbers below are compared against.
    - `.claude/rules/ai-engineering.md`, *gate una rimozione si fa per chiave*.
  </read_first>
  <action>
    **Step 1 — the snapshot.** The same cascade set as plan 45-08 task 2, counted again
    before anything. A file that deletes rows from `private.role_capabilities` is exactly the
    kind of file this discipline exists for, even when the rows it deletes are two.

    **Step 2 — the pre-state, read from the catalogue.** Record `private.capabilities` row
    count (expected 18), the two `production.read` grants with their `requires_approved`
    values, and — this is the load-bearing one — **the full list of (role, capability) pairs
    for the four section keys**, so that the after-state can be compared against it and not
    against a memory.

    **Step 3 — apply `20260817120500_production_read_retire.sql`** through
    `POST /v1/projects/{ref}/database/migrations`. Record the response and the assigned
    version string.

    **Step 4 — the read-back, from the catalogues.**
      - `private.capabilities` — **seventeen** rows, and no row whose key is
        `production.read`.
      - `private.role_capabilities` — the two `production.read` grants are gone, and **the
        eight section grants are untouched, listed pair by pair against step 2's list.**
        This is the sentence the whole split has to be able to answer: *the same two roles
        read the calendar after as before*, read from the catalogue.
      - `pg_policies` — the six calendar policies are **unchanged** by this file. It touches
        no policy; if a `qual` moved, something else ran.
      - Confirm no key remains whose name begins `production.` other than the four section
        keys — and confirm it by listing them, not by a count.

    **Step 5 — the post-snapshot.** Identical to step 1, table for table, except for the two
    intended rows in `private.role_capabilities` and the one in `private.capabilities`.
    **Three rows, named individually.** Any fourth difference stops the plan.

    **Step 6 — the gate.** Run `npm run verify:capabilities`. It should be **green for the
    first time since plan 45-05's commit**: seventeen keys declared, seventeen present,
    sixty-eight pairs, thirty-six grants, thirty-two refusals, and side 5 confirming that
    `staff` and `member` hold none of the four. If a number disagrees, the model is the fact
    and the constant is the error — **do not move a constant to make this pass**.
  </action>
  <acceptance_criteria>
    - The SUMMARY carries the pre- and post-snapshots with exactly three intended differences, each named individually.
    - `private.capabilities` holds 17 rows and none of them is `production.read`.
    - The eight section grants are listed pair by pair before and after, and are identical. **This is the evidence for D-45-04 constraint 3.**
    - The six calendar policies' `qual`s are unchanged by this application.
    - `npm run verify:capabilities` exits 0, and its transcript is recorded.
    - `npm run verify` exits 0.
    - No constant in `scripts/verify-capabilities.mjs` was edited during this plan: `git diff` for the plan touches no file under `scripts/`.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:capabilities</automated>
  </verify>
  <done>The old key is gone by key and by pair, the eight new grants are provably untouched, the six policies are provably untouched, and the capability gate is green for the first time since the split began.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the deployed bundle → the applied key set | The one place where an ordering slip takes a surface down instead of degrading it |
| a `DELETE` on a grants table → who can read what | Two rows, and they are the two rows the whole split is audited against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Denial of service | `/admin/calendar` and the three new addresses | mitigate | the deploy is **observed**, the first request is made by hand, and a master's access is confirmed **before** the key is removed. Applied out of order, the failure is silent — no error, no log line, and no error tracking to notice |
| T-45-08 | Tampering | `private.role_capabilities` | mitigate | removal **by key** and **by pair**, never by a `LIKE 'production%'` selector, which after this phase matches the four new keys; snapshot before and after, with exactly three named differences |
| T-45-04b | Elevation of privilege | the eight section grants | mitigate | listed pair by pair before and after; identity of the two lists is the evidence that the reach neither narrowed nor widened |
| T-45-12 | Repudiation | the migration history | mitigate | the migrations endpoint; the version string read from the history |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | no package is installed |
</threat_model>

<verification>
- `npm run verify:capabilities` exits 0 — the first green since the split's commit, and the
  one automatic check in this repository that can see the capability model at all.
- `npm run verify` exits 0.
- The recorded catalogue read-back, with the queries, and the pair-by-pair grant comparison.
- **What a green does NOT mean:** `verify:capabilities` reads through the Management API,
  which **bypasses RLS**. It proves the model is what the code declares. It proves nothing
  about who is refused — that is `verify:refusal`, and procedure P1 for the part no
  instrument can reach.
</verification>

<success_criteria>
- The deploy observed, the first request made by hand, and a master's calendar access confirmed before anything was removed.
- One version in the migration history; seventeen keys; no `production.read` anywhere.
- The eight section grants listed before and after and identical.
- The six calendar policies untouched by this file.
- The declared red closed by the model catching up, not by a constant moving.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-09-SUMMARY.md` when done.
</output>
