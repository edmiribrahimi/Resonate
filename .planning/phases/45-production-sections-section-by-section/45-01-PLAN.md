---
phase: 45-production-sections-section-by-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/production/sections/vocabulary.ts
  - supabase/migrations/20260817120100_production_location.sql
  - supabase/migrations/20260817120200_production_sections.sql
  - src/types/database.ts
autonomous: true
requirements: [PROD-02]

must_haves:
  truths:
    - "A scouted space cannot exist without a stage, and the stage vocabulary is the same four words `venue-acquisition.md` uses"
    - "A space at `acquired` without a line saying where the agreement is cannot be inserted"
    - "A section marked `not_decided` without what is missing and whose call it is cannot be inserted"
    - "An attribute nobody has asked about carries a value that says so, and is never blank"
    - "No score column exists anywhere in the schema"
  artifacts:
    - path: "src/lib/production/sections/vocabulary.ts"
      provides: "the closed vocabularies of the three sections, as `as const` tuples with derived types and total label Records"
      contains: "SECTION_STATES"
    - path: "supabase/migrations/20260817120100_production_location.sql"
      provides: "production_space and production_space_attribute, every constraint inside the CREATE, RLS enabled with zero policies"
      contains: "production_space_acquired_needs_evidence"
    - path: "supabase/migrations/20260817120200_production_sections.sql"
      provides: "production_section, production_open_question, production_visual_asset, RLS enabled with zero policies"
      contains: "production_section_not_decided_names_its_gap"
    - path: "src/types/database.ts"
      provides: "the five row types, importing their vocabularies from the literal source"
  key_links:
    - from: "src/types/database.ts"
      to: "src/lib/production/sections/vocabulary.ts"
      via: "import of SectionState, ExitReason, AttributeValue, AttributeProvenance"
      pattern: "from \"@/lib/production/sections/vocabulary\""
    - from: "supabase/migrations/20260817120100_production_location.sql"
      to: "src/lib/production/ics/vocabulary.ts"
      via: "the stage CHECK mirrors VENUE_STAGES — the same four words, written once on each side"
      pattern: "'mapped', *'verified', *'contacted', *'acquired'"
---

<objective>
The five tables of the three new sections, their closed vocabularies, and their row
types — written, not applied.

Purpose: everything else in this phase reads or writes these tables. They are written
first, in one place, so that the constraints carrying decisions (acquired means in
writing; a void names its gap; nobody has been called) are structural before any surface
can be tempted to enforce them in a form.
Output: one vocabulary module, two structural migration files, five row types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-CONTEXT.md
@.planning/phases/45-production-sections-section-by-section/45-PATTERNS.md
@.planning/STATE.md
@.claude/rules/venue-acquisition.md
@.claude/rules/sound-manifesto.md
@src/lib/production/ics/vocabulary.ts
@supabase/migrations/20260815120000_production_calendar.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: The closed vocabularies — src/lib/production/sections/vocabulary.ts</name>
  <files>src/lib/production/sections/vocabulary.ts</files>
  <read_first>
    - `src/lib/production/ics/vocabulary.ts:177-247` — the analog, named as **exact** in
      `45-PATTERNS.md` §B3. The `as const` tuple + derived type + **total** `Record` label
      map is the whole shape; the totality is what makes a missing label a build error.
    - `.claude/rules/venue-acquisition.md` — the four stages, and *derived is not verified*.
    - `.claude/rules/sound-manifesto.md` — *unwritten is an answer, invented is not* and
      *unwritten is not unconstrained*: the pair D-45-14's three states encode.
    - `45-CONTEXT.md` D-45-11 (revised), D-45-13, D-45-14, D-45-24.
  </read_first>
  <action>
    Declare, each as an `as const` tuple with a derived type and a **total** `Record`
    label map, in one new module under `src/lib/production/sections/`:

    · `SECTION_STATES` — `written`, `coordinates_declared`, `not_decided`. Write the
      paragraph that says why it is three and not two: a two-state model makes the middle
      one read as free, and the domain names two opposite errors, not one.
    · `SECTION_KINDS` — `manifesto`, `visual`.
    · `EXIT_REASONS` — `out_of_identity`, `refused`, `unreachable`, `other`. Carry the
      sentence from `venue-acquisition.md` that a space out of identity stays listed, at
      zero, and is never deleted.
    · `ATTRIBUTE_KEYS` — the ten scouted attributes, named in English:
      `outdoor_sunset`, `artistic_frame`, `aperitivo_vocation`, `exclusivity`,
      `evening_licence`, `events`, `music_at_home`, `audio`, `console`, `partnership`.
    · `ATTRIBUTE_VALUES` — `top`, `good`, `limited`, `no`, `not_asked`. **`not_asked` is a
      value, not an absence**, and that is the whole reason this tuple has five members
      and not four: the source encodes "to verify" per attribute rather than per record,
      and a blank cell and an unasked question are the same pixel unless the data
      distinguishes them.
    · `ATTRIBUTE_PROVENANCE` — `derived`, `field_verified`. This is where D-45-11
      mitigation 2 now lives: since a score is computed and never stored, provenance is a
      property of the **input**, and a computed score is only as verified as its weakest
      input.
    · `ANSWERS_SOURCE` — `not_asked`, `phone`, `on_site`, `public_listing`. The four
      phone answers need it because *"until 01:00 per the venue's public page"* and
      *"…because they said so on the phone"* are different facts, and the module says the
      public listing answers the wrong question.
    · `EXTENDED_HOURS_STANCE` — `not_asked`, `will_discuss`, `will_not_discuss`. Carry
      D-45-24's prohibition into the docblock in its own words: **no crawl, no inference
      and no default may ever move this off `not_asked`** — a derived value here is
      *derived is not verified* committed in the one column built to prevent it.
    · `SIZE_BANDS` — the source's four-value band, named in English.
    · `SPACE_CATEGORIES` — the eleven categories of `45-CONTEXT.md` `<code_context>`.

    **Do not redeclare the stages.** `VENUE_STAGES` and `VenueStage` already exist in
    `src/lib/production/ics/vocabulary.ts:187-195` and are the same four words; re-export
    them from this module or import them at each use site, and write the sentence that
    says a second tuple is a second thing to keep in sync with one CHECK.

    **No `SCORE_*` vocabulary and no score constant.** A score is computed from the
    attributes with the format's declared weights (plan 45-07) and is stored nowhere.
  </action>
  <acceptance_criteria>
    - `grep -c "as const" src/lib/production/sections/vocabulary.ts` returns 9 or more.
    - The stages are not redeclared: `grep -c "\"mapped\"" src/lib/production/sections/vocabulary.ts` returns 0, **or** the only occurrence is inside a re-export of `VENUE_STAGES`.
    - `not_asked` is present in `ATTRIBUTE_VALUES`, `ANSWERS_SOURCE` and `EXTENDED_HOURS_STANCE`: `grep -c "not_asked" src/lib/production/sections/vocabulary.ts` returns 3 or more.
    - No score vocabulary exists: `grep -vE '^\s*(\*|//)' src/lib/production/sections/vocabulary.ts | grep -ci "score"` returns 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Every closed vocabulary of the three sections has exactly one home, the stages are reused rather than restated, and no score vocabulary exists.</done>
</task>

<task type="auto">
  <name>Task 2: The location tables — 20260817120100_production_location.sql</name>
  <files>supabase/migrations/20260817120100_production_location.sql</files>
  <read_first>
    - `supabase/migrations/20260815120000_production_calendar.sql:168-305` (`production_plan`)
      and `:395-466` (`production_piece`) — the house style: every constraint declared
      **inside** the `CREATE TABLE`, named so a bad value arrives as a constraint name and
      not an anonymous `23514`.
    - The same file `:227-237` — the **⚠ INTERNAL, NEVER PUBLIC** banner. `name` and
      `address` each carry it, and `address` carries one clause more.
    - The same file `:244-253` — why `production_plan.venue_stage` is **nullable**. This
      table's `stage` is `NOT NULL DEFAULT 'mapped'`; say why they differ or the next
      reader reads it as an accident.
    - The same file `:433-449` — the XOR shape and the conditional-requirement shape.
    - The same file `:290-305` — indexes with their read named, and RLS enabled in the
      **structural** migration with **zero policies**.
    - `supabase/migrations/20260810161000_venues_read_narrowed.sql:371-397` — the one
      public road to an address, and its `FROM` clause. This table must have no edge into it.
    - `45-CONTEXT.md` D-45-10, D-45-11 (revised), D-45-12, D-45-13, D-45-21, D-45-22, D-45-24.
  </read_first>
  <action>
    One transaction, `BEGIN`/`COMMIT`, with the *why one transaction* paragraph copied in
    substance from `20260815120100:16-22`. Two tables.

    **`public.production_space`.** Columns: `id` uuid PK; `name` text NOT NULL;
    `address` text; `category` text; `source` text; `short_description` text;
    `home_format_id` uuid REFERENCES `public.formats(id)`; `stage` text NOT NULL DEFAULT
    `'mapped'`; `size_band` text; `real_capacity` integer; `rig` text; `guest_dj_allowed`
    boolean; `closing_time` time; `published_hours` text NOT NULL DEFAULT `'not_asked'`;
    `extended_hours_stance` text NOT NULL DEFAULT `'not_asked'`; `answers_source` text
    NOT NULL DEFAULT `'not_asked'`; `agreement_evidence` text; `exited_at` timestamptz;
    `exit_reason` text; `promoted_venue_id` uuid REFERENCES `public.venues(id)`;
    `source_key` text; `already_used` boolean NOT NULL DEFAULT false; `in_use` boolean NOT
    NULL DEFAULT false; `note` text; `first_seen_at` timestamptz NOT NULL DEFAULT now();
    `created_at`/`updated_at` timestamptz NOT NULL DEFAULT now(); `created_by` uuid
    REFERENCES `public.profiles(id)` ON DELETE SET NULL.

    **`closing_time` is `time` and not `timestamptz`** for `production_plan:197-201`'s
    reason: a night runs past midnight and a conversion moves a weekday.

    Named constraints, each carrying its decision in a comment above it:
    · `production_space_stage_check` — stage in the four words. Mirrors `VENUE_STAGES`.
    · `production_space_acquired_needs_evidence` — `stage <> 'acquired' OR
      btrim(coalesce(agreement_evidence,'')) <> ''`. **D-45-12 made structural: acquired
      means in writing, and the database is where that is true rather than where it is
      remembered.**
    · `production_space_exit_xor_reason` — `(exited_at IS NULL) = (exit_reason IS NULL)`.
      D-45-13: a row that left the race carries why and when, and no row is ever deleted.
    · `production_space_exit_reason_check`, `production_space_category_check`,
      `production_space_size_band_check`, `production_space_answers_source_check`,
      `production_space_extended_hours_stance_check` — each over its tuple.
    · `production_space_published_hours_not_blank` — `btrim(published_hours) <> ''`. The
      sentinel is a value; a blank is not an answer.
    · `production_space_real_capacity_positive` — `real_capacity IS NULL OR real_capacity > 0`.
    · `production_space_promotion_needs_acquired` — `promoted_venue_id IS NULL OR stage =
      'acquired'`. D-45-10: a promotion from any other stage is
      *ranking-is-not-availability*, encoded.
    · `production_space_source_key_unique` UNIQUE (`source_key`) — the re-runnability key
      of D-45-07 constraint 2, and a `COMMENT ON CONSTRAINT` saying that a nullable unique
      admits hand-typed rows that came from no import.

    **`public.production_space_attribute`.** `id` uuid PK; `space_id` uuid NOT NULL
    REFERENCES `public.production_space(id)` **ON DELETE RESTRICT** — D-45-13 says no
    space is ever deleted, so a cascade is a path that should not exist; `attribute` text
    NOT NULL with a named CHECK over the ten keys; `value` text NOT NULL with a named
    CHECK over the five values; `provenance` text NOT NULL with a named CHECK over
    `derived`/`field_verified` and **no default** — a value cannot exist without saying
    which it is; `answered_at` timestamptz; `UNIQUE (space_id, attribute)`.

    Two absences, each written as a decision rather than left as a gap, in the shape
    `20260810161000:244-264` uses:
    · **No `regime` column and no neighbours column** — offered under D-45-11 and D-45-22
      and **declined**. Both domain modules argue for them, which is exactly why the next
      reader will add them back as obviously useful. They come back as a decision.
    · **No score column, on either table.** A score is computed from the attributes with
      the format's declared weights and is comparable only within a format; a stored
      number detaches from its inputs at the first edit, and a single column would encode
      the opposite of *si legge per format, mai come un giudizio assoluto*.

    Indexes, each with the read it serves named: `stage`, `name`, and
    `production_space_attribute(space_id)`.

    `ALTER TABLE … ENABLE ROW LEVEL SECURITY` on both, with **zero policies here**, and
    the sentence from `20260815120100:28-33` explaining the pairing: the tables are closed
    from the moment they exist, and the access file adds the arms.

    **NO MATERIAL.** State it in the header, as `20260815120200:30` does: this file
    carries no space, no address and no candidate.
  </action>
  <acceptance_criteria>
    - The file exists and is a single transaction: `grep -c "^BEGIN;" …` returns 1 and `grep -c "^COMMIT;" …` returns 1.
    - Every constraint is declared inside a `CREATE TABLE`: `grep -c "ADD CONSTRAINT" …` returns 0.
    - The four decision-carrying constraints are present by name — `grep -cE "production_space_(acquired_needs_evidence|exit_xor_reason|promotion_needs_acquired|source_key_unique)" …` returns 4 or more.
    - RLS is enabled and no policy is created here: `grep -c "ENABLE ROW LEVEL SECURITY" …` returns 2 and `grep -c "CREATE POLICY" …` returns 0.
    - No score column: `grep -vE '^\s*--' … | grep -ci "score"` returns 0.
    - No candidate material: `grep -cE "[Vv]ia |[Cc]orso |[Pp]iazza " …` returns 0.
    - The address column carries the internal banner: the ⚠ paragraph appears above `address`, with the clause that this is a **street address** and not a word, and that the street address is the payload `venue_for_parties` exists to release deliberately.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this file is not applied by this plan and no runner reads SQL here. The mechanical evidence is the greps above; the applied evidence is the catalogue read-back in plan 45-08.</automated>
  </verify>
  <done>Two tables exist as a file, every constraint that carries a decision is named and commented, RLS is on with zero policies, and the two declined columns are declined in writing.</done>
</task>

<task type="auto">
  <name>Task 3: The section tables and the row types — 20260817120200_production_sections.sql, src/types/database.ts</name>
  <files>supabase/migrations/20260817120200_production_sections.sql, src/types/database.ts</files>
  <read_first>
    - `supabase/migrations/20260815120000_production_calendar.sql:395-466` — the same house
      style, for the three tables below.
    - `src/types/database.ts:1177-1260` (`ProductionPlan`) — the row-type analog, including
      the ⚠ banner repeated on the TypeScript side at `:1229-1238` and the *null is not a
      value* discipline at `:1240-1246`.
    - `src/lib/production/sections/vocabulary.ts` — written in task 1. This file imports
      **from** there; the arrow never points the other way.
    - `45-CONTEXT.md` D-45-08, D-45-14, D-45-15, D-45-16.
  </read_first>
  <action>
    **The migration**, one transaction, three tables.

    `public.production_section` — `id`; `section` text NOT NULL CHECK over
    `manifesto`/`visual`; `format_id` uuid REFERENCES `public.formats(id)`, **nullable**
    because a rule can belong to the whole brand rather than to one format; `title` text
    NOT NULL; `state` text NOT NULL CHECK over the three states **with no default** — a
    default of `written` fills the void and a default of `not_decided` answers for a
    coordinate that exists, so the author says which; `body` text; `missing` text;
    `decision_owner` text; `updated_at`; `updated_by`.

    Two constraints, each with its paragraph:
    · `production_section_not_decided_names_its_gap` — `state <> 'not_decided' OR (missing
      and decision_owner both non-blank)`. Without it, *not decided* is a shrug, and a
      shrug reads as *nobody's job*.
    · `production_section_written_has_a_body` — `state <> 'written' OR body non-blank`. An
      empty `written` row is the emptiness **not** declared, which is criterion 3 failing
      quietly.
    · And the deliberate third absence, written: `coordinates_declared` requires
      **neither**, because forcing a `missing` line on the middle state pushes the author
      into inventing the gap. The explicit negative is **not a separate column** — it
      belongs in `body`, because a `negatives` column invites a surface that shows the
      positives and drops them.

    `public.production_open_question` — `id`; `question` text NOT NULL; `decision_owner`
    text NOT NULL; `section` text; `format_id` uuid REFERENCES `public.formats(id)`;
    `opened_at` NOT NULL DEFAULT now(); `closed_at` timestamptz; `resolution` text;
    CHECK `(closed_at IS NULL) = (resolution IS NULL)` — a question closed without its
    answer is a question that will be reopened. **There is no constraint here that refuses
    anything else, and there must not be one**: D-45-15 warns and does not block, on
    D-44-16's reasoning — a block that fires under deadline is a block somebody routes
    around.

    `public.production_visual_asset` — `id`; `kind` text NOT NULL CHECK over
    `dj_photo`/`piece`; `object_key` text NOT NULL UNIQUE; `artist_name` text;
    `format_id` uuid REFERENCES `public.formats(id)`; `caption` text; `taken_on` date;
    `created_at`; `created_by`. `artist_name` carries a **narrower** banner than a venue's:
    a name in a line-up nobody has announced is material in the same sense a space under
    negotiation is, so no surface an unauthenticated visitor can reach may render it and
    no log line may echo it.

    RLS enabled on all three, **zero policies here**.

    **The row types.** Add `ProductionSpace`, `ProductionSpaceAttribute`,
    `ProductionSection`, `ProductionOpenQuestion` and `ProductionVisualAsset` to
    `src/types/database.ts`, importing `SectionState`, `SectionKind`, `ExitReason`,
    `AttributeKey`, `AttributeValue`, `AttributeProvenance`, `AnswersSource`,
    `ExtendedHoursStance`, `SizeBand`, `SpaceCategory` from
    `@/lib/production/sections/vocabulary` and `VenueStage` from its existing home.
    **Restate no vocabulary here.** Repeat the ⚠ banner on `name`, `address` and
    `artist_name` on the TypeScript side, in one commit with the SQL side.

    Write the paragraph the analog writes, in substance: **no Supabase client in this
    repository is parameterised with `Database`**, so a green `npm run build` proves the
    consuming code type-checks against these declarations and proves nothing about whether
    a column is spelled the way the applied migration spells it. Every name here was read
    out of the two migration files by hand; the catalogue confirms it in plan 45-08, and
    until then that is the only check performed.
  </action>
  <acceptance_criteria>
    - `grep -cE "production_(section|open_question|visual_asset)" supabase/migrations/20260817120200_production_sections.sql` returns 3 or more.
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260817120200_production_sections.sql` returns 3; `grep -c "CREATE POLICY" …` returns 0.
    - The two decision-carrying constraints are present by name: `grep -cE "production_section_(not_decided_names_its_gap|written_has_a_body)" …` returns 2.
    - `grep -c "from \"@/lib/production/sections/vocabulary\"" src/types/database.ts` returns 1.
    - The vocabularies are not restated in the types file: `grep -cE "\"coordinates_declared\" \| \"not_decided\"|'coordinates_declared' \| 'not_decided'" src/types/database.ts` returns 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Three tables exist as a file with RLS on and zero policies, and five row types import their vocabularies from the one place that declares them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the scouting record → the database column | A street address enters the product for the first time (D-45-24). Whatever protects it, protects **one column** |
| the schema file → this public repository | Every line committed here is a publication, and a publication is irreversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information disclosure | `production_space.address`, `production_space.name` | mitigate | no foreign key, view or function from this table toward `event_parties` or `venues` except `promoted_venue_id`, whose direction the public road never traverses; the ⚠ banner on both sides in one commit; the absence is **demonstrated** by the closure walk of plan 45-06, not asserted here |
| T-45-03 | Information disclosure | this migration file, tracked and public | mitigate | NO MATERIAL header; the acceptance grep for street tokens; criteria never candidates |
| T-45-10 | Elevation of privilege | five new tables | mitigate | RLS enabled in this structural file with **zero** policies, so the tables are closed from the moment they exist; the arms arrive in plan 45-04 |
| T-45-13 | Spoofing | a value impersonating a decision | mitigate | `state` has **no default**; `provenance` has no default; `not_asked` is a value and never a blank |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no package (`45-RESEARCH.md` § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run build` exits 0 after tasks 1 and 3.
- The greps in each task's acceptance criteria, run and recorded in the SUMMARY.
- **What a green does NOT mean:** nothing here has been applied. The build type-checks
  TypeScript that no live column has ever answered, and it would pass identically against
  an empty database. The schema becomes a fact in plan 45-08 and not before.
</verification>

<success_criteria>
- One vocabulary module, holding every closed vocabulary of the three sections exactly once, and reusing `VENUE_STAGES` rather than restating it.
- Two migration files on disk, five tables, every constraint inside its `CREATE TABLE`, RLS on, zero policies.
- The four constraints that carry decisions — acquired needs evidence, exit XOR reason, promotion needs acquired, a void names its gap — exist by name.
- No score column and no score vocabulary anywhere.
- Five row types importing their vocabularies from the literal source.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-01-SUMMARY.md` when done.
</output>
