---
phase: 45-production-sections-section-by-section
plan: 08
type: execute
wave: 3
depends_on: ["45-04", "45-05"]
files_modified: []
autonomous: false
requirements: [PROD-02]

must_haves:
  truths:
    - "Five migrations are in the project's migration history, applied through the migrations endpoint, with their assigned versions read from the history and not from a POST response"
    - "The catalogues confirm five new tables, their named constraints, one read arm per section key, four capability rows and eight grants — read back, not assumed"
    - "No pre-existing row moved: the cascade set was walked from pg_constraint and counted before the first request and after the last"
    - "At no instant of this application was an entitled reader refused the calendar: `production.read` is still granted when this plan ends"
    - "The refusal instrument was re-run and says the same thing about the same subjects as it did before the split"
  artifacts: []
  key_links:
    - from: "this machine"
      to: "POST /v1/projects/{ref}/database/migrations"
      via: "SUPABASE_ACCESS_TOKEN, the migrations endpoint and never /database/query"
      pattern: "database/migrations"
---

<objective>
**[BLOCKING].** Apply this phase's five additive migrations to production, prove they
landed by reading the catalogues, prove nothing moved, and leave the old key granted.

Purpose: every plan after this one reads or writes these tables, and **a build and a
typecheck pass without any of it being live** — the TypeScript row types come from a
hand-written file, not from the database. That is the false-positive verification state
this plan exists to close.
Output: five versions in the migration history, one recorded read-back, one recorded pair
of snapshots, and one re-run of the refusal instrument.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-production-sections-section-by-section/45-RESEARCH.md
@.planning/phases/45-production-sections-section-by-section/45-03-SUMMARY.md
@.planning/phases/45-production-sections-section-by-section/45-04-SUMMARY.md
@.planning/phases/45-production-sections-section-by-section/45-05-SUMMARY.md
@.planning/phases/44-the-production-calendar-comes-inside/44-07-PLAN.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: The owner's authorisation to write to production, dated and spent once</name>
  <what-built>
    Five migration files exist on disk and none has been applied:
    `20260817120000_production_section_keys.sql` (four capability rows, eight grants, six
    rewritten SELECT policies), `20260817120100_production_location.sql` (two tables),
    `20260817120200_production_sections.sql` (three tables),
    `20260817120300_production_sections_access.sql` (the read arms) and
    `20260817120400_visual_archive_bucket.sql` (one private bucket). The code half —
    `keys.ts`, `capability-routes.ts`, `staff-tabs.ts`, `verify-capabilities.mjs` — is
    committed and has not been deployed.
  </what-built>
  <how-to-verify>
    This is not a verification of work; it is **permission to change production**, granted
    per application, dated, and spent when it is used (`ai-engineering.md`, *gate
    l'autorizzazione a scrivere in produzione è un atto*; STATE.md records a Phase 38
    authorisation *spesa alle 11:15:24 UTC ed esaurita*).

    Five things the owner should know, each measured rather than recalled:

    1. **What changes.** Five new tables, one new private storage bucket, four new rows in
       `private.capabilities` with eight grants, and **six existing SELECT policies
       replaced** on the calendar's tables. No existing table is altered, no column is
       dropped, and no row is deleted.
    2. **Who can read what, before and after.** Unchanged. Master and organizer hold the
       calendar before this and hold it after; the three new keys go to the same two roles
       (D-45-03). A key rename that changed the reach would be two changes in one diff.
    3. **Why the old key is deliberately left in place.** Both single-transaction orders
       leave a window in which the master opens `/admin/calendar` and is redirected to
       `/dashboard`, with nothing in a log — this repository has **no error tracking**.
       This sequence has no window: during the interval that follows, the old bundle asks
       `production.read`, still granted, while the six policies ask the calendar's section
       key, granted to the same two roles. The old key is retired in plan 45-09, after the
       deploy, under its own gate.
    4. **What went wrong once.** D12: 63 production rows deleted during a *verification*,
       in seven tables, and point-in-time recovery is not active by the owner's own
       decision. That is why task 2 snapshots before touching anything, deletes nothing,
       and would remove — if it ever had to — only by primary key on an id captured at
       creation.
    5. **What this plan does NOT establish.** The Management API connects with a role that
       **bypasses RLS**. Nothing read here says the policies refuse anybody. That is task 4,
       and it is a different instrument.

    **A note on the deploy, which the owner decides and this plan does not perform.** When
    the split's commit ships, the first request after the deploy is when
    `capability-routes.ts`'s module-load assertion fires — inside a **middleware** bundle,
    which covers the payments webhook and the door's scan path. The standing rule holds:
    ship on a day without a night, and make the first request yourself.
  </how-to-verify>
  <read_first>
    - `.planning/STATE.md` §Blockers and §Session Continuity — the migrations-endpoint
      precedent, the pre-existing history drift, and the D12 incident.
    - `.planning/phases/44-the-production-calendar-comes-inside/44-07-PLAN.md` task 1 — the
      same checkpoint, one phase earlier, and the shape this one copies.
  </read_first>
  <acceptance_criteria>
    - The authorisation is recorded in `45-08-SUMMARY.md` with its date and its exact scope: **these five migrations, this once**, and explicitly **not** the retirement migration and **not** the seed.
    - If the owner declines or defers, the plan stops here, every downstream plan is marked blocked, and the SUMMARY says **which of the two** — deferred is not applied.
    - The corresponding ledger line in `45-PROCEDURES.md` (authorisation **A2**) is filled with the date.
  </acceptance_criteria>
  <files>(no repository file changes — this task obtains a permission)</files>
  <action>Put the five points and the deploy note to the owner, obtain an explicit dated authorisation covering these five migrations only, record it in the SUMMARY and in the procedures ledger, and send no request before it exists.</action>
  <verify>
    <automated>MISSING — a permission has no automated check. The evidence is the dated authorisation in 45-08-SUMMARY.md and the absence of any request before it.</automated>
  </verify>
  <done>The authorisation is recorded with its date and single-use scope, or the plan is stopped with the reason recorded.</done>
  <resume-signal>Type "authorised" with the date, or describe the condition to meet first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Snapshot, apply through the migrations endpoint, read back from the catalogues</name>
  <files>(no repository file changes — this task changes production)</files>
  <read_first>
    - `.planning/STATE.md` — `POST /v1/projects/{ref}/database/migrations` with
      `SUPABASE_ACCESS_TOKEN` from `.env.local`. **The migrations endpoint, not
      `/database/query`**, so the history stays truthful; and not `PUT`, which upserts the
      history without applying anything. The Supabase CLI is not installed on this machine
      and `supabase db push` is not a step anybody here can run.
    - `scripts/rls-baseline.mjs:191-224` — the environment read and the refuse shape: a
      missing key is a refusal, never a silent no-op.
    - All five migration files, in full, before sending any of them.
  </read_first>
  <action>
    In this order. Each step's output goes verbatim into the SUMMARY; a step whose output
    is not recorded did not happen.

    **Step 1 — the snapshot, before anything.** Count rows in every table reachable by
    cascade from the objects these migrations touch, **reading the constraint graph from
    `pg_constraint` rather than from memory**. The new tables reference `public.formats`,
    `public.profiles` and `public.venues`, so those three cascade sets are the ones that
    matter, and `private.capabilities` / `private.role_capabilities` are counted directly.
    Record the counts and the number of tables counted. A baseline taken after the change is
    not a baseline.

    **Step 2 — the pre-state.** Record: the last five migration history versions; whether any
    of the five table names already exists in `pg_class`; whether any of the four section
    keys already exists in `private.capabilities`; the current six policy names on the
    calendar's tables and the `qual` of each. This is what makes every idempotence claim
    checkable rather than asserted.

    **Step 3 — apply, one request per file, in this order**, recording each response and each
    assigned version string:
      1. `20260817120000_production_section_keys.sql`
      2. `20260817120100_production_location.sql`
      3. `20260817120200_production_sections.sql`
      4. `20260817120300_production_sections_access.sql`
      5. `20260817120400_visual_archive_bucket.sql`

    The order is not optional: the access file names tables the two structural files create,
    and Postgres refuses a policy that reads a table that does not exist.

    **Step 4 — the read-back, and it reads the CATALOGUES, not the responses.** A `200` on a
    `POST` is a report; the catalogue is the fact. Query and record:
      - `pg_class` — the five tables exist.
      - `pg_constraint` — every named constraint of both structural files exists **by name**,
        including `production_space_acquired_needs_evidence`,
        `production_space_exit_xor_reason`, `production_space_promotion_needs_acquired` and
        `production_section_not_decided_names_its_gap`.
      - `pg_policies` — **the six calendar policies now name
        `production.calendar.manage`**, and **every one of their `qual`s still begins
        `( SELECT `**. A rewritten policy that lost the InitPlan wrapper is a performance
        regression nothing else in this repository would catch, and the belief that `STABLE`
        substitutes for it was disproved by `EXPLAIN` and recorded at
        `20260807000000_capability_model.sql:177-184`.
      - `pg_policies` again — one read arm per new table, **the section keys they ask read
        out and listed**, and **zero** INSERT / UPDATE / DELETE policies on any of the five.
        This list is criterion 1's structural half: four policies, four different keys.
      - `private.capabilities` — **eighteen** rows: fourteen plus four. `production.read` is
        still among them, and that is correct at this point.
      - `private.role_capabilities` — the eight new grants, all `requires_approved = false`,
        plus the two `production.read` grants still present.
      - `storage.buckets` — `visual-archive` exists with `public = false`; and its policies
        in `pg_policies`: one SELECT for `authenticated`, and **no INSERT and no `anon`
        policy of any kind**.
      - The five new tables — **zero rows each**. The schema arrived empty of material.

    **Step 5 — the post-snapshot.** Repeat step 1's counts. **Every count must be identical.**
    A difference is not a rounding error; it is the D12 shape, and it stops the plan.

    Two prohibitions for the whole task: **delete nothing**, and if a row ever has to be
    removed, capture its id at creation and delete by primary key — never by a predicate and
    never by clicking a control.
  </action>
  <acceptance_criteria>
    - The SUMMARY carries the pre-snapshot and the post-snapshot side by side, with the number of tables counted, and they are identical.
    - Five version strings, read from the migration history and not from the `POST` bodies alone.
    - Five tables present; the four decision-carrying constraints present by name; one read arm per new table with its key named; **zero** write policies on any of the five.
    - The six calendar policies name `production.calendar.manage` and every `qual` begins `( SELECT `. If any does not, the plan **stops**: it is repaired by a forward migration, never by editing an applied file.
    - `private.capabilities` holds 18 rows and `production.read` is still one of them.
    - `visual-archive` exists, is not public, has one SELECT policy and no INSERT policy and no `anon` policy.
    - The five new tables hold zero rows each.
    - **Stated in the SUMMARY:** nothing measured here says a policy refuses anybody, because the API role bypasses RLS.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — no automated command in this repository can assert against production. The evidence is the recorded catalogue read-back. The mechanical checks that CAN run are `npm run verify:capabilities` (task 3) and `npm run build`.</automated>
  </verify>
  <done>Five migrations are in the history, the catalogues confirm every object, the calendar's six policies ask the new key with their wrapper intact, the old key is still granted, and the two snapshots are identical.</done>
</task>

<task type="auto">
  <name>Task 3: The declared red, observed</name>
  <files>(no repository file changes)</files>
  <read_first>
    - `45-05-SUMMARY.md` — the two red intervals, declared in advance.
    - `scripts/verify-capabilities.mjs:190-200` and `:500-549` — **if this trips, look at the
      capability model, not at this constant**, and the recorded shape of the failure the
      constant exists to catch.
  </read_first>
  <action>
    Run `npm run verify:capabilities` and record its output as it falls. It is expected
    **red**: the database now holds eighteen keys while `keys.ts` declares seventeen, and
    the orphan is `production.read`, which is still granted on purpose. Side 4 reports an
    orphan key as *a key nobody asks for* — a warning, not a failure.

    Record which sides pass and which do not, and record the sentence this plan needs to be
    read with: **this red was written down before it arrived**, it is the second of the two
    declared intervals, and it closes in plan 45-09. **Do not edit a constant.** Lowering a
    total to make a run pass is the failure that constant exists to catch, and it has a
    recorded shape in this repository.
  </action>
  <acceptance_criteria>
    - The transcript of `npm run verify:capabilities` is recorded verbatim, with its exit code.
    - The SUMMARY names the orphan key and states that it is expected and why.
    - `git diff` for this task is empty. No constant moved.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — the expected outcome is a non-zero exit. The evidence is the recorded transcript and an empty diff.</automated>
  </verify>
  <done>The declared red arrived, was recorded, and was not repaired by editing the thing that measures.</done>
</task>

<task type="auto">
  <name>Task 4: Re-run the refusal instrument, and compare it against its own baseline</name>
  <files>(no repository file changes)</files>
  <read_first>
    - `45-02-SUMMARY.md` — the baseline transcript, taken before the split.
    - `scripts/verify-refusal.mjs` — and its header's *what a green does not mean*.
    - `45-CONTEXT.md` D-45-03 and D-45-23 — what this run cannot claim.
  </read_first>
  <action>
    The session mint of plan 45-02 was authorised for **that sitting**, and it is spent. Ask
    the owner for a second, or run this task under the same sitting only if the
    authorisation of 45-08 task 1 named it — an authorisation covers what it described and
    nothing more.

    With it: extend `SECTION_TARGETS` to include the five new tables — they exist now — and
    run `npm run verify:refusal` once. Then put the two transcripts side by side.

    **What must be identical:** the pair on `production_pipeline_rule`. It carried 16 rows
    before the split and it carries 16 after; a master reads them and a member reads none.
    If that changed, the six rewritten policies changed who can read, and constraint 3 of
    D-45-04 was violated — which stops the phase.

    **What must be `REFUSED — not measured`:** every table holding zero rows, which is all
    five new ones and five of the six calendar ones. On an empty table a refusal and an
    empty table are the same bytes, and the instrument exits 2 rather than reporting a pass.
    That is the correct outcome here and it is not repaired by seeding early — the seed has
    its own authorisation and its own plan.

    **What must be written and must not be overstated:** the four policy `qual`s read in
    task 2 name **four different keys**. That is criterion 1's structural half and it is a
    different sentence from *a viewer holding one section is refused the others*, which
    under D-45-03 has no subject in production and is closed by procedure P1 in a throwaway
    environment. Write both sentences; do not let the first stand in for the second.
  </action>
  <acceptance_criteria>
    - Both transcripts appear in the SUMMARY, side by side, with the comparison stated row by row.
    - The `production_pipeline_rule` pair is identical before and after: a positive count under master, zero under member.
    - Every zero-row table is recorded as **REFUSED — not measured**, and the run's exit code is recorded as it fell.
    - Every minted session is provably revoked: the transcript shows each token re-read and no longer resolving.
    - The SUMMARY states, in its own words, that the four-different-keys reading is **not** a proof of refusal between sections, and names P1 as what closes that.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — the instrument needs an owner authorisation and a minted session; its honest exit here is 2. The evidence is the two transcripts and their comparison.</automated>
  </verify>
  <done>The one thing this project can measure about a refusal says the same thing after the rewrite as before it, and what it cannot measure is written down as such.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine → production | The Management API with `SUPABASE_ACCESS_TOKEN`, connecting as a role that **bypasses RLS**, so nothing it reads is evidence about a refusal |
| the `POST` response → the claim that the schema changed | The response is a report; the catalogue is the fact |
| the applied migration → the deployed bundle | They hold the same strings only after the deploy, and the retirement waits for it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-08 | Tampering | production rows | mitigate | pre- and post-snapshot over the cascade set read from `pg_constraint` before the first request; delete nothing; removal, if ever, by primary key on an id captured at creation. D12 is the precedent that makes this mandatory rather than tidy |
| T-45-12 | Repudiation | the migration history | mitigate | the **migrations** endpoint, never `/database/query` and never `PUT`; five versions read back from the history |
| T-45-04 | Denial of service | the calendar surface | mitigate | `production.read` is left granted; the retirement is a separate plan behind a deploy gate. The window this avoids is measured, not imagined |
| T-45-04b | Elevation of privilege | the six rewritten policies | mitigate | the `qual`s are read back and each must still begin `( SELECT `; the eight grants are read back and must be the same two roles |
| T-45-11 | Information disclosure | the archive bucket | mitigate | read back `public = false`, one SELECT policy, no INSERT policy, no `anon` policy |
| T-45-03 | Information disclosure | the read-back output | mitigate | count rows and read catalogue names; do **not** `SELECT` the contents of any section table into the SUMMARY. The tables are empty at this point, and the discipline must survive the moment they are not |
| T-45-05 | Denial of service | the middleware bundle after the deploy | transfer, to the owner, in writing | the deploy is the owner's act; the note says a day without a night, and the first request made by hand |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | no package is installed |
</threat_model>

<verification>
- The recorded catalogue read-back, with the queries that produced it, per the VERIFICATION
  gate in `CLAUDE.md`: a verification without a single citation does not satisfy it.
- Pre- and post-snapshots, identical.
- `npm run verify:capabilities` red, recorded, unrepaired.
- `npm run verify:refusal` re-run and compared against its baseline.
- `npm run build` exits 0 — and **proved nothing about the database before this plan ran,
  which is exactly the false-positive state this plan closes.**
</verification>

<success_criteria>
- One dated authorisation, spent once, recorded here and in the procedures ledger.
- Five migrations in the history with their assigned versions.
- Five tables, their named constraints, one read arm each with its key named, zero write policies, one private bucket with no client write and no anonymous read.
- The six calendar policies moved to the new key with their InitPlan wrappers intact.
- `production.read` still granted, deliberately, and the reason recorded.
- Two identical snapshots across the cascade set.
- The declared red observed and left alone; the refusal instrument re-run and unchanged where it must be.
</success_criteria>

<output>
Create `.planning/phases/45-production-sections-section-by-section/45-08-SUMMARY.md` when done.
</output>
