---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 11
type: execute
wave: 4
depends_on: ["52-07", "52-05"]
files_modified:
  - src/components/layout/AppNav.tsx
  - src/app/(admin)/admin/(work)/layout.tsx
  - src/components/staff/StaffNav.tsx
  - scripts/verify-touch-targets.mjs
  - scripts/conversion-manifest.mjs
autonomous: true
requirements: [NAV-03, NAV-04]

must_haves:
  truths:
    - "Su tablet e desktop la colonna ha una lista sola: il pannello Management sostituisce la sezione 'Work', che non esiste piu' (D-52-08)"
    - "Da telefono, dentro uno strumento, la striscia scorrevole degli strumenti resta appesa in cima (sticky, opaca, senza blur) mentre si scorre il contenuto (NAV-04, D-52-11)"
    - "La striscia e la lista seguono l'ordine alfabetico del pannello, calcolato al rendering con la stessa funzione; la dichiarazione di STAFF_TABS non si riordina (D-52-27)"
    - "Il wrapper md:[--nav-inset-inline-start:14rem] del layout di lavoro resta: e' una delle due meta' del controllo E di verify:conversion"
  artifacts:
    - path: "src/components/staff/StaffNav.tsx"
      provides: "solo la striscia, appesa, in ordine alfabetico"
      contains: "sticky top-0"
    - path: "src/app/(admin)/admin/(work)/layout.tsx"
      provides: "AppNav senza workNav"
  key_links:
    - from: "src/components/staff/StaffNav.tsx"
      to: "src/lib/routes/staff-tabs.ts sortTabsByLabel"
      via: "lo stesso ordine del pannello (D-52-27)"
      pattern: "sortTabsByLabel"
---

<objective>
Togliere la colonna «Work» a favore del pannello, e appendere la striscia degli
strumenti da telefono.

Purpose: D-52-08 (una lista sola in colonna), NAV-04 / D-52-11 (la striscia resta
in vista mentre si scorre uno strumento lungo, cosi' si cambia strumento senza
tornare su), D-52-27 (un ordine solo). La prop `workNav` e la forma `"column"`
di `StaffNav` perdono il loro unico consumatore e escono nello stesso commit, con
le due righe dei gate che le descrivono.
Output: `AppNav.tsx` senza `workNav`, `(work)/layout.tsx`, `StaffNav.tsx` a una
forma, `verify-touch-targets.mjs:701` e `conversion-manifest.mjs:194` aggiornati.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@./CLAUDE.md
@.claude/rules/nextjs-architecture.md
@.claude/rules/access-gating.md

<interfaces>
`src/components/staff/StaffNav.tsx`: `StaffNavProps.form: "strip" | "column"`
(`:85-99`, «the work layout mounts both»); `COLUMN_ENTRY` (`:102-104`); ramo
`column` (`:111-141`); ramo strip (`:143-177`): `<nav aria-label="Work surfaces"
className="mb-6 px-6 md:hidden">` con `Chip` (`min-h-11`, `aria-current`).
`src/app/(admin)/admin/(work)/layout.tsx:150-186`: `<AppNav … workNav={<StaffNav
capabilities={staffCapabilities} form="column" />} />`, poi il wrapper
`md:[--nav-inset-inline-start:14rem]` con `<StaffNav … form="strip" />` e
`{children}`.
`src/components/layout/AppNav.tsx`: prop `workNav?: ReactNode` (`:107-112`), il
blocco `:292-306` (`!isPhone && workNav`), `SectionHeading` importato per la
colonna Work.
Gate: `verify-touch-targets.mjs:701` `['StaffNav', 'src/components/staff/StaffNav.tsx',
'the eight work tabs in two forms']`; `conversion-manifest.mjs:194` «the eight work
tabs in two forms — mounted by the work layout». Nessun antenato con `overflow`
(`globals.css:471-501`): `sticky` funziona. Scala z: `z-10` (41 §10, aggiornata
dal piano 52-02).
`sortTabsByLabel` esportata da `src/lib/routes/staff-tabs.ts` (piano 52-07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Via la colonna Work — workNav, la forma column, e il layout che la montava</name>
  <files>src/components/layout/AppNav.tsx, src/app/(admin)/admin/(work)/layout.tsx, src/components/staff/StaffNav.tsx</files>
  <read_first>
    - `src/components/layout/AppNav.tsx` — la prop `workNav`, il blocco della colonna Work e la lista Management del piano 52-07
    - `src/app/(admin)/admin/(work)/layout.tsx` per intero
    - `src/components/staff/StaffNav.tsx` per intero
    - `scripts/verify-conversion.mjs` righe 3095-3150 — controllo E (mount di `AppNav` ↔ wrapper)
  </read_first>
  <action>
    `AppNav.tsx`: togliere la prop `workNav` da `AppNavProps` e dal
    destrutturamento, il blocco `!isPhone && workNav` con il suo `SectionHeading`
    (e l'import di `SectionHeading` se resta senza usi), e aggiornare la docblock
    della prop e della colonna: da D-52-08 la colonna porta una lista sola, il
    pannello Management, che parte aperto dentro uno strumento.
    `(work)/layout.tsx`: `<AppNav>` senza `workNav`; il wrapper
    `md:[--nav-inset-inline-start:14rem]`, il suo commento e la `StaffNav` in forma
    striscia **restano**.
    `StaffNav.tsx`: la forma `"column"`, `COLUMN_ENTRY` e il ramo relativo escono;
    la prop `form` esce (un solo albero) oppure resta a un solo valore — scegliere
    di toglierla e aggiornare l'unico chiamante; aggiornare la docblock («the work
    layout mounts both» → una forma, la striscia da telefono; la colonna e' il
    pannello di `AppNav` dal 2026-09-23, D-52-08).
    Nessun cambiamento ai tredici mount di `<AppNav>` diversi dal layout di lavoro.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:conversion &amp;&amp; test $(/usr/bin/grep -rc "workNav" src/components/layout/AppNav.tsx "src/app/(admin)/admin/(work)/layout.tsx" | /usr/bin/grep -c ":[1-9]") -eq 0 &amp;&amp; test $(/usr/bin/grep -c '"column"' src/components/staff/StaffNav.tsx) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify:conversion` escono 0
    - Zero `workNav` in `AppNav.tsx` e nel layout di lavoro; zero `"column"` in `StaffNav.tsx`
    - Il wrapper `md:[--nav-inset-inline-start:14rem]` e' ancora nel layout di lavoro (controllo E verde)
  </acceptance_criteria>
  <done>In colonna c'e' una lista sola.</done>
</task>

<task type="auto">
  <name>Task 2: La striscia appesa, in ordine alfabetico — e i gate che la descrivono</name>
  <files>src/components/staff/StaffNav.tsx, scripts/verify-touch-targets.mjs, scripts/conversion-manifest.mjs</files>
  <read_first>
    - `src/components/staff/StaffNav.tsx` (dopo il task 1)
    - `src/components/layout/AppNav.tsx` righe 159-188 — la causa documentata del difetto iOS con `backdrop-blur` su un elemento che si muove
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §C
    - `scripts/verify-touch-targets.mjs` righe 690-705; `scripts/conversion-manifest.mjs` righe 185-198
    - `src/lib/routes/staff-tabs.ts` — `sortTabsByLabel`
  </read_first>
  <action>
    Nella striscia: `<nav aria-label="Work surfaces" className="sticky top-0 z-10
    mb-6 border-b border-line bg-ground px-6 py-2 md:hidden">` — **opaca, nessun
    `backdrop-blur`** (commento: la causa documentata in `AppNav.tsx` del difetto
    iOS del 2026-08-14); la costruzione dello scorrimento (`staff-nav-scroll`,
    `-mx-6 px-6`) resta. Le tab si disegnano con
    `sortTabsByLabel(visibleStaffTabs(capabilities))` (D-52-27); il `Chip`
    selezionato porta `scrollMarginInline: 24px` come oggi, se c'e'. Commento:
    NAV-04 / D-52-11, 2026-09-23; `viewportFit` non si aggiunge (con `cover`
    `top-0` finirebbe sotto la barra di stato in standalone).
    `verify-touch-targets.mjs:701`: la descrizione diventa «the work tabs as one
    pinned strip, alphabetical» (nessun altro cambiamento alla riga o al gate).
    `conversion-manifest.mjs:194`: la descrizione diventa «the work tabs as one
    pinned strip — mounted by the work layout; the column is AppNav's Management
    panel since phase 52». Nessuna altra riga dei due file.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:touch-targets &amp;&amp; npm run verify:conversion &amp;&amp; npm run verify:no-viewport-read &amp;&amp; /usr/bin/grep -q "sticky top-0 z-10" src/components/staff/StaffNav.tsx &amp;&amp; test $(/usr/bin/grep -c "backdrop-blur" src/components/staff/StaffNav.tsx) -eq 0 &amp;&amp; /usr/bin/grep -q "sortTabsByLabel" src/components/staff/StaffNav.tsx &amp;&amp; test $(/usr/bin/grep -c "in two forms" scripts/verify-touch-targets.mjs scripts/conversion-manifest.mjs | /usr/bin/grep -c ":[1-9]") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `verify:touch-targets`, `verify:conversion`, `verify:no-viewport-read` escono 0
    - La striscia porta `sticky top-0 z-10`, `bg-ground` opaco, `border-b border-line`, nessun `backdrop-blur`
    - La striscia usa `sortTabsByLabel`; `DECLARED` in `staff-tabs.ts` non cambia ordine
    - «in two forms» non compare piu' in `verify-touch-targets.mjs` ne' in `conversion-manifest.mjs`
  </acceptance_criteria>
  <done>Da telefono, in fondo a Members, la striscia e' ancora li'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (tab visibili) | le tab arrivano filtrate per capability; il client ordina, non filtra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-51 | Elevation of privilege | una tab disegnata a chi non la puo' aprire | mitigate | la sorgente resta `visibleStaffTabs` con l'asserzione a caricamento; l'ordinamento non aggiunge ne' toglie voci |
| T-52-52 | Tampering | un gate che descrive una forma che non esiste piu' | mitigate | le due righe di descrizione si muovono nello stesso commit |
| T-52-53 | Denial of Service | striscia che copre il contenuto o finisce sotto la barra di stato | mitigate | `z-10`, opaca, niente `viewportFit`; P-52-C su telefono |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `npm run build`, `verify:touch-targets`, `verify:conversion`, `verify:no-viewport-read` verdi
- P-52-C sul laboratorio (piano 52-14)
</verification>

<success_criteria>
NAV-03 e NAV-04 chiusi: una lista in colonna, una striscia appesa da telefono,
un ordine solo.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-11-SUMMARY.md` when done
</output>
