---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 07
type: execute
wave: 3
depends_on: ["52-06"]
files_modified:
  - src/lib/rbac/roles.ts
  - src/lib/routes/staff-tabs.ts
  - src/components/layout/AppNav.tsx
  - src/app/globals.css
  - src/app/(members)/account/page.tsx
  - src/components/account/ManagementSection.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "Chi lavora vede in barra quattro voci, in quest'ordine: Events · Check-in · TASK · Management; Home non esiste e / resta il redirect a /events (D-52-01, D-52-02)"
    - "TASK e' disegnata ma spenta: button aria-disabled, nessun href, nessun onClick, nessun tooltip, nessun dato letto — e compare a master, organizer e staff, mai all'attendee (D-52-03, D-52-24)"
    - "Chi non ha voci nel pannello (attendee, anonimo, contesto fallito) vede Events · Account; Account per l'anonimo porta al login (D-52-04)"
    - "Check-in resta assente per chi non ha door.operate per ruolo o per assegnazione, non spento (D-52-05)"
    - "Il pannello e' visibleStaffTabs + Gallery (se gallery.view) + Account, in ordine alfabetico al rendering; da telefono e' un foglio sopra la barra che si chiude toccando fuori, la stessa voce, Escape, una riga o cambiando indirizzo; da tablet si espande nella colonna, aperto dentro una voce del pannello (D-52-07, D-52-08, D-52-09, D-52-27)"
    - "Lo strumento /admin/events si chiama 'Manage events' (D-52-10)"
    - "La barra si nasconde mentre un campo ha il fuoco, solo sotto pointer: coarse, solo CSS (D-52-06)"
    - "La sezione 'Management Tools' della pagina Account non esiste piu' e ManagementSection.tsx e' cancellato senza importatori (NAV-03)"
    - "La voce Gallery del pannello ha la stessa asserzione a caricamento delle tab: resolveRoute('/gallery')?.key === CAP.GALLERY_VIEW (NAV-02)"
  artifacts:
    - path: "src/lib/rbac/roles.ts"
      provides: "la funzione pura che restituisce { bar, panel } da sessione, ruolo, capability"
      contains: "TASK"
    - path: "src/components/layout/AppNav.tsx"
      provides: "barra a quattro voci, TASK spenta, pulsante Management, foglio da telefono, lista in colonna"
      contains: "aria-disabled"
    - path: "src/app/globals.css"
      provides: "la regola :has(:focus) sotto pointer: coarse e la keyframe del foglio"
      contains: "pointer: coarse"
  key_links:
    - from: "src/components/layout/AppNav.tsx"
      to: "src/lib/rbac/roles.ts"
      via: "la funzione pura riceve le chiavi gia' risolte dal server; AppNav disegna e non filtra"
      pattern: "from \"@/lib/rbac/roles\""
    - from: "src/lib/rbac/roles.ts (pannello)"
      to: "src/lib/routes/staff-tabs.ts visibleStaffTabs"
      via: "stessa sorgente della striscia, piu' Gallery e Account"
      pattern: "visibleStaffTabs"
    - from: "voce Gallery del pannello"
      to: "src/lib/routes/capability-routes.ts"
      via: "asserzione a caricamento del modulo"
      pattern: "resolveRoute\\(\"/gallery\"\\)"
---

<objective>
La barra a quattro voci e il pannello Management — la funzione pura che decide,
e `AppNav` che disegna.

Purpose: NAV-01 (riscritto il 2026-09-23), NAV-03, e la voce Gallery di NAV-02.
La regola della barra resta D-50-09 — sessione, ruolo, capability — e la
presenza del pannello e' **derivata** da quelle tre, non un quarto criterio
(D-52-04). `AppNav` e' `"use client"`: non filtra, non taglia, non legge il
viewport; riceve le chiavi dal server e disegna due alberi scelti da CSS.
Ogni voce disegnata ha la sua riga nella mappa e la sua guardia: nascondere una
voce non protegge una rotta (`access-gating.md`).
Output: `roles.ts`, `staff-tabs.ts` (etichetta), `AppNav.tsx`, `globals.css`,
la pagina Account senza «Management Tools», `ManagementSection.tsx` cancellato.
La colonna «Work» e la prop `workNav` escono nel piano 52-11, che dipende da
questo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@./CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/nextjs-architecture.md

<interfaces>
`src/lib/rbac/roles.ts` oggi: `interface NavItem` (`:120-192`, campi che pongono
una domanda obbligatori, disciplina `:182-191`); `NAV_ITEMS` (`:214-347`) con
Events, Gallery (`:224-252`), Check-in (`DOOR_HREF`, `:310`), Account (`:339`,
`hrefWhenAnonymous`); `getVisibleNavItems(role, capabilities,
liveAssignmentCapabilities)` (`:432-517`) — il ramo «ruolo **o** assegnazione
viva» (`:465-488`) e `null` che rifiuta (`:475-478`) restano identici per Check-in.

`src/lib/routes/staff-tabs.ts`: `STAFF_TABS` (`:345`), `visibleStaffTabs(held)`
(`:358-363`, sole capability di ruolo), asserzione a caricamento (`:323-342`:
`href` ↔ `capability` via `resolveRoute`); `:122` `label: "Events"`. Importa solo
`keys` e `capability-routes` (`:75-77`): `roles.ts` puo' importarlo senza cicli.

`src/components/layout/AppNav.tsx`: props `role`, `capabilities`,
`liveAssignmentCapabilities`, `form?: "responsive" | "phone"` (`:106`),
`workNav?: ReactNode` (`:112`, **resta** in questo piano, esce nel 52-11);
`icons` (`:115-147`); `NAV_PHONE`/`NAV_RESPONSIVE`; il `<nav>` con
`[transform:translate3d(0,0,0)]` (`:185-188`, ragione `:159-184`); `ENTRY_PHONE`
/`ENTRY_RESPONSIVE`; il `Link` con `isPhone ? ENTRY_PHONE : ENTRY_RESPONSIVE`
(`:272-288`) — **pinnato da `verify-touch-targets.mjs:790`, deve risolvere UN
elemento**; `INDICATOR_PHONE`/`INDICATOR_RESPONSIVE` (`:217-222`); la colonna
Work (`:292-306`). Tredici mount di `<AppNav>`; la porta monta `form="phone"`
(`DoorSurface.tsx:153-160`).

Scala z (41 §10 + 52-UI-SPEC §B.2): barra `z-50`; foglio e scrim `z-50`
(fratelli, non figli, del `<nav>`); `StickyBuyBar` `z-40`; toast `z-[70]`.
`--nav-inset-block-end` (`globals.css:335`, azzerata da `md` a `:361-365`) **non
cambia**. Nessuna regola `:has(` o `@media (pointer` esiste oggi in `globals.css`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: La funzione pura — barra e pannello da sessione, ruolo, capability</name>
  <files>src/lib/rbac/roles.ts, src/lib/routes/staff-tabs.ts</files>
  <read_first>
    - `src/lib/rbac/roles.ts` per intero (docblock degli esiti `:353-422`, regola della riscrittura `:355-360`, disciplina dei campi `:175-191`)
    - `src/lib/routes/staff-tabs.ts` righe 70-130, 300-365
    - `src/lib/routes/capability-routes.ts` — `resolveRoute` e la voce `[CAP.GALLERY_VIEW]` scritta dal piano 52-06
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §A (righe 355-405) e §B.1-B.2
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §A.1, §B.1
  </read_first>
  <action>
    In `staff-tabs.ts`: `:122` `label: "Events"` → `"Manage events"` (D-52-10), con
    una riga di commento. **Nessun riordino della dichiarazione** (D-52-27):
    aggiornare il paragrafo `:95-120` sulla contiguita' degli strumenti di
    produzione dicendo che la dichiarazione tiene il raggruppamento e il
    rendering applica l'ordine alfabetico. Esportare una funzione
    `sortTabsByLabel(tabs)` che ordina per `label.localeCompare(b.label, "en")`
    senza mutare l'array: una sola implementazione dell'ordine, usata dal
    pannello qui e dalla striscia nel piano 52-11.
    In `roles.ts`: un tipo discriminato `BarEntry` con tre forme — `{ kind:
    "link"; href: Route; label: string; icon: string; hrefWhenAnonymous?: … }`,
    `{ kind: "disabled"; label: "TASK"; icon: string }` (nessun `href`),
    `{ kind: "panel"; label: "Management"; icon: string }` — e `PanelEntry =
    { href: Route; label: string }`; i campi che pongono una domanda sono
    obbligatori. Una funzione pura esportata `getNavigation(role, capabilities,
    liveAssignmentCapabilities): { bar: BarEntry[]; panel: PanelEntry[] }`:
    pannello = `visibleStaffTabs(capabilities)` + `{ href: "/gallery", label:
    "Gallery" }` se `capabilities` include `CAP.GALLERY_VIEW`; se la lista e'
    vuota, pannello vuoto; altrimenti + `{ href: "/account", label: "Account" }`,
    ordinato con `sortTabsByLabel`. Barra = Events · Check-in (predicato **identico**
    a oggi: `door.operate` per ruolo **o** assegnazione viva; `null` rifiuta) · TASK
    (solo se `role` e' `master`, `organizer` o `staff` — D-52-24) · poi `panel` se
    il pannello non e' vuoto, altrimenti Account (con `hrefWhenAnonymous` →
    `/login` senza sessione). Contesto fallito → Events · Account (fail closed,
    coerente col middleware).
    La voce Gallery esce da `NAV_ITEMS`; il suo commento («Il cancello vero lo
    costruisce `NAV-02`, fase 52») si chiude dicendo dove e' andata (pannello,
    sotto `gallery.view`) senza ricopiare nomi di campi cancellati. Aggiungere,
    a caricamento del modulo, l'asserzione nella forma di `staff-tabs.ts:323-342`:
    `resolveRoute("/gallery")?.key === CAP.GALLERY_VIEW`, con due messaggi
    distinti (rotta assente dalla mappa / chiave diversa).
    `getVisibleNavItems`: `AppNav` passa a `getNavigation` nel task 2 dello stesso
    piano; se dopo il task 2 nessun file lo importa (grep: oggi solo `AppNav.tsx`),
    toglierlo. I due commenti che lo nominano come storia
    (`src/lib/capabilities/server.ts:203`, `src/lib/supabase/middleware.ts:351`)
    **non si toccano** in questo piano — il middleware e' fuori dal suo perimetro —
    e il SUMMARY li elenca come riferimento a un nome uscito, per la fase 57.
    Riscrivere la docblock degli esiti con la tabella di UI-SPEC §A.1 (anonimo,
    attendee, staff non assegnato / assegnato, organizer, master, contesto
    fallito) — quarta riscrittura, vale la regola `:355-360`.
    Home non torna: nessuna voce, D-52-02.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; /usr/bin/grep -q "Manage events" src/lib/routes/staff-tabs.ts &amp;&amp; /usr/bin/grep -q "getNavigation" src/lib/rbac/roles.ts &amp;&amp; /usr/bin/grep -q 'resolveRoute("/gallery")' src/lib/rbac/roles.ts &amp;&amp; /usr/bin/grep -q "sortTabsByLabel" src/lib/routes/staff-tabs.ts &amp;&amp; test $(/usr/bin/grep -c 'href: "/"' src/lib/rbac/roles.ts) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `staff-tabs.ts` porta `label: "Manage events"` per `/admin/events` e `sortTabsByLabel`; l'ordine di `DECLARED` non cambia (`git diff` non sposta righe della dichiarazione)
    - `roles.ts` esporta `getNavigation` con `BarEntry` a tre forme; TASK filtrata sui ruoli `master`, `organizer`, `staff`; nessuna voce con `href: "/"`
    - L'asserzione `resolveRoute("/gallery")?.key === CAP.GALLERY_VIEW` e' eseguita a caricamento del modulo, con due messaggi distinti
    - La docblock degli esiti porta i sette casi di UI-SPEC §A.1
  </acceptance_criteria>
  <done>Una funzione pura dice a ciascuno cosa sta in barra e nel pannello, e la gallery non puo' comparire in un pannello senza la sua riga nella mappa.</done>
</task>

<task type="auto">
  <name>Task 2: AppNav — quattro voci, TASK spenta, il foglio da telefono, la lista in colonna, la barra che si nasconde al fuoco</name>
  <files>src/components/layout/AppNav.tsx, src/app/globals.css</files>
  <read_first>
    - `src/components/layout/AppNav.tsx` per intero
    - `src/components/staff/StaffNav.tsx` righe 100-141 — la forma delle righe in colonna (`COLUMN_ENTRY`, indicatore al bordo)
    - `src/app/globals.css` righe 320-370 (`--nav-inset-block-end`) e 460-510
    - `scripts/verify-touch-targets.mjs` righe 780-800 (il frammento pinnato); `scripts/verify-dialogs.mjs` righe 1085-1150 (`OVERLAY_PARTS`, niente `inset-0`); `scripts/verify-no-viewport-read.mjs` righe 175-190; `scripts/verify-conversion.mjs` controllo A (palette) e E (mount)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §0, §A.2-A.5, §B.2-B.3, Accessibility Contract
    - `src/components/ui/Dialog.tsx` righe 205-215, 270-285 — perche' il foglio NON e' un `Dialog` (titolo obbligatorio, velo inerte sulla barra)
  </read_first>
  <action>
    `AppNav` passa a `getNavigation(...)`. Disegno, per UI-SPEC:
    **Voce-link** (Events, Check-in, Account): resta **l'unico** `Link` con
    `isPhone ? ENTRY_PHONE : ENTRY_RESPONSIVE`; stato corrente in tre canali
    (`text-accent`, indicatore 2 px, `aria-current="page"`). Tutte le voci portano
    il pozzetto `relative inline-flex h-7 w-10 items-center justify-center
    rounded-full border border-transparent` con l'icona `h-6 w-6`.
    **TASK spenta** (D-52-03): `<button type="button" aria-disabled="true">` senza
    `onClick` ne' `href`, classe con `min-h-11` **letterale**, `text-faint`,
    pozzetto `border-dashed border-faint`, `cursor-default`, nessun `active:`,
    nessun `title`; commento accanto al pozzetto con la geometria del posto del
    badge di TASK-04 (UI-SPEC §A.4) e nessun elemento disegnato li'.
    **Management**: due pulsanti in due alberi scelti da CSS — nella barra da
    telefono (`isPhone ? "" : "md:hidden"`) e nella colonna (`hidden md:flex`
    solo nel form responsive) — ciascuno `<button type="button" aria-expanded
    aria-controls>` con `min-h-11` letterale e il **proprio** stato
    (`sheetOpen`, `columnOpen`): nessuna lettura del viewport. Corrente quando
    `pathname` comincia con l'`href` di una voce del pannello (niente
    `aria-current` sul pulsante). Icona `squares-2x2`, che in barra diventa
    `x-mark` da aperto; in colonna `chevron-down` in coda che ruota
    (`transition-transform duration-150 motion-reduce:transition-none`). Icone
    Heroicons outline 24 px `strokeWidth={1.5}` nel dizionario `icons`, stessa
    forma di quelle presenti; il commento sul glifo `home` si chiude: Home non
    torna (D-52-02).
    **Il foglio** (D-52-07): scrim e pannello **fratelli** del `<nav>`, resi
    subito dopo, mai figli (il `<nav>` e' trasformato). Scrim: `fixed inset-x-0
    top-0 bottom-[var(--nav-inset-block-end)] z-50 bg-black/80`, `aria-hidden`,
    non `button`, un tocco chiude. Pannello: `<nav aria-label="Management"
    id="management-sheet">`, `fixed inset-x-0 bottom-[var(--nav-inset-block-end)]
    z-50 mx-auto max-w-lg rounded-t-2xl border-t border-line bg-surface py-2
    max-h-[calc(100dvh-var(--nav-inset-block-end)-4rem)] overflow-y-auto`;
    righe `Link` con `relative flex min-h-11 items-center px-6 text-sm text-ink-2
    active:bg-raised` + `FOCUS_RING`, corrente `text-accent` + indicatore +
    `aria-current="page"`; nessuna maniglia, titolo o Close. Nel form responsive
    scrim e pannello portano `md:hidden`; nel form `phone` compaiono a ogni
    larghezza. **Niente `inset-0`, niente `<dialog>`, niente `Dialog.tsx`**:
    dichiararlo nel codice (disclosure non modale, `Escape`, ritorno del fuoco,
    perche' non `Dialog`). Chiusura: tocco sullo scrim, tocco sulla stessa voce,
    `Escape` (fuoco al pulsante), tocco su una riga (anche la corrente), cambio di
    `usePathname()` (Pitfall 4). Apertura con la keyframe di `globals.css`
    (160 ms `ease-out`, `translateY(1rem)`+opacita'), `motion-reduce:animate-none`;
    chiusura istantanea.
    **La lista in colonna** (D-52-08): `<nav aria-label="Management"
    id="management-column">` subito sotto la voce Management, `flex flex-col
    gap-1`, chiusa con l'attributo **`hidden`** (non `CollapsibleSection`:
    Pitfall 9); righe `Link` `relative flex min-h-11 items-center rounded-xl
    ps-16 pe-4 text-sm text-muted hover:text-ink` + `FOCUS_RING`, corrente con
    indicatore al bordo e `aria-current`. Stato iniziale: aperta se il `pathname`
    comincia con l'`href` di una voce del pannello; un `useEffect` sul
    `pathname` la **riapre** entrando in una voce del pannello e non la chiude
    mai da sola. La colonna Work (`workNav`) resta in questo piano: la toglie il
    52-11.
    **Docblock di `AppNav`**: la sezione «It does not collapse … No toggle, no
    drawer» si **rovescia con la sua ragione** — RESP-04 chiede che le superfici
    di lavoro mostrino la navigazione senza menu da tablet in su, e dentro uno
    strumento la lista parte aperta (D-52-08), quindi RESP-04 regge dove si
    applica; fuori dagli strumenti la lista chiusa non nasconde una superficie di
    lavoro. Citare «hiding a nav item is not protecting a route» (da
    `ManagementSection.tsx:21-28` / `staff-tabs.ts:20`).
    **`globals.css`**: la keyframe del foglio; e, dentro `@media (pointer:
    coarse)`, la regola che mette `display: none` su `[data-nav-form="phone"]`,
    sullo scrim e sul foglio quando `body:has(:is(input:not([type="checkbox"],
    [type="radio"], [type="button"], [type="submit"], [type="hidden"]), textarea,
    select):focus)`, e su `[data-nav-form="responsive"]` solo sotto `48rem`; il
    `<nav>` porta `data-nav-form={form}`. Commento: D-52-06; nessuna lettura del
    viewport; `--nav-inset-block-end` non si azzera; `:has()` da provare sul
    telefono (assunzione A2, P-52-F). Colori solo da token.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:touch-targets &amp;&amp; npm run verify:dialogs &amp;&amp; npm run verify:no-viewport-read &amp;&amp; npm run verify:conversion &amp;&amp; npm run verify:breakpoints &amp;&amp; test $(/usr/bin/grep -c "isPhone ? ENTRY_PHONE : ENTRY_RESPONSIVE" src/components/layout/AppNav.tsx) -eq 1 &amp;&amp; /usr/bin/grep -q 'aria-disabled="true"' src/components/layout/AppNav.tsx &amp;&amp; test $(/usr/bin/grep -v "^\s*//\|^\s*\*" src/components/layout/AppNav.tsx | /usr/bin/grep -c "inset-0") -eq 0 &amp;&amp; /usr/bin/grep -q "pointer: coarse" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `verify:touch-targets`, `verify:dialogs`, `verify:no-viewport-read`, `verify:conversion`, `verify:breakpoints` escono 0
    - Il frammento `isPhone ? ENTRY_PHONE : ENTRY_RESPONSIVE` compare una sola volta
    - TASK e' un `button` con `aria-disabled="true"`, `min-h-11`, nessun `onClick`, nessun `href`, nessun `title`
    - Ogni `button`/`Link` nuovo in `AppNav.tsx` porta `min-h-11` letterale
    - Fuori dai commenti, `AppNav.tsx` non contiene `inset-0` ne' `<dialog`; scrim e foglio sono fratelli del `<nav>` (dichiarato nel codice)
    - Due pulsanti Management con due stati distinti; zero `matchMedia`/`innerWidth`/`useSyncExternalStore`
    - `globals.css` contiene la regola sotto `@media (pointer: coarse)` con `:has(` e la keyframe del foglio con la sua durata
    - La docblock di `AppNav` rovescia «It does not collapse» con la ragione RESP-04
  </acceptance_criteria>
  <done>La barra ha quattro voci per chi lavora, due per gli altri, e il pannello si apre e si chiude come deciso senza leggere il viewport.</done>
</task>

<task type="auto">
  <name>Task 3: Via «Management Tools» dalla pagina Account, e ManagementSection</name>
  <files>src/app/(members)/account/page.tsx, src/components/account/ManagementSection.tsx</files>
  <read_first>
    - `src/app/(members)/account/page.tsx` righe 1-100 (docblock, `:62-64` dice gia' che NAV-03 la toglie), 430-460, 780-800
    - `src/components/account/ManagementSection.tsx` per intero — il docblock `:21-28` («Hiding a nav item is not protecting a route»), che non si perde: e' gia' in `staff-tabs.ts:20` ed e' citato nella docblock nuova di `AppNav`
  </read_first>
  <action>
    Togliere dalla pagina Account `canReachManagementTools`, il mount di
    `<ManagementSection …/>` e il suo import; togliere anche `visibleStaffTabs` e
    `managementCapabilities` se restano senza consumatori (se `managementCapabilities`
    serve ad altro, lasciarlo). Aggiornare la docblock `:62-64`: NAV-03 l'ha
    tolta il 2026-09-23, fase 52; le voci stanno nel pannello Management della
    barra. Poi `grep` sugli importatori di `ManagementSection` in `src/`: se zero,
    **cancellare** `src/components/account/ManagementSection.tsx`.
    Se dopo la rimozione la pagina porta un wrapper `CollapsibleSection` vuoto o un
    separatore orfano, toglierlo; nient'altro della pagina cambia (email, password,
    Sign out restano).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:conversion &amp;&amp; test ! -f src/components/account/ManagementSection.tsx &amp;&amp; test $(/usr/bin/grep -rl "ManagementSection" src | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify:conversion` escono 0
    - `src/components/account/ManagementSection.tsx` non esiste; zero file in `src/` che nominano `ManagementSection`
    - La pagina Account non contiene la stringa «Management Tools»
    - La docblock della pagina dice che NAV-03 l'ha tolta, con la data
  </acceptance_criteria>
  <done>Management vive in un posto solo: il pannello.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (`getAccessContext`) → client (`AppNav`) | le chiavi arrivano risolte; il client disegna e non decide |
| barra/pannello → rotte | una voce disegnata e' una promessa che la mappa e la guardia devono mantenere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-27 | Elevation of privilege | voce del pannello verso una rotta senza riga nella mappa | mitigate | la voce Gallery ha l'asserzione `resolveRoute("/gallery")?.key === CAP.GALLERY_VIEW` a caricamento; le tab hanno la loro (`staff-tabs.ts:323-342`); la guardia e il middleware restano il confine (piano 52-08) |
| T-52-28 | Tampering | decisione d'accesso presa nel client | mitigate | la funzione pura riceve le chiavi dal server; `AppNav` non filtra per larghezza (`verify:no-viewport-read`) |
| T-52-29 | Information disclosure | TASK spenta che legge dati o mostra un contatore | mitigate | nessun `onClick`, nessun `href`, nessuna lettura: il posto del badge e' solo un commento (D-52-03) |
| T-52-30 | Denial of Service (operativo) | foglio che copre la barra della porta | mitigate | foglio e scrim finiscono dove comincia `--nav-inset-block-end`; la barra resta toccabile; chiusura su cambio di indirizzo |
| T-52-31 | Repudiation | Check-in spento come secondo verdetto sull'operatore | mitigate | Check-in resta assente, non spento (D-52-05); predicato identico al middleware |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato; icone SVG incollate |
</threat_model>

<verification>
- `npm run build`, `verify:touch-targets`, `verify:dialogs`, `verify:no-viewport-read`, `verify:conversion`, `verify:breakpoints` verdi
- P-52-A sul laboratorio (piano 52-14)
</verification>

<success_criteria>
NAV-01 e NAV-03 sono veri nella forma decisa il 2026-09-23, e il pannello non
puo' promettere una rotta che il middleware non apre.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-07-SUMMARY.md` when done
</output>
