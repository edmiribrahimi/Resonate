---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 15
type: execute
wave: 9
depends_on: ["52-14", "52-18", "52-19"]
files_modified:
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, NAV-07]

must_haves:
  truths:
    - "L'autorizzazione a scrivere in produzione e' scritta PRIMA che la domanda venga posta, con i numeri di oggi riletti read_only, il perimetro passo per passo e cio' che resta fuori (ai-engineering.md, D-52-12, D-52-25)"
    - "In produzione l'ordine e' stato M1 → push di main e deploy READY → M2, con l'ora UTC di ogni passo; mai M2 prima del deploy, mai il codice prima di M1 (vincolo §I.6, Pitfall 2 e 10)"
    - "Dopo l'atto verify:capabilities e verify:refusal (col gruppo gallery) sono verdi contro la produzione: 16 chiavi, 31 concessioni (16/14/1/0), righe approvate illeggibili senza gallery.view"
    - "L'autorizzazione e' dichiarata ESAURITA con l'ora, e da li' in poi non autorizza piu' niente"
  artifacts:
    - path: ".planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md"
      provides: "perimetro, numeri di oggi, finestre, domanda letterale, registro d'uso, chiusura"
      contains: "ESAURITA"
      min_lines: 80
  key_links:
    - from: "52-AUTHORISATION.md"
      to: "52-ESITI.md"
      via: "il registro d'uso con l'ora di ogni passo"
      pattern: "UTC"
---

<objective>
Portare la fase in produzione dentro **un atto datato**: M1, il deploy del codice,
M2 — in quest'ordine, e con il deploy **in mezzo** come passo esplicito.

Purpose: D-52-12 (la chiave in produzione sotto atto), D-52-25 (NAV-07 chiude in
produzione). Scritture su un database senza PITR: `ai-engineering.md` dice come
si fanno, e le fasi 50 e 51 hanno mostrato che scrivere l'autorizzazione **prima**
della domanda cambia cosa viene autorizzato. La rimozione per chiave e la
ri-spogliatura (D-52-30, D-52-31) sono un **secondo atto**, nel piano 52-16:
decisioni diverse, domande diverse.
Output: `52-AUTHORISATION.md` ESAURITA, produzione allineata, gate verdi contro la
produzione.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-13-SUMMARY.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-14-SUMMARY.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION-COMMENT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION-REFUSAL.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-13-PLAN.md
@./CLAUDE.md
@.claude/rules/ai-engineering.md
@.claude/rules/meta-gates.md
@.claude/rules/supabase-data.md

<interfaces>
Perimetro possibile, e non un byte oltre:
  (a) `POST /v1/projects/<produzione>/database/migrations` con M1
      (`20260923180000_gallery_view_and_media_paths.sql`) e rilettura `read_only`
  (b) `git push origin main` → deploy Vercel di produzione, atteso `READY`
  (c) `POST …/database/migrations` con M2 (`20260923180100_gallery_close_data.sql`)
      e rilettura `read_only`
  (d) `npm run verify:capabilities` e `npm run verify:refusal` contro la produzione
      (il secondo conia sessioni di prova: e' nel perimetro solo se scritto qui,
      nella forma di `51-AUTHORISATION-REFUSAL.md`)
  (e) la sonda anonima: `GET` dell'indirizzo pubblico di un oggetto approvato
      (URL mai scritto in `.planning/`), con l'ora e la finestra di cache
Produzione: il ref e' la costante `PRODUCTION_REF` degli script; il laboratorio
non compare in questo documento.

Le finestre che l'ordine apre, da dichiarare PRIMA:
- fra (a) e (b): il codice vecchio in produzione scrive righe senza `storage_path`
  (nullable in M1: nessun guasto); organizer e staff ricevono `gallery.view` ma il
  codice vecchio non ha ancora il cancello (la gallery e' ancora aperta a ogni
  sessione, come oggi)
- fra (b) e (c): il codice nuovo firma sul bucket ancora pubblico; un attendee
  vede gia' la barra nuova e il rifiuto su `/gallery`, ma via API legge ancora le
  righe approvate (la chiusura sui dati arriva con (c)); righe nate in (a)→(b)
  senza `storage_path` non si firmano (lo dice il testo dell'immagine) fino al
  ribackfill di M2
- dopo (c): la CDN puo' servire un oggetto gia' in cache fino al `max-age`
  (default 3600 s), il service worker 1 h per gli URL cross-origin, e
  l'ottimizzatore d'immagini per le miniature della moderazione di prima — si
  esauriscono da sole e si registrano, non si «convertono»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere 52-AUTHORISATION.md PRIMA di chiedere, con i numeri di oggi</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION-COMMENT.md` per intero — frontmatter, «Il perimetro, e non un byte oltre», «La domanda, alla lettera», «Registro d'uso», «Chiusura»
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION-REFUSAL.md` — la forma dell'autorizzazione a `verify:refusal` in produzione
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-13-PLAN.md` task 1 — le finestre dichiarate prima
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md` — il censimento del piano 52-01 (i numeri di allora) e l'esito della corsa sul laboratorio
    - `.claude/rules/ai-engineering.md` — l'autorizzazione e' un atto
  </read_first>
  <action>
    Riprendere **oggi**, `read_only`, in produzione: le letture (1)-(4) del
    censimento del piano 52-01 (solo numeri), piu' il catalogo delle capability
    (15 chiavi, 28 concessioni attese) e l'ultima versione in
    `schema_migrations`. Se un numero e' cambiato rispetto al censimento, dirlo.
    Scrivere `52-AUTHORISATION.md` nella forma di `51-AUTHORISATION-COMMENT.md`:
    frontmatter (`phase`, `document: autorizzazione a scrivere in produzione — M1,
    deploy, M2 di NAV-07 e la chiave gallery.view`, `written`, `granted: —`,
    `granted_by: —`, `scope`, `answer: —`, `status: SCRITTA, NON CONCESSA`);
    riquadro «scritto PRIMA che la domanda venisse posta».
    §1 — il perimetro, passo per passo (a)-(e) dell'interfaccia sopra, con i file
    di migration **nominati**, lo sha di `main` da spingere e il numero di commit
    rispetto a `origin/main`, e per ogni passo cosa tocca alla lettera (M1: 1
    chiave, 3 concessioni, una colonna, un indice, un `CHECK`, una policy su
    `storage.objects`, `url` nullable, N righe riempite dal backfill — il numero
    vero di oggi; M2: una policy sostituita, una tolta, un bucket privato,
    `NOT NULL`); **Fuori perimetro**: ogni altra scrittura, la rimozione per
    chiave e la ri-spogliatura (atto del piano 52-16), ogni sessione coniata
    fuori da (d), ogni campo di configurazione. **Reversibilita'**: M2 si annulla
    solo allargando, e cio' che e' stato visto mentre era aperto resta visto; M1 e'
    additiva. **Provata sul laboratorio**: le versioni coniate di M1 e M2 li', la
    data della corsa del proprietario e il suo esito.
    §1.1 — le tre finestre dell'interfaccia, con la loro conseguenza per chi usa il
    prodotto mentre l'atto e' in corso.
    §1.2 — le precondizioni della spedizione, da misurare al passo (b): zero file
    di `docs/` e `.firecrawl/` nel diff, zero `.env*`, zero ref di laboratorio,
    zero segreti; nessuna serata nella finestra ieri/oggi/domani; nessuna serata
    in vendita con scansioni in corso.
    §2 — la domanda, **alla lettera**, gia' scritta, con i numeri veri; la risposta
    vuota. §3 — registro d'uso vuoto con le colonne passo · eseguito (UTC) ·
    riletto dal catalogo · esito. §4 — chiusura vuota.
    Dichiarare che `verify:capabilities` e `verify:refusal` sono rossi contro la
    produzione fino a questo atto, e che e' questo atto a chiuderli.
    **Non porre la domanda** in questo task. Nessun ref di laboratorio, nessuna
    chiave, nessun URL di media, nessun nome di persona nel file.
  </action>
  <verify>
    <automated>f=.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md; test -f $f &amp;&amp; /usr/bin/grep -q "Fuori perimetro" $f &amp;&amp; /usr/bin/grep -q "20260923180000" $f &amp;&amp; /usr/bin/grep -q "20260923180100" $f &amp;&amp; /usr/bin/grep -q "status: SCRITTA" $f &amp;&amp; test $(/usr/bin/grep -c "object/public/event-media/" $f) -eq 0 &amp;&amp; test $(/usr/bin/grep -cE "[0-9a-f]{8}-[0-9a-f]{4}-" $f) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Il documento esiste con frontmatter completo e `status: SCRITTA, NON CONCESSA`
    - Il perimetro nomina i cinque passi, i due file di migration, lo sha da spingere, e i numeri di oggi con l'ora UTC
    - Le tre finestre sono scritte con la loro conseguenza; le precondizioni della spedizione sono elencate
    - La domanda e' scritta alla lettera; registro e chiusura vuoti
    - Zero URL di media, zero UUID, zero ref di laboratorio
  </acceptance_criteria>
  <done>La domanda che sara' posta ha un perimetro scritto e i numeri di oggi accanto.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Il proprietario autorizza — o non autorizza</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md</files>
  <read_first>
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md` — perimetro e numeri
  </read_first>
  <action>
    Avvisare il proprietario con suono e notifica (`afplay` + `osascript`). Porre
    la domanda **alla lettera** come scritta nel §2, con i numeri veri e le tre
    finestre, e le opzioni sotto senza raccomandarne una come gia' decisa.
    Registrare la risposta **letterale** nel frontmatter (`granted`,
    `granted_date`, `granted_by: il proprietario`, `answer`) e nel §2. Nessun passo
    si esegue prima che questa registrazione esista.
  </action>
  <decision>Se eseguire oggi, in produzione, M1 → deploy → M2 come scritti in 52-AUTHORISATION.md</decision>
  <context>
    La fase e' pronta e provata sul laboratorio nello stesso ordine; il
    proprietario l'ha percorsa su iPhone. In produzione M1 e' additiva, il deploy
    porta la barra nuova, la porta a cinque linguette e il codice che firma, M2
    chiude la gallery sui dati e rende privato il bucket. Il progetto non ha PITR.
    Le finestre fra i passi sono scritte nel §1.1.
  </context>
  <options>
    <option id="tutto">
      <name>Autorizza (a)-(e), oggi, nell'ordine</name>
      <pros>La produzione si allinea al laboratorio in un atto; i gate rossi si chiudono; le finestre durano il tempo fra passi consecutivi</pros>
      <cons>M2 rende privato un bucket: si torna indietro solo allargando</cons>
    </option>
    <option id="senza-m2">
      <name>Autorizza (a), (b) e (d) senza M2</name>
      <pros>Barra, porta e cancello in produzione subito</pros>
      <cons>La gallery resta leggibile via API a ogni sessione e il bucket pubblico: la seconda finestra resta aperta finche' non c'e' un nuovo atto</cons>
    </option>
    <option id="niente">
      <name>Non autorizzare oggi</name>
      <pros>Nessuna scrittura</pros>
      <cons>La fase resta chiusa sul laboratorio</cons>
    </option>
  </options>
  <resume-signal>Scegli: `tutto`, `senza-m2` o `niente`</resume-signal>
  <verify>
    <automated>/usr/bin/grep -q "^granted:" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md &amp;&amp; /usr/bin/grep -q "^answer:" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Il frontmatter porta `granted`, `granted_date`, `granted_by`, `answer` con la risposta letterale
    - Se la risposta non e' `tutto`, il task 3 esegue solo cio' che e' stato concesso e lo dichiara al posto di ciascun passo
  </acceptance_criteria>
  <done>Esiste un permesso datato con un perimetro — o il rifiuto, scritto.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Spendere l'atto — M1, il deploy nel mezzo, M2 — e dichiararlo esaurito</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md, .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md</files>
  <read_first>
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md` — il perimetro concesso, e solo quello
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-13-SUMMARY.md` — la stessa sequenza sul laboratorio, con le sue riletture
    - `scripts/purge-attendances.mjs` righe 218-248 — `sql()`; ogni script scratch che applica in produzione **legge questo documento** e rifiuta se `granted` non e' `yes`, se la data non e' oggi o se lo stato e' `ESAURITA`
  </read_first>
  <action>
    Eseguire i passi concessi, in quest'ordine, registrando l'ora UTC di ciascuno
    nel §3 e in `52-ESITI.md` mentre accadono:
    (a) M1 dall'endpoint `migrations` di produzione; rilettura `read_only`:
    versione coniata; 16 chiavi; 31 concessioni (16/14/1/0); `storage_path` con
    zero nulle; policy `event_media_objects_select_by_row` presente; bucket
    ancora pubblico. Se non corrisponde: **stop**.
    (b) precondizioni della spedizione misurate (§1.2); `git push origin main`;
    deploy di produzione atteso `READY` (sha, ora di creazione e di `READY`);
    verifica esterna: `curl -sI https://www.resonatemotion.com/gallery` senza
    sessione → `location` con `/login?next=%2Fgallery`. Se il deploy fallisce:
    **stop, e M2 non si applica** (immagini rotte a tutti, Pitfall 10).
    (c) M2 dall'endpoint `migrations`; rilettura `read_only`: `storage_path`
    NOT NULL, `event_media_select_gallery` presente, `event_media_select_approved`
    e «Anyone can view event media» assenti, `event-media` privato,
    `event-images` pubblico invariato.
    (d) `npm run verify:capabilities` e `npm run verify:refusal` contro la
    produzione: verdi.
    (e) sonda anonima su un oggetto approvato (indirizzo tenuto in memoria, mai
    scritto): esito e ora; se 200 per cache, si registra con la finestra e si
    ripete nel piano 52-17 dopo il `max-age`.
    Chiudere: `status: ESAURITA`, `exhausted: <utc>`, §4 con `# ⚠ ESAURITA —
    <utc>`, quanti passi su quanti, quanti falliti, e «Da qui in poi questo
    documento non autorizza piu' niente.» Un passo non eseguito si dichiara **al
    suo posto**, non riscrivendo il perimetro.
  </action>
  <verify>
    <automated>npm run verify:capabilities &amp;&amp; npm run verify:refusal &amp;&amp; /usr/bin/grep -q "ESAURITA" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md &amp;&amp; curl -sI https://www.resonatemotion.com/gallery | /usr/bin/grep -i "^location:" | /usr/bin/grep -q "login?next=%2Fgallery"</automated>
  </verify>
  <acceptance_criteria>
    - Ogni passo concesso ha la sua ora UTC nel §3 e in `52-ESITI.md`; nessun passo fuori perimetro eseguito
    - L'ordine registrato e' M1 → `READY` del deploy → M2 (le ore lo mostrano)
    - `verify:capabilities` e `verify:refusal` verdi contro la produzione
    - `curl` anonimo su `/gallery` di produzione → login con ritorno
    - `52-AUTHORISATION.md` dichiara `ESAURITA` con l'ora, i passi eseguiti e quelli no
  </acceptance_criteria>
  <done>La produzione porta la fase, la gallery e' chiusa sui dati, e il permesso e' speso e dichiarato tale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → produzione | scritture su un database senza PITR e deploy pubblico |
| permesso → perimetro | «applica la fase» non e' un perimetro, e' una delega |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-71 | Denial of Service | M2 prima del deploy → immagini rotte a tutti | mitigate | (c) solo dopo `READY` e `curl` di (b); stop se il deploy fallisce |
| T-52-72 | Denial of Service | codice prima di M1 → organizer e staff rimbalzati dalla gallery | mitigate | (a) precede (b); rilettura del catalogo prima del push |
| T-52-73 | Tampering | un passo fuori perimetro «gia' che ci siamo» | mitigate | perimetro con «Fuori perimetro» esplicito; rimozione e ri-spogliatura in un atto separato (52-16) |
| T-52-74 | Repudiation | autorizzazione riscritta a posteriori | mitigate | scritta prima della domanda; risposta letterale; smentite nella nota d'uso; `ESAURITA` con l'ora |
| T-52-75 | Information disclosure | materiale privato spinto con `main` | mitigate | precondizioni della spedizione misurate in (b) |
| T-52-76 | Information disclosure | foto servite da cache dopo M2 | accept, dichiarata | finestre scritte nel §1.1; sonda ripetuta dopo il `max-age` nel piano 52-17 |
| T-52-77 | Denial of Service (porta) | deploy in una sera di serata | mitigate | precondizione: nessuna serata ieri/oggi/domani, nessuna scansione in corso |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- Catalogo di produzione: 16 chiavi, 31 concessioni, `storage_path` NOT NULL, `event_media_select_gallery`, bucket privato
- `verify:capabilities`, `verify:refusal` verdi contro la produzione
- `52-AUTHORISATION.md` ESAURITA con il registro d'uso
</verification>

<success_criteria>
La produzione porta NAV-01..07 dentro un atto datato, nell'ordine che non rompe
nulla, e il permesso non puo' essere riusato.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-15-SUMMARY.md` when done
</output>
