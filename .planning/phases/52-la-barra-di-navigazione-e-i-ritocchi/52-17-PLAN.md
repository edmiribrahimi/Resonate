---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 17
type: execute
wave: 11
depends_on: ["52-16"]
files_modified:
  - .claude/rules/media-and-storage.md
  - .claude/rules/access-gating.md
  - .claude/CHANGELOG.md
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VERIFICATION.md
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, NAV-07]

must_haves:
  truths:
    - "media-and-storage.md non descrive piu' event-media come pubblico letto con getPublicUrl, non dice che non esiste sanitizzazione dei metadati, non dice che l'upload richiede un utente approvato: ogni riga corretta e' riletta dal codice, con la data (ai-engineering.md, documentazione datata)"
    - "access-gating.md nomina /gallery sotto gallery.view, con la chiave che governa anche righe e con cosa serve per riaprirla"
    - ".claude/CHANGELOG.md porta la versione nuova e npm run verify:persona e' verde DOPO le modifiche, con il caso peggiore del context budget scritto"
    - "Il residuo di cache dopo M2 e' stato misurato dopo la finestra: la sonda anonima su un oggetto approvato di produzione non risponde 200 (Pitfall 15)"
    - "52-VERIFICATION.md copre NAV-01..07 e D-52-01..31 con evidenza file:riga, procedure con esiti datati, anti-pattern trovati e debito dichiarato — compreso lo script di ri-spogliatura come secondo scrittore fuori da verify:media-strip e il numero di foto ripassate"
  artifacts:
    - path: ".planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VERIFICATION.md"
      provides: "la verifica di fase con evidenza file:riga"
      contains: "NAV-07"
      min_lines: 120
    - path: ".claude/CHANGELOG.md"
      provides: "la voce della versione nuova"
      contains: "gallery.view"
  key_links:
    - from: ".claude/rules/media-and-storage.md"
      to: "supabase/migrations/20260923180100_gallery_close_data.sql"
      via: "la riga sul bucket privato cita la migration che lo chiude"
      pattern: "20260923180100"
---

<objective>
Chiudere la fase: la persona smette di descrivere un prodotto che non c'e' piu',
il residuo di cache si misura dopo la sua finestra, e la verifica di fase si
scrive con le prove.

Purpose: `media-and-storage.md` e' datato su tre punti (e un quarto: il titolo al
caricamento) e si carica **da solo** sulle superfici media — una riga che descrive
male il prodotto in un modulo che si carica e' peggio di una riga assente
(`meta-gates.md`). Il gate VERIFICATION del progetto pretende `file:riga` per
requisito, e in un repo senza test l'evidenza osservabile e' l'unica prova.
Output: due moduli della persona, `CHANGELOG`, `52-VERIFICATION.md`, la sonda
dopo la finestra in `52-ESITI.md`, e le istantanee locali cancellate a data
scritta dopo l'approvazione del proprietario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-AUTHORISATION-MEDIA.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-VERIFICATION.md
@./CLAUDE.md
@.claude/rules/ai-engineering.md
@.claude/rules/meta-gates.md

<interfaces>
`.claude/rules/media-and-storage.md` (5058 byte, `paths:` components/media,
app/**/media, app/(public)/gallery): righe datate misurate il 2026-09-23 —
`:24` «`event-media`, `public: true`», `:26` «`getPublicUrl`», `:41` «Nessuna
sanitizzazione dei metadati esiste nel codice», `:56` gate *chi carica ha titolo*
«L'upload richiede oggi utente **approvato** con un biglietto».
`.claude/rules/access-gating.md` (8330 byte).
`.claude/CHANGELOG.md`: ultima versione `[1.25.0] - 2026-09-23`.
`npm run verify:persona`: sette controlli; caso peggiore del budget oggi
`DoorSurface.tsx` 13580/15000 token; la pagina di moderazione carica
media-and-storage + access-gating + nextjs-architecture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: La persona — media-and-storage e access-gating rileggono il prodotto, CHANGELOG, verify:persona dopo</name>
  <files>.claude/rules/media-and-storage.md, .claude/rules/access-gating.md, .claude/CHANGELOG.md</files>
  <read_first>
    - `.claude/rules/media-and-storage.md` per intero
    - `.claude/rules/access-gating.md` per intero
    - `.claude/rules/ai-engineering.md` — *documentazione datata*, context budget, coerenza indice ↔ frontmatter
    - `.claude/CHANGELOG.md` righe 1-60 — la forma di una voce
    - `src/lib/media/may-upload.ts`, `src/lib/media/finalize.ts:1-40`, `supabase/migrations/20260923180100_gallery_close_data.sql`, `src/lib/routes/capability-routes.ts` (voce `[CAP.GALLERY_VIEW]`) — le fonti da cui si riscrivono le righe
  </read_first>
  <action>
    In `media-and-storage.md`, correggere **rileggendo il codice**, non ricordando,
    con la data 2026-09-23 accanto a ogni correzione: il bucket `event-media` e'
    **privato** da `20260923180100_gallery_close_data.sql`; si legge solo con URL
    firmati dal client della sessione (`src/lib/media/sign-event-media.ts`), e la
    policy sull'oggetto chiede la riga; le righe approvate si leggono con
    `gallery.view`; `event-images`, `venue-photos`, `artist-photos` restano
    pubblici. La sanitizzazione **esiste** (route di finalize e
    `src/lib/media/strip-metadata.ts`, fase 35) e le foto pre-stripper sono state
    ripassate nella fase 52 (con il numero dall'atto, o il debito se non
    autorizzato). Il gate *chi carica ha titolo* si riscrive su cio' che
    `may-upload.ts` fa oggi (letto, non supposto). Il gate *moderazione = rimozione*
    guadagna una riga: dal 2026-09-23 un rifiuto e' irraggiungibile per firma, e
    gli oggetti rifiutati e orfani sono stati rimossi (o il debito, con il numero).
    Nessuna riga nuova che allunghi il modulo oltre il necessario: il file si
    carica sulla pagina di moderazione insieme a due altri moduli.
    In `access-gating.md`: una riga che nomina `/gallery` sotto `gallery.view`
    (voce di mappa con `alsoGatesTables`, prefisso protetto, guardia in pagina) e
    che riaprirla al pubblico e' anche una migration (D-52-14).
    `CHANGELOG.md`: voce `[1.26.0] - <data>` nella forma delle precedenti — cosa e'
    cambiato, cosa lo ha fatto scattare (NAV-07, D-52-25, `52-RESEARCH.md` §I.5),
    cosa diceva prima.
    Poi `npm run verify:persona` **dopo** le modifiche: verde; scrivere nel SUMMARY
    il caso peggiore del context budget e quello della pagina di moderazione.
  </action>
  <verify>
    <automated>npm run verify:persona &amp;&amp; /usr/bin/grep -q "1.26.0" .claude/CHANGELOG.md &amp;&amp; test $(/usr/bin/grep -c "public: true" .claude/rules/media-and-storage.md) -eq 0 &amp;&amp; test $(/usr/bin/grep -c "Nessuna sanitizzazione dei metadati esiste nel codice" .claude/rules/media-and-storage.md) -eq 0 &amp;&amp; /usr/bin/grep -q "gallery.view" .claude/rules/access-gating.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:persona` esce 0 dopo le modifiche; il SUMMARY riporta il caso peggiore del budget
    - `media-and-storage.md` non contiene piu' «public: true» per `event-media` ne' «Nessuna sanitizzazione dei metadati esiste nel codice»; ogni riga corretta porta la data
    - `access-gating.md` nomina `/gallery` e `gallery.view`
    - `CHANGELOG.md` porta `[1.26.0]`
  </acceptance_criteria>
  <done>La persona descrive la gallery e il bucket come sono oggi.</done>
</task>

<task type="auto">
  <name>Task 2: La sonda dopo la finestra di cache, e 52-VERIFICATION.md con le prove</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VERIFICATION.md, .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-VERIFICATION.md` per intero — la forma
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md` per intero
    - tutti i `52-*-SUMMARY.md`
    - `CLAUDE.md` §Gate VERIFICATION.md — schema richiesto
  </read_first>
  <action>
    (1) Se dall'atto M2 (ora nel registro di `52-AUTHORISATION.md`) e' passata
    meno di un'ora piu' il `max-age` dell'oggetto, **aspettare** e dirlo; poi
    ripetere la sonda anonima su un oggetto approvato di produzione (indirizzo mai
    scritto) e registrare in `52-ESITI.md` ora ed esito: atteso **non 200**. Se
    risponde ancora 200, e' un fallimento di NAV-07 da riportare, non da
    attendere oltre.
    (2) Scrivere `52-VERIFICATION.md` nella forma di `51-VERIFICATION.md`:
    frontmatter con `status`; per ogni requisito NAV-01..NAV-07 **e** per ogni
    decisione D-52-01..D-52-31, l'evidenza concreta — `file:riga` del codice
    (riletto oggi, non copiato dai SUMMARY), comportamento osservabile, procedura
    percorsa con data ed esito (`52-ESITI.md`), comando e uscita del gate
    (`npm run build`, `npm run verify` = `VERIFY_OK`, `verify:capabilities` e
    `verify:refusal` contro la produzione). Sezioni obbligatorie: **anti-pattern
    trovati** (grep di TODO/FIXME/stub nei file toccati dalla fase, con esito);
    **debito dichiarato**: lo script `scripts/restrip-event-media.mjs` come
    **secondo scrittore** su `event-media` fuori dal perimetro del controllo A di
    `verify:media-strip`, con la ragione; il **numero di foto ripassate** e di video
    non trattati (D-52-31); i numeri della rimozione (D-52-30); il residuo di cache
    con i tempi; `venue-secrecy.md` — NAV-07 **restringe** a chi tiene
    `gallery.view` ma non chiude il contenuto di una foto (un'insegna resta
    un'insegna); la decisione sul login (D-52-28); ogni passo di procedura non
    percorso; le divergenze temporanee dichiarate dai piani (tipi, commenti che
    nominano `getVisibleNavItems`). Nessuna frase «i test passano».
    Nessun URL di media, path, UUID o ref di laboratorio nel file.
  </action>
  <verify>
    <automated>f=.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VERIFICATION.md; npm run build &amp;&amp; npm run verify &amp;&amp; test -f $f &amp;&amp; for r in NAV-01 NAV-02 NAV-03 NAV-04 NAV-05 NAV-06 NAV-07; do /usr/bin/grep -q "$r" $f || exit 1; done; for n in $(seq -w 1 31); do /usr/bin/grep -q "D-52-$n" $f || exit 1; done; test $(/usr/bin/grep -cE "\.(tsx?|mjs|sql):[0-9]+" $f) -ge 20 &amp;&amp; test $(/usr/bin/grep -ci "i test passano" $f) -eq 0 &amp;&amp; test $(/usr/bin/grep -cE "[0-9a-f]{8}-[0-9a-f]{4}-" $f) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify` escono 0 (`VERIFY_OK`)
    - `52-VERIFICATION.md` nomina NAV-01..07 e D-52-01..31, con almeno 20 citazioni `file:riga`
    - Sezioni «anti-pattern trovati» e «debito dichiarato» presenti, con lo script di ri-spogliatura come secondo scrittore, il numero di foto ripassate, i numeri della rimozione, il residuo di cache con i tempi
    - `52-ESITI.md` porta la sonda anonima dopo la finestra con ora ed esito
    - Zero UUID, zero URL di media; la frase «i test passano» non compare
  </acceptance_criteria>
  <done>La fase ha la sua verifica, con le prove scritte e non evocate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Il proprietario approva la verifica — e le istantanee locali si cancellano a data scritta</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VERIFICATION.md`
  </read_first>
  <action>
    Avvisare con suono e notifica. Presentare al proprietario il riassunto di
    `52-VERIFICATION.md`: requisiti coperti, debito, numeri della rimozione e della
    ri-spogliatura, residuo di cache. Alla risposta «approvata», il prosieguo
    cancella `.env.restrip-snapshot.*` e `.env.media-purge-snapshot.*` (copie locali
    di foto e di nomi che contengono identificativi di persone:
    `legal-compliance.md`), rilegge che non esistono piu', e scrive la data e l'ora
    della cancellazione in `52-VERIFICATION.md`. Alla risposta «tieni», le
    istantanee restano e la VERIFICATION scrive fino a quando e perche'.
    Il push dei documenti finali **non** e' parte di questo piano: e' un atto del
    proprietario.
  </action>
  <what-built>La verifica di fase con le prove, la persona allineata, la sonda di cache dopo la finestra.</what-built>
  <how-to-verify>
    1. Leggere il riassunto di `52-VERIFICATION.md` presentato.
    2. Controllare che il debito elencato sia quello atteso (o che non ce ne sia oltre il dichiarato).
    3. Decidere delle istantanee locali: cancellarle ora o tenerle con una data.
  </how-to-verify>
  <resume-signal>Scrivi «approvata» (e le istantanee si cancellano) oppure «tieni fino a <data>», oppure descrivi cosa manca</resume-signal>
  <verify>
    <automated>/usr/bin/grep -qi "istantane" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - La risposta del proprietario e' scritta in `52-VERIFICATION.md`
    - Se «approvata»: nessun `.env.restrip-snapshot.*` ne' `.env.media-purge-snapshot.*` resta sul disco, e la data della cancellazione e' scritta
    - Se «tieni»: la data fino a cui restano e la ragione sono scritte
  </acceptance_criteria>
  <done>La fase e' chiusa con una verifica approvata, e le copie locali di cio' che si e' tolto hanno una fine scritta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persona → chi lavora sul repo | un modulo che si carica da solo e descrive male il prodotto guida decisioni sbagliate |
| disco locale → persone ritratte | istantanee di foto e nomi con identificativi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-84 | Repudiation | persona che dice «bucket pubblico» dopo M2 | mitigate | righe riscritte rileggendo il codice, con la data; `verify:persona` dopo |
| T-52-85 | Information disclosure | istantanee locali tenute senza fine | mitigate | cancellazione a data scritta dopo l'approvazione, o una data di scadenza dichiarata |
| T-52-86 | Repudiation | «chiuso» dichiarato mentre la cache serve | mitigate | sonda anonima ripetuta dopo la finestra; 200 = fallimento riportato |
| T-52-87 | Repudiation | VERIFICATION senza prove | mitigate | ≥ 20 citazioni `file:riga`, ogni NAV e ogni D-52 nominati, controllati dal comando di verifica |
</threat_model>

<verification>
- `npm run build`, `npm run verify` (VERIFY_OK), `npm run verify:persona` verdi
- `52-VERIFICATION.md` completo e approvato
</verification>

<success_criteria>
La fase 52 e' chiusa con una verifica che cita le proprie prove, una persona che
descrive il prodotto di oggi, e nessuna copia locale senza una fine.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-17-SUMMARY.md` when done
</output>
