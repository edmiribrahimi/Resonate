---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 12
type: execute
wave: 4
depends_on: ["52-08"]
files_modified:
  - src/app/(public)/events/[slug]/actions.ts
  - src/app/(public)/events/[slug]/page.tsx
  - src/app/(public)/events/[slug]/MediaGallerySection.tsx
  - src/app/(admin)/admin/(work)/events/[id]/media/page.tsx
  - src/components/media/MediaReviewGrid.tsx
autonomous: true
requirements: [NAV-07]

must_haves:
  truths:
    - "registerMedia scrive storage_path (la chiave restituita dalla route di finalize) e non costruisce piu' l'indirizzo pubblico; deleteMedia legge storage_path invece di tagliare un URL (D-52-25)"
    - "Se la rimozione dell'oggetto fallisce, deleteMedia NON cancella la riga e lo dice con una categoria: moderazione = rimozione, niente righe cancellate con oggetti vivi (Pitfall 11)"
    - "Sulla pagina di una serata la sezione 'Gallery' — titolo compreso — non esiste per chi non ha gallery.view; resta per chi la tiene, e resta il caricamento per chi puo' caricare, dichiarato (D-52-29, P5)"
    - "La pagina della serata firma con 3600 s e la moderazione con 300 s, col client della sessione; la moderazione non passa piu' da next/image (Pitfall 13)"
    - "Nessuna superficie del prodotto legge piu' event_media.url"
  artifacts:
    - path: "src/app/(public)/events/[slug]/actions.ts"
      provides: "registerMedia con storage_path, deleteMedia per chiave e senza fallimento silenzioso"
      contains: "storage_path"
    - path: "src/app/(public)/events/[slug]/page.tsx"
      provides: "sezione Gallery sotto gallery.view (o canUpload), media firmati"
      contains: "CAP.GALLERY_VIEW"
    - path: "src/components/media/MediaReviewGrid.tsx"
      provides: "<img> invece di next/image per gli URL firmati"
  key_links:
    - from: "src/app/(public)/events/[slug]/page.tsx"
      to: "src/lib/media/sign-event-media.ts"
      via: "signEventMedia(rows, EVENT_MEDIA_SIGNATURE_SECONDS)"
      pattern: "signEventMedia"
    - from: "src/app/(admin)/admin/(work)/events/[id]/media/page.tsx"
      to: "src/lib/media/sign-event-media.ts"
      via: "signEventMedia(rows, EVENT_MEDIA_REVIEW_SIGNATURE_SECONDS)"
      pattern: "EVENT_MEDIA_REVIEW_SIGNATURE_SECONDS"
---

<objective>
Il resto del codice che firma: le azioni che scrivono e cancellano, la pagina
della serata, la moderazione.

Purpose: D-52-25 (nessuna superficie serve piu' un indirizzo pubblico; si
firmano chiavi), D-52-29 (la pagina non annuncia foto che non si possono vedere),
e il gate *moderazione = rimozione*: oggi `deleteMedia` cancella la riga **anche
se** la rimozione dell'oggetto e' fallita (`actions.ts:330-333`), e dopo M2 quel
ramo lascerebbe oggetti vivi senza riga — irraggiungibili per firma, ma non
rimossi, in silenzio. Insieme al piano 52-08 e' l'ondata di codice che si
spedisce **fra M1 e M2**.
**Critical per `venue-secrecy.md`**: la pagina della serata puo' mostrare una
serata segreta; la firma non cambia *cosa* si vede, cambia *chi* lo raggiunge.
Output: `actions.ts`, `events/[slug]/page.tsx` (e `MediaGallerySection.tsx` se la
condizione lo tocca), la moderazione e `MediaReviewGrid.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@./CLAUDE.md
@.claude/rules/venue-secrecy.md
@.claude/rules/media-and-storage.md
@.claude/rules/access-gating.md
@.claude/rules/ticketing-payments.md

<interfaces>
`src/app/(public)/events/[slug]/actions.ts`:
- `registerMedia(eventId, partyId, storagePath, type, fileSize)` (`:154-200`): guardia
  `mayUploadToParty`, poi `publicUrl = ${NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/public/event-media/${storagePath}` (`:179`) e `insert({ …, url: publicUrl, …, status: "pending" })`
- `deleteMedia(mediaId)` (`:291-349`): `select("uploaded_by, url")`, guardia
  `uploaded_by === userId || CAP.STAFF_MANAGE`, path tagliato da `media.url`
  (`:318-323`), `supabase.storage.from("event-media").remove([storagePath])` col
  client di sessione, su errore `console.error([media.storage_delete_failed] …)` e
  **prosegue** (`:330-333`), poi `delete().eq("id", mediaId)`
`src/app/(public)/events/[slug]/page.tsx`: lettura `:1013-1027`
`select("id, url, type, uploaded_by, created_at")` approvate dell'evento →
`mediaItems`; render `:1962-1970` `<AnimatedSection><SectionHeading>Gallery</SectionHeading><MediaGallerySection media canUpload eventId uploadableParties/></AnimatedSection>`.
Moderazione `(work)/events/[id]/media/page.tsx:167-195`: conteggi per stato e le
`pending` con `url`; `MediaReviewGrid.tsx:4` `import Image from "next/image"`, `:169-170` `<Image src={item.url} …>`.
Firma: `signEventMedia(rows, seconds)`, `EVENT_MEDIA_SIGNATURE_SECONDS` (3600),
`EVENT_MEDIA_REVIEW_SIGNATURE_SECONDS` (300) da `src/lib/media/sign-event-media.ts`
(piano 52-08); risultato `{ ok: true; urls; unsigned } | { ok: false; reason }`.
Lettura d'accesso in pagina: `getAccessContext()` → `capabilities.has(CAP.GALLERY_VIEW)`.
`verify:venue-surfaces` pinna due stringhe esatte in `events/page.tsx` (non in
`[slug]/page.tsx`); leggere comunque `verify-venue-surfaces.mjs` per sapere cosa
pinna in `[slug]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Le azioni — registerMedia scrive la chiave, deleteMedia cancella per chiave e non tace</name>
  <files>src/app/(public)/events/[slug]/actions.ts</files>
  <read_first>
    - `src/app/(public)/events/[slug]/actions.ts` righe 1-60, 120-260, 280-350
    - `src/lib/media/may-upload.ts` — la guardia del caricamento (non cambia)
    - `supabase/migrations/20260225120000_phase7_media.sql` righe 86-101 — le due DELETE di storage che `deleteMedia` usa col client di sessione
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §I.4 «Il codice» e Pitfall 11
  </read_first>
  <action>
    `registerMedia`: togliere la costruzione di `publicUrl`; l'`insert` scrive
    `storage_path: storagePath` e **non** scrive `url` (M1 ha tolto il `NOT NULL`).
    Aggiornare il docblock: da NAV-07 la riga porta la chiave dell'oggetto, e
    l'indirizzo si ottiene solo firmando. Guardia `mayUploadToParty`, `status:
    "pending"` e il resto invariati.
    `deleteMedia`: `select("uploaded_by, storage_path")`; la guardia invariata; se
    `storage_path` e' nullo (riga della finestra fra deploy e M2) → `throw new
    Error("media.storage_path_missing")` con `console.error` categoria
    `[media.storage_path_missing]`, **senza** cancellare la riga; altrimenti
    `remove([media.storage_path])` col client di sessione; se la rimozione
    fallisce → `console.error` categoria `[media.storage_delete_failed]` e `throw
    new Error("media.storage_delete_failed")` **senza cancellare la riga**
    (commento: fino al 2026-09-23 qui si proseguiva; dopo NAV-07 una riga
    cancellata con l'oggetto vivo e' un rifiuto che non rimuove, *moderazione =
    rimozione*; la riga resta e il moderatore vede l'errore e riprova). L'ordine
    resta **oggetto prima, riga dopo**: la policy dell'oggetto chiede la riga.
    Le chiamate di questi errori nell'interfaccia (dove `deleteMedia` e'
    invocata) devono mostrare un messaggio distinto: leggere i chiamanti con grep
    e, se collassano ogni errore in uno generico, dichiararlo nel SUMMARY come
    debito (non allargare il perimetro del piano).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:media-strip &amp;&amp; test $(/usr/bin/grep -c "object/public/event-media" "src/app/(public)/events/[slug]/actions.ts") -eq 0 &amp;&amp; /usr/bin/grep -q "storage_path: storagePath" "src/app/(public)/events/[slug]/actions.ts" &amp;&amp; test $(/usr/bin/grep -c "Continue to delete the DB record even if storage deletion fails" "src/app/(public)/events/[slug]/actions.ts") -eq 0 &amp;&amp; /usr/bin/grep -q "media.storage_path_missing" "src/app/(public)/events/[slug]/actions.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify:media-strip` escono 0
    - Zero occorrenze di `object/public/event-media` in `actions.ts`; l'insert scrive `storage_path: storagePath` e non `url`
    - `deleteMedia` seleziona `storage_path`, non taglia URL, e su rimozione fallita o chiave assente lancia con categoria **senza** cancellare la riga
    - Il commento «Continue to delete the DB record even if storage deletion fails» non esiste piu'; al suo posto la ragione datata
  </acceptance_criteria>
  <done>Scrivere e cancellare un media passa dalla chiave, e un oggetto che non se ne va non lascia una riga cancellata a mentire.</done>
</task>

<task type="auto">
  <name>Task 2: La pagina della serata — sezione sotto gallery.view, media firmati</name>
  <files>src/app/(public)/events/[slug]/page.tsx, src/app/(public)/events/[slug]/MediaGallerySection.tsx</files>
  <read_first>
    - `src/app/(public)/events/[slug]/page.tsx` righe 1-80 (import, accesso), 960-1030 (`canUpload`, `uploadableParties`, lettura dei media), 1955-1975 (render)
    - `src/app/(public)/events/[slug]/MediaGallerySection.tsx` per intero
    - `scripts/verify-venue-surfaces.mjs` — cosa pinna in `events/[slug]/page.tsx`, per non toccarlo
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md` P5
  </read_first>
  <action>
    Leggere `hasGalleryView = capabilities.has(CAP.GALLERY_VIEW)` dal contesto
    d'accesso gia' risolto nella pagina (se la pagina non lo destruttura, usare la
    stessa chiamata `getAccessContext()`, che e' `cache()`-scoped).
    Lettura: se `hasGalleryView` e' falso, **non leggere** i media approvati
    affatto (nessuna query inutile verso righe che la RLS rifiutera' dopo M2);
    altrimenti `select("id, storage_path, type, uploaded_by, created_at")` —
    **mai `url`** — poi `signEventMedia(rows, EVENT_MEDIA_SIGNATURE_SECONDS)` e
    `mediaItems` con `url` = URL firmato (vuoto se non firmato: lo dichiara
    `MediaGrid`, piano 52-08). Firma fallita → un avviso visibile nella sezione
    (`role="alert"`, testo in inglese: the photos of this night could not be
    loaded, reload the page) distinto dalla lista vuota.
    Render (D-52-29 + P5): la sezione intera — `AnimatedSection`,
    `SectionHeading` «Gallery», `MediaGallerySection` — si monta solo se
    `hasGalleryView || canUpload`. Commento: D-52-29, la pagina non annuncia foto
    che non si possono vedere; P5, la sezione porta anche il caricamento, quindi
    chi puo' caricare per assegnazione senza tenere `gallery.view` vede la sezione
    con il solo caricamento (oggi l'insieme e' vuoto: `staff` tiene
    `gallery.view` per ruolo) — condizione scritta, non dedotta. Se in quel caso la
    griglia vuota con un testo «nessuna foto» mentisse sul perche', passare a
    `MediaGallerySection` un flag che non disegna la griglia quando
    `hasGalleryView` e' falso (modifica minima al componente, commentata).
    Le stringhe pinnate da `verify:venue-surfaces` restano identiche.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:venue-surfaces &amp;&amp; npm run verify:conversion &amp;&amp; /usr/bin/grep -q "CAP.GALLERY_VIEW" "src/app/(public)/events/[slug]/page.tsx" &amp;&amp; /usr/bin/grep -q "signEventMedia" "src/app/(public)/events/[slug]/page.tsx" &amp;&amp; test $(/usr/bin/grep -c 'select("id, url, type, uploaded_by' "src/app/(public)/events/[slug]/page.tsx") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `verify:venue-surfaces`, `verify:conversion` escono 0
    - La pagina non seleziona piu' `url` da `event_media`; firma con `EVENT_MEDIA_SIGNATURE_SECONDS`
    - La sezione «Gallery» (titolo compreso) e' montata solo se `hasGalleryView || canUpload`, con il commento D-52-29 / P5
    - Senza `gallery.view` la lettura dei media approvati non avviene
    - Firma fallita → avviso visibile distinto dallo stato vuoto
  </acceptance_criteria>
  <done>Un attendee non vede una sezione di foto che non puo' aprire; chi la puo' aprire vede foto firmate.</done>
</task>

<task type="auto">
  <name>Task 3: La moderazione firma a 300 s e lascia next/image</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/media/page.tsx, src/components/media/MediaReviewGrid.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/media/page.tsx` per intero
    - `src/components/media/MediaReviewGrid.tsx` per intero
    - `next.config.ts` righe 50-65 — `images.remotePatterns` per `*.supabase.co` (non si tocca: altre superfici lo usano)
    - `src/components/media/MediaGrid.tsx` (piano 52-08) — la forma dell'`onError` e del testo
  </read_first>
  <action>
    Pagina di moderazione: la lettura delle `pending` seleziona `storage_path` al
    posto di `url` (resto invariato: tipo, dimensione, data, nome di chi ha
    caricato — quest'ultimo resta visibile solo qui, superficie di lavoro);
    `signEventMedia(rows, EVENT_MEDIA_REVIEW_SIGNATURE_SECONDS)`; agli `items`
    arriva `url` = URL firmato; firma fallita → avviso `role="alert"` in pagina
    distinto dalla coda vuota.
    `MediaReviewGrid.tsx`: via `next/image`; `<img src alt="" loading="lazy"
    className=…>` con le stesse dimensioni e lo stesso `object-cover`, `onError` e
    `url` vuoto → «This image could not be loaded. Reload the page.» nella forma
    di `MediaGrid`. Commento: Pitfall 13 — l'ottimizzatore mette in cache per
    URL, una firma nuova a ogni render e' un miss a ogni render, e l'immagine
    ottimizzata sopravviverebbe alla scadenza della firma su `/_next/image`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:touch-targets &amp;&amp; npm run verify:conversion &amp;&amp; test $(/usr/bin/grep -c "next/image" src/components/media/MediaReviewGrid.tsx) -eq 0 &amp;&amp; /usr/bin/grep -q "EVENT_MEDIA_REVIEW_SIGNATURE_SECONDS" "src/app/(admin)/admin/(work)/events/[id]/media/page.tsx" &amp;&amp; test $(/usr/bin/grep -rn 'from("event_media")' src | wc -l) -ge 1 &amp;&amp; test $(/usr/bin/grep -rn "select(\"id, url\|, url, type\|\"url\"" src --include=*.tsx --include=*.ts | /usr/bin/grep -c event_media) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `verify:touch-targets`, `verify:conversion` escono 0
    - `MediaReviewGrid.tsx` non importa `next/image`; usa `<img>` con `onError` e il testo esatto
    - La moderazione firma con `EVENT_MEDIA_REVIEW_SIGNATURE_SECONDS`
    - Un grep su `src/` non trova piu' letture di `url` da `event_media` (il SUMMARY riporta il grep e il suo esito)
  </acceptance_criteria>
  <done>Nessuna superficie del prodotto serve piu' un indirizzo pubblico di `event-media`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pagina della serata → chiunque abbia una sessione | la pagina puo' essere di una serata segreta |
| moderatore → Storage | la rimozione passa dalle policy DELETE di storage col client di sessione |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-54 | Information disclosure | foto di una serata segreta raggiungibili da un attendee | mitigate | sezione e lettura solo con `gallery.view` (D-52-29); dopo M2 la RLS rifiuta comunque le righe; URL firmati a scadenza |
| T-52-55 | Repudiation | rifiuto che cancella la riga lasciando l'oggetto | mitigate | `deleteMedia` lancia con categoria e non cancella la riga se la rimozione fallisce (Pitfall 11) |
| T-52-56 | Tampering | una riga senza chiave scritta dal codice nuovo | mitigate | `registerMedia` scrive sempre `storage_path`; il `CHECK` di M1 ne vincola la forma |
| T-52-57 | Information disclosure | immagine ottimizzata servita oltre la firma | mitigate | niente `next/image` per `event-media` (Pitfall 13) |
| T-52-58 | Denial of Service | caricamento bloccato a chi carica per assegnazione | mitigate | condizione `hasGalleryView \|\| canUpload` scritta (P5) |
| T-52-59 | Information disclosure | nome di chi ha caricato fuori dalla moderazione | accept | resta solo nella superficie di lavoro, invariato |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `npm run build`, `verify:media-strip`, `verify:venue-surfaces`, `verify:conversion`, `verify:touch-targets` verdi
- Nessuna lettura di `event_media.url` in `src/`
- Sonde 5-7 di P-52-G sul laboratorio (piani 52-13, 52-14)
</verification>

<success_criteria>
Il codice che firma e' completo: dopo il suo deploy, M2 puo' chiudere il bucket
senza rompere un'immagine a chi le puo' vedere.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-12-SUMMARY.md` when done
</output>
