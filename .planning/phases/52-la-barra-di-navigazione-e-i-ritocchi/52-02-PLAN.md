---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VALIDATION.md
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md
  - .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, NAV-07]

must_haves:
  truths:
    - "Le sette procedure della fase (P-52-A..G) esistono SCRITTE prima di essere percorse, con passo · ruolo · cosa si deve osservare, e ogni Result e' pending"
    - "La procedura del viewport dichiara PRIMA di essere percorsa che su Safari iOS il ridimensionamento sotto la tastiera e' ATTESO ASSENTE, e registra separatamente le misure di layout (D-52-23, D-52-28)"
    - "52-VALIDATION.md dice la verita': nessun test runner; quick = npm run build; full = npm run verify; Wave 0 = procedure scritte e banco del laboratorio"
    - "Le tre raccomandazioni non bloccanti della UI-SPEC sono chiuse: conteggi senza parentesi citano D-52-26, l'eccezione --faint per TASK spenta e' in 41 §12, i gradini z-10 e z-40 sono in 41 §10"
  artifacts:
    - path: ".planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md"
      provides: "P-52-A..G, PRE-LAB, riquadri d'esito aperti"
      contains: "PRE-LAB"
      min_lines: 150
    - path: ".planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VALIDATION.md"
      provides: "la strategia di validazione onesta per un repo senza test"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "52-PROCEDURES.md P-52-F"
      to: "D-52-28"
      via: "l'aspettativa scritta prima della corsa"
      pattern: "atteso assente"
    - from: "52-VALIDATION.md"
      to: "i piani 52-01..52-17"
      via: "la mappa task → comando"
      pattern: "52-13"
---

<objective>
Scrivere le procedure manuali della fase **prima** che esista il codice che
misureranno, riempire onestamente la strategia di validazione, e chiudere le tre
raccomandazioni non bloccanti della UI-SPEC.

Purpose: in un repo senza test runner la procedura scritta e' l'unica prova che
esistera' (`meta-gates.md`, *il gate della verifica*). D-52-28 chiede che
l'aspettativa del viewport iOS sia scritta **prima** della corsa, o la prova
misurera' un meccanismo che Safari non ha (Pitfall 6).
Output: `52-PROCEDURES.md`, `52-VALIDATION.md` riempito, due righe nuove in
`41-UI-SPEC.md` e una in `52-UI-SPEC.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PROCEDURES.md
@./CLAUDE.md
@.claude/rules/checkin-offline.md
@.claude/rules/meta-gates.md

<interfaces>
I piani di questa fase, per la mappa di validazione (onda · piano · cosa):
  w1 52-01 censimento + banco media · 52-02 questo · 52-03 NAV-05 + legenda staff ·
     52-04 NAV-06 chip · 52-05 porta: linguette + viewport
  w2 52-06 M1 + catalogo TS, applicata al laboratorio
  w3 52-07 barra + foglio + lista in colonna · 52-08 cancello gallery + firma ·
     52-09 purge-media-orphans (lab) · 52-10 restrip-event-media (lab)
  w4 52-11 via la colonna Work + striscia appesa · 52-12 firma su serata, azioni, moderazione
  w5 52-13 M2 + deploy del laboratorio + M2 sul laboratorio + sonde
  w6 52-14 corsa del proprietario sul laboratorio (iPhone)
  w7 52-15 atto 1 in produzione: M1 → deploy → M2
  w8 52-16 atto 2 in produzione: rimozione per chiave + ri-spogliatura
  w9 52-17 persona, residuo di cache, VERIFICATION
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere 52-PROCEDURES.md — P-52-A..G, prima di percorrerle</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PROCEDURES.md` per intero — frontmatter (righe 1-10), riquadro «pubblicato» (14-18), «scritta PRIMA di essere percorsa» (20-24), `PRE-LAB` (48-54), tabella dei passi (116-126), «La rilettura conferma, non sostituisce» (146-149), «Se la corsa si ferma» (151-155), riquadri d'esito (159-171)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §A tabella degli esiti per soggetto (righe 392-401), §I.8 (le otto sonde, righe 958-981), Validation Architecture (righe 1196-1213), Pitfall 6 e 15
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §A.1, §B.2 (apertura/chiusura del foglio), §C, §D.1-D.4
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md` D-52-06, D-52-19..23, D-52-28, D-52-29
  </read_first>
  <action>
    Creare `52-PROCEDURES.md` nella forma di `51-PROCEDURES.md`: frontmatter
    (`phase`, `document: le procedure manuali della fase 52`, `written`,
    `written_by: piano 52-02, task 1`, `walked: —`, `procedures: 7`,
    `status: SCRITTA, NON PERCORSA — gli esiti vanno in 52-ESITI.md`,
    `requirements: [NAV-01..NAV-07]`); riquadro «Questo documento e' pubblicato»
    (ruoli, mai persone; il ref del laboratorio sta in `.env.lab.local`);
    riquadro «scritta PRIMA di essere percorsa».
    `PRE-LAB`: progetto `ACTIVE_HEALTHY` (NXDOMAIN = `INACTIVE`, non rete);
    `lab.resonatemotion.com` serve il commit dichiarato (sha corto + data scritti
    in `52-ESITI.md` **prima**); M2 applicata sul laboratorio (piano 52-13);
    precondizioni di dati: account anonimo, `attendee`, `staff` non assegnato,
    `staff` assegnato alla serata di prova, `organizer`, `master`; media del banco
    del piano 52-01; versione di iOS del telefono scritta.
    Sette procedure, ciascuna con tabella `# · Passo · Ruolo · Cosa si deve
    osservare` e il proprio riquadro `Result: pending`:
    **P-52-A — la barra e il pannello per soggetto** (NAV-01, NAV-03, D-52-01..10,
    D-52-24, D-52-27): per ognuno dei sei soggetti, a 360 px e da tablet, le voci
    di barra nell'ordine della tabella di UI-SPEC §A.1; TASK grigia con bordo
    tratteggiato, un tocco non fa nulla; Management apre il foglio sopra la barra,
    che resta visibile; il foglio si chiude toccando lo scrim, la stessa voce,
    `Escape`, una riga; voci del pannello in ordine alfabetico, «Manage events» e
    non «Events»; Account dentro il pannello per chi ha Management, in barra per
    `attendee` e anonimo (anonimo → login); la pagina Account non mostra piu'
    «Management Tools»; da tablet la lista parte aperta dentro uno strumento e
    chiusa su Events.
    **P-52-B — il cancello della gallery** (NAV-02, D-52-13, D-52-29): anonimo su
    `/gallery` → `/login?next=%2Fgallery`, e dopo il login si torna su `/gallery`;
    `attendee` loggato → `/account`, nessun 404, nessun messaggio; `staff` e
    `organizer` → 200 con immagini; sulla pagina di una serata la sezione «Gallery»
    e' **assente** per `attendee` e presente per `staff`.
    **P-52-C — la striscia appesa** (NAV-04, D-52-11, D-52-27): da telefono,
    dentro Members, scorrere fino in fondo: la striscia degli strumenti resta in
    cima, opaca; si cambia strumento dal fondo; ordine alfabetico.
    **P-52-D — chip e cifra staff** (NAV-05, NAV-06, D-52-15..18): da anonimo i chip
    sono solo i format con serate pubblicate; con `organizer` (che vede le bozze)
    compare il chip di un format con sola bozza, se il banco ne ha uno;
    selezionare un chip non fa sparire gli altri; `?format=` di un format senza
    serate visibili → nessun filtro; nella pagina membri la cifra `staff` non
    reagisce al tocco e la legenda dice che uno staff tiene la gallery per ruolo.
    **P-52-E — la porta in cinque linguette** (D-52-19..22, D-52-26), su iPhone,
    radio accesa e poi spenta: cinque linguette sempre presenti anche vuote, in
    una riga, All · Out · In · Recent · Alerts con numero accanto senza parentesi;
    Recent vuota «No scans yet», Alerts vuota «Nothing to report»; un avviso nuovo
    (rete spenta con lista vecchia, o una notizia di cache) **accende il pallino e
    alza il numero senza aprire Alerts**; pastiglie Offline/Pending e avviso della
    guest list a radio spenta **sempre visibili** con Recent o Alerts aperte;
    annullare una scansione da Recent chiede la stessa conferma di oggi; titolo e
    «QR Scan» restano in vista scorrendo con ogni linguetta.
    **P-52-F — la tastiera** (D-52-06, D-52-23, D-52-28): in testa, **prima dei
    passi**, il riquadro dell'aspettativa: *«Sull'iPhone la pagina NON si
    stringera' sopra la tastiera: Safari non implementa ancora
    `interactive-widget`. Questa voce si registra come ATTESO ASSENTE, non come
    fallimento. Quello che deve funzionare sono le misure di layout.»* Poi
    quattro misure registrate **ognuna separata**: (1) la barra sparisce al fuoco
    di un campo e torna al blur (porta con `form="phone"`, un form pubblico con
    barra); (2) alla porta l'avviso della guest list sta **sopra** la ricerca;
    (3) al fuoco la ricerca sale in cima e la riga trovata con il suo «Check in»
    sta intera sopra la tastiera; (4) ridimensionamento del viewport — atteso
    assente. Il login si prova **com'e'**: si registra se email, password e «Sign
    In» restano sopra la tastiera; l'esito decide se toccare `FOCUS_ROOT` di
    `PageShell` (D-52-28) e non e' un fallimento della fase.
    **P-52-G — i media chiusi** (NAV-07, D-52-25, D-52-29..31): le otto sonde di
    ricerca §I.8, con chi le esegue (le sonde 1-4, 6-8 le esegue chi esegue il
    piano 52-13 con sessioni coniate sul laboratorio; la 5 — immagini e un video
    messo in pausa e ripreso entro l'ora, da `organizer` — la percorre il
    proprietario); per la sonda 1 la finestra di cache scritta prima (CDN fino al
    `max-age` dell'oggetto, di default 3600 s; service worker cross-origin 1 h) e
    la ripetizione dopo la finestra (Pitfall 15); l'immagine che non si carica
    mostra «This image could not be loaded. Reload the page.».
    In fondo: «La rilettura conferma, non sostituisce», «Se la corsa si ferma», e i
    sette riquadri `Result: pending`.
  </action>
  <verify>
    <automated>f=.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md; test -f $f &amp;&amp; for p in P-52-A P-52-B P-52-C P-52-D P-52-E P-52-F P-52-G PRE-LAB; do /usr/bin/grep -q "$p" $f || exit 1; done; /usr/bin/grep -q "atteso assente\|ATTESO ASSENTE" $f &amp;&amp; test $(/usr/bin/grep -c "Result: pending" $f) -ge 7 &amp;&amp; test $(/usr/bin/grep -cE "[0-9a-f]{8}-[0-9a-f]{4}-" $f) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Il file contiene `PRE-LAB` e le sette procedure `P-52-A` … `P-52-G`, ciascuna con la tabella `# · Passo · Ruolo · Cosa si deve osservare`
    - `P-52-F` porta il riquadro dell'aspettativa con «atteso assente» **prima** del primo passo, e le quattro misure registrate separatamente
    - `P-52-G` elenca le otto sonde con chi le esegue e la finestra di cache della sonda 1
    - Almeno sette `Result: pending`; nessun UUID, nessun ref di laboratorio, nessun nome di persona
    - Frontmatter con `status: SCRITTA, NON PERCORSA`
  </acceptance_criteria>
  <done>Le procedure esistono prima del codice che misureranno, e quella del viewport dice in anticipo cosa non accadra'.</done>
</task>

<task type="auto">
  <name>Task 2: Riempire 52-VALIDATION.md onestamente</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VALIDATION.md` — il modello da riempire
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-VALIDATION.md` — come e' stato riempito in un repo senza test
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` Validation Architecture (righe 1181-1222)
    - `package.json` — gli script `build`, `verify`, `verify:*`, `baseline:rls`, `baseline:compare` (nessuno script `test`)
  </read_first>
  <action>
    Riscrivere `52-VALIDATION.md` mantenendo le sezioni del modello.
    Frontmatter: `status: planned`, `nyquist_compliant: true`,
    `wave_0_complete: false`. **Test Infrastructure**: Framework = «nessuno — non
    esiste un test runner per il prodotto (nessuno script `test`, nessun file
    `*.test.*`); gate statici in `scripts/verify-*.mjs`»; Config =
    `scripts/verify-all.mjs`; Quick = `npm run build` (e' anche il typecheck) piu'
    il gate del file toccato; Full = `npm run build && npm run verify` (24 gate,
    oggi `VERIFY_OK`); runtime stimato misurato con un lancio reale di `npm run
    verify` e scritto. **Sampling**: dopo ogni task il comando del task; dopo ogni
    onda `npm run verify`; prima della verifica di fase `npm run verify` verde +
    `verify:capabilities` e `verify:refusal` verdi **contro la produzione** dopo
    l'atto del piano 52-15.
    **Per-Task Verification Map**: una riga per ogni task dei piani 52-01..52-17
    (id `52-NN-T`), con requisito, riferimento di minaccia `T-52-xx` del piano,
    tipo (`static` / `database` / `manual`) e il comando `<automated>` del task
    come scritto nel piano; le righe dei checkpoint portano `manual` e la
    procedura (`P-52-x`) o l'atto (`52-AUTHORISATION*.md`).
    **Wave 0**: `52-PROCEDURES.md` (questo piano), il censimento e il banco dei
    media (52-01), `scripts/verify-capabilities.mjs` e `PROBE_PAYLOADS.event_media`
    mossi nello stesso piano di M1 (52-06). Dichiarare: «nessun framework da
    installare: e' una decisione del progetto, non un buco».
    **Manual-Only Verifications**: P-52-A..G con requisito, perche' manuale (porta
    su un telefono vero; Safari iOS; sonde con sessioni vere) e il rimando alla
    procedura. **Sign-Off**: le caselle spuntate solo dove vere, `Approval:
    pending`.
    Mai la frase «i test passano».
  </action>
  <verify>
    <automated>f=.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-VALIDATION.md; /usr/bin/grep -q "nyquist_compliant: true" $f &amp;&amp; /usr/bin/grep -q "npm run build" $f &amp;&amp; /usr/bin/grep -q "npm run verify" $f &amp;&amp; test $(/usr/bin/grep -c "{pytest\|{quick command}\|REQ-{XX}" $f) -eq 0 &amp;&amp; test $(/usr/bin/grep -ci "i test passano" $f) -eq 0 &amp;&amp; /usr/bin/grep -q "52-17" $f</automated>
  </verify>
  <acceptance_criteria>
    - Nessun segnaposto del modello resta (`{pytest…}`, `{quick command}`, `REQ-{XX}`)
    - Framework dichiarato «nessuno», quick `npm run build`, full `npm run build && npm run verify`
    - La mappa porta una riga per ogni task dei piani 52-01..52-17, con comando o procedura
    - Wave 0 elenca procedure, censimento/banco e i gate mossi con M1; nessun framework da installare
    - La frase «i test passano» non compare
  </acceptance_criteria>
  <done>La strategia di validazione descrive quello che il repo puo' davvero provare, e come.</done>
</task>

<task type="auto">
  <name>Task 3: Chiudere le tre raccomandazioni non bloccanti della UI-SPEC</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md, .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md</files>
  <read_first>
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §10 (righe 1033-1052, la scala z) e §12 (righe 1102-1140, la riga su `--faint`)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §Color («L'unico uso di `--faint`…»), §B.2 (z-50), §C (z-10), §D.1 (conteggio senza parentesi)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md` D-52-19 e D-52-26
  </read_first>
  <action>
    (1) In `52-UI-SPEC.md` §D.1, riga «Contenuto»: aggiungere che il conteggio
    **senza parentesi** e' D-52-26 («etichette corte, numero accanto»), che
    supera la forma «Recent (12)» scritta in D-52-19 — la decisione piu' recente
    del proprietario, citata per non sembrare una deviazione.
    (2) In `41-UI-SPEC.md` §12, dopo la riga «`--faint` is not a small-text
    colour»: una riga che registra **l'unica eccezione**, datata 2026-09-23, fase
    52: il testo della voce TASK spenta, esentato da WCAG 1.4.3 come componente
    d'interfaccia inattivo; lo stato non lo porta il colore ma il bordo
    tratteggiato (3.54:1, sopra il 3:1 di 1.4.11) e `aria-disabled`; lista delle
    eccezioni: una.
    (3) In `41-UI-SPEC.md` §10, due righe nella tabella: `z-10` (`sticky`) — la
    barra fissa della porta (`ScannerClient.tsx`, piano 51-07) e la striscia degli
    strumenti appesa (fase 52, D-52-11); `z-40` (`fixed`) — `StickyBuyBar`
    (`StickyBuyBar.tsx:121`). Sotto la tabella, una frase datata: i due gradini
    esistevano gia' nell'albero e mancavano dalla scala scritta; la fase 52 li
    registra **senza aggiungerne** (il foglio Management sta su `z-50`, il rango
    della navigazione).
    Nessun'altra riga di 41-UI-SPEC si tocca.
  </action>
  <verify>
    <automated>/usr/bin/grep -q "D-52-26" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md &amp;&amp; /usr/bin/grep -q "z-40" .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md &amp;&amp; /usr/bin/grep -q "z-10" .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md &amp;&amp; /usr/bin/grep -q "TASK" .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `52-UI-SPEC.md` §D.1 cita D-52-26 accanto al conteggio senza parentesi
    - `41-UI-SPEC.md` §12 porta la riga datata dell'unica eccezione `--faint` (TASK spenta), con bordo tratteggiato 3.54:1 e `aria-disabled` come canali dello stato
    - `41-UI-SPEC.md` §10 porta le righe `z-10` (sticky: barra della porta, striscia appesa) e `z-40` (`StickyBuyBar`), con la frase che dichiara che non si aggiungono gradini
    - Nessun'altra riga di `41-UI-SPEC.md` cambia (`git diff` mostra solo aggiunte in quelle due sezioni)
  </acceptance_criteria>
  <done>Le tre raccomandazioni del controllo della UI-SPEC sono chiuse nei documenti che le reggono.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` → pubblico | procedure e validazione sono pubblicate con il repo |
| procedura → esito | un `Result` riempito in anticipo afferma una corsa che non c'e' stata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Repudiation | `52-PROCEDURES.md` | mitigate | ogni `Result` resta `pending`; gli esiti vanno in `52-ESITI.md` con ora UTC e ruolo; «la rilettura conferma, non sostituisce» |
| T-52-06 | Information disclosure | procedure pubblicate | mitigate | ruoli e mai persone; nessun ref di laboratorio; nessun UUID (controllato dal comando di verifica) |
| T-52-07 | Repudiation | P-52-F «verificato» per la ragione sbagliata | mitigate | l'aspettativa «atteso assente» e' scritta prima dei passi e le quattro misure si registrano separate (Pitfall 6) |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `52-PROCEDURES.md` con PRE-LAB e P-52-A..G, tutte `pending`
- `52-VALIDATION.md` senza segnaposto, `nyquist_compliant: true`
- 41-UI-SPEC §10 e §12, 52-UI-SPEC §D.1 aggiornati
</verification>

<success_criteria>
La fase ha le sue prove scritte prima del codice, e la strategia di validazione
non promette una copertura che il repo non ha.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-02-SUMMARY.md` when done
</output>
