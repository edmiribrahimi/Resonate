---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 09
type: execute
wave: 3
depends_on: ["52-06", "52-01"]
files_modified:
  - scripts/purge-media-orphans.mjs
autonomous: true
requirements: [NAV-07]

must_haves:
  truths:
    - "Esiste uno strumento che rimuove PER CHIAVE gli oggetti delle foto rifiutate (oggetto e riga) e gli oggetti orfani (senza riga) del bucket event-media, su una lista catturata dal censimento, dopo istantanea, con il conteggio dopo preso da una fonte diversa (D-52-30)"
    - "Le due popolazioni — rifiutate e orfani — si contano, si catturano e si cancellano SEPARATE, e un oggetto nato nell'ultima ora non e' mai un orfano (e' un caricamento in volo)"
    - "Lo strumento punta al laboratorio per default; in produzione ci arriva solo con --authorised e --dated su un blocco di concessione proprio, che rilegge i numeri e che lo strumento marca esaurito da se' (ai-engineering.md)"
    - "Sul laboratorio la rimozione e' stata eseguita: gli oggetti rifiutati e l'orfano del banco non esistono piu', e il riconteggio viene da una fonte diversa da quella che ha cancellato"
  artifacts:
    - path: "scripts/purge-media-orphans.mjs"
      provides: "rimozione per chiave, due popolazioni, istantanea di soli metadati, atto consumato"
      contains: "purge-media-orphans: grant"
      min_lines: 300
  key_links:
    - from: "scripts/purge-media-orphans.mjs"
      to: "52-AUTHORISATION-MEDIA.md (piano 52-16)"
      via: "blocco <!-- purge-media-orphans: grant --> … <!-- /purge-media-orphans: grant --> letto solo al suo interno"
      pattern: "purge-media-orphans: grant"
---

<objective>
Scrivere lo strumento di D-52-30 — la rimozione per chiave degli oggetti
rifiutati e orfani — e provarlo **tutto** sul laboratorio.

Purpose: D-52-30 chiude il gate *moderazione = rimozione* (`media-and-storage.md`):
rifiutare deve rendere un contenuto irraggiungibile, non invisibile. M2 rendera'
quegli oggetti non firmabili; toglierli davvero e' un'altra operazione, e il
proprietario ha deciso che si fa **in questa fase**. E' una cancellazione
irreversibile su un progetto senza PITR: segue `purge-attendances.mjs` **per
struttura** (P7: laboratorio per default, produzione solo sotto atto — non la
forma `lab-*` che rifiuta la produzione del tutto).
Output: `scripts/purge-media-orphans.mjs`; il laboratorio ripulito; i numeri nel
SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
@./CLAUDE.md
@.claude/rules/ai-engineering.md
@.claude/rules/media-and-storage.md
@.claude/rules/legal-compliance.md
@.claude/rules/supabase-data.md

<interfaces>
Analogo: `scripts/purge-attendances.mjs` (551 righe). Da seguire per struttura:
testata «NON PUNTA MAI ALLA PRODUZIONE PER DEFAULT» (`:1-15`); «LE QUATTRO
REGOLE DELLA RIMOZIONE» (`:26-49`); «UNA DECISIONE SENZA SOGGETTI NON SI
ESEGUE» (`:51-54`); blocco di concessione e controlli (`:56-73`, `:155-204`);
argomenti prima di agire, `--dry-run` | `--apply` obbligatori e mai insieme
(`:107-153`); `refuse()` → exit 2, `fail(category, …)` → exit 1 (`:129-132`,
`:213-217`); `sql(query, { readOnly })` via Management API (`:218-248`);
istantanea in `.env*` datato, mai sotto `.planning/` (`:99-103`, `:210-211`);
`DELETE … WHERE id = ANY(ARRAY[<lista>])` + `delete_short` (`:480-512`);
contatore da fonte diversa (PostgREST col service role, `:250-287`, `:514-527`);
consumo dell'autorizzazione scritto dallo strumento (`:529-546`).

**Differenza voluta**: `purge-attendances.mjs` legge i campi del blocco con
`^name:` sull'intero documento. Qui un documento solo (`52-AUTHORISATION-MEDIA.md`)
portera' **due** blocchi (questo strumento e `restrip-event-media.mjs`): i campi si
leggono e si marcano **solo fra** `<!-- purge-media-orphans: grant -->` e
`<!-- /purge-media-orphans: grant -->`.

Storage: `supabase.storage.from("event-media").remove([...names])` col service
role (non e' una scrittura per `verify:media-strip`, che vede solo `src/`).
Colonna `event_media.storage_path` (M1, piano 52-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere scripts/purge-media-orphans.mjs</name>
  <files>scripts/purge-media-orphans.mjs</files>
  <read_first>
    - `scripts/purge-attendances.mjs` per intero — la struttura da seguire, non da semplificare
    - `src/app/(public)/events/[slug]/actions.ts` righe 140-200 e 291-349 — la finestra fra scrittura dell'oggetto e insert della riga (`:145-152`), l'ordine oggetto-prima-riga-dopo di `deleteMedia`
    - `.claude/rules/legal-compliance.md` — gate *immagini delle persone* e *i dati dei soci*: perche' l'istantanea qui non porta byte
    - `.gitignore` riga ~34 — `.env*` coperto
  </read_first>
  <action>
    Creare `scripts/purge-media-orphans.mjs` nella struttura di
    `purge-attendances.mjs`. Testata: cosa fa (D-52-30), NON PUNTA MAI ALLA
    PRODUZIONE PER DEFAULT, e le regole della rimozione riscritte per due
    popolazioni: (1) si conta **per popolazione** — oggetti di righe `rejected`
    (popolazione R: oggetto **e** riga) e oggetti **orfani** (popolazione O: oggetto
    senza riga con `storage_path = name`) — e le due restano separate in ogni
    riga del referto, perche' sommarle farebbe autorizzare l'una dalla decisione
    sull'altra; (2) **un oggetto creato meno di `--older-than-minutes` fa (default
    60) non e' un orfano**: e' un caricamento fra la scrittura della route di
    finalize e l'insert di `registerMedia` (`actions.ts:145-152`); si conta a
    parte come «in volo» e non si tocca; (3) istantanea **prima**, in
    `.env.media-purge-snapshot.<stamp>.json`, di **soli metadati** — nome
    dell'oggetto, dimensione, mimetype, `created_at`; per R anche `id`, `status`,
    `created_at` della riga — **nessun byte**: la rimozione di una foto rifiutata
    e' lo scopo, e conservarne una copia sul disco di chi esegue la vanificherebbe
    (`legal-compliance.md`, immagini delle persone); dichiararlo; (4) per chiave su
    lista catturata dalla stessa interrogazione che ha contato; (5) conferma da
    fonte diversa.
    Argomenti: `--dry-run` | `--apply` (obbligatori, mai insieme), `--project`,
    `--authorised <percorso>`, `--dated <YYYY-MM-DD>`, `--older-than-minutes <n>`.
    Il rifiuto e' la prima cosa eseguita dopo gli argomenti: con il ref di
    produzione servono `--authorised` e `--dated`, e il documento deve contenere
    il blocco `<!-- purge-media-orphans: grant -->` … `<!-- /purge-media-orphans:
    grant -->` con, **dentro il blocco**, `act: event_media.purge`, `target:
    production`, `granted: yes`, `granted_on: <data>`, `spent: no`,
    `rejected_objects: <n>`, `orphan_objects: <n>`; rifiuto (exit 2) se manca il
    blocco, se un campo non corrisponde, se la data non coincide con `--dated`.
    Se i numeri catturati oggi differiscono da quelli scritti nel blocco, **exit
    1** con categoria `count_drift` e nessuna scrittura: chi ha concesso ha
    concesso quei numeri.
    Sequenza: (a) conteggio per popolazione con `sql(…, { readOnly: true })` su
    `storage.objects` e `public.event_media`; zero in entrambe → lo dice ed esce
    0; (b) istantanea; (c) cattura: nomi degli oggetti R con gli `id` delle loro
    righe, nomi degli oggetti O; (d) con `--dry-run` si ferma qui; (e) atto R:
    `remove([...nomi R])` col service role, **poi** `DELETE FROM public.event_media
    WHERE id = ANY(ARRAY[<id catturati>]::uuid[]) AND status = 'rejected'
    RETURNING id` via Management API — oggetto prima e riga dopo, come
    `deleteMedia`; controllo `delete_short` su entrambi; (f) atto O: `remove([...nomi
    O])`; `delete_short`; (g) riconteggio da **fonte diversa**: gli oggetti via
    Management API su `storage.objects` (non via l'API Storage che ha cancellato),
    le righe `rejected` via PostgREST col service role (non via Management API);
    residui > 0 → `fail("residual", …)`; (h) in produzione, marca il blocco
    esaurito **al suo interno** (`spent: yes` + `spent_at: <utc>`).
    Log e referto: **mai** un nome di oggetto, un path, un URL o un `id` sullo
    standard output — solo numeri, categorie, ore UTC e il nome del file
    d'istantanea. Categorie `[purge-media-orphans.<categoria>]` distinte per
    causa. Variabili: `SUPABASE_ACCESS_TOKEN`, `LAB_PROJECT_REF`,
    `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`; l'URL di PostgREST
    verificato contro il ref (forma di `purge-attendances.mjs:250-287`).
  </action>
  <verify>
    <automated>node --check scripts/purge-media-orphans.mjs &amp;&amp; /usr/bin/grep -q "purge-media-orphans: grant" scripts/purge-media-orphans.mjs &amp;&amp; /usr/bin/grep -q "/purge-media-orphans: grant" scripts/purge-media-orphans.mjs &amp;&amp; /usr/bin/grep -q "older-than-minutes" scripts/purge-media-orphans.mjs &amp;&amp; /usr/bin/grep -q "count_drift" scripts/purge-media-orphans.mjs &amp;&amp; node scripts/purge-media-orphans.mjs --dry-run --apply; test $? -eq 2 &amp;&amp; SUPABASE_ACCESS_TOKEN=x node scripts/purge-media-orphans.mjs --dry-run --project cjsfocnhfzycbbgkwocx; test $? -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` esce 0
    - `--dry-run --apply` insieme → exit 2; il ref di produzione senza `--authorised`/`--dated` → exit 2, prima di qualunque lettura (entrambi provati dal comando di verifica)
    - Il blocco di concessione si legge e si marca solo fra i due marcatori; contiene `rejected_objects` e `orphan_objects`; uno scarto dai numeri concessi → exit 1 `count_drift`
    - Le due popolazioni sono contate, catturate, cancellate e ricontate separatamente; gli oggetti piu' giovani della soglia sono contati come «in volo» e mai toccati
    - Istantanea in `.env.media-purge-snapshot.*.json` di soli metadati, con il paragrafo che dice perche' niente byte
    - Nessuna riga di output stampa un nome di oggetto, un path, un URL o un UUID (dichiarato nel docblock e osservato nel task 2)
  </acceptance_criteria>
  <done>Lo strumento esiste e sa dire di no prima di leggere.</done>
</task>

<task type="auto">
  <name>Task 2: Eseguirlo sul laboratorio — prima guardare, poi togliere, poi ricontare altrove</name>
  <files>scripts/purge-media-orphans.mjs</files>
  <read_first>
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-01-SUMMARY.md` — cosa ha seminato il banco (almeno una rifiutata con oggetto, un orfano)
    - `scripts/dev-lab.sh` — le variabili del laboratorio
  </read_first>
  <action>
    Caricare `.env.local` e poi `.env.lab.local`. Accertare il laboratorio
    `ACTIVE_HEALTHY`. Lanciare `node scripts/purge-media-orphans.mjs --dry-run` e
    leggere il referto: R ≥ 1, O ≥ 1 (dal banco del piano 52-01), «in volo» = 0.
    Se l'orfano del banco risulta «in volo» perche' e' stato seminato da meno di
    un'ora, rilanciare con `--older-than-minutes` minore e **dirlo** nel SUMMARY
    (la soglia di produzione resta 60).
    Poi `--apply`. Rileggere a mano, `read_only`, su `storage.objects` e
    `public.event_media` del laboratorio: zero oggetti di righe `rejected`, zero
    righe `rejected` catturate, zero orfani piu' vecchi della soglia; le righe
    approvate e in attesa del banco **intatte** (stesso conteggio di prima).
    Rilanciare `--dry-run`: deve dire «zero soggetti» ed uscire 0 (idempotenza).
    Scrivere nel SUMMARY: i numeri del dry-run, dell'atto, del riconteggio, l'ora
    UTC di ciascuno, il nome del file d'istantanea (non il contenuto). Confermare
    che l'istantanea e' ignorata da git (`git check-ignore`).
    Zero scritture in produzione, dichiarato.
  </action>
  <verify>
    <automated>node --check scripts/purge-media-orphans.mjs &amp;&amp; ls .env.media-purge-snapshot.*.json &gt;/dev/null &amp;&amp; git check-ignore -q $(ls .env.media-purge-snapshot.*.json | head -1) &amp;&amp; test -z "$(git status --porcelain | /usr/bin/grep media-purge-snapshot)"</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY riporta dry-run, atto e riconteggio con i numeri per popolazione e l'ora UTC
    - Sul laboratorio, dopo l'atto: zero oggetti di righe rifiutate, zero orfani oltre la soglia, righe approvate e in attesa invariate (conteggi prima/dopo nel SUMMARY)
    - Il secondo `--dry-run` dice zero soggetti ed esce 0
    - L'istantanea esiste, e' ignorata da git e non compare in `git status`
    - Zero scritture in produzione, dichiarato
  </acceptance_criteria>
  <done>Sul laboratorio i rifiutati e l'orfano non esistono piu', e lo dice una fonte diversa da quella che li ha tolti.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → Storage e database | cancellazione irreversibile, progetto senza PITR |
| documento di autorizzazione → strumento | il permesso e' letto e consumato dallo strumento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-39 | Tampering | cancellazione di foto approvate o in attesa | mitigate | lista catturata dalla stessa interrogazione che conta; `DELETE` per chiave con `AND status = 'rejected'`; riconteggio delle approvate prima/dopo |
| T-52-40 | Tampering | un caricamento in volo scambiato per orfano | mitigate | soglia `--older-than-minutes` (60 in produzione); gli oggetti giovani contati «in volo» e mai toccati |
| T-52-41 | Elevation of privilege | lo strumento in produzione senza atto, o con un atto vecchio o gia' speso | mitigate | exit 2 prima di ogni lettura; `granted_on` = `--dated`; `spent: no`; `count_drift` se i numeri sono cambiati |
| T-52-42 | Information disclosure | nomi di oggetti (contengono id di utente e di evento) su stdout o in `.planning/` | mitigate | solo numeri e categorie; istantanea in `.env*` ignorato |
| T-52-43 | Information disclosure | copia locale delle foto rifiutate | mitigate | istantanea di soli metadati, dichiarato con la ragione |
| T-52-44 | Repudiation | conferma presa con lo strumento che ha cancellato | mitigate | riconteggio da fonte diversa (Management API per gli oggetti, PostgREST per le righe) |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `node --check` e i due rifiuti (exit 2) provati
- Laboratorio ripulito, riconteggio da fonte diversa, secondo dry-run a zero
</verification>

<success_criteria>
D-52-30 ha uno strumento provato, che in produzione fara' solo cio' che l'atto
del piano 52-16 dira', una volta.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-09-SUMMARY.md` when done
</output>
