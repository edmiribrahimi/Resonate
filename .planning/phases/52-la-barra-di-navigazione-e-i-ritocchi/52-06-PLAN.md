---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 06
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - supabase/migrations/20260923180000_gallery_view_and_media_paths.sql
  - src/lib/capabilities/keys.ts
  - src/lib/routes/capability-routes.ts
  - scripts/verify-capabilities.mjs
  - scripts/rls-baseline.mjs
  - src/types/database.ts
autonomous: true
requirements: [NAV-02, NAV-07]

must_haves:
  truths:
    - "Il catalogo porta la chiave gallery.view concessa per ruolo a master, organizer e staff: 16 chiavi, 31 concessioni (16/14/1/0) — e la costante CAP.GALLERY_VIEW nasce nello stesso commit della migration (D-52-12)"
    - "/gallery e' nella mappa delle rotte sotto gallery.view, con alsoGatesTables: true e la nota per chi riaprira' la gallery, che dice che oggi riaprirla e' anche una migration (D-52-13, D-52-14, P2)"
    - "M1 e' additiva: event_media guadagna storage_path con backfill, url perde il NOT NULL, e la policy sugli oggetti chiede la riga (EXISTS); il bucket resta pubblico e il codice di oggi continua a caricare (D-52-25)"
    - "M1 e' applicata al LABORATORIO dall'endpoint migrations e riletta dal catalogo; verify:capabilities e' verde contro il laboratorio e dichiarato rosso contro la produzione fino al piano 52-15"
  artifacts:
    - path: "supabase/migrations/20260923180000_gallery_view_and_media_paths.sql"
      provides: "chiave + 3 concessioni, storage_path con backfill, url nullable, policy EXISTS sugli oggetti, DO che solleva, COMMENT ricontati"
      contains: "event_media_objects_select_by_row"
    - path: "src/lib/capabilities/keys.ts"
      provides: "CAP.GALLERY_VIEW e la sua descrizione byte-identica"
      contains: "GALLERY_VIEW"
    - path: "src/lib/routes/capability-routes.ts"
      provides: "[CAP.GALLERY_VIEW]: { routes: [\"/gallery\"], alsoGatesTables: true }"
      contains: "CAP.GALLERY_VIEW"
    - path: "scripts/verify-capabilities.mjs"
      provides: "16 / 64 / 31 / 33"
      contains: "EXPECTED_KEY_COUNT = 16"
  key_links:
    - from: "supabase/migrations/20260923180000_gallery_view_and_media_paths.sql"
      to: "src/lib/capabilities/keys.ts CAP_DESCRIPTIONS"
      via: "la descrizione della chiave, byte-identica, nello stesso commit"
      pattern: "gallery.view"
    - from: "src/lib/capabilities/keys.ts"
      to: "src/lib/routes/capability-routes.ts"
      via: "satisfies Record<CapabilityKey, Binding> — chiave senza voce = errore di build"
      pattern: "GALLERY_VIEW"
    - from: "storage.objects policy event_media_objects_select_by_row"
      to: "public.event_media.storage_path"
      via: "EXISTS valutato sotto la RLS di chi firma"
      pattern: "storage_path = storage.objects.name"
---

<objective>
La prima migration della fase — M1, additiva — con il catalogo TypeScript e i
gate che si muovono nello stesso commit, applicata al laboratorio e riletta dal
catalogo.

Purpose: D-52-12 (chiave e costante nello stesso commit), D-52-13/14 (la voce di
mappa e la sua nota), D-52-25 (NAV-07: la colonna che permette di firmare una
chiave invece di un URL, e la policy sull'oggetto che chiede la riga). L'ordine
di NAV-07 e' un vincolo duro: **M1 additiva → deploy del codice che firma → M2
che chiude**. Questo piano scrive e applica solo M1, e solo al laboratorio.

**Correzione alla ricerca, dichiarata qui (planner, 2026-09-23).** La forma di M1
in ricerca §I.4 mette `storage_path NOT NULL` gia' in M1. Ma fra M1 e il deploy
il codice **di oggi** inserisce righe senza `storage_path` (`registerMedia`
scrive solo `url`, `actions.ts:179-192`): con `NOT NULL` in M1 **il caricamento
si rompe in produzione** per tutta la finestra — lo stesso guasto che §I.6 attribuisce
al solo ordine inverso. Quindi: in M1 `storage_path` nasce **nullable** con
backfill e `url` perde il `NOT NULL` (innocuo per il codice di oggi, necessario al
codice nuovo che smette di scriverlo); in **M2** (piano 52-13) si ripete il
backfill per le righe nate nella finestra e solo allora `SET NOT NULL`, con un
`DO` che solleva se ne resta una. Nessuna finestra rompe il caricamento.
Output: M1, `keys.ts`, `capability-routes.ts`, `verify-capabilities.mjs`,
`rls-baseline.mjs`, `database.ts`; il laboratorio con M1 applicata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
@./CLAUDE.md
@.claude/rules/supabase-data.md
@.claude/rules/access-gating.md
@.claude/rules/media-and-storage.md
@.claude/rules/ai-engineering.md

<interfaces>
Endpoint: `POST /v1/projects/$LAB_PROJECT_REF/database/migrations` — avvolge il
corpo in UNA transazione: **niente `BEGIN;`/`COMMIT;` nel file** (P1,
`20260922120000:32-42`). La versione registrata e' coniata dall'endpoint, non il
nome del file (`51-08-SUMMARY.md:67-71`): la prova e' la rilettura dal catalogo.

Numeri del catalogo OGGI (P3, `20260923120000_role_capabilities_comment.sql`):
15 chiavi; 28 concessioni = 15 master + 13 organizer + 0 staff + 0 attendee.
DOPO M1: 16 chiavi; 31 = 16 + 14 + 1 + 0.

`scripts/verify-capabilities.mjs`: `EXPECTED_KEY_COUNT = 15` (`:267`) → 16;
`EXPECTED_PAIR_COUNT = 60` (`:784`) → 64; `EXPECTED_GRANT_COUNT = 28` (`:785`) → 31;
`EXPECTED_REFUSAL_COUNT = 32` (`:786`) → 33; `ROLE_GRANTS` (`:325-688`), quattro blocchi.

`scripts/rls-baseline.mjs:1270-1282` `PROBE_PAYLOADS.event_media.insert.columns =
['event_id','party_id','url','type','uploaded_by']`, url di sonda
`'https://example.invalid/rls-baseline-probe'`.

`src/lib/routes/capability-routes.ts`: forma di una voce a `:489-492`
(`CATALOGUE_MANAGE`) e `:883-894` (`PRODUCTION_VISUAL_MANAGE`, paragrafo «The
middleware is UX. The RLS is the boundary.»); `alsoGatesTables` spiegato a
`:158-190`; `satisfies Record<CapabilityKey, Binding>` a `:895`.

`src/lib/capabilities/keys.ts`: `CAP` a `:316-371`; `CAP_DESCRIPTIONS` a
`:408-445` (`Record<CapabilityKey, string>`, BYTE-IDENTICAL alla colonna
`description`); docblock `:400-406` «fifteen rows».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: M1 e il catalogo TypeScript, in un commit solo</name>
  <files>supabase/migrations/20260923180000_gallery_view_and_media_paths.sql, src/lib/capabilities/keys.ts, src/lib/routes/capability-routes.ts, scripts/verify-capabilities.mjs, scripts/rls-baseline.mjs, src/types/database.ts</files>
  <read_first>
    - `supabase/migrations/20260922120000_role_attendee_and_capability_keys.sql` righe 1-49 (testata: verso del deploy, «UNA TRANSAZIONE SOLA, E NON C'E' `BEGIN;`», «cosa questa migration NON fa»), 587-603 (il `DO` che solleva), 638-642 (i `COMMENT`)
    - `supabase/migrations/20260923120000_role_capabilities_comment.sql` per intero — il testo attuale del `COMMENT` su `private.role_capabilities`, da riscrivere ricontato
    - `supabase/migrations/20260817120000_production_section_keys.sql` righe 104-121, 165-174 — la forma dell'inserimento di chiavi e concessioni (qui **due** colonne: `requires_approved` non esiste dalla fase 50)
    - `supabase/migrations/20260817120400_visual_archive_bucket.sql` righe 116-128, 180-208, 257-266 — braccio di lettura sugli oggetti `TO authenticated`, assenze dichiarate per descrizione, «WHAT TO DO INSTEAD»
    - `supabase/migrations/20260225120000_phase7_media.sql` righe 20-30, 60-101 — le policy di `event_media` e del bucket oggi
    - `src/lib/capabilities/keys.ts` righe 300-450; `src/lib/routes/capability-routes.ts` righe 130-200, 480-500, 880-896
    - `scripts/verify-capabilities.mjs` righe 225-270, 320-330, 500-520, 640-720, 780-790
    - `scripts/rls-baseline.mjs` righe 1260-1285
    - `src/types/database.ts` righe 410-470 — il tipo di `event_media`
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md` §Censimento — se `url` di altra forma > 0, **fermarsi qui** e riportarlo
  </read_first>
  <action>
    **La migration** `supabase/migrations/20260923180000_gallery_view_and_media_paths.sql`
    (se esiste gia' una migration con timestamp maggiore, usare un timestamp
    strettamente maggiore e aggiornare i riferimenti di questo piano nel SUMMARY).
    Testata nella forma di `20260922120000:1-49`: titolo, fase 52, D-52-12,
    D-52-13, D-52-25; **il verso del deploy** — migration PRIMA, codice DOPO
    (codice prima: `registerMedia` scriverebbe una colonna che non c'e'); il
    paragrafo «UNA TRANSAZIONE SOLA, E NON C'E' `BEGIN;` PERCHE' NON SERVE»
    ricopiato da `:32-42`; «COSA QUESTA MIGRATION NON FA»: non chiude il bucket,
    non tocca `event_media_select_approved`, non toglie «Anyone can view event
    media», non rende `storage_path` `NOT NULL` — tutto questo e' M2
    (`20260923180100_gallery_close_data.sql`), da applicare **solo dopo** il
    deploy del codice che firma; e il paragrafo della correzione alla ricerca
    (perche' `storage_path` resta nullable qui).
    Corpo, in quest'ordine:
    (1) `INSERT INTO private.capabilities (key, description) VALUES
    ('gallery.view', '<descrizione>') ON CONFLICT (key) DO NOTHING;` — la
    descrizione, in inglese come le altre, dice: see the gallery — approved
    photos and videos of the nights — granted by role to master, organizer and
    staff (phase 52, D-52-12); governs rows of event_media since NAV-07.
    (2) `INSERT INTO private.role_capabilities (role, capability) VALUES
    ('master','gallery.view'), ('organizer','gallery.view'), ('staff','gallery.view')
    ON CONFLICT (role, capability) DO NOTHING;`
    (3) `ALTER TABLE public.event_media ADD COLUMN IF NOT EXISTS storage_path text;`
    poi `UPDATE public.event_media SET storage_path = substring(url from
    '/storage/v1/object/public/event-media/(.+)$') WHERE storage_path IS NULL;`
    (gate *default sulle righe esistenti*: il file dichiara cosa succede alle
    righe gia' presenti).
    (4) `ALTER TABLE public.event_media ALTER COLUMN url DROP NOT NULL;` — con la
    ragione: il codice nuovo smette di scriverlo; `url` resta per storia e smette
    di funzionare quando il bucket si chiude.
    (5) Un `CHECK` con nome, `event_media_storage_path_is_a_key`: `storage_path
    IS NULL OR (storage_path !~ '^(https?:|/)' AND length(storage_path) > 0)`.
    (6) `CREATE UNIQUE INDEX IF NOT EXISTS event_media_storage_path_key ON
    public.event_media (storage_path);` — gate *indici*: la policy `EXISTS` la
    valuta per ogni firma.
    (7) `DROP POLICY IF EXISTS event_media_objects_select_by_row ON storage.objects;`
    e `CREATE POLICY event_media_objects_select_by_row ON storage.objects AS
    PERMISSIVE FOR SELECT TO authenticated USING (bucket_id = 'event-media' AND
    EXISTS (SELECT 1 FROM public.event_media m WHERE m.storage_path =
    storage.objects.name));` — commento per senso da `20260817120400:180-196`
    (perche' `TO authenticated`, perche' si firma col client della sessione e mai
    col service role; la sottoquery gira sotto la RLS di `event_media` di chi
    firma, assunzione A8 che la sonda 3 del piano 52-13 prova).
    (8) Il `DO` nella forma di `20260922120000:587-603`, che **solleva** se:
    concessioni di `gallery.view` ≠ 3; concessioni totali ≠ 31; concessioni di
    `attendee` ≠ 0; righe con `storage_path IS NULL` dopo il backfill > 0 — con
    `HINT` che dice che una riga di forma diversa si guarda una per una e non si
    indovina.
    (9) `COMMENT ON TABLE private.role_capabilities` riscritto **ricontato**: 31
    righe, 16 a master, 14 a organizer, **1 a staff (gallery.view, fase 52,
    D-52-12)**, ZERO ad attendee; la frase «ZERO a staff … asimmetria dichiarata»
    diventa: staff tiene una chiave per ruolo, la gallery; la porta resta
    dell'assegnazione. `COMMENT ON TABLE private.capabilities`: 16 chiavi dalla
    fase 52. `COMMENT ON COLUMN public.event_media.storage_path`: la chiave
    dell'oggetto nel bucket `event-media`, cio' che si firma; nullable fino a M2.
    Assenze dichiarate **per descrizione**, mai scrivendo per esteso la clausola
    vietata: nessuna INSERT per `authenticated` sul bucket (`verify:media-strip`
    controllo D), nessun braccio per l'anonimo.
    **Il TypeScript nello stesso commit:**
    `keys.ts` — `GALLERY_VIEW: "gallery.view",` in `CAP` con JSDoc (chi la tiene,
    che `staff` la tiene **per ruolo** ed e' la sua prima concessione per ruolo,
    che da NAV-07 governa righe); la descrizione in `CAP_DESCRIPTIONS`
    **byte-identica** a quella della migration; il docblock «fifteen rows» →
    «sixteen».
    `capability-routes.ts` — voce `[CAP.GALLERY_VIEW]: { routes: ["/gallery"],
    alsoGatesTables: true }`, **senza** `assignmentOpenable`, con la nota
    D-52-12/13/14: PER RIAPRIRLA AL PUBBLICO togliere questa voce, la guardia in
    cima a `(public)/gallery/page.tsx`, `"/gallery"` da `PROTECTED_PREFIXES` e il
    suo pattern da `NEXT_ALLOW_LIST` (`next-redirect.ts`), e — da NAV-07 — anche
    **una migration** (la policy `event_media_select_gallery` e il bucket
    privato); la chiave resta, innocua. Poi il paragrafo «The middleware is UX.
    The RLS is the boundary.». Ricontare il paragrafo di `:158-190` che dice
    quante chiavi portano `alsoGatesTables` («Nine of the seventeen» e' datato):
    contare le voci con il flag e scrivere il numero vero su 16.
    `verify-capabilities.mjs` — le quattro costanti a 16 / 64 / 31 / 33, ognuna con
    la sua riga di storia datata («Da 15 a 16, fase 52, D-52-12 — ed e'
    un'AGGIUNTA»); `'gallery.view': 'GRANTED'` nei blocchi `master`, `organizer`,
    `staff` e `'gallery.view': 'REFUSED'` in `attendee` con il suo commento di
    rifiuto nella forma di `:676-687`; la prosa `:245-265` riscritta con 16/14/1/0;
    l'aritmetica pre-registrata `:690-720` **sommata in coda**, non riscritta; la
    nota «ROSSO CONTRO LA PRODUZIONE, VERDE CONTRO IL LABORATORIO» ripetuta con il
    nome del piano che applica in produzione: **52-15**.
    `rls-baseline.mjs` — `PROBE_PAYLOADS.event_media.insert`: `'storage_path'` in
    `columns` e il valore `'rls-baseline-probe/probe.jpg'` (non comincia con `/`
    ne' con `http`, rispetta il `CHECK`), con un commento: la sonda deve misurare
    la policy, non fallire per una colonna (Pitfall 14; con `NOT NULL` da M2).
    `database.ts` — nel tipo di `event_media`: `storage_path: string | null`
    con commento (nullable fino a M2, piano 52-13). **`url` resta `string` nel
    tipo** fino al piano 52-13: oggi il codice lo legge ancora; il SUMMARY
    dichiara la divergenza temporanea fra schema e tipo e il piano che la chiude.
  </action>
  <verify>
    <automated>f=supabase/migrations/20260923180000_gallery_view_and_media_paths.sql; npm run build &amp;&amp; test -f $f &amp;&amp; test $(/usr/bin/grep -ciE "^\s*(BEGIN|COMMIT);" $f) -eq 0 &amp;&amp; /usr/bin/grep -q "event_media_objects_select_by_row" $f &amp;&amp; /usr/bin/grep -q "DROP NOT NULL" $f &amp;&amp; test $(/usr/bin/grep -v "^\s*--" $f | /usr/bin/grep -c "SET NOT NULL") -eq 0 &amp;&amp; test $(/usr/bin/grep -v "^\s*--" $f | /usr/bin/grep -c "public = false") -eq 0 &amp;&amp; /usr/bin/grep -q "EXPECTED_KEY_COUNT = 16" scripts/verify-capabilities.mjs &amp;&amp; /usr/bin/grep -q "EXPECTED_GRANT_COUNT = 31" scripts/verify-capabilities.mjs &amp;&amp; /usr/bin/grep -q "GALLERY_VIEW" src/lib/capabilities/keys.ts &amp;&amp; /usr/bin/grep -q "storage_path" scripts/rls-baseline.mjs &amp;&amp; npm run verify:media-strip</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify:media-strip` escono 0
    - La migration non contiene `BEGIN;` ne' `COMMIT;`; contiene la chiave, le tre concessioni, `storage_path` con backfill, `url DROP NOT NULL`, il `CHECK` nominato, l'indice unico, la policy `event_media_objects_select_by_row` `TO authenticated`, il `DO` che solleva sui quattro conti, i tre `COMMENT`
    - Fuori dai commenti, la migration non contiene `SET NOT NULL` ne' `public = false` (sono di M2)
    - La descrizione di `gallery.view` e' byte-identica fra migration e `CAP_DESCRIPTIONS` (controllo: estrarre le due stringhe e confrontarle con `diff`, scritto nel SUMMARY)
    - `capability-routes.ts` ha `[CAP.GALLERY_VIEW]` con `routes: ["/gallery"]`, `alsoGatesTables: true`, nessun `assignmentOpenable`, e la nota di riapertura che nomina la migration
    - `verify-capabilities.mjs`: 16 / 64 / 31 / 33, `gallery.view` in tutti e quattro i blocchi di `ROLE_GRANTS`, la nota che nomina 52-15
    - `rls-baseline.mjs` porta `storage_path` fra le colonne della sonda
    - `database.ts` porta `storage_path: string | null` nel tipo di `event_media`
  </acceptance_criteria>
  <done>Chiave, costante, mappa, gate e colonna esistono insieme, in un commit, e nessuna parte di M1 chiude qualcosa.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Applicare M1 al LABORATORIO e rileggere dal catalogo</name>
  <files>supabase/migrations/20260923180000_gallery_view_and_media_paths.sql</files>
  <read_first>
    - `scripts/purge-attendances.mjs` righe 218-248 — `sql()` via Management API
    - `scripts/seed-lab-door.mjs` righe 70-92 — il rifiuto del ref di produzione come prima riga eseguita, per ogni script scratch che si scrive per applicare
    - `scripts/dev-lab.sh` — `.env.local` poi `.env.lab.local`
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-08-SUMMARY.md` — come e' stata registrata un'applicazione al laboratorio (versione coniata, riletture, ora UTC)
  </read_first>
  <action>
    Accertare il laboratorio `ACTIVE_HEALTHY`. Applicare il file con `POST
    /v1/projects/$LAB_PROJECT_REF/database/migrations` (nome = il nome del file
    senza timestamp, corpo = il file). Se si scrive uno script scratch per farlo,
    la sua prima riga eseguita rifiuta il ref di produzione. **Mai
    `supabase db push`, mai `/database/query` per applicare.** La produzione e'
    fuori dal perimetro di questo piano: la tocca il 52-15, sotto atto datato.
    Rileggere **dal catalogo**, `read_only`, e scrivere nel SUMMARY con ora UTC:
    (a) la versione coniata in `supabase_migrations.schema_migrations`;
    (b) `count(*)` di `private.capabilities` = 16 e la riga `gallery.view`;
    (c) concessioni per ruolo: master 16, organizer 14, staff 1, attendee 0 —
    totale 31;
    (d) `obj_description('private.role_capabilities'::regclass)` contiene «1 a
    staff»; `obj_description('private.capabilities'::regclass)` contiene «16»;
    (e) `information_schema.columns` per `event_media`: `storage_path` presente e
    `is_nullable = YES`, `url` `is_nullable = YES`; righe con `storage_path IS
    NULL` = 0; il `CHECK` e l'indice presenti (`pg_constraint`, `pg_indexes`);
    (f) `pg_policies` su `storage.objects`: `event_media_objects_select_by_row`
    presente, `roles = {authenticated}`, `cmd = SELECT`; «Anyone can view event
    media» **ancora presente**; `storage.buckets.public` per `event-media`
    **ancora true**.
    Poi lanciare `npm run verify:capabilities` **contro il laboratorio**
    (caricando `.env.local` e poi `.env.lab.local` come `scripts/dev-lab.sh`):
    verde. Dichiarare nel SUMMARY che contro la **produzione** resta rosso fino al
    piano 52-15, che lo chiude.
    Se una sola rilettura non corrisponde: **fermarsi** e riportarlo.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; set -a &amp;&amp; . ./.env.local &amp;&amp; . ./.env.lab.local &amp;&amp; set +a &amp;&amp; npm run verify:capabilities</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:capabilities` contro il laboratorio esce 0 (16 chiavi, 31 concessioni)
    - Il SUMMARY riporta le riletture (a)-(f) prese dal catalogo, con l'ora UTC e la versione coniata
    - Sul laboratorio: `storage_path` nullable con zero righe nulle; bucket ancora pubblico; «Anyone can view event media» ancora presente (M1 non chiude nulla)
    - Zero scritture sul progetto di produzione, dichiarato nel SUMMARY; lo stato rosso di `verify:capabilities` contro la produzione e' dichiarato con il piano che lo chiude (52-15)
  </acceptance_criteria>
  <done>Il laboratorio conosce `gallery.view` e `storage_path`, e lo dice il catalogo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogo delle capability → RLS | una concessione e' il modello d'accesso; da M2 `gallery.view` governa righe |
| client della sessione → storage.objects | la firma passa dalla SELECT sull'oggetto; da M1 esiste un braccio che chiede la riga |
| chi esegue → laboratorio | l'unico progetto che questo piano scrive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-20 | Elevation of privilege | `gallery.view` concessa al ruolo sbagliato | mitigate | tre concessioni esplicite; il `DO` solleva se ≠ 3, se il totale ≠ 31 o se `attendee` ne ha una; `verify:capabilities` confronta in entrambe le direzioni |
| T-52-21 | Information disclosure | firma di oggetti pending/rifiutati da chi indovina il path | mitigate | la policy sull'oggetto chiede la **riga** (`EXISTS` sotto la RLS di chi firma), non la sola chiave; la sonda 4 del piano 52-13 lo prova |
| T-52-22 | Denial of Service | caricamento rotto fra M1 e deploy | mitigate | `storage_path` nullable e `url` senza `NOT NULL` in M1; `NOT NULL` solo in M2 dopo il ribackfill (correzione dichiarata alla ricerca) |
| T-52-23 | Tampering | backfill che indovina una riga di forma diversa | mitigate | il `DO` solleva se resta una `storage_path` nulla; il censimento del piano 52-01 le ha contate prima |
| T-52-24 | Elevation of privilege | una INSERT per `authenticated` sul bucket reintrodotta | mitigate | M1 crea solo una SELECT; `npm run verify:media-strip` (controllo D) nel comando di verifica |
| T-52-25 | Tampering | scrittura accidentale in produzione | mitigate | solo `$LAB_PROJECT_REF`; ogni script scratch rifiuta il ref di produzione; produzione nel piano 52-15 sotto atto |
| T-52-26 | Repudiation | gate rosso senza chi lo chiude | mitigate | la nota in `verify-capabilities.mjs` e il SUMMARY nominano 52-15 |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `npm run build`, `verify:media-strip` verdi; `verify:capabilities` verde contro il laboratorio
- Catalogo del laboratorio: 16 chiavi, 31 concessioni (16/14/1/0), `storage_path` nullable con zero nulle, policy `EXISTS` presente, bucket ancora pubblico
</verification>

<success_criteria>
M1 esiste, e' additiva per costruzione, e il laboratorio la porta — riletta dal
catalogo, non dalla risposta del POST.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-06-SUMMARY.md` when done
</output>
