---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/scanner/ScannerClient.tsx
  - scripts/verify-touch-targets.mjs
  - src/app/layout.tsx
  - src/app/(public)/events/EventTabs.tsx
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Alla porta ci sono cinque linguette in una riga — All · Out · In · Recent · Alerts — sempre presenti anche vuote, con il numero accanto senza parentesi e Alerts senza numero a zero; le posizioni non cambiano mai (D-52-19, D-52-26)"
    - "Un avviso nuovo accende un pallino sulla linguetta Alerts e ne alza il numero, ma activeFilter non cambia mai da solo (D-52-20)"
    - "In testata restano sempre visibili le pastiglie Offline / Pending / Sign in again e l'avviso della guest list a radio spenta, spostato SOPRA la ricerca; in Alerts vanno la banda d'eta', l'esito del drenaggio, cacheNotices e la deriva dell'orologio (D-52-21)"
    - "L'annullamento resta sulla riga della cronologia dentro Recent, con la stessa conferma e lo stesso ramo di supervisione (D-52-22)"
    - "Ricerca → linguette → lista sono contigui; al fuoco la ricerca sale sotto la barra fissa, senza leggere il viewport (D-52-28)"
    - "Il meta viewport porta interactive-widget=resizes-content con il commento che dichiara Safari iOS assente; l'unico vh rimasto diventa dvh (D-52-23, D-52-28)"
    - "DOOR_TARGET_DEBT scende da 14 a 13: le linguette dichiarano min-h-11 e la loro riga esce nello stesso commit; nessun quindicesimo"
  artifacts:
    - path: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      provides: "FilterTab a cinque valori, Recent e Alerts, seenAlertKeys, avviso guest list sopra la ricerca"
      contains: "\"alerts\""
    - path: "scripts/verify-touch-targets.mjs"
      provides: "il debito della porta a 13"
    - path: "src/app/layout.tsx"
      provides: "interactiveWidget nel viewport"
      contains: "interactiveWidget"
  key_links:
    - from: "ScannerClient.tsx (bottone linguetta)"
      to: "verify-touch-targets.mjs DOOR_TARGET_DEBT"
      via: "il frammento setActiveFilter(tab.key), che esce dal debito nello stesso commit in cui dichiara min-h-11"
      pattern: "setActiveFilter\\(tab.key\\)"
---

<objective>
La porta: cronologia e avvisi in due linguette proprie, la ricerca contigua alla
lista, e il viewport che dichiara cosa l'iPhone non fara'.

Purpose: i due todo della fase 51 (`door-tabs-recent-scans-and-alerts.md`,
`door-and-login-viewport-crops-under-keyboard.md`), entrati come D-52-19..23,
D-52-26, D-52-28. Nessun ID NAV proprio: stanno sotto NAV-01 perche' D-52-06 e
D-52-23 (la barra e la tastiera) sono la stessa misura vista dalla porta.
**Critical** (`checkin-offline.md`): rifiutare un ospite valido e' peggio che
ammetterne uno doppio, perche' il primo errore avviene davanti a una fila.
Nulla di questo piano tocca la coda, la decisione o la sincronizzazione: sposta
**dove** si leggono le cose, e ogni spostamento va misurato contro la domanda
«lo staff vede ancora l'istruzione su chi far entrare?».
Output: `ScannerClient.tsx` a cinque linguette, il debito della porta a 13, il
viewport, `60dvh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@.planning/todos/pending/door-tabs-recent-scans-and-alerts.md
@.planning/todos/pending/door-and-login-viewport-crops-under-keyboard.md
@./CLAUDE.md
@.claude/rules/checkin-offline.md
@.claude/rules/nextjs-architecture.md
@.claude/rules/meta-gates.md

<interfaces>
`src/app/(admin)/admin/scanner/ScannerClient.tsx` (misurato 2026-09-23):
- `:376` `guestListWarningText(…)`, `:404` `stalenessBandText(channelIsLive, ageMs)`
- `:415` `type FilterTab = "all" | "not_arrived" | "checked_in";` — default `"not_arrived"` a `:631`
- `:634` `const searchRef = useRef<HTMLInputElement>(null);`
- `:2755-2759` `FILTER_TABS: { key: FilterTab; label: string; count: number }[]` costruito nel render
- `:2881` `driftMinutes`; `:2928-3027` barra fissa di 51-07 (NON allungarla, docblock `:2888-2927`)
- `:3039-3054` «This night is over» + «Scan anyway» — resta fuori dalle linguette
- `:3058-3067` deriva dell'orologio → Alerts
- `:3080-3130` pastiglie della coda → testata; `:3132-3134` `blockedResult` → Alerts; `:3139-3165` `failedEntries` resta attaccato alla sua pastiglia
- `:3231-3273` ricerca (`ref={searchRef}` a `:3246`); `:3276-3291` linguette (`onClick={() => setActiveFilter(tab.key)}`)
- `:3308-3316` `cameraFault` resta sopra la camera
- `:3351-3365` banda d'eta' (`requestReload("band")`) → Alerts; `:3395-3403` avviso guest list → testata SOPRA la ricerca
- `:3405-3420` `cacheNotices` → Alerts (la banda NON entra in `cacheNotices`: `:3318-3337`)
- `:3458-3594` cronologia («Recent scans» a `:3461`, `handleUndoCheckIn(record)`) → Recent
- `:3597-3669` lista ospiti e testi vuoti
`scripts/verify-touch-targets.mjs`: `DOOR_TARGET_DEBT_CEILING.count = 14` (`:1302-1307`, NON si edita); la riga `[SCANNER_CLIENT, 'button', 'setActiveFilter(tab.key)', …]` a `:1414-1418` esce. Frammenti che devono restare identici e unici: `ref={searchRef}`, `setSearchQuery("")`, `requestReload("band")`, `handleUndoCheckIn(record)`, `handleRetryBlocked`.
`verify:scan-legibility` legge `CONNECTIVITY_PILL` e le tinte della pastiglia offline (`:523`): non spostarle ne' rinominarle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cinque linguette, Recent e Alerts — e il debito da 14 a 13 nello stesso commit</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx, scripts/verify-touch-targets.mjs</files>
  <read_first>
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx` righe 370-420, 625-640, 2690-2770, 2880-2935, 3030-3170, 3225-3300, 3315-3470, 3590-3670 — ogni blocco che si sposta, letto prima di spostarlo
    - `scripts/verify-touch-targets.mjs` righe 780-800 e 1280-1460 — come si risolve un frammento, cosa succede se ne risolvono zero o due, il tetto che non si edita
    - `scripts/verify-scan-legibility.mjs` righe 150-170
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §D.1-D.4 — geometria, stati, cosa sta dove, testi vuoti
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §G (righe 603-660)
  </read_first>
  <action>
    (1) Tipo: `type FilterTab = "all" | "not_arrived" | "checked_in" | "recent" |
    "alerts";` — default `"not_arrived"` invariato; `filteredAttendees` invariato.
    (2) `FILTER_TABS` a cinque voci con **etichette corte** (D-52-26): `All`,
    `Out`, `In`, `Recent`, `Alerts`; ogni voce porta anche `ariaLabel` col nome
    intero e il numero (`All guests, n`, `Not arrived, n`, `Checked in, n`,
    `Recent scans, n`, `Alerts, n` piu' `, new` quando il pallino e' acceso).
    Recent conta `scanHistory.length` (gia' limitata a 5). Alerts conta un array
    **derivato al render** di chiavi: `"band"` se la lista e' vecchia (la
    condizione che oggi mostra la banda), ogni `cacheNotices[i].key`, `"drain"` se
    `blockedResult`, `"drift"` se `driftMinutes !== null`. La banda **non** entra
    in `cacheNotices`.
    (3) Evidenziare senza aprire (D-52-20): stato `seenAlertKeys` (Set di
    stringhe); quando `activeFilter` diventa `"alerts"` le chiavi correnti
    diventano viste; il pallino e' acceso se esiste una chiave corrente non vista.
    **Nessun codice cambia `activeFilter` al di fuori del tocco sulla
    linguetta.** Commento: D-52-20, chi cerca un nome non perde la lista.
    (4) Il gruppo: `grid grid-cols-5 overflow-hidden rounded-xl bg-surface`,
    `role="group" aria-label="Guest list view"`, senza `p-1` ne' `gap-1`. Il
    bottone: `relative flex min-h-11 flex-wrap items-center justify-center gap-x-1
    px-1 text-xs font-semibold` + `FOCUS_RING` (gia' importato nel file? se no,
    importarlo da `@/components/ui/Button`), `aria-pressed`, `aria-label`,
    l'espressione `onClick={() => setActiveFilter(tab.key)}` **identica e unica**
    nel sorgente. Stati come UI-SPEC §D.2 (attiva `bg-raised text-ink` +
    indicatore `absolute inset-x-2 bottom-0 h-0.5 rounded-full bg-accent`;
    conteggio `font-mono`; pallino `h-2 w-2 rounded-full bg-sem-warn` prima
    dell'etichetta, senza `animate-pulse`). Commento: numero senza parentesi per
    D-52-26, che supera la forma «Recent (12)» di D-52-19.
    (5) Sotto le linguette il render sceglie per `activeFilter`: `all` /
    `not_arrived` / `checked_in` → la lista di oggi; `recent` → la cronologia
    (l'intestazione «Recent scans» `text-[10px]` sparisce, righe e
    `handleUndoCheckIn(record)` identici, conferma e supervisione invariate) o,
    vuota, titolo `No scans yet` e corpo `The last five check-ins appear here. Tap
    one to undo it.`; `alerts` → in quest'ordine banda d'eta' (con
    `requestReload("band")` identico), `blockedResult`, `cacheNotices`, deriva
    dell'orologio, `space-y-2`, o, vuota, titolo `Nothing to report` e corpo `List
    and queue warnings appear here and light up this tab.` Stati vuoti `px-6 py-8
    text-center`, titolo `text-base font-semibold text-ink`, corpo `text-sm
    text-muted`, **nessun pulsante**.
    (6) Restano dove sono: barra fissa (non si allunga), «This night is over» +
    «Scan anyway», pastiglie e `failedEntries`, `cameraFault`, camera,
    `CONNECTIVITY_PILL`.
    (7) In `verify-touch-targets.mjs`: togliere la voce `setActiveFilter(tab.key)`
    da `DOOR_TARGET_DEBT` **nello stesso commit**, e aggiornare la prosa che conta
    «fourteen» dove descrive la lista corrente (non il tetto, che resta 14 con la
    sua data: il debito scende sotto il tetto liberamente). Una riga di storia
    datata accanto: pagato il 2026-09-23, fase 52, le linguette sono cinque e
    dichiarano `min-h-11`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:touch-targets &amp;&amp; npm run verify:scan-legibility &amp;&amp; npm run verify:no-viewport-read &amp;&amp; test $(/usr/bin/grep -c "setActiveFilter(tab.key)" "src/app/(admin)/admin/scanner/ScannerClient.tsx") -eq 1 &amp;&amp; test $(/usr/bin/grep -c "handleUndoCheckIn(record)" "src/app/(admin)/admin/scanner/ScannerClient.tsx") -eq 1 &amp;&amp; /usr/bin/grep -q "No scans yet" "src/app/(admin)/admin/scanner/ScannerClient.tsx" &amp;&amp; /usr/bin/grep -q "Nothing to report" "src/app/(admin)/admin/scanner/ScannerClient.tsx" &amp;&amp; test $(/usr/bin/grep -c "'setActiveFilter(tab.key)'" scripts/verify-touch-targets.mjs) -eq 0 &amp;&amp; /usr/bin/grep -q "count: 14" scripts/verify-touch-targets.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `verify:touch-targets`, `verify:scan-legibility`, `verify:no-viewport-read` escono 0
    - `FilterTab` ha cinque valori; le etichette visibili sono `All`, `Out`, `In`, `Recent`, `Alerts`
    - `setActiveFilter(tab.key)` e `handleUndoCheckIn(record)` compaiono una volta ciascuno nel sorgente
    - Il bottone della linguetta contiene `min-h-11` letterale; la voce `setActiveFilter(tab.key)` non e' piu' in `DOOR_TARGET_DEBT`; `DOOR_TARGET_DEBT_CEILING.count` resta `14`
    - I testi `No scans yet` e `Nothing to report` sono nel sorgente; gli stati vuoti non contengono `<button`
    - Nessuna riga assegna `setActiveFilter("alerts")` fuori dall'`onClick` della linguetta (grep: zero)
    - La barra fissa di 51-07 ha lo stesso numero di righe di markup di prima (dichiarato nel SUMMARY con il diff)
  </acceptance_criteria>
  <done>La porta ha cinque linguette fisse, gli avvisi si segnalano senza strappare la lista, e il debito dei bersagli e' sceso di uno.</done>
</task>

<task type="auto">
  <name>Task 2: L'avviso della guest list sopra la ricerca, e la ricerca che sale al fuoco</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx` righe 370-400 (`guestListWarningText`, decisione WR-04 del 2026-09-23), 2885-2935 (barra fissa e la sua altezza), 3225-3275 (ricerca), 3380-3405 (avviso)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §H.3 (righe 682-699) e assunzione A3
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §D.3 e §E «La ricerca della porta al fuoco»
    - `scripts/verify-no-viewport-read.mjs` righe 175-190
  </read_first>
  <action>
    (1) Spostare il blocco dell'avviso della guest list a radio spenta
    (`guestListWarningText(listAgeMs, channelLive)` e il suo contenitore) **sopra**
    la ricerca, con le pastiglie; testo, condizione e forma **invariati parola per
    parola** (WR-04). Commento: D-52-21 — e' un'istruzione su chi far entrare, non
    una notizia, quindi sta in testata e non in Alerts; e D-52-28 — fra ricerca e
    lista non deve stare nulla.
    (2) Al campo di ricerca: `onFocus` che, **dopo** l'evento di fuoco (in un
    `requestAnimationFrame` o `setTimeout(…, 0)`, perche' su iOS la tastiera sta
    ancora salendo — assunzione A3), chiama `searchRef.current?.scrollIntoView({
    block: "start" })`; il campo porta una classe `scroll-mt-*` pari
    all'altezza della barra fissa di 51-07, **misurata** dal markup (padding e
    righe della barra) e scritta come gradino della scala Tailwind con la misura
    nel commento. Nessuna lettura del viewport (`matchMedia`, `innerWidth`,
    `visualViewport`, `useSyncExternalStore`): vietate da
    `verify:no-viewport-read` e dallo spirito di 41-UI-SPEC §0.6.
    `ref={searchRef}` resta **identico e unico** (e' pinnato dal debito).
    (3) Verificare nel render che l'ordine sia: pastiglie + avviso guest list →
    contatore → ricerca → linguette → contenuto della linguetta; `cameraFault` e
    camera restano dove sono oggi e il SUMMARY dichiara la loro posizione rispetto
    alla ricerca (se stanno fra ricerca e lista, dirlo: la contiguita' e' la misura
    di P-52-F).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:touch-targets &amp;&amp; npm run verify:no-viewport-read &amp;&amp; test $(/usr/bin/grep -c "ref={searchRef}" "src/app/(admin)/admin/scanner/ScannerClient.tsx") -eq 1 &amp;&amp; /usr/bin/grep -q "scrollIntoView" "src/app/(admin)/admin/scanner/ScannerClient.tsx" &amp;&amp; test $(/usr/bin/grep -c "visualViewport\|matchMedia\|innerWidth" "src/app/(admin)/admin/scanner/ScannerClient.tsx") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `verify:touch-targets`, `verify:no-viewport-read` escono 0
    - Nel sorgente il blocco di `guestListWarningText(` precede l'input con `ref={searchRef}` (numero di riga minore)
    - Il testo dell'avviso guest list e' identico a prima (`git diff` non tocca la funzione `guestListWarningText`)
    - L'input di ricerca porta `onFocus` con `scrollIntoView({ block: "start" })` differito e una classe `scroll-mt-*` con la misura nel commento
    - Zero `visualViewport`, `matchMedia`, `innerWidth` nel file
  </acceptance_criteria>
  <done>Con la tastiera aperta, il nome cercato e il suo «Check in» stanno subito sotto il campo, e l'istruzione su chi far entrare resta sempre in vista.</done>
</task>

<task type="auto">
  <name>Task 3: Il viewport — interactive-widget con l'aspettativa scritta, e l'ultimo vh</name>
  <files>src/app/layout.tsx, src/app/(public)/events/EventTabs.tsx</files>
  <read_first>
    - `src/app/layout.tsx` righe 78-110 — `export const viewport`; il blocco sulla scala e' pinnato da un grep e **non si tocca ne' si cita per esteso**
    - `src/app/(public)/events/EventTabs.tsx` righe 575-590 — `minHeight: "60vh"`
    - `node_modules/next/dist/lib/metadata/types/extra-types.d.ts` riga 53 — `interactiveWidget?: 'resizes-visual' | 'resizes-content' | 'overlays-content'`
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §H.1-H.2 e Code Examples «Il viewport»
  </read_first>
  <action>
    In `layout.tsx`, dentro `viewport`, dopo `initialScale` e prima del commento
    sulla scala: `interactiveWidget: "resizes-content",` con un commento che dice:
    Android (Chrome 108+, Firefox 132+) ridimensiona il layout sotto la tastiera;
    **Safari iOS lo ignora** al 2026-09 (WebKit l'ha nel sorgente da agosto 2026,
    non in una release), quindi sull'iPhone valgono le misure di layout della
    porta, non questa riga — D-52-23, D-52-28, P-52-F. **Niente `viewportFit`**:
    con `cover` la barra fissa della porta e la striscia appesa finirebbero sotto
    la barra di stato in standalone. Non toccare ne' riformulare il blocco sulla
    scala.
    In `EventTabs.tsx`: le due `minHeight: "60vh"` diventano `"60dvh"`, con una
    riga di commento (D-52-23: l'ultimo `vh` del prodotto).
    `PageShell.tsx` **non si tocca** (digest in `verify:conversion`; il login si
    prova com'e', D-52-28).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:conversion &amp;&amp; /usr/bin/grep -q 'interactiveWidget: "resizes-content"' src/app/layout.tsx &amp;&amp; test $(/usr/bin/grep -c "viewportFit" src/app/layout.tsx) -eq 0 &amp;&amp; test $(/usr/bin/grep -c '"60vh"' "src/app/(public)/events/EventTabs.tsx") -eq 0 &amp;&amp; test -z "$(git diff --name-only -- src/components/ui/PageShell.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify:conversion` escono 0
    - `layout.tsx` contiene `interactiveWidget: "resizes-content"` con il commento che nomina Safari iOS assente; zero `viewportFit`
    - `EventTabs.tsx` non contiene piu' `"60vh"`
    - `PageShell.tsx` non e' modificato
  </acceptance_criteria>
  <done>Il viewport dice cosa aiuta e dove non aiuta, e il prodotto non ha piu' un'altezza in `vh`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff alla porta → schermo | l'operatore decide chi entra leggendo cio' che la porta mostra, spesso offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-14 | Denial of Service (operativo) | avviso guest list nascosto in una linguetta → ospite valido rifiutato alla fila | mitigate | l'avviso «do not refuse them… let them in» resta in testata, sempre visibile, spostato sopra la ricerca; testo invariato (WR-04). Costo di un rifiuto falso: un ospite con diritto mandato via davanti alla fila — il peggiore dei due errori |
| T-52-15 | Repudiation | avviso di lista vecchia o di coda non letto | mitigate | pallino + numero + `, new` nel nome accessibile; pastiglie Pending/Sign in again sempre in testata |
| T-52-16 | Denial of Service | la linguetta che si apre da sola mentre lo staff cerca un nome | mitigate | `activeFilter` cambia solo al tocco (D-52-20), verificato da grep |
| T-52-17 | Tampering | annullamento reso piu' facile o piu' nascosto | mitigate | `handleUndoCheckIn(record)` identico, conferma e ramo di supervisione invariati (D-52-22) |
| T-52-18 | Denial of Service | bersaglio mancato che seleziona la linguetta vicina | mitigate | linguette a `min-h-11`, debito 14 → 13 nello stesso commit |
| T-52-19 | Tampering | coda offline o decisione toccate per sbaglio | mitigate | nessuna modifica a `src/lib/offline/**`, al drenaggio o alla decisione; il SUMMARY elenca le funzioni non toccate |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `npm run build`, `verify:touch-targets` (debito 13, tetto 14), `verify:scan-legibility`, `verify:no-viewport-read`, `verify:conversion` verdi
- P-52-E e P-52-F sul laboratorio, su iPhone (piano 52-14)
</verification>

<success_criteria>
La porta ha la stessa forma dall'apertura alla chiusura, l'istruzione su chi far
entrare non si sposta mai fuori vista, e l'aspettativa sul viewport iOS e'
scritta nel codice oltre che nella procedura.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-05-SUMMARY.md` when done
</output>
