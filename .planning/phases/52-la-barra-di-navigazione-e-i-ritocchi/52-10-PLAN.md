---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 10
type: execute
wave: 3
depends_on: ["52-06", "52-01"]
files_modified:
  - scripts/lib/server-only-shim.mjs
  - scripts/restrip-event-media.mjs
autonomous: true
requirements: [NAV-07]

must_haves:
  truths:
    - "Esiste uno strumento che ripassa dallo stripper TUTTE le foto caricate prima dello stripper — stesso path, stessa riga — usando lo stripper del prodotto (src/lib/media/strip-metadata.ts) e mai una seconda copia della chiamata a sharp (D-52-31, P4)"
    - "Le tre scelte di P4 sono dichiarate nel codice: (1) lo stripper si importa via type stripping nativo di Node con server-only risolto a un modulo vuoto; (2) la riscrittura e' upsert: true sullo stesso path, fuori dal principio di finalize.ts, con la ragione; (3) lo script e' un secondo scrittore su event-media che verify:media-strip non vede, e lo dice"
    - "I video non si spogliano: si contano a parte, mai saltati in silenzio"
    - "Prima di sovrascrivere si prende un'istantanea dei byte originali in un percorso .env* locale, perche' la sovrascrittura e' irreversibile; la sua cancellazione e' un passo datato del piano 52-17"
    - "Sul laboratorio la foto pre-stripper con GPS del banco esce senza EXIF, e la riga e il path sono gli stessi"
  artifacts:
    - path: "scripts/lib/server-only-shim.mjs"
      provides: "hook di risoluzione che mappa 'server-only' a next/dist/compiled/server-only/empty.js"
      contains: "server-only"
    - path: "scripts/restrip-event-media.mjs"
      provides: "ri-spogliatura per chiave, laboratorio per default, produzione sotto atto consumato"
      contains: "restrip-event-media: grant"
      min_lines: 300
  key_links:
    - from: "scripts/restrip-event-media.mjs"
      to: "src/lib/media/strip-metadata.ts stripImageMetadata"
      via: "import dinamico del .ts sotto il hook di scripts/lib/server-only-shim.mjs"
      pattern: "strip-metadata.ts"
    - from: "scripts/restrip-event-media.mjs"
      to: "52-AUTHORISATION-MEDIA.md (piano 52-16)"
      via: "blocco <!-- restrip-event-media: grant --> letto e marcato solo al suo interno"
      pattern: "restrip-event-media: grant"
---

<objective>
Scrivere lo strumento di D-52-31 — ogni foto precedente allo stripper ripassa
dallo stripper, stesso path e stessa riga — e provarlo sul laboratorio con una
foto che porta GPS davvero.

Purpose: le righe caricate prima di `20260809006000_event_media_server_upload_only.sql`
non sono passate dallo stripper e possono portare coordinate (`media-and-storage.md`,
*EXIF prima della pubblicazione*; `venue-secrecy.md`, irreversibilita'). M2 le
rendera' visibili solo a chi tiene `gallery.view`; il proprietario ha deciso di
ripulirle tutte, in questa fase. PATTERNS P4 pone tre nodi che il piano **decide**
qui, e il codice li dichiara:
1. **Lo stripper e' TypeScript `server-only`.** Scelta: lo si importa **cosi'
   com'e'** — Node 25 esegue `.ts` con il type stripping nativo, e un hook di
   risoluzione (`module.register`) mappa lo specificatore `server-only` al modulo
   vuoto che Next stesso usa (`next/dist/compiled/server-only/empty.js`).
   Scartate: riscrivere la chiamata a `sharp` nello script (e' «la seconda copia
   di un ordine», il modo in cui si smette di spogliare, `finalize.ts:28-37`); una
   rotta interna che chiama `finalizeStrippedUpload` (un endpoint nuovo in
   produzione per un'operazione una-tantum: superficie d'attacco senza ragione).
   Se lo stripper non si carica sotto type stripping, **ci si ferma** e si
   riporta: non si ripiega sulla copia.
2. **«Stesso path» = `upsert: true`**, che `finalize.ts:339-341` esclude per
   principio. Scelta dichiarata: la sovrascrittura e' voluta (D-52-31, «stesso
   path, stessa riga»), e l'irreversibilita' si paga con un'istantanea dei byte
   originali presa **prima**.
3. **Secondo scrittore su `event-media` fuori da `src/`**, invisibile al
   controllo A di `verify:media-strip` (`verify-media-strip.mjs:6-8`). Dichiarato
   nel docblock dello script con la ragione, e nel VERIFICATION (piano 52-17).
Output: `scripts/lib/server-only-shim.mjs`, `scripts/restrip-event-media.mjs`, la
prova sul laboratorio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
@./CLAUDE.md
@.claude/rules/media-and-storage.md
@.claude/rules/venue-secrecy.md
@.claude/rules/legal-compliance.md
@.claude/rules/ai-engineering.md

<interfaces>
`src/lib/media/strip-metadata.ts`: riga 1 `import "server-only";`, riga 2
`import type { Sharp } from "sharp";` (cancellato dal type stripping), `sharp`
caricato con `await import("sharp")` (`:270`); `STRIPPABLE_MIME_TYPES =
["image/jpeg","image/png","image/webp"] as const` (`:105-109`); `class
MediaStripRefusal extends Error` con `constructor(reason: …)` (parametro
normale, sintassi cancellabile); `export async function stripImageMetadata(input:
Buffer, mime: string): Promise<Buffer>` (`:246`) — lancia `MediaStripRefusal` su
ogni altro esito, mai i byte ricevuti.
`src/lib/media/finalize.ts:245-375`: la sequenza scarica (service role, 404/400
«non c'e'» distinto da «non risponde», `:259-281`) → spoglia → scrivi con
`contentType` esplicito (`:343-352`, senza un Buffer diventa `text/plain`); log
`code=… message=…` e mai la chiave (`:69-79`).
`node --version` = v25.6.1 (`process.features.typescript = "strip"`).
`next/dist/compiled/server-only/empty.js` esiste in `node_modules`; il pacchetto
`server-only` NON e' installato alla radice.
Soglia «prima dello stripper»: la versione di `20260809006000` nella history di
ciascun progetto (censimento, `52-ESITI.md`) — passata esplicita con `--before`.
Blocco di concessione: stessa disciplina del piano 52-09, campi letti e marcati
solo fra `<!-- restrip-event-media: grant -->` e `<!-- /restrip-event-media: grant -->`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Il hook per server-only, e la prova che lo stripper del prodotto si carica</name>
  <files>scripts/lib/server-only-shim.mjs</files>
  <read_first>
    - `src/lib/media/strip-metadata.ts` righe 1-30, 160-190, 220-280
    - `node_modules/next/dist/compiled/server-only/` — `index.js` (che lancia) ed `empty.js`
    - `scripts/lib/comments.mjs` — la forma di un modulo di supporto in `scripts/lib/`
  </read_first>
  <action>
    Creare `scripts/lib/server-only-shim.mjs`: chiama `register()` di `node:module`
    con un hook di risoluzione (inline via `data:` URL o in un secondo file nello
    stesso modulo) che, **solo** per lo specificatore esatto `server-only`,
    restituisce l'URL assoluto di `node_modules/next/dist/compiled/server-only/empty.js`
    e per ogni altro specificatore delega a `nextResolve`. Docblock: perche' esiste
    (P4, nodo 1), perche' non si installa il pacchetto `server-only` (cambierebbe
    `package.json` per uno script una-tantum), e che il hook **non** e' un modo di
    importare codice server in un client: vale solo nei processi Node che lo
    importano esplicitamente come prima riga.
    Provare subito che lo stripper si carica: un comando
    `node --input-type=module -e` che importa `./scripts/lib/server-only-shim.mjs`,
    poi `./src/lib/media/strip-metadata.ts`, e stampa `typeof
    stripImageMetadata` e `STRIPPABLE_MIME_TYPES`; poi genera con `sharp` un JPEG
    con EXIF, lo passa a `stripImageMetadata(buf, "image/jpeg")` e verifica che
    `sharp(out).metadata()` non abbia `exif`. Se il caricamento fallisce (sintassi
    non cancellabile, import con alias `@/`, qualunque cosa), **fermarsi e
    riportare** la causa: non si scrive il task 2 su una copia della chiamata a
    `sharp`.
  </action>
  <verify>
    <automated>node --check scripts/lib/server-only-shim.mjs &amp;&amp; node --input-type=module -e "await import('./scripts/lib/server-only-shim.mjs'); const m = await import('./src/lib/media/strip-metadata.ts'); if (typeof m.stripImageMetadata !== 'function') process.exit(1); const sharp=(await import('sharp')).default; const src=await sharp({create:{width:8,height:8,channels:3,background:'#000'}}).jpeg().withExif({IFD0:{Copyright:'probe'}}).toBuffer(); if(!(await sharp(src).metadata()).exif) process.exit(3); const out=await m.stripImageMetadata(src,'image/jpeg'); process.exit((await sharp(out).metadata()).exif ? 2 : 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/lib/server-only-shim.mjs` esce 0
    - Il comando di verifica esce 0: lo stripper del prodotto si carica sotto il hook, e un JPEG con EXIF ne esce senza
    - Il hook tocca **solo** lo specificatore `server-only` (letto nel sorgente)
    - `package.json` non cambia
  </acceptance_criteria>
  <done>Lo script potra' chiamare lo stripper vero, non una sua imitazione.</done>
</task>

<task type="auto">
  <name>Task 2: Scrivere scripts/restrip-event-media.mjs</name>
  <files>scripts/restrip-event-media.mjs</files>
  <read_first>
    - `scripts/purge-attendances.mjs` per intero — guardia, atto consumato, istantanea, `fail(category, …)`
    - `scripts/purge-media-orphans.mjs` (piano 52-09) — il blocco di concessione letto solo al suo interno
    - `src/lib/media/finalize.ts` righe 20-80 e 245-375 — la sequenza e le sue categorie
    - `scripts/verify-media-strip.mjs` righe 1-60 — il perimetro del controllo A, da citare nel docblock
  </read_first>
  <action>
    Creare `scripts/restrip-event-media.mjs`. **Prima riga eseguita**:
    `await import("./lib/server-only-shim.mjs")`; poi gli argomenti; poi il
    rifiuto (struttura di `purge-attendances.mjs`: laboratorio per default,
    produzione solo con `--authorised` + `--dated` su un blocco
    `<!-- restrip-event-media: grant -->` … `<!-- /restrip-event-media: grant -->`
    con, al suo interno, `act: event_media.restrip`, `target: production`,
    `granted: yes`, `granted_on`, `spent: no`, `before: <istante ISO della
    soglia>`, `photos: <n>`, `videos: <n>`).
    Docblock: D-52-31; le **tre scelte di P4** scritte per esteso (hook e type
    stripping invece di una copia; `upsert: true` voluto e perche'; secondo
    scrittore su `event-media` fuori da `src/`, invisibile al controllo A di
    `verify:media-strip`, con la ragione e il rimando al VERIFICATION); la cache
    (`media-and-storage.md`, *cache e contenuto rimosso*): la CDN puo' servire la
    versione con GPS fino al suo `max-age`, e il bucket reso privato da M2 non
    cambia le copie gia' in cache — il residuo si scrive, non si tace.
    Argomenti: `--dry-run` | `--apply`, `--project`, `--authorised`, `--dated`,
    `--before <ISO>` (obbligatorio: la soglia letta dal catalogo di quel progetto;
    in produzione deve coincidere con `before` del blocco, o exit 2).
    Sequenza: (a) cattura, `read_only`, delle righe `event_media` con
    `created_at < before` e `storage_path` non nullo: `id`, `storage_path`,
    `type`; conteggio foto e video; zero foto → lo dice ed esce 0; in produzione,
    scarto dai numeri del blocco → exit 1 `count_drift`; (b) **istantanea dei byte
    originali** di ogni foto catturata in `.env.restrip-snapshot.<stamp>/` (una
    directory `.env*`, coperta da `.gitignore`) con un indice JSON di soli
    metadati — ragione: la sovrascrittura e' irreversibile, e questa e' l'unica
    via di ritorno; la sua cancellazione e' un passo datato del piano 52-17,
    dopo la conferma del VERIFICATION; (c) `--dry-run` si ferma qui; (d) per ogni
    foto, nell'ordine di `finalize.ts`: scarica col service role (404/400
    `object_missing` distinto da `download_failed`), `mime` dall'estensione
    **verificata** contro il contenitore reale (lo stripper lo ricontrolla),
    `stripImageMetadata(bytes, mime)` (un `MediaStripRefusal` → categoria
    `strip_refused`, la foto resta com'era e si conta), `upload(storage_path,
    stripped, { contentType: mime, upsert: true })` sullo **stesso path**;
    rilettura dell'oggetto scritto e `sharp(...).metadata()` senza `exif` →
    altrimenti `still_has_exif`; la riga **non si tocca**; (e) i video: contati in
    `videos_skipped`, mai toccati, mai taciuti; (f) referto: foto riscritte,
    rifiuti per categoria, video non trattati, ora UTC; (g) conferma da fonte
    diversa: un campione (tutte se ≤ 20) riletto via URL firmato generato col
    service role — non via la chiamata di download che ha scritto — senza `exif`;
    (h) in produzione marca il blocco esaurito al suo interno.
    Log: `code=… message=…`, **mai** un path, un URL o un `id` sullo standard
    output; categorie `[restrip-event-media.<categoria>]`.
  </action>
  <verify>
    <automated>node --check scripts/restrip-event-media.mjs &amp;&amp; /usr/bin/grep -q "server-only-shim" scripts/restrip-event-media.mjs &amp;&amp; /usr/bin/grep -q "restrip-event-media: grant" scripts/restrip-event-media.mjs &amp;&amp; /usr/bin/grep -q "upsert: true" scripts/restrip-event-media.mjs &amp;&amp; /usr/bin/grep -q "stripImageMetadata" scripts/restrip-event-media.mjs &amp;&amp; test $(/usr/bin/grep -c "\.rotate(\|\.withMetadata(\|keepMetadata" scripts/restrip-event-media.mjs) -eq 0 &amp;&amp; SUPABASE_ACCESS_TOKEN=x node scripts/restrip-event-media.mjs --dry-run --project cjsfocnhfzycbbgkwocx --before 2026-08-09T00:00:00Z; test $? -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` esce 0; il ref di produzione senza atto → exit 2 prima di ogni lettura (provato)
    - Lo script importa lo shim come prima riga eseguita e chiama `stripImageMetadata` del prodotto; zero chiamate dirette a metodi di `sharp` che riscrivono l'immagine (`rotate`, `withMetadata`, `keepMetadata`)
    - Il docblock porta le tre scelte di P4 e la nota sulla cache
    - `upsert: true` sullo stesso `storage_path`; la riga di `event_media` non e' mai aggiornata dallo script
    - Istantanea dei byte in `.env.restrip-snapshot.<stamp>/` con indice di soli metadati
    - Video contati in `videos_skipped`; rifiuti dello stripper contati per categoria; nessun path/URL/UUID stampato
  </acceptance_criteria>
  <done>La ri-spogliatura usa lo stripper vero, dichiara cio' che scavalca, e tiene una via di ritorno.</done>
</task>

<task type="auto">
  <name>Task 3: Provarlo sul laboratorio con la foto che porta GPS davvero</name>
  <files>scripts/restrip-event-media.mjs</files>
  <read_first>
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-01-SUMMARY.md` — la foto pre-stripper del banco e la soglia del laboratorio
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md` §Censimento — la soglia del laboratorio
  </read_first>
  <action>
    Caricare `.env.local` e `.env.lab.local`; laboratorio `ACTIVE_HEALTHY`.
    Leggere, `read_only`, la soglia del laboratorio (versione di
    `20260809006000` convertita in istante). Lanciare `--dry-run --before
    <soglia>`: foto ≥ 1 (quella del banco), il resto contato. Prima dell'atto,
    rileggere la foto del banco e registrare che `metadata().exif` e' presente.
    `--apply`. Poi, a mano: la stessa riga (`id` invariato, `storage_path`
    invariato, conteggio righe invariato) e l'oggetto riletto **senza** `exif`;
    le foto caricate dopo la soglia dal percorso vero **non toccate** (le loro
    `updated_at`/`last_modified` in `storage.objects` invariate).
    Rilanciare `--dry-run`: le stesse foto sono ancora «pre-soglia» (lo sono per
    data), e il SUMMARY dichiara che lo script e' **idempotente nell'effetto**
    (ri-spogliare una foto senza EXIF la riscrive uguale) ma non nel conteggio —
    ragione per cui in produzione si esegue **una volta**, sotto atto.
    Scrivere nel SUMMARY: numeri, ore UTC, esito della prova GPS prima/dopo, nome
    della directory d'istantanea (non il contenuto), `git check-ignore` su di essa.
    Zero scritture in produzione.
  </action>
  <verify>
    <automated>node --check scripts/restrip-event-media.mjs &amp;&amp; ls -d .env.restrip-snapshot.* &gt;/dev/null &amp;&amp; git check-ignore -q $(ls -d .env.restrip-snapshot.* | head -1) &amp;&amp; test -z "$(git status --porcelain | /usr/bin/grep restrip-snapshot)"</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY dice: la foto del banco aveva `exif` prima e non ce l'ha dopo; stessa riga, stesso path
    - Le foto caricate dopo la soglia non sono state riscritte (verificato su `storage.objects`)
    - La directory d'istantanea esiste, e' ignorata da git, non compare in `git status`
    - Zero scritture in produzione, dichiarato
  </acceptance_criteria>
  <done>Sul laboratorio una foto con coordinate e' uscita dallo strumento senza, allo stesso indirizzo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → bucket `event-media` | un secondo scrittore, fuori dal perimetro di `verify:media-strip` |
| hook di risoluzione → modulo server | `server-only` risolto a vuoto solo in questo processo Node |
| chi esegue → produzione | sovrascrittura irreversibile, sotto atto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-45 | Information disclosure | foto riscritta ancora con GPS | mitigate | lo stripper del prodotto (non una copia); rilettura di ogni oggetto scritto senza `exif`; campione riletto da fonte diversa |
| T-52-46 | Tampering | foto distrutta da una sovrascrittura sbagliata | mitigate | istantanea dei byte prima dell'atto in `.env*` ignorato; il rifiuto dello stripper lascia la foto com'era |
| T-52-47 | Tampering | secondo scrittore non visto dai gate | mitigate | dichiarato nel docblock e nel VERIFICATION; eseguibile in produzione solo sotto atto consumato |
| T-52-48 | Elevation of privilege | lo shim usato per importare codice server nel client | accept | lo shim vale solo nei processi Node che lo importano come prima riga; nessun file di `src/` lo importa |
| T-52-49 | Information disclosure | copia locale di foto di persone | mitigate | istantanea necessaria per l'irreversibilita'; cancellazione datata nel piano 52-17 dopo la conferma |
| T-52-50 | Information disclosure | versione con GPS servita da cache | accept, dichiarata | CDN fino al `max-age`; residuo scritto nel VERIFICATION con i tempi |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` invariato |
</threat_model>

<verification>
- Lo stripper del prodotto si carica sotto il hook e spoglia (comando del task 1)
- Rifiuto della produzione senza atto (exit 2)
- Laboratorio: foto del banco senza EXIF, stessa riga e stesso path
</verification>

<success_criteria>
D-52-31 ha uno strumento che ripulisce con lo stripper vero, dichiara cio' che
scavalca, e in produzione girera' una volta sola.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-10-SUMMARY.md` when done
</output>
