---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/MemberTable.tsx
autonomous: true
requirements: [NAV-05, NAV-02]

must_haves:
  truths:
    - "Nella pagina membri la cifra staff non e' piu' un pulsante con filtro: e' uno span con la stessa forma delle altre due cifre (NAV-05, D-52-18)"
    - "La legenda visibile sotto la tabella non dice piu' che uno staff 'can do nothing an attendee cannot' ne' che la gallery viene dall'assegnazione: dice che uno staff tiene la gallery per ruolo e che la porta resta dell'assegnazione (D-52-12, Pitfall 5)"
    - "Il commento del badge staff e quello della cifra sono riscritti con la loro ragione e la data, non cancellati"
  artifacts:
    - path: "src/components/admin/MemberTable.tsx"
      provides: "tre cifre uguali e una legenda vera dal giorno della migration"
      contains: "gallery"
  key_links:
    - from: "src/components/admin/MemberTable.tsx (legenda)"
      to: "private.role_capabilities (staff → gallery.view, piano 52-06)"
      via: "la frase che si legge prima di promuovere qualcuno"
      pattern: "gallery"
---

<objective>
NAV-05, e la legenda che la concessione `gallery.view` renderebbe falsa.

Purpose: D-52-18 — la cifra `staff` si comporta come le altre. E la legenda
della pagina membri dice oggi *«A staff account grants nothing of its own … Working
the door or a gallery comes from the night's own assignment»*: dal giorno in cui
la migration del piano 52-06 concede `gallery.view` a `staff` **per ruolo**
(D-52-12), quella frase e' due volte sbagliata, e si legge **prima di promuovere
qualcuno** (Pitfall 5). La ricerca la voleva nello stesso piano della migration;
qui sta in un piano suo per non caricare il piano 52-06, ma **spedisce nello
stesso deploy** (atto del piano 52-15) — ed e' cio' che conta: nessun momento in
cui chi legge la legenda in produzione legge il falso.
Output: `MemberTable.tsx` con tre cifre uguali e una legenda vera.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@./CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/nextjs-architecture.md
@.claude/rules/community-membership.md

<interfaces>
Oggi in `src/components/admin/MemberTable.tsx`:
- le due cifre sorelle (righe ~678-685): `<span><span className="font-mono font-semibold text-ink">{organizerCount}</span>{" "}organizers</span>`
- la cifra staff (righe ~686-695): commento che rimanda a NAV-05 + `<button type="button" onClick={() => setRoleFilter("staff")} className={`inline-flex min-h-11 … ${FOCUS_RING}`}>`
- il commento del badge staff (righe ~120-135): «`staff` non concede **nulla** che un `attendee` non abbia gia'»
- la legenda (commento ~700-722, testo ~723-729): «A staff account grants nothing of its own — it can do nothing an attendee cannot, and it opens no door on its own. Working the door or a gallery comes from the night's own assignment, which an organizer makes and which ends with the night.»
Nessun gate statico pinna frammenti di `MemberTable` (ricerca §F.1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: La cifra staff diventa una cifra (NAV-05)</name>
  <files>src/components/admin/MemberTable.tsx</files>
  <read_first>
    - `src/components/admin/MemberTable.tsx` righe 660-700 — le tre cifre e il commento della disuguaglianza
    - `src/components/admin/MemberTable.tsx` — ogni altro uso di `setRoleFilter` e di `FOCUS_RING` (grep), per non lasciare un import o uno stato senza consumatori per caso
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §E riga NAV-05
  </read_first>
  <action>
    Sostituire il `<button … onClick={() => setRoleFilter("staff")}>` con uno
    `<span>` **identico** nella forma alle due cifre sorelle: `<span className=
    "font-mono font-semibold text-ink">{staffCount}</span>{" "}staff` dentro uno
    `<span>` esterno senza classi. Perde `min-h-11`, sottolineatura, hover e
    `FOCUS_RING` (non e' piu' un bersaglio).
    Riscrivere il commento che la precedeva: la disuguaglianza e' chiusa il
    2026-09-23, fase 52, D-52-18; il filtro per ruolo resta raggiungibile dal
    selettore del ruolo nei filtri (dove `setRoleFilter` continua a essere usato).
    Se `FOCUS_RING` resta senza altri usi nel file, togliere il suo import; se ha
    altri usi, lasciarlo. Non toccare `setRoleFilter` se il selettore lo usa.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c 'setRoleFilter("staff")' src/components/admin/MemberTable.tsx) -eq 0 &amp;&amp; /usr/bin/grep -q "D-52-18" src/components/admin/MemberTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero occorrenze di `setRoleFilter("staff")` in `MemberTable.tsx`
    - La cifra staff e' uno `span` con `font-mono font-semibold text-ink`, come `organizerCount`
    - Il commento accanto cita D-52-18 e dice dove resta il filtro per ruolo
  </acceptance_criteria>
  <done>Tre cifre, una sola forma.</done>
</task>

<task type="auto">
  <name>Task 2: La legenda e il commento del badge dicono la verita' dopo gallery.view</name>
  <files>src/components/admin/MemberTable.tsx</files>
  <read_first>
    - `src/components/admin/MemberTable.tsx` righe 110-160 (commento e badge staff) e 698-732 (commento e testo della legenda)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §F.2 (righe 599-601)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md` D-52-12, D-52-25
  </read_first>
  <action>
    Riscrivere il **testo visibile** della legenda, in inglese britannico come il
    resto dell'interfaccia, con questo contenuto: uno staff account opens the
    gallery — photos and videos from the nights — and nothing else of its own; it
    opens no door on its own: working the door comes from the night's own
    assignment, which an organizer makes and which ends with the night. Nessuna
    parola che alluda a un genere o a un suono; `re:sonate` con la e normale se
    compare.
    Riscrivere il **commento** sopra la legenda aggiungendo, sotto la storia gia'
    scritta (non al suo posto), il paragrafo datato 2026-09-23, fase 52: D-52-12
    concede `gallery.view` a `staff` per ruolo — la prima concessione per ruolo
    mai data a `staff` — e la frase «a gallery comes from the night's own
    assignment» diventava falsa il giorno della migration; la legenda spedisce
    nello stesso deploy della migration (atto del piano 52-15).
    Aggiornare il commento del badge (righe ~120-135): «`staff` non concede nulla
    che un `attendee` non abbia gia'» diventa: `staff` tiene **una** chiave per
    ruolo, `gallery.view` (fase 52), e nessun potere operativo — nessuna riga
    `door.operate` per ruolo; il bordo tratteggiato resta perche' continua a dire
    «non concede potere operativo», non «non concede nulla».
    Il badge stesso (classi, bordo tratteggiato) **non cambia**.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c "it can do nothing an attendee cannot" src/components/admin/MemberTable.tsx) -eq 0 &amp;&amp; test $(/usr/bin/grep -c "Working the door or a gallery comes from" src/components/admin/MemberTable.tsx) -eq 0 &amp;&amp; /usr/bin/grep -q "gallery.view" src/components/admin/MemberTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Le frasi «it can do nothing an attendee cannot» e «Working the door or a gallery comes from» non compaiono piu'
    - Il testo visibile dice che uno staff apre la gallery e che la porta viene dall'assegnazione della serata
    - Il commento sopra la legenda porta il paragrafo datato con D-52-12; il commento del badge nomina `gallery.view` e l'assenza di `door.operate` per ruolo
    - Le classi del badge staff sono invariate (`git diff` non tocca la riga del badge)
  </acceptance_criteria>
  <done>Chi promuove qualcuno a staff legge cosa riceve davvero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superficie → chi promuove | la legenda e' letta prima di cambiare il ruolo di una persona |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08 | Elevation of privilege | promozione a `staff` decisa su una legenda falsa | mitigate | la legenda nomina l'unica chiave per ruolo (`gallery.view`, cioe' foto di serate anche segrete) e spedisce nello stesso deploy della concessione |
| T-52-09 | Repudiation | commento cancellato invece che datato | mitigate | la storia resta, il paragrafo nuovo si aggiunge con data e decisione |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `npm run build` verde
- La pagina membri (manuale, P-52-D): la cifra staff non reagisce al tocco; la legenda nomina la gallery
</verification>

<success_criteria>
NAV-05 e' vero, e la pagina che crea gli staff descrive la concessione che la
fase introduce.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-03-SUMMARY.md` when done
</output>
