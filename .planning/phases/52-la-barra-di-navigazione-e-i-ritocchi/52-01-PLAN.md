---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
  - scripts/seed-lab-media.mjs
autonomous: true
requirements: [NAV-07]

must_haves:
  truths:
    - "Prima che esista una riga di M1, si sa quante righe di event_media ci sono per forma dell'indirizzo, per stato, per serata segreta e prima dello stripper — su laboratorio E produzione, solo conteggi (D-52-25, D-52-30, D-52-31)"
    - "La soglia 'prima dello stripper' e' la versione con cui 20260809006000 e' registrata nella history di ciascun progetto, riletta dal catalogo, non supposta (D-52-31)"
    - "Il laboratorio porta media veri di ogni forma che la fase deve provare: approvata, in attesa, rifiutata con oggetto, di serata segreta, orfana, e una foto 'prima dello stripper' con GPS (D-52-29, D-52-30, D-52-31)"
    - "Nessun indirizzo, path, identificativo o URL di un media e' stampato in .planning/ (repo pubblico)"
  artifacts:
    - path: ".planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md"
      provides: "il censimento di NAV-07 su due progetti, con l'ora UTC, solo numeri"
      contains: "Censimento"
    - path: "scripts/seed-lab-media.mjs"
      provides: "il banco dei media sul laboratorio per cio' che il prodotto non sa produrre (orfano, foto pre-stripper con GPS)"
      contains: "PRODUCTION_REF"
  key_links:
    - from: "scripts/seed-lab-media.mjs"
      to: "LAB_PROJECT_REF"
      via: "rifiuto del ref di produzione come prima riga eseguita dopo gli argomenti"
      pattern: "PRODUCTION_REF"
    - from: "52-ESITI.md §Censimento"
      to: "52-15 / 52-16 (atti in produzione)"
      via: "i numeri di oggi che l'autorizzazione riprende e rilegge"
      pattern: "rifiutate"
---

<objective>
Contare prima di scrivere, e dare al laboratorio i media che la fase deve
provare.

Purpose: NAV-07 e' Critical (RLS, media di serate segrete, bucket pubblico).
La ricerca §I.7 chiede il censimento `read_only` **prima** di scrivere M1: se
esistono righe con un indirizzo di forma diversa dal prefisso pubblico, il
backfill non le copre e vanno guardate una per una. D-52-30 e D-52-31 chiedono
che il numero di oggetti rifiutati, orfani e pre-stripper sia noto **prima**
della domanda al proprietario, non scoperto a meta' dell'atto. E su un
laboratorio senza media le sonde di NAV-07 misurano il vuoto (ricerca §I.6).
Output: `52-ESITI.md` con il censimento dei due progetti; `scripts/seed-lab-media.mjs`;
un laboratorio con righe e oggetti di ogni forma, contati nel SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@./CLAUDE.md
@.claude/rules/media-and-storage.md
@.claude/rules/venue-secrecy.md
@.claude/rules/ai-engineering.md
@.claude/rules/supabase-data.md

<interfaces>
Endpoint (misurati dalla fase 51, non da rimisurare):
- lettura: `POST /v1/projects/{ref}/database/query` con `read_only: true` — solo SELECT
- laboratorio: `LAB_PROJECT_REF` da `.env.lab.local`; produzione: la costante
  `PRODUCTION_REF` di `scripts/purge-attendances.mjs:125` (non ricopiarla in `.planning/`)
- il laboratorio va in pausa dopo una settimana: NXDOMAIN sul suo host = progetto
  `INACTIVE`, non rete; restore dal Management API prima di tutto

Colonne note di `public.event_media` (`supabase/schema.sql:257` e migration
successive): `id, event_id, party_id, url text not null, type, status
('pending'|'approved'|'rejected'), uploaded_by, created_at`.
Forma dell'indirizzo scritto da `registerMedia` (`actions.ts:179`):
`${NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/public/event-media/${storagePath}`.
Path dell'oggetto: `${eventId}/${userId}/${Date.now()}-${i}.${ext}` (`MediaUpload.tsx:401`).
Lo stripper: `src/lib/media/strip-metadata.ts` (`STRIPPABLE_MIME_TYPES` =
jpeg/png/webp); la migration che ha tolto la scrittura dal browser:
`20260809006000_event_media_server_upload_only.sql`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Il censimento di NAV-07, read_only, su laboratorio e produzione — solo numeri</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md</files>
  <read_first>
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §I.7 (righe 911-956) — le interrogazioni e come si leggono
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-ESITI.md` righe 1-60 — la forma di un registro di esiti pubblicato (ruoli, mai persone; ora UTC; nessun ref di laboratorio)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md` — la forma di un censimento fatto prima di una migration
    - `scripts/purge-attendances.mjs` righe 218-248 — la funzione `sql(query, { readOnly })` via Management API
    - `.claude/rules/venue-secrecy.md` — nessun conteggio che riveli una serata non annunciata: qui si conta su tutte le serate insieme, mai per serata
  </read_first>
  <action>
    Accertare prima che il laboratorio sia `ACTIVE_HEALTHY` (Management API,
    stato del progetto); se e' `INACTIVE`, fare il restore e riattendere.
    Eseguire, con `read_only: true`, **su entrambi i progetti**, queste letture e
    **solo queste**, restituendo numeri aggregati e mai righe:
    (1) su `public.event_media m`: totale; `url` che contiene
    `/storage/v1/object/public/event-media/`; `url` di altra forma; approvate; in
    attesa; rifiutate; `party_id IS NULL`; righe la cui serata
    (`public.event_parties p` con `p.id = m.party_id`) ha `p.venue_secret`
    vero; righe per `type` (foto / video).
    (2) la versione con cui `20260809006000` e' registrata in
    `supabase_migrations.schema_migrations` (colonna `version`, cercata per
    `name` che contiene `event_media_server_upload_only`) — questa e' la soglia
    «prima dello stripper» di **quel** progetto; poi il conteggio delle righe con
    `created_at` anteriore a quell'istante, separato per foto e video (i video non
    si spogliano: si contano a parte, D-52-31).
    (3) su `storage.objects` con `bucket_id = 'event-media'`: totale oggetti;
    oggetti **orfani** (nessuna riga di `event_media` il cui `url` finisca con
    `'/event-media/' || o.name`); oggetti che appartengono a righe `rejected`.
    (4) `storage.buckets` per `id = 'event-media'`: il valore di `public`.
    Creare `52-ESITI.md` con frontmatter (`phase`, `document: gli esiti della
    fase 52 — censimento, corse sul laboratorio, atti`, `written`) e il riquadro
    «Questo documento e' pubblicato» nella forma di `51-PROCEDURES.md:14-18`.
    Scrivere una sezione `## Censimento di NAV-07` con due tabelle — laboratorio
    e produzione — con l'ora UTC di ogni lettura e **solo numeri**. Sotto,
    l'interpretazione scritta come la ricerca chiede: se `url` di altra forma e'
    maggiore di zero, **dirlo** e fermarsi prima di M1 (il `DO` di M1 solleva
    su quelle righe, ed e' giusto che le guardi il proprietario); il numero di
    rifiutate con oggetto e di orfani e' il soggetto di D-52-30; il numero di
    foto pre-soglia e' il soggetto di D-52-31; le righe di serate segrete sono
    il numero che interessa `venue-secrecy.md`.
    Nessun URL, nessun path, nessun `id`, nessun ref di laboratorio nel file.
  </action>
  <verify>
    <automated>test -f .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md &amp;&amp; /usr/bin/grep -q "Censimento di NAV-07" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md &amp;&amp; test $(/usr/bin/grep -c "storage/v1/object/public/event-media/[0-9a-f]" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md) -eq 0 &amp;&amp; test $(/usr/bin/grep -cE "[0-9a-f]{8}-[0-9a-f]{4}-[0-9a-f]{4}" .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `52-ESITI.md` esiste con la sezione `## Censimento di NAV-07` e due tabelle, laboratorio e produzione, ognuna con l'ora UTC
    - Ogni tabella porta i conteggi (1)-(4): totale, forma dell'indirizzo, stato, serate segrete, foto/video, soglia dello stripper (versione riletta) e righe pre-soglia, oggetti totali, orfani, oggetti di righe rifiutate, `public` del bucket
    - Zero UUID, zero path di oggetto, zero URL di media, zero ref di laboratorio nel file (il comando di verifica ne conta zero)
    - Se `url` di altra forma > 0 in un progetto, il file lo dice e il SUMMARY dichiara che M1 (piano 52-06) non si scrive finche' il proprietario non ha visto quelle righe
    - Tutte le letture sono state fatte con `read_only: true`; il SUMMARY lo dichiara
  </acceptance_criteria>
  <done>I numeri di partenza di NAV-07 esistono, su entrambi i progetti, prima di una riga di migration.</done>
</task>

<task type="auto">
  <name>Task 2: Lo script del banco dei media sul laboratorio</name>
  <files>scripts/seed-lab-media.mjs</files>
  <read_first>
    - `scripts/seed-lab-door.mjs` righe 1-120 e 255-290 — la forma di uno script di laboratorio: caricamento di `.env.local` poi `.env.lab.local`, `PRODUCTION_REF` e il rifiuto come prima riga eseguita (righe 76-92), la creazione di una serata di prova con `venue_secret`
    - `.env.lab.seed.json` — gli account e la serata del banco gia' esistenti (leggerli, non inventarli)
    - `scripts/verify-media-strip.mjs` righe 15-30 — la prova «un file con GPS noto entra, l'oggetto riletto esce senza»
    - `src/lib/media/strip-metadata.ts` righe 100-115, 240-275 — i tipi trattati e come si carica `sharp`
    - `src/components/media/MediaUpload.tsx` riga 401 — la forma del path
  </read_first>
  <action>
    Creare `scripts/seed-lab-media.mjs`, script di **solo laboratorio**, nella forma
    di `seed-lab-door.mjs`: carica `.env.local` e poi `.env.lab.local`; dichiara
    `PRODUCTION_REF` e, come **prima riga eseguita dopo la lettura degli
    argomenti**, esce con codice 2 se il ref risolto e' quello di produzione; usa
    la chiave di servizio del laboratorio. Il caricamento dei file `.env*` **non
    sovrascrive** una variabile gia' presente nell'ambiente: e' cio' che permette
    di provare il rifiuto passando il ref di produzione da riga di comando.
    Lo script produce **soltanto** le due forme che il prodotto non sa produrre da
    se', piu' la precondizione della serata segreta:
    (a) accerta che sul laboratorio esista almeno una serata (`event_parties`) con
    `venue_secret` vero, legata a un evento di prova; se non c'e', la crea nella
    forma di `seed-lab-door.mjs:264-274`, e lo dice;
    (b) un **oggetto orfano**: un JPEG generato con `sharp` (colore pieno, 64x64),
    scritto nel bucket `event-media` sotto un path della forma
    `<eventId>/<userId>/<timestamp>-orphan.jpg`, **senza** riga in `event_media` —
    simula la finestra fra la scrittura e l'insert di `registerMedia`
    (`actions.ts:145-152`);
    (c) una **foto pre-stripper con GPS**: un JPEG generato con `sharp` a cui si
    scrive un EXIF con coordinate GPS note (latitudine e longitudine di un punto
    qualsiasi in mare, **mai** di una sede), scritto nel bucket `event-media` e
    registrato in `event_media` con `url` nella forma pubblica di `actions.ts:179`,
    `status: 'approved'`, `type: 'photo'`, `party_id` di una serata di prova, e
    `created_at` **anteriore** alla soglia dello stripper del laboratorio letta
    nel task 1 (la riga rappresenta la popolazione di D-52-31).
    Prima di chiudere, lo script rilegge dall'oggetto scritto in (c) che
    `sharp(buffer).metadata()` porta un campo `exif` non vuoto — la prova che il
    GPS c'e' davvero, senza la quale il piano 52-10 misurerebbe il vuoto.
    Lo script stampa **conteggi e categorie**, mai path, URL o identificativi.
    Argomenti: `--dry-run` (legge e dice cosa farebbe) e `--apply`, obbligatori e
    mai insieme, come `purge-attendances.mjs:107-153`. Idempotente: se l'orfano e
    la foto pre-stripper del banco esistono gia' (riconosciuti da un suffisso di
    nome dichiarato nello script), non ne crea di nuovi e lo dice.
  </action>
  <verify>
    <automated>node --check scripts/seed-lab-media.mjs &amp;&amp; /usr/bin/grep -q "PRODUCTION_REF" scripts/seed-lab-media.mjs &amp;&amp; /usr/bin/grep -q "\-\-dry-run" scripts/seed-lab-media.mjs &amp;&amp; LAB_PROJECT_REF=$(node -e "const s=require('fs').readFileSync('scripts/purge-attendances.mjs','utf8');console.log(s.match(/PRODUCTION_REF\s*=\s*['\"]([a-z0-9]+)/)[1])") node scripts/seed-lab-media.mjs --dry-run; test $? -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/seed-lab-media.mjs` esce 0
    - Lanciato con `LAB_PROJECT_REF` uguale al ref di produzione, lo script esce con codice **2** prima di qualunque lettura o scrittura (il comando di verifica lo prova)
    - Lo script contiene `--dry-run` e `--apply`, rifiuta entrambi insieme e nessuno dei due
    - L'output dello script non contiene path, URL o UUID (solo conteggi e categorie)
    - Le coordinate GPS scritte nell'EXIF sono dichiarate nel sorgente come punto in mare, con la ragione
  </acceptance_criteria>
  <done>Esiste uno strumento che da' al laboratorio le due forme di media che il prodotto non sa produrre, e che rifiuta la produzione.</done>
</task>

<task type="auto">
  <name>Task 3: Seminare il laboratorio — dal percorso vero dove esiste, dallo script dove no</name>
  <files>scripts/seed-lab-media.mjs</files>
  <read_first>
    - `src/app/(public)/events/[slug]/MediaGallerySection.tsx` e `src/components/media/MediaUpload.tsx` — la superficie di caricamento che si usa
    - `src/app/(admin)/admin/(work)/events/[id]/media/page.tsx` — la moderazione (approva, rifiuta)
    - `src/app/api/media/finalize/route.ts` righe 200-320 — il video su serata segreta e' rifiutato a monte: sulla serata segreta si carica una foto
    - `scripts/dev-lab.sh` — come si avvia il prodotto contro il laboratorio
  </read_first>
  <action>
    Avviare il prodotto contro il laboratorio con `npm run dev:lab` e, con Chrome
    via CDP (profilo isolato, porta letta dal log), entrare come l'account
    `organizer` del banco (da `.env.lab.seed.json`). Dal **percorso vero** —
    `MediaUpload` → quarantena → `/api/media/finalize` → `registerMedia` —
    caricare quattro foto JPEG generate al momento (nessuna foto di persone,
    nessuna sede riconoscibile): due su una serata non segreta e due sulla serata
    segreta del task 2. Dalla moderazione: approvarne una per serata, rifiutarne
    una, lasciarne una in attesa.
    Poi lanciare `node scripts/seed-lab-media.mjs --dry-run`, leggere cosa farebbe,
    e `--apply`.
    Rileggere dal laboratorio, `read_only`, i conteggi di `event_media` per stato
    e di `storage.objects` nel bucket, e scriverli nel SUMMARY con l'ora UTC
    accanto a quelli del censimento del task 1 (prima/dopo). Attesi, come minimo
    in piu' rispetto al censimento: approvate +3 (una di serata segreta e la
    pre-stripper), in attesa +1, rifiutate +1 con oggetto, orfani +1.
    Chiudere il server di sviluppo. Nessuna scrittura in produzione: dichiararlo
    nel SUMMARY.
  </action>
  <verify>
    <automated>node --check scripts/seed-lab-media.mjs &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY riporta, con l'ora UTC, i conteggi del laboratorio prima (dal task 1) e dopo: approvate almeno +3, in attesa almeno +1, rifiutate almeno +1 con oggetto presente, orfani almeno +1
    - Almeno una riga approvata appartiene a una serata con `venue_secret` vero (conteggio, non identificativo)
    - La foto pre-stripper riletta dal bucket porta `exif` non vuoto (dichiarato nel SUMMARY con l'esito della rilettura)
    - Le foto dal percorso vero sono passate da `/api/media/finalize` (dichiarato), non inserite a mano
    - Zero scritture sul progetto di produzione, dichiarato nel SUMMARY
  </acceptance_criteria>
  <done>Il laboratorio porta media di ogni forma che NAV-07, D-52-30 e D-52-31 devono provare.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → produzione (lettura) | il censimento legge la produzione: solo SELECT aggregati, `read_only: true` |
| chi esegue → laboratorio (scrittura) | l'unico progetto in cui questo piano scrive |
| `.planning/` → pubblico | ogni riga di questo repo e' una pubblicazione irreversibile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information disclosure | `52-ESITI.md` | mitigate | solo numeri aggregati su tutte le serate; il comando di verifica conta zero UUID e zero URL di media; nessun conteggio per singola serata (venue-secrecy) |
| T-52-02 | Tampering | censimento in produzione | mitigate | `read_only: true` su ogni chiamata; nessun `/database/migrations` verso la produzione in questo piano |
| T-52-03 | Tampering | `seed-lab-media.mjs` puntato alla produzione | mitigate | rifiuto di `PRODUCTION_REF` come prima riga eseguita, provato dal comando di verifica (exit 2) |
| T-52-04 | Information disclosure | foto e GPS del banco | mitigate | immagini generate (colore pieno), GPS in mare dichiarato nel sorgente; nessuna foto di persone, nessuna sede |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato; `sharp` e' gia' in `dependencies` |
</threat_model>

<verification>
- `52-ESITI.md` porta il censimento dei due progetti, solo numeri, con l'ora UTC
- `scripts/seed-lab-media.mjs` rifiuta la produzione (exit 2)
- Il laboratorio porta approvate, in attesa, rifiutata con oggetto, di serata segreta, orfano e pre-stripper con GPS
- `npm run build` verde
</verification>

<success_criteria>
Il piano di M1 parte da numeri misurati su entrambi i progetti, e le sonde di
NAV-07 avranno soggetti veri sul laboratorio.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-01-SUMMARY.md` when done
</output>
