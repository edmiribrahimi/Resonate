---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 19
type: execute
wave: 8
depends_on: ["52-14", "52-18"]
files_modified:
  - src/components/ui/Input.tsx
  - src/components/ui/AutocompleteInput.tsx
  - src/app/globals.css
  - .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
  - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
autonomous: true
gap_closure: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Sulla pagina di login, toccando il campo email o il campo password su iPhone (Safari), la pagina NON si ingrandisce: `visualViewport.scale` resta 1 al tocco e dopo il blur (requisito letterale del proprietario, 2026-09-23)"
    - "Alla porta, toccando la ricerca «Search by name...», la pagina non si ingrandisce: scale 1, e «QR Scan» e la linguetta Alerts restano dentro lo schermo"
    - "Un `<select>` nativo (il ruolo nel modulo Create account) e il campo del codice sconto di una serata non ingrandiscono la pagina: scale 1"
    - "Lo zoom a due dita resta possibile: nessun `maximum-scale`, nessun `user-scalable=no` (D-41-08); la correzione e' la dimensione del testo nei campi, non un blocco della scala"
    - "Prima della correzione la stessa sonda, sullo stesso simulatore, misura scale 1,1436: la linea di base riproduce il difetto, quindi la misura dopo prova qualcosa"
    - "Con il fuoco sulla password del login, email, password e «Sign In» stanno sopra la tastiera (P-52-F passo 5 rimisurato): altrimenti D-52-28 si riapre e 52-15 non parte"
    - "Il laboratorio (lab.resonatemotion.com) serve il commit della correzione, READY, e la produzione non e' stata toccata"
  artifacts:
    - path: "src/components/ui/Input.tsx"
      provides: "CONTROL con text-base md:text-sm"
      contains: "text-base md:text-sm"
    - path: "src/components/ui/AutocompleteInput.tsx"
      provides: "CONTROL con text-base md:text-sm, come il primitivo"
      contains: "text-base md:text-sm"
    - path: "src/app/globals.css"
      provides: "rete di sicurezza: 16 px su input/select/textarea sotto puntatore grossolano, per ogni campo che non usa il primitivo (ricerca della porta, tag)"
      contains: "font-size: 1rem"
    - path: ".planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md"
      provides: "la deroga datata alla scala tipografica per i campi, e la stringa di §8.6 aggiornata"
      contains: "2026-09-23"
    - path: ".planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md"
      provides: "sezione «Chiusura delle lacune»: prima/dopo per campo, P-52-F passo 5, orizzontale, deploy lab"
      contains: "Chiusura delle lacune"
  key_links:
    - from: "src/app/globals.css (regola non a strati)"
      to: "le utility text-sm dei campi grezzi (ScannerClient.tsx:3369, TagInput, AutocompleteTagInput)"
      via: "cascata: una regola fuori da @layer vince sulle utility di Tailwind 4, che stanno in @layer utilities"
      pattern: "@media \\(pointer: coarse\\)"
    - from: "src/components/ui/Input.tsx CONTROL"
      to: "41-UI-SPEC.md §8.6"
      via: "la stringa verbatim, aggiornata nello stesso commit"
      pattern: "text-base md:text-sm"
---

<objective>
Chiudere il difetto 1 della corsa del 52-14: Safari iOS ingrandisce la pagina a
ogni tocco su un campo sotto i 16 px, e l'ingrandimento resta dopo aver chiuso la
tastiera. Alla porta il bordo destro perde «QR Scan» e la linguetta Alerts.

Purpose: e' **Critical** perche' tocca la porta (`checkin-offline.md`: la porta non
ha rete e non ha pazienza — un pulsante di ingresso fuori schermo davanti a una fila
e' il costo peggiore). Il requisito del proprietario e' letterale: *«se sono sulla
pagina di login, e clicco sul campo mail o password, non deve avvenire lo zoom»*. Il
vincolo e' D-41-08: lo zoom a due dita non si blocca, quindi niente `maximum-scale`
ne' `user-scalable=no`; la correzione e' 16 px sui campi, provata sul simulatore
dalla corsa stessa. La chiusura non e' fatta finche' non e' **rimisurata su Safari**,
prima e dopo, con la stessa sonda.
Output: primitivo e rete CSS a 16 px sotto puntatore grossolano; deroga datata in
41-UI-SPEC; gate verdi; misure prima/dopo in `52-ESITI.md` «Chiusura delle lacune»;
laboratorio al commit della correzione.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/rules/checkin-offline.md
@.claude/rules/nextjs-architecture.md
@.claude/rules/meta-gates.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-14-SUMMARY.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-13-SUMMARY.md

<interfaces>
Il difetto, misurato da 52-14 ([3], iPhone 17e simulato, iOS 27.0, viewport 390×699):
font-size 14 px su ogni campo toccato; al tocco `visualViewport.scale` 1 → 1,1436
(larghezza visibile 390 → 341 px); resta dopo il blur. Correzione provata con
`input,select,textarea{font-size:16px}` iniettato: scale resta 1.

src/components/ui/Input.tsx:102-104 —
`const CONTROL = "min-h-11 w-full rounded-xl border border-control bg-sunk px-4 text-sm text-ink " + "placeholder:text-muted";`
usata da `<input>` :221-225, `<textarea>` :252-256, `<select>` :284-288. Il docblock sopra dice «§8.6's string, verbatim».
src/components/ui/AutocompleteInput.tsx:89 — un secondo `CONTROL` con la stessa stringa (`… px-4 text-sm text-ink `).

Campi grezzi con `text-sm` che NON usano il primitivo (la rete CSS li copre, nessuno si modifica):
- src/app/(admin)/admin/scanner/ScannerClient.tsx:3358-3370 — la ricerca della porta, `placeholder="Search by name..."`, classe `… py-3 pl-10 pr-10 text-sm text-ink …`. NON si modifica: le deroghe di palette di `verify:conversion` sono ancorate a righe di questo file, e la rete CSS basta (la misura lo prova).
- src/components/events/TagInput.tsx:110, src/components/events/AutocompleteTagInput.tsx:267
(Il codice sconto della pagina pubblica di una serata usa invece il primitivo: `<Input … placeholder="Enter code">` in src/app/(public)/events/[slug]/TierSelection.tsx:502-510 — lo coprono entrambi i meccanismi.)
Nessun campo del tree ha una classe di dimensione sopra `text-base` (misurato in pianificazione: zero `text-lg`/`text-xl`/`text-[…]` su input, select, textarea o sui consumatori di Input/Select/Textarea): una regola a 16 px non rimpicciolisce nessuno.

src/app/globals.css:
- :1 `@import "tailwindcss";` — le utility vivono in `@layer utilities`; una regola scritta FUORI da `@layer` vince su qualunque utility, a prescindere dalla specificita'. E' il meccanismo della rete.
- :560-640 le due regole D-52-06 (la barra lascia lo schermo col fuoco): stessa lista di esclusione `input:not([type="checkbox"],[type="radio"],[type="button"],[type="submit"],[type="hidden"])`, textarea, select — il modello da seguire per il commento e per la forma.
- :650-708 il blocco «The pointer query — written ONCE, and only ever used to SHRINK» (41-UI-SPEC §6.2) e `@custom-variant pointer-fine-only`: quella regola riguarda i BERSAGLI e non si tocca; la rete e' un'altra domanda e va dichiarata come tale nel suo commento.

src/app/layout.tsx:81-95 — `viewport`: `width: "device-width"`, `initialScale: 1`, `interactiveWidget: "resizes-content"`, e il commento «QUI NON SI BLOCCA LA SCALA» (D-41-08). NON si tocca.

41-UI-SPEC.md: §7 tabella dei ruoli (:600-605, Body = `text-sm` 14px), §8.6 la stringa dei controlli (:899-910), §12 il punto `userScalable: false` rimosso (:1125-1127).

Gate che leggono questi file: `verify:touch-targets` legge `CONTROL` di Input.tsx e AutocompleteInput.tsx per `min-h-11` (verify-touch-targets.mjs:823-841, :1023-1029) — `min-h-11` deve restare nella stringa; `verify:tokens` legge globals.css (nessun colore, nessun `var(--x)` nuovo qui); `verify:conversion` custodisce il digest di `PageShell.tsx` soltanto (SHELL_FILE), che questo piano NON tocca; `verify:breakpoints` conta i prefissi responsive (`md:` e' uno dei due ammessi). ATTENZIONE: `verify-conversion.mjs` contiene byte NUL — cercarci dentro con `/usr/bin/grep -a`, mai `grep` nudo (ugrep li salta in silenzio).

La sonda Appium (W3C), provata in 52-14:
- server `http://localhost:4723` (se non risponde a `GET /status`: `appium --port 4723` in background, log nella scratchpad); driver XCUITest installato
- simulatore iPhone 17e, iOS 27.0, UDID `1526779A-6F5A-4B5C-A1E1-A58741DD20CF` (`xcrun simctl boot <UDID>`; puo' essere gia' avviato: «Unable to boot device in current state: Booted» non e' un errore)
- `POST /session` con `{"capabilities":{"alwaysMatch":{"platformName":"iOS","appium:automationName":"XCUITest","browserName":"Safari","appium:udid":"1526779A-6F5A-4B5C-A1E1-A58741DD20CF","appium:platformVersion":"27.0","appium:deviceName":"iPhone 17e"}}}`
- `POST /session/<sid>/url` `{"url":"http://localhost:<porta>/login"}`
- `POST /session/<sid>/element` `{"using":"css selector","value":"input[type=email]"}` → `POST /session/<sid>/element/<id>/click` (tocco nativo: tastiera e zoom)
- `POST /session/<sid>/execute/sync` `{"script":"return JSON.stringify({scale:visualViewport.scale,w:innerWidth,vw:visualViewport.width,fs:getComputedStyle(document.activeElement).fontSize,tag:document.activeElement.tagName,sw:document.documentElement.scrollWidth})","args":[]}`
- `POST /session/<sid>/orientation` `{"orientation":"LANDSCAPE"}` / `"PORTRAIT"`
- `DELETE /session/<sid>` alla fine
Il simulatore raggiunge il `localhost` del Mac: la sonda gira contro `npm run dev:lab` (database del laboratorio, mai la produzione).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: La linea di base — la sonda sul simulatore riproduce il difetto prima di toccare il codice</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md</files>
  <read_first>
    - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md (righe 292-320: le quattro fonti e i viewport; righe 441-537: i tre difetti e D-52-28)
    - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md (righe 178-222: P-52-F, in particolare il passo 5)
    - scripts/dev-lab.sh (rifiuta la produzione; la porta si legge dal log)
    - /Users/etiesse/.claude/projects/-Users-etiesse-Resonate/memory/lab-pauses-after-a-week.md (NXDOMAIN = laboratorio in pausa: ripristino prima delle prove)
    - src/app/(auth)/login/ (la pagina: `#login-email`, `#login-password`, il pulsante submit «Sign In»)
  </read_first>
  <action>
    Sul tree com'e' dopo 52-18, SENZA la correzione. Avviare `npm run dev:lab` in background con il log nella scratchpad della sessione e leggere la porta dalla riga «Local:». Verificare che Appium risponda (`curl -s http://localhost:4723/status`), altrimenti avviarlo; avviare il simulatore se serve. Scrivere la sonda come script usa-e-getta nella scratchpad della sessione (MAI nel repo, MAI in `.planning/`): legge `LAB_ACCOUNT_PASSWORD` con `node --env-file=.env.lab.local` e la passa ai campi con `POST /session/<sid>/element/<id>/value` `{"text":"…"}` senza mai stamparla; per ogni campo esegue: caricare l'URL di nuovo (lo scale riparte da 1 — registrarlo come «riposo»), trovare il campo, tocco nativo con `/click`, attendere 1 s, leggere la misura (scale, w, vw, fs, tag, sw), poi `document.activeElement.blur()` via `execute/sync`, attendere 1 s, leggere di nuovo («dopo il blur»).
    I campi, in quest'ordine: (a) `/login` email `input[type=email]`; (b) `/login` password `input[type=password]`; (c) `/door` ricerca `input[placeholder="Search by name..."]` dopo l'accesso come `door@lab.invalid` (staff assegnato del banco; l'accesso si fa compilando il login dalla sonda); (d) `/admin/members` come `master@lab.invalid`: aprire il modulo Create account (pulsante il cui testo contiene «Create account»), poi il `select` del ruolo; (e) il campo del codice sconto (`input[placeholder="Enter code"]`, dopo aver aperto il suo interruttore) sulla pagina pubblica di una serata in vendita del laboratorio (la stessa di 52-14 [3], 17:05Z, da `attendee` o anonimo — leggere l'indirizzo della serata dalla lista `/events`, mai scriverne lo slug in `.planning/` se la serata e' segreta); (f) di nuovo `/login` email, ma dopo `POST /orientation` `LANDSCAPE` — registrare anche `innerWidth` (atteso sopra 768, cioe' sopra il bordo `md`), poi tornare `PORTRAIT`.
    Controllo di validita' OBBLIGATORIO: su (a), (b) e (c) la linea di base deve riprodurre 52-14 — `fs` 14px e scale ≈ 1,1436 al tocco, ancora > 1 dopo il blur. Se lo scale resta 1 prima della correzione, il click non e' un tocco nativo: aggiungere `"appium:nativeWebTap": true` alle capabilities e ripetere; se ancora non riproduce, FERMARSI e riportarlo — una misura «dopo» su una sonda che non vede il difetto non prova nulla.
    Aprire in `52-ESITI.md`, dopo la sezione «La decisione sul login (D-52-28)», la sezione `## Chiusura delle lacune (piani 52-18 e 52-19) — 2026-09-23` con la sottosezione «Linea di base, prima della correzione»: fonte (simulatore iPhone 17e iOS 27.0, Appium, tocco nativo, dev:lab contro il laboratorio), commit misurato (sha corto), e una tabella campo × (riposo, al tocco, dopo il blur, fs) con l'ora UTC di ogni riga. Nessun indirizzo email, nessuna password, nessun UUID, nessun ref del laboratorio nel file. Lasciare il dev server acceso per il task 3 solo se lo stesso esecutore prosegue senza interruzioni; altrimenti chiuderlo.
  </action>
  <verify>
    <automated>E='.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md'; /usr/bin/grep -n 'Chiusura delle lacune' "$E" && /usr/bin/grep -n 'Linea di base' "$E" && test "$(/usr/bin/grep -cE '[a-z0-9._-]+@[a-z0-9.-]+\.[a-z]+' "$E")" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `52-ESITI.md` ha la sezione «Chiusura delle lacune» con la sottosezione «Linea di base, prima della correzione»
    - la tabella ha le sei righe (a)-(f), ciascuna con ora UTC, scale a riposo, al tocco, dopo il blur e `fs`
    - (a), (b), (c) mostrano `fs` 14px e scale al tocco fra 1,14 e 1,15, e dopo il blur > 1 (il difetto riprodotto); se no, il task e' fermo e il SUMMARY lo dice
    - zero indirizzi email, zero UUID, zero ref del laboratorio nel file (grep)
    - la sonda vive solo nella scratchpad: `git status --porcelain` non mostra file nuovi fuori da `52-ESITI.md`
  </acceptance_criteria>
  <done>La linea di base e' scritta e riproduce il difetto su login, porta e select con la stessa sonda che misurera' la correzione.</done>
</task>

<task type="auto">
  <name>Task 2: 16 px sui campi sotto puntatore grossolano — primitivo, rete CSS, deroga datata in 41-UI-SPEC, gate verdi</name>
  <files>src/components/ui/Input.tsx, src/components/ui/AutocompleteInput.tsx, src/app/globals.css, .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md</files>
  <read_first>
    - src/components/ui/Input.tsx (righe 80-110: il docblock e `CONTROL`; 215-292: i tre controlli)
    - src/components/ui/AutocompleteInput.tsx (righe 80-100: il suo `CONTROL`)
    - src/app/globals.css (righe 1-10, 495-520, 555-708: l'import di Tailwind, `body`, le regole D-52-06 sotto `pointer: coarse`, il blocco «The pointer query — written ONCE, and only ever used to SHRINK»)
    - src/app/layout.tsx (righe 78-100: `viewport` e «QUI NON SI BLOCCA LA SCALA», D-41-08 — da NON toccare)
    - .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md (§7 righe 594-606, §8.6 righe 899-930, §12 righe 1109-1160)
    - scripts/verify-touch-targets.mjs (righe 815-845 e 1018-1030: le voci che leggono `CONTROL`)
  </read_first>
  <action>
    Primitivo (per D-41-08 e il difetto 1 di `52-ESITI.md`): in Input.tsx la stringa `CONTROL` passa da `text-sm` a `text-base md:text-sm`, tutto il resto della stringa invariato (`min-h-11` resta: lo legge `verify:touch-targets`). Aggiungere al docblock di `CONTROL` un paragrafo datato 2026-09-23: Safari iOS ingrandisce la pagina al fuoco di ogni campo sotto i 16 px e non la riporta a 1 dopo il blur (misura 52-14: 1,1436); 16 px sotto `md` e' la correzione, e non un blocco della scala, perche' D-41-08 lascia lo zoom a due dita a chi ne ha bisogno; il riferimento e' la deroga in 41-UI-SPEC §7. Lo stesso cambio, con una riga di commento che rimanda a Input.tsx, nel `CONTROL` di AutocompleteInput.tsx (che dichiara di ricalcare il primitivo).
    Rete CSS in globals.css, subito dopo le due regole D-52-06: una regola FUORI da ogni `@layer` sotto `@media (pointer: coarse)` che imposta `font-size: 1rem` su `input:not([type="checkbox"],[type="radio"],[type="button"],[type="submit"],[type="reset"],[type="hidden"],[type="file"],[type="range"],[type="color"],[type="image"])`, `textarea`, `select`. Scelte, da scrivere nel commento sopra la regola (forma e tono dei commenti D-52-06): (1) fuori da `@layer` di proposito — in Tailwind 4 le utility stanno in `@layer utilities` e una regola non a strati le batte tutte, quindi copre i campi grezzi che portano `text-sm` e non usano il primitivo: la ricerca della porta (ScannerClient.tsx, «Search by name...») e i due campi dei tag; nessun campo del tree ha una dimensione sopra `text-base`, quindi la regola non rimpicciolisce nessuno — e chi un giorno volesse un campo piu' grande di 16 px lo scopre qui; (2) chiave `pointer: coarse` SENZA limite di larghezza, scelta del pianificatore dichiarata: un iPhone in orizzontale supera i 48rem (il bordo `md`) e zooma lo stesso, perche' Safari decide sulla dimensione del testo, non sulla larghezza; il costo e' che su un tablet i campi sono a 16 px anche sopra `md`, cioe' il ruolo Heading della scala, un costo visivo contro uno zoom che resta; (3) non e' il blocco «written ONCE, and only ever used to SHRINK» di 41-UI-SPEC §6.2, che riguarda i bersagli: questa e' un'altra domanda, e non tocca `pointer-fine-only`; (4) niente `maximum-scale`, niente `user-scalable=no`: D-41-08; (5) il riferimento alla misura (52-14 [3]) e al requisito del proprietario. ScannerClient.tsx NON si modifica (le deroghe di palette di `verify:conversion` sono ancorate alle sue righe, e la rete basta — lo prova il task 3). layout.tsx NON si modifica.
    41-UI-SPEC.md, nella lingua del documento (inglese): dopo la tabella di §7 una sottosezione `### 7.4 Dated deviation — form fields at 16px under a coarse pointer (2026-09-23, phase 52, plan 52-19)`: la regola (campi di testo, select e textarea a 16 px — `text-base` — sotto `md` nel primitivo e sotto ogni puntatore grossolano nella rete di globals.css; il corpo resta 14 px ovunque altrove), la ragione misurata (Safari iOS 26.7 e 27.0: zoom a 1,1436 al fuoco sotto 16 px, persistente dopo il blur; alla porta «QR Scan» e Alerts fuori schermo), perche' non `maximum-scale` (D-41-08), i numeri prima/dopo come rimando a `52-ESITI.md` «Chiusura delle lacune», e che cambiare di nuovo la dimensione dei campi e' una decisione di sistema da dichiarare qui. In §8.6 la stringa del blocco dei controlli diventa `min-h-11 w-full rounded-xl border border-control bg-sunk px-4 text-base md:text-sm text-ink`, con una riga che rimanda a §7.4. In §12, accanto al punto su `userScalable: false`, una frase: lo zoom al fuoco non si previene bloccando la scala ma con la dimensione dei campi, §7.4.
    Poi i gate, tutti verdi: `npm run build`, `npm run verify:tokens`, `npm run verify:touch-targets`, `npm run verify:conversion`, `npm run verify:breakpoints`, `npm run verify:no-viewport-read`. Se `verify:conversion` diventa rosso per un digest di un file congelato toccato da questo task, aggiornare il digest nello stesso commit seguendo le istruzioni stampate dal gate e scriverlo nel SUMMARY (atteso: nessun digest da aggiornare — il solo digest custodito e' quello di `PageShell.tsx`, che qui non si tocca; se il gate si arrossa su PageShell, FERMARSI: significa che qualcosa ha toccato il file sbagliato).
  </action>
  <verify>
    <automated>/usr/bin/grep -n 'text-base md:text-sm' src/components/ui/Input.tsx src/components/ui/AutocompleteInput.tsx && /usr/bin/grep -n 'font-size: 1rem' src/app/globals.css && test "$(/usr/bin/grep -v '^\s*//' src/app/layout.tsx | /usr/bin/grep -ciE 'maximumScale|userScalable|maximum-scale|user-scalable')" = "0" && /usr/bin/grep -n '7.4 Dated deviation' .planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md && npm run build && npm run verify:tokens && npm run verify:touch-targets && npm run verify:conversion && npm run verify:breakpoints && npm run verify:no-viewport-read</automated>
  </verify>
  <acceptance_criteria>
    - `CONTROL` in Input.tsx e in AutocompleteInput.tsx contiene `text-base md:text-sm` e `min-h-11`, e non contiene piu' un `text-sm` isolato prima di `text-ink` (grep `px-4 text-sm text-ink` = 0 in entrambi)
    - globals.css ha una regola `font-size: 1rem` sotto `@media (pointer: coarse)` fuori da ogni `@layer`, sui controlli di testo, textarea e select, con la lista di esclusione dei tipi senza tastiera
    - layout.tsx invariato (`git diff --stat src/app/layout.tsx` vuoto); nessuna occorrenza di codice di `maximumScale`/`userScalable`/`maximum-scale`/`user-scalable` nel tree `src/`
    - ScannerClient.tsx invariato (`git diff --stat` vuoto su quel file); PageShell.tsx invariato
    - 41-UI-SPEC.md ha `### 7.4 Dated deviation` con la data 2026-09-23, la stringa di §8.6 con `text-base md:text-sm`, e la frase in §12
    - `npm run build`, `verify:tokens`, `verify:touch-targets`, `verify:conversion`, `verify:breakpoints`, `verify:no-viewport-read` escono tutti 0
  </acceptance_criteria>
  <done>I campi sono a 16 px dove Safari zoomerebbe, senza bloccare lo zoom a due dita; la scala e' dichiarata nel contratto; i gate sono verdi.</done>
</task>

<task type="auto">
  <name>Task 3: La ri-misura su Safari, P-52-F passo 5, e il laboratorio al commit della correzione</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md</files>
  <read_first>
    - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md (la sezione «Chiusura delle lacune» scritta dal task 1, e «La decisione sul login (D-52-28)»)
    - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-18-SUMMARY.md (le prove dei difetti 2 e 3, da citare)
    - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-13-PLAN.md (task 2: precondizioni del push su `lab`) e 52-13-SUMMARY.md (righe 110-130 e 265-275: push, tabella del deploy, API Vercel senza `teamId`)
  </read_first>
  <action>
    Con il codice del task 2 servito da `npm run dev:lab` (riavviarlo se era acceso sul tree vecchio: il CSS deve essere quello nuovo — controllo: `fs` del campo toccato 16px), rieseguire la STESSA sonda del task 1, stesse capabilities, stessi sei campi (a)-(f), stesso ordine. Atteso per ognuno: `fs` 16px, scale 1 a riposo, al tocco e dopo il blur, `sw` uguale a `w` (nessuno scorrimento orizzontale). Su (c) leggere anche, a tastiera aperta, se il pulsante «QR Scan» e la linguetta Alerts sono dentro lo schermo (`getBoundingClientRect().right <= innerWidth` per ciascuno, via `execute/sync`; trovarli per testo).
    P-52-F passo 5, rimisurato: su `/login`, fuoco su email poi su password (tocco nativo), e a ciascun fuoco leggere per `#login-email`, `#login-password` e il pulsante submit se il rettangolo sta dentro la regione visibile sopra la tastiera (`rect.top >= visualViewport.offsetTop` e `rect.bottom <= visualViewport.offsetTop + visualViewport.height`). Atteso, per D-52-28 `resta`: con il fuoco sulla password tutti e tre dentro. Se con il fuoco sulla password «Sign In» risultasse coperto, FERMARSI prima del deploy: D-52-28 si riapre (`allinea`, che tocca `FOCUS_ROOT` di PageShell e il suo digest) e serve un piano di chiusura in piu' prima di 52-15 — registrarlo e riportarlo, non decidere qui.
    Scrivere in `52-ESITI.md`, nella sezione «Chiusura delle lacune», la sottosezione «Dopo la correzione»: la tabella prima/dopo per campo (riposo, tocco, blur, fs) con le ore UTC e lo sha misurato; «QR Scan» e Alerts dentro lo schermo alla porta; P-52-F passo 5 con i tre controlli per ciascun fuoco e l'esito rispetto a D-52-28; una riga per i difetti 2 e 3 che rimanda al SUMMARY di 52-18 con le sue ore; e la frase che il requisito del proprietario («non deve avvenire lo zoom») e' soddisfatto sul simulatore iOS 27.0 e da confermare sul suo iPhone dopo il deploy. Chiudere la sessione Appium (`DELETE /session/<sid>`) e il dev server.
    Deploy del laboratorio, come 52-13 task 2, solo dopo il commit su `main` locale dei task 1-2 e della sottosezione «Dopo la correzione» (la riga del deploy si aggiunge dopo, in un commit che resta locale): precondizioni misurate e scritte nel SUMMARY — `npm run build` verde sul commit da spingere; `git merge-base --is-ancestor origin/lab main` (avanzamento veloce); nel diff `origin/lab..main` zero file sotto `docs/` e `.firecrawl/`, zero `.env*`, zero occorrenze del ref del laboratorio, zero token (`sbp_`, `sk_`, `re_`, JWT), zero UUID fra le righe aggiunte. Registrare `git rev-parse origin/main` PRIMA. Poi `git push origin main:lab` — SOLO il ramo `lab`: il push di `main` e' il deploy di produzione ed e' un passo dell'atto di 52-15. Attendere il dispiegamento `READY` del ramo `lab` dall'API Vercel (`GET https://api.vercel.com/v6/deployments?limit=10` e `GET /v13/deployments/<id>`, con `VERCEL_TOKEN` da `.env.local`, SENZA `teamId`: con `teamId` risponde 403), registrare sha corto, ora di creazione e di `READY`, e verificare l'alias `lab.resonatemotion.com`. Verifica esterna: `curl -sI https://lab.resonatemotion.com/gallery` da anonimo → `location` con `login?next=%2Fgallery`, e `curl -s https://lab.resonatemotion.com/login` restituisce 200. Registrare `git rev-parse origin/main` DOPO: identico a prima. Aggiungere a «Chiusura delle lacune» una riga col deploy (sha, `READY` UTC) e l'invito al proprietario a riprovare login e porta sul suo iPhone.
  </action>
  <verify>
    <automated>E='.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md'; /usr/bin/grep -n 'Dopo la correzione' "$E" && /usr/bin/grep -n 'READY' "$E" | tail -1 && test "$(/usr/bin/grep -cE '[a-z0-9._-]+@[a-z0-9.-]+\.[a-z]+' "$E")" = "0" && curl -sI https://lab.resonatemotion.com/gallery | /usr/bin/grep -i '^location:' | /usr/bin/grep -q 'login?next=%2Fgallery'</automated>
  </verify>
  <acceptance_criteria>
    - «Dopo la correzione» in `52-ESITI.md` registra, per (a)-(f): `fs` 16px e `visualViewport.scale` 1 a riposo, al tocco e dopo il blur, con ora UTC; accanto, i valori della linea di base (1,1436 su (a)-(c))
    - alla porta, a tastiera aperta, «QR Scan» e Alerts hanno `right <= innerWidth`
    - P-52-F passo 5: con il fuoco sulla password, email, password e «Sign In» dentro la regione visibile; D-52-28 `resta` confermato — oppure il task e' fermo e il SUMMARY dice che D-52-28 si riapre
    - il SUMMARY registra le precondizioni del push, lo sha spinto su `lab`, creazione e `READY` UTC; `origin/main` identico prima e dopo
    - `curl -sI https://lab.resonatemotion.com/gallery` anonimo → `location` con `login?next=%2Fgallery`
    - zero indirizzi email, password, UUID o ref del laboratorio in `52-ESITI.md` e nel SUMMARY
  </acceptance_criteria>
  <done>Lo zoom al tocco non avviene piu' su login, porta, select e codice sconto, misurato sulla stessa sonda che lo vedeva; il login resta com'e'; il laboratorio serve la correzione; la produzione non e' stata toccata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → repo pubblico | `git push origin main:lab` e' una pubblicazione irreversibile |
| service worker → porta offline | il foglio di stile nuovo arriva alla porta solo quando il service worker si aggiorna |
| sonda → laboratorio | le prove leggono e scrivono sul database del laboratorio con account del banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-84 | Denial of Service | la porta: «QR Scan» e Alerts fuori schermo dopo lo zoom | mitigate | 16 px sui campi (primitivo + rete non a strati sotto `pointer: coarse`); ri-misura con `right <= innerWidth` a tastiera aperta (task 3) |
| T-52-85 | Denial of Service (accessibilita') | blocco dello zoom a due dita | mitigate | nessun `maximum-scale`/`user-scalable=no` (D-41-08): layout.tsx invariato, grep nel task 2 |
| T-52-86 | Information Disclosure | push del ramo `lab` sul repo pubblico | mitigate | precondizioni di 52-13: zero `docs/`, `.firecrawl/`, `.env*`, ref, token, UUID nel diff; solo `lab`, `origin/main` invariato prima/dopo |
| T-52-87 | Information Disclosure | credenziali del banco nella sonda | mitigate | password letta da `.env.lab.local` a runtime, sonda solo nella scratchpad, mai stampata; grep di email/UUID su `52-ESITI.md` |
| T-52-88 | Tampering | la sonda contro la produzione | mitigate | `dev:lab` rifiuta il ref di produzione come prima riga; la sonda punta solo a `localhost` |
| T-52-89 | Denial of Service | porta offline con il CSS vecchio in cache | accept | fino all'aggiornamento del service worker la porta mostra ancora i campi a 14 px: lo zoom torna, ma nessun ospite viene rifiutato (l'ammissione non dipende dal CSS); si chiude da solo al primo aggiornamento, e in produzione e' 52-15 |
| T-52-90 | Denial of Service | D-52-28: «Sign In» coperto dopo la correzione | mitigate | P-52-F passo 5 rimisurato; se coperto, stop prima del deploy e piano in piu' prima di 52-15 |
</threat_model>

<verification>
- La linea di base riproduce il difetto (1,1436) con la stessa sonda che poi misura 1: senza la prima, la seconda non prova nulla
- `npm run build`, `verify:tokens`, `verify:touch-targets`, `verify:conversion`, `verify:breakpoints`, `verify:no-viewport-read` verdi
- Nessun test automatico esiste per il prodotto: la prova e' la misura su Safari simulato scritta in `52-ESITI.md`, e la conferma sull'iPhone del proprietario resta da fare sul laboratorio
- La produzione non e' stata toccata: `origin/main` invariato
</verification>

<success_criteria>
- Login (email, password), ricerca della porta, select e codice sconto: `visualViewport.scale` 1 al tocco e dopo il blur, in verticale e in orizzontale
- Alla porta «QR Scan» e Alerts restano nello schermo a tastiera aperta
- Lo zoom a due dita resta possibile (D-41-08)
- D-52-28 `resta` confermato dalla misura, o riaperto e riportato prima del deploy
- `lab.resonatemotion.com` serve la correzione, READY; 52-15 puo' partire
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-19-SUMMARY.md` when done
</output>
