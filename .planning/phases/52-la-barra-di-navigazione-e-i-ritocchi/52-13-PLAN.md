---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 13
type: execute
wave: 5
depends_on: ["52-02", "52-03", "52-04", "52-05", "52-09", "52-10", "52-11", "52-12"]
files_modified:
  - supabase/migrations/20260923180100_gallery_close_data.sql
  - scripts/verify-refusal.mjs
  - scripts/verify-media-strip.mjs
  - scripts/probe-event-media-lab.mjs
  - src/types/database.ts
autonomous: true
requirements: [NAV-07, NAV-02]

must_haves:
  truths:
    - "M2 chiude: la lettura delle righe approvate di event_media chiede gallery.view, 'Anyone can view event media' non esiste piu', il bucket event-media e' privato, storage_path e' NOT NULL dopo il ribackfill delle righe nate fra M1 e il deploy (D-52-25)"
    - "Sul laboratorio l'ordine e' stato quello della produzione: M1 (piano 52-06) → deploy del codice della fase sul ramo lab, READY → M2; mai il contrario (vincolo §I.6)"
    - "Le sonde di P-52-G eseguibili da script sono state eseguite sul laboratorio dopo M2, con esiti registrati: anonimo non scarica un oggetto approvato, attendee legge 0 righe e non firma, staff firma un approvato e non un pending, organizer cancella oggetto e riga, un nuovo caricamento porta storage_path, una firma scaduta risponde errore"
    - "verify:refusal ha un gruppo gallery su event_media ed e' verde sul laboratorio; baseline:rls + baseline:compare mostrano come differenza solo le policy di NAV-07"
    - "I tipi dicono cio' che lo schema dice dopo M2: storage_path string, url string | null"
  artifacts:
    - path: "supabase/migrations/20260923180100_gallery_close_data.sql"
      provides: "policy di riga su gallery.view, via Anyone can view, bucket privato, ribackfill + NOT NULL, DO che solleva"
      contains: "event_media_select_gallery"
    - path: "scripts/probe-event-media-lab.mjs"
      provides: "le sonde 1-4, 6-8 di P-52-G, solo laboratorio"
      contains: "PRODUCTION_REF"
    - path: "scripts/verify-refusal.mjs"
      provides: "gruppo gallery in SECTION_TARGETS"
      contains: "section: \"gallery\""
  key_links:
    - from: "supabase/migrations/20260923180100_gallery_close_data.sql"
      to: "deploy del ramo lab (READY)"
      via: "testata NON APPLICARE PRIMA DEL DEPLOY DEL CODICE CHE FIRMA, e l'ordine eseguito in questo piano"
      pattern: "NON APPLICARE PRIMA"
---

<objective>
Scrivere M2, portare il laboratorio al codice della fase, chiudere il
laboratorio con M2 nell'ordine giusto, e misurare che e' chiuso.

Purpose: D-52-25 — «niente foto di una serata segreta puo' essere raggiungibile da
un URL non firmato». Il vincolo d'ordine (ricerca §I.6, Pitfall 10) e' duro: M2
prima del deploy rompe le immagini a tutti. Sul laboratorio lo si percorre **nello
stesso ordine** della produzione, perche' la prova vale solo se misura la stessa
sequenza. E su un laboratorio senza media le sonde misurano il vuoto: il banco
del piano 52-01 le ha rese possibili.
Output: M2, il gruppo `gallery` di `verify:refusal`, la prosa di
`verify:media-strip` corretta, lo script delle sonde, i tipi allineati; il
laboratorio con il codice della fase e M2 applicata; gli esiti delle sonde nel
SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-06-SUMMARY.md
@./CLAUDE.md
@.claude/rules/supabase-data.md
@.claude/rules/access-gating.md
@.claude/rules/media-and-storage.md
@.claude/rules/venue-secrecy.md
@.claude/rules/ai-engineering.md

<interfaces>
Policy in vigore su `public.event_media` (ricerca §I.1): `event_media_select_approved`
(SELECT `TO authenticated`, `status = 'approved'`, `20260225120000:25-27`),
`event_media_select_own`, `event_media_select_admin` (`staff.manage`), le
UPDATE/DELETE, `event_media_insert_staff` — **solo la prima si sostituisce**.
Su `storage.objects` per `event-media`: `"Anyone can view event media"` (SELECT
senza `TO`, `20260225120000:79-82`) → via; `event_media_objects_select_by_row`
(M1) resta; le due DELETE (`:86-101`) restano.
Forma del predicato di catalogo: `(SELECT private.has_capability('gallery.view'))`
(`20260817120400:188-191, 207`). Bucket: `UPDATE storage.buckets SET public =
false WHERE id = 'event-media';` (non `INSERT … ON CONFLICT DO NOTHING`, che non
corregge un bucket esistente, `20260817120400:116-120`).
Laboratorio servito da `lab.resonatemotion.com`, legato al ramo `lab`: il deploy
e' `git push origin main:lab` (precedente: `51-ESITI.md:278`, avanzamento veloce).
Sessioni di prova: `generateLink` + `verifyOtp` col service role, forma di
`scripts/verify-refusal.mjs:328-390`. `verify-refusal.mjs` e' READ-ONLY per
costruzione (auto-controllo dei verbi di scrittura, `:1-60`): il gruppo nuovo non
deve introdurne. `SECTION_TARGETS` a `:196-230`.
`scripts/verify-media-strip.mjs:185` `export const PUBLIC_BUCKET = 'event-media';`
e la prosa «the public bucket» — il controllo e' per letterale quotato: resta
verde, la prosa diventa falsa.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere M2, il gruppo gallery di verify:refusal, la prosa di verify:media-strip, i tipi e lo script delle sonde</name>
  <files>supabase/migrations/20260923180100_gallery_close_data.sql, scripts/verify-refusal.mjs, scripts/verify-media-strip.mjs, scripts/probe-event-media-lab.mjs, src/types/database.ts</files>
  <read_first>
    - `supabase/migrations/20260923180000_gallery_view_and_media_paths.sql` (M1, piano 52-06) — testata, forma, `DO`
    - `supabase/migrations/20260817120400_visual_archive_bucket.sql` per intero — bucket privato, «le due assenze», «WHAT TO DO INSTEAD» (`:257-266`)
    - `supabase/migrations/20260922120000_role_attendee_and_capability_keys.sql` righe 1-49 e 587-603
    - `scripts/verify-refusal.mjs` righe 1-120, 190-240, 320-400 e il punto in cui i gruppi si misurano
    - `scripts/verify-media-strip.mjs` righe 1-60 e 175-190
    - `scripts/seed-lab-door.mjs` righe 70-92 — il rifiuto della produzione per lo script delle sonde
    - `src/types/database.ts` — il tipo di `event_media` (piano 52-06)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §I.6, §I.8
  </read_first>
  <action>
    **M2** `supabase/migrations/20260923180100_gallery_close_data.sql` (timestamp
    strettamente maggiore di M1). Testata: **«NON APPLICARE PRIMA DEL DEPLOY DEL
    CODICE CHE FIRMA»** con la ragione (le pagine disegnerebbero indirizzi
    pubblici che rispondono errore: immagini rotte a tutti, Pitfall 10); il
    paragrafo «UNA TRANSAZIONE SOLA, E NON C'E' `BEGIN;`»; «COSA QUESTA MIGRATION
    NON FA» (non tocca `select_own`, `select_admin`, UPDATE/DELETE, le DELETE di
    storage, `event-images` e gli altri bucket pubblici); la **reversibilita'
    dichiarata**: si torna indietro solo allargando, e cio' che e' stato visto
    mentre era aperto resta visto (`venue-secrecy.md`).
    Corpo: (1) ribackfill `UPDATE public.event_media SET storage_path =
    substring(url from '/storage/v1/object/public/event-media/(.+)$') WHERE
    storage_path IS NULL AND url IS NOT NULL;` (le righe nate col codice vecchio
    fra M1 e il deploy); (2) `ALTER TABLE public.event_media ALTER COLUMN
    storage_path SET NOT NULL;`; (3) `DROP POLICY IF EXISTS
    event_media_select_approved ON public.event_media;` e `CREATE POLICY
    event_media_select_gallery ON public.event_media AS PERMISSIVE FOR SELECT TO
    authenticated USING (status = 'approved' AND (SELECT
    private.has_capability('gallery.view')));`; (4) `DROP POLICY IF EXISTS
    "Anyone can view event media" ON storage.objects;`; (5) `UPDATE
    storage.buckets SET public = false WHERE id = 'event-media';`; (6) il `DO` che
    rilegge e **solleva** se: una riga ha `storage_path` nullo; esiste in
    `pg_policies` una SELECT su `storage.objects` il cui `qual` nomina
    `event-media` e i cui `roles` non sono esattamente `{authenticated}`;
    `storage.buckets.public` per `event-media` non e' `false`;
    `event_media_select_gallery` manca; (7) `COMMENT ON POLICY` per la policy
    nuova e `COMMENT ON COLUMN … storage_path` aggiornato (NOT NULL da M2).
    La sezione «WHAT TO DO INSTEAD» ricopiata per `event-media`: la riparazione in
    buona fede («le miniature non caricano, apri il bucket») nominata per essere
    riconosciuta; la risposta giusta e' una firma che manca, non un bucket
    aperto. Assenze per descrizione (nessuna INSERT per `authenticated`, nessun
    braccio per l'anonimo).
    **`verify-refusal.mjs`**: un gruppo in piu' in `SECTION_TARGETS`, `section:
    "gallery"`, `tables: ["event_media"]`, `note` che dice: one SELECT arm asks
    gallery.view on approved rows (NAV-07, D-52-25); the uploader's own arm and the
    staff.manage arm stay and are not what this measures; master reads rows,
    attendee and anon read zero; on a database with no approved row the instrument
    refuses instead of passing. Nessun verbo di scrittura introdotto
    (l'auto-controllo del file deve restare verde).
    **`verify-media-strip.mjs`**: la prosa che chiama `event-media` «the public
    bucket» si corregge (privato dal 2026-09-23, NAV-07); la costante
    `PUBLIC_BUCKET` resta col suo nome e una riga che dice perche' (il nome e'
    pinnato da import/controlli; il controllo e' per letterale quotato e resta
    corretto). Nessun controllo cambia comportamento.
    **`database.ts`**: `storage_path: string` (NOT NULL da M2), `url: string |
    null` (nessuna superficie lo legge piu', piano 52-12), commento datato.
    **`scripts/probe-event-media-lab.mjs`** (solo laboratorio, rifiuto di
    `PRODUCTION_REF` come prima riga eseguita dopo gli argomenti): conia sessioni
    `attendee`, `staff` (senza `staff.manage`) e `organizer` del banco come
    `verify-refusal.mjs:328-390`; legge col service role le chiavi di un oggetto
    approvato, di uno pending e di una riga di serata segreta; esegue le sonde 1-4,
    6-8 di ricerca §I.8: (1) `GET` anonimo dell'indirizzo pubblico di un oggetto
    approvato → atteso **non 200**; (2) `attendee` PostgREST `event_media`
    `status=eq.approved` → **0 righe**; (3) `attendee` `createSignedUrls` su una
    chiave nota → **nessun URL**; (4) `staff` firma una chiave **pending** →
    rifiutata, una **approvata** (anche di serata segreta) → firmata e l'URL
    risponde 200; (6) `organizer` cancella un media di prova via la server action
    **non e' eseguibile da script**: la sonda 6 verifica invece col client di
    sessione `organizer` che `remove` sull'oggetto e `delete` sulla riga di un
    media di prova **seminato dallo script stesso** riescano entrambi, e riconta
    col service role; (7) la sonda del caricamento nuovo si rilegge dal catalogo
    dopo la corsa del piano 52-14 (qui: `--check-new-upload` legge l'ultima riga e
    controlla `storage_path` non nullo e `url` nullo); (8) firma con TTL di 5 s,
    attesa 10 s, `GET` → **non 200**. Stampa per ogni sonda: numero, atteso,
    osservato, esito, ora UTC — **mai** una chiave, un URL o un `id`.
  </action>
  <verify>
    <automated>f=supabase/migrations/20260923180100_gallery_close_data.sql; npm run build &amp;&amp; npm run verify:media-strip &amp;&amp; test -f $f &amp;&amp; test $(/usr/bin/grep -ciE "^\s*(BEGIN|COMMIT);" $f) -eq 0 &amp;&amp; /usr/bin/grep -q "NON APPLICARE PRIMA" $f &amp;&amp; /usr/bin/grep -q "event_media_select_gallery" $f &amp;&amp; /usr/bin/grep -q "public = false" $f &amp;&amp; /usr/bin/grep -q "SET NOT NULL" $f &amp;&amp; /usr/bin/grep -q 'section: "gallery"' scripts/verify-refusal.mjs &amp;&amp; node --check scripts/probe-event-media-lab.mjs &amp;&amp; LAB_PROJECT_REF=cjsfocnhfzycbbgkwocx node scripts/probe-event-media-lab.mjs; test $? -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify:media-strip` escono 0
    - M2 senza `BEGIN;`/`COMMIT;`, con testata «NON APPLICARE PRIMA DEL DEPLOY», ribackfill + `SET NOT NULL`, `event_media_select_gallery`, via «Anyone can view event media», `public = false`, `DO` che solleva sui quattro controlli, «WHAT TO DO INSTEAD»
    - `verify-refusal.mjs` porta il gruppo `gallery` senza nuovi verbi di scrittura
    - `database.ts`: `storage_path: string`, `url: string | null`
    - `probe-event-media-lab.mjs` rifiuta la produzione con exit 2 (provato) e non stampa chiavi, URL o UUID
  </acceptance_criteria>
  <done>La chiusura e le sue misure esistono, e nessuna delle due tocca ancora un database.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Il deploy nel mezzo, sul laboratorio — il ramo lab al codice della fase, READY</name>
  <files>.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-13-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-ESITI.md` righe 265-285 — come e' stato dispiegato il ramo `lab` e come si e' registrato
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-13-PLAN.md` task 3 (a) — le precondizioni di una spedizione su un repo pubblico
    - `.gitignore` — `docs/`, `.firecrawl/`, `.env*`
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-06-SUMMARY.md` — M1 applicata sul laboratorio (precondizione)
  </read_first>
  <action>
    Precondizioni, **misurate** e scritte nel SUMMARY prima del push: M1 applicata
    sul laboratorio (dal SUMMARY del 52-06 e riletta: `storage_path` esiste);
    `npm run build` e `npm run verify` verdi sul commit da spingere; nel diff fra
    `origin/lab` e `main`: zero file sotto `docs/` e `.firecrawl/`, zero `.env*`,
    zero occorrenze del ref del laboratorio, zero segreti (grep delle chiavi
    note); `lab` e' antenato di `main` (avanzamento veloce, nessuna riscrittura).
    Poi `git push origin main:lab` — **solo il ramo `lab`**, mai `main` (il push di
    `main` e' il deploy di produzione ed e' un passo dell'atto del piano 52-15).
    Attendere il dispiegamento `READY` del ramo `lab` su Vercel e registrare sha
    corto, ora di creazione e di `READY`. Verifica esterna: `curl -sI
    https://lab.resonatemotion.com/gallery` senza sessione → `location` contiene
    `/login?next=%2Fgallery`. Se il dispiegamento fallisce o il `curl` non
    corrisponde, **fermarsi**: M2 non si applica su un laboratorio che non serve il
    codice che firma.
  </action>
  <verify>
    <automated>curl -sI https://lab.resonatemotion.com/gallery | /usr/bin/grep -i "^location:" | /usr/bin/grep -q "login?next=%2Fgallery"</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY registra le precondizioni misurate, lo sha spinto su `lab`, l'ora di creazione e di `READY`
    - `main` non e' stato spinto (`git rev-parse origin/main` invariato prima/dopo, scritto nel SUMMARY)
    - `curl -sI https://lab.resonatemotion.com/gallery` da anonimo → `location` con `login?next=%2Fgallery`
  </acceptance_criteria>
  <done>Il laboratorio serve il codice che firma; ora, e solo ora, M2 si puo' applicare li'.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] M2 sul laboratorio, poi le sonde, verify:refusal e la linea di base RLS</name>
  <files>scripts/probe-event-media-lab.mjs</files>
  <read_first>
    - `supabase/migrations/20260923180100_gallery_close_data.sql`
    - `scripts/rls-baseline.mjs` e `scripts/rls-baseline-compare.mjs` — come si prende e si confronta la linea di base (non modificare `rls-baseline-compare.mjs`)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PROCEDURES.md` P-52-G
  </read_first>
  <action>
    Prima di M2: `npm run baseline:rls` contro il laboratorio (linea di base
    «prima»), con il nome del file risultante nel SUMMARY.
    Applicare M2 con `POST /v1/projects/$LAB_PROJECT_REF/database/migrations`
    (mai `db push`, mai `/database/query`). Rileggere dal catalogo, `read_only`:
    versione coniata; `storage_path` `is_nullable = NO`; `pg_policies`:
    `event_media_select_gallery` presente con `gallery.view` nel `qual`,
    `event_media_select_approved` assente, «Anyone can view event media»
    assente, `event_media_objects_select_by_row` presente; `storage.buckets.public`
    = `false` per `event-media`, `true` invariato per `event-images`.
    Poi `npm run baseline:rls` («dopo») e `npm run baseline:compare` fra i due: la
    differenza deve essere **solo** le policy di NAV-07 (M1 era gia' nella «prima»:
    qui cambiano `event_media_select_gallery`, `event_media_select_approved`,
    «Anyone can view»). Qualunque altra differenza: fermarsi e riportarla.
    Poi `node scripts/probe-event-media-lab.mjs`: le sonde 1-4, 6, 8 con esito
    atteso; la sonda 1 si registra con l'ora e, se risponde ancora 200 per cache,
    **si ripete dopo la finestra** (CDN fino al `max-age`, default 3600 s — Pitfall
    15) e si registrano entrambe le misure, senza dichiarare «chiuso» prima.
    Poi `npm run verify:refusal` contro il laboratorio: verde, con il gruppo
    `gallery`. `npm run verify:capabilities` contro il laboratorio: verde.
    Scrivere tutto nel SUMMARY con ore UTC. La sonda 5 (immagini e video da
    `organizer`, pausa e ripresa) e la 7 (nuovo caricamento) le percorre il
    proprietario nel piano 52-14.
  </action>
  <verify>
    <automated>set -a &amp;&amp; . ./.env.local &amp;&amp; . ./.env.lab.local &amp;&amp; set +a &amp;&amp; npm run verify:refusal &amp;&amp; npm run verify:capabilities &amp;&amp; node scripts/probe-event-media-lab.mjs</automated>
  </verify>
  <acceptance_criteria>
    - M2 risulta nella history del laboratorio con la versione coniata; le riletture del catalogo sono nel SUMMARY con l'ora UTC
    - `baseline:compare` mostra come differenza solo le policy di NAV-07 (output riassunto nel SUMMARY)
    - `probe-event-media-lab.mjs` esce 0 con le sonde 1-4, 6, 8 conformi; la sonda 1 registrata con l'ora e, se serve, ripetuta dopo la finestra di cache
    - `verify:refusal` (con il gruppo `gallery`) e `verify:capabilities` verdi contro il laboratorio
    - Zero scritture in produzione, dichiarato; lo stato rosso di `verify:refusal` contro la produzione (gruppo `gallery`) e' dichiarato con il piano che lo chiude (52-15)
  </acceptance_criteria>
  <done>Il laboratorio e' chiuso nell'ordine giusto, e le misure dicono che e' chiuso per chi deve e aperto per chi deve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → repo pubblico | `git push origin main:lab` e' una pubblicazione |
| chi esegue → laboratorio | M2 chiude il bucket del laboratorio |
| attendee / anonimo → righe e oggetti di `event_media` | il confine che M2 stringe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-61 | Denial of Service | M2 prima del deploy → immagini rotte | mitigate | task 3 dipende dal task 2 (READY + curl); testata di M2 lo dice |
| T-52-62 | Information disclosure | righe approvate lette da un attendee | mitigate | `event_media_select_gallery` (approvate E `gallery.view`); sonda 2 e `verify:refusal` gruppo `gallery` |
| T-52-63 | Information disclosure | oggetto scaricato per indirizzo pubblico | mitigate | `public = false` + via «Anyone can view»; sonda 1, ripetuta dopo la finestra di cache |
| T-52-64 | Elevation of privilege | staff che firma un pending indovinandone il path | mitigate | la policy dell'oggetto chiede la riga sotto RLS; sonda 4 |
| T-52-65 | Information disclosure | materiale privato spinto sul ramo pubblico | mitigate | precondizioni misurate prima del push (zero `docs/`, `.firecrawl/`, `.env*`, ref di lab, segreti); solo `lab`, mai `main` |
| T-52-66 | Tampering | una differenza RLS non voluta | mitigate | `baseline:rls` prima/dopo + `baseline:compare`: solo le policy di NAV-07 |
| T-52-67 | Repudiation | «chiuso» dichiarato mentre la cache serve | mitigate | sonda 1 con ora e ripetizione dopo la finestra (Pitfall 15) |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `npm run build`, `verify:media-strip` verdi; `verify:refusal`, `verify:capabilities` verdi contro il laboratorio
- Laboratorio: codice della fase servito (READY), M2 applicata, sonde conformi, differenza RLS limitata a NAV-07
</verification>

<success_criteria>
La sequenza M1 → deploy → M2 e' stata percorsa sul laboratorio esattamente come
la percorrera' la produzione, e le misure dicono che NAV-07 tiene.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-13-SUMMARY.md` when done
</output>
