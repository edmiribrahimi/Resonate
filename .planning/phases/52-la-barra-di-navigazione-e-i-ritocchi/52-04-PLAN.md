---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/events/page.tsx
  - src/app/(public)/events/FormatFilterRow.tsx
autonomous: true
requirements: [NAV-06]

must_haves:
  truths:
    - "Nella pagina eventi compare un chip solo per i format che hanno almeno una serata — passata o futura — visibile a chi guarda (NAV-06, D-52-15)"
    - "I chip si ricavano dall'array di eventi gia' letto, PRIMA del filtro per format, e mai da una seconda interrogazione: selezionare un chip non fa sparire gli altri (D-52-15)"
    - "Un format ritirato con serate visibili mostra il suo chip (D-52-16)"
    - "Con zero serate visibili la riga dei chip non si monta affatto: niente 'All' da solo (D-52-17)"
    - "Nome, colore e ordine del chip vengono dal catalogo, mai da CardFormat.name (che puo' essere il nome di una serie, e una serie quello di una sede)"
  artifacts:
    - path: "src/app/(public)/events/page.tsx"
      provides: "lo Set degli slug visibili dall'array, i chip = catalogo ∩ slug visibili, l'allow-list di ?format= = i chip"
      contains: "D-52-15"
    - path: "src/app/(public)/events/FormatFilterRow.tsx"
      provides: "la riga assente con zero chip"
      contains: "D-52-17"
  key_links:
    - from: "src/app/(public)/events/page.tsx (allEvents)"
      to: "FormatFilterRow formats prop"
      via: "Set degli slug calcolato prima di shownEvents"
      pattern: "visibleFormatSlugs"
---

<objective>
NAV-06: i chip dei format nascono dalle serate che chi guarda sta gia' vedendo.

Purpose: D-52-15 rovescia di proposito D-36-13/D-36-16 («i chip non variano con
chi guarda»). La riga varia con chi guarda **ma non gli mostra nulla che non
stia gia' vedendo** — e l'unico modo di garantirlo e' ricavarla dall'array gia'
filtrato dalla RLS, mai da una lettura del tipo «questo format ha qualcosa?»,
che e' il canale che rivela una bozza senza mostrarla (`venue-secrecy.md`).
Output: `events/page.tsx` e `FormatFilterRow.tsx` riscritti su questa regola.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-CONTEXT.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md
@./CLAUDE.md
@.claude/rules/venue-secrecy.md
@.claude/rules/nextjs-architecture.md

<interfaces>
`src/app/(public)/events/page.tsx` oggi:
- `:183-220` catalogo FUORI dal try: `from("formats").select("id, slug, name, color").eq("listed", true).is("retired_at", null).order("sort_order").order("name")`, `throw` con `[events.catalogue_read_failed]`
- `:222` `const formatOptions: FormatOption[] = formatRows ?? [];`
- `:237-239` `activeFormatOption = formatOptions.find((f) => f.slug === formatParam) ?? null; activeFormat = activeFormatOption?.slug ?? null;` — FUORI dal try (P6)
- `:577` `const allEvents = (events ?? []).map(transformEvent);` — DENTRO il try
- `:584-586` `shownEvents = activeFormat ? allEvents.filter((e) => e.formats.some((f) => f.slug === activeFormat)) : allEvents;`
- `:662-664` `<FormatFilterRow formats={formatOptions} activeFormat={activeFormat} … />`; `:680-682` stato vuoto filtrato che legge `activeFormatOption.name`
- due stringhe esatte pinnate da `verify:venue-surfaces` (`verify-venue-surfaces.mjs:535-567`): `venue_label: nightIsSecret ? null :` e `const nightIsSecret = p.venue_secret !== false;` — **non toccarle**
`FormatFilterRow.tsx:155-175` disegna sempre il `Link` «All».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chip dall'array, prima del filtro; allow-list = chip; catalogo senza retired_at</name>
  <files>src/app/(public)/events/page.tsx</files>
  <read_first>
    - `src/app/(public)/events/page.tsx` righe 180-300 (catalogo, allow-list, `canSeeDrafts`) e 520-690 (`transformEvent`, `CardFormat`, `allEvents`, filtro, render)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-RESEARCH.md` §E (righe 574-595)
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-PATTERNS.md` P6 e la sezione `events/page.tsx` (righe 632-654)
    - `scripts/verify-venue-surfaces.mjs` righe 535-567 — le due stringhe da non toccare
  </read_first>
  <action>
    (1) Catalogo: togliere `.is("retired_at", null)` (D-52-16); `.eq("listed",
    true)`, gli `order` e il `throw` con `[events.catalogue_read_failed]` restano
    fuori dal try. Riscrivere il commento di testa: il paragrafo «the chip row must
    not vary with the data OR with the viewer» e' **rovesciato** da D-52-15 il
    2026-09-23, con la ragione (la riga varia con chi guarda ma non gli mostra
    nulla che non stia gia' vedendo, perche' si ricava dall'array gia' filtrato
    dalla RLS); il paragrafo su `listed` / `retired_at` dice ora che ritirato
    significa «nessuna serata nuova», non «le vecchie spariscono» (D-52-16); il
    paragrafo «No count, no join … A count is the one channel that reveals an
    unannounced night» **resta** com'e'.
    (2) P6 — la risoluzione del filtro si sposta: dichiarare fuori dal try
    `let chips: FormatOption[] = []` e `let activeFormatOption: FormatOption |
    null = null`. Dentro il try, **subito dopo** `allEvents` e **prima** di
    `shownEvents`: `const visibleFormatSlugs = new Set(allEvents.flatMap((e) =>
    e.formats.map((f) => f.slug)))`; `chips = formatOptions.filter((f) =>
    visibleFormatSlugs.has(f.slug))` (nome, colore e ordine dal catalogo — dall'array
    **solo lo slug**, perche' `CardFormat.name` puo' essere il nome pubblico di
    una serie, e una serie puo' portare il nome di una sede); poi
    `activeFormatOption = chips.find((f) => f.slug === formatParam) ?? null` e
    `activeFormat` da quello. L'allow-list di `?format=` diventa **i chip**: un
    format senza serate visibili risolve come «unknown, retired, unlisted,
    repeated or absent» — nessun filtro, «All» corrente, nessun redirect.
    Aggiornare il commento dell'allow-list con questa riga e con la ragione (nessun
    chip selezionato fuori dalla riga). Nel `catch`, `chips` e
    `activeFormatOption` restano ai valori vuoti.
    (3) Passare `chips` (non `formatOptions`) a `FormatFilterRow`; lo stato vuoto
    filtrato continua a leggere `activeFormatOption`.
    Nessuna query nuova, nessun conteggio passato ai figli. Le due stringhe di
    `verify:venue-surfaces` restano identiche.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:venue-surfaces &amp;&amp; test $(/usr/bin/grep -c 'is("retired_at", null)' "src/app/(public)/events/page.tsx") -eq 0 &amp;&amp; /usr/bin/grep -q "visibleFormatSlugs" "src/app/(public)/events/page.tsx" &amp;&amp; /usr/bin/grep -q "D-52-15" "src/app/(public)/events/page.tsx" &amp;&amp; test $(/usr/bin/grep -c '\.from("formats")' "src/app/(public)/events/page.tsx") -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` e `npm run verify:venue-surfaces` escono 0
    - Zero `.is("retired_at", null)` nel file; una sola lettura `.from("formats")` (nessuna query nuova)
    - `visibleFormatSlugs` e' calcolato fra `allEvents` e `shownEvents` (ordine delle righe nel sorgente)
    - `activeFormatOption` e' risolto su `chips`, non su `formatOptions`
    - `FormatFilterRow` riceve `chips`
    - Il commento di testa cita D-52-15 e D-52-16 e conserva il paragrafo «No count, no join»
  </acceptance_criteria>
  <done>La riga dei chip e' ricavata da cio' che chi guarda gia' vede, prima del filtro.</done>
</task>

<task type="auto">
  <name>Task 2: Con zero chip la riga non esiste (D-52-17)</name>
  <files>src/app/(public)/events/FormatFilterRow.tsx</files>
  <read_first>
    - `src/app/(public)/events/FormatFilterRow.tsx` per intero — la docblock, il `Link` «All», la mappa dei chip
    - `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-UI-SPEC.md` §E riga NAV-06
  </read_first>
  <action>
    In testa al componente: se `formats.length === 0` restituisce `null` — nessun
    `<nav>`, nessun «All», nessuno spazio riservato. Commento accanto: D-52-17,
    2026-09-23 — con zero serate visibili un «All» da solo sarebbe un filtro senza
    scelta; lo stato vuoto della lista resta quello di `EventTabs`, invariato.
    Aggiornare la docblock del file se dice che la riga e' sempre presente o che i
    chip «non variano con chi guarda» (riscrivere con D-52-15).
    Nessun'altra modifica al disegno dei chip.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; /usr/bin/grep -q "D-52-17" "src/app/(public)/events/FormatFilterRow.tsx" &amp;&amp; /usr/bin/grep -q "formats.length === 0" "src/app/(public)/events/FormatFilterRow.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `FormatFilterRow` restituisce `null` quando `formats.length === 0`, con il commento D-52-17
    - Nessuna classe dei chip cambia (`git diff` limitato al ramo vuoto e ai commenti)
  </acceptance_criteria>
  <done>Senza serate visibili la pagina non disegna un filtro vuoto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database (RLS su events/formats) → pagina | l'array arriva gia' filtrato per chi guarda |
| `?format=` (input) → filtro | parametro controllato dal visitatore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-10 | Information disclosure | bozza rivelata da un chip | mitigate | chip dal solo `Set` degli slug di `allEvents` (gia' filtrato dalla RLS: le bozze solo a chi tiene `staff.manage`); nessuna query «ha serate?», verificato dal conteggio di `.from("formats")` = 1 |
| T-52-11 | Information disclosure | nome di sede in un chip (serie) | mitigate | nome e colore dal catalogo; dall'array solo lo slug |
| T-52-12 | Information disclosure | oracolo su `?format=` | mitigate | allow-list = chip; slug sconosciuto, ritirato senza serate o assente risolvono uguale, nessun redirect |
| T-52-13 | Tampering | le due stringhe di `verify:venue-surfaces` | mitigate | non toccate; `npm run verify:venue-surfaces` nel comando di verifica |
| T-52-SC | Tampering | npm install | accept | nessun pacchetto installato |
</threat_model>

<verification>
- `npm run build`, `npm run verify:venue-surfaces` verdi
- P-52-D sul laboratorio (manuale)
</verification>

<success_criteria>
NAV-06 e' vero, e la riga dei chip non puo' dire nulla che l'elenco sotto non
dica gia'.
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-04-SUMMARY.md` when done
</output>
