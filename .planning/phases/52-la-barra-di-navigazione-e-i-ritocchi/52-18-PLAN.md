---
phase: 52-la-barra-di-navigazione-e-i-ritocchi
plan: 18
type: execute
wave: 7
depends_on: ["52-14"]
files_modified:
  - src/app/(members)/account/page.tsx
  - src/app/(admin)/admin/members/actions.ts
  - src/app/(admin)/admin/members/CreateAccountForm.tsx
autonomous: true
gap_closure: true
requirements: [NAV-05]

must_haves:
  truths:
    - "Uno `staff` che apre Account legge «Staff» nel badge del ruolo, e nessuna riga della pagina gli dice «Attendee» (difetto 2, gate lessicale: `attendee` e' l'account leggero di chi ha comprato)"
    - "master legge «Admin», organizer «Organizer», attendee (o nessuna riga di profilo) «Attendee»: le etichette esistenti non cambiano"
    - "Creare un account con un indirizzo che l'autenticazione rifiuta come malformato (per esempio con il punto finale lasciato dalla tastiera iOS) mostra un avviso che dice che l'indirizzo non e' valido e che nulla e' stato creato ne' inviato — non piu' «The write failed» (difetto 3)"
    - "`write_failed` resta la frase dei guasti veri di scrittura, e ogni etichetta della mappa ha la sua voce: il typecheck di `Record<NoticeKind, …>` lo impone"
  artifacts:
    - path: "src/app/(members)/account/page.tsx"
      provides: "etichetta del ruolo esaustiva sui quattro valori di UserRole"
      contains: "Record<UserRole, string>"
    - path: "src/app/(admin)/admin/members/actions.ts"
      provides: "l'etichetta address_refused, dai codici validation_failed / email_address_invalid dell'autenticazione"
      contains: "address_refused"
    - path: "src/app/(admin)/admin/members/CreateAccountForm.tsx"
      provides: "la voce address_refused nella mappa NOTICES"
      contains: "address_refused:"
  key_links:
    - from: "src/app/(admin)/admin/members/actions.ts (ramo authError di createAccount)"
      to: "NOTICES.address_refused in CreateAccountForm.tsx"
      via: "CreateAccountFailure → NoticeKind → Record<NoticeKind, …>"
      pattern: "address_refused"
    - from: "src/app/(members)/account/page.tsx"
      to: "src/types/database.ts UserRole"
      via: "mappa esaustiva ROLE_LABEL"
      pattern: "Record<UserRole, string>"
---

<objective>
Chiudere i difetti 2 e 3 trovati dalla corsa del piano 52-14: la pagina Account di
uno `staff` dice «Attendee», e un indirizzo rifiutato dall'autenticazione come
malformato produce «The write failed».

Purpose: il difetto 2 viola il gate lessicale del progetto (`attendee` e' l'account
leggero di chi ha comprato un biglietto: dire a uno staff che e' un attendee e' dirgli
il ruolo sbagliato, e a chi guarda il suo schermo alla porta dare un'informazione
falsa). Il difetto 3 e' il pattern del newsletter di `meta-gates.md`: una causa
(l'indirizzo) raccontata con la frase di un'altra (un guasto del database). Entrambi
stanno sulla strada della produzione: 52-15 non parte prima che siano chiusi.
Output: tre file di prodotto corretti, `npm run build` verde, la prova sul laboratorio
nel SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/meta-gates.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-14-SUMMARY.md
@.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md

<interfaces>
Da src/types/database.ts:99 — `export type UserRole = "master" | "organizer" | "staff" | "attendee";`
(gia' importato in account/page.tsx:17 come `import type { UserRole } from "@/types/database";`)

Da src/app/(members)/account/page.tsx, oggi:
- :232 `const fullName = user.user_metadata?.full_name || "Attendee";`
- :246-252 `attendeeSince` (mese e anno da `profile.created_at`)
- :254-259 `roleLabel` = master → "Admin", organizer → "Organizer", ogni altro → "Attendee"  ← il difetto 2
- :271 `const isAttendeeRole = !profile?.role || profile.role === "attendee";` (NON si tocca: decide i biglietti)
- :487 `<Badge className="mt-2 shrink-0">{roleLabel}</Badge>`
- :490-492 `Attendee since {attendeeSince}` — la stessa parola sbagliata, sotto il badge, per ogni ruolo

Da src/app/(admin)/admin/members/actions.ts:
- :1358-1369 `export type CreateAccountFailure = "capabilities_unavailable" | "forbidden" | "invalid_input" | "app_url_missing" | "already_exists" | "profile_missing" | "constraint_refused" | "write_failed" | "invitation_link_failed" | "invitation_link_misaimed" | "invitation_send_failed";`
- :1334-1356 il docblock che dice «Eleven causes rather than a tidy three…»
- :1586-1602 il ramo `if (authError || !created?.user)`: riconosce `email_exists` / `user_already_exists` → `already_exists`; tutto il resto → log `[members.write_failed] createAccount: auth code=… status=…` e `{ ok: false, failure: "write_failed", detail: code ?? "auth" }`  ← il difetto 3

Da src/app/(admin)/admin/members/CreateAccountForm.tsx:
- :43 `type NoticeKind = CreateAccountFailure | "transport_unavailable";` (commento: «The client-only twelfth case»)
- :58 `const NOTICES: Record<NoticeKind, { title: string; body: string }> = { … }` — mappa ESAUSTIVA per tipo: aggiungere un'etichetta all'unione senza la voce e' un errore di build
- :115-121 `write_failed` («The write failed» / «The database or the auth service refused or did not answer…»)
- :178-182 `ROLE_OPTIONS` — le etichette «Attendee», «Staff», «Organizer» sono «this product's word for it, chosen once and used identically on the members table»
- :421 `const notice = NOTICES[kind];`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: L'etichetta del ruolo su Account — esaustiva sui quattro ruoli (difetto 2)</name>
  <files>src/app/(members)/account/page.tsx</files>
  <read_first>
    - src/app/(members)/account/page.tsx (righe 225-275 e 460-495: `fullName`, `attendeeSince`, `roleLabel`, `isAttendeeRole`, il badge e la riga «since»)
    - src/types/database.ts (righe 95-110: `UserRole` e la nota D-51-06)
    - src/app/(admin)/admin/members/CreateAccountForm.tsx (righe 165-182: `ROLE_OPTIONS`, le parole del prodotto per i ruoli)
    - .claude/rules/access-gating.md (lessico: `attendee` non e' «socio», e uno staff non e' un attendee)
  </read_first>
  <action>
    Sostituire il ternario `roleLabel` (:254-259) con una costante di modulo `ROLE_LABEL: Record<UserRole, string>` — `master: "Admin"`, `organizer: "Organizer"`, `staff: "Staff"`, `attendee: "Attendee"` — e calcolare `roleLabel` come `ROLE_LABEL[profile?.role ?? "attendee"]`. Il fallback su «Attendee» quando non c'e' riga di profilo resta, ed e' coerente con `isAttendeeRole` (:271), che NON si tocca: decide chi vede i biglietti, e non e' parte del difetto. Il `Record` e' esaustivo per tipo: un quinto ruolo futuro senza etichetta diventa un errore di build invece di un'altra ricaduta silenziosa su «Attendee» — scriverlo in un commento di due righe accanto alla costante, in italiano, col riferimento al difetto 2 di `52-ESITI.md`. Le parole «Staff», «Organizer», «Attendee» sono quelle di `ROLE_OPTIONS` in CreateAccountForm.tsx (la parola del prodotto, usata uguale sulla tabella dei membri); «Admin» per master resta com'e'.
    Stessa parola sbagliata, stessa pagina: la riga sotto il badge (:490-492) oggi dice «Attendee since {mese anno}» a ogni ruolo. Farla dire `{roleLabel} since {attendeeSince}` (uno staff legge «Staff since September 2026», un attendee legge esattamente quello di prima); rinominare la variabile `attendeeSince` in `accountSince` in tutte le sue occorrenze. E il saluto (:232): il fallback del nome quando `full_name` manca passa da «Attendee» a `roleLabel`, spostando il calcolo di `fullName` dopo quello di `roleLabel` (che a sua volta segue la lettura di `profile`). Nessun altro cambiamento alla pagina: niente query nuove, niente policy, niente ramo dei biglietti.
  </action>
  <verify>
    <automated>F='src/app/(members)/account/page.tsx'; /usr/bin/grep -c 'Record<UserRole, string>' "$F" && /usr/bin/grep -n 'staff: "Staff"' "$F" && test "$(/usr/bin/grep -v '^\s*//' "$F" | /usr/bin/grep -c 'Attendee since')" = "0" && test "$(/usr/bin/grep -c 'attendeeSince' "$F")" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'Record<UserRole, string>' 'src/app/(members)/account/page.tsx'` ≥ 1
    - la costante porta le quattro chiavi: `master: "Admin"`, `organizer: "Organizer"`, `staff: "Staff"`, `attendee: "Attendee"` (quattro grep, ognuno ≥ 1)
    - nessuna riga di codice (commenti esclusi) contiene `Attendee since`; zero occorrenze di `attendeeSince`
    - `isAttendeeRole` e' byte-identico a prima (`/usr/bin/grep -n 'const isAttendeeRole = !profile?.role || profile.role === "attendee";'` ≥ 1)
    - il ternario `: "Attendee";` di :259 non esiste piu'
  </acceptance_criteria>
  <done>Il badge e la riga «since» dicono il ruolo vero per tutti e quattro i ruoli; la mappa e' esaustiva per tipo.</done>
</task>

<task type="auto">
  <name>Task 2: `address_refused` — l'indirizzo rifiutato ha la sua frase (difetto 3)</name>
  <files>src/app/(admin)/admin/members/actions.ts, src/app/(admin)/admin/members/CreateAccountForm.tsx</files>
  <read_first>
    - src/app/(admin)/admin/members/actions.ts (righe 1320-1405: ordine, docblock «Eleven causes», `CreateAccountFailure`, `asCreateFailure`; righe 1504-1605: controlli d'ingresso e ramo `authError`)
    - src/app/(admin)/admin/members/CreateAccountForm.tsx (righe 1-160: il docblock «Never add a shared fallback string here», `NoticeKind`, `NOTICES`; riga ~421 dove la voce si legge)
    - .claude/rules/meta-gates.md (sezione «Controllo zero fallimenti silenziosi»: il precedente del newsletter)
    - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md (sezione «Difetto 3»)
  </read_first>
  <action>
    In actions.ts aggiungere all'unione `CreateAccountFailure` l'etichetta `address_refused`, subito dopo `invalid_input`, e aggiornare il docblock sopra l'unione: «Eleven causes» diventa «Twelve causes», con un punto elenco che dice che `address_refused` e' l'indirizzo che l'autenticazione — l'autorita' sull'indirizzo, come dice gia' il commento dei controlli d'ingresso — ha rifiutato come malformato: nulla e' stato creato, nulla inviato, e il rimedio e' correggere l'indirizzo, non riprovare.
    Nel ramo `if (authError || !created?.user)` di `createAccount`, dopo il riconoscimento di `email_exists` / `user_already_exists` e prima della ricaduta su `write_failed`, riconoscere `code === "validation_failed" || code === "email_address_invalid"`: loggare `[members.address_refused] createAccount: auth code=<code> status=<status>` (MAI l'indirizzo ne' il corpo dell'errore — stessa regola del log accanto) e restituire `{ ok: false, failure: "address_refused", detail: code }`. Commento breve accanto, in italiano: i due codici sono quelli che GoTrue usa per un indirizzo malformato; il primo e' stato osservato dal proprietario il 2026-09-23 con un punto finale lasciato dalla tastiera iOS (difetto 3 di `52-ESITI.md`); il secondo si riconosce per la stessa ragione per cui si riconoscono entrambe le etichette del duplicato. NON aggiungere un controllo stringa sul punto finale nei controlli d'ingresso: il commento esistente dice che l'autenticazione e' l'autorita' e che la stringa ferma solo il vuoto e l'ovvio — un secondo validatore divergerebbe dal primo.
    In CreateAccountForm.tsx aggiungere a `NOTICES` la voce `address_refused` subito dopo `invalid_input`. Titolo: «That address was refused as not valid». Corpo, in inglese come il resto del modulo (una lingua sola per percorso): l'autenticazione ha rifiutato l'indirizzo cosi' com'e' scritto — la causa tipica e' un punto o uno spazio in coda lasciato dalla tastiera del telefono; nessun account e' stato creato e nessun messaggio e' partito; si corregge l'indirizzo e si riprova, e riprovare e' sicuro perche' non esiste nulla da ripulire. La voce `write_failed` NON cambia: resta la frase dei guasti veri. Aggiornare il commento di `NoticeKind` («twelfth case» → «thirteenth case»). Nessuna stringa di ricaduta condivisa: il docblock del file lo vieta, e il `Record<NoticeKind, …>` rende il typecheck la prova che ogni etichetta ha la sua voce — la verifica «altre etichette senza voce» del piano e' quel typecheck, e se `npm run build` passa, non ce ne sono.
  </action>
  <verify>
    <automated>A='src/app/(admin)/admin/members/actions.ts'; C='src/app/(admin)/admin/members/CreateAccountForm.tsx'; test "$(/usr/bin/grep -c '"address_refused"' "$A")" -ge 2 && /usr/bin/grep -n 'validation_failed' "$A" && /usr/bin/grep -n 'email_address_invalid' "$A" && test "$(/usr/bin/grep -c 'address_refused:' "$C")" = "1" && /usr/bin/grep -n 'title: "The write failed"' "$C" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `"address_refused"` compare in actions.ts almeno due volte (unione e `return`); `validation_failed` e `email_address_invalid` compaiono nel ramo `authError`
    - il log della nuova etichetta e' `[members.address_refused]` e non interpola `email` (grep: nessuna riga con `address_refused` e `${email}`)
    - `address_refused:` compare esattamente una volta in CreateAccountForm.tsx, con un titolo che contiene «not valid»
    - `title: "The write failed"` e' ancora presente e il suo corpo e' byte-identico a prima
    - nessun nuovo controllo sul punto finale nei controlli d'ingresso (la riga `if (!email || !email.includes("@") || email.includes(" "))` e' invariata)
    - `npm run build` esce 0 (e' il typecheck: prova che `NOTICES` copre ogni etichetta)
  </acceptance_criteria>
  <done>Un indirizzo malformato produce un avviso che parla dell'indirizzo; i guasti veri tengono la loro frase; il build e' verde.</done>
</task>

<task type="auto">
  <name>Task 3: La prova sul laboratorio — lo staff legge «Staff», il punto finale legge «not valid»</name>
  <files>(nessun file del repo — esiti nel SUMMARY del piano)</files>
  <read_first>
    - scripts/dev-lab.sh (rifiuta la produzione come prima cosa; la porta si legge dal log)
    - ~/.claude/references/browser-cdp.md (Chrome CDP sulla 9222, profilo isolato)
    - /Users/etiesse/.claude/projects/-Users-etiesse-Resonate/memory/lab-pauses-after-a-week.md (NXDOMAIN sull'host del laboratorio = progetto in pausa: ripristino dal Management API prima delle prove)
    - .planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-ESITI.md (sezione PRE-LAB: i soggetti del banco per ruolo, senza indirizzi). Gli account: `door@lab.invalid` e' lo staff assegnato, `master@lab.invalid` il master; un `attendee` con la password del banco, se PRE-LAB ne registra uno
  </read_first>
  <action>
    Solo sul laboratorio, mai in produzione (`meta-gates.md`, laboratorio permanente). Avviare `npm run dev:lab` in background con il log nella scratchpad della sessione, leggere la porta dalla riga «Local:» del log (la 3000 puo' essere occupata). La password del banco e' `LAB_ACCOUNT_PASSWORD` da `.env.lab.local`, letta con `node --env-file=.env.lab.local -e 'console.log(process.env.LAB_ACCOUNT_PASSWORD)'` e passata al browser senza mai scriverla in `.planning/`, nel SUMMARY o in un commit.
    Prova 1 (difetto 2), con Chrome via CDP a 390 px: accesso come `door@lab.invalid` (staff), aprire `/account`, leggere il testo del badge del ruolo e la riga sotto l'indirizzo; atteso «Staff» e «Staff since <Mese Anno>», zero occorrenze di «Attendee» nel testo della pagina (`document.body.innerText`). Ripetere da `master@lab.invalid`: atteso «Admin». Se il banco ha un `attendee` (PRE-LAB), ripetere: atteso «Attendee» e «Attendee since …».
    Prova 2 (difetto 3): come master su `/admin/members`, aprire il modulo di creazione, indirizzo `gap-check-5218@lab.invalid.` (punto finale), un nome, ruolo `staff`, invio. Atteso: il titolo «That address was refused as not valid»; nel log del dev server una riga `[members.address_refused] createAccount: auth code=validation_failed` (o `email_address_invalid`) SENZA l'indirizzo. Poi rileggere la lista dei membri: nessun account con quell'indirizzo (nulla creato). Se l'autenticazione del laboratorio restituisse un codice diverso dai due, fermarsi e registrarlo: il codice misurato decide la correzione, non questo piano.
    Registrare nel SUMMARY, con l'ora UTC di ciascuna: le due prove, i testi letti, la riga di log (senza indirizzo), il conteggio dei membri prima/dopo. Chiudere il dev server alla fine.
  </action>
  <verify>
    <automated>npm run build && npm run verify:touch-targets && npm run verify:conversion</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY registra: staff → badge «Staff» e riga «Staff since …», zero «Attendee» nel testo di `/account`; master → «Admin»; attendee (se presente sul banco) → «Attendee»
    - Il SUMMARY registra: indirizzo con punto finale → titolo «That address was refused as not valid»; riga di log `[members.address_refused]` con il codice dell'autenticazione e senza l'indirizzo; numero di membri invariato prima/dopo
    - Nessuna password, nessun ref del laboratorio, nessun UUID nel SUMMARY
    - `npm run build`, `verify:touch-targets`, `verify:conversion` escono 0
  </acceptance_criteria>
  <done>I due difetti sono chiusi e provati sul laboratorio, con gli esiti orari nel SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → servizio di autenticazione | `createAccount` inoltra l'indirizzo a GoTrue con la chiave di servizio; la risposta d'errore torna come etichetta |
| server → schermo dell'operatore | l'avviso e' l'unico posto in cui il guasto esiste (nessun error tracking) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-80 | Information Disclosure | log di `address_refused` in actions.ts | mitigate | il log porta solo codice e status, mai l'indirizzo ne' il corpo dell'errore (stessa regola del log `write_failed` accanto); criterio di accettazione del task 2 |
| T-52-81 | Repudiation / Integrity | avviso fuorviante su un rifiuto | mitigate | etichetta propria e frase propria; `write_failed` invariato per i guasti veri; `Record<NoticeKind>` fa del build la prova dell'esaustivita' |
| T-52-82 | Spoofing (informativo) | badge del ruolo su `/account` | mitigate | `Record<UserRole, string>` esaustivo: uno staff non si legge piu' «Attendee»; nessun cambio a `isAttendeeRole`, alle query o alle policy — l'etichetta e' UX, la sicurezza resta nella RLS |
| T-52-83 | Tampering | prova sul laboratorio | mitigate | `dev:lab` rifiuta la produzione come prima riga; nessuna scrittura in produzione; il tentativo di creazione con punto finale non crea nulla (conteggio prima/dopo) |
</threat_model>

<verification>
- `npm run build` verde (typecheck: ruoli e avvisi esaustivi)
- `npm run verify:touch-targets`, `npm run verify:conversion` verdi
- Prova manuale scritta ed eseguita sul laboratorio (task 3), esiti orari nel SUMMARY
- Nessun test automatico esiste per il prodotto: nessuna affermazione «i test passano»
</verification>

<success_criteria>
- Uno staff su Account legge «Staff», mai «Attendee»; gli altri ruoli leggono le etichette di prima
- Un indirizzo malformato produce «That address was refused as not valid», nulla creato, nulla inviato
- `write_failed` resta la frase dei guasti veri
</success_criteria>

<output>
Create `.planning/phases/52-la-barra-di-navigazione-e-i-ritocchi/52-18-SUMMARY.md` when done
</output>
