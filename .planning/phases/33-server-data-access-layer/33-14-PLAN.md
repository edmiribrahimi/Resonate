---
phase: 33-server-data-access-layer
plan: 14
type: execute
wave: 3
depends_on: ["33-02", "33-03", "33-04", "33-05", "33-06", "33-07", "33-08", "33-09", "33-10", "33-11", "33-12", "33-13"]
files_modified:
  - src/lib/supabase/middleware.ts
  - src/app/(members)/dashboard/page.tsx
autonomous: false
requirements: [CAP-05]

must_haves:
  truths:
    - "Nothing under src/ reads an x-user-* header except the middleware that deletes them"
    - "The middleware no longer manufactures an identity header, and still deletes any inbound one"
    - "A user bounced by a capability lookup that failed can tell that apart from a refusal"
    - "A forged identity header is answered exactly as an anonymous request, proved with a positive control"
    - "CAP-03 is clean on both targets at the end of the phase, and every role reaches exactly what it reached before"
  artifacts:
    - path: "src/lib/supabase/middleware.ts"
      provides: "the strip kept and re-argued, the injection deleted"
      contains: "requestHeaders.delete"
  key_links:
    - from: "scripts/verify-no-header-identity.mjs"
      to: "src/"
      via: "the mechanical CAP-05 assertion"
      pattern: "verify-no-header-identity"
---

<objective>
Close the phase in the only order that is safe, and prove it.

Both halves of `src/lib/supabase/middleware.ts:199-223` are involved and they get
**opposite verdicts**:

```
:201  const requestHeaders = new Headers(request.headers);
:210  requestHeaders.delete("x-user-role");     ┐
:211  requestHeaders.delete("x-user-status");   │  THE STRIP  — KEEP. Forever.
:212  requestHeaders.delete("x-user-id");       ┘
:217  requestHeaders.delete(CAPABILITY_DIAGNOSTIC_HEADER);
:219  if (user) {
:220    requestHeaders.set("x-user-role", …);   ┐
:221    requestHeaders.set("x-user-status", …); │  THE INJECTION — DELETE.
:222    requestHeaders.set("x-user-id", …);     ┘
:223  }
```

**The order is not negotiable.** Delete the injection before the readers are
gone and every `headers().get()` returns `null`, so `role = null`, so
`if (role !== "master") redirect("/dashboard")`, so **every master is locked out
of every `/admin` surface**. It fails closed, so it is an availability break and
not a security one — but it is total, and `npm run build` cannot see it. That is
why this plan is the last wave and why its first task is a census, not an edit.

**Keeping the strip is a recommendation worth arguing, and this plan argues it.**
The counter-argument — *"a header nobody reads is a trap for the next person"* —
applies to the **injection**, which manufactures a value that looks
authoritative. It does not apply to the **deletion**, which manufactures nothing
and costs three lines. The failure it prevents is silent and security-relevant:
the next person who writes `headers().get("x-user-role")`, from muscle memory or
by copying a pattern out of git history, gets attacker input with **no**
protection at all. Three lines against a silent elevation of privilege is the
asymmetric-cost choice. What changes is the comment: it no longer protects
readers, it prevents a reader from being born.

WR-04 is in scope by owner decision: this plan edits that exact block, and it
will never be cheaper.

Purpose: criteria 1, 2 and 4, and the structural claim that makes criterion 1 a
property of the repository rather than a fact about one commit.

Output: a middleware that manufactures nothing, a visible degraded path, and the
phase's evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-server-data-access-layer/33-RESEARCH.md
@.planning/phases/33-server-data-access-layer/33-VALIDATION.md
@.planning/phases/33-server-data-access-layer/33-02-SUMMARY.md
@.planning/phases/32-capability-model-in-the-database/32-REVIEW.md
@.planning/phases/32-capability-model-in-the-database/32-CARRY-FORWARD.md
@CLAUDE.md
@.claude/rules/meta-gates.md
@.claude/rules/access-gating.md
@.claude/rules/checkin-offline.md

@src/lib/supabase/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The census gate — prove there is nothing left to break</name>
  <files>src/lib/supabase/middleware.ts</files>
  <action>
**Run this BEFORE editing anything.** It is a gate, not a formality.

```bash
npm run verify:no-header-identity
grep -rni 'x-user-' src/ | grep -v 'src/lib/supabase/middleware.ts'
```

Both must report **zero**. The pre-phase figure was **98** lines (97 reads
across 44 files plus one stale comment at `src/types/database.ts:389`, which
plan 33-01 removed).

⚠️ **The `-i` is not cosmetic.** HTTP header names are case-insensitive and
`headers().get()` is too, so `headers().get("X-User-Role")` reads the same value
a lower-case grep cannot see. `scripts/verify-no-header-identity.mjs` matches
case-insensitively by design (33-02); an "independent" cross-check that does not
is **weaker than the instrument it is auditing**, and would agree with it for the
wrong reason. This repository has the recorded incident of that exact class:
`grep -c 'CREATE POLICY' supabase/schema.sql` returns 0 while `grep -ci` returns
37, and that zero became the false guardrail D-32-C. **MEASURED 2026-08-07:**
`grep -rn` and `grep -rni` both report 98 today, so adding `-i` does not disturb
the recorded baseline — it only removes a way for the final zero to be a lie.

If either reports non-zero:
- **STOP.** Do not delete the injection.
- Name every surviving path. A reader that survives means one of the twelve
  conversion plans did not land, or a merge lost a hunk — and the consequence of
  proceeding is every master locked out of every `/admin` surface, silently, in
  production.
- The safe action is to report and wait, not to convert the stragglers here.
  This plan's file ownership is the middleware; converting an app surface in it
  would put an unreviewed change inside the plan that removes the safety net.

Record both numbers in the summary. The delta from 98 to 0 **is** criterion 1's
evidence, and it is a structural claim: a surface that cannot read the header
cannot be fooled by it.
  </action>
  <verify>
    <automated>npm run verify:no-header-identity</automated>
    <automated>test "$(grep -rni 'x-user-' src/ | grep -vc 'src/lib/supabase/middleware.ts')" -eq 0</automated>
  </verify>
  <done>
`npm run verify:no-header-identity` exits **0** for the first time in the phase.
The independent case-insensitive grep returns 0 lines. Both numbers are recorded
against the pre-phase 98, and the summary states that the cross-check was run
with `-i` and why.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the injection, keep the strip, close WR-04</name>
  <files>src/lib/supabase/middleware.ts, src/app/(members)/dashboard/page.tsx</files>
  <action>
Four edits in `src/lib/supabase/middleware.ts`, in this order.

**(a) Delete `:219-223` — the injection, and only the injection.** The whole
`if (user) { requestHeaders.set(…) ×3 }` block goes. A diff that touches
`:210-212` is the warning sign named as pitfall P-2: the two blocks are eleven
lines apart in the same function and read as one concern, and removing the wrong
one makes every remaining surface forgeable at once.

**(b) Keep `:210-212` and rewrite the comment above them.** The current comment
justifies the deletion by naming what would break — *"the SumUp refund path,
which gates on x-user-role and then uses a service-role client that bypasses
every RLS policy"*. That reader is gone (plan 33-03). Write the new reason:

> These three lines no longer protect any reader — after phase 33 nothing under
> `src/` reads an `x-user-*` header, and `npm run verify:no-header-identity`
> asserts it. They stay anyway, and deliberately. An inbound `x-user-*` is
> attacker-supplied input; deleting it costs three lines and manufactures
> nothing. What they now prevent is a *future* reader: the next person who
> writes `headers().get("x-user-role")` — from muscle memory, or by copying a
> pattern out of this file's own git history — would otherwise receive attacker
> input with no protection at all. The injection that used to sit below was
> removed in the same change, because a header nobody reads that something
> still *manufactures* is a trap; a header nobody reads that nothing manufactures
> and that we delete on the way in is a guard.
>
> Identity now comes from `public.my_access_context()` via
> `src/lib/capabilities/server.ts`. **Middleware is UX; RLS is the security
> boundary** — this deletion is neither, it is input hygiene, and nothing here
> substitutes for a row-level policy.

**(c) Fix the stale comment at `:76`.** It says *"46 files read x-user-role /
x-user-status, and replacing that transport is a later phase"*. The measured
number was 44 and it is now 0, and this is that phase. Rewrite the paragraph to
say why `role` and `status` still travel in the `my_access_context()` payload:
`MobileNav` and `StaffNav` are `"use client"` components that take them as
props and cannot import the DAL, so a parent Server Component resolves and
passes down. Removing the two fields from the payload is **phase 34** (STAFF-03),
not this one. A comment that lies survives the code it described, and this one
would otherwise be read as an instruction to delete the fields — which would
turn a transport swap into a nav redesign.

⚠️ The phrase occurs **twice** in `src/`, measured: `middleware.ts:76` and
`src/lib/capabilities/server.ts:112`. Plan 33-01 removes the second. The
assertion below is therefore src-wide rather than file-scoped, so it actually
asserts what its `done` criterion claims.

**(d) WR-04 — make the degraded bounce visible.** Today `capabilitiesResolveFailed`
sets `x-capabilities-resolve-failed` on the response, and the file's own comment
admits *"It is never read"*. This project has **no error tracking**, so a header
nothing reads and a log nobody watches are the same thing: the user sees an
ordinary bounce to `/dashboard` and cannot tell an infrastructure fault from a
permissions refusal.

In `bounceToDashboard()`, when `capabilitiesResolveFailed` is true, add
`url.searchParams.set("access", "unavailable")` before constructing the
redirect. Keep the response header as well — it costs nothing and it is the only
signal available on the non-redirect path at `:231-233`.

Then in `src/app/(members)/dashboard/page.tsx`, read `searchParams` and render a
single distinct notice when `access === "unavailable"`: something that says the
system could not check permissions right now and to try again, **explicitly not**
"you do not have access". That distinction is the whole point — collapsing a
network fault, a missing grant and a genuine refusal into one message is the
recorded newsletter defect (`.planning/codebase/CONCERNS.md`,
*"Qualcosa è andato storto"*), and `meta-gates.md` requires an **observable
effect**, not a log line.

⚠️ Three constraints on the WR-04 edit:
- The notice must not claim the user lacks permission. It is the opposite claim.
- Do not add a `try`/`catch` anywhere to produce it. The signal already exists as
  a boolean in the middleware; carrying it in the URL is transport, not error
  handling.
- `dashboard/page.tsx` was converted by plan 33-12 in the previous wave. Re-read
  it as it now stands; do not apply this against the pre-conversion version.

⚠️ **`middleware.ts:235-248` re-applies Supabase cookies to the final response**,
with the comment *"Without this, users would be logged out on every navigation"*.
The injection block sits directly above it. **Do not disturb the cookie
re-application, and do not reorder anything below `:225`.** Verify by signing in
and navigating three times after the edit — a broken cookie path logs everybody
out and looks nothing like a header change.

⚠️ **The two `requestHeaders` counts below filter comment lines out first, and
that is load-bearing.** Plan 33-12's positive control comments out exactly the
three `requestHeaders.delete("x-user-*")` lines and restores them afterwards. An
unfiltered `grep -c` counts a commented-out line and would report **3** for a
middleware whose strip was left disabled by a botched restore — a green from a
disarmed guard, on the one line that protects every future reader. Filter the
comments, then count the code. **MEASURED 2026-08-07:** filtered and unfiltered
both read 3 deletes and 3 sets on the current file, so the filter changes nothing
today and changes everything in the failure case.
  </action>
  <verify>
    <automated>test "$(grep -v '^[[:space:]]*//' src/lib/supabase/middleware.ts | grep -c 'requestHeaders.set("x-user-')" -eq 0</automated>
    <automated>test "$(grep -v '^[[:space:]]*//' src/lib/supabase/middleware.ts | grep -c 'requestHeaders.delete("x-user-')" -eq 3</automated>
    <automated>test "$(grep -rl '46 files' src/ | wc -l | tr -d ' ')" -eq 0</automated>
    <automated>grep -n 'access.*unavailable' src/lib/supabase/middleware.ts 'src/app/(members)/dashboard/page.tsx'</automated>
    <automated>rm -rf .next &amp;&amp; npm run build</automated>
  </verify>
  <done>
With comment lines filtered out, `requestHeaders.set("x-user-` → 0 and
`requestHeaders.delete("x-user-` → **3**: the strip is present as live code, not
as three commented-out lines left behind by a positive control. No file under
`src/` contains "46 files" — asserted src-wide (it occurred twice: `middleware.ts:76`
and `src/lib/capabilities/server.ts:112`), which is what the criterion claims.
The degraded bounce carries `?access=unavailable` and `/dashboard` renders a
distinct notice for it that does not claim a permission refusal. `git diff` shows
no change below `:225` other than the WR-04 line, and the cookie re-application
loop is byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 3: The phase evidence — instruments, baselines, and the probe with its control</name>
  <files>.planning/phases/33-server-data-access-layer/</files>
  <action>
Run the full gate and record every result. There is **no test runner for this
product** (`CLAUDE.md` Guardrail 1) — this is the evidence that will exist.

**1. Build, from a cleared cache.**
```
rm -rf .next && npm run build
```
A stale `.next` after a worktree merge gives a false failure (P-6). Clear it
before diagnosing anything.

**2. The mechanical assertions.**
```
npm run verify:no-header-identity        # expect exit 0
npm run verify:capabilities              # expect exit 0
grep -rni 'x-user-' src/ | grep -v 'src/lib/supabase/middleware.ts' | wc -l   # expect 0
```
The `-i` on the census is required, for the reason in task 1: the instrument is
case-insensitive, so a case-sensitive cross-check agrees with it for the wrong
reason. Record `verify:capabilities`'s result **with what it does not say**: it
reads the **catalogue** (`private.capabilities`), never the **grants**
(`private.role_capabilities`), so a green there is not a statement about who can
do what (D-32-L). A command named `verify` will otherwise be cited as if it were.

**3. Criterion 3, counted — and counted with a pattern that can fail.**
```
grep -rn 'function verifyOrganizer\|function verifyEventOwnership' src/   # expect 0
grep -rniE 'role !== ?"(master|organizer)"' src/ \
  | grep -vE ':[0-9]+: *(\*|//|/\*)'                                     # expect 0
grep -rn 'created_by !== ' src/                                          # expect only the shared guard
```
Two things about the middle command, both measured, and both the difference
between a check and a decoration:

- **It is not `grep -rnF 'profile.role !== '`.** That literal scores **0** on
  `src/app/api/membership/verify/route.ts` — whose line 104 reads
  `(userProfile.role !== "master" && userProfile.role !== "organizer")` — because
  lower-case `profile` does not occur inside `userProfile`. The dead code could
  be left sitting beside its replacement on the route that decides whether a
  membership card is honoured, and the check would report a clean zero. That is
  the D-32-C shape, and D-32-C's zero became a written guardrail telling readers
  not to look where 37 policies are.
- **It filters comment lines.** `src/app/(admin)/admin/newsletter/actions.ts:20`
  and `:29` *document* the removed predicate in prose — *"This read the injected
  role header and refused on `role !== "master"`"*. An unfiltered count would
  fail on a correct repository, which teaches the next reader to wave the check
  through.

**MEASURED 2026-08-07, before the phase:** 52 matching lines, **50** after the
comment filter, across **44** files — every one of them owned by a plan in this
phase. The expected end state is 0.

Reconcile against the per-plan lists of deleted `file:line`s recorded in the
summaries of 33-08, 33-09, 33-10 and 33-13. Nineteen database-sourced predicate
sites and thirty-one header-derived gates existed at the start; state where each
family went.

**4. CAP-03, both targets.**
```
npm run baseline:rls       -- --target=production --only=B1,B2    --phase-point=33-final
npm run baseline:container --                       --only=B1,B2,B3 --phase-point=33-final
npm run baseline:compare   -- --target=production --only=B1,B2    --before-point=33-pre --after-point=33-final
npm run baseline:compare   -- --target=container  --only=B1,B2,B3 --before-point=33-pre --after-point=33-final
```
Expect `CAP-03: clean` on both. The only database change in the phase was the
`user_id` field on `my_access_context()`; no policy, table or grant moved. A
defect here means something did. **Do not pin, do not whitelist, do not pass
`--allow-lint-move`** — it is LINT-wide, not entity-wide (D-32-G). And
`unused_index` is **never** pinned: it counts indexes not scanned since the last
statistics reset, so it moves with use and not with schema
(`33-VALIDATION.md` § *Advisor lints*).

Remember: an existing artefact aborts the capture with exit 1, deliberately
(CR-02). Capture to a new `--phase-point`, never over an old one, and note that
the harness writes into phase 32's `baseline/` directory with a `32-BASELINE-`
prefix because that path is hard-coded — that is the instrument, not a mistake.

**5. Criterion 2 — the probe, with its positive control.**
Run `scripts/probe-forged-identity.sh` against a dev server:
- **converted state** → `Add Item` 0 in both anon and forged;
- **mutated state** (the three `requestHeaders.delete` lines commented out) →
  assert `git diff --stat src/lib/supabase/middleware.ts` is **non-empty before
  reading any result**, then observe. ⚠️ Dev server only, reverted in the same
  session; this mutation removes the only remaining protection against a future
  reader.
- **restored** → 0 and 0, and `git status --porcelain` empty. Re-run task 2's
  comment-filtered `requestHeaders.delete` count and confirm it still reads 3 as
  **live code** — a restore that leaves the lines commented passes an unfiltered
  grep and disarms the guard.

Then the structural claim, which is stronger and cheaper than any sample: after
this phase **no surface reads the header at all**, so the probe's green is a
consequence of the census rather than a sample of it. If the probe cannot be
made to fire in the mutated state — because there is no reader left to fool —
**say exactly that**. It is the correct end state and it is not the same thing
as a passing probe. Do not report a green you cannot distinguish from a dead
instrument.

**6. Write `33-VERIFICATION.md`.** Per `CLAUDE.md`'s VERIFICATION gate it must
carry **concrete evidence per requirement** — `file:line`, an observable
behaviour, or a manual step actually executed. A `33-VERIFICATION.md` with no
`file:line` citation does not satisfy the gate. Include:
- the four success criteria, each with its evidence, mapped against
  `33-VALIDATION.md`'s per-criterion table;
- the census delta 98 → 0 with both commands, both run with `-i`;
- both CAP-03 verdicts;
- the probe results in all three states;
- **the money-path row of criterion 2, recorded OWED**, with the reason:
  `/admin/finance` is already gated non-forgeably by `middleware.ts:172-177` on
  `admin.access`, granted to `master` alone
  (`supabase/migrations/20260807000000_capability_model.sql:408`), so plan
  33-03's forged-header steps return the same answer before and after the
  conversion and are not criterion-2 evidence. What carries the money path is
  33-12's positive-controlled probe plus this plan's census. Do not silently
  upgrade it to green; `32-VERIFICATION.md` set the precedent that a deliberate
  `nyquist_compliant: false` beats an unearned `true`.
- **IN-01, closed by construction.** `32-REVIEW.md` IN-01 records that when the
  RPC failed, `role`/`status` fell back to `member`/`pending` and were injected
  **as fact** into headers that 44 files read — those files received an
  assertion, not an unknown. After this phase the fallback lives in one place
  (`getAccessContext()`'s throw) and there is no transport to inject it into.
  IN-01 closes here; say so.
- **WR-04, closed** — with the `file:line` of the search-param set and of the
  banner.
- **What this phase did NOT fix**, named honestly:
  - `(public)/events/[slug]/menu/page.tsx` still branches a service-client read
    on a TypeScript boolean. `canManage` is now session-derived — the part this
    phase was asked to fix — but it remains a legitimate concentration of trust
    in code, on the one page with neither a middleware prefix gate nor an RLS
    backstop.
  - The guest-list ownership read still uses the service client where its two
    siblings use the cookie client. Carried forward with its evidence (33-09).
  - `profiles_update_own` and the `42P17` recursion are untouched (D-32-A). The
    `profiles` UPDATE cells must still read `42P17` in the final baseline —
    **check that they do**, and record it. If they do not, something moved that
    this phase had no business moving.
  - `MASTER_EMAIL` still promotes and never demotes
    (`src/app/api/auth/callback/route.ts:27`). Not this phase's; folding a
    one-way-switch repair into a transport swap would put a Critical change
    inside a mechanical phase.
  - The `middleware.ts` → `proxy.ts` deprecation is real and visible in the
    build output (`ƒ Proxy (Middleware)`). Deliberately not done here: it
    renames the file this phase edits, on a path that carries every request
    including the door.

**7. Write `33-CARRY-FORWARD.md`** in the shape of `32-CARRY-FORWARD.md`, so
phases 34 and 35 do not re-derive: the `cache()` execution-context matrix and
its consequence for actions and routes; `hasCapability(key)` as the primary API
with `hasCapability(key, { partyId })` as the phase-35 growth path; the note
that `public.my_access_context()` takes no argument by design and phase 35 will
need a new wrapper of the shape `public.my_party_capabilities(p_party_id uuid)`
whose argument is a *party* id, not a *person* id, so D-04's enumeration-oracle
objection does not apply; the standing prohibition on passing a capability `Set`
across a module boundary as a prop convention; and — as a rule rather than an
anecdote — **name the mechanism that would make a check fail before writing the
check**, which is the lesson three incidents have now paid for (D-32-I, the
insensitive `/events` probe, and the `/admin/finance` checkpoint corrected in
33-03).
  </action>
  <verify>
    <automated>npm run verify:no-header-identity &amp;&amp; npm run verify:capabilities</automated>
    <automated>test "$(grep -rniE 'role !== ?"(master|organizer)"' src/ | grep -vcE ':[0-9]+: *(\*|//|/\*)')" -eq 0</automated>
    <automated>npm run baseline:compare -- --target=production --only=B1,B2 --before-point=33-pre --after-point=33-final</automated>
    <automated>npm run baseline:compare -- --target=container --only=B1,B2,B3 --before-point=33-pre --after-point=33-final</automated>
    <automated>test -f .planning/phases/33-server-data-access-layer/33-VERIFICATION.md &amp;&amp; grep -cE '[a-zA-Z0-9_/.()\[\]-]+\.(ts|tsx|sql|mjs):[0-9]+' .planning/phases/33-server-data-access-layer/33-VERIFICATION.md</automated>
  </verify>
  <done>
All mechanical assertions pass. The criterion-3 count is **0** with the
case-insensitive, variable-agnostic, comment-filtered pattern, against **50**
measured before the phase. Both CAP-03 comparisons print clean.
`33-VERIFICATION.md` exists, carries at least one `file:line` citation per
success criterion, records the probe in all three states, records the money-path
criterion-2 row as **OWED** with its reason, closes IN-01 and WR-04 by name, and
lists what was not fixed. `33-CARRY-FORWARD.md` exists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: The eleven-persona sweep and the session check</name>
  <action>
This is a verification checkpoint, not an edit. Write no code. Execute the
numbered procedure below exactly as written, in order, and record each observed
result verbatim in the plan summary — including any step whose expected result
could not be established. There is no test runner for this product, so what is
written here is the only evidence that will exist (`CLAUDE.md` Guardrail 1).
Stop and report on the first deviation rather than continuing.
  </action>
  <what-built>
The `x-user-*` transport is gone. The middleware manufactures no identity and
still deletes any inbound one. A capability lookup that fails now bounces with
`?access=unavailable` and says so on the dashboard.
  </what-built>
  <how-to-verify>
This is the phase's last gate and it is the one that catches an availability
break, which is the failure mode the removal order was designed around. There is
**no test runner**; write down what you observe.

`rm -rf .next && npm run build && PORT=3007 npm run start`
(ports 3000 and 3002 are held by Docker on the development machine).

**The eleven-persona sweep.** For each role, confirm the surfaces reachable are
exactly the ones reachable before this phase. Phase 32's verdict table is the
reference; re-run it.

1. **`master` / `approved`** — `/admin`, `/admin/finance`, `/admin/members`,
   `/admin/events`, `/admin/scanner`, `/organizer/events`, `/dashboard`,
   `/membership-card`. **Expected:** all reachable. This is the step the removal
   order exists for: if the injection had been deleted before the readers, every
   one of these would bounce.
2. **`organizer` / `approved`** — `/organizer/*` reachable, `/admin/finance`
   bounces to `/dashboard`, `/admin/scanner` reachable.
3. **`organizer` / `pending`** — `/admin/scanner` **reachable** (role alone; a
   refusal here happens in front of a queue), `/organizer/events` reachable,
   venue creation still refused.
4. **`member` / `approved`** — `/dashboard`, `/membership-card`, `/attendance`
   reachable; `/admin/*` and `/organizer/*` bounce.
5. **`member` / `pending`** — `/dashboard` reachable and shows the pending
   notice; `/membership-card` and `/attendance` bounce.
6. **Anonymous** — public pages render; `/dashboard` redirects to `/login`.

**The session check, which is not about permissions at all.**
7. Sign in and navigate between five pages. **Expected:** you stay signed in.
   The injection block sat directly above the cookie re-application loop, whose
   comment says *"Without this, users would be logged out on every navigation"*.
   If you are logged out, the edit disturbed something below `:225`.

**The degraded path.**
8. Visit `/dashboard?access=unavailable` directly. **Expected:** the notice
   appears and says the system could not check permissions — **not** that you
   lack access. Confirm the wording distinguishes the two.

**The door, once more, because it is the door.**
9. As `organizer` / `pending`, open `/admin/scanner` and admit a valid ticket.
   **Expected:** admitted. Then repeat with the network off and confirm the scan
   queues and later drains.

**The forged header, last.**
10. Signed out, request three surfaces with `-H 'x-user-role: master'
    -H 'x-user-status: approved' -H 'x-user-id: 00000000-0000-0000-0000-000000000000'`:
    `/admin/finance`, `/events/<slug>/menu`, `/api/tickets/checkin`.
    **Expected:** identical responses to the same requests without the headers.
    ⚠️ Only the middle one is a sensitive surface. `/admin/finance` is refused by
    the middleware's `admin.access` rule and `/api/tickets/checkin` never read a
    header at all, so both return the same answer as they did before this phase.
    Record all three, and record which one carries the evidence.

Any deviation on steps 1–7 is a regression this phase must fix before it closes.
  </how-to-verify>
  <resume-signal>Type "approved", or name the numbered step that differed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Next server | Inbound `x-user-*`, still deleted unconditionally |
| middleware → downstream | No longer carries a manufactured identity |
| middleware → user | The degraded path, previously signalled only by a header nothing reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-75 | Spoofing | a future `headers().get("x-user-role")` written from muscle memory | mitigate | The strip is **kept** and re-argued in place; `npm run verify:no-header-identity` fails the moment such a reader appears, and it matches case-insensitively so `X-User-Role` cannot slip past it |
| T-33-76 | Denial of service | deleting the injection while a reader survives — every master locked out of every `/admin` surface | mitigate | Task 1 is a blocking census gate that must read zero **before** any edit, with an explicit instruction to stop rather than to convert stragglers here |
| T-33-77 | Spoofing | removing the strip instead of the injection (pitfall P-2), or leaving it commented out after a positive control | mitigate | With comment lines filtered, `requestHeaders.delete("x-user-` is asserted to be exactly 3 and `…set("x-user-` exactly 0. The filter is what distinguishes a live guard from three commented-out lines, and 33-12's positive control comments out exactly those lines |
| T-33-78 | Repudiation | an infrastructure fault rendered as a permission refusal | mitigate | WR-04 closed: `?access=unavailable` on the bounce plus a dashboard notice whose wording must not claim a refusal |
| T-33-79 | Denial of service | the cookie re-application disturbed, logging everyone out | mitigate | No reordering below `:225`; checkpoint step 7 observes five navigations on one session |
| T-33-80 | Elevation of privilege | a permission moved somewhere in 63 converted sites | mitigate | CAP-03 clean on both targets against the `33-pre` capture, plus the eleven-persona sweep |
| T-33-81 | Repudiation | a probe reported green that could no longer fire | mitigate | The positive control is run and, if it cannot be established because no reader survives, that is stated rather than reported as a pass. The money-path row stays **OWED** rather than being upgraded on the strength of an insensitive check |
| T-33-82 | Tampering | npm/pip/cargo installs | accept | No dependency added in any plan of this phase; `33-RESEARCH.md`'s only candidate (`server-only`, `[OK]`) was not adopted |
| T-33-85 | Repudiation | a criterion-3 duplicate surviving behind a grep that cannot see it | mitigate | The src-wide count is case-insensitive, variable-agnostic and comment-filtered, measured at **50** before the phase; the `profile.role !== ` literal it replaces scores 0 on `src/app/api/membership/verify/route.ts` whether the code is there or not |
</threat_model>

<verification>
- `npm run verify:no-header-identity` exits 0.
- `grep -rni 'x-user-' src/ | grep -v 'src/lib/supabase/middleware.ts'` → 0 lines.
  Run with `-i`: the instrument is case-insensitive, so a case-sensitive
  cross-check would agree with it for the wrong reason.
- With comment lines filtered:
  `grep -c 'requestHeaders.delete("x-user-' src/lib/supabase/middleware.ts` → 3,
  `grep -c 'requestHeaders.set("x-user-'` → 0.
- `grep -rl '46 files' src/ | wc -l` → 0, src-wide (it occurred in two files).
- `grep -rniE 'role !== ?"(master|organizer)"' src/ | grep -vE ':[0-9]+: *(\*|//|/\*)'`
  → 0 lines, against **50** measured before the phase.
- `rm -rf .next && npm run build` passes.
- `CAP-03: clean` on production and container against `33-pre`.
- The `profiles` UPDATE cells still report `42P17` for every persona — D-32-A is
  out of bounds and must be observably still out of bounds.
- `33-VERIFICATION.md` carries at least one `file:line` per success criterion,
  and records the money-path criterion-2 row as OWED with its reason.
- The eleven-persona sweep is executed and recorded, step by step.
- No test runner exists and none was added.
</verification>

<success_criteria>
1. Nothing under `src/` reads an `x-user-*` header except the middleware that
   deletes them, asserted mechanically, case-insensitively, and re-checkable.
2. The injection is deleted; the strip is kept **as live code** with a comment
   that says why it now exists.
3. A forged identity header is answered exactly as an anonymous request, proved
   where the mechanism can be shown to fire and recorded as OWED where it
   cannot.
4. `npm run build` passes and every role reaches exactly the surfaces it reached
   before, observed across the eleven-persona sweep and confirmed by `CAP-03:
   clean` on both targets.
5. WR-04 is closed and IN-01 is closed by construction, both named in
   `33-VERIFICATION.md`.
</success_criteria>

<output>
Create `.planning/phases/33-server-data-access-layer/33-14-SUMMARY.md`,
`33-VERIFICATION.md` and `33-CARRY-FORWARD.md` when done.
</output>
