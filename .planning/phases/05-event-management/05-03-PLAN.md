---
phase: 05-event-management
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(public)/events/page.tsx
  - src/app/(public)/events/[slug]/page.tsx
autonomous: true
requirements: [EVNT-03, EVNT-04, EVNT-05, EVNT-06, EVNT-07]

must_haves:
  truths:
    - "Events page shows real data from Supabase, not mock data"
    - "Events page has Upcoming and Past tabs with correct sorting"
    - "Event detail page shows capacity as secondary text (X spots left or Sold out)"
    - "Secret location shows CTA to buy a ticket instead of revealing the address"
    - "Events are accessible via URL-friendly slugs"
  artifacts:
    - path: "src/app/(public)/events/page.tsx"
      provides: "Events browsing page with real Supabase data and tab UI"
      min_lines: 40
    - path: "src/app/(public)/events/[slug]/page.tsx"
      provides: "Event detail page with real data, capacity, and secret location logic"
      min_lines: 60
  key_links:
    - from: "src/app/(public)/events/page.tsx"
      to: "supabase events table"
      via: "server-side query for published events"
      pattern: "from\\(['\"]events['\"]\\).*is_published"
    - from: "src/app/(public)/events/[slug]/page.tsx"
      to: "supabase events table"
      via: "server-side query by slug"
      pattern: "from\\(['\"]events['\"]\\).*slug"
    - from: "src/app/(public)/events/[slug]/page.tsx"
      to: "supabase rsvps table"
      via: "RSVP count for capacity display"
      pattern: "from\\(['\"]rsvps['\"]\\)"
---

<objective>
Replace mock event data in the two public event pages with real Supabase queries. Implement tab-based browsing (Upcoming/Past), compact event cards, event detail with capacity display, and secret location CTA.

Purpose: Make the events page live with real data, fulfilling EVNT-03 through EVNT-07 (display side). This is what members see when browsing events.
Output: Updated events list page and event detail page, both reading from Supabase.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-event-management/05-CONTEXT.md
@.planning/phases/05-event-management/05-RESEARCH.md
@.planning/phases/05-event-management/05-01-SUMMARY.md
@src/app/(public)/events/page.tsx
@src/app/(public)/events/[slug]/page.tsx
@src/types/database.ts
@src/lib/supabase/server.ts
@src/components/layout/MobileNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace events list page with real data and tab UI</name>
  <files>src/app/(public)/events/page.tsx</files>
  <action>
Rewrite `src/app/(public)/events/page.tsx` to replace all mock data with real Supabase queries.

**Data fetching (server component):**
- Use `createClient` from `@/lib/supabase/server`
- Calculate today's date: `new Date().toISOString().split("T")[0]`
- Fetch upcoming and past events in parallel using `Promise.all`:
  - Upcoming: `supabase.from("events").select("slug, title, date, time, location, location_secret, capacity").eq("is_published", true).gte("date", today).order("date", { ascending: true })`
  - Past: `supabase.from("events").select("slug, title, date, time, location, location_secret, capacity").eq("is_published", true).lt("date", today).order("date", { ascending: false })`
- Wrap in try/catch with graceful fallback (per Phase 1 decision: homepage renders cleanly even without DB). If queries fail, show empty state.
- Also fetch RSVP counts per event for capacity display: For each event with capacity, count RSVPs. Use a single query: `supabase.from("rsvps").select("event_id", { count: "exact" })` -- actually, aggregate counts efficiently. Approach: fetch all rsvps for the relevant event IDs and compute client-side, OR use separate count queries. Simplest approach: for the list view, just show capacity (not spots left). Spots left is for the detail page only (per locked decision: capacity status in a dense row/card format). So on the list, show capacity as "X capacity" or nothing if unlimited.

**Tab UI (client component):**
- Extract the tab switcher and event list rendering into a client component (e.g., inline `"use client"` component or a separate `EventTabs` component defined in the same file or imported).
- Per locked decision: tab switcher with "Upcoming" and "Past" tabs on the same page, only one set visible at a time.
- Tab styling: Use underline or pill-style tabs. Active tab: `text-accent border-b-2 border-accent`. Inactive: `text-muted`.
- Default to "Upcoming" tab.

**Compact event cards (per locked decision):**
No cover images in list view. Each card is a dense row format:
- Title (bold, medium text)
- Date and time (formatted nicely, e.g., "Saturday, March 15 · 23:00")
- Location: show location name or "Secret Location" (with lock emoji) if `location_secret` is true
- Capacity: show capacity number if set (e.g., "150 capacity"), omit if unlimited
- Wrap each card in a Link to `/events/{slug}`
- Card styling: `rounded-2xl border border-card-border bg-card p-5 transition-colors hover:border-accent/50` (matches existing pattern)
- Past events: slightly faded (`opacity-70 hover:opacity-100 bg-card/50`)

**Empty states (per locked decision):**
- Upcoming tab empty: "No upcoming events -- check back soon."
- Past tab empty: "No past events yet."
- Simple text, no CTAs.

**Keep:**
- Read role/status from middleware headers for MobileNav (same as current)
- MobileNav at the bottom
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
    <manual>Verify src/app/(public)/events/page.tsx contains no mockEvents variable, imports createClient, queries events table with is_published filter, and renders tab UI.</manual>
  </verify>
  <done>Events page fetches real data from Supabase, displays Upcoming/Past tabs with compact event cards (title, date, location/secret, capacity), empty states for both tabs, and no mock data remains. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace event detail page with real data, capacity, and secret location CTA</name>
  <files>src/app/(public)/events/[slug]/page.tsx</files>
  <action>
Rewrite `src/app/(public)/events/[slug]/page.tsx` to replace mock data with real Supabase queries.

**Data fetching (server component):**
- Fetch event by slug: `supabase.from("events").select("*").eq("slug", slug).eq("is_published", true).single()`
- If event not found, show a 404-style page ("Event not found" with link back to /events). Use Next.js `notFound()` from `next/navigation` for proper 404.
- Fetch RSVP count for capacity display: `supabase.from("rsvps").select("*", { count: "exact", head: true }).eq("event_id", event.id)`
- Calculate spots left: `event.capacity ? event.capacity - (rsvpCount || 0) : null`
- Keep auth check (user/isAuthenticated/isApproved) -- same as current code

**Cover image (per locked decision: medium size, not full-width hero):**
- If event has cover_image: display using Next.js `<Image>` component from `next/image` for optimization. Set reasonable width/height (e.g., `width={800} height={400}`) with `className="w-full max-h-80 object-cover rounded-2xl"`. NOT full-bleed hero -- contained within content area with rounded corners.
- If no cover_image: show the existing music note placeholder but smaller/contained

**Date, title, description:** Same layout as current but using real data.

**Location section (per locked decision):**
Secret location logic (different from current implementation):
- If `location_secret` is true AND user is NOT authenticated: show "Secret Location" with CTA "Sign up to become a member" linking to `/register`
- If `location_secret` is true AND user IS authenticated: show "Secret Location -- Buy a ticket to reveal" with a styled CTA button/link. Since tickets don't exist yet (Phase 6), link to `#tickets` anchor or show as a visually distinct call-to-action that will be wired in Phase 6. Use accent-colored text.
- If `location_secret` is false: show the location address normally
- Remove the old "You'll receive the address 24h before the event" messaging

**Lineup (per locked decision: styled tags/chips):**
- Each artist in a pill/chip style matching TagInput visual: `rounded-full bg-accent/20 px-3 py-1 text-sm text-accent font-medium`
- Display in a flex-wrap row (not stacked vertically as current)

**Capacity display (per locked decision: exact count, subtle, secondary text):**
- If capacity is set and spotsLeft > 0: show "X spots left" as muted secondary text near the top (below date/time)
- If capacity is set and spotsLeft <= 0: show "Sold out" in a distinct style (e.g., `text-red-400 font-medium`)
- If no capacity set: show nothing (unlimited)
- This should be subtle -- not a headline, just secondary detail

**RSVP button:**
- Keep the existing RSVP button logic (authenticated + approved members see it)
- This is informational for now -- actual RSVP functionality can remain as-is or be a placeholder

**Keep:**
- MobileNav at bottom
- Role/status from headers
- Back button to /events
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
    <manual>Verify src/app/(public)/events/[slug]/page.tsx contains no mock event object, fetches from Supabase by slug, displays capacity as "X spots left", shows secret location CTA, and renders lineup as chips.</manual>
  </verify>
  <done>Event detail page fetches real event data by slug from Supabase. Shows medium-sized cover image (or placeholder), capacity as secondary "X spots left" / "Sold out" text, secret location with CTA "Buy a ticket to reveal", lineup as styled chips, and proper 404 for missing events. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes with 0 errors
- No `mockEvents` variable remains in either events page
- Events page queries Supabase with `is_published = true` filter
- Events page has tab switching between Upcoming and Past
- Event detail page fetches by slug, shows capacity, shows secret location CTA
- Lineup renders as chips, not vertical list
- Empty states display appropriate messages
</verification>

<success_criteria>
- Events page shows real Supabase data with Upcoming/Past tabs
- Event cards are compact (no cover images in list view)
- Event detail shows medium cover image, capacity as "X spots left", secret location CTA
- Lineup displays as styled chips matching tag input visual
- Slugs work as URL parameters for event detail pages
- Both pages handle error and empty states gracefully
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-event-management/05-03-SUMMARY.md`
</output>
