---
phase: 35-per-night-assignments
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - supabase/migrations/20260809001000_assignment_resolver.sql
  - src/lib/capabilities/keys.ts
  - scripts/verify-capabilities.mjs
autonomous: true
requirements: [ASSIGN-01, ASSIGN-02, ASSIGN-05]

must_haves:
  truths:
    - "Un'assegnazione a una notte concede la capability in quella notte e in nessun'altra"
    - "Un'assegnazione smette di valere quando la notte finisce, e a deciderlo e' l'orologio del server"
    - "Esiste una chiave che risponde alla domanda «puo' questa persona annullare un check-in stanotte?», distinta da «puo' lavorare la porta»"
  artifacts:
    - path: "supabase/migrations/20260809001000_assignment_resolver.sql"
      provides: "il secondo braccio dell'OR, tre chiavi nuove con i loro grant, l'overload my_access_context(uuid)"
      contains: "p_party_id is not null"
    - path: "src/lib/capabilities/keys.ts"
      provides: "CAP.DOOR_SUPERVISE, CAP.MEDIA_UPLOAD, CAP.PARTY_MANAGE + le loro descrizioni nel Record totale"
    - path: "scripts/verify-capabilities.mjs"
      provides: "dodici decisioni nuove in ROLE_GRANTS e i tre totali aggiornati"
      contains: "door.supervise"
  key_links:
    - from: "private.has_capability"
      to: "public.party_assignments"
      via: "secondo EXISTS in OR, spento su p_party_id NULL"
      pattern: "party_assignments"
    - from: "public.my_access_context(uuid)"
      to: "private.has_capability(text, uuid)"
      via: "risoluzione per-notte esposta al DAL"
      pattern: "my_access_context"
---

<objective>
Il secondo braccio dell'OR. Il resolver della fase 32 porta gia' `p_party_id`,
**accettato e inutilizzato**, con il commento che istruisce testualmente ad
aggiungere qui la seconda fonte: *«un secondo braccio dello stesso OR,
modificando il corpo di questa funzione; nessuna policy e nessun chiamante
cambiano quando arriva»*.

Purpose: e' l'unico punto in cui l'assegnazione diventa visibile alle 67 policy
RLS **senza toccarne nessuna**. E porta con se' la riga che non e' difensiva ma
e' ASSIGN-01: `p_party_id is not null` come **prima** condizione del braccio.
Senza, un'assegnazione a una notte concede la capability **ovunque**, perche' le
67 policy esistenti chiamano il resolver senza secondo argomento, cioe' con
`NULL`.

Output: la migration 8 della coda, tre chiavi di capability nuove con le loro
dodici decisioni, e l'overload che permette al DAL di fare la domanda per-notte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-PATTERNS.md
@.planning/ACCESS-MODEL-DECISIONS.md
@.claude/rules/access-gating.md
</context>

<interfaces>
Il corpo attuale del resolver — il **braccio 1 deve restare byte-identico**
(`supabase/migrations/20260807000000_capability_model.sql:192-217`):

firma `private.has_capability(p_capability text, p_party_id uuid default null)
returns boolean`, `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = ''`;
braccio 1 = `exists (select 1 from public.profiles p join
private.role_capabilities rc on rc.role = p.role where p.id = (select
auth.uid()) and rc.capability = p_capability and (not rc.requires_approved or
p.status = 'approved'))`.

Il catalogo oggi: nove chiavi — `staff.manage`, `master.manage`,
`catalogue.manage`, `membership.active`, `admin.access`, `organizer.access`,
`door.operate`, `membership.card.view`, `register.read`
(`src/lib/capabilities/keys.ts:64-83`).

I tre totali asseriti da `scripts/verify-capabilities.mjs:347-349`, verificati in
questa sessione: `EXPECTED_PAIR_COUNT = 36`, `EXPECTED_GRANT_COUNT = 20`,
`EXPECTED_REFUSAL_COUNT = 16`.

Le colonne di `public.party_assignments` che il braccio 2 legge, dal piano
35-02: `party_id`, `user_id`, `capability`, `revoked_at`, `ends_at`.

`public.my_access_context()` oggi: zero argomenti, `GRANT` al solo
`authenticated`, **due soli chiamanti** — `src/lib/supabase/middleware.ts:90` e
`src/lib/capabilities/server.ts:202` (`capability_model.sql:262-297`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Le tre chiavi nuove, coniate dalla domanda che rispondono</name>
  <files>supabase/migrations/20260809001000_assignment_resolver.sql, src/lib/capabilities/keys.ts, scripts/verify-capabilities.mjs</files>
  <read_first>
    - `src/lib/capabilities/keys.ts:38-45` — la regola di naming: una chiave si nomina **dalla domanda che risponde**, mai dal predicato a cui si risolve. E' la ragione per cui `staff.manage`, `organizer.access` e `door.operate` sono tre chiavi che condividono un predicato invece di una chiave sola
    - `src/lib/capabilities/keys.ts:87-99` — `CAP_DESCRIPTIONS` e' un `Record` **totale** sulla union: aggiungere una chiave a `CAP` senza la sua descrizione e' un errore di `npm run build`. E' l'unica meta' del contratto che il compilatore tiene, in un repository senza test runner
    - `supabase/migrations/20260808002000_membership_register.sql:63-138` — il precedente completo di come si conia una chiave: `description` NOT NULL scritta per esteso, `INSERT … ON CONFLICT DO NOTHING` su entrambe le tabelle, e il paragrafo che **punta a `verify-capabilities.mjs`** per le rinunce invece di sostituirsi a esso
    - `supabase/migrations/20260808000500_staff_role.sql:139-171` — le sei rinunce di `staff`, e il paragrafo su **come si esprime un rifiuto**: e' l'**assenza di una riga**. Non esiste una colonna `granted`, e aggiungerne una **concederebbe** la capability, perche' l'`EXISTS` del resolver non ha nessun `granted` dentro
    - `supabase/migrations/20260808000500_staff_role.sql:190-191` — il corollario che riguarda proprio questa fase
    - `scripts/verify-capabilities.mjs:130-146` — ogni coppia (ruolo × capability) e' dichiarata come grant o come `REFUSED`, **mai come silenzio**; e la frase *«se questo scatta, guarda il modello, non questa costante»*
    - `scripts/verify-capabilities.mjs:173-206` — `ROLE_GRANTS` e le due decisioni piu' recenti, ognuna con il proprio paragrafo, incluso quello su `door.operate: false` che e' la trappola dichiarata dal ROADMAP
    - `scripts/verify-capabilities.mjs:347-349` e `:413-424` — i tre totali e le loro asserzioni
  </read_first>
  <action>
    Aprire `supabase/migrations/20260809001000_assignment_resolver.sql` con
    l'intestazione nella forma di `membership_register.sql:1-61`: modifiche
    numerate, come fallisce ogni meta' applicata, idempotenza voce per voce,
    `BEGIN;` esplicito.

    Coniare **tre** chiavi in `private.capabilities`, con `INSERT … ON CONFLICT
    (key) DO NOTHING`, ognuna con la sua `description` scritta per esteso e
    nominata **dalla domanda**:

    - `door.supervise` — «Reverse a check-in already recorded at the door.»
      E' una domanda **diversa** da `door.operate`: riusare `staff.manage`
      concederebbe sedici tabelle, e riusare `door.operate` renderebbe ogni
      operatore un supervisore, che e' esattamente cio' che ASSIGN-05 vieta.
    - `media.upload` — «Upload media to a night.» E' il mestiere «photo» di
      ASSIGN-01. Non esiste oggi, ed e' la ragione per cui un fotografo non
      poteva essere assegnato: il mestiere c'era, la chiave no.
    - `party.manage` — «Manage one night's operational surfaces.» E' il mestiere
      «organizer» di ASSIGN-01 quando e' **per una notte sola**. Deliberatamente
      **non** `organizer.access`, che e' l'accesso all'area e non la gestione di
      una serata.

    Inserire i grant in `private.role_capabilities`, con `ON CONFLICT (role,
    capability) DO NOTHING`, secondo queste **dodici** decisioni — sei grant e
    sei rifiuti, e un rifiuto e' **l'assenza di una riga**:

    - `door.supervise`: `master` grant con `requires_approved = false`;
      `organizer` grant con `requires_approved = false`; `staff` RIFIUTATO;
      `member` RIFIUTATO.
    - `media.upload`: `master` grant con `requires_approved = true`;
      `organizer` grant con `requires_approved = true`; `staff` RIFIUTATO;
      `member` RIFIUTATO.
    - `party.manage`: `master` grant con `requires_approved = true`;
      `organizer` grant con `requires_approved = true`; `staff` RIFIUTATO;
      `member` RIFIUTATO.

    I due `false` di `door.supervise` portano il **loro paragrafo**, nella stessa
    forma di quello di `door.operate`: sono `false` per la stessa ragione e
    **non si «puliscono»**. Il vincolo `role ⇒ approved` protegge il database;
    l'impostazione della porta protegge la notte dal giorno in cui quel vincolo
    viene rilassato per un caso speciale.

    Il rifiuto di `media.upload` a `staff` non e' una svista: essere `staff`
    **non** concede la capacita' di caricare foto. Un fotografo la ottiene
    **dall'assegnazione**, per la notte che ha lavorato — e questa e' la
    dimostrazione che il modello per-serata funziona invece di essere una
    decorazione del ruolo. Scriverlo nella migration.

    Aggiungere le tre chiavi a `CAP` in `src/lib/capabilities/keys.ts` come
    `DOOR_SUPERVISE`, `MEDIA_UPLOAD`, `PARTY_MANAGE`, **con le loro tre
    descrizioni in `CAP_DESCRIPTIONS`**: il `Record` e' totale e il build
    fallisce senza.

    Aggiornare `scripts/verify-capabilities.mjs`: dodici voci nuove in
    `ROLE_GRANTS`, e i tre totali da 36/20/16 a **48/26/22**. Le due righe
    `door.supervise` con `false` portano il paragrafo sopra, come le righe
    `door.operate` portano il loro.
  </action>
  <acceptance_criteria>
    - `npm run build` passa: il `Record` totale di `CAP_DESCRIPTIONS` e' il gate
    - `npm run verify:capabilities -- --target=container` esce 0 e riporta cinque lati verdi
    - `scripts/verify-capabilities.mjs` contiene `EXPECTED_PAIR_COUNT = 48`, `EXPECTED_GRANT_COUNT = 26`, `EXPECTED_REFUSAL_COUNT = 22`
    - La migration non contiene la stringa `granted`
    - Le tre chiavi compaiono sia nella migration sia in `keys.ts` sia in `ROLE_GRANTS`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q "EXPECTED_PAIR_COUNT = 48" scripts/verify-capabilities.mjs && grep -q "EXPECTED_GRANT_COUNT = 26" scripts/verify-capabilities.mjs && grep -q "EXPECTED_REFUSAL_COUNT = 22" scripts/verify-capabilities.mjs && npm run verify:capabilities -- --target=container</automated>
  </verify>
  <done>Tre chiavi esistono nel catalogo, nei grant, in `keys.ts` e in `ROLE_GRANTS`, con dodici decisioni dichiarate e i totali aggiornati. Nessun rifiuto e' espresso come colonna.</done>
</task>

<task type="auto">
  <name>Task 2: Il secondo braccio dell'OR, null-safe</name>
  <files>supabase/migrations/20260809001000_assignment_resolver.sql</files>
  <read_first>
    - `supabase/migrations/20260807000000_capability_model.sql:192-217` — il corpo da modificare, per intero, con il commento che istruisce ad aggiungere il braccio **qui**
    - `supabase/migrations/20260807000000_capability_model.sql:166-184` — due cose: perche' `SET search_path = ''` con ogni riferimento schema-qualificato e' load-bearing su una `SECURITY DEFINER`, e la misura `EXPLAIN` che dice che **non e' `STABLE`** a produrre una valutazione per statement ma il wrapper `(select …)`
    - `supabase/migrations/20260807000000_capability_model.sql:231-237` — la regola di forma sull'API: una funzione esposta risponde **sul chiamante**, mai su un identificatore arbitrario, perche' in questo repository non esiste rate limiting
    - `.planning/phases/35-per-night-assignments/35-RESEARCH.md` § *Pitfall 1* — cosa va storto senza la guardia su `NULL`, e il segnale d'allarme: una cella della matrice di scrittura B3 che cambia su una tabella che questa fase non ha toccato
  </read_first>
  <action>
    `CREATE OR REPLACE FUNCTION private.has_capability(p_capability text,
    p_party_id uuid default null)` con la **stessa** firma, lo stesso
    `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = ''`, e il **braccio 1
    riprodotto byte per byte**.

    Aggiungere in `OR` un secondo `exists` su `public.party_assignments` con,
    **in quest'ordine**, queste condizioni:

    1. `p_party_id is not null` — **prima condizione**, e il commento accanto
       dice che **non e' difensiva: e' ASSIGN-01**. Le 67 policy esistenti
       chiamano senza secondo argomento; senza questa riga un'assegnazione a
       una notte concederebbe la capability ovunque, che e' l'opposto esatto di
       *«senza cambiare cio' che puo' fare in ogni altra serata»*.
    2. `pa.party_id = p_party_id`
    3. `pa.user_id = (select auth.uid())`
    4. `pa.capability = p_capability`
    5. `pa.revoked_at is null` — ASSIGN-03: la revoca e' un record, non una
       cancellazione, quindi il braccio deve escludere le righe revocate invece
       di aspettarsi che non esistano
    6. `now() < pa.ends_at` — **ASSIGN-02, lato server**, e questo e' il confine
       di sicurezza. `now()` e' l'orologio del **server**: non c'e' nessun modo
       per un dispositivo di spostarlo

    Scrivere accanto al braccio il paragrafo che il prossimo lettore chiedera':
    **il braccio dell'assegnazione non consulta `requires_approved` e non
    consulta lo stato**, e la ragione non e' una dimenticanza. Con la decisione
    bloccata (solo `master`, `organizer` e `staff` sono assegnabili) e con
    `profiles_role_implies_approved` in vigore dalla fase 43, un assegnatario e'
    `approved` **per regola di database**: la domanda su un `pending` sparisce
    **per costruzione**, invece di essere risposta con un test di stato. E un
    test di stato li' sarebbe un **nuovo modo di rifiutare qualcuno alla porta**,
    che e' precisamente cio' che questo progetto non aggiunge.

    Ripetere `GRANT EXECUTE ON FUNCTION private.has_capability(text, uuid) TO
    authenticated, anon;` dopo il `CREATE OR REPLACE`: con firma invariata i
    grant sopravvivono, ma la riga si rilegge e non si assume.

    **Nessuna policy nuova, nessuna policy modificata in questo task.** Le 67
    esistenti restano intatte: e' l'intera ragione per cui il braccio va qui.
  </action>
  <acceptance_criteria>
    - `grep -n "p_party_id is not null" supabase/migrations/20260809001000_assignment_resolver.sql` restituisce una riga, e quella riga precede `pa.party_id` nel file
    - Il file **non** contiene nessun `CREATE POLICY`
    - Il file contiene `now() < pa.ends_at` e `pa.revoked_at is null`
    - `npm run baseline:container -- --phase-point=35-03` gira, e il confronto con `35-pre` mostra **zero** differenze nelle policy: il braccio nuovo non altera nessuna definizione di policy
    - `npm run verify:capabilities -- --target=container` resta verde
  </acceptance_criteria>
  <verify>
    <automated>F=supabase/migrations/20260809001000_assignment_resolver.sql; grep -q "p_party_id is not null" $F && ! grep -q "CREATE POLICY" $F && grep -q "pa.revoked_at is null" $F && npm run baseline:container -- --phase-point=35-03 && npm run verify:capabilities -- --target=container</automated>
  </verify>
  <done>Il resolver ha due fonti; con `p_party_id` a `NULL` la seconda e' muta; nessuna policy e' stata toccata.</done>
</task>

<task type="auto">
  <name>Task 3: L'overload `public.my_access_context(uuid)`</name>
  <files>supabase/migrations/20260809001000_assignment_resolver.sql</files>
  <read_first>
    - `supabase/migrations/20260807000000_capability_model.sql:262-297` — `public.my_access_context()`: la forma del payload, il `REVOKE` seguito dal `GRANT`, e il fatto che i chiamanti sono **due soli**
    - `supabase/migrations/20260808002000_membership_register.sql:469-488` — la coppia REVOKE-poi-GRANT come **due statement in quell'ordine**, mai assunta: Postgres concede `EXECUTE` a `PUBLIC` di default su ogni funzione nuova, quindi il solo `GRANT` lascerebbe il default in piedi
    - `.planning/phases/35-per-night-assignments/35-PATTERNS.md` § S2 — la nota specifica sull'overload: `CREATE OR REPLACE` **non** cambia la lista argomenti, crea un secondo oggetto, quindi la coppia REVOKE/GRANT va scritta **per la firma nuova**
    - `supabase/migrations/20260808000000_access_context_user_id.sql` — l'ultima modifica al payload, per riprodurne esattamente le chiavi
  </read_first>
  <action>
    Creare `public.my_access_context(p_party_id uuid)` come **overload** — non
    una modifica della firma esistente. Il paragrafo che accompagna la scelta
    dice perche' l'alternativa e' stata rifiutata: cambiare la firma della
    funzione a zero argomenti richiederebbe `DROP FUNCTION` + `CREATE`
    (`CREATE OR REPLACE` fallisce con `42P13`), e un `DROP` in una coda applicata
    **a mano** e' una finestra in cui la funzione non esiste — nella coda
    sbagliata, quella che il middleware chiama a ogni richiesta.

    Il payload restituisce **le stesse chiavi** della versione a zero argomenti,
    con le capability risolte **su quella notte**: cioe' l'unione delle
    capability di ruolo e di quelle che l'assegnazione concede per
    `p_party_id`. Nessuna chiave nuova, nessun dato personale.

    Scrivere il paragrafo che risponde all'obiezione che qualcuno fara' in buona
    fede, citando `capability_model.sql:231-237`: quella regola vieta una
    funzione che risponda su un **identificatore arbitrario di soggetto**,
    perche' senza rate limiting sarebbe un oracolo. `p_party_id` **non e' quella
    forma**: non dice *«di chi parlare»*, dice *«in quale notte»*, e la funzione
    continua a rispondere **sul chiamante**, via `auth.uid()`. Senza questo
    paragrafo qualcuno rifiutera' la funzione per la ragione sbagliata.

    Chiudere con, in quest'ordine e come due statement separati:
    `REVOKE ALL ON FUNCTION public.my_access_context(uuid) FROM public, anon, authenticated;`
    poi `GRANT EXECUTE ON FUNCTION public.my_access_context(uuid) TO authenticated;`
    e un `COMMENT ON FUNCTION` che dice perche' esiste.

    `SET search_path = ''` con ogni riferimento schema-qualificato, anche se
    l'analogo piu' vicino non ce l'avesse.

    Chiudere la migration con `COMMIT;`.
  </action>
  <acceptance_criteria>
    - Il file contiene `REVOKE ALL ON FUNCTION public.my_access_context(uuid)` **prima** di `GRANT EXECUTE ON FUNCTION public.my_access_context(uuid)`
    - Il file contiene `SET search_path = ''` per ogni `CREATE OR REPLACE FUNCTION` che definisce
    - La funzione a zero argomenti non e' stata droppata: `grep -c "DROP FUNCTION" supabase/migrations/20260809001000_assignment_resolver.sql` restituisce 0
    - `npm run baseline:container -- --phase-point=35-03-final` gira senza errori
  </acceptance_criteria>
  <verify>
    <automated>F=supabase/migrations/20260809001000_assignment_resolver.sql; grep -q "REVOKE ALL ON FUNCTION public.my_access_context(uuid)" $F && grep -q "GRANT EXECUTE ON FUNCTION public.my_access_context(uuid)" $F && test "$(grep -c 'DROP FUNCTION' $F)" = "0" && npm run baseline:container -- --phase-point=35-03-final</automated>
  </verify>
  <done>Esiste una seconda firma che risponde la domanda per-notte; la prima e' intatta; i due chiamanti esistenti non sono stati toccati.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessione autenticata → PostgREST | `my_access_context` e' l'unica funzione del modello esposta a `authenticated` |
| policy RLS → `private.has_capability` | 67 predicati chiamano il resolver; un errore qui si propaga a tutti in una volta |
| `private` → PostgREST | Lo schema `private` non e' esposto: la lista degli schemi e' `public, graphql_public` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-10 | Elevation of Privilege | un'assegnazione di una notte che concede la capability ovunque | mitigate | `p_party_id is not null` come **prima** condizione del braccio 2; asserito dal confronto B2/B3 byte-identico su ogni altra tabella (piano 35-06) |
| T-35-11 | Elevation of Privilege | permanenza del potere oltre la notte, lato server | mitigate | `now() < pa.ends_at` nel braccio; `now()` e' l'orologio del server e nessun dispositivo lo sposta |
| T-35-12 | Elevation of Privilege | `my_access_context(uuid)` come oracolo su un soggetto arbitrario | mitigate | La funzione risponde **sul chiamante** (`auth.uid()`); `p_party_id` e' *«in quale notte»*, non *«di chi»*. `REVOKE` da `public`/`anon`, `GRANT` al solo `authenticated` |
| T-35-13 | Elevation of Privilege | `EXECUTE` di default a `PUBLIC` sull'overload nuovo | mitigate | REVOKE **poi** GRANT, due statement, per la firma nuova — non ereditati dalla vecchia |
| T-35-14 | Tampering | `search_path` mutabile su una `SECURITY DEFINER` | mitigate | `SET search_path = ''` con ogni riferimento schema-qualificato |
| T-35-15 | Denial of Service | valutazione del resolver una volta per riga | mitigate | Ogni call site nuovo nella forma `(select private.has_capability(…))`; nessuna policy nuova in questo piano |
</threat_model>

<verification>
- `npm run verify:capabilities -- --target=container` verde su cinque lati, con 48 coppie
- `npm run baseline:container` prima e dopo: **nessuna differenza nelle definizioni di policy**
- `npm run build` passa
</verification>

<success_criteria>
- Il resolver ha due fonti e il secondo braccio e' spento quando la notte non e' nominata
- Tre chiavi nuove esistono, ognuna con quattro decisioni dichiarate
- Il DAL puo' fare la domanda per-notte senza che nessuna delle 67 policy sia stata toccata
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-03-SUMMARY.md` when done
</output>
