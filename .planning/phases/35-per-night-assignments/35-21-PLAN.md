---
phase: 35-per-night-assignments
plan: 21
type: execute
wave: 8
depends_on: ["35-18", "35-20"]
files_modified:
  - src/app/(public)/events/[slug]/page.tsx
  - src/app/(public)/events/[slug]/MediaGallerySection.tsx
  - src/components/media/MediaUpload.tsx
  - supabase/migrations/20260809006000_event_media_server_upload_only.sql
  - scripts/verify-media-strip.mjs
  - package.json
autonomous: true
requirements: [ASSIGN-01]

must_haves:
  truths:
    - "Chi carica sceglie — o si vede assegnata — la notte a cui il file appartiene, e quella notte arriva fino alla riga scritta"
    - "Una persona `staff` assegnata come «photo» a una notte vede la casella di caricamento; senza assegnazione non la vede"
    - "Il browser non puo' piu' scrivere nel bucket pubblico: l'unico che scrive e' la rotta che spoglia"
    - "Esiste un controllo strutturale che fallisce se il percorso di persistenza diventa raggiungibile senza passare dalla spoglia"
  artifacts:
    - path: "src/components/media/MediaUpload.tsx"
      provides: "il deposito in quarantena, la chiamata alla rotta di finalizzazione, e la notte scelta"
      contains: "event-media-quarantine"
    - path: "supabase/migrations/20260809006000_event_media_server_upload_only.sql"
      provides: "la quindicesima riga della coda: il browser perde la scrittura sul bucket pubblico"
      contains: "Members can upload event media"
    - path: "scripts/verify-media-strip.mjs"
      provides: "il controllo strutturale: un solo scrittore verso il bucket pubblico, e passa dalla spoglia"
      contains: "stripImageMetadata"
  key_links:
    - from: "src/app/(public)/events/[slug]/page.tsx"
      to: "MediaGallerySection → MediaUpload"
      via: "l'elenco delle serate su cui questa persona puo' caricare, risolto lato server"
      pattern: "MEDIA_UPLOAD"
    - from: "src/components/media/MediaUpload.tsx"
      to: "/api/media/finalize"
      via: "la finalizzazione, che e' l'unico modo di far diventare pubblico un file"
      pattern: "api/media/finalize"
---

<objective>
Chiudere le due meta' che restano: **la superficie deve saper nominare la
notte**, e **il browser deve smettere di poter scrivere nel bucket pubblico**.

Purpose: senza la prima, il permesso per-notte esiste e non e' spendibile — e
c'e' di piu' di un dettaglio di interfaccia. Misurato il 2026-08-08,
`page.tsx:385` calcola `canUpload` come *approvato **e** presente*, oppure
organizer, oppure master; il braccio della presenza interroga una tabella che
non esiste (`.from("attendance")` contro `public.attendances`,
`schema.sql:231`), quindi **oggi la casella di caricamento non compare a nessun
membro**. Un fotografo `staff` assegnato non la vedrebbe nemmeno con il
predicato del piano 35-16 gia' spedito: e' la stessa forma di guasto che il
piano 35-17 ha chiuso sul middleware — un permesso concesso a qualcuno che non
riesce ad arrivare allo strumento.

Senza la seconda, la spoglia dei metadati e' aggirabile: chiunque puo' scrivere
direttamente nel bucket pubblico dal browser, ed e' esattamente cio' che il
codice fa oggi (`MediaUpload.tsx:160-162`).

Output: la superficie che porta la notte, la quindicesima migration, e il
controllo strutturale che vale piu' di una prova su un file solo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-PATTERNS.md
@.claude/rules/media-and-storage.md
@.claude/rules/venue-secrecy.md
@.claude/rules/nextjs-architecture.md
</context>

<interfaces>
Lo stato verificato, letto il 2026-08-08:

- `src/app/(public)/events/[slug]/page.tsx:315-325` — il calcolo di
  `hasAttended`, con la query verso `attendance` che non trova nessuna tabella.
- `src/app/(public)/events/[slug]/page.tsx:385` —
  `const canUpload = isAuthenticated && ((isApproved && hasAttended) || isOrganizer || isMasterRole);`
- `src/app/(public)/events/[slug]/page.tsx:721` — dove `canUpload` viene passato
  a `MediaGallerySection`.
- `src/app/(public)/events/[slug]/MediaGallerySection.tsx:10-14` — le tre props
  odierne: `media`, `canUpload`, `eventId`.
- `src/components/media/MediaUpload.tsx:8-14` — i tipi ammessi e i tetti.
- `src/components/media/MediaUpload.tsx:151-178` — il ciclo di caricamento:
  path derivato, `supabase.storage.from("event-media").upload(...)`, poi
  `registerMedia(...)`.
- `supabase/migrations/20260225120000_phase7_media.sql:70-77` — la policy
  *«Members can upload event media»*, `FOR INSERT TO authenticated`, che e'
  esattamente il permesso che la migration di questo piano toglie.
- `supabase/migrations/20260225120000_phase7_media.sql:79-101` — la policy di
  lettura pubblica e le **due** policy di cancellazione: **nessuna delle tre si
  tocca**, perche' `deleteMedia` cancella l'oggetto con il client legato ai
  cookie e senza quelle smetterebbe di funzionare.

Dal piano 35-16: `registerMedia(eventId, partyId, storagePath, type, fileSize)`.
Dal piano 35-20: `POST /api/media/finalize` con
`{ eventId, partyId, quarantinePath, mimeType, fileSize }`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: La superficie impara a nominare la notte</name>
  <files>src/app/(public)/events/[slug]/page.tsx, src/app/(public)/events/[slug]/MediaGallerySection.tsx, src/components/media/MediaUpload.tsx</files>
  <read_first>
    - `src/app/(public)/events/[slug]/page.tsx:300-400` — dove nascono `hasAttended` e `canUpload`, e cosa la pagina gia' legge: il perimetro da **non** allargare
    - `src/app/(public)/events/[slug]/page.tsx:700-730` — il punto in cui `MediaGallerySection` riceve le sue props
    - `src/components/media/MediaUpload.tsx:125-193` — `handleUpload` per intero: la validazione, il path derivato, la scrittura allo storage e la registrazione
    - `src/lib/capabilities/server.ts` — `hasCapability(key, { partyId })` (piano 35-07) e la regola che **ogni fallimento lancia**
    - `.claude/rules/venue-secrecy.md` — gate *percorsi enumerati*: questa pagina **e'** uno dei percorsi che possono rivelare una sede, quindi ogni lettura aggiunta qui va giustificata
    - `.claude/rules/nextjs-architecture.md` — gate *stato vuoto e d'errore*, e gate *segreti nel bundle*
  </read_first>
  <action>
    **In `page.tsx`.** Leggere le serate dell'evento — **soltanto** `id`,
    `title` e `type` da `public.event_parties` per `event_id` — e risolvere, per
    ciascuna, `hasCapability(CAP.MEDIA_UPLOAD, { partyId })`. Il risultato e' un
    elenco `uploadableParties` di sole serate su cui **questa** persona puo'
    caricare.

    **Perche' qui un ciclo va bene e nel predicato no**, e va scritto accanto:
    la domanda del gate e' *«puo' caricare su QUESTA notte?»* — singolare, una
    risoluzione, ed e' il piano 35-16. La domanda della superficie e' *«su quali
    notti puo' caricare?»* — plurale per costruzione, perche' deve disegnare un
    elenco. `event_parties` porta `UNIQUE (event_id, type)` su tre valori:
    **al massimo tre** risoluzioni, su un render di pagina, e **non** sul
    percorso della porta. Il vincolo che limita il ciclo va nominato, perche' se
    un giorno i tipi di serata diventassero N, questo diventerebbe N andate e
    ritorni.

    `canUpload` diventa: i due bracci di oggi **invariati** — e il braccio della
    presenza resta **parola per parola** quello attuale, difetto della tabella
    compreso, per la ragione scritta nel piano 35-16 — **oppure**
    `uploadableParties.length > 0`.

    **Il perimetro di lettura non si allarga.** Non leggere `venue_secret`, non
    leggere l'indirizzo, non leggere nulla che la pagina non legga gia': questa
    e' una superficie enumerata da `venue-secrecy.md`, e l'unica cosa nuova che
    arriva al client e' **id, titolo e tipo** di serate che quella persona ha
    gia' il diritto di vedere. Scriverlo nel commento e' cio' che permette a chi
    rilegge di verificarlo senza rifare l'analisi.

    Passare `uploadableParties` a `MediaGallerySection`, e da li' a
    `MediaUpload`. **Nessun'altra prop, nessun'altra modifica** a
    `MediaGallerySection`: e' un passacarte, e va lasciato tale.

    **In `MediaUpload.tsx`.** Tre cambiamenti, e nient'altro:

    1. **La notte si sceglie prima di caricare.** Se le serate sono piu' di una,
       un selettore obbligatorio, **senza preselezione**: precompilare
       significherebbe che il caso comune e' scegliere per distrazione, e la
       notte sbagliata su una foto scattata dentro una sede segreta non e' un
       dettaglio di catalogazione. Con **una sola** serata, nessun selettore: si
       usa quella, e la si **nomina** a schermo, cosi' che chi carica sappia
       dove sta mettendo il file. Con **zero** serate il componente non renderizza
       il pulsante — ma il caso non dovrebbe arrivare qui, perche' `canUpload`
       sarebbe falso: se ci arriva, e' uno stato d'errore distinguibile, non una
       casella muta.
    2. **Il deposito cambia bucket.** `supabase.storage.from(
       "event-media-quarantine").upload(path, file)` con lo **stesso** schema di
       path di oggi. Poi `POST /api/media/finalize` con `{ eventId, partyId,
       quarantinePath, mimeType, fileSize }`, e **solo se** la rotta risponde
       con successo, `registerMedia(eventId, partyId, storagePath, type,
       fileSize)` con il path che la rotta ha restituito.
    3. **I rifiuti diventano frasi distinte.** La rotta risponde con una
       **categoria**; il componente la traduce in una frase per ognuna, mai in un
       messaggio unico. Le due che contano davvero e vanno scritte per esteso:
       *la spoglia dei metadati non e' riuscita, quindi il file non e' stato
       caricato* e *i video non sono ammessi su questa serata*. Non esiste error
       tracking in questo prodotto: **questa schermata e' l'unico posto in cui il
       fallimento diventa osservabile**, e un `console.error` non e' un effetto.

    **Cio' che non si tocca:** i tipi ammessi, i tetti di dimensione, l'anteprima,
    il ciclo su piu' file, il testo di successo. Questo task cambia **dove
    atterrano i byte** e **cosa li accompagna**, non cosa e' ammesso.
  </action>
  <acceptance_criteria>
    - `page.tsx` legge `event_parties` e risolve `CAP.MEDIA_UPLOAD` per serata; `canUpload` ha un terzo braccio
    - `page.tsx` **non** contiene `venue_secret` in nessuna riga aggiunta dal diff
    - Il braccio della presenza in `page.tsx` e' invariato: `grep -c 'from("attendance")' ` resta 1
    - `MediaUpload.tsx` scrive **solo** su `event-media-quarantine`: `grep -c 'from("event-media")' ` restituisce 0
    - `MediaUpload.tsx` chiama `/api/media/finalize` e passa `partyId` a `registerMedia`
    - `MediaUpload.tsx` porta almeno due frasi di rifiuto distinte, e nessun messaggio generico unico
    - `npm run build` passa
  </acceptance_criteria>
  <verify>
    <automated>P='src/app/(public)/events/[slug]/page.tsx'; U=src/components/media/MediaUpload.tsx; npm run build && grep -q 'MEDIA_UPLOAD' "$P" && test "$(git diff -- "$P" | grep -c '^+.*venue_secret')" = "0" && test "$(grep -c 'from(\"attendance\")' "$P")" = "1" && test "$(grep -c 'from(\"event-media\")' $U)" = "0" && grep -q 'event-media-quarantine' $U && grep -q 'api/media/finalize' $U && grep -q 'partyId' $U && echo SURFACE_OK</automated>
  </verify>
  <done>Chi e' assegnato come «photo» a una notte vede la casella, sceglie la notte, e quella notte arriva fino alla riga scritta.</done>
</task>

<task type="auto">
  <name>Task 2: La quindicesima migration — il browser perde la scrittura sul bucket pubblico</name>
  <files>supabase/migrations/20260809006000_event_media_server_upload_only.sql</files>
  <read_first>
    - `supabase/migrations/20260225120000_phase7_media.sql:68-102` — le quattro policy su `storage.objects` per `event-media`: **una** si toglie, **tre** restano, e quali sono va letto qui e non ricordato
    - `.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md` — la coda scritta dal piano 35-01, e il blocco «dopo il deploy» in cui questa riga sta gia' elencata
    - `.claude/rules/media-and-storage.md` — i **due confini che non coincidono**: la riga e l'oggetto
    - `.claude/rules/meta-gates.md` — le guardie monotone: questa migration rende una porta **piu' difficile** da attraversare, mai piu' facile, che e' la direzione consentita
  </read_first>
  <action>
    Scrivere `20260809006000_event_media_server_upload_only.sql`, e deve essere
    **corta**: una sola istruzione che conta.

    `DROP POLICY IF EXISTS "Members can upload event media" ON storage.objects;`
    e **nessuna policy nuova al suo posto**. Da quel momento l'unico che puo'
    scrivere nel bucket `event-media` e' il service role, cioe' la rotta
    `/api/media/finalize`, cioe' il percorso che spoglia.

    **Le tre policy che restano, elencate nel commento con il loro nome** — e va
    scritto perche' restano, non solo che restano:
    - *«Anyone can view event media»*: il bucket e' pubblico in lettura oggi e
      resta tale. Renderlo privato e' una decisione diversa, con conseguenze su
      ogni URL gia' pubblicato, e non e' in nessuno degli otto requisiti.
    - *«Members can delete own event media»* e *«Admins can delete event
      media»*: `deleteMedia` cancella l'oggetto con il client legato ai cookie
      (`actions.ts:228-230`). Togliere queste due romperebbe la cancellazione —
      e `media-and-storage.md` pretende che la rimozione resti **davvero
      possibile**, perche' e' cio' che rende praticabile la revoca del consenso
      di chi e' ritratto (`legal-compliance.md`).

    **L'intestazione del file deve portare l'eccezione, per intero.** Questa e'
    **l'unica riga della coda che si applica DOPO il deploy del codice della
    fase**, e la ragione non e' una preferenza: applicarla prima lascerebbe i
    caricamenti dei membri rotti fino al deploy, perche' il codice vecchio scrive
    ancora dal browser. Applicarla dopo lascia aperta, per la durata del deploy,
    **la stessa porta che e' aperta oggi** — cioe' non peggiora niente rispetto
    allo stato attuale. Fra un peggioramento e uno stato invariato si sceglie lo
    stato invariato, e lo si **dichiara**.

    E la frase che chiude: **finche' questa riga non e' applicata, la spoglia dei
    metadati e' aggirabile.** Va scritta qui, in testa al file che la chiude,
    perche' e' il posto in cui la trovera' chi sta per applicarla.

    **Nessuna tabella toccata. Nessuna policy su `public.*`.**
  </action>
  <acceptance_criteria>
    - Il file contiene esattamente un `DROP POLICY`, e nomina *«Members can upload event media»*
    - Il file **non** contiene nessun `CREATE POLICY`
    - Il file **non** nomina `Anyone can view event media`, `Members can delete own event media` o `Admins can delete event media` in un `DROP`
    - Il file contiene, in testa, la frase «dopo il deploy» e la frase che dice che finche' non e' applicata la spoglia e' aggirabile
    - `npm run baseline:container -- --phase-point=35-final` gira senza errori
  </acceptance_criteria>
  <verify>
    <automated>F=supabase/migrations/20260809006000_event_media_server_upload_only.sql; test "$(grep -c 'DROP POLICY' $F)" = "1" && grep -q 'Members can upload event media' $F && ! grep -q 'CREATE POLICY' $F && ! grep -qE 'DROP POLICY.*(Anyone can view|can delete)' $F && grep -qi 'dopo il deploy' $F && npm run baseline:container -- --phase-point=35-final</automated>
  </verify>
  <done>Il browser non puo' piu' scrivere nel bucket pubblico, la lettura e la cancellazione sono invariate, e l'eccezione d'ordine e' scritta dove verra' letta.</done>
</task>

<task type="auto">
  <name>Task 3: `verify:media-strip` — il controllo che vale piu' di una prova su un file solo</name>
  <files>scripts/verify-media-strip.mjs, package.json</files>
  <read_first>
    - `scripts/verify-no-header-identity.mjs:1-60` — l'intestazione: **cosa asserisce in una frase**, perche' quella frase e non una piu' bella, e soprattutto la sezione *cosa un verde NON significa*. E' la forma obbligatoria per un controllo strutturale in questo repository
    - `scripts/verify-no-header-identity.mjs` (coda) — la forma dell'uscita: le righe offensive con `path:riga`, il verdetto, e `process.exit(1)`
    - `.claude/rules/ai-engineering.md` — gate *prova per mutazione*: ogni controllo va provato rompendo l'invariante e verificando che scatti, **e la mutazione va verificata di per se'**, perche' una sostituzione che non va a segno produce un falso negativo
    - `.claude/rules/meta-gates.md` — il gate della verifica in un repo senza test
  </read_first>
  <action>
    Creare `scripts/verify-media-strip.mjs` e registrarlo in `package.json` come
    `"verify:media-strip"`.

    **La frase che asserisce, scritta in testa e in una riga sola:** *nessun file
    sotto `src/` scrive nel bucket `event-media` tranne
    `src/app/api/media/finalize/route.ts`, e in quel file la chiamata a
    `stripImageMetadata` precede ogni scrittura.*

    I cinque controlli, ognuno con il suo messaggio d'uscita che nomina il file e
    la riga:

    - **A.** Nessun file sotto `src/` diverso dalla rotta di finalizzazione
      contiene una scrittura verso il bucket `event-media` — cioe' il nome del
      bucket accompagnato da `.upload(`. Il nome del bucket di quarantena e'
      **escluso**, ma con un confronto che non lo confonda col prefisso:
      `event-media-quarantine` comincia con `event-media`, ed e' esattamente il
      tipo di corrispondenza parziale che farebbe passare un controllo per il
      motivo sbagliato. Scrivere questa nota accanto.
    - **B.** Nella rotta di finalizzazione, il numero di riga dell'ultima
      chiamata a `stripImageMetadata(` e' **minore** di quello della prima
      scrittura verso `event-media`.
    - **C.** `src/components/media/MediaUpload.tsx` non contiene nessuna
      scrittura verso `event-media` (solo verso la quarantena).
    - **D.** Esiste `supabase/migrations/20260809006000_event_media_server_upload_only.sql`,
      contiene il `DROP POLICY` della policy di caricamento dei membri, e
      **nessuna** migration con timestamp successivo ricrea una policy di INSERT
      per `authenticated` su quel bucket. E' il controllo che impedisce a una
      fase futura di riaprire la porta senza accorgersene.
    - **E.** `sharp` e' dichiarato in `dependencies` di `package.json`.

    **Igiene dei grep, che qui e' portante.** I file da ispezionare vanno letti e
    filtrati **togliendo le righe di commento prima di contare**, altrimenti
    l'intestazione di questo stesso script — che nomina `event-media` e
    `stripImageMetadata` — e i commenti dei file ispezionati farebbero scattare o
    tacere il controllo per conto proprio. Un controllo che si auto-invalida
    scrivendo la propria documentazione e' il precedente che questo repository ha
    gia' registrato.

    **La sezione «cosa un verde NON significa», obbligatoria**, e va scritta con
    almeno queste quattro righe:
    - non dimostra che `sharp` **rimuova davvero** i metadati: e' una proprieta'
      a tempo d'esecuzione, e la prova e' la procedura manuale in
      `35-HUMAN-UAT.md` — un file con GPS noto entra, l'oggetto salvato torna
      senza;
    - non dimostra niente sui **video**, che questa fase non spoglia;
    - non dimostra che la migration sia stata **applicata**: legge un file, non
      un database;
    - non dimostra che qualcuno sia autorizzato: quello e' `verify:capabilities`,
      e comunque **la RLS e' il confine**, non uno script.

    **La prova per mutazione, e va eseguita davvero.** Per ognuno dei cinque
    controlli: rompere deliberatamente l'invariante, **verificare che la
    sostituzione sia andata a segno**, osservare l'uscita 1 con il messaggio
    giusto, ripristinare. Registrare nel SUMMARY le cinque mutazioni con il
    messaggio ottenuto. Un controllo non provato per mutazione, in un repository
    senza test, e' indistinguibile da un controllo morto.

    **Non collegare lo script a `npm run build`.** Il build e' il gate dei tipi, e
    un gate dei tipi che comincia a fallire per una ragione che non e' un tipo
    insegna a tutti a ignorarlo — e' la stessa decisione gia' presa per
    `verify-no-header-identity.mjs`.
  </action>
  <acceptance_criteria>
    - `scripts/verify-media-strip.mjs` esiste, e `package.json` porta `"verify:media-strip"`
    - Lo script esce **0** sull'albero come questo piano lo lascia
    - Lo script contiene la sezione «cosa un verde NON significa» con almeno quattro voci, e la nota sul prefisso `event-media` / `event-media-quarantine`
    - Il SUMMARY riporta **cinque** mutazioni, ognuna con la conferma che la sostituzione e' andata a segno e con il messaggio d'uscita ottenuto
    - `npm run build` **non** invoca questo script
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q '"verify:media-strip"' package.json && ! node -e "const p=require('./package.json'); if(/verify:media-strip/.test(p.scripts.build)) process.exit(1)" ; node scripts/verify-media-strip.mjs && grep -qi "NON significa\|does NOT mean" scripts/verify-media-strip.mjs && grep -q "event-media-quarantine" scripts/verify-media-strip.mjs && echo STRIP_STRUCTURAL_OK</automated>
  </verify>
  <done>Esiste un controllo che fallisce se qualcuno riapre un secondo percorso verso il bucket pubblico, ed e' stato provato rompendolo cinque volte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `storage.objects` | Finche' la quindicesima migration non e' applicata, il browser scrive nel bucket pubblico |
| pagina pubblica dell'evento → chiunque | E' una superficie enumerata da `venue-secrecy.md`: ogni lettura aggiunta qui e' un percorso d'uscita potenziale |
| client → `/api/media/finalize` | Il `partyId` arriva dal client e non e' una prova di niente: chi decide e' il predicato lato server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-115 | Elevation of Privilege | il browser che scrive direttamente nel bucket pubblico, aggirando la spoglia | mitigate | La quindicesima migration toglie la policy di INSERT per `authenticated`; il controllo **D** impedisce che una fase futura la ricrei |
| T-35-116 | Information Disclosure | la pagina pubblica che, per disegnare l'elenco delle serate, comincia a leggere piu' del necessario | mitigate | Solo `id`, `title`, `type`; nessun `venue_secret`, asserito sul diff |
| T-35-117 | Spoofing | un `partyId` scelto a mano nel client | mitigate | Il client propone, il predicato lato server decide; la rotta rifiuta con la sua categoria |
| T-35-118 | Repudiation | un rifiuto della spoglia che l'utente legge come «errore di rete» | mitigate | Una frase per categoria, e le due che contano scritte per esteso; non esiste error tracking, quindi lo schermo e' l'unico effetto osservabile |
| T-35-119 | Denial of Service | la cancellazione dei media rotta insieme al caricamento | mitigate | Le due policy di DELETE e quella di SELECT non sono toccate, asserito da grep, e la ragione e' scritta nel commento della migration |
| T-35-120 | Repudiation | un controllo strutturale che passa perche' non controlla niente | mitigate | Cinque mutazioni eseguite e registrate, ognuna con la conferma che la sostituzione e' andata a segno |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Questo piano aggiunge una voce a `scripts`, **nessuna** a `dependencies` |
</threat_model>

<verification>
- `npm run build` passa
- `node scripts/verify-media-strip.mjs` esce 0
- `npm run baseline:container -- --phase-point=35-final` applica l'intera coda
- Le tre policy di lettura e cancellazione del bucket sono invariate
</verification>

<success_criteria>
- Il permesso per-notte e' spendibile: chi e' assegnato vede la casella e nomina la notte
- Esiste un solo percorso verso il bucket pubblico, e un controllo che fallisce se ne compare un secondo
- L'eccezione d'ordine della quindicesima migration e' scritta dove chi la applica la leggera'
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-21-SUMMARY.md` when done
</output>
