---
phase: 35-per-night-assignments
plan: 09
type: execute
wave: 5
depends_on: ["35-03", "35-06"]
files_modified:
  - supabase/migrations/20260809004000_door_scan_events_by_assignment.sql
  - scripts/container/seed.mjs
autonomous: true
requirements: [ASSIGN-01]

must_haves:
  truths:
    - "Chi tiene `staff.manage` continua a leggere l'intero registro della porta"
    - "Chi ha solo un'assegnazione legge il registro della notte che ha lavorato, e di nessun'altra"
    - "Chi e' assegnato come organizer di una notte legge il registro di quella notte: la superficie di revisione del piano 35-15 non mostra una lista vuota a chi ha titolo"
    - "Il commento che indicava questa fase come lavoro da fare e' stato sostituito, non affiancato"
  artifacts:
    - path: "supabase/migrations/20260809004000_door_scan_events_by_assignment.sql"
      provides: "door_scan_events_select_admin riscritta sul predicato di capability, per assegnazione, a tre braccia"
      contains: "door_scan_events_select_admin"
  key_links:
    - from: "door_scan_events_select_admin"
      to: "private.has_capability('door.operate', party_id)"
      via: "secondo braccio del predicato, per-riga sulla colonna party_id della tabella"
      pattern: "has_capability"
    - from: "door_scan_events_select_admin"
      to: "private.has_capability('party.manage', party_id)"
      via: "terzo braccio: chi gestisce la notte legge il registro della notte, ed e' cio' che rende non vuota la pagina di revisione del piano 35-15"
      pattern: "party.manage"
---

<objective>
Restringere `door_scan_events_select_admin`, che oggi e' **deliberatamente
grossolana** e porta il commento che nomina **questa fase** come quella che deve
restringerla: *«per-night scoping of an organizer does not exist in this product
until Phase 35, which is the phase that should narrow this predicate»*.

Purpose: la restrizione e' **per assegnazione, non per ruolo**. Chi tiene
`staff.manage` continua a leggere tutto — un organizer che rivede il record di
una serata dopo non deve diventare cieco per un miglioramento d'accesso. Chi ha
solo un'assegnazione legge la propria notte. E il predicato passa da
`public.is_admin_or_organizer()`, che e' **superato**, a
`private.has_capability`, che e' la sola definizione.

Output: la migration 11 della coda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-PATTERNS.md
@.claude/rules/checkin-offline.md
</context>

<interfaces>
La policy attuale (`supabase/migrations/20260805120000_door_scan_events.sql:151-156`):

```
-- Deliberately coarse: per-night scoping of an organizer does not exist in this
-- product until Phase 35, which is the phase that should narrow this predicate.
CREATE POLICY door_scan_events_select_admin ON public.door_scan_events
  FOR SELECT USING ((SELECT public.is_admin_or_organizer()));
```

La tabella porta `party_id uuid NOT NULL REFERENCES public.event_parties ON
DELETE CASCADE` (`:65`), verificato — quindi i bracci per-notte hanno una colonna
su cui agganciarsi riga per riga.

`public.door_scan_events` **non ha nessuna policy di scrittura**, e l'omissione
e' deliberata (`:158-163`). Questo piano **non la aggiunge**.

Il consumatore del terzo braccio, che non esisteva quando questo piano e' stato
scritto la prima volta: la pagina
`src/app/(organizer)/organizer/events/[id]/review/page.tsx`, che il piano 35-15
apre a chi tiene `party.manage` su **quella** notte. Quella pagina legge
`door_scan_events` con il **client legato ai cookie** e non con il service
client — la scelta e' dichiarata nel suo docblock — quindi la policy qui e'
l'unica cosa che decide cosa vede. Senza il terzo braccio la pagina renderebbe
una lista **vuota** a chi ha titolo, e su quella superficie una lista vuota e'
lo stato **progettato** di una serata tranquilla: sarebbe un fallimento
silenzioso con una faccia neutra (`meta-gates.md`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: La policy riscritta a tre braccia, e il commento sostituito</name>
  <files>supabase/migrations/20260809004000_door_scan_events_by_assignment.sql</files>
  <read_first>
    - `supabase/migrations/20260805120000_door_scan_events.sql:140-163` — la RLS attuale per intero: la policy grossolana con il commento che nomina la fase 35, e il paragrafo che dichiara l'omissione delle policy di scrittura
    - `supabase/migrations/20260808002000_membership_register.sql:314-349` — la forma da seguire: `DROP POLICY IF EXISTS` prima, predicato **sempre** dentro `(SELECT …)`, e il paragrafo che dichiara ogni omissione
    - `supabase/migrations/20260807000000_capability_model.sql:177-184` — il wrapper `(select …)` e' load-bearing e **non e' `STABLE`** a produrlo: e' la misura `EXPLAIN` fatta su questo database
    - `.planning/phases/35-per-night-assignments/35-PATTERNS.md:1277-1297` — § D di *Cosa NON copiare*: `is_admin_or_organizer()` e' il predicato superato, e **il commento vecchio va sostituito, non appeso** — un commento che indica un lavoro gia' fatto e' peggio di nessun commento
    - `src/app/(organizer)/organizer/events/[id]/review/page.tsx:96-120` — il paragrafo che dichiara che quella pagina usa il client legato ai cookie **e non** il service client, e che percio' questa policy e' il confine di lettura: e' la ragione del terzo braccio
  </read_first>
  <action>
    Creare `supabase/migrations/20260809004000_door_scan_events_by_assignment.sql`,
    intestazione nella forma consueta, `BEGIN;`.

    `DROP POLICY IF EXISTS door_scan_events_select_admin ON public.door_scan_events;`
    poi ricrearla con un predicato a **tre braccia**, in OR:

    - `(SELECT private.has_capability('staff.manage'))` — chi gestisce lo staff
      legge tutto. E' il braccio che **non restringe niente rispetto a oggi**
      per chi contava, e va detto: la restrizione non deve produrre un organizer
      che non riesce piu' a rivedere una serata
    - **oppure** `(SELECT private.has_capability('door.operate', party_id))` —
      chi ha lavorato **quella** porta legge **quella** riga. `party_id` e' la
      colonna della riga in valutazione: e' il punto in cui il secondo braccio
      del resolver diventa una restrizione per-notte senza che nessuna riga di
      applicazione cambi
    - **oppure** `(SELECT private.has_capability('party.manage', party_id))` —
      chi **gestisce** quella notte legge il registro di quella notte. E' un
      braccio diverso dal precedente e non una comodita': lavorare la porta e
      gestire la serata sono due mestieri distinti (`keys.ts:38-45`, una chiave
      si nomina dalla domanda che risponde), e chi e' assegnato come organizer
      di una notte non e' automaticamente assegnato alla sua porta.
      **Scrivere accanto la ragione operativa**: la pagina di revisione della
      notte legge questa tabella con il client legato ai cookie, quindi senza
      questo braccio mostrerebbe una lista vuota — e una lista vuota li' e' lo
      stato progettato di una serata senza anomalie. Il difetto non alzerebbe
      nessun errore: direbbe *«nessun problema»* a chi non ha il permesso di
      vedere i problemi

    Tutte e tre le chiamate nella forma `(SELECT …)`, senza eccezioni.

    **Sostituire il commento vecchio**, non affiancarlo: il paragrafo che diceva
    *«deliberately coarse … until Phase 35»* sparisce, e al suo posto va il
    criterio nuovo, scritto per esteso — **si restringe per assegnazione, non per
    ruolo**, con la ragione: restringere per ruolo renderebbe invisibile una
    serata a chi deve rivederla dopo, e questo prodotto non aggiunge modi nuovi
    di negare qualcosa a chi lo aveva.

    Scrivere anche cosa **non** cambia: `public.is_admin_or_organizer()`
    continua a esistere ed e' ancora usata altrove; questo piano **non** la
    rimuove e non la tocca. Rimuoverla e' un lavoro di un'altra fase, e
    accennarlo qui senza farlo sarebbe un altro commento che indica un lavoro
    che nessuno ha preso.

    **Nessuna policy di scrittura**, e il paragrafo che ricorda perche'
    l'omissione resta deliberata.

    `COMMIT;`.
  </action>
  <acceptance_criteria>
    - Il file contiene `DROP POLICY IF EXISTS door_scan_events_select_admin` prima del `CREATE POLICY`
    - Il file **non** contiene `is_admin_or_organizer` dentro il predicato della policy nuova
    - Tutte e tre le chiamate a `has_capability` sono avvolte in `(SELECT …)`
    - Il file contiene la stringa `party.manage`
    - Il file **non** contiene `FOR INSERT`, `FOR UPDATE` o `FOR DELETE`
    - Il file contiene il criterio nuovo scritto per esteso, che nomina *per assegnazione, non per ruolo*
  </acceptance_criteria>
  <verify>
    <automated>F=supabase/migrations/20260809004000_door_scan_events_by_assignment.sql; grep -q "DROP POLICY IF EXISTS door_scan_events_select_admin" $F && ! grep -q "FOR INSERT" $F && ! grep -q "FOR UPDATE" $F && ! grep -q "FOR DELETE" $F && grep -q "party.manage" $F && test "$(grep -c 'SELECT private.has_capability' $F)" -ge 3 && echo POLICY_OK</automated>
  </verify>
  <done>La policy legge il criterio nuovo su tre braccia, e nel repository esiste un solo paragrafo su questa policy.</done>
</task>

<task type="auto">
  <name>Task 2: La prova che l'insieme di lettura non si e' ristretto per chi contava</name>
  <files>scripts/container/seed.mjs</files>
  <read_first>
    - `scripts/rls-baseline.mjs` — la sezione B2, la matrice di **lettura**: e' lo strumento che risponde alla domanda di questo task
    - `.planning/phases/32-capability-model-in-the-database/baseline/` — le catture `35-pre` e quelle dei piani precedenti di questa fase, da confrontare
    - `scripts/container/seed.mjs` — le tre persone del terzo asse introdotte dal piano 35-06: sono le uniche che rendono questa prova non vacua
  </read_first>
  <action>
    Catturare e confrontare, e **registrare il risultato nel SUMMARY come dati**,
    non come affermazione.

    Eseguire `npm run baseline:container -- --phase-point=35-09` e poi
    `npm run baseline:compare` contro `35-pre`.

    Le **quattro** osservazioni che il SUMMARY deve riportare con i loro numeri:

    1. Per ogni persona sintetica che teneva `staff.manage` **prima** di questa
       fase, il numero di righe leggibili da `door_scan_events` e'
       **identico** a `35-pre`. Se e' diminuito anche di una riga, questo piano
       ha creato un modo nuovo di negare qualcosa a chi lo aveva, e va fermato.
    2. La persona *staff assegnato alla notte 1* legge le righe della notte 1 e
       **zero** righe della notte 2.
    3. La persona *staff non assegnato* legge **zero** righe.
    4. La persona *staff assegnato come `party.manage` alla notte 1* legge le
       righe della notte 1 e **zero** righe della notte 2. Se il seed del piano
       35-06 assegna il terzo asse **solo** con `door.operate`, il terzo braccio
       della policy non e' esercitato da nessuna cella: **estendere il seed in
       questo task** con una quarta persona che tiene `party.manage` sulla notte
       1 e nessuna assegnazione di porta. Senza, il braccio nuovo e' una riga che
       nessuna situazione raggiungibile attraversa — cioe' la definizione
       esatta di un gate che non puo' fallire (`ai-engineering.md`).

    Se una di queste quattro non e' osservabile perche' il seed non produce righe
    di `door_scan_events` per le due serate sintetiche, **il seed va esteso in
    questo task** con almeno una riga per notte — altrimenti la prova e' vacua e
    dirlo verde sarebbe peggio che non averla.
  </action>
  <acceptance_criteria>
    - Il SUMMARY riporta i quattro conteggi come numeri, non come giudizi
    - `npm run baseline:compare` fra `35-pre` e `35-09` mostra la sola differenza attesa sulla definizione di `door_scan_events_select_admin`, piu' le aggiunte delle due tabelle nuove
    - Il conteggio di righe leggibili per ogni persona con `staff.manage` e' invariato rispetto a `35-pre`
    - `scripts/container/seed.mjs` contiene almeno un'assegnazione con `capability` `party.manage`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "party.manage" scripts/container/seed.mjs && npm run baseline:container -- --phase-point=35-09 && npm run baseline:compare</automated>
  </verify>
  <done>Si sa, per numeri, che nessuno legge meno di prima, che chi ha solo un'assegnazione legge una notte sola, e che il braccio `party.manage` e' attraversato da una persona sintetica invece che da nessuno.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessione autenticata → `public.door_scan_events` | La policy di SELECT e' l'unico gate sul registro della porta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-43 | Information Disclosure | il registro di ogni notte leggibile da chi ha lavorato una notte sola | mitigate | Bracci 2 e 3 agganciati a `party_id` della riga in valutazione |
| T-35-44 | Denial of Service | un organizer che non riesce piu' a rivedere una serata | mitigate | Primo braccio su `staff.manage`, e la prova per numeri che il conteggio non e' diminuito per nessuno |
| T-35-45 | Denial of Service | valutazione del predicato una volta per riga | mitigate | Tutte e tre le chiamate nella forma `(SELECT …)`, che Postgres valuta come InitPlan |
| T-35-46 | Repudiation | un commento che indica un lavoro gia' fatto | mitigate | Il paragrafo vecchio e' **sostituito**, e il criterio nuovo e' scritto al suo posto |
| T-35-47 | Repudiation | una lista vuota che dice *«nessuna anomalia»* a chi non ha il permesso di vederle | mitigate | Terzo braccio su `party.manage`, esercitato da una persona sintetica dedicata nel seed |
</threat_model>

<verification>
- `npm run baseline:container` con la migration applicata
- `npm run baseline:compare` contro `35-pre`, con le differenze attese e nessun'altra
- I quattro conteggi di lettura registrati nel SUMMARY
</verification>

<success_criteria>
- Il registro della porta e' ristretto per assegnazione e non per ruolo
- Nessuno legge meno di quanto leggeva prima
- Il braccio `party.manage` e' attraversato da una situazione raggiungibile, non solo scritto
- Il repository contiene un solo paragrafo su questa policy, ed e' quello giusto
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-09-SUMMARY.md` when done
</output>
