---
phase: 35-per-night-assignments
plan: 11
type: execute
wave: 5
depends_on: ["35-07"]
files_modified:
  - src/app/api/tickets/checkin/undo/route.ts
autonomous: true
requirements: [ASSIGN-05]

must_haves:
  truths:
    - "Annullare un check-in e' rifiutato a chi e' assegnato solo alla porta, e permesso a un organizer"
    - "Il rifiuto arriva come valore deciso per posizione, distinguibile da un rifiuto d'autenticazione e da una risoluzione fallita"
    - "Cosa questo piano NON chiude e' scritto, non lasciato scoprire"
  artifacts:
    - path: "src/app/api/tickets/checkin/undo/route.ts"
      provides: "il gate di supervisione dopo la risoluzione unica, con il suo status scelto leggendo la tabella del drain"
      contains: "DOOR_SUPERVISION_REQUIRED"
  key_links:
    - from: "src/app/api/tickets/checkin/undo/route.ts"
      to: "requireDoorOperator({ partyId })"
      via: "una sola risoluzione, il cui maySupervise decide il ramo"
      pattern: "maySupervise"
---

<objective>
ASSIGN-05, lato server. Il file **porta gia' il segnaposto** a `:19-20`:
*«**chi puo'** annullare e' la domanda della fase 35; **chi ha annullato**
appartiene a questo file, perche' la lista di revisione della serata e'
illeggibile senza»*. Questo piano risponde alla prima meta'.

Purpose: la decisione e' bloccata dal proprietario e sta nel ROADMAP —
*«annullare un check-in richiede una capability di supervisione; chi e'
assegnato alla porta per una serata non puo' annullare, salvo che sia anche
organizer»*. Il gate va nel punto in cui il contesto e' **gia' risolto**, e lo
status code va scelto **leggendo la tabella di classificazione del drain**, non
per abitudine: e' la stessa disciplina che ha prodotto il `503` di
`require-operator.ts:64-79`.

Output: il gate, con il suo status motivato, e la dichiarazione scritta della
meta' che questo piano **non** chiude.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-PATTERNS.md
@.claude/rules/checkin-offline.md
@.claude/rules/meta-gates.md
</context>

<interfaces>
Dal piano 35-07: `requireDoorOperator({ partyId })` restituisce, nel ramo
`ok: true`, `maySupervise: boolean`, e le costanti esportate
`DOOR_SUPERVISION_REQUIRED` (valore machine-readable) e
`DOOR_SUPERVISION_REQUIRED_ERROR` (la frase umana, in inglese).

La tabella di classificazione del drain, da leggere **prima** di scegliere uno
status (`src/lib/offline/sync-manager.ts:102-175`): `401` o `403` →
`blocked` · `408` o `429` → `retry` · `>= 500` → `retry` · body
`outcome: "recorded"` o `"already_recorded"` → `done` · `"not_valid"` → `dead` ·
qualunque altra cosa → `dead`.

La struttura odierna del file: `requireDoorOperator()` a `:47`; ramo **ticket**
`:96-257`, che scrive **prima** la riga `door_scan_events` con `is_undo: true`
e solo dopo aggiorna il ticket (`:206-229`); ramo **guest list** `:267-307`, che
**non scrive nessuna riga** di registro (differimento dichiarato a `:259-266`);
ramo **membership** `:310-337`, che **cancella** la riga `attendances` e non
scrive nessun registro.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Il gate di supervisione, subito dopo la risoluzione</name>
  <files>src/app/api/tickets/checkin/undo/route.ts</files>
  <read_first>
    - `src/app/api/tickets/checkin/undo/route.ts:1-100` — il docblock con il segnaposto a `:18-20`, `UUID_PATTERN` a `:23-24`, l'envelope di rifiuto a `:44-61` e la risoluzione del `partyId` per un ticket a livello evento a `:138-148`
    - `src/app/api/tickets/checkin/undo/route.ts:200-340` — i tre rami, per sapere dove ognuno risolve il proprio `partyId` e quale dei tre scrive un record
    - `src/lib/door/require-operator.ts:102-149` — il valore machine-readable deciso per posizione, la frase umana accanto, e il vincolo di risoluzione unica
    - `src/lib/offline/sync-manager.ts:102-175` — la tabella di classificazione, **da leggere prima di scegliere lo status**
    - `src/lib/capabilities/guards.ts:73-79` — S3: la categoria non viaggia nel messaggio
  </read_first>
  <action>
    Passare il `partyId` gia' risolto alla **stessa e unica** chiamata di
    `requireDoorOperator` che il file fa a `:47`, cambiandola in
    `requireDoorOperator({ partyId })`. Se il `partyId` non e' ancora noto in
    quel punto per uno dei tre rami, **risolverlo prima** e mantenere **una
    sola** risoluzione d'autorizzazione: un secondo `await requireDoorOperator(`
    nello stesso corpo e' il difetto, e nessun compilatore lo vede.

    Subito dopo il controllo `if (!auth.ok)`, aggiungere il gate:
    se `!auth.maySupervise`, rispondere con `success: false`,
    `error: DOOR_SUPERVISION_REQUIRED_ERROR` e
    `status: DOOR_SUPERVISION_REQUIRED` **nel corpo** — la categoria e' un
    **valore deciso per posizione**, mai una frase interpretata, perche' Next
    redige il messaggio di un errore lanciato in un build di produzione.

    **Lo status HTTP: `403`, e la scelta va motivata nel file, non ereditata.**
    Il ragionamento e' quello che va scritto: un `403` finisce nel bucket
    `blocked` del drain, che *«aspetta un nuovo login»* — e per uno **scan** in
    coda quello sarebbe un difetto, perche' un nuovo login non restituisce
    un'assegnazione revocata. Ma **un undo non passa mai dal drain**: la coda non
    contiene undo. Quindi `403` e' corretto **qui** e sarebbe sbagliato per uno
    scan rifiutato per assegnazione, che e' materia del piano 35-12. I due casi
    vanno separati **esplicitamente** nel commento, altrimenti qualcuno copiera'
    lo status dal vicino sbagliato — che e' esattamente il modo in cui T-1 si
    materializza.

    Il gate sta **prima** di qualunque scrittura, in tutti e tre i rami: un undo
    rifiutato non deve aver gia' toccato niente.
  </action>
  <acceptance_criteria>
    - `npm run build` passa
    - `grep -c "await requireDoorOperator(" src/app/api/tickets/checkin/undo/route.ts` e' esattamente **1**
    - Il file contiene `DOOR_SUPERVISION_REQUIRED` **nel corpo della risposta**, non dentro il testo di `error`
    - Il file contiene il commento che separa il caso dell'undo dal caso dello scan, nominando il bucket `blocked` del drain
    - Il gate precede ogni `getServiceClient()` del file: `grep -n` mostra la riga del gate prima della riga `:93`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && F=src/app/api/tickets/checkin/undo/route.ts; test "$(grep -c 'await requireDoorOperator(' $F)" = "1" && grep -q "DOOR_SUPERVISION_REQUIRED" $F && G=$(grep -n "maySupervise" $F | head -1 | cut -d: -f1); S=$(grep -n "getServiceClient()" $F | head -1 | cut -d: -f1); test "$G" -lt "$S" && echo UNDO_GATE_OK</automated>
  </verify>
  <done>Chi ha solo la porta riceve un rifiuto distinguibile prima che qualunque cosa sia stata scritta; un organizer passa.</done>
</task>

<task type="auto">
  <name>Task 2: Dichiarare per iscritto la meta' che questo piano non chiude</name>
  <files>src/app/api/tickets/checkin/undo/route.ts</files>
  <read_first>
    - `src/app/api/tickets/checkin/undo/route.ts:259-266` — il differimento gia' dichiarato del ramo guest list
    - `src/app/api/tickets/checkin/undo/route.ts:206-229` — il ramo ticket, che scrive **prima** il record e poi muta, con la frase *«un undo senza record e' il modo piu' semplice di far rientrare qualcuno non osservato»*
    - `src/app/api/tickets/checkin/undo/route.ts:310-337` — il ramo membership, che **cancella** la riga `attendances` senza scrivere nessun registro
    - `.claude/rules/checkin-offline.md` — gate *annullamento limitato* e la regola C10: ogni undo va registrato con chi e quando
  </read_first>
  <action>
    ASSIGN-05 chiude **chi puo'**. C10 chiede anche **che sia registrato**, e
    **due rami su tre non scrivono niente**: guest list (differito e dichiarato
    a `:259-266`) e membership (cancella la riga e non registra).

    Questo piano **non** chiude quella meta', e la scelta va **scritta invece
    che lasciata scoprire**: lasciarla aperta in silenzio la rende invisibile
    per sempre, che e' peggio di entrambe le alternative.

    Aggiornare il docblock del file: sostituire il segnaposto di `:18-20` — che
    ora indica un lavoro **fatto** — con un paragrafo che dice tre cose:

    1. **Chi puo' annullare e' ora risposto**, e come: `door.supervise`, per
       ruolo oppure per assegnazione, risolto in una sola volta.
    2. **Chi ha annullato e' registrato solo sul ramo ticket.** I rami guest
       list e membership **non scrivono ancora** una riga di registro. La
       conseguenza, detta senza attenuazioni: su quei due rami un annullamento
       resta invisibile nella lista di revisione della serata, e questo prodotto
       **non ha error tracking** — quindi non lo scoprira' nessuno da solo.
    3. **Perche' non e' chiuso qui**: e' un lavoro di registrazione, non di
       autorizzazione, e ha un perimetro suo — il ramo membership dovrebbe
       smettere di **cancellare** una riga di presenza, che e' una modifica al
       significato dei dati di presenza e non un ritocco. Nominare il lavoro
       invece di farlo a meta'.

    Registrare la stessa dichiarazione, in una riga, nel SUMMARY del piano,
    cosi' che arrivi nel `35-VERIFICATION.md` come **debito differito e
    nominato**, non come una cosa che si scopre leggendo il codice.
  </action>
  <acceptance_criteria>
    - Il docblock non contiene piu' una frase che dice che «chi puo' annullare» e' una domanda **futura**
    - Il docblock nomina esplicitamente i due rami che non registrano e la conseguenza osservabile
    - Il SUMMARY del piano contiene la stessa dichiarazione in una riga, marcata come debito differito
    - `npm run build` passa
  </acceptance_criteria>
  <verify>
    <automated>npm run build && F=src/app/api/tickets/checkin/undo/route.ts; grep -qi "guest list" $F && grep -q "door.supervise" $F && echo DECLARED_OK</automated>
  </verify>
  <done>Il file dice cosa e' chiuso e cosa non lo e', e il debito ha un nome invece di essere una scoperta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispositivo della porta → route di undo | La route scrive con il **service client**: la RLS non la vede. Il confine e' `requireDoorOperator` |
| rifiuto → client | La categoria non puo' viaggiare nel messaggio: Next lo redige in produzione |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-52 | Elevation of Privilege | chi ha solo la porta che annulla un check-in | mitigate | Gate su `maySupervise`, prima di qualunque scrittura, in tutti e tre i rami |
| T-35-53 | Repudiation | undo non registrato sui rami guest list e membership | accept | **Debito differito e nominato** nel docblock e nel SUMMARY: e' lavoro di registrazione, con un perimetro proprio, e non si chiude a meta' |
| T-35-54 | Repudiation | categoria di rifiuto persa in produzione | mitigate | `DOOR_SUPERVISION_REQUIRED` come valore nel corpo, deciso per posizione |
| T-35-55 | Denial of Service | status scelto per abitudine che manda la coda in `blocked` | mitigate | `403` scelto **leggendo** la tabella del drain, con il commento che separa il caso undo dal caso scan |
| T-35-56 | Denial of Service | doppia risoluzione nello stesso handler | mitigate | Conteggio asserito: esattamente una chiamata a `requireDoorOperator` |
</threat_model>

<verification>
- `npm run build` passa
- Il conteggio delle risoluzioni nel file e' 1
- Il gate precede ogni uso del service client nel file
- Le due prove che richiedono una persona (la frase distinguibile in build di produzione, e il ramo offline) sono **scritte** in `35-HUMAN-UAT.md` dal piano 35-14
</verification>

<success_criteria>
- Un assegnato alla sola porta non puo' annullare, e lo scopre da una frase che dice cosa e' successo
- Un organizer puo'
- Cio' che resta aperto ha un nome
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-11-SUMMARY.md` when done
</output>
