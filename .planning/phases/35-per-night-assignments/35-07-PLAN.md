---
phase: 35-per-night-assignments
plan: 07
type: execute
wave: 4
depends_on: ["35-03"]
files_modified:
  - src/lib/capabilities/server.ts
  - src/lib/door/require-operator.ts
autonomous: true
requirements: [ASSIGN-05, ASSIGN-08]

must_haves:
  truths:
    - "Il server puo' chiedere «puo' questa persona fare X in QUESTA notte» in un solo posto"
    - "L'autorizzazione della porta risponde in una sola risoluzione per handler, e porta con se' il verdetto di supervisione"
    - "Il fallimento della risoluzione resta un quarto esito distinto, mai un rifiuto"
  artifacts:
    - path: "src/lib/capabilities/server.ts"
      provides: "hasCapability(key, { partyId }) — source-compatible con ogni call site esistente"
      contains: "partyId"
    - path: "src/lib/door/require-operator.ts"
      provides: "requireDoorOperator({ partyId }) con maySupervise e validUntil, DOOR_SUPERVISION_REQUIRED"
      contains: "DOOR_SUPERVISION_REQUIRED"
  key_links:
    - from: "src/lib/capabilities/server.ts"
      to: "public.my_access_context(uuid)"
      via: "rpc con il client legato ai cookie, mai il service client"
      pattern: "my_access_context"
    - from: "src/lib/door/require-operator.ts"
      to: "src/lib/capabilities/server.ts"
      via: "una sola risoluzione per handler, restituita al chiamante"
      pattern: "getAccessContext"
---

<objective>
Il posto dove la domanda per-notte diventa chiamabile. `guards.ts:88-92` ha
gia' **riservato la forma**: *«`hasCapability(key, { partyId })` e'
source-compatible con ogni call site che questa fase scrive»*. Questo piano la
implementa e la fa arrivare fino alla porta.

Purpose: le tre route della porta scrivono con il **client service**, che
bypassa ogni RLS. Li' la RLS non e' il confine e non puo' esserlo: il confine e'
`requireDoorOperator()`. Percio' l'assegnazione deve arrivare **anche li'**, e
deve arrivare in **una sola risoluzione** — `cache()` non memoizza in una Route
Handler ne' in una Server Action, misurato, e un secondo `await` e' un secondo
round trip completo davanti a una fila. **Nessun compilatore lo vede.**

Output: l'unica funzione che risolve la domanda per-notte, e l'unica funzione
che autorizza la porta, che ora restituisce anche il verdetto di supervisione.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-PATTERNS.md
@.claude/rules/access-gating.md
@.claude/rules/checkin-offline.md
</context>

<interfaces>
L'unione taggata da estendere (`src/lib/door/require-operator.ts:130-134`):

```
export type DoorAuth =
  | { ok: true; userId: string }
  | { ok: false; kind: "unauthenticated"; error: string; status: 401 }
  | { ok: false; kind: "forbidden";       error: string; status: 403 }
  | { ok: false; kind: "unresolved";      error: string; status: 503 };
```

Il valore machine-readable deciso **per posizione** (`:102-116`):
`export const DOOR_UNRESOLVED_STATUS = "capability_unresolved";` con accanto
`DOOR_UNRESOLVED_ERROR`, la frase umana che *«deliberatamente non dice niente
sui permessi: il senso di questo braccio e' che il permesso e' ignoto»*.

Chi chiama `requireDoorOperator()` oggi, verificato: `checkin/route.ts:151`,
`checkin/undo/route.ts:47`, `tickets/attendance/route.ts:203` e `:527`,
`membership/list/route.ts:68`, `membership/verify/route.ts:139`.

Il DAL (`src/lib/capabilities/server.ts:198-286`): `getAccessContext()`
memoizzata con `cache()`, `hasCapability(key)`. E il limite misurato a
`:103-121`: `cache()` **non** memoizza in una Server Action ne' in una Route
Handler — tre chiamate, tre esecuzioni, identico in `next dev` e in un build di
produzione.

La firma nuova disponibile dal piano 35-03: `public.my_access_context(p_party_id uuid)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `hasCapability(key, { partyId })` nel DAL</name>
  <files>src/lib/capabilities/server.ts</files>
  <read_first>
    - `src/lib/capabilities/server.ts:1-60` — il modulo per intero fino alla dichiarazione server-only, e in particolare `:21-30`: il controllo si fa con il **client legato ai cookie**, mai con il service client, perche' un token service-role non porta `sub`, quindi `auth.uid()` e' `null` e la funzione risponde *«nessuna capability»* con sicurezza **riguardo a nessuno**
    - `src/lib/capabilities/server.ts:103-121` — il limite misurato di `cache()`, e perche' e' un vincolo che nessun compilatore vede
    - `src/lib/capabilities/server.ts:198-286` — `getAccessContext()` e `hasCapability(key)`: la forma esistente, e la regola che **ogni fallimento lancia**, mai un valore degradato
    - `src/lib/capabilities/guards.ts:88-92` — la frase che riserva questa forma, e la ragione per cui una convenzione che passa un `Set` non puo' far crescere l'argomento
    - `src/lib/capabilities/guards.ts:73-79` — S3: una categoria che deve attraversare verso un client **non puo' viaggiare nel messaggio**, perche' Next redige il messaggio di un errore lanciato da una Server Action in un build di produzione. Deve essere un **valore deciso per posizione**
  </read_first>
  <action>
    Aggiungere al DAL la forma per-notte, **senza toccare** la firma esistente:
    `hasCapability(key)` resta l'API primaria e ogni call site attuale continua a
    compilare senza modifiche.

    - `getAccessContext()` resta com'e', a zero argomenti, memoizzata, e continua
      a chiamare `public.my_access_context()`.
    - Aggiungere `getPartyAccessContext(partyId: string)`, che chiama
      `public.my_access_context(p_party_id)` con il **client legato ai cookie** e
      restituisce lo stesso tipo di contesto, con le capability risolte **su
      quella notte**. Non memoizzata con `cache()` — o memoizzata **per
      `partyId`**, mai globalmente: due notti diverse sono due domande diverse, e
      una memoizzazione che le confonde e' un difetto d'accesso.
    - Estendere `hasCapability` alla forma `hasCapability(key, opts?: { partyId?: string })`:
      senza `partyId` il comportamento e' **byte-identico** a oggi; con
      `partyId` risolve dal contesto per-notte.

    Validare `partyId` come uuid **prima** di passarlo alla RPC, riusando il
    pattern gia' esistente nel repository (`UUID_PATTERN` in
    `src/app/api/tickets/checkin/undo/route.ts:23-24`). Un valore non-uuid non
    e' un errore da inoltrare al database: e' un input rifiutato all'ingresso.

    **Ogni fallimento lancia**, come nel resto del modulo. Nessun
    `catch { return false }`: rifiuterebbe un master esattamente come rifiuta un
    `pending`, e in un prodotto senza error tracking nessuno lo scoprirebbe.

    Aggiungere nel docblock del modulo il paragrafo che il prossimo lettore
    chiedera': perche' esistono **due** funzioni di contesto invece di una con
    argomento opzionale. La ragione e' la memoizzazione: `getAccessContext()`
    e' memoizzata perche' la risposta e' la stessa per tutta la richiesta; la
    versione per-notte **non lo e' nella stessa forma**, perche' la sua risposta
    dipende da un argomento.
  </action>
  <acceptance_criteria>
    - `npm run build` passa **senza modifiche a nessun call site esistente** di `hasCapability(` — verificabile con `git diff --stat`, che non deve elencare nessun file diverso da `src/lib/capabilities/server.ts`
    - `grep -n "my_access_context" src/lib/capabilities/server.ts` mostra **entrambe** le chiamate, quella a zero argomenti e quella con `p_party_id`
    - Il file non contiene nessun `catch` che restituisca `false`
    - Il file non usa mai il service client: `grep -c "getServiceClient" src/lib/capabilities/server.ts` restituisce 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test "$(grep -c 'getServiceClient' src/lib/capabilities/server.ts)" = "0" && test "$(git diff --name-only -- src/ | wc -l | tr -d ' ')" = "1" && echo DAL_OK</automated>
  </verify>
  <done>La domanda per-notte e' chiamabile da un solo posto, e nessun call site esistente e' cambiato.</done>
</task>

<task type="auto">
  <name>Task 2: `requireDoorOperator({ partyId })` restituisce di piu'</name>
  <files>src/lib/door/require-operator.ts</files>
  <read_first>
    - `src/lib/door/require-operator.ts` — per intero. In particolare: `:30-37` (non c'e' nessun test di stato in questo file, e **aggiungerne uno sarebbe un difetto**), `:44-62` (l'unico `catch` legittimo, entrato **per posizione**, che non legge mai `error.message` e restituisce un **quarto esito**), `:64-79` (perche' il 503 esiste), `:81-99` (l'effetto osservabile, e il limite gia' misurato della superficie dello scanner, che mappa lo status a un titolo **prima** di leggere il body), `:102-116` (il valore machine-readable e la frase umana), `:130-134` (l'unione taggata), `:136-149` (una sola risoluzione per handler, e perche' un round trip risparmiato qui e' un round trip risparmiato su una rete debole), `:151-188` (il corpo)
    - `src/lib/capabilities/keys.ts:38-45` — la regola di naming, per cui `door.supervise` e' una chiave diversa da `door.operate` e non un flag su di essa
    - `.claude/rules/checkin-offline.md` — gate *annullamento limitato* e gate *il fallimento va visto*
  </read_first>
  <action>
    Estendere `requireDoorOperator()` a `requireDoorOperator(opts?: { partyId?: string })`.
    Senza argomento il comportamento resta **identico**, e i sei call site
    esistenti continuano a compilare e a comportarsi come oggi.

    Con `partyId`, la funzione fa **una sola** risoluzione — quella che gia' fa —
    e la usa per rispondere a **tre** domande invece di una:

    1. `mayScan` — `door.operate`, per ruolo **oppure** per assegnazione a quella
       notte. E' il verdetto che decide `ok`.
    2. `maySupervise` — `door.supervise`, per ruolo **oppure** per assegnazione.
       Questo e' ASSIGN-05: chi e' assegnato **solo** alla porta non ce l'ha, e
       un organizer si'.
    3. `validUntil` — l'istante in cui la notte finisce, **come cortesia
       dell'interfaccia**, mai come confine. `null` quando la notte non dichiara
       un orario di fine. Va accompagnato da `resolvedAt`, l'ora del **server**
       alla risoluzione, cosi' che il dispositivo possa **misurare la deriva del
       proprio orologio invece di fidarsene**.

    Allargare il ramo `ok: true` dell'unione taggata a
    `{ ok: true; userId: string; mayScan: true; maySupervise: boolean; validUntil: string | null; resolvedAt: string }`.
    I tre rami di rifiuto restano **invariati**: 401, 403, 503 con
    `DOOR_UNRESOLVED_STATUS`.

    Aggiungere accanto a `DOOR_UNRESOLVED_STATUS`, **nella stessa forma**, due
    costanti esportate:
    - `DOOR_SUPERVISION_REQUIRED = "door_supervision_required"` — il valore
      machine-readable deciso **per posizione**, mai una frase interpretata
    - `DOOR_SUPERVISION_REQUIRED_ERROR` — la frase umana:
      «Undoing a check-in needs a supervisor. Ask an organizer for this night.»
      L'interfaccia resta **solo in inglese**: e' una decisione di milestone.

    **Il vincolo che nessun compilatore vede**, e che va ripetuto nel docblock
    invece che assunto: `requireDoorOperator` va chiamata **una volta per
    handler**, e per portare `maySupervise` deve **restituire di piu'** —
    esattamente come `assertStaffManage()` restituisce il contesto. Un secondo
    `await requireDoorOperator(` nello stesso corpo e' il difetto.

    **Il fallimento della risoluzione del verdetto di supervisione prende lo
    stesso trattamento dell'esito `unresolved`**, e non viene mai collassato in
    `maySupervise: false`. Un falso `false` qui e' un rifiuto travestito da
    risposta. Se la risoluzione per-notte non arriva, la funzione restituisce il
    ramo `unresolved` — *«non e' un rifiuto»* — e il chiamante decide.

    Aggiungere un paragrafo che **rinnova** la nota gia' presente a `:90-99`: lo
    scanner mappa lo status HTTP a un titolo **prima** di leggere il body, e il
    503 ha gia' il suo. La stessa cosa vale ora per il rifiuto di supervisione,
    e il piano 35-13 e' quello che chiude quella meta' sul dispositivo.
  </action>
  <acceptance_criteria>
    - `npm run build` passa **senza modifiche a nessuno dei sei call site esistenti**
    - `grep -c "DOOR_SUPERVISION_REQUIRED" src/lib/door/require-operator.ts` >= 2 (valore + frase)
    - Il file **non** contiene nessun test su `status` di un profilo: `grep -niE "status *[=!]== *['\"](approved|pending|rejected)" src/lib/door/require-operator.ts` non trova nulla
    - Il file contiene **un solo** blocco `catch`, che restituisce il ramo `unresolved`
    - Il ramo `ok: true` dell'unione taggata contiene `maySupervise`, `validUntil` e `resolvedAt`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test "$(grep -c 'DOOR_SUPERVISION_REQUIRED' src/lib/door/require-operator.ts)" -ge 2 && ! grep -niE "status *[=!]== *['\"](approved|pending|rejected)" src/lib/door/require-operator.ts && test "$(grep -c 'catch' src/lib/door/require-operator.ts)" -le 2 && echo DOOR_GUARD_OK</automated>
  </verify>
  <done>Una sola risoluzione per handler risponde a tre domande, e il fallimento della risoluzione resta il quarto esito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route Handler della porta → database | Le route scrivono con il **service client**: la RLS non le vede. Il confine e' `requireDoorOperator()` |
| DAL → sessione | Il contesto si risolve **solo** con il client legato ai cookie: con il service client `auth.uid()` e' `null` e la risposta e' un «nessuno» sicuro di se' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-31 | Elevation of Privilege | risoluzione fatta con il service client | mitigate | Il modulo non importa `getServiceClient`, ed e' asserito con un conteggio |
| T-35-32 | Spoofing | `partyId` arbitrario inoltrato al database | mitigate | Validazione uuid all'ingresso, con il `UUID_PATTERN` gia' nel repository |
| T-35-33 | Denial of Service | doppia risoluzione per handler su una rete debole | mitigate | `requireDoorOperator` **restituisce** il verdetto invece di far ri-chiedere; il vincolo e' scritto nel docblock perche' nessun compilatore lo vede |
| T-35-34 | Repudiation | risoluzione fallita collassata in un rifiuto | mitigate | Il ramo `unresolved` a 503 con `DOOR_UNRESOLVED_STATUS`; nessun `catch { return false }` |
| T-35-35 | Elevation of Privilege | un operatore che diventa supervisore riusando la stessa chiave | mitigate | `door.supervise` e' una chiave distinta, coniata dalla domanda; il verdetto e' un campo separato dell'unione |
</threat_model>

<verification>
- `npm run build` passa e `git diff --name-only -- src/` elenca **solo** i due file di questo piano
- `npm run verify:no-header-identity` resta verde
- `npm run verify:capabilities -- --target=container` resta verde
</verification>

<success_criteria>
- Esiste un solo posto in cui si chiede «puo' X in questa notte»
- La porta risponde a tre domande con una sola risoluzione
- Un fallimento di risoluzione non e' mai un rifiuto
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-07-SUMMARY.md` when done
</output>
