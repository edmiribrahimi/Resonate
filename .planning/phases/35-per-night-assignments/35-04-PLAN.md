---
phase: 35-per-night-assignments
plan: 04
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - supabase/migrations/20260809002000_assignment_acts.sql
  - src/lib/membership/acts.ts
autonomous: true
requirements: [ASSIGN-03, ASSIGN-04]

must_haves:
  truths:
    - "Assegnare e revocare sono due atti registrati, con chi li ha fatti e quando"
    - "Mutazione e registrazione avvengono in una sola transazione: non esiste lo stato in cui una e' riuscita e l'altra no"
    - "La revoca aggiorna una riga; nessun percorso di questo prodotto cancella un'assegnazione"
  artifacts:
    - path: "supabase/migrations/20260809002000_assignment_acts.sql"
      provides: "CHECK act allargato a 9 valori, public.record_party_assignment_act, D-18 riscritta"
      contains: "record_party_assignment_act"
    - path: "src/lib/membership/acts.ts"
      provides: "la union MembershipAct con 'assigned' e 'unassigned'"
      contains: "unassigned"
  key_links:
    - from: "public.record_party_assignment_act"
      to: "public.membership_acts"
      via: "delega a public.record_membership_act nella stessa transazione"
      pattern: "record_membership_act"
    - from: "public.record_party_assignment_act"
      to: "public.party_end_instant"
      via: "calcolo di ends_at alla concessione, dalla notte e mai dal client"
      pattern: "party_end_instant"
---

<objective>
Il registro. Assegnare e revocare **scrivono nel registro che esiste gia'**,
`public.membership_acts`, usando la colonna `party_id` che la fase 43 ha
aggiunto *«ON PURPOSE»* per questo, e i due valori d'atto — `'assigned'` e
`'unassigned'` — che `src/lib/membership/acts.ts:47-48` **nomina gia'**.

Purpose: `ACCESS-MODEL-DECISIONS.md` § 5 pretende attribuzione su tutta la
linea, e l'assegnazione e' nominata in quell'elenco. E due chiamate PostgREST
non possono essere atomiche: la mutazione riuscirebbe mentre il record fallisce,
**in silenzio**, in un prodotto senza error tracking.

Questo piano risolve anche una contraddizione lasciata aperta dentro una
migration gia' committata (D-18): la **riscrive**, non la aggira.

Output: la migration 9 della coda, e il writer atomico che ogni superficie di
questa fase usera'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-PATTERNS.md
@.planning/ACCESS-MODEL-DECISIONS.md
@.claude/rules/meta-gates.md
</context>

<interfaces>
La firma esatta del writer esistente, da riusare invece di re-implementare
(`supabase/migrations/20260808002000_membership_register.sql:388-493`):

`public.record_membership_act(p_subject_id uuid, p_act text, p_actor_id uuid,
p_actor_kind text, p_role text, p_status text, p_note text, p_party_id uuid)
RETURNS uuid`, `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`,
`EXECUTE` revocato a `public`/`anon`/`authenticated` e concesso al solo
`service_role`.

Semantica delle colonne che un atto d'assegnazione lascia a `NULL`
(`membership_register.sql` e `src/types/database.ts:422-444`): `role_before`,
`role_after`, `status_before`, `status_after` — `NULL` significa *«questo atto
non ha toccato quell'asse»*, e un'assegnazione non tocca ne' il ruolo ne' lo
stato. `subject_label` e' **un `membership_code`, mai un'email e mai un nome
completo**, e la regola e' imposta dal writer, non dal chiamante.

Le colonne di `public.party_assignments` scritte da questo writer, dal piano
35-02: `party_id`, `user_id`, `capability`, `assignee_role`, `assigned_by`,
`ends_at`, `revoked_at`, `revoked_by`.

Il CHECK da allargare (`membership_register.sql:174-183`): sette valori oggi —
`created`, `approved`, `rejected`, `promoted`, `demoted`, `deactivated`,
`reactivated`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: I due valori d'atto, nel CHECK e nella union, nello stesso commit</name>
  <files>supabase/migrations/20260809002000_assignment_acts.sql, src/lib/membership/acts.ts</files>
  <read_first>
    - `src/lib/membership/acts.ts:10-33` — la regola scritta: le due union sono specchiate da `CHECK` SQL, e **modificare un lato significa modificare entrambi, nello stesso commit**. E la ragione per cui la regola e' dichiarata invece che assunta: una divergenza **non fallisce rumorosamente**, produce un valore che TypeScript accetta ovunque e che il database rifiuta con un `23514` **nel momento in cui qualcuno compie l'atto**, non a build time
    - `src/lib/membership/acts.ts:47-50` — i due nomi gia' riservati per questa fase, e la frase che dice che un override alla porta **non** entra qui
    - `supabase/migrations/20260808002000_membership_register.sql:174-183` — il CHECK da allargare, nella sua forma attuale
    - `supabase/migrations/20260808001000_role_implies_approved.sql:103-119` — WR-04: `DROP CONSTRAINT IF EXISTS` prima di ogni `ADD CONSTRAINT`, perche' la coda si applica a mano
  </read_first>
  <action>
    Aprire `supabase/migrations/20260809002000_assignment_acts.sql` con
    l'intestazione nella forma consueta (modifiche numerate, come fallisce ogni
    meta', idempotenza voce per voce), poi `BEGIN;`.

    Allargare il CHECK con la forma idempotente:
    `ALTER TABLE public.membership_acts DROP CONSTRAINT IF EXISTS membership_acts_act_check;`
    seguito da `ADD CONSTRAINT membership_acts_act_check CHECK (act IN (…))` con
    i sette valori esistenti **piu'** `'assigned'` e `'unassigned'`.

    Nello **stesso commit**, aggiungere i due valori alla union `MembershipAct`
    in `src/lib/membership/acts.ts`, e sostituire la frase che li *riserva* con
    la frase che dice che sono *arrivati* — un commento che indica un lavoro
    ancora da fare, quando il lavoro e' fatto, e' peggio di nessun commento.

    Se `acts.ts` porta anche un'etichetta leggibile per atto o una mappa di
    descrizioni, aggiungere le voci corrispondenti: le due frasi umane sono
    «Assigned to a night» e «Assignment revoked», **senza mai nominare la
    persona** — il registro parla per `membership_code`.
  </action>
  <acceptance_criteria>
    - `npm run build` passa
    - `grep -c "'assigned'" supabase/migrations/20260809002000_assignment_acts.sql` >= 1 e lo stesso per `'unassigned'`
    - `grep -q "unassigned" src/lib/membership/acts.ts` vero
    - Il file `acts.ts` non contiene piu' una frase che dice che i due valori *arriveranno*
    - Il CHECK e' preceduto da un `DROP CONSTRAINT IF EXISTS` con lo stesso nome
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q "membership_acts_act_check" supabase/migrations/20260809002000_assignment_acts.sql && grep -q "DROP CONSTRAINT IF EXISTS membership_acts_act_check" supabase/migrations/20260809002000_assignment_acts.sql && grep -q "unassigned" src/lib/membership/acts.ts && echo ACTS_OK</automated>
  </verify>
  <done>Il database e il tipo conoscono gli stessi nove valori, nello stesso commit.</done>
</task>

<task type="auto">
  <name>Task 2: Riscrivere D-18 con il criterio corretto</name>
  <files>supabase/migrations/20260809002000_assignment_acts.sql</files>
  <read_first>
    - `supabase/migrations/20260808002000_membership_register.sql:259-272` — la colonna `party_id`, aggiunta *«ON PURPOSE»* perche' l'assegnazione per-serata scriva **in questo registro**
    - `supabase/migrations/20260808002000_membership_register.sql:299-312` — **D-18**: un override alla porta non entra nel registro perche' *«non cambia chi qualcuno E'. Ammette una persona, in una notte, e scade con quella notte»*. E la frase che vieta un secondo registro: *«due registri che tengono verita' sovrapposte sono peggio di entrambi, perche' la prima domanda di ogni lettore successivo diventa quale dei due ha ragione»*
    - `.planning/ACCESS-MODEL-DECISIONS.md` § 5 — l'elenco di cio' che deve registrare chi e quando, in cui **«per-night assignment» e' nominata**
  </read_first>
  <action>
    Scrivere nella migration una sezione numerata che **riscrive il paragrafo
    D-18**, e la riscrittura e' l'atto: `COMMENT ON COLUMN
    public.membership_acts.party_id` viene **sostituito**, non affiancato.

    Il criterio corretto, scritto per esteso: letteralmente, D-18 escluderebbe
    anche l'assegnazione, perche' anche l'assegnazione **scade con la notte**.
    Ma la scadenza non e' il criterio. Il criterio e' un altro, ed e' quello che
    D-18 stava cercando: **ammettere una persona non e' concedere un potere**.
    Un override alla porta ammette una persona a una serata e non cambia cosa
    quella persona puo' fare; un'assegnazione **concede un potere** — fare la
    porta, caricare foto, gestire una notte — ed e' quindi attribuibile per
    `ACCESS-MODEL-DECISIONS.md` § 5, che nomina l'assegnazione nell'elenco.

    Dire anche cosa **resta vero** di D-18 dopo la riscrittura: un override alla
    porta continua a **non** entrare qui e resta in `door_scan_events`. La
    riscrittura non allarga il registro: lo definisce.

    E dire perche' non si crea un secondo registro, citando la frase che lo
    vieta: due registri con verita' sovrapposte sono peggio di entrambi.

    Lasciare la contraddizione in piedi sarebbe la scelta peggiore di tutte:
    il primo lettore successivo si chiederebbe quale dei due paragrafi vale.
  </action>
  <acceptance_criteria>
    - Il file contiene `COMMENT ON COLUMN public.membership_acts.party_id`
    - Il commento nuovo contiene la distinzione fra *ammettere una persona* e *concedere un potere*
    - Il commento nuovo dice esplicitamente che un override alla porta resta fuori dal registro
    - `npm run baseline:container -- --phase-point=35-04` gira senza errori
  </acceptance_criteria>
  <verify>
    <automated>grep -q "COMMENT ON COLUMN public.membership_acts.party_id" supabase/migrations/20260809002000_assignment_acts.sql && npm run baseline:container -- --phase-point=35-04</automated>
  </verify>
  <done>Esiste un solo paragrafo che spiega cosa entra nel registro, e non contraddice se stesso.</done>
</task>

<task type="auto">
  <name>Task 3: `public.record_party_assignment_act` — una transazione, due effetti</name>
  <files>supabase/migrations/20260809002000_assignment_acts.sql</files>
  <read_first>
    - `supabase/migrations/20260808002000_membership_register.sql:388-493` — il writer da imitare per intero: `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`, il `SELECT … FOR UPDATE` che blocca e legge il soggetto **come e' adesso**, il `RAISE EXCEPTION` il cui messaggio nomina **solo l'identificatore** e mai un indirizzo o un nome (perche' un messaggio sollevato raggiunge un log, e su questo progetto un log raggiunge uno screenshot)
    - `supabase/migrations/20260808002000_membership_register.sql:469-488` — `REVOKE` **poi** `GRANT`, due statement in quell'ordine e mai assunti: Postgres concede `EXECUTE` a `PUBLIC` di default su ogni funzione nuova
    - `supabase/migrations/20260808001000_role_implies_approved.sql:194-201` — quando un rifiuto viene gestito nel codice applicativo si ramifica su `error.code`, mai su un messaggio interpretato, e **non si logga mai `error.details`**: su queste tabelle PostgREST ci mette l'**intera riga fallita**
    - `.planning/phases/35-per-night-assignments/35-PATTERNS.md:289-345` — § 1h, i tre estratti che il writer di questa fase deve replicare
  </read_first>
  <action>
    Creare **una sola** funzione `SECURITY DEFINER` che copre entrambi gli atti,
    perche' due funzioni con la stessa forma divergono:

    `public.record_party_assignment_act(p_party_id uuid, p_subject_id uuid,
    p_capability text, p_act text, p_actor_id uuid) RETURNS uuid`,
    `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`.

    `p_act` accetta solo `'assigned'` e `'unassigned'`; qualunque altro valore
    solleva con `ERRCODE = 'invalid_parameter_value'`.

    **Ramo `'assigned'`**, in quest'ordine dentro la stessa transazione:
    1. Leggere e bloccare il soggetto: `SELECT p.role, p.membership_code INTO …
       FROM public.profiles p WHERE p.id = p_subject_id FOR UPDATE`. Il `FOR
       UPDATE` non e' prudenza: il ruolo che si sta per copiare in
       `assignee_role` deve essere quello **che vale in questo statement**, non
       quello di un istante prima.
       Se non trovato, `RAISE EXCEPTION` con il **solo identificatore**.
    2. Calcolare `ends_at` **dalla notte, mai dal chiamante**:
       `public.party_end_instant(e.date, coalesce(ep.end_time, '06:00'::time))`
       leggendo `public.event_parties ep join public.events e on e.id =
       ep.event_id where ep.id = p_party_id`. Se la notte non esiste, sollevare
       nominando `p_party_id`.
       Il fallback `'06:00'` e' **la chiusura dichiarata di una notte in questo
       prodotto**, non un default inventato, e la sua direzione d'errore e'
       quella sicura: una finestra piu' larga ammette invece di rifiutare.
    3. `INSERT INTO public.party_assignments (party_id, user_id, capability,
       assignee_role, assigned_by, ends_at)` con `assignee_role` preso dalla
       lettura al punto 1. **Nessun `ON CONFLICT`**: l'indice unico parziale
       `party_assignments_live_unique` deve **rifiutare** una seconda
       assegnazione viva identica, e il chiamante ramifica su `23505`.
       Il `CHECK` `party_assignments_no_self_grant` e la FK composta si
       applicano qui, e i loro rifiuti (`23514`, `23503`) **non vengono
       catturati**: attraversano fino al chiamante, che decide per codice.
    4. Chiamare `public.record_membership_act(p_subject_id, 'assigned',
       p_actor_id, 'user', NULL, NULL, NULL, p_party_id)` — i quattro assi
       ruolo/stato a `NULL` perche' un'assegnazione non li tocca, e `p_note` a
       `NULL` perche' una nota qui non potrebbe che essere un nome.
    5. Restituire l'`id` della riga di assegnazione.

    **Ramo `'unassigned'`**, nella stessa transazione:
    1. `UPDATE public.party_assignments SET revoked_at = now(), revoked_by =
       p_actor_id, assignee_role = NULL WHERE party_id = p_party_id AND user_id
       = p_subject_id AND capability = p_capability AND revoked_at IS NULL`.
       **Mai un `DELETE`**, e il commento dice perche': il drain deve poter
       chiedere *«era viva al tempo `scannedAt`?»* **dopo** la revoca. Una riga
       cancellata non risponde a quella domanda, e la scansione resta appesa —
       che e' esattamente cio' che ASSIGN-03 vieta.
       L'azzeramento di `assignee_role` e' cio' che libera il ruolo della
       persona dalla foreign key composta: il commento lo dice, perche' senza
       sembra un campo dimenticato.
    2. Se `NOT FOUND`, sollevare con `ERRCODE = 'no_data_found'` nominando
       **solo** gli identificatori. Una revoca che non trova niente da revocare
       non e' un successo silenzioso: e' l'informazione che due persone stanno
       guardando due stati diversi.
    3. Chiamare `record_membership_act` con `'unassigned'`, stessa forma.

    Chiudere con, in quest'ordine e come due statement:
    `REVOKE ALL ON FUNCTION public.record_party_assignment_act(uuid, uuid, text, text, uuid) FROM public, anon, authenticated;`
    poi `GRANT EXECUTE … TO service_role;`
    e un `COMMENT ON FUNCTION` che dice perche' la funzione esiste e perche' e'
    una e non due.

    Chiudere la migration con `COMMIT;`.
  </action>
  <acceptance_criteria>
    - Il file contiene `REVOKE ALL ON FUNCTION public.record_party_assignment_act` **prima** di `GRANT EXECUTE ON FUNCTION public.record_party_assignment_act`
    - Il file contiene `SET search_path = ''` sulla funzione
    - Il file **non** contiene la stringa `DELETE FROM public.party_assignments`
    - Il file contiene `FOR UPDATE` e `party_end_instant`
    - Nessun `RAISE EXCEPTION` del file interpola un nome, un'email o un `membership_code`: `grep -n "RAISE EXCEPTION" -A 1` mostra solo interpolazioni di `p_party_id`, `p_subject_id` o `p_capability`
    - `npm run baseline:container -- --phase-point=35-04-final` gira senza errori
  </acceptance_criteria>
  <verify>
    <automated>F=supabase/migrations/20260809002000_assignment_acts.sql; grep -q "REVOKE ALL ON FUNCTION public.record_party_assignment_act" $F && grep -q "GRANT EXECUTE ON FUNCTION public.record_party_assignment_act" $F && ! grep -q "DELETE FROM public.party_assignments" $F && grep -q "FOR UPDATE" $F && npm run baseline:container -- --phase-point=35-04-final</automated>
  </verify>
  <done>Esiste un solo modo di scrivere un'assegnazione, ed e' atomico con la sua registrazione. Nessun percorso cancella una riga.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → `record_party_assignment_act` | Chiamata con il service client: la RLS non e' il confine, il confine e' `EXECUTE` al solo `service_role` piu' i vincoli di riga |
| `record_party_assignment_act` → `membership_acts` | Il registro e' append-only per costruzione; l'atto sopravvive al soggetto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-16 | Elevation of Privilege | `record_party_assignment_act` raggiungibile via REST | mitigate | `REVOKE` da `public`/`anon`/`authenticated`, `GRANT` al solo `service_role`, nell'ordine e come due statement |
| T-35-17 | Repudiation | mutazione riuscita e registrazione fallita, in silenzio | mitigate | Una sola funzione `plpgsql`: i due effetti sono nella stessa transazione, non due chiamate PostgREST |
| T-35-18 | Repudiation | revoca che cancella la riga | mitigate | Il ramo `'unassigned'` e' un `UPDATE`; il file non contiene nessun `DELETE FROM public.party_assignments`, ed e' asserito |
| T-35-19 | Information Disclosure | un nome o un indirizzo dentro un `RAISE EXCEPTION` | mitigate | I messaggi nominano **solo** identificatori; `subject_label` resta un `membership_code` imposto dal writer |
| T-35-20 | Tampering | `assignee_role` copiato da una lettura stantia | mitigate | `SELECT … FOR UPDATE` sul profilo, nello stesso statement che inserisce |
| T-35-21 | Denial of Service | doppia assegnazione viva | mitigate | Nessun `ON CONFLICT`: l'indice unico parziale rifiuta con `23505` e il chiamante ramifica per codice |
</threat_model>

<verification>
- `npm run baseline:container` esce pulito con la migration applicata
- `npm run build` passa
- Una prova SQL in container: assegnare, poi revocare, e verificare che la riga **esista ancora** con `revoked_at` valorizzato e `assignee_role` a `NULL`, e che `membership_acts` contenga **due** righe con lo stesso `party_id`
</verification>

<success_criteria>
- Assegnare e revocare producono ognuno un atto con autore e timestamp
- Nessuno stato in cui l'assegnazione esiste e l'atto no
- D-18 e' un paragrafo solo, e non contraddice se stesso
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-04-SUMMARY.md` when done
</output>
