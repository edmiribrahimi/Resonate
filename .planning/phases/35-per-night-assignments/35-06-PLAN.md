---
phase: 35-per-night-assignments
plan: 06
type: execute
wave: 4
depends_on: ["35-02", "35-05"]
files_modified:
  - scripts/rls-baseline.mjs
  - scripts/container/seed.mjs
autonomous: true
requirements: [ASSIGN-01, ASSIGN-04, ASSIGN-06]

must_haves:
  truths:
    - "La matrice di scrittura B3 gira su entrambe le tabelle nuove invece di rifiutarsi"
    - "Il tentativo di auto-assegnazione ha una sonda dedicata il cui esito atteso e' 23514, e la sonda e' tarata per mutazione"
    - "Il seed produce il terzo asse: staff assegnato alla notte 1, alla notte 2, e non assegnato"
    - "Le celle B2/B3 di ogni tabella che questa fase non ha toccato restano identiche a `35-pre`"
  artifacts:
    - path: "scripts/rls-baseline.mjs"
      provides: "le due voci PROBE_PAYLOADS e la sonda negativa dedicata ad ASSIGN-04"
      contains: "party_assignments"
    - path: "scripts/container/seed.mjs"
      provides: "il terzo asse delle persone sintetiche"
      contains: "35000001"
  key_links:
    - from: "scripts/rls-baseline.mjs"
      to: "public.party_assignments"
      via: "voce PROBE_PAYLOADS, senza cui B3 si rifiuta di girare"
      pattern: "party_assignments"
    - from: "scripts/container/seed.mjs"
      to: "public.party_assignments"
      via: "tre persone che differiscono per assegnazione a parita' di ruolo e stato"
      pattern: "party_assignments"
---

<objective>
Rendere misurabile cio' che le tre migration hanno costruito. Senza questo
piano, `ASSIGN-01` **e' vacuo in ogni cella**: la griglia dell'harness ha due
assi, ruolo × stato, e un'assegnazione e' un **terzo asse** che oggi non esiste.
E `ASSIGN-04` sarebbe **rosso per costruzione** invece che provato.

Purpose: `PROBE_PAYLOADS` si rifiuta di eseguire B3 se una tabella con RLS non
ha la sua voce — quindi due tabelle nuove **obbligano** a dichiarare due
payload. Ma la convenzione dell'harness dice `auth.uid()` per **ogni** colonna
che nomina un soggetto, e su `party_assignments` **sia `user_id` sia
`assigned_by` nominano un soggetto**: applicare la convenzione a entrambe
produrrebbe un `23514` permanente sulla cella normale, cioe' una matrice rossa
che non prova niente.

Output: due payload dichiarati, una sonda negativa dedicata e tarata, e tre
persone sintetiche che differiscono **solo** per assegnazione.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-PATTERNS.md
@.planning/phases/35-per-night-assignments/35-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: I due payload, e la decisione D-J scritta nel file</name>
  <files>scripts/rls-baseline.mjs</files>
  <read_first>
    - `scripts/rls-baseline.mjs:1041-1065` — il rifiuto che rende obbligatoria la voce (*«l'harness si rifiuta di eseguire B3 se una tabella enumerata non ha la sua voce»*), le due convenzioni (`auth.uid()` per ogni colonna che nomina il soggetto; `{{table}}` sostituito con l'id piu' basso della tabella referenziata, risolto **una volta** con una lettura privilegiata) e il divieto: nessuna colonna `UPDATE` della sonda puo' essere una guardia monotona
    - `scripts/rls-baseline.mjs:1069` e `:1092-1099` — `PROBE_PAYLOADS` e la voce `door_scan_events`, che e' la tabella con piu' colonne CHECK-vincolate, cioe' il caso piu' vicino a `party_assignments`
    - `.planning/phases/35-per-night-assignments/35-PATTERNS.md:930-967` — § 13, la tensione reale su ASSIGN-04 e la richiesta esplicita che il piano **dichiari** quale colonna prende `auth.uid()`
    - `supabase/migrations/20260809000000_party_assignments.sql` — le colonne e i vincoli reali della tabella, dal piano 35-02
    - `supabase/migrations/20260809003000_party_credits.sql` — le colonne reali di `party_credits`, dal piano 35-05
  </read_first>
  <action>
    Aggiungere due voci a `PROBE_PAYLOADS`.

    **`party_assignments` — e qui va scritta la decisione D-J, nel file, sopra
    la voce.** La convenzione «`auth.uid()` per ogni colonna che nomina il
    soggetto» **non si applica a entrambe le colonne** di questa tabella:
    applicarla produrrebbe `assigned_by = user_id`, cioe' una violazione
    permanente di `party_assignments_no_self_grant`, cioe' una cella rossa per
    costruzione che non distingue un rifiuto di policy da un rifiuto di
    vincolo.

    La decisione, scritta: **`user_id` prende `auth.uid()`** — e' la colonna del
    soggetto nel senso in cui la convenzione la intende, quella su cui vive il
    predicato di proprieta' `party_assignments_select_own`. **`assigned_by`
    prende un identificatore fisso e diverso**, risolto con la stessa meccanica
    `{{table}}` gia' usata per le foreign key: l'id piu' basso di
    `public.profiles` **che non sia il soggetto corrente**. Cosi' la cella
    normale misura la policy e non il vincolo.

    Le altre colonne della sonda: `party_id` con la meccanica `{{table}}` verso
    `public.event_parties`; `capability` con il valore letterale
    `'door.operate'` (una chiave che esiste dopo la migration 8 e che appartiene
    all'insieme assegnabile); `assignee_role` con `'staff'`; `ends_at` con un
    istante futuro fisso e **non** `now()`, perche' una sonda che dipende
    dall'orologio non e' riproducibile fra due esecuzioni.

    Nessuna colonna `UPDATE` della sonda tocca una guardia monotona: `revoked_at`
    non e' monotona nel senso di `meta-gates.md` (non e' `venue_reveal_sent`, non
    e' uno stato di pagamento, non e' un progressivo di serie), e va **detto**,
    perche' altrimenti il prossimo lettore la scambiera' per una.

    **`party_credits`** — voce ordinaria: `party_id` e `artist_id` con la
    meccanica `{{table}}`, `credit` con `'dj'`, `sort_order` con `0`,
    `created_by` con `auth.uid()`.
  </action>
  <acceptance_criteria>
    - `npm run baseline:container -- --phase-point=35-06` esegue **anche B3**, senza il messaggio «PROBE_PAYLOADS has no entry for»
    - Il file contiene, sopra la voce `party_assignments`, il paragrafo che dichiara quale delle due colonne prende `auth.uid()` e perche' l'altra no
    - La voce `party_assignments` **non** contiene due occorrenze di `auth.uid()`
    - La voce `party_assignments` **non** contiene `now()`
  </acceptance_criteria>
  <verify>
    <automated>npm run baseline:container -- --phase-point=35-06 2>&1 | tee /tmp/35-06-b3.log; ! grep -q "PROBE_PAYLOADS has no entry" /tmp/35-06-b3.log && echo B3_RUNS</automated>
  </verify>
  <done>B3 gira su entrambe le tabelle nuove, e la cella normale di `party_assignments` misura una policy e non un vincolo.</done>
</task>

<task type="auto">
  <name>Task 2: La sonda negativa di ASSIGN-04, tarata per mutazione</name>
  <files>scripts/rls-baseline.mjs</files>
  <read_first>
    - `scripts/rls-baseline.mjs:1041-1065` (di nuovo, per la meccanica delle sonde) e la sezione che esegue B3, per capire dove si aggancia una sonda che si aspetta un **errore** invece di un successo
    - `.planning/phases/35-per-night-assignments/35-VALIDATION.md:61` — la riga di ASSIGN-04: *«sonda B3 con `assigned_by = user_id` deve tornare `23514`; e la mutazione va provata: rimuovere il `CHECK` deve far diventare verde la cella»*
    - `.claude/rules/ai-engineering.md` — gate *prova per mutazione*: una prova la cui mutazione non e' stata asserita come applicata non ha provato niente
  </read_first>
  <action>
    Aggiungere una sonda **dedicata e separata** dalla voce ordinaria, il cui
    esito atteso e' un rifiuto: un `INSERT` su `public.party_assignments` con
    `assigned_by` **uguale** a `user_id`, entrambi `auth.uid()`, per **ogni**
    persona sintetica che possa scrivere.

    L'asserzione e' precisa: la sonda deve fallire con `SQLSTATE 23514` e con il
    nome del vincolo `party_assignments_no_self_grant`. Un fallimento con un
    codice **diverso** — per esempio `42501` (rifiuto di policy) o `23503` (FK) —
    e' un **fallimento della sonda**, non un successo: significherebbe che la
    scrittura e' stata fermata prima di arrivare al `CHECK`, e quindi che
    ASSIGN-04 non e' stato misurato. Lo script deve distinguere i due casi e
    dirlo.

    Scrivere accanto il paragrafo che dice perche' questa sonda e' separata
    dalla voce ordinaria: sono **due domande diverse** — *«chi puo' scrivere una
    riga»* e *«il database rifiuta una riga disonesta»* — e infilarle nella
    stessa cella produrrebbe una matrice rossa per la ragione giusta e
    illeggibile per la ragione sbagliata.

    **La taratura, che e' parte del task e non un controllo successivo.**
    Eseguire la prova per mutazione e **registrarla nel SUMMARY** con i suoi
    output:
    1. Far girare l'harness e osservare che la cella della sonda negativa e'
       **verde** (cioe': il rifiuto atteso e' arrivato, con `23514`).
    2. In una copia usa-e-getta del container, eseguire
       `ALTER TABLE public.party_assignments DROP CONSTRAINT party_assignments_no_self_grant;`
       e **asserire che il `DROP` sia andato a buon fine** prima di leggere
       qualunque risultato — una mutazione non applicata produce un risultato
       che sembra una prova e non lo e'.
    3. Rifare girare la sonda e osservare che la cella **cambia**: l'`INSERT`
       riesce, e quindi la sonda stava davvero misurando quel vincolo e non
       qualcos'altro.
    4. Distruggere il container. Non c'e' nessun percorso per cui questa
       mutazione tocchi un database vero: l'harness a container **non legge
       nessuna variabile d'ambiente**.
  </action>
  <acceptance_criteria>
    - `npm run baseline:container -- --phase-point=35-06-final` produce una cattura in cui la sonda negativa di ASSIGN-04 e' presente e verde
    - Il SUMMARY riporta i quattro passi della prova per mutazione con gli output osservati, incluso l'esito del `DROP CONSTRAINT` **prima** del risultato che ne dipende
    - Lo script distingue nel suo output un rifiuto `23514` da un rifiuto `42501`
  </acceptance_criteria>
  <verify>
    <automated>npm run baseline:container -- --phase-point=35-06-final && grep -q "party_assignments_no_self_grant" scripts/rls-baseline.mjs && echo SELF_GRANT_PROBE_OK</automated>
  </verify>
  <done>ASSIGN-04 ha una prova che e' stata tarata: si sa cosa la fa diventare rossa e cosa la fa diventare verde.</done>
</task>

<task type="auto">
  <name>Task 3: Il terzo asse nel seed del container</name>
  <files>scripts/container/seed.mjs</files>
  <read_first>
    - `scripts/container/seed.mjs:173-208` — `FORBIDDEN_WRITES` e il precedente **esatto** di come si allarga una lista quando un asse cresce: *«sei, non quattro, dal piano 43-08 … una lista di quattro sarebbe stata un rilevatore che guarda due terzi della regola e riporta un verde per tutta»*
    - `scripts/container/seed.mjs:230-249` — `buildPersonas` e la convenzione d'identita' delle persone sintetiche: id il cui primo gruppo e' letterale, indirizzo sul TLD riservato `.invalid`, un `fullName` che e' un **ruolo e mai una persona**, un `membershipCode` che `handle_new_user()` **non puo'** coniare
    - `scripts/container/seed.mjs` — la sezione che rilassa `profiles_role_implies_approved` durante la semina e lo **ripristina dopo**: le tre persone nuove non devono romperla, perche' sono tutte `staff` e quindi `approved`
    - `supabase/migrations/20260808001000_role_implies_approved.sql:179-181` — la regola: chi rinomina un vincolo lo rinomina **anche** nel seed. Ogni vincolo nominato dalla fase 35 e asserito dal seed eredita questa regola
  </read_first>
  <action>
    Aggiungere al seed **il terzo asse**, che oggi non esiste: la griglia e'
    ruolo × stato, e un'assegnazione e' un asse in piu'. Nessun analogo di terzo
    asse esiste in questo repository — la forma va progettata, non copiata, e il
    precedente piu' vicino e' l'allargamento di `FORBIDDEN_WRITES` da quattro a
    sei, che pero' e' un asse che **cresce** e non un asse che **si aggiunge**.

    Seminare almeno **due serate** distinte (notte 1 e notte 2) e **tre** persone
    sintetiche, tutte `staff` e `approved`, che differiscono **solo** per
    assegnazione:

    - *staff assegnato alla notte 1* — una riga viva in `party_assignments` con
      `capability = 'door.operate'` per la notte 1
    - *staff assegnato alla notte 2* — la stessa riga per la notte 2
    - *staff non assegnato* — nessuna riga

    Senza queste tre, ASSIGN-01 e' **vacuo in ogni cella**: la proprieta' da
    provare e' *«usa gli strumenti di quella notte e di nessun'altra»*, e serve
    almeno una coppia (persona, notte) in cui la risposta e' `false` mentre la
    stessa persona su un'altra notte risponde `true`.

    Seminare anche una **quarta** riga d'assegnazione, gia' **revocata**
    (`revoked_at` valorizzato, `revoked_by` valorizzato, `assignee_role` a
    `NULL`), sulla notte 1 per la persona non assegnata: e' l'unico modo di
    provare in container che una riga revocata **non concede** e **non blocca
    una demozione**.

    Le identita' seguono la convenzione: primo gruppo uuid **`35000001`** (fase
    35, primo piano che le introduce), indirizzo `@example.invalid`, un
    `fullName` che e' **un ruolo con il suo asse** — per esempio
    `Seed Persona staff assigned night1` — e un `membershipCode` fuori
    dall'alfabeto di `handle_new_user()`. **Ruoli, mai persone**: questo
    repository e' pubblico.

    Nota d'ordine: le assegnazioni si seminano **dopo** i profili e **dopo** le
    serate, perche' la foreign key composta verso `public.profiles (id, role)`
    e' valutata all'inserimento. Se il seed rilassa un vincolo durante la
    semina, **non** rilassare `party_assignments_assignee_role_fk`: le tre
    persone sono `staff` e la FK deve reggere — se non regge, il seed ha appena
    scoperto un difetto e deve fallire rumorosamente.
  </action>
  <acceptance_criteria>
    - `npm run baseline:container -- --phase-point=35-06-personas` esce 0
    - `grep -c "35000001" scripts/container/seed.mjs` >= 3
    - Il seed non contiene nessun nome di persona reale, nessun indirizzo che non finisca in `.invalid`
    - La cattura prodotta mostra almeno una riga revocata in `party_assignments`
    - Il seed **non** contiene un `DROP CONSTRAINT party_assignments_assignee_role_fk`
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '35000001' scripts/container/seed.mjs)" -ge 3 && ! grep -q "DROP CONSTRAINT party_assignments_assignee_role_fk" scripts/container/seed.mjs && npm run baseline:container -- --phase-point=35-06-personas</automated>
  </verify>
  <done>Il container conosce tre persone identiche per ruolo e stato e diverse per assegnazione, piu' una revoca, e ASSIGN-01 smette di essere vacuo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → database | `rls-baseline-container.mjs` **non legge nessuna variabile d'ambiente**: non esiste percorso per cui tocchi un database vero |
| seed → repository pubblico | Ogni identita' sintetica e' una pubblicazione: ruoli, mai persone, e TLD riservato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-27 | Repudiation | una matrice che salta in silenzio una tabella | mitigate | `PROBE_PAYLOADS` rifiuta B3 senza la voce; le due voci sono aggiunte e la corsa e' asserita |
| T-35-28 | Elevation of Privilege | ASSIGN-04 «provato» da una cella rossa per la ragione sbagliata | mitigate | Sonda negativa dedicata, che distingue `23514` da `42501`, tarata per mutazione con il `DROP` asserito prima del risultato |
| T-35-29 | Elevation of Privilege | l'assegnazione che concede altrove, non rilevata | mitigate | Il confronto B2/B3 contro `35-pre` deve mostrare **zero** variazioni su ogni tabella non toccata da questa fase |
| T-35-30 | Information Disclosure | un'identita' reale nel seed di un repo pubblico | mitigate | Primo gruppo uuid letterale, `.invalid`, `fullName` che e' un ruolo, `membershipCode` non coniabile |
</threat_model>

<verification>
- `npm run baseline:container` esegue B1, B2 **e** B3 senza rifiuti
- `npm run baseline:compare` fra `35-pre` e l'ultima cattura: le uniche differenze sono su `party_assignments` e `party_credits`
- La prova per mutazione di ASSIGN-04 e' registrata nel SUMMARY, con l'esito del `DROP CONSTRAINT` letto **prima** del risultato che ne dipende
</verification>

<success_criteria>
- Le due tabelle nuove sono misurate invece di essere saltate
- ASSIGN-04 ha una sonda dedicata e tarata
- Il container conosce il terzo asse, e ASSIGN-01 e' verificabile in almeno una coppia dove la risposta e' `false`
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-06-SUMMARY.md` when done
</output>
