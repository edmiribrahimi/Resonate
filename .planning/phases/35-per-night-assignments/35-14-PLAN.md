---
phase: 35-per-night-assignments
plan: 14
type: execute
wave: 7
depends_on: ["35-06", "35-08", "35-09", "35-10", "35-11", "35-12", "35-13", "35-15", "35-16", "35-17"]
files_modified:
  - .planning/phases/35-per-night-assignments/35-HUMAN-UAT.md
  - .planning/phases/35-per-night-assignments/35-VALIDATION.md
autonomous: true
requirements: [ASSIGN-01, ASSIGN-02, ASSIGN-03, ASSIGN-05, ASSIGN-08]

must_haves:
  truths:
    - "Ogni verifica che nessuno strumento di questo repository puo' raggiungere ha una procedura scritta: chi la fa, con quale ruolo, quali passi, cosa deve osservare"
    - "La dichiarazione di copertura onesta e' scritta: 4 requisiti su 8 chiudibili automaticamente, gli altri 4 con una meta' irraggiungibile"
    - "La meta' di ASSIGN-01 che nessun comando prova — che una persona assegnata RAGGIUNGA lo strumento — ha la sua procedura"
    - "Il debito differito di questa fase ha un nome, non e' una scoperta"
  artifacts:
    - path: ".planning/phases/35-per-night-assignments/35-HUMAN-UAT.md"
      provides: "la coda 6+6 in testa, undici procedure scritte, il debito differito, la copertura onesta"
      min_lines: 300
    - path: ".planning/phases/35-per-night-assignments/35-VALIDATION.md"
      provides: "il sign-off con nyquist_compliant e wave_0_complete aggiornati"
  key_links:
    - from: ".planning/phases/35-per-night-assignments/35-HUMAN-UAT.md"
      to: ".planning/phases/35-per-night-assignments/35-VALIDATION.md"
      via: "una procedura per ogni riga della tabella Manual-Only Verifications"
      pattern: "ASSIGN-0"
---

<objective>
Il deliverable che chiude la fase, e che in questo progetto **non e' un
formalismo**: non esiste un test runner per il prodotto, quindi *«l'unica prova
che esistera' e' una persona che guarda uno schermo e scrive cosa ha visto»*.
Questo piano scrive quella lista.

Purpose: quattro degli otto requisiti — ASSIGN-02, 03, 05 e 08 — hanno **una
meta' che nessuno strumento di questo repository puo' raggiungere**, perche'
vive su un telefono, con la radio spenta, in un build di produzione. E ASSIGN-01
ne ha guadagnata una: la matrice di scrittura prova che il **permesso** e'
per-notte, e **non prova** che la persona **arrivi** allo strumento — quello
dipende dal routing, e il routing si osserva solo aprendo l'applicazione. Per
tutto questo il deliverable della fase e' una **procedura scritta**. Il documento
va **scritto, non eseguito**: eseguirlo richiede una serata, e la decisione del
proprietario (`ACCESS-MODEL-DECISIONS.md` § 12) rimanda tutta la verifica
manuale alla fine della costruzione, con il suo prezzo dichiarato.

Output: `35-HUMAN-UAT.md` completo, e il sign-off di `35-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-night-assignments/35-VALIDATION.md
@.planning/ACCESS-MODEL-DECISIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Le undici procedure, scritte per una persona con un telefono</name>
  <files>.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/43-role-model-account-creation/43-HUMAN-UAT.md` — per intero, o almeno le prime 200 righe e tre procedure complete. E' il modello: ogni prova dice **chi** la fa, **cosa serve prima**, i **passi uno per uno**, **cosa deve succedere**, e **cosa significa se non succede**. Dove serve una competenza tecnica e' scritto a lettere piene **[serve una mano tecnica]**, e quei passi non sono per il proprietario. **Ruoli, mai persone**: nessun indirizzo, nessun nome, nessun codice
    - `.planning/phases/43-role-model-account-creation/43-HUMAN-UAT.md:96-120` — la sezione delle **finestre che si chiudono**: le prove che non si possono rimandare perche' dopo un certo momento non c'e' piu' niente da guardare
    - `.planning/phases/35-per-night-assignments/35-VALIDATION.md:89-104` — le righe di *Manual-Only Verifications*: sono l'indice di questo task
    - I SUMMARY dei piani 35-02, 35-08, 35-11, 35-12, 35-13, 35-16 e 35-17 — perche' le procedure descrivono **cio' che e' stato costruito davvero**, non cio' che il piano prevedeva
  </read_first>
  <action>
    Completare `35-HUMAN-UAT.md`, che il piano 35-01 ha aperto con la coda 6 + 6
    in testa, aggiungendo **undici procedure numerate**, ognuna nella forma di
    `43-HUMAN-UAT.md`.

    1. **ASSIGN-02 — l'accesso non sopravvive alla notte, su un dispositivo che
       non vede la rete da ore.** Scanner aperto **prima** della fine della
       notte, radio spenta, scansione **dopo** l'ora di fine. Cosa deve
       succedere: gli strumenti si nascondono e compare la frase che dice che la
       notte e' finita; **nessuna voce sparisce dalla coda**. Cosa significa se
       la coda si svuota: `validUntil` e' stato trattato come un confine invece
       che come una cortesia, ed e' un difetto da correggere prima della serata
       successiva. Osservare **quale delle due parti decide** — il dispositivo o
       il server.

    2. **ASSIGN-03 — una scansione in coda non resta appesa quando
       l'assegnazione viene revocata.** Scansionare offline, revocare
       l'assegnazione dal server, riaccendere la radio, guardare il drain. Cosa
       deve succedere: la scansione si **risolve** e il contatore «ancora in
       attesa» **scende**. Cosa significa se non scende: la voce e' finita in
       `blocked`, che aspetta un nuovo login — e un nuovo login non restituisce
       un'assegnazione revocata, quindi resterebbe li' per sempre. **Il segnale
       d'allarme e' scritto**: il contatore che non scende dopo una
       riconnessione.

    3. **ASSIGN-05, prima meta' — il rifiuto arriva come frase distinguibile.**
       **Solo in un build di produzione**: Next redige i messaggi delle Server
       Action soltanto li', quindi in `next dev` questa prova non prova niente e
       va scritto. Sessione assegnata alla **sola** porta, tentare
       l'annullamento; poi la stessa azione da un account organizer. Cosa deve
       succedere: due esiti diversi, e il primo dice **cosa fare** («chiedi a un
       organizer per questa notte»), non «qualcosa e' andato storto».

    4. **ASSIGN-05, seconda meta' — l'annullamento locale con la radio spenta
       non aggira la supervisione.** Radio spenta, sessione assegnata alla sola
       porta, tentare l'annullamento di una scansione in coda. Cosa deve
       succedere: un rifiuto **rumoroso** sullo schermo. Ripetere con una
       sessione da organizer: l'annullamento procede **e la voce resta in coda
       marcata**, non sparisce. Cosa significa se sparisce: il record della
       serata non contiene l'annullamento, e *«il percorso piu' semplice per far
       rientrare qualcuno»* e' tornato invisibile.

    5. **ASSIGN-08 — l'autorizzazione si risolve una volta sola.** Pannello di
       rete aperto, N scansioni consecutive. Cosa deve succedere: **N chiamate
       di check-in e ZERO chiamate d'autorizzazione**. E' un conteggio, non
       un'impressione: si scrive il numero.

    6. **ASSIGN-01 e ASSIGN-06 — la superficie fa quello che dice.** Assegnare e
       revocare da `/organizer/events/[id]/assignments`; verificare i **due
       atti** nel registro con autore e timestamp; creare un credito per una
       persona **senza account** e verificare che non compaia nessun account
       nuovo e che il credito non conceda nulla. E, come parte della stessa
       prova, il caso che la decisione D-A rende reale: **tentare di assegnare
       un `member`** e osservare che il rifiuto dice che va prima promosso a
       staff — e che il **credito pubblico non ha bisogno ne' di ruolo ne' di
       account**.

    **Aggiungere due procedure che nascono da cio' che questa fase ha costruito
    e che non erano nella tabella**, perche' senza di esse un difetto reale
    resterebbe non osservato:

    7. **L'upgrade di IndexedDB v4 → v5 su un dispositivo che porta gia' una
       coda non vuota.** Il precedente esiste (piano 43-13, esercitato su un
       dispositivo reale) e **va rifatto, non ereditato**. Cosa significa se la
       coda si svuota: presenze perse, in silenzio, e non c'e' error tracking
       che lo dica. **Questa e' una finestra che si chiude**: va esercitata
       **prima della prima serata reale**, non a un fine-costruzione astratto,
       perche' e' una prova il cui fallimento distrugge **dati** e non codice.

    8. **La demozione bloccata e il suo percorso d'uscita.** Assegnare una
       persona a una notte, poi provare a demoterla. Cosa deve succedere: il
       rifiuto **nomina le assegnazioni che bloccano** — la serata e il
       mestiere — e offre l'azione unica che revoca e poi demota. Cosa significa
       se dice solo «la scrittura e' fallita»: e' il precedente registrato
       *«Qualcosa e' andato storto»*, in un percorso che sara' urgente.

    **E aggiungere le tre che chiudono la meta' di ASSIGN-01 che nessun comando
    raggiunge — che una persona assegnata ARRIVI allo strumento.** Sono le prove
    che coprono i piani 35-15, 35-16 e 35-17, e senza di esse la fase avrebbe
    costruito un permesso senza sapere se e' spendibile.

    9. **ASSIGN-01, la porta — una persona `staff` assegnata raggiunge lo
       scanner.** **[serve una mano tecnica]** perche' pretende che la coda di
       migration sia applicata: senza la migration 12, il gate grossolano si
       comporta come prima e la prova e' falsa-negativa **per configurazione**.
       Con una sessione `staff` **senza** assegnazione, aprire lo scanner: deve
       rimbalzare. Assegnarla alla porta di una notte, ricaricare: deve
       **entrare**, e nella lista delle serate deve comparire **quella notte e
       nessun'altra**. Cosa significa se rimbalza lo stesso: e' il difetto che
       questa fase esiste per chiudere, e va guardato **prima** della serata —
       alle due di notte, davanti a una fila, non c'e' tempo di scoprirlo.
       Osservare anche le **tre cause distinte** del rimbalzo: assegnato ma non a
       questo strumento; controllo non eseguibile; nessuna assegnazione. Devono
       essere tre schermate diverse. Se sono la stessa, il rimbalzo e' tornato
       muto.

    10. **ASSIGN-01, il fotografo — l'assegnazione «photo» sblocca il
        caricamento.** Con una sessione `staff` **senza** assegnazione, tentare
        di caricare un media sulla pagina pubblica di un evento: deve essere
        rifiutata, e il rifiuto deve dire **quale** dei due motivi e'.
        Assegnarla come «photo» a una notte di quell'evento e riprovare: il
        caricamento deve riuscire. Cosa significa se riesce **anche senza**
        assegnazione: la chiave non gate nulla e ASSIGN-01 e' vuota sul mestiere
        «photo».

    11. **ASSIGN-01, l'organizer di una notte — e il gate che deve fallire.**
        Assegnare una persona come organizer di **una** notte di un evento con
        **due** serate. Aprire la lista di revisione di quella notte: deve
        vedersi. Cambiare a mano la serata nell'indirizzo verso l'altra: deve
        **rimbalzare**. Cosa significa se si vede anche l'altra: il gate e'
        per-evento e non per-notte, cioe' l'opposto del requisito. E cosa
        significa se la prima e' **vuota** invece che rimbalzata: manca il braccio
        `party.manage` nella policy del registro (piano 35-09), e una lista vuota
        li' e' lo stato normale di una serata tranquilla — cioe' un fallimento
        che si traveste da buona notizia.

    Aggiornare la sezione **finestre che si chiudono** in testa al documento,
    nominando la 7 come l'unica **irreversibile** di questa fase, e la 9 come
    quella da fare **prima della prima serata**, perche' il suo fallimento si
    scoprirebbe altrimenti davanti a una fila.
  </action>
  <acceptance_criteria>
    - `35-HUMAN-UAT.md` contiene **undici** procedure numerate, ognuna con: chi la fa, cosa serve prima, i passi, cosa deve succedere, cosa significa se non succede
    - Ogni procedura porta il suo requisito (`ASSIGN-0x`) e uno `status: pending`
    - La procedura 3 dice esplicitamente che vale **solo in un build di produzione**
    - La procedura 9 dice esplicitamente che senza la migration 12 applicata la prova e' falsa-negativa per configurazione
    - La procedura 7 e' marcata come finestra che si chiude, da esercitare prima della prima serata reale; la 9 e' marcata come da fare prima della prima serata
    - Il documento non contiene nessun nome proprio, nessun indirizzo email, nessun codice tessera, nessuna data non annunciata, nessuna sede
    - La coda 6 + 6 scritta dal piano 35-01 e' ancora in testa e non e' stata spostata
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md; test "$(grep -c 'ASSIGN-0' $F)" -ge 11 && test "$(grep -c 'status: pending' $F)" -ge 11 && test "$(grep -o '20260809[0-9]*_[a-z_]*\.sql' $F | sort -u | wc -l | tr -d ' ')" = "6" && ! grep -qiE "@(gmail|outlook|resonatemotion)" $F && echo UAT_OK</automated>
  </verify>
  <done>Esistono undici procedure che una persona puo' eseguire senza chiedere niente a nessuno, e due di esse sono marcate come non rimandabili.</done>
</task>

<task type="auto">
  <name>Task 2: La dichiarazione di copertura onesta, e il debito differito con il suo nome</name>
  <files>.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/35-per-night-assignments/35-VALIDATION.md:51-66` — la tabella per requisito con la colonna *Copertura automatica*: e' il contenuto della dichiarazione, e va riportato senza addolcirlo
    - I SUMMARY di 35-11, 35-12, 35-16 e 35-17 — le dichiarazioni di debito differito e i limiti dichiarati prodotti da quei piani
    - `.claude/rules/meta-gates.md` — il gate della verifica in un repo senza test: **non dire mai** che una modifica al prodotto e' verificata perche' «i test passano»
    - `.claude/rules/media-and-storage.md` — gate *EXIF prima della pubblicazione*, che e' la fonte della voce 6 sotto
  </read_first>
  <action>
    Aggiungere in coda a `35-HUMAN-UAT.md` due sezioni.

    **(a) La dichiarazione di copertura onesta**, scritta come dati e non come
    giudizio:

    - **Chiudibili automaticamente: quattro su otto** — ASSIGN-01 **per la parte
      del permesso** (matrice B3 su `party_assignments`, **e** B2/B3
      byte-identiche su ogni altra tabella, che e' la prova che l'assegnazione non
      filtra altrove), ASSIGN-04 (sonda negativa dedicata, tarata per mutazione),
      ASSIGN-06 (matrice piu' la prova negativa: una persona con un credito e
      nessuna assegnazione ha la stessa matrice di un `member`), ASSIGN-07
      (`npm run verify:no-credit-account`).
    - **La precisazione su ASSIGN-01**, che va scritta e non addolcita: la
      matrice prova che il **permesso** e' per-notte. **Non** prova che la persona
      **arrivi** allo strumento — quello dipende dal routing e da due pagine, e si
      osserva solo aprendo l'applicazione. Le procedure 9, 10 e 11 sono quella
      meta'.
    - **Con una meta' irraggiungibile: quattro su otto** — ASSIGN-02, 03, 05, 08.
      La ragione, scritta una volta: quella meta' **vive su un telefono, con la
      radio spenta, in un build di produzione**, e nessuno strumento di questo
      repository la raggiunge.
    - **La frase che vale per tutte:** `npm run build` e' un typecheck, e **passa
      senza che nessuna migration sia applicata**. Un build verde in questa fase
      **non e' evidenza** che qualcosa funzioni.

    **(b) Il debito differito, con il suo nome**, perche' un debito senza nome
    diventa invisibile:

    1. **L'annullamento non e' registrato su due rami su tre** — guest list e
       membership (piano 35-11). Conseguenza: su quei due percorsi un
       annullamento resta invisibile nella lista di revisione della serata.
       Perche' e' differito: e' lavoro di **registrazione**, non di
       autorizzazione, e il ramo membership dovrebbe smettere di **cancellare**
       una riga di presenza — che e' una modifica al significato dei dati di
       presenza, non un ritocco.
    2. **La concessione reciproca non e' coperta** — il `CHECK` ferma *«A
       assegna A»*, non *«A assegna B, B assegna A»*. **Non e' in ASSIGN-04**, ed
       e' dichiarata fuori scopo. Il mitigante che esiste gia' e' l'attribuzione:
       `membership_acts` con l'indice sull'autore rende quella coppia
       **leggibile**.
    3. **`attendances.entry_role` non ha ancora il suo terzo caso** — la
       migration della fase 43 dice che un'assegnazione per-serata e' un terzo
       modo d'ingresso. Questa fase **non** lo scrive, perche' non e' in nessuno
       degli otto requisiti. Nominarlo qui e' cio' che gli impedisce di sparire.
    4. **`event_parties.lineup` e `party_credits` convivono** — la frase che dice
       quale vince per cosa e' scritta nella migration; la migrazione fra le due
       e' rimandata a **una fase che la nomini**.
    5. **La superficie delle assegnazioni sara' spostata dalla fase 34**, dietro
       un redirect permanente. E' previsto, non un difetto.
    6. **I metadati di una foto non vengono spogliati, e questa fase ha
       aggiunto il caricatore piu' esposto.** Non esiste nessuna sanitizzazione
       EXIF in questo repository, e una foto scattata dentro una sede segreta
       porta le coordinate **nel file**, non nella didascalia. Il fotografo
       assegnato a una notte e' esattamente la persona che scatta **dentro** quella
       sede. Il percorso e' **preesistente** e il meccanismo non e' cambiato —
       questa fase aggiunge una **classe di caricatore**, non una falla nuova — ma
       e' `venue-secrecy.md` e quindi **irreversibile**: una volta che il file e'
       online, le coordinate sono uscite. La chiusura e' spogliare i metadati
       prima che l'oggetto diventi raggiungibile, e **non e' in nessuno degli otto
       requisiti**. Scritto qui perche' chi assegna un fotografo deve saperlo
       prima della serata, non dopo.
    7. **`media.upload` e' per-notte, `event_media` e' per-evento.** Essere
       assegnati come «photo» a **una** serata apre il caricamento su **tutto**
       l'evento. Il divario si chiude aggiungendo `party_id` a `event_media`, che
       e' un cambio di modello dati con un backfill sulle righe che esistono gia'.
       Fuori dagli otto requisiti; nominato perche' la sua assenza sembrera' una
       svista a chi legge la chiave.
    8. **Un evento non pubblicato non e' raggiungibile per assegnazione.** Chi
       arriva alla revisione della notte per il braccio `party.manage` e'
       tipicamente `staff`, e le policy di `events` e `event_parties` gli mostrano
       solo cio' che e' pubblicato. Per una lista che si guarda **dopo** la serata
       il limite e' stretto, ma esiste. Chiuderlo richiede un braccio in piu' su
       quelle policy — una tredicesima migration, fuori dagli otto requisiti.
    9. **Nessuna navigazione porta alla revisione per-notte.** Chi e' assegnato
       come organizer di una notte raggiunge quella pagina solo con un indirizzo
       che qualcuno gli passa: `NAV_ITEMS` nasconde le voci **per ruolo**, e un
       `staff` assegnato non ha un ruolo che la mostri. E' STAFF-03 della fase 34
       — la navigazione ricostruita su quattro ruoli — e non e' un difetto di
       questa fase, ma senza il nome diventa *«lo strumento non funziona»* invece
       di *«lo strumento non si trova»*.
  </action>
  <acceptance_criteria>
    - Il documento contiene la dichiarazione con i due gruppi di quattro requisiti, nominati per ID
    - Il documento contiene la precisazione su ASSIGN-01: la matrice prova il permesso, non l'arrivo allo strumento
    - Il documento contiene la frase esplicita che un `npm run build` verde non e' evidenza in questa fase
    - Il documento contiene le **nove** voci di debito differito, ognuna con la ragione per cui e' differita
    - Nessuna voce di debito e' formulata come «da valutare»: ognuna dice cosa manca e perche' non e' stato fatto
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md; grep -q "ASSIGN-07" $F && grep -q "verify:no-credit-account" $F && grep -qi "entry_role" $F && grep -qi "lineup" $F && grep -qi "EXIF" $F && grep -qi "NAV_ITEMS" $F && echo COVERAGE_OK</automated>
  </verify>
  <done>Chi legge sa cosa e' provato, cosa non lo e', e cosa e' stato deliberatamente lasciato fuori — comprese le tre cose che questa fase ha reso piu' probabili senza costruirle.</done>
</task>

<task type="auto">
  <name>Task 3: Il sign-off di `35-VALIDATION.md`</name>
  <files>.planning/phases/35-per-night-assignments/35-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/35-per-night-assignments/35-VALIDATION.md:69-118` — i requisiti di Wave 0 e la lista di sign-off, che sono l'indice di questo task
    - I SUMMARY dei piani 35-01 e 35-06 — sono i due piani che hanno soddisfatto i requisiti di Wave 0, e il sign-off va spuntato **contro cio' che e' successo**, non contro cio' che era previsto
  </read_first>
  <action>
    Aggiornare `35-VALIDATION.md`:

    - Spuntare le cinque voci di **Wave 0 Requirements** **solo** se il SUMMARY
      del piano corrispondente le documenta. Una voce non soddisfatta resta non
      spuntata, con una riga che dice **quale piano** avrebbe dovuto
      soddisfarla: una lista di controllo spuntata per cortesia e' peggio di una
      lista incompleta.
    - Impostare `wave_0_complete: true` **solo se** tutte e cinque sono
      soddisfatte.
    - Spuntare le voci di **Validation Sign-Off**, e in particolare riportare la
      dichiarazione di copertura onesta con gli stessi ID.
    - Impostare `nyquist_compliant` in base al fatto reale: `true` se ogni task
      della fase ha portato un `<automated>` oppure una dipendenza esplicita da
      Wave 0; **`false` con la ragione scritta** altrimenti. La fase 31 ha gia'
      tenuto `nyquist_compliant: false` **deliberatamente**, e tenerlo falso con
      una ragione e' una risposta legittima; metterlo vero senza guardare non lo
      e'.
    - `status: draft` diventa lo stato reale del documento.

    **Non toccare** ne' la tabella *Per-Requirement Verification Map* ne' la
    sezione *Feedback Latency Tiers*: la prima e' la dichiarazione onesta di cosa
    un comando puo' provare e non va aggiustata per farla concordare con l'esito;
    la seconda dichiara quanto costa davvero ogni livello di prova, ed e' un
    fatto misurato, non una preferenza.
  </action>
  <acceptance_criteria>
    - Il frontmatter porta `wave_0_complete` e `nyquist_compliant` con valori corrispondenti ai fatti registrati nei SUMMARY
    - Ogni voce di Wave 0 non spuntata porta il nome del piano che avrebbe dovuto soddisfarla
    - La tabella *Per-Requirement Verification Map* e la sezione *Feedback Latency Tiers* sono invariate nel diff
    - `Approval` riflette lo stato reale
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/35-per-night-assignments/35-VALIDATION.md; grep -qE "^nyquist_compliant: (true|false)" $F && grep -qE "^wave_0_complete: (true|false)" $F && grep -q "Feedback Latency Tiers" $F && echo SIGNOFF_OK</automated>
  </verify>
  <done>Il contratto di validazione dice cosa e' successo, e le sue caselle non sono spuntate per cortesia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` → repository pubblico | Ogni riga di questi documenti e' una pubblicazione irreversibile |
| documento → persona che eseguira' la prova | Una procedura ambigua e' una prova che non verra' fatta, o che verra' fatta e dara' un verde falso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-68 | Information Disclosure | un nome, un indirizzo o una sede in un documento pubblico | mitigate | Ruoli, mai persone; nessun indirizzo, nessun codice tessera, nessuna data non annunciata; asserito con un grep |
| T-35-69 | Repudiation | una fase dichiarata verificata quando non lo e' | mitigate | La dichiarazione di copertura onesta, con la frase che un build verde non e' evidenza in questa fase |
| T-35-70 | Repudiation | un debito che sparisce perche' nessuno lo ha nominato | mitigate | Nove voci di debito differito, ognuna con la ragione |
| T-35-71 | Repudiation | una lista di controllo spuntata per cortesia | mitigate | Ogni voce non soddisfatta resta non spuntata e nomina il piano che avrebbe dovuto soddisfarla |
| T-35-93 | Repudiation | ASSIGN-01 dichiarato coperto dalla sola matrice, mentre la raggiungibilita' non e' provata | mitigate | La precisazione scritta nella dichiarazione, e le procedure 9, 10 e 11 che sono quella meta' |
</threat_model>

<verification>
- `35-HUMAN-UAT.md` contiene undici procedure con `status: pending`
- La coda 6 + 6 e' ancora in testa
- `35-VALIDATION.md` porta valori booleani reali, e la tabella per requisito e i livelli di latenza sono invariati
- Nessun documento contiene un dato che il repository pubblico non deve portare
</verification>

<success_criteria>
- Ogni verifica manual-only ha una procedura eseguibile senza chiedere niente a nessuno
- La copertura e' dichiarata come dati: quattro su otto automatici, quattro con una meta' irraggiungibile, e ASSIGN-01 con la sua meta' di raggiungibilita' nominata
- Il debito ha un nome, e le due finestre che si chiudono sono marcate come tali
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-14-SUMMARY.md` when done
</output>
