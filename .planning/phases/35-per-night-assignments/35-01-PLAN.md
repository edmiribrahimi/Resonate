---
phase: 35-per-night-assignments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-capability-model-in-the-database/baseline/
  - .planning/phases/35-per-night-assignments/35-HUMAN-UAT.md
autonomous: false
requirements: [ASSIGN-01, ASSIGN-04]

must_haves:
  truths:
    - "Esiste una cattura di baseline presa PRIMA della prima riga di DDL della fase 35"
    - "La coda di migration da applicare a mano e' scritta come 6 + 6, in un ordine unico, con la ragione per riga"
    - "Chi legge sa che `npm run build` verde NON e' prova che le migration siano applicate"
  artifacts:
    - path: ".planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-policies.container.35-pre.json"
      provides: "Il termine di paragone contro cui si misura ogni cambiamento di questa fase"
    - path: ".planning/phases/35-per-night-assignments/35-HUMAN-UAT.md"
      provides: "L'ordine di applicazione in testa, e il posto dove atterrano le procedure delle wave successive"
      contains: "20260808000500_staff_role.sql"
  key_links:
    - from: ".planning/phases/35-per-night-assignments/35-HUMAN-UAT.md"
      to: ".planning/phases/43-role-model-account-creation/43-HUMAN-UAT.md"
      via: "la coda 6 + N: le sei della fase 43 vengono prima, tutte"
      pattern: "43-HUMAN-UAT"
---

<objective>
Wave 0. Mettere in piedi le due sole cose che **non possono essere fatte dopo**:
la cattura di baseline **prima della prima riga di DDL**, e la dichiarazione
scritta della coda di migration applicata a mano.

Purpose: una baseline presa dopo il cambiamento non e' una baseline — e' una
fotografia del risultato. E in questa fase la coda non parte da zero: **le sei
migration della fase 43 sono committate e NON applicate**
(`43-VERIFICATION.md`: `migrations_applied: 0`, `deployed: false`). Il ruolo
`staff`, il vincolo `role ⇒ approved` e `membership_acts` **non esistono in
produzione oggi**. Tutto cio' che questa fase costruisce si appoggia su di essi.

Output: la cattura `35-pre`, e `35-HUMAN-UAT.md` con la tabella ordinata
**6 + 6** in testa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ACCESS-MODEL-DECISIONS.md
@.planning/phases/35-per-night-assignments/35-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: La cattura di baseline, prima di qualunque DDL</name>
  <files>.planning/phases/32-capability-model-in-the-database/baseline/</files>
  <read_first>
    - `scripts/rls-baseline-container.mjs:1-90` — l'intestazione, l'uso di `--phase-point`, e la riga che dice che questo script **non legge nessuna variabile d'ambiente** (non esiste percorso per cui tocchi un database vero)
    - `scripts/rls-baseline.mjs:60-90` — `PHASE_DIR` / `BASELINE_DIR` sono **cablati sulla directory della fase 32**: la cattura di questa fase atterra li' e si distingue solo per `--phase-point`
    - `scripts/rls-baseline.mjs:400-470` — la costruzione del nome file e il rifiuto quando un `--phase-point` esiste gia' senza `--overwrite`
    - `.planning/phases/32-capability-model-in-the-database/baseline/` — l'elenco esistente, per vedere la convenzione dei phase-point usati dalla fase 43 (`43-05`, `43-final`, …)
  </read_first>
  <action>
    Eseguire la cattura a container con `--phase-point=35-pre`, che e' il nome
    riservato a questa fase, e committarla.

    Comando: `npm run baseline:container -- --phase-point=35-pre`.

    **Non** usare `--overwrite`: se un file `35-pre` esistesse gia', il rifiuto
    dello script e' l'informazione, non l'ostacolo — significa che qualcuno ha
    gia' catturato e va capito quando.

    Se il demone Docker non risponde, lo script esce **2**, e 2 significa
    *«niente e' stato misurato, quindi niente e' fallito»*: in quel caso il task
    non e' completo e non si prosegue con nessuna DDL. Questo e' l'unico punto
    della fase in cui un exit 2 e' bloccante.

    Registrare nel SUMMARY i nomi esatti dei file prodotti e il numero di policy
    catturate, perche' e' il numero contro cui si confrontera' ogni wave
    successiva.
  </action>
  <acceptance_criteria>
    - `ls .planning/phases/32-capability-model-in-the-database/baseline/ | grep 35-pre` elenca almeno un file
    - Nessun file sotto `supabase/migrations/` con prefisso `202608090` esiste nel commit che contiene la cattura — verificabile con `ls supabase/migrations/202608090* 2>/dev/null; echo "exit=$?"` che deve stampare `exit=2` (nessun match)
    - Il SUMMARY riporta il conteggio di policy della cattura, come numero
  </acceptance_criteria>
  <verify>
    <automated>test -n "$(ls .planning/phases/32-capability-model-in-the-database/baseline/ | grep 35-pre)" && ! ls supabase/migrations/202608090* >/dev/null 2>&1 && echo BASELINE_BEFORE_DDL_OK</automated>
  </verify>
  <done>La cattura `35-pre` esiste ed e' committata, e nella stessa revisione non esiste ancora nessuna migration della fase 35.</done>
</task>

<task type="auto">
  <name>Task 2: `35-HUMAN-UAT.md` — la coda 6 + 6 in testa</name>
  <files>.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/43-role-model-account-creation/43-HUMAN-UAT.md:1-120` — il modello: l'intestazione che spiega perche' il file esiste, la tabella ordinata delle sei migration con la ragione per riga, la regola *migration prima, codice dopo* con il suo accoppiamento misurato, e la prova piu' economica (`npm run verify:capabilities`)
    - `.planning/ACCESS-MODEL-DECISIONS.md` § 12 — la decisione del proprietario di rimandare **tutta** la verifica manuale alla fine della costruzione, **con il suo prezzo dichiarato**. Il file nuovo la cita, non la ridiscute
    - `.planning/phases/35-per-night-assignments/35-VALIDATION.md:89-104` — le sei verifiche manual-only che questo file dovra' ospitare
  </read_first>
  <action>
    Creare `35-HUMAN-UAT.md` con `status: partial` nel frontmatter, in italiano,
    con la stessa disciplina di `43-HUMAN-UAT.md`: **ruoli, mai persone**,
    nessun indirizzo, nessun codice.

    Contenuto obbligatorio di questa versione (le procedure per requisito
    arrivano nel piano 35-14, e il file dichiara che sono attese):

    1. **La coda 6 + 6, in un'unica tabella ordinata**, con la ragione per riga.
       Le prime sei sono quelle della fase 43, riprese testualmente:
       `20260808000500_staff_role.sql`, `20260808001000_role_implies_approved.sql`,
       `20260808002000_membership_register.sql`,
       `20260808003000_attendances_entry_role.sql`,
       `20260808004000_master_reconcile.sql`,
       `20260808005000_membership_acts_append_only.sql`.
       Le sei della fase 35 vengono **dopo tutte e sei, senza eccezioni**:
       `20260809000000_party_assignments.sql`,
       `20260809001000_assignment_resolver.sql`,
       `20260809002000_assignment_acts.sql`,
       `20260809003000_party_credits.sql`,
       `20260809004000_door_scan_events_by_assignment.sql`,
       `20260809005000_live_assignment_flag.sql`.
       La ragione per riga, in una frase ciascuna: la 7 crea la tabella e ha
       bisogno del ruolo `staff` (riga 1) e del vincolo `role ⇒ approved`
       (riga 2); la 8 modifica il corpo del resolver e legge la tabella della
       riga 7; la 9 allarga il CHECK di `membership_acts`, che nasce alla riga 3;
       la 10 e' indipendente dalle altre quattro ma sta dopo per non spezzare la
       lettura; la 11 riscrive una policy di `door_scan_events` usando il
       predicato che la riga 8 ha appena esteso; la 12 aggiunge **una chiave** al
       payload di `public.my_access_context()` leggendo la tabella della riga 7,
       e sta **per ultima** perche' quella funzione e' chiamata dal middleware a
       **ogni richiesta**: si applica quando tutto il resto e' gia' in piedi, e
       se resta non applicata la chiave semplicemente non arriva e il gate
       grossolano si comporta come oggi.

    2. **La frase sul falso verde.** Scritta a lettere piene: `npm run build` e
       il typecheck **passano senza che nessuna migration sia applicata**, perche'
       i tipi vengono da `src/types/database.ts` e non dal database vivo. Un
       build verde qui e' uno **stato di verifica falso-positivo**, non una prova.

    3. **La regola migration→codice**, ripresa da `43-HUMAN-UAT.md:58-74`: le
       migration prima, il codice dopo, mai il contrario; il verso opposto e'
       misurato sicuro.

    4. **La prova piu' economica**, subito dopo l'applicazione:
       `npm run verify:capabilities`, che deve diventare verde. Dichiarare che
       il conteggio atteso **cambia in questa fase**: la fase 35 conia tre
       chiavi, e i totali di `verify-capabilities.mjs` passano da 36 coppie /
       20 grant / 16 rifiuti a 48 / 26 / 22.

    5. **Un elenco segnaposto** delle sei verifiche manual-only di
       `35-VALIDATION.md`, ognuna con `status: pending` e la nota che la
       procedura completa arriva col piano 35-14.

    6. **La dichiarazione di copertura onesta**, in una riga: quattro requisiti
       su otto (ASSIGN-01, 04, 06, 07) sono chiudibili automaticamente; gli altri
       quattro (02, 03, 05, 08) hanno una meta' che **nessuno strumento di questo
       repository puo' raggiungere**, perche' vive su un telefono, con la radio
       spenta, in un build di produzione.
  </action>
  <acceptance_criteria>
    - Il file esiste e contiene, nell'ordine, le dodici voci di migration sopra elencate
    - `grep -c '20260808' 35-HUMAN-UAT.md` >= 6 e `grep -c '20260809' 35-HUMAN-UAT.md` >= 6
    - Il file contiene la frase esplicita che un `npm run build` verde non prova che le migration siano applicate
    - Il file non contiene nessun nome proprio, nessun indirizzo email, nessun codice tessera
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md; test "$(grep -o '20260808[0-9]*_[a-z_]*\.sql' $F | sort -u | wc -l | tr -d ' ')" = "6" && test "$(grep -o '20260809[0-9]*_[a-z_]*\.sql' $F | sort -u | wc -l | tr -d ' ')" = "6" && echo QUEUE_OK</automated>
  </verify>
  <done>`35-HUMAN-UAT.md` esiste, apre con la coda ordinata 6 + 6, dichiara il falso verde del build, e ospita i sei segnaposto delle verifiche manuali.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] La coda di migration, letta e accettata prima di scrivere DDL</name>
  <files>.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md` — il documento prodotto dal task 2, con la coda 6 + 6 in testa
    - `.planning/ACCESS-MODEL-DECISIONS.md` § 12 — la decisione del proprietario di rimandare tutta la verifica manuale alla fine della costruzione, **con il prezzo dichiarato**
    - `.planning/phases/43-role-model-account-creation/43-VERIFICATION.md` — i dati che rendono questo checkpoint necessario: `migrations_applied: 0`, `deployed: false`
  </read_first>
  <action>
    Presentare al proprietario i tre fatti elencati sotto e **fermarsi** finche'
    non risponde. Non applicare nessuna migration, non eseguire nessun deploy e
    non modificare nessun file in questo task: l'unico output e' la risposta,
    registrata nel SUMMARY con la data e con le parole con cui e' stata data.
  </action>
  <what-built>
    La cattura di baseline `35-pre` (task 1) e la tabella ordinata delle dodici
    migration in `35-HUMAN-UAT.md` (task 2).
  </what-built>
  <how-to-verify>
    Questo checkpoint **non chiede di applicare niente**. Chiede di leggere e
    confermare tre fatti, perche' ogni piano successivo di questa fase e'
    costruito sopra di essi.

    1. Aprire `.planning/phases/35-per-night-assignments/35-HUMAN-UAT.md` e
       leggere la tabella delle dodici migration. Confermare che le prime sei
       sono **esattamente** quelle della fase 43 e che nessuna delle sei
       nuove le precede.
    2. Confermare di aver letto la frase sul falso verde: durante tutta questa
       fase `npm run build` sara' verde **senza che nessuna migration sia
       applicata**. Il verde non e' evidenza. L'unico posto in cui la DDL viene
       davvero esercitata prima che una persona la applichi e'
       `npm run baseline:container`.
    3. Confermare la decisione gia' presa e **non riaperta qui**
       (`ACCESS-MODEL-DECISIONS.md` § 12): tutta la verifica manuale — le
       quattordici prove della fase 32, le sedici della 43 e le sei di questa —
       viene eseguita **alla fine della costruzione**, non ora. Il prezzo e'
       dichiarato: se una di quelle prove e' rossa, cio' che le e' stato
       costruito sopra e' costruito su una fondazione sbagliata, e il
       rifacimento e' proporzionale a quanto e' stato costruito.

    Se qualcuno vuole invece **applicare adesso** le sei della fase 43, e' una
    decisione diversa e legittima: si applica la coda dall'inizio, si fa girare
    `npm run verify:capabilities` e si annota l'esito in `35-HUMAN-UAT.md`. Ma
    e' una scelta, e va detta.
  </how-to-verify>
  <acceptance_criteria>
    - La risposta del proprietario e' registrata nel SUMMARY del piano, con la data
    - Se la scelta e' «procedi senza applicare», il SUMMARY lo scrive con quelle parole, cosi' che il `35-VERIFICATION.md` non possa dire «verificato»
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/35-per-night-assignments/35-HUMAN-UAT.md && echo CHECKPOINT_INPUT_PRESENT</automated>
  </verify>
  <done>La risposta del proprietario e' registrata nel SUMMARY con la data; nessuna migration e' stata applicata da questo task.</done>
  <resume-signal>Scrivere "letto, procedi" oppure "applico prima le sei"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → database di produzione | Nessuno strumento di questa fase applica migration. Il confine e' una persona |
| repository pubblico → chiunque | Ogni file di `.planning/` e' una pubblicazione irreversibile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Repudiation | baseline non presa prima della DDL | mitigate | Task 1 asserisce meccanicamente l'assenza di file `supabase/migrations/202608090*` nella stessa revisione |
| T-35-02 | Information Disclosure | `35-HUMAN-UAT.md` in un repo pubblico | mitigate | Ruoli mai persone; nessun indirizzo, nessun codice tessera; criterio di `S7` in 35-PATTERNS.md |
| T-35-03 | Tampering | applicazione della coda fuori ordine | mitigate | Tabella ordinata con la ragione per riga; le sei della fase 43 dichiarate prime senza eccezioni |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | **Questa fase installa zero pacchetti** (35-RESEARCH § Standard Stack, `Package Legitimacy Audit`: non applicabile). Nessun install task esiste in nessun piano; se un piano ne proponesse uno, va prima ripassato dal gate di legittimita' |
</threat_model>

<verification>
- `ls .planning/phases/32-capability-model-in-the-database/baseline/*35-pre*` restituisce almeno un file
- Nessuna migration `202608090*` esiste nella revisione della cattura
- `35-HUMAN-UAT.md` elenca 6 file `20260808*` e 6 file `20260809*`
- Il checkpoint bloccante ha una risposta registrata con la data
</verification>

<success_criteria>
- Esiste un termine di paragone anteriore a ogni DDL di questa fase
- La coda di applicazione manuale e' scritta una volta, in un ordine unico, e nessun piano successivo la deve ricostruire
- Il proprietario ha confermato per iscritto che si procede sapendo che nulla e' applicato
</success_criteria>

<output>
Create `.planning/phases/35-per-night-assignments/35-01-SUMMARY.md` when done
</output>
