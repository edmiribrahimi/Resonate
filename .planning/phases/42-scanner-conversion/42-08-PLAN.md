---
phase: 42-scanner-conversion
plan: 08
type: execute
wave: 4
execution_group: after-door-pass
blocked_on: the-door-pass-on-the-unconverted-scanner
depends_on: ["42-04", "42-06"]
files_modified:
  - src/app/(admin)/admin/scanner/ScannerClient.tsx
autonomous: true
requirements: [DS-04]

must_haves:
  truths:
    - "Every raw palette utility left on the door either became a token or is a derogation somebody argued in writing — none of them survives by not having been looked at"
    - "The five class strings chosen by an expression carry the same decision in both branches, so a reader asking what colour a band is finds one answer"
    - "Colour is still never the only channel: every state that changed hue still carries its own words"
    - "The three outcomes still measure above the threshold after the pill and the queue states moved"
  artifacts:
    - path: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      provides: "the door's surface on tokens, with the accept vocabulary declared rather than accidental"
      contains: "sem-warn"
  key_links:
    - from: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      to: "src/app/globals.css"
      via: "every target is an exposed token utility, and no colour is invented"
      pattern: "sem-"
    - from: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      to: "scripts/verify-scan-legibility.mjs"
      via: "the connectivity pill's constant, which the gate reads on every run"
      pattern: "FLASH_STATES"
---

> # ⛔ BLOCKED — do not start this plan until the door pass has been run
>
> D-42-04 and the roadmap's ordering constraint. Check row **3m** of
> `.planning/phases/42-scanner-conversion/42-PROCEDURES.md`; if its `Result:`
> reads `pending`, stop and report. The date of that night is deliberately absent
> from `.planning/`, which is public; read it from the calendar in `docs/`.

<objective>
Put the rest of the door's surface on the token vocabulary, applying a table
somebody wrote when the whole picture was in view.

Purpose: after the three outcomes, `ScannerClient.tsx` still carries the great
majority of the perimeter's raw palette — the connectivity pill, the queue
states, the end-of-night notice, the camera error, the freshness band, the stale
cache notice, the torch, the guest list badges and counters, and one white ink on
an accent fill that is the exact violation the token layer names at 2,91:1.

Two properties of this file decide how the work is done. It is **3449 lines and
is not restructured** — the conversion is mechanical and in place, because this
is a safety surface and a refactor inside a phase that promised not to touch
behaviour is how behaviour changes. And **five of its class strings are chosen by
an expression**, so a substitution driven by a string search catches them while a
reader asking *what colour is this band* finds two answers in two branches. Both
branches are in the mapping, as their own rows.

Nothing here is decided at the keyboard. The targets, the derogations, the violet
that must not become the completion semantic and the pill's branch were all
settled in plan 42-04 with numbers attached.

Output: a door whose colours come from the vocabulary, and four or five
utilities that stay raw because somebody wrote down why.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-scanner-conversion/42-CONTEXT.md
@.planning/phases/42-scanner-conversion/42-MAPPING.md
@.planning/phases/42-scanner-conversion/42-BASELINE.md
@.claude/rules/checkin-offline.md
</context>

<phase_rules>
1. **There is no test runner.** Exit codes, greps with expected counts, source
   assertions, `npm run build`, written procedures.
2. **⚠ `scripts/verify-conversion.mjs` holds NUL bytes**; use
   `LC_ALL=C /usr/bin/grep` or the Read tool on it.
3. **`.planning/` is PUBLIC.** Roles, never people; no night, no venue, no sigla.
4. **DEF-41-01 — never spell a bracketed arbitrary-value utility in prose.**
5. **macOS/BSD.** `grep -E`, `sed -i ''`.
6. **No package is installed by this phase.**
7. **No behaviour change and no restructuring.** No outcome, no dwell, no haptic,
   no queue constant, no undo, no torch behaviour, no auto-return, no `qrbox`,
   no `fps`, no `facingMode`, no `showFlash` call site added or removed, no
   component extracted, no hook moved.
8. **The four error-message tables are byte for byte.** A conversion that
   collapsed three distinct causes into one sentence would recreate this
   project's recorded defect while looking like a styling commit.
9. **Adding prose to this file is expensive** — it is the persona's worst
   measured context case. Substitute; do not annotate.
</phase_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: The header, the connectivity pill, the queue states and the end of the night</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `.planning/phases/42-scanner-conversion/42-MAPPING.md` — the rows for this region, and the derogation section. This is the specification; do not re-derive a target
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:2636-2700` — the night selector and its guest list counter
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:2760-2830` — the surface root, the sticky header, the connectivity pill in its constant, and the white ink on the accent fill
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:2850-2960` — the end-of-night notice with its *scan anyway*, the clock drift row, the queue pills — pending, could not be recorded, sign in again, undone and held on this device, cannot read own queue — and the list of failures
    - `src/app/globals.css:145-200` and `:354-400` — the semantic set with its two recorded coincidences, and which names actually have a utility
    - `.planning/phases/42-scanner-conversion/42-BASELINE.md` blocks 4, 7 and 13 — the call sites, the queue constants and the message tables, which this task must leave identical
  </read_first>
  <action>
Apply the mapping's rows for this region, in place. Substitutions only: no
element added, none removed, none re-nested, no class reordered beyond the
substitution itself.

The three that carry a decision rather than a rename, and are already argued in
the mapping:

- **The connectivity pill** takes the branch the mapping recorded. If that branch
  is the amber semantic, the legality is the pill's own text: the amber semantic
  is also a format's identification colour, and the standing mitigation is that
  *anything amber carries text*. The pill carries the word. If the branch is the
  raw yellow kept as a derogation, change nothing here and leave the constant as
  it is — the derogation entry ships in plan 42-11.
- **The violet sites** — the guest list counter here, and the *undone at the door*
  pill — take what the mapping assigned. **None of them takes the completion
  semantic**, which now means *already recorded* on this surface; a category
  badge that reads as an outcome is the amber-versus-yellow defect moved to a new
  pair, and the measured distance is in the mapping beside the decision.
- **The white ink on the accent fill** takes the ground token. A fill made of a
  brand or semantic colour carries the ground as its ink — never the primary ink,
  never white.

Assert what did not move, as part of the work rather than after it: the queue's
five pills still carry their five distinct sentences, the failure list still
distinguishes its causes, and the end-of-night notice still says what it says.
**Distinct causes stay distinct.** This project has a recorded defect where a
form collapsed every error into one message and made itself undebuggable for both
the user and the developer; a colour commit that tidied five sentences into two
would repeat it while looking harmless.
  </action>
  <verify>
    <automated>npm run build; echo "build=$?"; node scripts/verify-scan-legibility.mjs; echo "legibility=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `node scripts/verify-scan-legibility.mjs` exits **0**, and its printed minimum is quoted in the summary alongside the value it had at the end of plan 42-06 — a drop is expected only if the pill moved, and it must stay above the threshold
    - Counting raw palette utilities in lines 2600 to 3000 with the gate's own matcher returns only the sites the mapping marked as derogations, and the count matches the mapping's derogation rows for this region exactly
    - A fresh baseline capture diffed against `42-BASELINE.md` shows no change in blocks 4, 7, 10, 11 and 13
    - The five queue pills still carry five distinct sentences — assert with a grep listing them, quoted in the summary
  </acceptance_criteria>
  <done>The half of the door a person glances at while deciding whether to trust a verdict is on the vocabulary, and the sentences beside those colours are untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The scanning body, and the five colours chosen by an expression</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `.planning/phases/42-scanner-conversion/42-MAPPING.md` — the rows for this region, **including the ten rows that are the two branches of the five expression-chosen sites**
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:3040-3110` — the search field, its clear control and the three filter tabs
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:3110-3230` — the camera error panel, the freshness band, the stale-cache notice, the viewfinder card and the torch control
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:3250-3430` — the scan history rows, whose three outcome glyphs were already converted in plan 42-06 and **must not be touched again**, and the attendance list with its guest list badge and its arrived marker
    - `.planning/phases/42-scanner-conversion/42-RESEARCH.md` §4.3 — the five expression-chosen sites listed with both branches, and why a string search catches them while a reader does not
    - `.planning/phases/42-scanner-conversion/42-BASELINE.md` block 11 — the camera configuration, which this task passes beside and does not touch
  </read_first>
  <action>
Apply the mapping's rows for this region, in place, substitutions only.

**The five expression-chosen sites get both branches converted in the same
edit**: the freshness band's live and stale branches, the notice's error and
warning branches, the torch's on and off states, the scanner toggle's active and
inactive states, and the history row's outcome chain. Convert a ternary as a unit
— one branch converted and the other left raw is a surface that changes
vocabulary depending on a runtime condition, which is worse than either state
alone.

**Two things in this region are not colour and are not touched:**

- the camera configuration — frames per second, the decode box, the facing mode.
  The viewfinder's container is plan 42-10's and these three literals are a
  baseline block;
- the fourteen small tap targets. They are a recorded debt with a number that can
  only go down, and enlarging one here would change the layout of a surface under
  a mandate that said it would only change colour, contrast and type. Do not
  enlarge, and do not add a class that changes a hit area.

When the region is done, count what remains. Every raw palette utility still
present in this file must appear in the mapping's derogation list, and the two
counts must be equal. **A leftover is not a rounding error**: it is a colour
nobody decided, on the surface where a wrong signal happens in front of a queue.
  </action>
  <verify>
    <automated>npm run build; echo "build=$?"; node scripts/verify-scan-legibility.mjs; echo "legibility=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `node scripts/verify-scan-legibility.mjs` exits 0
    - Counting raw palette utilities across the whole file with the gate's matcher returns exactly the mapping's derogation total; the two numbers are quoted side by side in the summary
    - `LC_ALL=C /usr/bin/grep -c 'text-amber-500\|bg-amber-500' "src/app/(admin)/admin/scanner/ScannerClient.tsx"` returns **0**
    - Each of the five expression-chosen sites has both branches on the vocabulary, or both branches on a declared derogation — quote the five, with both branches, in the summary
    - `LC_ALL=C /usr/bin/grep -n 'qrbox\|fps\|facingMode' "src/app/(admin)/admin/scanner/ScannerClient.tsx"` matches `42-BASELINE.md` block 11 exactly
    - A fresh baseline capture diffed against `42-BASELINE.md` shows no change in any invariance block
    - `npm run verify:touch-targets` reports the same fourteen elements it reported before this plan — no target grew, and none was lost
  </acceptance_criteria>
  <done>Every colour on the door is either a token or a derogation with an argument, and no runtime branch changes which vocabulary the surface speaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a class-string substitution → a 3449-line safety surface | The file owns the offline queue, the torch, auto-return and undo; the edit is textual and the risk is that it stops being textual |
| distinct error causes → one sentence | Collapsing them is invisible in a diff of class names and is a recorded defect of this project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-25 | Tampering | the four message tables | mitigate | Baseline block 13 diffed as an acceptance criterion in both tasks; the five queue sentences are counted and quoted |
| T-42-26 | Spoofing | a half-converted ternary | mitigate | Both branches of each of the five expression-chosen sites are converted as a unit and quoted in the summary |
| T-42-27 | Elevation of Privilege | leftover raw utilities | mitigate | The remaining count must equal the mapping's derogation count; a leftover is treated as a colour nobody decided, not as noise |
| T-42-28 | Denial of Service | the fourteen small targets | accept | Recorded as a numbered debt that can only go down; enlarging one here would change the layout this phase promised not to move |
| T-42-SC | Tampering | npm installs | mitigate | Nothing installed |
</threat_model>

<verification>
- `npm run build` exits 0
- `node scripts/verify-scan-legibility.mjs` exits 0 with its minimum above threshold
- Raw palette remaining equals the mapping's derogation total
- A fresh baseline capture differs from `42-BASELINE.md` in no invariance block
</verification>

<success_criteria>
The door's surface speaks the product's vocabulary, the exceptions are a short
list with an argument each, and nothing about what the scanner does moved.
</success_criteria>

<output>
Create `.planning/phases/42-scanner-conversion/42-08-SUMMARY.md` when done.
</output>
