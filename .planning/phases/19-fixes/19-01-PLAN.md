---
phase: 19-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sumup.ts
  - src/types/database.ts
  - src/app/(members)/dashboard/actions.ts
  - src/app/(members)/dashboard/page.tsx
  - src/app/(members)/dashboard/SavedCardsSection.tsx
  - src/app/(organizer)/organizer/events/actions.ts
  - src/app/(public)/events/[slug]/TierSelection.tsx
  - src/app/(public)/events/[slug]/page.tsx
  - src/app/(public)/events/[slug]/menu/GuestDrinkMenu.tsx
  - src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx
  - src/app/(public)/events/[slug]/menu/page.tsx
  - src/app/api/webhooks/sumup/route.ts
  - src/app/(admin)/admin/finance/payouts/page.tsx
  - src/components/admin/PayoutList.tsx
  - src/components/admin/FinanceSubNav.tsx
  - src/app/(admin)/admin/finance/page.tsx
  - src/app/(admin)/admin/finance/actions.ts
  - supabase/migrations/20260306500000_phase18_card_tokenization.sql
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "All Phase 18 card tokenization code must be completely removed"
    - "All Phase 16 payout reports code must be completely removed"
    - "No sumup_customer_id references remain in codebase"
    - "No SavedCardsSection, savedCards props, or listSavedCards imports remain"
    - "No payouts page, PayoutList, FinanceSubNav, or listPayouts remain"
    - "Existing ticket/drink purchase flows must continue working (only card widget, no saved cards)"
    - "Finance page (transactions + refunds) must continue working without FinanceSubNav"
  artifacts:
    - path: "src/lib/sumup.ts"
      provides: "SumUp SDK helpers (checkout, refund only -- no customer/tokenization)"
      exports: ["sumup", "createCheckout", "getCheckout", "refundTransaction"]
    - path: "src/app/(admin)/admin/finance/actions.ts"
      provides: "Finance server actions (transactions + refund only -- no payouts)"
      exports: ["listTransactions", "getTransactionDetail", "refundTransactionAction"]
  key_links:
    - from: "src/app/(public)/events/[slug]/TierSelection.tsx"
      to: "src/app/(organizer)/organizer/events/actions.ts"
      via: "import { purchaseTicket }"
      pattern: "purchaseTicket (kept), purchaseTicketWithSavedCard (removed)"
---

<objective>
Remove all card tokenization (Phase 18) and payout reports (Phase 16) code from the codebase. These features are not needed: card tokenization adds unnecessary complexity, and payout reports duplicate what SumUp dashboard already provides.

Purpose: Clean codebase by removing unused features, reducing maintenance burden.
Output: Leaner codebase with only essential SumUp integrations (checkout, transactions, refunds).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-fixes/19-CONTEXT.md
@src/lib/sumup.ts
@src/app/(admin)/admin/finance/actions.ts
@src/app/(admin)/admin/finance/page.tsx
@src/app/(public)/events/[slug]/page.tsx
@src/app/(public)/events/[slug]/menu/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove card tokenization from SumUp library, types, and migration</name>
  <files>src/lib/sumup.ts, src/types/database.ts, supabase/migrations/20260306500000_phase18_card_tokenization.sql</files>
  <action>
**1. Remove Phase 18 section from `src/lib/sumup.ts`:**

Delete everything from line 82 to line 252 (the entire "Phase 18: Card Tokenization" section). This removes:
- `getOrCreateCustomer()`
- `createTokenizationCheckout()`
- `processWithSavedCard()`
- `listSavedCards()`
- `deactivateCard()`

Also update the comment on line 8 from "Export singleton for direct SDK access in future phases (14-18)" to "Export singleton for direct SDK access".

After removal, the file should end after `refundTransaction()` at approximately line 80.

**2. Remove `sumup_customer_id` from `src/types/database.ts`:**

Remove the `sumup_customer_id: string | null;` field from the Profile interface.

**3. Delete the migration file:**

Delete `supabase/migrations/20260306500000_phase18_card_tokenization.sql`.

Note: The column already exists in the production database. A separate DOWN migration should be created if needed, but for now we just remove the migration file and ignore the column (it's nullable, harmless if left in DB).
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && grep -r "sumup_customer_id\|getOrCreateCustomer\|createTokenizationCheckout\|processWithSavedCard\|listSavedCards\|deactivateCard" src/lib/sumup.ts src/types/database.ts 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <done>
    - `src/lib/sumup.ts` exports only: sumup, createCheckout, getCheckout, refundTransaction
    - No Phase 18 functions remain
    - `sumup_customer_id` removed from Profile type
    - Migration file deleted
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove card tokenization from server actions</name>
  <files>src/app/(members)/dashboard/actions.ts, src/app/(organizer)/organizer/events/actions.ts</files>
  <action>
**1. Clean `src/app/(members)/dashboard/actions.ts`:**

Remove these functions entirely:
- `getSavedCards()` (fetches saved cards for authenticated member)
- `deleteSavedCard()` (deletes a saved card token)
- `payWithSavedCard()` (creates checkout and processes with saved card)

Also remove the imports they use from `@/lib/sumup` (e.g., `listSavedCards`, `deactivateCard`, `processWithSavedCard`, `getOrCreateCustomer`, `createCheckout`).

If the file becomes empty after removing these functions, delete it entirely. If other functions remain (check carefully), keep the file with remaining functions only.

**2. Clean `src/app/(organizer)/organizer/events/actions.ts`:**

Remove these imports from line 7:
- `getOrCreateCustomer`
- `createTokenizationCheckout`
- `processWithSavedCard`

Remove the entire "Phase 18 - Saved Card Payment" section (approximately lines 1123-1513):
- `purchaseTicketWithSavedCard()`
- `purchaseDrinksWithSavedCard()`

Keep all other functions in this file intact (purchaseTicket, purchaseDrinks, getDrinkItems, etc.).
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && grep -rn "SavedCard\|savedCard\|WithSavedCard\|getOrCreateCustomer\|createTokenizationCheckout\|processWithSavedCard" src/app/(members)/dashboard/actions.ts src/app/(organizer)/organizer/events/actions.ts 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <done>
    - Dashboard actions cleaned of all saved card functions
    - Organizer actions cleaned of purchaseTicketWithSavedCard and purchaseDrinksWithSavedCard
    - Tokenization imports removed from organizer actions
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove card tokenization from UI components and pages</name>
  <files>src/app/(members)/dashboard/SavedCardsSection.tsx, src/app/(members)/dashboard/page.tsx, src/app/(public)/events/[slug]/TierSelection.tsx, src/app/(public)/events/[slug]/page.tsx, src/app/(public)/events/[slug]/menu/GuestDrinkMenu.tsx, src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx, src/app/(public)/events/[slug]/menu/page.tsx, src/app/api/webhooks/sumup/route.ts</files>
  <action>
**1. Delete `src/app/(members)/dashboard/SavedCardsSection.tsx`** (entire file).

**2. Clean `src/app/(members)/dashboard/page.tsx`:**
- Remove `import { listSavedCards } from "@/lib/sumup"` (line 14)
- Remove the `sumup_customer_id` fetch from the profile select query (around line 29)
- Remove the saved cards fetching block (lines 170-175 approximately)
- Remove `<SavedCardsSection initialCards={savedCards} />` rendering (line 349)
- Remove the SavedCardsSection import

**3. Clean `src/app/(public)/events/[slug]/TierSelection.tsx`:**
- Remove `import { purchaseTicketWithSavedCard } from ...` (line 4)
- Remove `savedCards` from props interface and destructuring
- Remove the payment method state (`paymentMethod`, `selectedCardToken`)
- Remove the payment method selector UI (radio buttons for saved cards vs new card, approximately lines 281-330)
- Remove the "Save card for future payments" checkbox (approximately lines 320-330)
- Remove the `handleSavedCardCheckout` logic (or any code path that calls purchaseTicketWithSavedCard)
- Keep the existing card widget checkout flow intact

**4. Clean `src/app/(public)/events/[slug]/page.tsx`:**
- Remove `import { listSavedCards } from "@/lib/sumup"` (line 17)
- Remove the entire "Fetch saved cards for authenticated members" block (lines 375-390)
- Remove `savedCards` prop from both TierSelection renderings (lines 523 and 671)

**5. Clean `src/app/(public)/events/[slug]/menu/GuestDrinkMenu.tsx`:**
- Remove `import { purchaseDrinksWithSavedCard } from ...` (line 6)
- Remove `savedCards` from props interface and destructuring
- Remove payment method state and `handleSavedCardCheckout` function
- Remove payment method selector UI for saved cards
- Keep existing card widget checkout flow intact

**6. Clean `src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx`:**
- Remove `savedCards` prop from the GuestDrinkMenu rendering (line 88)
- Remove `savedCards` from PartyDrinkMenu props interface if present

**7. Clean `src/app/(public)/events/[slug]/menu/page.tsx`:**
- Remove `import { listSavedCards } from "@/lib/sumup"` (line 6)
- Remove the entire "Fetch saved cards for authenticated members" block (lines 100-115)
- Remove `savedCards` prop from PartyDrinkMenu rendering (line 174)

**8. Clean `src/app/api/webhooks/sumup/route.ts`:**
- Remove any comments about tokenization checkouts (lines 231-237 approximately)
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && grep -rn "savedCards\|SavedCards\|listSavedCards\|sumup_customer_id\|savedCard\|WithSavedCard\|save.card\|SETUP_RECURRING" src/app/ src/components/ 2>/dev/null | grep -v node_modules | grep -v ".planning"; echo "Exit: $?"</automated>
  </verify>
  <done>
    - SavedCardsSection deleted
    - Dashboard page no longer fetches or renders saved cards
    - TierSelection uses only card widget (no saved card payment method)
    - Event page no longer fetches saved cards
    - GuestDrinkMenu uses only card widget (no saved card payment method)
    - PartyDrinkMenu no longer passes savedCards
    - Menu page no longer fetches saved cards
    - Webhook handler cleaned of tokenization comments
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove payout reports (page, components, actions)</name>
  <files>src/app/(admin)/admin/finance/payouts/page.tsx, src/components/admin/PayoutList.tsx, src/components/admin/FinanceSubNav.tsx, src/app/(admin)/admin/finance/page.tsx, src/app/(admin)/admin/finance/actions.ts</files>
  <action>
**1. Delete entire files:**
- `src/app/(admin)/admin/finance/payouts/page.tsx` (and remove the `payouts/` directory)
- `src/components/admin/PayoutList.tsx`
- `src/components/admin/FinanceSubNav.tsx`

**2. Clean `src/app/(admin)/admin/finance/page.tsx`:**
- Remove `import FinanceSubNav from "@/components/admin/FinanceSubNav"` (line 6)
- Remove `<FinanceSubNav />` rendering (line 27)
- Keep everything else (TransactionList, role checks, page title)

**3. Clean `src/app/(admin)/admin/finance/actions.ts`:**
- Remove the entire `listPayouts` function (lines 91-109)
- Keep: `listTransactions`, `getTransactionDetail`, `refundTransactionAction`
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && ls src/app/\(admin\)/admin/finance/payouts/ 2>/dev/null; ls src/components/admin/PayoutList.tsx 2>/dev/null; ls src/components/admin/FinanceSubNav.tsx 2>/dev/null; grep -rn "FinanceSubNav\|listPayouts\|PayoutList" src/ 2>/dev/null | grep -v node_modules; echo "Exit: $?"</automated>
  </verify>
  <done>
    - Payouts page and directory deleted
    - PayoutList component deleted
    - FinanceSubNav component deleted
    - Finance page no longer renders FinanceSubNav
    - listPayouts action removed
    - Finance page works standalone with TransactionList only
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes -- no TypeScript errors
2. `npm run build` passes -- all routes compile
3. No references to card tokenization remain: `grep -r "savedCards\|SavedCards\|listSavedCards\|sumup_customer_id\|WithSavedCard\|tokenization\|SETUP_RECURRING" src/` returns nothing
4. No references to payout reports remain: `grep -r "FinanceSubNav\|PayoutList\|listPayouts\|payouts" src/` returns nothing
5. Finance page still renders TransactionList with working refund flow
6. Ticket purchase via card widget still works (TierSelection)
7. Drink purchase via card widget still works (GuestDrinkMenu)
</verification>

<success_criteria>
- FIX-01: All card tokenization code removed (sumup helpers, server actions, UI components, migration, type fields)
- FIX-02: All payout reports code removed (page, components, actions, navigation)
- Existing payment flows (card widget only) continue working unchanged
- Finance page (transactions + refunds) continues working without sub-navigation
</success_criteria>

<output>
After completion, create `.planning/phases/19-fixes/19-01-SUMMARY.md`
</output>
