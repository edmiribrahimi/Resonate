---
phase: 07-event-media
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(organizer)/organizer/events/[id]/media/page.tsx
  - src/app/(admin)/admin/events/[id]/media/page.tsx
  - src/app/(members)/dashboard/page.tsx
  - src/components/events/EventList.tsx
autonomous: true
requirements: [MDIA-03, MDIA-04, MDIA-06]

must_haves:
  truths:
    - "Organizer can see pending media uploads for their events"
    - "Organizer can approve or reject each media item"
    - "Master admin can also moderate media for any event"
    - "Approved media is then visible in the event gallery"
    - "Member dashboard shows their uploaded media grouped by event"
    - "Profile media shows event name and date as section headers"
    - "Members can delete their own uploads from the profile section"
  artifacts:
    - path: "src/app/(organizer)/organizer/events/[id]/media/page.tsx"
      provides: "Organizer media review page with approve/reject actions"
      min_lines: 40
    - path: "src/app/(admin)/admin/events/[id]/media/page.tsx"
      provides: "Admin media review page (same functionality as organizer)"
      min_lines: 30
    - path: "src/app/(members)/dashboard/page.tsx"
      provides: "My Media section showing member's uploads grouped by event"
      contains: "My Media"
  key_links:
    - from: "src/app/(organizer)/organizer/events/[id]/media/page.tsx"
      to: "updateMediaStatus server action"
      via: "import from event detail actions"
      pattern: "updateMediaStatus"
    - from: "src/app/(members)/dashboard/page.tsx"
      to: "event_media table"
      via: "supabase query filtered by uploaded_by"
      pattern: "from.*event_media.*uploaded_by"
    - from: "src/components/events/EventList.tsx"
      to: "organizer media review page"
      via: "Media link in event list"
      pattern: "media"
---

<objective>
Build the organizer moderation workflow and member profile media section. Organizers review and approve/reject pending uploads. Members see their own uploads on their dashboard grouped by event, with delete capability.

Purpose: Completes the media lifecycle -- upload (Plan 02) -> moderation (this plan) -> display (Plans 02+03). Also fulfills MDIA-04 (media on member profile page).
Output: Organizer review pages, admin review pages, member dashboard "My Media" section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-event-media/07-CONTEXT.md
@.planning/phases/07-event-media/07-RESEARCH.md
@.planning/phases/07-event-media/07-01-SUMMARY.md
@src/app/(organizer)/organizer/events/[id]/page.tsx
@src/app/(organizer)/organizer/events/page.tsx
@src/app/(admin)/admin/events/page.tsx
@src/app/(members)/dashboard/page.tsx
@src/components/events/EventList.tsx
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Organizer and admin media review pages</name>
  <files>
    src/app/(organizer)/organizer/events/[id]/media/page.tsx
    src/app/(admin)/admin/events/[id]/media/page.tsx
    src/components/events/EventList.tsx
  </files>
  <action>
Create media review pages for organizers and admin, plus add a "Media" link to the event list.

**Organizer media review page** (`src/app/(organizer)/organizer/events/[id]/media/page.tsx`):
- Server component that fetches pending media for the event
- Query: `event_media` where `event_id = params.id` and `status = 'pending'`, ordered by `created_at asc`, join with `profiles(full_name)` on `uploaded_by` to show uploader name
- Also show counts: N pending, N approved, N rejected (query all statuses with count)
- Display pending media in a grid using `<MediaGrid>` component from `src/components/media/MediaGrid.tsx`
- Each media item shows: thumbnail, uploader name, upload date, file size
- Each item has two action buttons: Approve (green) and Reject (red)
- Approve/reject calls `updateMediaStatus(mediaId, 'approved'|'rejected')` server action imported from `@/app/(public)/events/[slug]/actions` -- NOTE: since this page uses event `id` not `slug`, you may need to adjust the import path or create a shared actions module. Check how the existing organizer event pages import actions.
- Use a client component wrapper for the interactive parts (approve/reject buttons with loading state)
- Show event title as page heading (fetch event by id)
- Back link to organizer events page
- Empty state: "No pending media to review" with count of total approved media

**Admin media review page** (`src/app/(admin)/admin/events/[id]/media/page.tsx`):
- Same functionality as organizer page. Can share the same client component for the review grid.
- Query same data. Admin can moderate any event's media.
- Back link to admin events page instead of organizer page

**EventList "Media" link:**
- Read the existing `EventList.tsx` component (used on both organizer and admin event pages)
- Add a "Media" link/button next to existing action links (Edit, Manage Tickets, Sales)
- Link format: `{basePath}/{event.id}/media` following the same pattern as existing links
- Show pending media count badge on the link if there are pending items (optional -- if the data is available from the event query, show it; if not, just show the link without count to avoid an N+1 query)
- The `basePath` prop already controls whether links go to `/organizer/events` or `/admin/events`

IMPORTANT: Read the existing EventList component carefully to understand its props, the basePath pattern (there was a specific decision in Phase 6 about basePath scoping), and how links are structured. The Media link should follow the same conventions.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Navigate to organizer events -> click Media link -> see pending media with approve/reject buttons</manual>
  </verify>
  <done>Organizer and admin media review pages created with pending media grid, approve/reject actions, and counts. EventList has "Media" link with basePath support. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Member dashboard "My Media" section</name>
  <files>
    src/app/(members)/dashboard/page.tsx
  </files>
  <action>
Add a "My Media" section to the member dashboard page showing the member's uploaded media grouped by event.

Read the existing dashboard page (`src/app/(members)/dashboard/page.tsx`) first to understand its structure, existing sections (greeting, membership card, referral link, My Tickets, etc.), and data fetching patterns.

**Add "My Media" section:**

1. **Query member's media** in the server component:
   ```
   const { data: myMedia } = await supabase
     .from('event_media')
     .select('id, url, type, status, file_size, created_at, events(id, title, date, slug)')
     .eq('uploaded_by', user.id)
     .order('created_at', { ascending: false });
   ```
   Note: The join syntax depends on the foreign key relationship. If `event_media.event_id` references `events.id`, use `events(...)` or `events!event_media_event_id_fkey(...)`. Check the actual table/FK names.

2. **Group media by event** in the server component or a client component:
   - Create a Map or object grouping media items by `event_id`
   - Each group has: event title, event date, list of media items

3. **Render grouped sections:**
   - Section heading: "My Media" (consistent heading style with other dashboard sections like "My Tickets")
   - For each event group: subheading with event name + date (e.g., "Event Title -- Feb 25, 2026"), link to event page
   - Under each event heading: small grid of thumbnails using `<MediaGrid>` component
   - Each item shows a status badge: "Pending" (yellow), "Approved" (green), "Rejected" (red)
   - Each item has a delete button (trash icon) that calls `deleteMedia(mediaId)` server action
   - Delete confirmation via `window.confirm()` (consistent with Phase 5 pattern for event deletion)

4. **Create a `MyMediaSection` client component** (co-located or inline):
   - Receives the grouped media data from server
   - Handles delete action with loading state
   - Shows lightbox on thumbnail click (reuse `<Lightbox>` component)
   - On delete, call `deleteMedia` then `router.refresh()` to update

5. **Empty state:** "You haven't uploaded any media yet. Attend an event and share your photos!"

6. **Place the section** after the existing "My Tickets" section (or wherever makes sense in the dashboard flow).

IMPORTANT: Members can delete their own uploads regardless of status (pending, approved, rejected). The `deleteMedia` server action handles both the storage file and the database record.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visit dashboard page to see "My Media" section with grouped media and delete buttons</manual>
  </verify>
  <done>Dashboard page has "My Media" section showing member's uploads grouped by event with event name/date headings, status badges, delete buttons, and lightbox viewing. Empty state displayed when no media. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Organizer review page shows pending media with approve/reject
- Admin review page provides same moderation capability
- EventList has Media link for organizer and admin views
- Dashboard shows member's media grouped by event
- Delete works for own uploads
</verification>

<success_criteria>
- Organizer can review and approve/reject pending media uploads
- Admin/master has same moderation capability for any event
- EventList shows "Media" link to review page
- Member dashboard shows "My Media" section grouped by event (MDIA-04)
- Status badges show pending/approved/rejected state
- Members can delete their own uploads
- Approved media becomes visible in event gallery after moderation
</success_criteria>

<output>
After completion, create `.planning/phases/07-event-media/07-03-SUMMARY.md`
</output>
