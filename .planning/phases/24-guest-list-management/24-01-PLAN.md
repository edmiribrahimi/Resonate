---
phase: 24-guest-list-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260310000000_guest_list.sql
  - src/types/database.ts
  - src/app/(public)/tickets/refund-actions.ts
  - src/app/(organizer)/organizer/events/[id]/sales/page.tsx
autonomous: true
requirements: [GSTL-09, GSTL-14, GSTL-15, GSTL-16]

must_haves:
  truths:
    - "profiles table has approved_via column tracking referral/guest_list/admin_manual"
    - "tickets table has ticket_type column distinguishing purchased vs guest_list"
    - "tickets.tier_id is nullable for guest list tickets"
    - "guest_list_entries table exists with proper schema, indexes, and RLS"
    - "handle_new_user() trigger checks guest_list_entries for email match and auto-approves"
    - "Refund flow guards against free ticket refunds (amount_paid=0 or ticket_type=guest_list)"
    - "Sales dashboard distinguishes paid vs free tickets"
  artifacts:
    - path: "supabase/migrations/20260310000000_guest_list.sql"
      provides: "Schema migration: guest_list_entries table, approved_via, ticket_type, tier_id nullable, updated trigger"
      min_lines: 80
    - path: "src/types/database.ts"
      provides: "GuestListEntry interface + updated Profile (approved_via) + updated Ticket (ticket_type)"
      contains: "GuestListEntry"
    - path: "src/app/(public)/tickets/refund-actions.ts"
      provides: "Guard against refunding free tickets"
      contains: "guest_list"
    - path: "src/app/(organizer)/organizer/events/[id]/sales/page.tsx"
      provides: "Paid vs free ticket separation in sales view"
      contains: "ticket_type"
  key_links:
    - from: "supabase/migrations/20260310000000_guest_list.sql"
      to: "src/types/database.ts"
      via: "TypeScript interfaces match DB schema"
      pattern: "GuestListEntry|approved_via|ticket_type"
    - from: "handle_new_user() trigger"
      to: "guest_list_entries table"
      via: "Email match lookup for auto-approval"
      pattern: "guest_list_entries.*email"
---

<objective>
Database foundation for guest list management -- schema migration, trigger update, type definitions, and guards on existing systems (refund flow, sales dashboard).

Purpose: All subsequent guest list features depend on the database schema, updated trigger, and TypeScript types. Existing systems (refunds, sales) must handle the new ticket_type and free tickets gracefully. This MUST complete before any application code.

Output: Migration SQL, updated types, guarded refund flow, updated sales dashboard
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-guest-list-management/24-RESEARCH.md

<interfaces>
<!-- Existing trigger from supabase/migrations/20260225000000_phase3_referral.sql -->
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger AS $$
DECLARE
  new_code text;
  chars text := 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
  ref_code text;
  referrer_id uuid;
  new_status text;
BEGIN
  new_code := 'RSN-';
  FOR i IN 1..8 LOOP
    new_code := new_code || substr(chars, floor(random() * length(chars) + 1)::int, 1);
  END LOOP;
  ref_code := new.raw_user_meta_data->>'referral_code';
  IF ref_code IS NOT NULL AND ref_code <> '' THEN
    SELECT id INTO referrer_id
    FROM public.profiles WHERE membership_code = ref_code AND status = 'approved';
  END IF;
  IF referrer_id IS NOT NULL THEN
    new_status := 'approved';
  ELSE
    new_status := 'pending';
  END IF;
  INSERT INTO public.profiles (id, email, full_name, membership_code, role, status, referred_by)
  VALUES (new.id, new.email, coalesce(new.raw_user_meta_data->>'full_name', ''), new_code, 'member', new_status, referrer_id);
  RETURN new;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

<!-- Existing Ticket interface from src/types/database.ts -->
export interface Ticket {
  id: string;
  event_id: string;
  party_id: string | null;
  tier_id: string;
  user_id: string;
  sumup_checkout_id: string | null;
  sumup_transaction_code: string | null;
  amount_paid: number;
  reminder_sent: boolean;
  checked_in: boolean;
  checked_in_at: string | null;
  checked_in_by: string | null;
  created_at: string;
}

<!-- Existing Profile interface from src/types/database.ts -->
export interface Profile {
  id: string;
  email: string;
  full_name: string;
  membership_code: string;
  role: UserRole;
  status: UserStatus;
  referred_by: string | null;
  created_at: string;
  updated_at: string;
}

<!-- Refund guard targets in src/app/(public)/tickets/refund-actions.ts -->
- approveRefund (line 75): checks ticket.sumup_transaction_code before SumUp refund
- adminRefund (line 299): checks ticket.sumup_transaction_code before SumUp refund
Both need early guard: if amount_paid === 0 or ticket_type === 'guest_list', skip SumUp and handle gracefully

<!-- Sales page in src/app/(organizer)/organizer/events/[id]/sales/page.tsx -->
- Fetches tiers and counts sold tickets per tier
- Needs to show free guest list tickets separately from paid tickets
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration -- guest_list_entries table, schema changes, trigger update</name>
  <files>supabase/migrations/20260310000000_guest_list.sql</files>
  <action>
Create the migration SQL file with the following changes (run in Supabase SQL Editor as a single transaction):

1. **Add approved_via to profiles:**
   - `ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS approved_via text CHECK (approved_via IN ('referral', 'guest_list', 'admin_manual'));`
   - Backfill existing approved users: those with `referred_by IS NOT NULL` get `'referral'`, others get `'admin_manual'`

2. **Add ticket_type to tickets:**
   - `ALTER TABLE public.tickets ADD COLUMN IF NOT EXISTS ticket_type text NOT NULL DEFAULT 'purchased' CHECK (ticket_type IN ('purchased', 'guest_list'));`

3. **Make tier_id nullable on tickets** (guest list tickets have no tier):
   - `ALTER TABLE public.tickets ALTER COLUMN tier_id DROP NOT NULL;`

4. **Create guest_list_entries table:**
   - `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`
   - `event_id uuid NOT NULL REFERENCES public.events ON DELETE CASCADE`
   - `party_id uuid REFERENCES public.event_parties ON DELETE CASCADE` (nullable: NULL = all parties)
   - `first_name text NOT NULL`
   - `last_name text NOT NULL`
   - `email text` (nullable: guests without email are door check-in only)
   - `added_by uuid NOT NULL REFERENCES public.profiles(id)`
   - `status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'invited', 'registered', 'ticket_issued', 'checked_in', 'already_has_ticket', 'failed'))`
   - `profile_id uuid REFERENCES public.profiles(id)` (populated when user is registered/matched)
   - `ticket_id uuid REFERENCES public.tickets(id)` (populated when free ticket is created)
   - `error_message text` (for failed processing)
   - `created_at timestamptz DEFAULT now()`
   - `updated_at timestamptz DEFAULT now()`

5. **Unique constraint:** `CREATE UNIQUE INDEX guest_list_entries_event_email_unique ON public.guest_list_entries (event_id, LOWER(email)) WHERE email IS NOT NULL;`

6. **Indexes:**
   - `idx_guest_list_event_id ON guest_list_entries (event_id)`
   - `idx_guest_list_email ON guest_list_entries (LOWER(email)) WHERE email IS NOT NULL`
   - `idx_guest_list_status ON guest_list_entries (status)`

7. **RLS policies** (same pattern as other admin tables):
   - SELECT: `(SELECT public.is_admin_or_organizer())`
   - INSERT: `(SELECT public.is_admin_or_organizer())`
   - UPDATE: `(SELECT public.is_admin_or_organizer())`
   - DELETE: `(SELECT public.is_admin_or_organizer())`

8. **Update handle_new_user() trigger** to:
   - Check `raw_user_meta_data->>'guest_list_event_id'` -- if present, set `status='approved'`, `approved_via='guest_list'`
   - Check `guest_list_entries` for email match (organic registration with guest-list email) -- if found, set `status='approved'`, `approved_via='guest_list'`, update the guest_list_entry `status='registered'` and `profile_id=new.id`
   - Fall through to existing referral check logic
   - Add `approved_via` to the INSERT INTO profiles statement (use the new column)

Wrap entire migration in BEGIN/COMMIT. Use the exact trigger code from the RESEARCH.md Pattern 3 as reference, adapting to include the `approved_via` column in the INSERT.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && cat supabase/migrations/20260310000000_guest_list.sql | head -5 && echo "---" && grep -c "CREATE TABLE\|ALTER TABLE\|CREATE.*INDEX\|CREATE POLICY\|CREATE OR REPLACE FUNCTION" supabase/migrations/20260310000000_guest_list.sql</automated>
  </verify>
  <done>Migration file exists with: guest_list_entries table, approved_via column, ticket_type column, tier_id nullable, updated handle_new_user trigger with guest_list logic, RLS policies, indexes, unique constraint</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript types, refund guards, and sales dashboard update</name>
  <files>src/types/database.ts, src/app/(public)/tickets/refund-actions.ts, src/app/(organizer)/organizer/events/[id]/sales/page.tsx</files>
  <action>
**1. Update src/types/database.ts:**
- Add `approved_via: 'referral' | 'guest_list' | 'admin_manual' | null;` to `Profile` interface
- Update `Ticket` interface: change `tier_id: string` to `tier_id: string | null`, add `ticket_type: 'purchased' | 'guest_list';`
- Add new `GuestListEntry` interface:
```typescript
export type GuestListStatus = 'pending' | 'invited' | 'registered' | 'ticket_issued' | 'checked_in' | 'already_has_ticket' | 'failed';

export interface GuestListEntry {
  id: string;
  event_id: string;
  party_id: string | null;
  first_name: string;
  last_name: string;
  email: string | null;
  added_by: string;
  status: GuestListStatus;
  profile_id: string | null;
  ticket_id: string | null;
  error_message: string | null;
  created_at: string;
  updated_at: string;
}
```

**2. Update src/app/(public)/tickets/refund-actions.ts:**
- In `approveRefund` function (line ~75): after fetching the ticket (line ~115-119), add a guard BEFORE the SumUp refund block: `if (ticket.amount_paid === 0) { /* skip SumUp refund, just update records and delete ticket */ }`. The amount check is already in the data -- if amount_paid is 0, skip calling `refundTransaction()`, set sumupStatus to null, and proceed to update refund record + delete ticket.
- In `adminRefund` function (line ~299): after fetching the ticket (line ~324-328), add an early guard: `if (ticket.amount_paid === 0) { throw new Error("This is a complimentary ticket -- no refund needed"); }`. Guest list tickets should not have the refund button at all (UI will handle), but this is a safety net.
- Add `ticket_type` to the select query in adminRefund so the type is available for the guard.

**3. Update src/app/(organizer)/organizer/events/[id]/sales/page.tsx:**
- In the buyers query (line ~72-78), add `ticket_type` to the select
- After computing tier sales data and before rendering, add a section that counts guest list tickets (where ticket_type = 'guest_list') separately:
  - Query count of tickets with `ticket_type = 'guest_list'` and `event_id = eventId`
  - Pass `guestListCount` as a prop to `SalesDashboard` OR render a simple info box above/below the SalesDashboard showing "Guest List: {N} free tickets issued"
- Since SalesDashboard is a separate component, the simplest approach is to add a small section directly in the page.tsx BEFORE the SalesDashboard component:
```tsx
{guestListCount > 0 && (
  <div className="mb-4 rounded-xl border border-white/10 bg-white/5 p-4">
    <p className="text-sm text-muted">Guest List</p>
    <p className="text-lg font-bold">{guestListCount} free ticket{guestListCount !== 1 ? 's' : ''}</p>
  </div>
)}
```
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>GuestListEntry type exported, Profile has approved_via, Ticket has ticket_type and nullable tier_id, refund flow guards against free tickets, sales page shows guest list count separately from paid sales</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- `src/types/database.ts` exports `GuestListEntry` and `GuestListStatus`
- `Profile` interface includes `approved_via`
- `Ticket` interface includes `ticket_type` and `tier_id: string | null`
- Migration SQL contains all schema changes + trigger update
- Refund actions guard against `amount_paid === 0`
- Sales page queries and displays guest list ticket count
</verification>

<success_criteria>
- Database migration file ready to execute in Supabase SQL Editor
- TypeScript types match the new schema exactly
- Refund flow will not crash on guest list tickets
- Sales page separates paid vs free ticket counts
- `npm run build` green
</success_criteria>

<output>
After completion, create `.planning/phases/24-guest-list-management/24-01-SUMMARY.md`
</output>
