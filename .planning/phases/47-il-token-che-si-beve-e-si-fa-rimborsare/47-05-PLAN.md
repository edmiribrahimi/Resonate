---
phase: 47-il-token-che-si-beve-e-si-fa-rimborsare
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/events/[slug]/menu/GuestTokenDisplay.tsx
  - src/app/(public)/events/[slug]/RedeemConfirmationModal.tsx
  - src/app/(members)/dashboard/DashboardDrinkTokens.tsx
autonomous: false
requirements: [DRK-06, DRK-07]

must_haves:
  truths:
    - "La schermata SERVED resta in vista abbastanza da essere letta al tocco"
    - "SERVED non puo' comparire senza che il server abbia cambiato stato — e la ragione e' scritta accanto al codice che lo garantisce"
    - "Nessuna transizione di stato del token diventa ottimistica"
  artifacts:
    - path: "src/app/(public)/events/[slug]/menu/GuestTokenDisplay.tsx"
      provides: "il congedo a 5 secondi e il commento che dichiara l'invariante"
      contains: "5000"
---

<objective>
Dare al barista il tempo di leggere la conferma, e **scrivere accanto al codice
perche' quella conferma vale qualcosa**.

Purpose: la procedura del banco — *si tocca, si legge SERVED, poi si versa* — e'
cio' che previene il ciclo del difetto di questa fase. Regge su una sola
proprieta' del codice: `setPhase("served")` sta **dopo** l'`await`, quindi quella
schermata non puo' comparire senza che il server abbia cambiato stato. La
proprieta' e' vera oggi **per costruzione, non per intenzione dichiarata** — e
una proprieta' non dichiarata e' una proprieta' che qualcuno ottimizza via.

Output: 5 secondi invece di 3, e l'invariante scritta dove sta il codice che la
regge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/47-il-token-che-si-beve-e-si-fa-rimborsare/47-CONTEXT.md
@.claude/rules/ticketing-payments.md
@.claude/rules/meta-gates.md
</context>

<scope_limits>
- **Il numero e' 5 secondi, non «piu' a lungo».** Una prima stesura della fase
  chiedeva che la schermata non si chiudesse affatto, temendo un barista che
  preme, si gira e torna a conferma scaduta. Quel timore descrive un ordine
  diverso — *premi, versa, poi verifica* — che **non e' quello in uso**. Il
  proprietario ha dichiarato la sequenza il 2026-08-19: la lettura avviene al
  tocco. **Non reintrodurre il congedo manuale.**
- **Nessun altro comportamento della modale cambia.** Non toccare il percorso di
  attivazione, quello di annullamento, ne' il testo degli errori.
</scope_limits>

<tasks>

<task type="auto">
  <name>Task 1: Cinque secondi, e l'invariante scritta</name>
  <files>src/app/(public)/events/[slug]/menu/GuestTokenDisplay.tsx</files>

  <read_first>
    - `src/app/(public)/events/[slug]/menu/GuestTokenDisplay.tsx`, il blocco
      `useEffect` sul `phase === "served"` (righe ~424-433 al 2026-08-19,
      **rimisura**) e i tre gestori `handleActivate` / `handleServe` /
      `handleCancel` (~435-476).
    - `.planning/v1.6-PHASE-47-PROBE.md`, la sezione sulle controprove.
  </read_first>

  <action>
    Porta il timeout da `3000` a `5000`.

    Sopra quel `useEffect`, scrivi **perche' quel numero esiste** — non cosa fa,
    che si legge dal codice:

    > La sequenza al banco e' *si tocca, si legge SERVED, poi si versa*: la
    > lettura avviene **al tocco**, quindi questa schermata deve sopravvivere alla
    > lettura, non alla versata. Cinque secondi sono per il barista, non per il
    > cliente. Un numero piu' basso toglie l'unico controllo che ha in mano;
    > un congedo manuale e' stato valutato e **scartato**, perche' la verifica sta
    > prima del gesto e non durante.

    Poi, sopra `handleServe`, l'invariante — ed e' la parte che conta di piu' di
    tutto questo piano:

    > **`setPhase("served")` sta DOPO l'`await`, e non e' uno stile: e' una
    > garanzia di sicurezza.** Il barista consegna il drink perche' ha visto
    > questa schermata; se comparisse prima della conferma del server, un momento
    > di rete assente produrrebbe un drink versato e un token mai riscattato —
    > cioe' esattamente il difetto che la fase 47 e' andata a chiudere, riaperto
    > da un'ottimizzazione dell'interfaccia. **Nessuna transizione di stato di
    > questo token diventa ottimistica.** Vedi `.planning/v1.6-PHASE-47-PROBE.md`.

    **Non cambiare l'ordine di nessuna istruzione.** L'unica modifica di
    comportamento in questo task e' il numero.
  </action>

  <verify>
    <automated>LC_ALL=C /usr/bin/grep -c '5000' 'src/app/(public)/events/[slug]/menu/GuestTokenDisplay.tsx'</automated>
    <automated>npm run build</automated>
  </verify>

  <acceptance_criteria>
    - il timeout della fase `served` vale `5000`
    - `3000` non compare piu' in quel `useEffect`
    - i due commenti ci sono, e il secondo nomina il referto
    - in `handleServe`, `setPhase("served")` e' ancora **dopo** l'`await`
    - `npm run build` verde
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Le altre due superfici che mostrano un token</name>
  <files>src/app/(public)/events/[slug]/RedeemConfirmationModal.tsx, src/app/(members)/dashboard/DashboardDrinkTokens.tsx</files>

  <read_first>
    - Entrambi i file, cercando qualunque congedo a tempo della conferma e
      qualunque aggiornamento di stato che preceda la risposta del server.
  </read_first>

  <action>
    Applica lo stesso numero **dove esiste lo stesso congedo**, e **solo li'**.

    Se una di queste superfici aggiorna lo stato mostrato **prima** della risposta
    del server, **non ripararlo in silenzio**: e' lo stesso difetto di
    `DRK-07` su un'altra superficie, e va **riportato nel SUMMARY** con il file e
    la riga. Ripararlo qui e' dentro perimetro; nasconderlo no.

    Se nessuna delle due ha un congedo a tempo, **dillo nel SUMMARY** invece di
    lasciare il task senza esito: un task che non trova nulla ha comunque un
    risultato, ed e' che le superfici sono due e non tre.
  </action>

  <verify>
    <automated>npm run build</automated>
    <manual>Il SUMMARY dichiara, per ciascuno dei due file, se un congedo a tempo esisteva e cosa e' stato fatto</manual>
  </verify>

  <acceptance_criteria>
    - nessuna delle due superfici mostra una conferma prima della risposta del server
    - il SUMMARY dichiara l'esito per entrambi i file, anche quando l'esito e' «non c'era niente»
    - `npm run build` verde
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 3: La prova manuale, perche' non ci sono test</name>
  <files></files>

  <action>
    Scritta passo per passo, perche' e' l'unica prova che esistera'.

    1. Compra un token da ospite su una serata di prova.
    2. Attiva. Osserva: la schermata dice attivo.
    3. Premi SERVE **e conta**: la conferma resta in vista cinque secondi.
    4. Ripeti con la **rete spenta**: la schermata **non** deve mai dire SERVED,
       e deve tornare ad `active` mostrando un errore leggibile.
    5. Riaccendi la rete, premi SERVE: ora dice SERVED.

    Il passo 4 e' quello che vale: e' la prova che l'invariante di `DRK-07` regge
    **osservata**, non dedotta.
  </action>

  <verify>
    <manual>I cinque passi eseguiti, con l'esito di ciascuno scritto nel SUMMARY</manual>
  </verify>

  <acceptance_criteria>
    - il passo 4 ha prodotto un errore visibile e **nessun** SERVED
    - i cinque passi sono nel SUMMARY con il loro esito, non evocati
    - **CHECKPOINT UMANO**: serve un dispositivo, non si chiude alla scrivania
  </acceptance_criteria>
</task>

</tasks>
