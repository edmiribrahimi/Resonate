---
phase: 01-ui-foundation-english-migration
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/(public)/events/page.tsx
  - src/app/(public)/events/[slug]/page.tsx
  - src/app/(public)/gallery/page.tsx
  - src/app/(public)/newsletter/page.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(members)/dashboard/page.tsx
  - src/app/(members)/membership-card/page.tsx
  - src/app/(members)/attendance/page.tsx
  - src/app/(admin)/admin/scanner/page.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/membership/MembershipCardView.tsx
  - src/lib/supabase/middleware.ts
  - next.config.ts
autonomous: true
requirements: [UIBR-01, UIBR-02]

must_haves:
  truths:
    - "Every page, label, error message, and placeholder displays in English with no remaining Italian text"
    - "All routes use English paths (/events, /register, /gallery, /attendance)"
    - "Old Italian paths (/eventi, /registrati, /galleria, /presenze) permanently redirect to English equivalents"
    - "Date formatting uses en-US locale on all pages (no Italian day/month names)"
    - "Internal links point to English paths (no /eventi hrefs remaining)"
    - "Middleware protects /attendance instead of /presenze"
  artifacts:
    - path: "src/app/(public)/events/page.tsx"
      provides: "English events listing page at /events"
      contains: "Events"
    - path: "src/app/(auth)/register/page.tsx"
      provides: "English registration page at /register"
      contains: "Sign Up"
    - path: "src/app/(members)/attendance/page.tsx"
      provides: "English attendance page at /attendance"
      contains: "Attendance"
    - path: "src/app/(public)/gallery/page.tsx"
      provides: "English gallery page at /gallery"
      contains: "Gallery"
    - path: "next.config.ts"
      provides: "Permanent redirects from Italian paths to English paths"
      contains: "redirects"
    - path: "src/lib/supabase/middleware.ts"
      provides: "Route protection for English path /attendance"
      contains: "/attendance"
  key_links:
    - from: "next.config.ts"
      to: "src/app/(public)/events/"
      via: "redirect from /eventi/:path* to /events/:path*"
      pattern: "eventi.*events"
    - from: "src/lib/supabase/middleware.ts"
      to: "src/app/(members)/attendance/"
      via: "memberRoutes array includes /attendance"
      pattern: "/attendance"
    - from: "src/components/layout/MobileNav.tsx"
      to: "src/app/(public)/events/"
      via: "href points to /events"
      pattern: "/events"
---

<objective>
Translate all Italian text to English across every page and component, rename route directories from Italian to English, configure permanent redirects, and update all internal links.

Purpose: Complete the English migration so no Italian text or Italian URLs remain anywhere in the application.
Output: All 14 files with Italian text fully translated, 4 route directories renamed, redirect config added, middleware updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-foundation-english-migration/01-RESEARCH.md
@.planning/phases/01-ui-foundation-english-migration/01-CONTEXT.md
@.planning/phases/01-ui-foundation-english-migration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename route directories + configure redirects + update middleware</name>
  <files>next.config.ts, src/lib/supabase/middleware.ts</files>
  <action>
1. Rename route directories (use git mv to preserve history):
   - `src/app/(public)/eventi/` -> `src/app/(public)/events/`
   - `src/app/(auth)/registrati/` -> `src/app/(auth)/register/`
   - `src/app/(members)/presenze/` -> `src/app/(members)/attendance/`
   - `src/app/(public)/galleria/` -> `src/app/(public)/gallery/`

2. In `next.config.ts`, add an `async redirects()` function to the nextConfig object:
   ```typescript
   async redirects() {
     return [
       { source: "/eventi/:path*", destination: "/events/:path*", permanent: true },
       { source: "/registrati", destination: "/register", permanent: true },
       { source: "/presenze", destination: "/attendance", permanent: true },
       { source: "/galleria", destination: "/gallery", permanent: true },
     ]
   }
   ```
   Keep all existing config (turbopack, images, PWA wrapper) unchanged.

3. In `src/lib/supabase/middleware.ts`, update the memberRoutes array:
   - Change `"/presenze"` to `"/attendance"`
   - Keep `"/dashboard"` and `"/membership-card"` unchanged
   - Keep all other middleware logic unchanged

Do NOT modify the page.tsx files in this task -- that's Task 2.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -20</automated>
    <manual>Verify directory structure: `ls src/app/(public)/events/ src/app/(auth)/register/ src/app/(members)/attendance/ src/app/(public)/gallery/` -- all should exist. Old Italian directories should NOT exist.</manual>
  </verify>
  <done>All 4 route directories renamed to English. Permanent redirects configured in next.config.ts. Middleware protects /attendance instead of /presenze. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Translate all Italian text to English in every page and component</name>
  <files>
    src/app/(public)/events/page.tsx,
    src/app/(public)/events/[slug]/page.tsx,
    src/app/(public)/gallery/page.tsx,
    src/app/(public)/newsletter/page.tsx,
    src/app/(auth)/login/page.tsx,
    src/app/(auth)/register/page.tsx,
    src/app/(members)/dashboard/page.tsx,
    src/app/(members)/membership-card/page.tsx,
    src/app/(members)/attendance/page.tsx,
    src/app/(admin)/admin/scanner/page.tsx,
    src/components/layout/MobileNav.tsx,
    src/components/membership/MembershipCardView.tsx
  </files>
  <action>
Translate ALL Italian text in each file to English. Use a casual-but-clear tone that fits a music community (per Claude's Discretion). Also update ALL internal href links to use the new English paths.

**Translation map (complete -- apply systematically file by file):**

**src/app/(auth)/login/page.tsx:**
- "Accedi" (h1) -> "Sign In"
- "Accedi alla tua area membri" -> "Access your member area"
- "Email o password non corretti" -> "Incorrect email or password"
- "Accesso..." -> "Signing in..."
- "Accedi" (button) -> "Sign In"
- "Non hai un account?" -> "Don't have an account?"
- "Registrati" (link text) -> "Sign Up"
- `href="/registrati"` -> `href="/register"`

**src/app/(auth)/register/page.tsx (renamed from registrati):**
- "Controlla la tua email" -> "Check your email"
- "Ti abbiamo inviato un link di conferma..." -> "We sent you a confirmation link. Click it to activate your account."
- "Diventa membro" -> "Become a Member"
- "Unisciti alla community Resonate" -> "Join the Resonate community"
- "Nome completo" -> "Full name"
- "Password (min 6 caratteri)" -> "Password" (remove min char hint -- UIBR-08 in Plan 03 adds real-time validation)
- "Registrazione..." -> "Signing up..."
- "Registrati" (button) -> "Sign Up"
- "Hai gia un account?" -> "Already have an account?"
- "Accedi" -> "Sign In"

**src/app/(public)/events/page.tsx (renamed from eventi):**
- "Eventi" (h1) -> "Events"
- ALL `toLocaleDateString("it-IT", ...)` -> `toLocaleDateString("en-US", ...)`

**src/app/(public)/events/[slug]/page.tsx:**
- `href="/eventi"` -> `href="/events"`
- `toLocaleDateString("it-IT", ...)` -> `toLocaleDateString("en-US", ...)`
- "Riceverai l'indirizzo 24h prima dell'evento" -> "You'll receive the address 24h before the event"
- "Diventa membro per ricevere l'indirizzo" -> "Become a member to get the address"
- "Ci saro" / "Ci saro" -> "I'm going" / "I'm going"
- "Diventa membro per confermare la presenza" -> "Sign up to confirm attendance" (for non-logged-in users)
- `href="/registrati"` (all instances) -> `href="/register"`

**src/app/(public)/gallery/page.tsx (renamed from galleria):**
- "Galleria" -> "Gallery"
- "Momenti dai nostri eventi" -> "Moments from our events"
- "Le foto e i video dei prossimi eventi appariranno qui." -> "Photos and videos from upcoming events will appear here."

**src/app/(public)/newsletter/page.tsx:**
- "Iscritto!" -> "Subscribed!"
- "Riceverai le novita sui prossimi eventi." -> "You'll receive updates about upcoming events."
- "Ricevi aggiornamenti sui prossimi eventi..." -> "Get updates about upcoming events right in your inbox."
- "La tua email" -> "Your email"
- "Qualcosa e andato storto. Riprova." -> "Something went wrong. Please try again."
- "Iscrizione..." -> "Subscribing..."
- "Iscriviti" -> "Subscribe"

**src/app/(members)/dashboard/page.tsx:**
- "Membro" (fallback name) -> "Member"
- "Ciao," -> "Hey,"
- "Visualizza la tua card" -> "View your card"
- "Le tue presenze" -> "Your attendance"
- "Storico eventi" -> "Event history"
- "Prossimi eventi confermati" -> "Upcoming confirmed events"
- "Nessun evento confermato" -> "No confirmed events"
- "Scopri gli eventi" -> "Discover events"
- `href="/presenze"` -> `href="/attendance"`
- `href="/eventi"` -> `href="/events"`

**src/app/(members)/membership-card/page.tsx:**
- "Membro" (fallback) -> "Member"
- "Come usare la card" -> "How to use your card"
- "Mostra il QR code all'ingresso dell'evento" -> "Show the QR code at the event entrance"
- "Lo staff scannerizzera il codice" -> "Staff will scan the code"
- "La tua presenza verra registrata automaticamente" -> "Your attendance will be recorded automatically"
- "Aggiungi a Apple/Google Wallet" -> "Add to Apple/Google Wallet"

**src/app/(members)/attendance/page.tsx (renamed from presenze):**
- "Le tue presenze" -> "Your Attendance"
- `{attendances.length === 1 ? "evento" : "eventi"} frequentati` -> `{attendances.length === 1 ? "event" : "events"} attended`
- "Nessuna presenza registrata ancora. Vieni al prossimo evento!" -> "No attendance recorded yet. Come to the next event!"
- `toLocaleDateString("it-IT", ...)` -> `toLocaleDateString("en-US", ...)`

**src/app/(admin)/admin/scanner/page.tsx:**
- "Scanner QR" -> "QR Scanner"
- "Presenza registrata" -> "Attendance recorded"
- "Membership non valida" -> "Invalid membership"
- "Errore di connessione" -> "Connection error"
- "Verifica in corso..." -> "Verifying..."
- "Scansiona un altro QR" -> "Scan another QR"

**src/components/layout/MobileNav.tsx:**
- "Eventi" -> "Events"
- "Galleria" -> "Gallery"
- "Area Membri" -> "Members"
- `href: "/eventi"` -> `href: "/events"`
- `href: "/galleria"` -> `href: "/gallery"`

**src/components/membership/MembershipCardView.tsx:**
- "Membro dal" -> "Member since"
- `toLocaleDateString("it-IT", ...)` -> `toLocaleDateString("en-US", ...)`

**After all translations, do a final verification sweep:**
- Grep entire `src/` for remaining `"it-IT"` -- should be ZERO matches
- Grep entire `src/` for `/eventi` href patterns -- should be ZERO matches (only in redirect config)
- Grep entire `src/` for `/registrati` href patterns -- should be ZERO matches
- Grep entire `src/` for `/presenze` href patterns -- should be ZERO matches
- Grep entire `src/` for `/galleria` href patterns -- should be ZERO matches
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -20 && echo "---GREP CHECK---" && grep -r "it-IT" src/ && echo "FAIL: Italian locale found" || echo "PASS: No Italian locale" && grep -rn "href=\"/eventi\|href=\"/registrati\|href=\"/presenze\|href=\"/galleria" src/ && echo "FAIL: Italian hrefs found" || echo "PASS: No Italian hrefs"</automated>
    <manual>Browse every page in dev mode. No Italian text should appear anywhere. Dates should show English month/day names.</manual>
  </verify>
  <done>All 12 page/component files translated to English. Zero Italian strings remain in src/. All internal hrefs point to English paths. All date formatting uses en-US locale. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep -r "it-IT" src/` returns zero matches
- `grep -rn 'href="/eventi\|href="/registrati\|href="/presenze\|href="/galleria' src/` returns zero matches
- Visiting /eventi in browser redirects to /events (308)
- Visiting /registrati redirects to /register (308)
- Visiting /presenze redirects to /attendance (308)
- Visiting /galleria redirects to /gallery (308)
- Middleware still protects /attendance (unauthenticated user redirected to /login)
</verification>

<success_criteria>
- Every user-visible string across all pages displays in English
- All 4 Italian route paths permanently redirect to English equivalents
- No Italian locale strings ("it-IT") remain in source code
- No internal links reference old Italian paths
- Middleware protects the new /attendance path
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-foundation-english-migration/01-02-SUMMARY.md`
</output>
