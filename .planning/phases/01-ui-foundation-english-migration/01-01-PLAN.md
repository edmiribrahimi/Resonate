---
phase: 01-ui-foundation-english-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - public/images/logo-white.png
  - public/manifest.json
autonomous: true
requirements: [UIBR-03, UIBR-04]

must_haves:
  truths:
    - "The Orbitron font renders as the primary typeface on every page"
    - "The homepage shows the Resonate logo image instead of a text heading"
    - "The tagline 'motion music hub' appears below the logo on the homepage"
    - "If no upcoming events exist, the homepage shows logo + tagline only (no empty state clutter)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Orbitron font via next/font/google, lang=en, CSS variable --font-orbitron"
      contains: "Orbitron"
    - path: "src/app/globals.css"
      provides: "Body font-family using Orbitron CSS variable"
      contains: "font-orbitron"
    - path: "src/app/page.tsx"
      provides: "Homepage with logo image, tagline, and optional next-event preview"
      contains: "logo-white"
    - path: "public/images/logo-white.png"
      provides: "White Resonate logo on transparent background"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-orbitron set on html element, consumed in body font-family"
      pattern: "font-orbitron"
    - from: "src/app/page.tsx"
      to: "public/images/logo-white.png"
      via: "next/image component referencing /images/logo-white.png"
      pattern: "logo-white"
---

<objective>
Set up the Resonate brand foundation: Orbitron font site-wide, logo asset, and redesigned homepage.

Purpose: Establish the visual identity (font, logo, dark landing page) that all subsequent translation and migration work builds on top of.
Output: Root layout with Orbitron font, updated globals.css, homepage with logo + tagline + event preview, logo asset in public/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-foundation-english-migration/01-RESEARCH.md
@.planning/phases/01-ui-foundation-english-migration/01-CONTEXT.md
@src/app/layout.tsx
@src/app/globals.css
@src/app/page.tsx
@public/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orbitron font setup + root layout update</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <action>
1. In `src/app/layout.tsx`:
   - Import Orbitron from `next/font/google`: `import { Orbitron } from "next/font/google"`
   - Instantiate: `const orbitron = Orbitron({ subsets: ["latin"], variable: "--font-orbitron" })`
   - Change `<html lang="it">` to `<html lang="en" className={orbitron.variable}>`
   - Keep all existing metadata, viewport, head content, body structure unchanged

2. In `src/app/globals.css`:
   - Update the body `font-family` from `system-ui, -apple-system, sans-serif` to `var(--font-orbitron), system-ui, -apple-system, sans-serif`
   - Keep all existing CSS variables (:root block) and @theme block unchanged
   - Keep the PWA safe area CSS unchanged

3. In `public/manifest.json`:
   - Update `description` to English if it contains Italian text (change to "Music events community" or similar)

No new npm packages needed. Orbitron is loaded via next/font/google (built-in).

Design decision (Claude's Discretion - Orbitron scope): Use Orbitron for ALL text site-wide. It is a geometric display font matching the electronic music aesthetic. The platform has short-form content (labels, listings, cards) where Orbitron remains readable at 14px+. If readability issues emerge, a secondary body font can be introduced later.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -20</automated>
    <manual>Run `npm run dev`, visit http://localhost:3000 -- Orbitron font should render on all text. Page should display in English lang attribute (inspect html tag).</manual>
  </verify>
  <done>Orbitron font renders on every page via CSS variable. HTML lang attribute is "en". No Italian text remains in layout.tsx or globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Logo asset + homepage redesign</name>
  <files>public/images/logo-white.png, src/app/page.tsx</files>
  <action>
1. Copy logo file to public:
   - Source: `/Users/etiesse/Documents/Resonate/Logo/Logo Sfondo Trasparente Scritta Bianca.png`
   - Destination: `public/images/logo-white.png`
   - Create `public/images/` directory if it doesn't exist

2. Redesign `src/app/page.tsx` as a dark landing page:
   - Import `Image` from `next/image` and `Link` from `next/link`
   - Import `createClient` from `@/lib/supabase/server` for the event preview query
   - Make the component async: `export default async function Home()`
   - Query next upcoming event from Supabase:
     ```
     const supabase = await createClient()
     const { data: nextEvent } = await supabase
       .from("events")
       .select("slug, title, date")
       .gte("date", new Date().toISOString().split("T")[0])
       .eq("is_published", true)
       .order("date", { ascending: true })
       .limit(1)
       .single()
     ```
   - If this query fails or returns null, that's fine -- handle gracefully (no event preview shown)
   - Layout structure (centered, full viewport height):
     - `<Image>` component with `src="/images/logo-white.png"` alt="re:sonate" width={320} height={90} priority
       (Logo is 3232x914 native, aspect ratio ~3.54:1. Display at 320x90 for mobile-friendly sizing.)
     - Below logo: `<p>` with "motion music hub" -- styled as uppercase, tracking-widest, text-sm, text-muted
     - If nextEvent exists: a `<Link>` to `/events/${nextEvent.slug}` showing event title + formatted date (en-US locale). Styled minimally -- event name in font-semibold, date in text-sm text-muted. Add subtle card-like styling (border, rounded, padding).
     - If no events: show nothing below tagline (per user decision: no empty state clutter)
     - Below event preview (or tagline if no events): two CTA buttons
       - Primary: "Discover Events" linking to `/events` (accent background, rounded-full)
       - Secondary: "Join" linking to `/register` (border outline, rounded-full)
   - Keep `<MobileNav />` at the bottom
   - Use dark theme styling throughout (bg-background or transparent, the existing :root already defines dark colors)

IMPORTANT: Link to `/events` and `/register` (English paths). These directories will be created in Plan 02. The homepage will show 404 for those links temporarily until Plan 02 renames the routes. This is acceptable -- the redirect config in Plan 02 will also catch it.

Do NOT add the `<MobileNav />` import if it would break without translation. Keep the existing MobileNav import -- it will be translated in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -20</automated>
    <manual>Run `npm run dev`, visit http://localhost:3000 -- logo image should display centered, "motion music hub" tagline below, CTA buttons visible. If no events in DB, only logo + tagline + buttons shown.</manual>
  </verify>
  <done>Homepage shows Resonate logo image (not text heading), "motion music hub" tagline, optional next-event preview, and English CTA buttons. Logo file exists at public/images/logo-white.png.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Orbitron font CSS variable is set on `<html>` element
- `<html lang="en">` in rendered output
- Homepage displays logo image, not text "Resonate"
- "motion music hub" tagline visible below logo
- No Italian text on homepage
</verification>

<success_criteria>
- Orbitron font renders as primary typeface on all pages (verified via browser dev tools font tab)
- Homepage shows the re:sonate logo image centered with tagline
- If no events in database, homepage shows logo + tagline + buttons only (no empty state)
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-foundation-english-migration/01-01-SUMMARY.md`
</output>
