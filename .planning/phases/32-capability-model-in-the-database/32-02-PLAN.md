---
phase: 32-capability-model-in-the-database
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md
autonomous: true
requirements: [CAP-03]

must_haves:
  truths:
    - "Every server-side permission decision in the application is listed once, with its file:line and its exact predicate, as it stands before the phase"
    - "The three live inconsistencies the phase must preserve are recorded verbatim, each with its two file:line citations"
    - "Anyone can re-open any row of the register and see the same predicate at the same line"
  artifacts:
    - path: ".planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md"
      provides: "B4 — the pre-phase register of every permission surface in application code"
      min_lines: 120
      contains: "src/lib/supabase/middleware.ts:82"
  key_links:
    - from: "baseline/32-BASELINE-surfaces.md"
      to: "src/lib/supabase/middleware.ts"
      via: "file:line citation of each of the four prefix rules"
      pattern: "middleware\\.ts:(82|90|99|108)"
    - from: "baseline/32-BASELINE-surfaces.md"
      to: "src/lib/rbac/roles.ts"
      via: "the NAV_ITEMS /admin/scanner entry, quoted"
      pattern: "roles\\.ts:6[4-9]"
---

<objective>
Write **B4**, the fourth baseline artefact: a hand-written register of every
server-side permission decision in application code, with `file:line` and the
**exact predicate**, as it stands before a single line of this phase's code is
written.

Purpose: B1, B2, B3 and B5 measure the database. Nothing measures the application,
and ROADMAP success criterion 1 says the comparison is made "surface by surface".
B4 is that surface list, and it is the artefact the phase gate compares against with
the predicate column required to be **character-identical**.

Output: one committed markdown file. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-capability-model-in-the-database/32-VALIDATION.md
@src/lib/supabase/middleware.ts
@src/lib/rbac/roles.ts
</context>

<phase_decisions>
- **D-09** — English.
- **D-16** — B4 records inconsistencies **verbatim and unresolved**. Three are known
  before writing (see Task 2). A register that tidies what it finds is not a
  baseline; CAP-03 requires the phase to reproduce the inconsistency, so the
  inconsistency must first be written down.
- **D-17** — `.planning/` is public: the register names **roles and files**, never a
  person, an email or a project reference.
</phase_decisions>

<tasks>

<task type="auto">
  <name>Task 1: The route surfaces — middleware, door routes, navigation</name>
  <files>.planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md</files>
  <read_first>
    - `src/lib/supabase/middleware.ts` in full — the profile read at `:44-57`, the
      four prefix rules at `:82`, `:90`, `:99`, `:108`, the redirect body repeated four
      times at `:84-87`, the header-injection block at `:120-139`.
    - `src/lib/rbac/roles.ts` in full — `NAV_ITEMS` at `:36-82` and
      `getVisibleNavItems` at `:94-129`.
    - `src/app/api/tickets/checkin/route.ts:113-148` — the comment recording why the
      door decides on role alone, and the guard itself.
    - `src/app/api/tickets/checkin/undo/route.ts:32,49`,
      `src/app/api/membership/verify/route.ts:104`,
      `src/app/api/tickets/attendance/route.ts:11,28` — the other three door guards.
    - `32-RESEARCH.md` § *(f) What the middleware does today* — the four-rule mapping
      table and the note that the `:82`/`:90` ordering is load-bearing.
    - `32-PATTERNS.md` § *`.../baseline/32-BASELINE-surfaces.md` (B4, hand-written
      artefact)* — the table shape and the five guard families already located.
  </read_first>
  <action>
Create the file with a header block modelled on `31-REFUND-PROBE.md`'s *Where it was
run*: what this artefact is, the commit it was written against (`git rev-parse
--short HEAD`), the date, and one sentence saying it is a **pre-phase** record whose
predicate column must still read identically at the phase gate.

Section 1 — the four middleware prefix rules. One row each: prefix, `file:line`,
the predicate **quoted exactly as written in the source** (`role !== "master" && role
!== "organizer"` and the rest), the action taken on failure, and the capability key
this phase will map it to (`door.operate`, `admin.access`, `organizer.access`,
`membership.card.view`). Record, in prose beneath the table, that `/admin/scanner` at
`:82` is tested **before** the general `/admin` branch at `:90` in an `if` / `else
if` pair, that `/organizer` at `:99` and the `/membership-card`/`/attendance` pair at
`:108` are two **separate** `if` statements rather than a continuation of that chain,
and that inverting the first pair locks organizers out of the door.

Record two further behaviours of that block that the rules table hides, because both
are behaviour and both must survive: the `?? "member"` and `?? "pending"` defaults at
`:55-56`, which mean an authenticated user with **no profile row** is treated as a
pending member; and the discarded error at `:49`, where `{ data: profile }` drops the
query error so that a failed read silently produces the same pending-member default.

Section 2 — the header-injection block at `:120-139`. Quote the unconditional
`delete` of the three `x-user-*` names and the conditional `set`, and state that this
block is **out of scope** (CAP-05, phase 33, 46 files) and must still read the same
at the phase gate, with `role` and `status` still reaching it as values.

Section 3 — the four door routes. One row each with `file:line` and the exact
predicate. Note that all four check role alone and that the comment at
`checkin/route.ts:113-127` states they must never diverge.

Section 4 — `NAV_ITEMS`. The five entries with their `roles` / `requireApproved` /
`requireAuth` / `hideWhenAuth` values and the filter logic at `:101-128`. State that
hiding a link is not protecting a route (`access-gating.md`, gate *coerenza
navigazione/permessi*) and that this list is **not** converted in this phase.
  </action>
  <verify>
    <automated>for r in 'middleware.ts:82' 'middleware.ts:90' 'middleware.ts:99' 'middleware.ts:108' 'middleware.ts:120' 'checkin/route.ts' 'roles.ts:36'; do grep -q "$r" .planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md || { echo "MISSING $r"; exit 1; }; done; echo "all route surfaces cited"</automated>
  </verify>
  <acceptance_criteria>
    - The four prefix rules appear with `src/lib/supabase/middleware.ts:82`, `:90`,
      `:99` and `:108`, and each predicate string in the register matches the source
      line character for character — checked by re-opening each line.
    - The `if` / `else if` ordering of `:82`/`:90` is stated in prose with the
      consequence of inverting it.
    - The `?? "member"` / `?? "pending"` defaults and the discarded error at `:49` are
      both recorded as behaviour to preserve.
    - The four door routes each carry a `file:line` and an exact predicate; all four
      read role only, and the register says so.
    - `NAV_ITEMS` is recorded with all five entries and their four flags.
  </acceptance_criteria>
  <done>Sections 1 to 4 exist, every citation re-opens to the quoted predicate, and nothing has been tidied.</done>
</task>

<task type="auto">
  <name>Task 2: The action surfaces, the role-read census, and the three inconsistencies recorded verbatim</name>
  <files>.planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md</files>
  <read_first>
    - `src/app/(organizer)/organizer/events/actions.ts:25-51` and
      `src/app/(organizer)/organizer/events/[id]/tickets/actions.ts:20` — the two
      byte-identical `verifyOrganizer` copies, differing only in the error message.
    - `src/app/(organizer)/organizer/events/[id]/guest-list/actions.ts:14` —
      `verifyOrganizerAccess`.
    - `src/app/(admin)/admin/finance/actions.ts:9-15` and
      `src/app/(admin)/admin/newsletter/actions.ts:15` — the two header-reading
      `requireMaster` copies.
    - `src/app/(admin)/admin/members/actions.ts:45,71,100` — `verifyMaster`,
      `verifyAdminOrOrganizer`, and the line where `updateMemberRole` sets
      `status = 'approved'` when it grants the organizer role.
    - `src/lib/rbac/roles.ts:64-72` and `src/lib/supabase/middleware.ts:82-88` — the
      nav-versus-middleware mismatch on `/admin/scanner`.
    - `32-RESEARCH.md` § *The five predicates that exist today* — P1 versus P3, and
      why the disagreement must be reproduced.
  </read_first>
  <action>
Section 5 — the five guard-helper families. One row per family: name, every
`file:line` it appears at, the exact predicate, the failure mode (`throw`, `redirect`,
or a returned `{ error, status }`), and whether it reads the **session** or the
**header**. Record that the two `verifyOrganizer` copies are byte-identical except
for their error message, and that deleting the duplication is phase 33's job, not
this one's.

Section 6 — the census. Four counts, each with the exact macOS/BSD command that
produced it and the number observed: files reading `x-user-` (`grep -rl 'x-user-'
src | wc -l`), `select("role"` sites, files importing `getServiceClient`, and
`redirect("/dashboard")` sites. Measured on 2026-08-06 as 46, 21, 29 and 32; record
what the command actually returns and flag any divergence rather than copying these
numbers.

Section 7 — **the three inconsistencies, recorded and not resolved.** This is the
load-bearing section of B4 and each entry carries two citations and one sentence
saying what would happen if the phase tidied it.

  1. *Two definitions of "organizer" in the database.* Thirty-four policies gate on
     `public.is_admin_or_organizer()`, which reads `role` and ignores `status`
     (`supabase/migrations/20260224_rbac_migration.sql:127-135`). Four —
     `artists_insert_organizer`, `artists_update_organizer`, `venues_insert_organizer`,
     `venues_update_organizer` — use an inline `EXISTS` requiring role **and**
     `status = 'approved'` (`supabase/migrations/20260226200000_venues.sql:29-51`,
     `20260226100000_artist_profiles.sql:30-42`). **Today a pending organizer can
     create a ticket tier but not a venue.** Collapsing these into one capability is
     this phase's single unrecoverable CAP-03 defect.

  2. *Navigation and route disagree on `/admin/scanner`.* The nav entry at
     `src/lib/rbac/roles.ts:64-72` carries `requireApproved: true`; the middleware
     rule for the same path at `src/lib/supabase/middleware.ts:82-88` checks role
     only. A pending organizer sees no link to a route that would admit them. The
     phase reproduces both sides and resolves neither.

  3. *The login redirect parameter is dead.* `src/lib/supabase/middleware.ts:75`
     writes `?redirect=`; `src/app/(auth)/login/page.tsx:11` reads `?next=`. This
     changes navigation, not access, so it does not touch CAP-03 — and fixing it
     inside a constant-behaviour phase would blur the comparison. Raised to the owner
     as its own item.

Close the file with a *How this artefact is used* block: at the phase gate the same
table is rebuilt from the post-phase code, the **call site** column may change and
the **predicate** column may not, and any predicate that has moved is a CAP-03
defect until explained.
  </action>
  <verify>
    <automated>grep -c 'file:line\|\.ts:' .planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md && grep -q 'is_admin_or_organizer' .planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md && grep -q 'requireApproved' .planning/phases/32-capability-model-in-the-database/baseline/32-BASELINE-surfaces.md && echo 'inconsistencies recorded'</automated>
  </verify>
  <acceptance_criteria>
    - All five guard families appear with every `file:line` listed in
      `32-PATTERNS.md` § *B4*, and each row states session-reading or header-reading.
    - The four census commands are written out and their observed numbers recorded;
      any divergence from 46 / 21 / 29 / 32 is flagged in the SUMMARY.
    - Section 7 holds exactly three entries, each with **two** citations and a
      consequence sentence. The P1/P3 entry names all four `artists`/`venues` policies.
    - The closing block states the predicate column must be character-identical at the
      gate.
    - `grep -rnE '@[A-Za-z0-9._-]+\.[a-z]{2,}|[0-9a-f]{8}-[0-9a-f]{4}' 32-BASELINE-surfaces.md`
      returns nothing — no email, no uuid, no project reference in a public file.
  </acceptance_criteria>
  <done>B4 is complete, every citation re-opens, and the three inconsistencies are written down without being resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → public repository | every line of this artefact is published irreversibly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Information disclosure | the committed register | mitigate | roles and file paths only; asserted by a grep for emails, uuids and project references |
| T-32-02-02 | Repudiation | a register that tidies what it finds | mitigate | section 7 records the three inconsistencies verbatim with two citations each; the acceptance criteria require exactly three entries |
| T-32-02-03 | Information disclosure | quoting a guard's full body | accept | every excerpt is code already published in this public repository; no secret is quoted |
| T-32-02-SC | Tampering | npm installs | mitigate | this plan installs nothing and writes no code |
</threat_model>

<verification>
- Every `file:line` in the register re-opens to the quoted text.
- The census commands, re-run, return the recorded numbers.
- No email, uuid or project reference in the file.
</verification>

<success_criteria>
- One file records every server-side permission decision in application code, before anything moves.
- The three inconsistencies the phase must preserve are written down, unresolved.
- The comparison rule for the phase gate is stated in the artefact itself.
</success_criteria>

<output>
Create `.planning/phases/32-capability-model-in-the-database/32-02-SUMMARY.md` when done.
</output>
