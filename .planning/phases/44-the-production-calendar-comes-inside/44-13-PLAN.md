---
phase: 44-the-production-calendar-comes-inside
plan: 13
type: execute
wave: 7
depends_on: ["44-09", "44-12"]
files_modified:
  - src/lib/routes/staff-tabs.ts
  - scripts/verify-calendar-surface.mjs
  - scripts/verify-all.mjs
  - package.json
  - .planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md
autonomous: false
requirements: [PROD-01]

must_haves:
  truths:
    - "The calendar is reachable from the staff navigation, and the tab, the middleware and the page guard all read one declaration"
    - "The ten surface rules of the UI contract are asserted by a command instead of remembered"
    - "The four things only a person can settle are written down as procedures with named steps, named roles and a pending result"
  artifacts:
    - path: "src/lib/routes/staff-tabs.ts"
      provides: "the Calendar tab, landing after its page exists on disk"
    - path: "scripts/verify-calendar-surface.mjs"
      provides: "the ten mechanical assertions of 44-UI-SPEC §15"
    - path: ".planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md"
      provides: "the written manual procedures, each with Result: pending"
  key_links:
    - from: "src/lib/routes/staff-tabs.ts"
      to: "src/lib/routes/capability-routes.ts"
      via: "the module-load assertion that a tab cannot point at an address nobody binds"
      pattern: "resolveRoute"
---

<objective>
Put the calendar in the navigation, turn the UI contract's ten checkable rules into a
command, and write down the four things no command in this repository can settle.

Purpose: criterion 4's third reader is the navigation, and it can only land now — a static
address enters the generated route union once a `page.tsx` serves it. And the phase closes
with a VERIFICATION.md carrying `file:line` evidence, which needs the procedures to exist
before it can cite their results.
Output: one tab, one check, one registration, and one procedures document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-the-production-calendar-comes-inside/44-UI-SPEC.md
@.planning/phases/44-the-production-calendar-comes-inside/44-VALIDATION.md
@.planning/phases/44-the-production-calendar-comes-inside/44-PATTERNS.md
@.planning/ROADMAP.md
@src/lib/routes/staff-tabs.ts
@scripts/verify-tokens.mjs
@scripts/verify-all.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Calendar tab — page first, tab second</name>
  <files>src/lib/routes/staff-tabs.ts</files>
  <read_first>
    - `src/lib/routes/staff-tabs.ts:20-28` — *hiding a nav item is not protecting a route*.
      A viewer who never sees a tab is not thereby refused the address; the refusal is the
      middleware's, and the boundary on the DATA is the RLS policy in the migrations.
    - `src/lib/routes/staff-tabs.ts:107-130` — the Formats entry, which is the *page first,
      tab second* precedent in writing, including the two workarounds that were weighed and
      rejected: widening `href` would turn `typedRoutes` off for every tab above it, and
      asserting the type on one entry would compile and be a hole outliving the week it was
      needed.
    - `src/lib/routes/staff-tabs.ts:150-175` — the module-load assertion, two failures and
      two sentences.
    - `44-UI-SPEC.md` §2 — the tab label is `Calendar`, one word, matching the six
      incumbents. Not `Production` — that is Phase 45's noun.
  </read_first>
  <action>
    Add `{ href: "/admin/calendar", label: "Calendar", capability: CAP.PRODUCTION_READ }` to
    the declared tabs, placed among its peers in the order the file already establishes.

    The two rejected workarounds stay rejected: do not widen `StaffTab.href` and do not
    assert the type on this entry. The page exists on disk from plan 44-09, so the static
    address is in the generated union and neither workaround is needed.

    Add the entry's comment in the file's register, and make it carry the sentence that
    matters more here than on any other tab: **hiding this tab protects nothing.** The
    calendar holds unannounced dates and spaces under negotiation, and what refuses a
    door-assigned staff account is the middleware entry and the six RLS policies — not the
    absence of a link. Say it here, because a surface whose whole content is a secret is
    exactly where somebody would be tempted to believe the opposite.

    The module-load assertion at `:150-175` now covers this entry: if the tab pointed at an
    address `CAPABILITY_ROUTES` did not bind, or claimed a key the map binds to a different
    address, the file throws at import with a sentence naming which. That assertion is the
    check for this task; it is not something to add.
  </action>
  <acceptance_criteria>
    - `grep -c "label: \"Calendar\"" src/lib/routes/staff-tabs.ts` returns 1, bound to `CAP.PRODUCTION_READ`.
    - No workaround was used: `grep -ci "as Route|@ts-expect-error|@ts-ignore" src/lib/routes/staff-tabs.ts` returns 0.
    - `npm run build` exits 0 — and the module-load assertion runs at import, so a green build here does mean the tab resolves through the map, unlike the backward assertion in `capability-routes.ts`.
    - `npm run verify:routes` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:routes</automated>
  </verify>
  <done>The Calendar tab exists, resolves through `CAPABILITY_ROUTES` at module load, uses neither rejected workaround, and carries the sentence saying that hiding it protects nothing.</done>
</task>

<task type="auto">
  <name>Task 2: The surface check — scripts/verify-calendar-surface.mjs</name>
  <files>scripts/verify-calendar-surface.mjs, scripts/verify-all.mjs, package.json</files>
  <read_first>
    - `44-UI-SPEC.md` §15 — the ten assertions U1 to U10, each with an empty exemption list,
      scoped to `src/app/(admin)/admin/calendar/**` and `src/app/(admin)/admin/(work)/calendar/**`
      and to nothing else.
    - `scripts/verify-tokens.mjs` — the header shape, the WHAT A GREEN DOES NOT MEAN section,
      and the `REVERSED_E` discipline: the check matches the reversed glyph **by code point**
      and never by writing the character into the script.
    - `scripts/verify-media-strip.mjs` — the per-check and verdict shape.
    - `scripts/verify-all.mjs` — the runner array, where this check DOES belong, because it
      reads only tracked source and needs no `docs/` and no server.
    - `src/app/(admin)/admin/formats/actions.ts:53-67` — *a grep whose only match is the
      sentence forbidding the thing is a grep that gets ignored the third time it goes red.*
  </read_first>
  <action>
    Create `scripts/verify-calendar-surface.mjs` implementing all ten assertions of
    `44-UI-SPEC.md` §15 over the two calendar directories and nothing else:

    U1 `Podcast` appears zero times — labels, comments, identifiers, all of it.
    U2 no `<input type="date">`, no `type="file"`, no date-picker import.
    U3 no `new Date(`, `toISOString`, `toLocaleDateString`, `Intl.DateTimeFormat`.
    U4 `conforms_to_rule` appears in no JSX and reaches no prop of a rendering component.
    U5 a piece's date is rendered only through `PieceDate` — assert that no file other than
       `PieceDate.tsx` calls `formatCivilDate` on a piece's date field.
    U6 `Badge tone="emphasis"` occurs only adjacent to `Late` or `Diverged`.
    U7 no `--sem-warn`, no `bg-amber`, no inline background style, no format identification
       colour applied to a row.
    U8 `normal-case` is present on every element rendering a format name, a sigla or the
       brand.
    U9 the reversed glyph does not occur — matched **by code point**, never by writing the
       character into this script.
    U10 no `SUMMARY` value, no venue word and no line-up reaches a `console.*`, a thrown
       message or an analytics call — asserted structurally: no `console.*` in these
       directories interpolates a field named `summary`, `title`, `venue_word` or `lineup`,
       and no `.details` is read anywhere.

    **Grep hygiene, and it is not optional here.** Several of these tokens appear in the
    header of the very files being checked, in the sentence forbidding them. Strip comment
    lines before counting — a check that goes red because of the comment explaining why it
    should be green is a check that gets disabled the third time.

    Header in the repository's shape, including a **WHAT A GREEN DOES NOT MEAN** section:
    these are string assertions. None of them proves the surface is correct, none proves a
    proposal reads as unsettled, and there is no test runner for the product. Say it before
    anybody quotes the green.

    Per-check `✓` / `✗` output and a verdict block: `CALENDAR_SURFACE_OK` or
    `CALENDAR_SURFACE_FAIL — N check(s) failed: …` with exit 1.

    Add `"verify:calendar-surface": "node scripts/verify-calendar-surface.mjs"` to
    `package.json`, and **register it in `scripts/verify-all.mjs`'s runner array** — unlike
    `verify:ics`, this one reads only tracked source, so it runs everywhere and belongs in
    the green.

    Then perform the **mutation obligation** on all ten: break each, confirm the mutation
    landed by reading the line back, run the check, observe it fire, restore, confirm the
    restoration. Record the ten rows in the SUMMARY. Finish with `npm run verify` and
    `npm run build` both green, and `git status --porcelain` clean of anything unintended.
  </action>
  <acceptance_criteria>
    - `node scripts/verify-calendar-surface.mjs` exits 0.
    - All ten checks are implemented and named: `grep -cE "^\s*(//)?\s*U(10|[1-9])\b" scripts/verify-calendar-surface.mjs` returns 10 or more.
    - The reversed glyph is matched by code point: `grep -c "\\\\u" scripts/verify-calendar-surface.mjs` returns 1 or more, and the character itself does not occur in the file.
    - `grep -c "verify-calendar-surface" scripts/verify-all.mjs` returns 1, inside the runner array.
    - `npm run verify` exits 0 and its output includes the new check.
    - The SUMMARY carries a ten-row mutation table: the mutation, the evidence it landed, the observed failure, the evidence it was restored.
    - `git status --porcelain` is clean of unintended changes after the tenth restoration.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <done>Ten assertions run as a command, are registered in `verify-all`, and every one has been broken, observed to fire, and restored with evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The written procedures — the four things no command can settle</name>
  <what-built>
    `.planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md`, written in
    the shape of `39-DOOR-PASS.md`: numbered steps, the role each is performed as, the exact
    observation expected, and a `Result:` line per step, every one of them **`pending`**
    until a person runs it.

    It carries no venue word, no unannounced date and no line-up — a step says *open the
    calendar and read the first row*, never what that row says.
  </what-built>
  <how-to-verify>
    The four procedures, and what each closes:

    **P1 — Criterion 4, with real roles.** Five sessions: master, organizer (approved),
    organizer (pending, seeded by hand), staff assigned to the door, and a plain member. For
    each: request `/admin/calendar` and `/admin/calendar/[id]`, and observe the verdict at
    **three levels** — the middleware's redirect, the page guard's redirect, and the row
    level, by issuing a PostgREST read of `production_plan` with that session's own token.
    Record all three per role. **The row level is the one that matters**: the Management API
    connects as a role that bypasses RLS, so nothing measured in plan 44-07 said anything
    about a refusal, and this step is the first evidence that exists.
    Expected: master and organizer admitted at all three; staff and member refused at all
    three; the pending organizer's verdict follows the `requires_approved` value the owner
    chose in plan 44-04, and if it does not, that is a finding.

    **P2 — Criterion 3, and it is a judgement rather than a measurement.** Show S1 and S2 to
    somebody who has not read the spec. Ask: *which of these dates are decided, and which are
    not?* Do not explain first. `44-UI-SPEC.md` §15 says this is the one thing only a person
    can settle, and the whole of §7 exists to make the answer obvious. Record what they said,
    verbatim, including a wrong answer — a wrong answer here is the finding, not a failure of
    the reader.

    **P3 — The announcement act, end to end, with venue-secrecy observations at each step.**
    On a plan row chosen for the purpose, **id captured first**: open the dialog and read all
    four body parts; confirm the open items are named rather than counted; confirm the
    monotone-number sentence is present; confirm what part 1 says about the venue matches the
    owner's decision from plan 44-12. Then press Announce. Observe: an `event_parties` row
    exists; `linked_party_id` is set on the plan row; `party_series.highest_assigned` has
    risen and **not** fallen; and — the observation that matters most — **request the public
    event page as an anonymous session with no cookie and confirm what is and is not
    readable**, against the venue-secrecy decision. Then press Announce a second time on the
    same row and confirm it is refused with its own reason code, so a second number cannot
    be spent.

    **P4 — The tick, and the refusal that is not a failure.** Tick and untick a checklist
    item as an organizer; confirm the author line changes both times. Then attempt the tick
    as a role without the key and confirm the sentence is *you do not have permission*, and
    not *it did not save* — two facts, two sentences, and this is the step that proves they
    did not collapse.
  </how-to-verify>
  <read_first>
    - `44-VALIDATION.md` §Manual-Only Verifications — the five rows this document turns into
      steps.
    - `.claude/rules/meta-gates.md` §Il gate della verifica, in un repo senza test — what
      *verified* means here, and that a written procedure is the only evidence that will
      exist.
    - `.planning/STATE.md` §Blockers — the 83 open `human_needed` entries, all of the same
      species: nothing in this repository can authenticate as a role. These four join that
      list unless they are run.
    - `CLAUDE.md` §Gate VERIFICATION.md — a verification without a single `file:line`
      citation does not satisfy the gate.
  </read_first>
  <acceptance_criteria>
    - `44-PROCEDURES.md` exists with four procedures, every step numbered, every step naming
      the role it is performed as and the exact observation expected, and every `Result:`
      line reading `pending`.
    - The document contains no venue word, no unannounced date, no line-up and no personal
      name — asserted by reading it, and by `npm run verify:persona` check F still passing.
    - The SUMMARY states plainly which procedures were **run** and which were **deferred**,
      and that **deferred is not verified**. This project has said so once already, about
      thirty-two entries, and the sentence exists so a `[x]` never lands on a phase whose
      only open point is that it is not yet proved.
    - If the owner batches these with the end-of-milestone session (the standing pattern for
      role-dependent checks), the SUMMARY records the batching **and** flags P3 separately:
      it writes to production and creates something the public may see, so it needs its own
      dated authorisation and cannot ride along with the others.
  </acceptance_criteria>
  <files>.planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md</files>
  <action>Write `44-PROCEDURES.md` in the shape of `39-DOOR-PASS.md`: four procedures P1 to P4 as described above, every step numbered, every step naming the role it is performed as and the exact observation expected, and a `Result:` line per step reading `pending`. Carry no venue word, no unannounced date, no line-up and no personal name — a step says *open the calendar and read the first row*, never what that row says. Then put the document to the owner: run the procedures now, or batch them with the end-of-milestone session. If batched, record the batching and flag P3 separately, because it writes to production and creates something the public may see, so it needs its own dated authorisation.</action>
  <verify>
    <automated>npm run verify:persona</automated>
  </verify>
  <done>`44-PROCEDURES.md` exists with four procedures, named roles, named observations and a `Result:` per step, and the SUMMARY says per procedure whether it was run or deferred — and that deferred is not verified.</done>
  <resume-signal>Type "approved" once the procedures are written, or "run" with the results if they were executed now.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the staff navigation → the calendar | Hiding a tab is not protecting a route. The refusal is the middleware's and the boundary on the data is the policy |
| a real session → the six tables | The only place the RLS policies are ever actually exercised. Nothing before this plan touched that boundary |
| a procedures document → the public repository | `.planning/` is tracked, so the document is a publication and a step must never quote a row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-18 | Elevation of privilege | a door-assigned staff session | mitigate | P1 exercises the refusal at three levels with five real roles, including the row level with that session's own token — the only evidence that can exist, since every automated path in this repository bypasses RLS |
| T-44-05 | Information disclosure | the announcement act | mitigate | P3 requests the public event page anonymously, with no cookie, and records what is and is not readable against the owner's venue-secrecy decision |
| T-44-03 | Tampering | a series progressivo | mitigate | P3 confirms the watermark rose and did not fall, and that a second announcement on the same row is refused with its own code |
| T-44-20 | Repudiation | a proposal read as settled | mitigate | P2 asks a reader who has not seen the spec, and records a wrong answer as a finding rather than as a reader's failure |
| T-44-33 | Repudiation | two refusals collapsing into one | mitigate | P4 exercises both paths and confirms the two sentences stayed two |
| T-44-01 | Information disclosure | the procedures document | mitigate | steps name an action and an observation, never a row's content; `verify:persona` check F still asserts `docs/` is ignored and untracked |
| T-44-34 | Repudiation | a phase marked complete while unproved | mitigate | the SUMMARY must say, per procedure, run or deferred — and that deferred is not verified. The recorded precedent is a roadmap box that went `[x]` on a phase whose only open point was that it was not yet proved |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no package |
</threat_model>

<verification>
- `npm run build`, `npm run verify` (now including `verify:calendar-surface`),
  `npm run verify:routes`, `npm run verify:ics` (locally) and `npm run verify:persona` all
  exit 0.
- The ten-row mutation table for the surface check.
- The four procedures, written, with their results recorded as run or pending.
- **What a green does NOT mean, and this is the phase's closing sentence:** every automated
  check in this repository reads declarations or files. Not one of them opens a session, and
  criterion 4's real question — *is the calendar refused to somebody the model does not
  admit* — has no automated answer here. It has a written procedure and a role, and until
  that procedure has a result, the criterion is open.
</verification>

<success_criteria>
- The Calendar tab exists and resolves through the map at module load, with neither
  rejected workaround.
- Ten surface assertions run as a command, are registered in `verify-all.mjs`, and each has
  been broken and observed to fire.
- `44-PROCEDURES.md` exists with four procedures, named roles, named observations, and a
  `Result:` per step.
- The SUMMARY states, per procedure, whether it was run or deferred — and that deferred is
  not verified.
</success_criteria>

<output>
Create `.planning/phases/44-the-production-calendar-comes-inside/44-13-SUMMARY.md` when done.
</output>
