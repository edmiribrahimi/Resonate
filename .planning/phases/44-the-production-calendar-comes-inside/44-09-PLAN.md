---
phase: 44-the-production-calendar-comes-inside
plan: 09
type: execute
wave: 4
depends_on: ["44-04", "44-05", "44-07"]
files_modified:
  - src/lib/routes/capability-routes.ts
  - src/app/(admin)/admin/(work)/calendar/page.tsx
  - src/app/(admin)/admin/(work)/calendar/loading.tsx
  - src/app/(admin)/admin/calendar/ImportRunSummary.tsx
autonomous: true
requirements: [PROD-01]

must_haves:
  truths:
    - "A night is readable in the product carrying its format, its series number, its venue state and its editorial anchors"
    - "The middleware, the page guard and the row-level policy ask the same question of the same definition"
    - "The effect of the last import is visible on every visit, whether or not anything went wrong"
    - "A failed read is distinguishable from an empty calendar"
  artifacts:
    - path: "src/lib/routes/capability-routes.ts"
      provides: "the two calendar patterns bound to the new key, on the routes branch, with alsoGatesTables"
      contains: "/admin/calendar"
    - path: "src/app/(admin)/admin/(work)/calendar/page.tsx"
      provides: "the chronological calendar, server-fetched, guarded by the same key"
    - path: "src/app/(admin)/admin/calendar/ImportRunSummary.tsx"
      provides: "the observable effect of the last import"
  key_links:
    - from: "src/app/(admin)/admin/(work)/calendar/page.tsx"
      to: "src/lib/routes/capability-routes.ts"
      via: "the page guard asks the key the map binds to this address"
      pattern: "CAP\\.PRODUCTION_READ"
    - from: "src/app/(admin)/admin/(work)/calendar/page.tsx"
      to: "src/app/(admin)/admin/calendar/CalendarList.tsx"
      via: "absolute specifier one level out (R-WORK-ROUTES)"
      pattern: "@/app/\\(admin\\)/admin/calendar/"
---

<objective>
Bind `/admin/calendar` and `/admin/calendar/[id]` to the new capability, and build S1 —
the chronological list of nights and occupied days, with the last import's effect at its
foot.

Purpose: criterion 1 (a night is readable in the product) and criterion 4 (three readers,
one declaration) both land here. Criterion 4 is a property of the **map**, not of this page:
the page's job is to ask the same entry the middleware asks.
Output: one map entry, one page, its loading placeholder, and the import block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-the-production-calendar-comes-inside/44-UI-SPEC.md
@.planning/phases/44-the-production-calendar-comes-inside/44-PATTERNS.md
@.planning/phases/44-the-production-calendar-comes-inside/44-05-SUMMARY.md
@.planning/phases/44-the-production-calendar-comes-inside/44-07-SUMMARY.md
@src/lib/routes/capability-routes.ts
@src/app/(admin)/admin/(work)/formats/page.tsx
@src/app/(admin)/admin/(work)/members/loading.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: The map entry — two patterns, one key, on the routes branch</name>
  <files>src/lib/routes/capability-routes.ts</files>
  <read_first>
    - `src/lib/routes/capability-routes.ts:424-437` — the `CATALOGUE_MANAGE` entry, which is
      this phase's shape exactly: a key that opens one address and also gates tables; and
      the note that a map entry whose page is not on disk yet is a plan not yet run.
    - `src/lib/routes/capability-routes.ts:466-475` — the trap documented in the file: a page
      bound to a `scope: "table"` key is unreachable **for everyone**, with no build error
      and nothing in a log, because `resolveRoute` returns `null` and the middleware fails
      closed.
    - `src/lib/routes/capability-routes.ts:153-154` — `alsoGatesTables`, and D-34-11's
      *declaration that lies by omission*.
    - `src/lib/routes/capability-routes.ts:238-241` and the load-time ambiguity throw at the
      foot of the file.
    - `src/app/(admin)/admin/(work)/venues/[slug]/page.tsx:31-40` — the reachability
      paragraph, and the note that `next build` would NOT have caught a missing row.
  </read_first>
  <action>
    Add an entry for `CAP.PRODUCTION_READ` carrying `routes: ["/admin/calendar",
    "/admin/calendar/[id]"]` and `alsoGatesTables: true`.

    **The `routes` branch, never `scope: "table"`.** The file already records this mistake in
    the opposite direction at the `CATALOGUE_MANAGE` entry: a page bound to a table-only key
    is unreachable for everyone, silently. Both patterns go in ONE entry — the dynamic sister
    address needs its own pattern exactly as `/admin/venues` and `/admin/venues/[slug]` do,
    and a second entry or a second key would be a second predicate.

    **`alsoGatesTables: true` is not optional here:** the key gates six tables, and omitting
    it produces no error at all, which is the lie by omission D-34-11 exists to prevent.

    **The ambiguity check.** `/admin/calendar` is two literal segments and every existing
    two-segment `/admin/*` pattern in the map is literal and distinct, so the load-time throw
    at the foot of the file does not fire. `/admin/calendar/[id]` is three segments with a
    dynamic tail; confirm against the existing three-segment patterns that no tie exists.
    **Re-run this check by reading, not by hoping:** that throw is a module-load `throw`
    inside a middleware bundle, so it fires on the FIRST REQUEST AFTER DEPLOY and not at
    `npm run build`, and a tie would be a 500 on every route the middleware covers —
    payments webhook and check-in included (WR-04, and the rule STATE.md records: ship on a
    day without a night, and make the first request yourself).

    Write the entry's comment in the file's own register: what question the key answers, why
    it is not `organizer.access` (Phase 45 must have something to take away), and the
    sentence that must survive into every reader of this phase — **the middleware is UX; the
    RLS is the boundary.** The map decides where a REDIRECT happens. It stops somebody
    arriving on a page; it stops nobody reading a row. Criterion 4 asks all three readers to
    ask the same question of the same definition; it does not promote the map to a security
    control, and this comment must not describe it as one.

    Note in the comment that the page arrives in this same plan and the TAB does not
    (`staff-tabs.ts` is plan 44-13) — page first, tab second, because `StaffTab.href` is
    `Route` and a static address enters the generated union only once a `page.tsx` serves it.
  </action>
  <acceptance_criteria>
    - `grep -c "/admin/calendar" src/lib/routes/capability-routes.ts` returns 2 or more, both inside a `routes:` array.
    - `grep -c "alsoGatesTables: true" src/lib/routes/capability-routes.ts` increases by exactly 1 relative to the pre-edit file.
    - The entry is not on the table branch: the `CAP.PRODUCTION_READ` entry contains no `scope: "table"` — confirmed by reading it.
    - `npm run verify:routes` exits 0.
    - `npm run build` exits 0 — and note that a green build here proves nothing about the map, because the backward assertion `_everyStaffRouteIsBound` asks the opposite direction and does not see the address until a `page.tsx` serves it.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:routes</automated>
  </verify>
  <done>Both calendar patterns are bound to `CAP.PRODUCTION_READ` in one entry on the `routes` branch, with `alsoGatesTables: true`, the ambiguity check re-read, and the middleware-is-UX sentence written into the comment.</done>
</task>

<task type="auto">
  <name>Task 2: S1 — the calendar page and its loading placeholder</name>
  <files>src/app/(admin)/admin/(work)/calendar/page.tsx, src/app/(admin)/admin/(work)/calendar/loading.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/formats/page.tsx` in full — imports, the guard at
      `:51-61`, the three-readers docblock at `:20-39`, the untyped-select paragraph at
      `:224-253`, and the empty-state class-string pattern at `:153-161`.
    - `src/app/(admin)/admin/(work)/members/loading.tsx` — the two rules that travel: a
      placeholder must not leak a count or a name, and its shell width must match the page's
      or the content jumps sideways when the data lands.
    - `44-UI-SPEC.md` §8.1 (shell `wide`, no filter/search/sort/pagination, the `Today`
      marker), §13.1 (the four empty states, verbatim) and §13.2 (read failed).
    - `src/app/(admin)/admin/calendar/CalendarList.tsx` and `StageBadge.tsx` (plan 44-05).
    - `44-PATTERNS.md` §`src/app/(admin)/admin/(work)/calendar/page.tsx`.
  </read_first>
  <action>
    Create `src/app/(admin)/admin/(work)/calendar/page.tsx` as a server component.

    **Route placement, per R-WORK-ROUTES:** the route file alone inside `(work)`; every
    non-route module one level out at `src/app/(admin)/admin/calendar/`, imported by absolute
    specifier. The sibling directory has no `page.tsx`, so it contributes no route.

    **The guard**, in the analog's shape and with its paragraph: `const { capabilities } =
    await getAccessContext();` then `if (!capabilities.has(CAP.PRODUCTION_READ)) redirect(...)`.
    Resolved once by `(work)/layout.tsx` and `cache()`-scoped per request, so the second ask
    costs no round trip. The page keeps its own guard because the middleware and the page
    give the same verdict — they read the same entry — and a page that stops asking is a page
    protected by a redirect alone.

    **Fetch**, with the `createClient` cookie-bound client so **RLS is the thing being
    relied on**: plan rows joined to their format and series, their pieces, and the
    commitments, all in date order across the whole archive (D-44-08: past nights too, back
    to the file's earliest entry, because making the rotation checkable in the product
    instead of remembered is the archive's reason for existing). Do **not** use the service
    client on this page: a read that bypasses the policy proves nothing about the policy, and
    the policy is criterion 4's fourth reader.

    **Every PostgREST embed through a table with more than one relationship must be
    qualified with its constraint name.** The measured precedent is exact: an unqualified
    embed of `party_series` through `event_parties` returns `HTTP 300 PGRST201` and fails
    **in silence**, because PostgREST returns `data: null` without an exception and the page
    renders *no events*. Check each embed used here against the foreign keys the two
    migrations created.

    **Three outcomes, never two**, and this is OBS-03 at the page level: rows returned; zero
    rows; and **the read itself failed**. The third renders §13.2's copy —
    `We could not read the production calendar.` plus `This is a failed read, not an empty
    calendar. Reload the page; nothing in the data has changed.` — and never the empty state.
    Log the failure with `error.code` and `error.message` and **never** the error object and
    **never** PostgREST's `details` field, which carries the rejected row, and here that row
    carries `venue_word`.

    **Empty states**, verbatim from §13.1: never imported → `The calendar has not been
    imported yet` with `Run the import on the machine that holds the calendar file. There is
    nothing to upload here — the file never leaves that machine.` The second sentence is not
    decoration: without it a missing upload button reads as an unfinished feature and
    somebody builds it (D-44-26). Imported but no nights → `No nights in the calendar` with
    `The import ran and read no nights. Check that it was given the right file.`
    Distinguish the two by whether a `production_import_run` row exists.

    **Shell:** `PageShell width="wide"`, which adds `/admin/calendar` to the closed `wide`
    list of `41-UI-SPEC.md` §4 — declared here as a decision and recorded in the SUMMARY, per
    that document's own discipline of editing such a list by decision and not by diff.
    One `<h1>` via `PageTitle`, reading `Calendar`.

    **The untyped-select paragraph is mandatory here.** No Supabase client in this repository
    is parameterised with `Database`, so `.select("…")` returns values the compiler cannot
    relate to a column. Declare the row interfaces in the file and write the paragraph saying
    that a green build proves the JSX type-checks against these declarations and nothing
    about whether a column is spelled the way the applied migration spells it — and that
    every name was read out of the two migration files by hand and confirmed against plan
    44-07's catalogue read-back.

    Then create `src/app/(admin)/admin/(work)/calendar/loading.tsx` from
    `members/loading.tsx`: `PageShell width="wide"`, `SkeletonLine` for the title,
    `SkeletonCard` repeated a LITERAL number of times. **The count must not leak** — here it
    would be a count of unannounced dates. Use `Skeleton`, never a hand-rolled
    `animate-pulse` (`44-UI-SPEC.md` §12).
  </action>
  <acceptance_criteria>
    - The page guards on the same key the map binds: `grep -c "CAP.PRODUCTION_READ" "src/app/(admin)/admin/(work)/calendar/page.tsx"` returns 1 or more.
    - No service client on a read path: `grep -c "getServiceClient" "src/app/(admin)/admin/(work)/calendar/page.tsx"` returns 0.
    - Failed read is not empty state: `grep -c "This is a failed read, not an empty calendar" "src/app/(admin)/admin/(work)/calendar/page.tsx"` returns 1.
    - The no-upload sentence is present: `grep -c "the file never leaves that machine" "src/app/(admin)/admin/(work)/calendar/page.tsx"` returns 1.
    - No upload or date control: `grep -ciE "type=\"file\"|type=\"date\"|<input" "src/app/(admin)/admin/(work)/calendar/page.tsx" "src/app/(admin)/admin/(work)/calendar/loading.tsx"` returns 0.
    - No `details` in any log: `grep -c "\.details" "src/app/(admin)/admin/(work)/calendar/page.tsx"` returns 0.
    - No date construction: `grep -cE "new Date|toISOString|toLocaleDateString|Intl\.DateTimeFormat" "src/app/(admin)/admin/(work)/calendar/page.tsx"` returns 0.
    - Both files use `PageShell width="wide"`.
    - `npm run build` exits 0; `npm run verify:routes` exits 0.
    - Manual, and it is the criterion-4 half no command can reach: a **real** session as
      master, as organizer and as door-assigned staff. Written and run in plan 44-13, not
      claimed here.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`/admin/calendar` serves the chronological archive behind the same key the map binds, distinguishes a failed read from an empty calendar, explains the absence of an upload control, and its placeholder leaks no count.</done>
</task>

<task type="auto">
  <name>Task 3: S3 — the import block, which is this phase's answer to having no monitoring</name>
  <files>src/app/(admin)/admin/calendar/ImportRunSummary.tsx</files>
  <read_first>
    - `44-UI-SPEC.md` §10 in full — the block's five rules, its tallies and the reason it is
      not a page.
    - `44-UI-SPEC.md` §13.4 — the tally strings.
    - `.claude/rules/meta-gates.md` §Controllo zero fallimenti silenziosi — there is no error
      tracking in this repository, so *loggare l'errore non è sufficiente*: a failure that
      counts must have an observable effect.
    - `src/components/ui/Card.tsx` and `Typography.tsx` — `Card`, `SectionHeading`.
  </read_first>
  <action>
    Create `src/app/(admin)/admin/calendar/ImportRunSummary.tsx`, rendered by S1 **at the
    foot of the page and never behind a click**. An import summary behind a second click is a
    log with a nicer font, and this repository has no monitoring: the block renders on every
    visit, whether or not anything went wrong.

    A `Card` headed `LAST IMPORT` via `SectionHeading`, carrying the tallies in the data
    face: entries read, nights, pieces, days taken, unclassified, divergences, unsupported
    recurrences, and the run's timestamp.

    **Four rules, none optional:**
      - **Every tally is a real count or a sentence saying it could not be read** — never `0`
        standing in for *we did not measure* (OBS-03). Where the value is null, print
        `We could not count`, not a zero and not an em-dash.
      - **`N unclassified` is drawn as prominently as the rest, never behind a disclosure.**
        Entries the grammars do not describe must be seen and counted, never guessed; a count
        nobody sees is a guess with extra steps.
      - **Unclassified detail carries UIDs and reason codes — never a `SUMMARY`.** An entry's
        title is an unannounced date, a venue under negotiation or a line-up.
      - **`unclassified`, `divergence` and `unsupported recurrence` are three tallies, never
        one `problems` figure.** Three findings, three repairs.

    **Never imported yet** renders §13.1's empty state, not a zeroed block: a block of zeros
    says the import ran and found nothing, and it did not run.

    Render the timestamp through the phase's own formatter (`dates.ts`, plan 44-05) — no
    `Date`, no locale call.
  </action>
  <acceptance_criteria>
    - The three findings are three tallies: `grep -cE "unclassified|divergence|unsupported" "src/app/(admin)/admin/calendar/ImportRunSummary.tsx"` returns 3 or more, and no string containing the word `problems` exists.
    - `grep -c "We could not count" "src/app/(admin)/admin/calendar/ImportRunSummary.tsx"` returns 1 or more.
    - No disclosure around the unclassified count: `grep -ciE "<details|summary>|collaps|accordion" "src/app/(admin)/admin/calendar/ImportRunSummary.tsx"` returns 0.
    - No date construction: `grep -cE "new Date|toISOString|toLocaleDateString|Intl\.DateTimeFormat" "src/app/(admin)/admin/calendar/ImportRunSummary.tsx"` returns 0.
    - No raw title reaches a render: the component's props carry no `summary`, `title` or `venue_word` field for an unclassified entry — only `uid` and a reason code. Confirmed by reading the prop type.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The last import's effect renders at the foot of S1 on every visit, with three separate tallies, a real count or a sentence in every cell, and UIDs rather than titles in every detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an authenticated session → `/admin/calendar` | The middleware redirects; the RLS refuses. Only the second is a boundary |
| a plan row → the rendered page and its logs | `venue_word` and every date cross here; PostgREST's `details` field would carry the whole row |
| the import run row → the screen | The one place the phase makes a failure observable in a repository with no monitoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Elevation of privilege | `/admin/calendar` | mitigate | the map entry is on the `routes` branch with `alsoGatesTables`, the page re-asks the same key, and the page reads with the cookie-bound client so the RLS policy is actually exercised rather than bypassed |
| T-44-18 | Elevation of privilege | a door-assigned staff session | mitigate | `staff` holds neither grant of `production.read`. Proved only by a real session — plan 44-13 |
| T-44-01 | Information disclosure | logs and error copy | mitigate | `error.code` and `error.message` only, never the error object and never `.details`, which carries the rejected row and therefore `venue_word`; the unclassified detail carries UIDs and reason codes |
| T-44-27 | Denial of service | the whole middleware surface | mitigate | the pattern-ambiguity throw at the foot of `capability-routes.ts` fires at module load inside a middleware bundle, so a tie is a 500 on every covered route including the payments webhook and check-in. The check is re-read against both new patterns, and the deploy rule stands: ship on a day without a night and make the first request yourself |
| T-44-28 | Repudiation | a silently failed read | mitigate | three outcomes and never two; a failed read renders its own sentence and never the empty state |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no package |
</threat_model>

<verification>
- `npm run build`, `npm run verify:routes`, `npm run verify:tables`,
  `npm run verify:touch-targets`, `npm run verify:breakpoints` all exit 0.
- `npm run verify` exits 0 and names `verify:ics` among the gates it did not run.
- **What a green does NOT mean:** `verify:routes` reads declarations. It does not open a
  session, so nothing here says the calendar is refused to a door-assigned staff account —
  criterion 4's second half is a written procedure with a real role, plan 44-13. And
  `_everyStaffRouteIsBound` asks the opposite direction, so a green build never proved the
  map entry existed.
</verification>

<success_criteria>
- Both calendar patterns are bound to `CAP.PRODUCTION_READ`, on the `routes` branch, with
  `alsoGatesTables: true`, in one entry.
- `/admin/calendar` serves the whole archive in date order, guarded by the same key, read
  through the cookie-bound client.
- A failed read, an un-run import and an empty calendar are three distinguishable states.
- The import block renders at the foot of the page on every visit, with three separate
  tallies and no zero standing in for an unmeasured value.
- No upload control, no date input, no `Date` construction and no `.details` in any log.
</success_criteria>

<output>
Create `.planning/phases/44-the-production-calendar-comes-inside/44-09-SUMMARY.md` when done.
</output>
