---
phase: 44-the-production-calendar-comes-inside
plan: 04
type: execute
wave: 2
depends_on: ["44-02"]
files_modified:
  - src/lib/capabilities/keys.ts
  - supabase/migrations/20260815120100_production_calendar_access.sql
autonomous: false
requirements: [PROD-01]

must_haves:
  truths:
    - "A fourteenth capability key exists, named by the question it answers, and its description is byte-identical in TypeScript and in SQL"
    - "Every one of the six production tables refuses a SELECT to a session that does not hold the key"
    - "A number already written on a plan row cannot be changed by any caller, including one that forgot to check"
    - "A checklist tick is an act with an author, and it is reversible"
  artifacts:
    - path: "src/lib/capabilities/keys.ts"
      provides: "CAP.PRODUCTION_READ and its total-record description"
      contains: "production.read"
    - path: "supabase/migrations/20260815120100_production_calendar_access.sql"
      provides: "the capability row, its two grants, six SELECT policies, the number-refusing trigger and the author-recording tick function"
  key_links:
    - from: "src/lib/capabilities/keys.ts"
      to: "private.capabilities"
      via: "the description string, shared bytes, same commit"
      pattern: "production\\.read"
    - from: "the six production tables"
      to: "private.has_capability"
      via: "the initplan-wrapped SELECT policy"
      pattern: "\\(SELECT private\\.has_capability\\('production\\.read'\\)\\)"
---

<objective>
Bind the production calendar to the capability model: one new key, one row in
`private.capabilities`, two grants, six SELECT policies, the trigger that makes
renumbering structurally impossible, and the function that records a checklist tick with
its author.

Purpose: criterion 4 asks the middleware, the page guard and the row-level policy to ask
the same question of the same definition; criterion 5 asks that a progressivo is appended
and never renumbered. This plan writes the definition and the guard. It does **not** apply
them — plan 44-07 does.
Output: one edited TypeScript file and one new SQL file, committed together, because the
description string is shared bytes (`keys.ts:29-36`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-the-production-calendar-comes-inside/44-CONTEXT.md
@.planning/phases/44-the-production-calendar-comes-inside/44-RESEARCH.md
@.planning/phases/44-the-production-calendar-comes-inside/44-PATTERNS.md
@.planning/phases/44-the-production-calendar-comes-inside/44-02-SUMMARY.md
@src/lib/capabilities/keys.ts
@supabase/migrations/20260810160000_manual_venue_reveal.sql
@supabase/migrations/20260810120000_formats_and_series.sql
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: The owner's call — is the calendar's key `requires_approved` or not?</name>
  <decision>
    On both grants of the new `production.read` capability, is `requires_approved` **true**
    or **false**?
  </decision>
  <context>
    This is the standing constraint of `44-CONTEXT.md` §Claude's Discretion: technical
    checkpoints are the expert persona's, but anything touching **access** returns to the
    owner. This touches access, and the two sources of truth disagree — deliberately.

    **D-44-27 (the owner, 2026-08-15) points at `false`:** *from now on organizer accounts
    are created inside the app by an admin or an organizer; nobody signs up any more, so
    `pending` no longer has a meaning.* An organizer created by the owner is trusted by
    construction, so requiring an approved state gates on a value that is about to stop
    varying. This is the first place the coming milestone reaches into current work.

    **`44-RESEARCH.md` §Access recommends `true` for the model as it stands today:** the
    surface holds spaces under negotiation and unannounced dates, and an organizer whose own
    access was never approved would be shown a calendar every row of which is a secret. The
    research is not wrong about today; it is answering a different question from the one the
    owner is answering.

    **What the planner has already done rather than smuggling either in:** the key is new,
    not reused, so whichever value is chosen can be changed in one row without touching any
    other surface. The gate is on the **key**, never on `status` read directly from a page
    or a policy — so nothing in this phase has to be unpicked when `pending` stops varying.

    **Consequence of each answer, stated so it is not discovered:**
    - `false` — a `pending` organizer, if one ever exists again, reads the whole calendar.
      Today no signup path creates one; the value is a bet on that staying true.
    - `true` — matches the eleven-key precedent for surfaces holding a secret, and it is
      the value `venue.reveal` carries for an argument written out in `keys.ts:86-108`. It
      becomes dead weight, not a defect, when `pending` stops varying.
  </context>
  <options>
    <option id="requires-approved-false">
      <name>`requires_approved = false` on both grants (follows D-44-27)</name>
      <pros>Honours the owner's stated direction; nothing to remove when the milestone lands</pros>
      <cons>A `pending` organizer would read unannounced dates and open negotiations if one ever exists</cons>
    </option>
    <option id="requires-approved-true">
      <name>`requires_approved = true` on both grants (follows the research)</name>
      <pros>Matches the precedent for every surface holding a secret; correct for the model as it is today</pros>
      <cons>Gates on a value the owner says is about to stop varying; becomes dead weight</cons>
    </option>
  </options>
  <read_first>
    - `src/lib/capabilities/keys.ts:86-108` — the `VENUE_REVEAL` argument, which is the same
      argument in the same shape.
    - `44-CONTEXT.md` D-44-17 and D-44-27.
    - `44-RESEARCH.md` §Access.
  </read_first>
  <acceptance_criteria>
    - The owner's answer is recorded verbatim in `44-04-SUMMARY.md`, with its date, before
      any SQL is written.
    - If the answer is `false`, the migration's comment beside the grants states the reason
      **and** flags it as a bet on the signup path staying closed — never as a silent value.
    - If a gate on `status` proves unavoidable anywhere else in this phase for consistency
      with a neighbouring surface, it is written down as debt the coming milestone removes
      and is not smuggled in.
  </acceptance_criteria>
  <files>(no repository file changes — this task records a decision)</files>
  <action>Present the two options above to the owner with the consequence of each, wait for the answer, and record it verbatim with its date in the SUMMARY. Do not write task 3's grant rows before the answer arrives; the value is not the planner's to pick and it is not to be defaulted.</action>
  <verify>
    <automated>MISSING — a decision has no automated check. The evidence is the answer recorded with its date in 44-04-SUMMARY.md, which task 3's grant rows must then match.</automated>
  </verify>
  <done>The owner's answer is recorded with its date, and the grant rows written in task 3 carry that value.</done>
  <resume-signal>Select: requires-approved-false or requires-approved-true</resume-signal>
</task>

<task type="auto">
  <name>Task 2: The fourteenth key — src/lib/capabilities/keys.ts</name>
  <files>src/lib/capabilities/keys.ts</files>
  <read_first>
    - `src/lib/capabilities/keys.ts` in full — in particular `:17-21` (a capability key has
      no SQL mirror the compiler can hold), `:29-36` (the description is the string the
      migration inserts: same commit, same bytes), `:86-108` (the thirteenth key's argument
      shape) and `:157-173` (the total `Record`, which is a build error when it is missing).
    - `44-RESEARCH.md` §Access — the four-candidate rejection table, already written in
      exactly the form `keys.ts` uses.
  </read_first>
  <action>
    Add `PRODUCTION_READ: "production.read"` to `CAP`, with a one-line doc comment naming
    the question the key answers — *may they read the production calendar* — and the
    `requires_approved` verdict from task 1.

    Above the `CAP` object, add the fourteenth key's argument block in the shape of
    `:86-108`: one bullet per rejected candidate, each naming the DIRECTION of the mistake,
    drawn from `44-RESEARCH.md` §Access:
      - `organizer.access` — the question is *may they reach the organizer area*, a routing
        question about the account. Reusing it makes the calendar reachable by every
        organizer permanently and leaves **Phase 45 nothing to take away** when entitlement
        becomes per-section (PROD-02). Un-widening later is the direction `meta-gates.md`
        permits only with an explicit, documented authorisation.
      - `catalogue.manage` — *may they create an artist or a venue*. Wrong question; it ties
        calendar reach to a catalogue grant.
      - `admin.access` — master only. D-44-17 wants organizer as well.
      - `staff.manage` — `requires_approved = false` ON PURPOSE, so a pending organizer is
        not refused in front of a queue. Nobody is standing in a queue in front of a
        production calendar; the reason does not travel. This is the argument the file
        already makes, in writing, for `venue.reveal`.

    Add the matching entry to `CAP_DESCRIPTIONS`. The sentence must name what the key opens
    and what it gates: the production calendar surface and the six production tables, whose
    rows are unannounced dates and spaces under negotiation. Record the
    `requires_approved` verdict inside the sentence with its reason, exactly as the
    `venue.reveal` description does.

    **This string is inserted verbatim by task 3's migration. Same commit, same bytes.**
    `scripts/verify-capabilities.mjs` is the only thing that checks the two agree, it needs
    a live database, and it is RED between this commit and the deploy that applies the
    migration — expected, and it must be said out loud in the SUMMARY rather than discovered
    (`keys.ts:168-172`).
  </action>
  <acceptance_criteria>
    - `grep -c "PRODUCTION_READ: \"production.read\"" src/lib/capabilities/keys.ts` returns 1.
    - `grep -c "\"production.read\":" src/lib/capabilities/keys.ts` returns 1 (the description entry).
    - All four rejected candidates are named in the argument block: `grep -cE "organizer\.access|catalogue\.manage|admin\.access|staff\.manage" src/lib/capabilities/keys.ts` increases by 4 or more relative to the pre-edit file.
    - `npm run build` exits 0 — the total `Record` is the compiler's half of this contract, and a missing description would fail here.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`CAP.PRODUCTION_READ` and its description exist, the four rejected candidates are argued in writing, and the build is green.</done>
</task>

<task type="auto">
  <name>Task 3: The access migration — capability row, six policies, the number guard, the tick function</name>
  <files>supabase/migrations/20260815120100_production_calendar_access.sql</files>
  <read_first>
    - `supabase/migrations/20260810160000_manual_venue_reveal.sql:131-149` — the capability
      row and grant pattern: two inserts, both `ON CONFLICT DO NOTHING`, and a comment per
      grant explaining why the row exists at all rather than being master-by-absence.
    - `supabase/migrations/20260810160000_manual_venue_reveal.sql:330-426` — the
      author-recording `SECURITY DEFINER` function: the actor is an ARGUMENT (every path
      arrives via the service client, under which `auth.uid()` is null, so a trigger would
      record nobody), refusals are RETURNs and never exceptions (PostgREST puts the entire
      failing row in `error.details`, and a `production_plan` row carries `venue_word`), and
      the return contract is written into the header.
    - `supabase/migrations/20260810120000_formats_and_series.sql:365-384` — the RLS read
      shape, including why the `(SELECT ...)` initplan wrapper is load-bearing.
    - `supabase/migrations/20260810120000_formats_and_series.sql:452-476` — the
      no-write-policy paragraph, including its WHAT THIS DOES NOT CLAIM disclaimer.
    - `supabase/migrations/20260810120000_formats_and_series.sql:590-614` —
      `bump_series_watermark`, the monotone guard whose SHAPE the new trigger copies and
      whose LOGIC must not be re-implemented.
    - `44-RESEARCH.md` §Idempotence and the Monotone Guard, §RLS.
    - `44-CONTEXT.md` D-44-16 — a tick warns but does not block — and the note that
      D-44-02's read-only governs the calendar DATES, not the checklist.
  </read_first>
  <action>
    Create `supabase/migrations/20260815120100_production_calendar_access.sql`. Header in
    the established shape, with its own declared idempotence list and its own publication
    paragraph.

    1. THE CAPABILITY ROW AND ITS TWO GRANTS. Insert into `private.capabilities` the key
    `production.read` with a description that is BYTE-IDENTICAL to `CAP_DESCRIPTIONS` in
    `keys.ts` from task 2, `ON CONFLICT (key) DO NOTHING`. Then insert into
    `private.role_capabilities` the two grants — `master` and `organizer` — with the
    `requires_approved` value the owner chose in task 1, `ON CONFLICT (role, capability) DO
    NOTHING`, one comment per grant. Copy the analog's reason for the master row existing:
    a capability nobody is granted resolves false for everyone, including the master, so the
    row exists rather than leaving the key master-by-absence.

    2. RLS ON ALL SIX TABLES. `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on
    `production_plan`, `production_piece`, `production_commitment`,
    `production_checklist_item`, `production_import_run`, `production_pipeline_rule`. Then
    one SELECT policy each, `DROP POLICY IF EXISTS` first, in the initplan form:
    `USING ((SELECT private.has_capability('production.read')))`. Copy the comment
    explaining that the `(SELECT ...)` wrapper is load-bearing and that it is not `STABLE`
    that produces the effect — it makes Postgres evaluate the call once per statement as an
    InitPlan instead of once per row.

    **NO PUBLIC READ ARM ON ANY OF THE SIX.** Unlike `formats`, nothing here is a catalogue:
    every row is an unannounced date, a space under negotiation or the shape of an internal
    plan. Write that sentence, because the next reader will otherwise wonder why the
    analog has two arms and this has one.

    3. NO INSERT, UPDATE OR DELETE POLICY — DELIBERATE. Copy the analog's paragraph in
    argument and keep its disclaimer. Writes arrive from the import with the SERVICE client,
    which bypasses every policy, so a write policy would constrain nothing on the only path
    that writes while reading to the next person as though the boundary were covered. Three
    tables in this repository already omit their write policies on purpose. And keep the
    closing sentence: **the service client is not a boundary; it is the absence of one.**

    4. THE NUMBER GUARD. `CREATE OR REPLACE FUNCTION public.refuse_production_plan_renumber()
    RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`, raising an
    exception when `NEW.number IS DISTINCT FROM OLD.number` and `OLD.number IS NOT NULL`,
    with an `ERRCODE` and a message naming ONLY the plan id — never the venue word, never
    the title. Attach it as `BEFORE UPDATE OF number ON public.production_plan FOR EACH ROW`.
    `COMMENT ON FUNCTION` carrying the argument in full: the number is READ FROM THE FILE and
    never generated, so a re-import that reads a different number for a known `UID` is a
    REPORTED DIVERGENCE (D-44-07) and not an update. A guard in a trigger survives the caller
    that forgot it; a guard in application code does not.
    **And write down what the guard deliberately is NOT:** it is *number never changes*, and
    NOT *number must exceed the watermark*. The archive legitimately contains numbers far
    below `party_series.highest_assigned` (D-44-08), and a watermark comparison would refuse
    the entire past. Do not re-implement `bump_series_watermark`; it fires on
    `event_parties`, a table this phase never writes.

    5. THE TICK FUNCTION. `CREATE OR REPLACE FUNCTION public.record_checklist_tick(
    p_item_id uuid, p_ticked boolean, p_actor_id uuid, p_actor_name text) RETURNS jsonb
    LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`. The ACTOR IS AN ARGUMENT, for
    the analog's measured reason: every path arrives via the service client, under which
    `auth.uid()` is null, so a trigger would record nobody. Refuse — as a RETURNED jsonb,
    never an exception — when the actor is missing, when the item does not exist, or when
    the caller does not hold `production.read`. Write the return contract into the header
    in the analog's form: success as `{"ok": true, "item_id": ..., "ticked_at": ...}`,
    refusal as `{"ok": false, "reason": "<code>"}`. A refusal must never carry the item's
    label or its plan's venue word — PostgREST puts the entire failing row in
    `error.details`, which is exactly the exit Pitfall 10 names.
    **The tick is REVERSIBLE**, and say so beside the function: unticking re-records the
    author. It is NOT a monotone guard, and the three one-way switches of `meta-gates.md`
    are untouched by this phase — that sentence exists so nobody borrows the wrong
    precedent from the neighbouring file.

    6. `GRANT EXECUTE` on both functions to `authenticated` and `service_role` as the
    analog does, and nothing wider.
  </action>
  <acceptance_criteria>
    - Six tables get RLS and exactly one SELECT policy each: `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260815120100_production_calendar_access.sql` returns 6, and `grep -c "FOR SELECT USING ((SELECT private.has_capability('production.read')))" supabase/migrations/20260815120100_production_calendar_access.sql` returns 6.
    - No write policy exists: `grep -ciE "FOR (INSERT|UPDATE|DELETE)" supabase/migrations/20260815120100_production_calendar_access.sql` returns 0.
    - No public arm: `grep -c "USING (true)" supabase/migrations/20260815120100_production_calendar_access.sql` returns 0.
    - The renumber guard exists and is a `BEFORE UPDATE`: `grep -c "BEFORE UPDATE OF number ON public.production_plan" supabase/migrations/20260815120100_production_calendar_access.sql` returns 1.
    - The watermark is not re-implemented: `grep -c "highest_assigned" supabase/migrations/20260815120100_production_calendar_access.sql` returns 0 outside the comment explaining why it is untouched — verify by `grep -n "highest_assigned"` and reading each hit.
    - Both functions are hardened: `grep -c "SET search_path = ''" supabase/migrations/20260815120100_production_calendar_access.sql` returns 2.
    - The description strings match: `node -e` comparison, or a manual byte comparison, between the SQL insert's description literal and `CAP_DESCRIPTIONS["production.read"]` in `keys.ts`. Any difference is a defect this commit must not carry.
    - No material: `grep -cE "20[0-9]{2}-[0-9]{2}-[0-9]{2}" supabase/migrations/20260815120100_production_calendar_access.sql` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -ciE "FOR (INSERT|UPDATE|DELETE)" supabase/migrations/20260815120100_production_calendar_access.sql) -eq 0</automated>
  </verify>
  <done>The key row, its two grants, six SELECT policies with no write arm and no public arm, the number-refusing trigger and the author-recording tick function all exist in one idempotent SQL file, and its description string is byte-identical to `keys.ts`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an authenticated session → the six production tables | The real boundary. The middleware is UX; the RLS is the boundary (`CLAUDE.md` principle 2) |
| the service client → the six production tables | It bypasses every policy. The boundary in front of it is the capability check in the caller, and that check is plan 44-09's and 44-12's |
| PostgREST error details → a client | A failing row travels whole in `error.details`, and a `production_plan` row carries `venue_word` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Elevation of privilege | the six tables | mitigate | one SELECT policy each asking `production.read` through `private.has_capability`; no public arm, no write arm; the key is new so Phase 45 has something to take away |
| T-44-03 | Tampering | `production_plan.number` | mitigate | `BEFORE UPDATE OF number` trigger refusing any change to a non-null number, `SECURITY DEFINER` with `SET search_path = ''`; deliberately *never changes* rather than *must exceed the watermark*, because the archive holds numbers below it |
| T-44-01 | Information disclosure | refusals raised out of either function | mitigate | refusals are RETURNED jsonb carrying a reason code; the one genuine `RAISE` names a plan id and nothing else; no `venue_word`, no label, no title reaches a message |
| T-44-15 | Repudiation | a checklist tick | mitigate | actor id and name are arguments and are recorded; a refusal to record an anonymous tick is explicit |
| T-44-18 | Elevation of privilege | a door-assigned staff session | mitigate | `staff` holds neither grant; `door.operate` is a different key and is not consulted here. Proved only by a real staff session — a written manual procedure, plan 44-13, because the Management API bypasses RLS and no query of ours can settle it |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no package |
</threat_model>

<verification>
- `npm run build` exits 0 (the total `Record` in `keys.ts` is the compiler's half).
- The grep assertions on task 3.
- `npm run verify:capabilities` will be **RED** from this commit until plan 44-07 applies
  the migration. That is expected and documented at `keys.ts:168-172`; it must be stated in
  the SUMMARY rather than discovered by the next person who runs `npm run verify`.
- **What a green does NOT mean:** none of this is applied. The policies exist as text.
  Whether they hold against a real session is settled by plan 44-13's written procedure with
  five real accounts, not by any query this repository can issue — the Management API runs
  as a role that bypasses RLS.
</verification>

<success_criteria>
- `CAP.PRODUCTION_READ` exists with a total-record description, and the four rejected
  candidates are argued in writing.
- Six tables carry RLS with exactly one SELECT policy each and no write policy.
- The number-refusing trigger exists as a `BEFORE UPDATE OF number`, `SECURITY DEFINER`,
  `SET search_path = ''`, and does not re-implement the watermark.
- The tick function records an author from arguments and returns refusals as values.
- The SQL description literal and `CAP_DESCRIPTIONS["production.read"]` are byte-identical.
- The owner's `requires_approved` answer is recorded with its date.
</success_criteria>

<output>
Create `.planning/phases/44-the-production-calendar-comes-inside/44-04-SUMMARY.md` when done.
</output>
