---
phase: 44-the-production-calendar-comes-inside
plan: 07
type: execute
wave: 3
depends_on: ["44-04"]
files_modified:
  - src/types/database.ts
autonomous: false
requirements: [PROD-01]

must_haves:
  truths:
    - "The six tables, their constraints, their policies and the two functions exist in production, read back from the catalogues rather than from the response to the POST"
    - "No pre-existing row moved while the schema changed"
    - "The migration history carries two new versions, applied through the migrations endpoint"
  artifacts:
    - path: "src/types/database.ts"
      provides: "the six tables' row types, importing the vocabularies from the literal source"
  key_links:
    - from: "src/types/database.ts"
      to: "src/lib/production/ics/vocabulary.ts"
      via: "import of PieceKind, PieceDateOrigin, UnresolvedReason, VenueStage"
      pattern: "from \"@/lib/production/ics/vocabulary\""
---

<objective>
Apply the phase's two migrations to production through the Management API's **migrations**
endpoint, prove they landed by reading the catalogues, prove nothing moved, and only then
write the TypeScript row types.

Purpose: **[BLOCKING].** Every plan after this one reads or writes these tables. And a
build and a typecheck pass without the schema being live — because the TypeScript types
come from a generated file and not from the database — which is a false-positive
verification state this plan exists to close.
Output: two versions in the project's migration history, a recorded read-back, and one
edited types file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-the-production-calendar-comes-inside/44-RESEARCH.md
@.planning/phases/44-the-production-calendar-comes-inside/44-02-SUMMARY.md
@.planning/phases/44-the-production-calendar-comes-inside/44-04-SUMMARY.md
@.planning/STATE.md
@supabase/migrations/20260810120000_formats_and_series.sql
@src/types/database.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: The owner's authorisation to write to production, dated and spent once</name>
  <what-built>
    Two migration files exist on disk and have not been applied:
    `supabase/migrations/20260815120000_production_calendar.sql` (six tables, one added
    column, the published pipeline rules) and
    `supabase/migrations/20260815120100_production_calendar_access.sql` (the capability row
    and its two grants, six SELECT policies, the number-refusing trigger, the tick function).
    Both are idempotent by construction and neither seeds production material.
  </what-built>
  <how-to-verify>
    This is not a verification of work; it is a permission to change production, and this
    project grants it **per application, dated, and it expires when it is spent** (STATE.md,
    Phase 38: an authorisation given on 2026-08-11 was *spesa alle 11:15:24 UTC ed
    esaurita*).

    Before answering, the owner should know four things, each measured rather than recalled:

    1. **What changes.** Six new tables, one new column on `public.party_series`, one new
       row in `private.capabilities` with two grants, six SELECT policies, two functions,
       one trigger. **No existing table is altered other than by that one added column, and
       no existing policy is dropped or replaced.**
    2. **What the precedent is.** The last comparable application (Phase 36, version
       `20260810144239`) moved zero rows, and it was proved by walking the cascade set over
       `pg_constraint` and counting before and after.
    3. **What went wrong once.** D12: 63 production rows were deleted during a *verification*
       in seven tables, and PITR is not active by the owner's own decision. That incident is
       why task 2 takes a snapshot before touching anything, and why every later step deletes
       nothing.
    4. **The order matters.** Migration first, deploy second. The reverse has already been
       identified as the direction that takes pages down rather than degrading them.
  </how-to-verify>
  <read_first>
    - `.planning/STATE.md` §Blockers and §Session Continuity — the migrations-endpoint
      precedent, the pre-existing history drift, and the D12 incident.
    - `44-RESEARCH.md` §Migration Shape, subsection *How it is applied*.
  </read_first>
  <acceptance_criteria>
    - The authorisation is recorded in `44-07-SUMMARY.md` with its date and the exact scope
      it covers: **these two migrations, this once.**
    - If the owner declines or defers, the plan stops here and every downstream plan stays
      blocked. Deferred is not applied, and the SUMMARY must say which of the two it is.
  </acceptance_criteria>
  <files>(no repository file changes — this task obtains a permission)</files>
  <action>Put the four points above to the owner, obtain an explicit, dated authorisation covering these two migrations only, and record it in the SUMMARY before task 2 sends a single request. If it is refused or deferred, stop the plan and mark every downstream plan blocked — deferred is not applied, and the SUMMARY must say which of the two it is.</action>
  <verify>
    <automated>MISSING — a permission has no automated check. The evidence is the dated authorisation recorded in 44-07-SUMMARY.md, and the absence of any request before it.</automated>
  </verify>
  <done>The owner's authorisation is recorded with its date and its single-use scope, or the plan is stopped with the reason recorded.</done>
  <resume-signal>Type "authorised" with the date, or describe the condition to meet first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Snapshot, apply through the migrations endpoint, read back from the catalogues</name>
  <files>(no repository file changes — this task changes production)</files>
  <read_first>
    - `.planning/STATE.md` — `POST /v1/projects/{ref}/database/migrations` with
      `SUPABASE_ACCESS_TOKEN` from `.env.local`. **The migrations endpoint, NOT
      `/database/query`**, so the project's migration history stays truthful. The Supabase
      CLI is not installed on this machine and `supabase db push` is not a step anybody here
      can run.
    - `scripts/rls-baseline.mjs:191-224` — the env-reading and refuse shape: a missing key is
      a refusal, never a silent no-op, and the sentence to keep is *nothing was measured*.
    - `supabase/migrations/20260815120000_production_calendar.sql` and
      `..._access.sql` — read both in full before sending either.
  </read_first>
  <action>
    Perform this in the order written. Each step's output goes verbatim into the SUMMARY;
    a step whose output is not recorded did not happen.

    **Step 1 — the snapshot, before anything.** Count rows in every table reachable by
    cascade from the tables this migration touches, reading the constraint graph from
    `pg_constraint` rather than from memory. `public.party_series` gains a column, so its
    cascade set is the one that matters. Record the counts. A baseline taken after the
    change is not a baseline.

    **Step 2 — the pre-state.** Record: the current migration history versions (last five);
    whether any of the six table names already exists in `pg_class`; whether
    `production.read` already exists in `private.capabilities`. This is what makes the
    idempotence claim checkable rather than asserted.

    **Step 3 — apply `20260815120000_production_calendar.sql`** through
    `POST /v1/projects/{ref}/database/migrations`. Record the response and the version
    string assigned. **Do not use `/database/query`**, and do not use `PUT`, which upserts
    the history without applying anything.

    **Step 4 — apply `20260815120100_production_calendar_access.sql`** the same way. The
    order is not optional: the policies name tables and the trigger names a column, and
    Postgres refuses a policy that reads a column that does not exist — the same lesson
    Phase 36 recorded for its own two-part change.

    **Step 5 — the read-back, and it reads the CATALOGUES, not the response.** A `200` on a
    `POST` is a report; the catalogue is the fact. Query and record:
      - `pg_class` / `information_schema.tables` — the six tables exist.
      - `pg_constraint` — every named constraint of both files exists, by name, including
        `production_piece_date_xor_reason` and `production_piece_proposal_has_no_source`.
      - `pg_policies` — exactly six policies on the six tables, each `FOR SELECT`, and
        **zero** `INSERT`, `UPDATE` or `DELETE` policies on any of them.
      - `pg_proc` — both functions exist with `prosecdef = true` and
        `proconfig` containing `search_path=`.
      - `pg_trigger` — the `BEFORE UPDATE OF number` trigger exists on `production_plan`.
      - `private.capabilities` and `private.role_capabilities` — one key row, two grants,
        with the `requires_approved` value the owner chose in plan 44-04.
      - `public.party_series` — the `ics_alias` column exists and **every row's value is
        null**, which is the runtime-arrival claim made checkable.
      - The six new tables — **zero rows each**, except `production_pipeline_rule`, whose
        row count is reported and compared against the number of `INSERT` statements that
        could resolve a format. A pipeline rule table with zero rows means the format codes
        did not match and is a finding, not a pass.

    **Step 6 — the post-snapshot.** Repeat step 1's counts. **Every count must be identical.**
    A difference is not a rounding error; it is the D12 shape, and it stops the plan.

    Two prohibitions for the whole task, both from `ai-engineering.md` and the D12 incident:
    **delete nothing**, and if any row must ever be removed, capture its id at creation and
    delete by primary key — never by clicking a control and never by predicate.
  </action>
  <acceptance_criteria>
    - The SUMMARY carries the pre-snapshot and the post-snapshot side by side, with the
      number of tables counted, and they are identical.
    - The SUMMARY carries the two version strings the migrations endpoint assigned, read
      from the history and not from the `POST` response body alone.
    - Six tables present; six `SELECT` policies present; **zero** write policies present.
    - Both functions report `prosecdef = true` and a set `search_path`.
    - The trigger is present on `production_plan` and fires `BEFORE UPDATE OF number`.
    - `ics_alias` exists and holds null on every row.
    - `production_plan`, `production_piece`, `production_commitment`,
      `production_checklist_item` and `production_import_run` each hold **zero rows** —
      this is criterion 2 made checkable: the schema arrived empty of material.
    - `production_pipeline_rule`'s row count is recorded, and a zero is reported as a
      finding rather than a pass.
    - **What this does NOT establish, stated in the SUMMARY:** the Management API connects
      as a role that **bypasses RLS**. Nothing measured here says the six policies refuse
      anybody. That is settled only by a real session with a real role, in plan 44-13's
      written procedure.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — no automated command can assert against production from this repository. The evidence is the recorded catalogue read-back in 44-07-SUMMARY.md, and the mechanical checks that CAN run are `npm run verify:capabilities` (which needs the live database and goes green only after this task) and `npm run build`.</automated>
  </verify>
  <done>Both migrations are in the project's migration history, the catalogues confirm every table, constraint, policy, function and trigger, the six calendar tables hold zero rows, and the pre- and post-snapshots are identical.</done>
</task>

<task type="auto">
  <name>Task 3: The row types — src/types/database.ts</name>
  <files>src/types/database.ts</files>
  <read_first>
    - `src/types/database.ts` — the existing shape, so the six new tables land in the same
      form as their neighbours.
    - `src/lib/production/ics/vocabulary.ts` (plan 44-01) — the literal source. This file
      imports FROM there; the arrow never points the other way.
    - `src/app/(admin)/admin/(work)/formats/page.tsx:224-253` — the untyped-select paragraph,
      which is what a green build does and does not prove here.
  </read_first>
  <action>
    Add row types for `production_plan`, `production_piece`, `production_commitment`,
    `production_checklist_item`, `production_import_run` and `production_pipeline_rule`, and
    add `ics_alias: string | null` to `party_series`.

    **Import the vocabularies from `@/lib/production/ics/vocabulary` rather than restating
    them.** `PieceKind`, `PieceDateOrigin`, `UnresolvedReason`, `VenueStage`, `AnchorKind`
    and `AnchorDirection` have one home, and a second copy here would be a second truth that
    nothing checks. The literal source imports nothing, precisely so this direction is the
    only possible one.

    Write the paragraph the analog writes, in substance: **no Supabase client in this
    repository is parameterised with `Database`**, measured at four call sites, so
    `.select("…")` returns values the compiler cannot relate to a column. A green
    `npm run build` proves the consuming code type-checks against these declarations; it
    proves nothing about whether a column is spelled the way the applied migration spells it.
    Every name below was read out of the two migration files by hand and confirmed against
    the catalogue read-back of task 2 — **that is the only check performed on them, and it
    is a better one than the neighbours got**, so say which.
  </action>
  <acceptance_criteria>
    - `grep -c "from \"@/lib/production/ics/vocabulary\"" src/types/database.ts` returns 1.
    - The vocabularies are not restated: `grep -cE "\"listing\" \| \"tonight\"|'listing' \| 'tonight'" src/types/database.ts` returns 0.
    - All six tables are declared: `grep -cE "production_(plan|piece|commitment|checklist_item|import_run|pipeline_rule)" src/types/database.ts` returns 6 or more.
    - `grep -c "ics_alias" src/types/database.ts` returns 1.
    - `npm run build` exits 0.
    - `npm run verify:capabilities` exits 0 — it needed the live database and the applied migration, and it has both now. It was RED from plan 44-04's commit until task 2 of this plan, which is expected and documented at `keys.ts:168-172`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The six tables and the added column have row types that import their vocabularies from the literal source, and `verify:capabilities` is green for the first time since plan 44-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine → production | The Management API with `SUPABASE_ACCESS_TOKEN`. It connects as a role that **bypasses RLS**, so nothing it reads is evidence about a policy's refusal |
| the `POST` response → the claim that the schema changed | The response is a report; the catalogue is the fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-22 | Tampering | production rows | mitigate | pre- and post-snapshot over the cascade set read from `pg_constraint`, counted before the first request; delete nothing; the D12 incident is the precedent that makes this mandatory rather than tidy |
| T-44-23 | Repudiation | the migration history | mitigate | the **migrations** endpoint, never `/database/query` and never `PUT`, so the history records what was applied. The pre-existing drift (`20260508000000_drink_token_active_state.sql` applied but absent from the history) is the owner's call and is not repaired here |
| T-44-02 | Elevation of privilege | the six policies | accept **for this plan only, and declared** | the read-back proves the policies EXIST; it cannot prove they REFUSE, because the API role bypasses RLS. The refusal is proved by plan 44-13's written procedure with a real role, and the phase is not verified until it is |
| T-44-01 | Information disclosure | the read-back output | mitigate | count rows, read catalogue names; do not `SELECT` the contents of any calendar table into the SUMMARY. The tables are empty at this point, and the discipline must survive the moment they are not |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no package |
</threat_model>

<verification>
- `npm run build` exits 0.
- `npm run verify:capabilities` exits 0 after task 2 — it is the one automatic check in this
  repository that can see the key row, and it needed both halves.
- The recorded catalogue read-back is the evidence for criterion 1's *the tables exist* and
  for criterion 2's *the schema arrived empty of material*. It is written into
  `44-07-SUMMARY.md` with the queries that produced it, per the VERIFICATION.md gate in
  `CLAUDE.md`: a verification without a single citation does not satisfy it.
- **What a green does NOT mean:** the build passed before this plan ran, with no schema
  live at all. That is the false-positive state this plan closes, and the SUMMARY must say
  so plainly so nobody reads a past green as evidence about the database.
</verification>

<success_criteria>
- The owner's authorisation is recorded with its date and its single-use scope.
- Both migrations appear in the project's migration history with their assigned versions.
- The catalogues confirm six tables, six SELECT policies, zero write policies, two hardened
  functions, one trigger, one capability row and two grants.
- Every calendar table holds zero rows; `ics_alias` holds null on every row.
- Pre- and post-snapshots are identical across the cascade set.
- `src/types/database.ts` imports its vocabularies from the literal source.
- `npm run build` and `npm run verify:capabilities` exit 0.
</success_criteria>

<output>
Create `.planning/phases/44-the-production-calendar-comes-inside/44-07-SUMMARY.md` when done.
</output>
