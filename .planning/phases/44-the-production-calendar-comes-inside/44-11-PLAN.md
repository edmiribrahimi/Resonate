---
phase: 44-the-production-calendar-comes-inside
plan: 11
type: execute
wave: 5
depends_on: ["44-09"]
files_modified:
  - src/app/(admin)/admin/(work)/calendar/[id]/page.tsx
  - src/app/(admin)/admin/(work)/calendar/[id]/loading.tsx
  - src/app/(admin)/admin/calendar/PiecesSection.tsx
  - src/app/(admin)/admin/calendar/ChecklistSection.tsx
autonomous: true
requirements: [PROD-01]

must_haves:
  truths:
    - "One night's editorial pieces are readable with their dates, and a piece with no date says which of three reasons applies"
    - "A night's checklist covers the pieces plus the production steps, and an unticked item whose date has passed reads as late"
    - "A LiveCut is identified by its part marker and never by a person's name"
  artifacts:
    - path: "src/app/(admin)/admin/(work)/calendar/[id]/page.tsx"
      provides: "S2 — one night, its pieces and its checklist"
    - path: "src/app/(admin)/admin/calendar/PiecesSection.tsx"
      provides: "the pieces list, rendering every date through PieceDate"
    - path: "src/app/(admin)/admin/calendar/ChecklistSection.tsx"
      provides: "the checklist rows, their authors and their late marks"
  key_links:
    - from: "src/app/(admin)/admin/calendar/PiecesSection.tsx"
      to: "src/app/(admin)/admin/calendar/PieceDate.tsx"
      via: "the only renderer of a piece's date"
      pattern: "PieceDate"
    - from: "src/app/(admin)/admin/(work)/calendar/[id]/page.tsx"
      to: "src/lib/routes/capability-routes.ts"
      via: "the guard asks the key bound to /admin/calendar/[id]"
      pattern: "CAP\\.PRODUCTION_READ"
---

<objective>
Build S2: one night, its editorial pieces with their five date states, and its checklist
with the production steps beside the pieces.

Purpose: criterion 1 finishes here — *a night is readable carrying its editorial anchors* —
and criterion 3's product-visible half lands here: a derived date must be visible AND must
never read as settled.
Output: one dynamic route, its placeholder, and the two sections. The tick and the
announcement are plan 44-12's; this plan renders, it does not write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-the-production-calendar-comes-inside/44-UI-SPEC.md
@.planning/phases/44-the-production-calendar-comes-inside/44-PATTERNS.md
@.planning/phases/44-the-production-calendar-comes-inside/44-09-SUMMARY.md
@src/app/(admin)/admin/(work)/venues/[slug]/page.tsx
@src/app/(admin)/admin/calendar/PieceDate.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: The night page and its placeholder</name>
  <files>src/app/(admin)/admin/(work)/calendar/[id]/page.tsx, src/app/(admin)/admin/(work)/calendar/[id]/loading.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/venues/[slug]/page.tsx:1-40` — the imports, the guard,
      and above all the REACHABILITY paragraph: `admin` in the URL is an address, not an
      authorisation; what decides is the row in `capability-routes.ts`, read by the
      middleware, by the guard below and by the navigation, so the three cannot disagree.
      And its warning: `next build` would NOT have caught a missing row here.
    - `44-UI-SPEC.md` §9.1 — shell `default`, one `<h1>`, the label-role line beneath it, and
      the two sections in order with `PIECES` first.
    - `44-UI-SPEC.md` §13.1 — the two S2 empty states, verbatim.
    - `src/app/(admin)/admin/(work)/members/loading.tsx` — the placeholder rules.
  </read_first>
  <action>
    Create `src/app/(admin)/admin/(work)/calendar/[id]/page.tsx`.

    Guard on `CAP.PRODUCTION_READ`, the same key `/admin/calendar/[id]` is bound to in the
    map entry plan 44-09 wrote — one entry, two patterns, three readers. Reproduce the
    reachability paragraph in substance: the directory name authorises nothing, the map row
    does, and a green build would not have caught its absence.

    Shape-check the `id` parameter against a UUID pattern **before** it reaches a query, in
    the shape `formats/actions.ts:111-113` uses. A parameter that fails the shape check is
    `notFound()`, not a query.

    Read with the cookie-bound `createClient`, never the service client: the RLS policy is
    the boundary being relied on and a service read would prove nothing about it. Fetch the
    plan row with its format, its series, its venue and stage, its pieces and its checklist
    items. Qualify every PostgREST embed that crosses a table with more than one
    relationship with its constraint name — the unqualified form returns `HTTP 300 PGRST201`
    and fails **in silence**, rendering an empty page instead of raising.

    Three outcomes, never two: the row exists; the row does not exist (`notFound()`); the
    read failed. The third renders §13.2's `We could not read the production calendar.` plus
    `This is a failed read, not an empty calendar. Reload the page; nothing in the data has
    changed.` **A failed read must not render as a missing night** — on a page reached from
    a list, that reads as *the night was deleted*.

    `PageShell width="default"`. One `<h1>` via `PageTitle`, carrying the night's name in the
    display face with `normal-case`. Beneath it, in the label role: the sigla, the date
    weekday-first through `formatCivilDate`, the venue word and its `StageBadge`. Then two
    sections headed by `SectionHeading`: `PIECES`, then `CHECKLIST`. **Pieces first**,
    because the checklist's items are mostly about the pieces, and reading a tick before
    knowing what it is about is reading an answer before the question.

    Empty states, verbatim: no pieces written → `Nothing is written for this night yet` with
    `Every piece below is a proposal until it is written in the calendar file.`; no checklist
    items → `This format owes no production steps` with `Pieces still appear above.`

    Then create `[id]/loading.tsx` from the analog: `PageShell width="default"`,
    `SkeletonLine` for the title, `SkeletonCard` a literal number of times. The `default`
    width must match the page's, or the content jumps sideways when the data lands. No count
    is leaked; `Skeleton` is the primitive and a hand-rolled `animate-pulse` is not.
  </action>
  <acceptance_criteria>
    - `grep -c "CAP.PRODUCTION_READ" "src/app/(admin)/admin/(work)/calendar/[id]/page.tsx"` returns 1 or more.
    - No service client: `grep -c "getServiceClient" "src/app/(admin)/admin/(work)/calendar/[id]/page.tsx"` returns 0.
    - The id is shape-checked before any query: a UUID regex test precedes the first `.from(` call — confirmed by reading.
    - A failed read is not a missing night: `grep -c "This is a failed read" "src/app/(admin)/admin/(work)/calendar/[id]/page.tsx"` returns 1, and `notFound()` is called only on a genuinely absent row.
    - Both files use `PageShell width="default"`.
    - No date construction: `grep -cE "new Date|toISOString|toLocaleDateString|Intl\.DateTimeFormat" "src/app/(admin)/admin/(work)/calendar/[id]/page.tsx" "src/app/(admin)/admin/(work)/calendar/[id]/loading.tsx"` returns 0.
    - No input of any kind on the page itself: `grep -ciE "type=\"file\"|type=\"date\"" "src/app/(admin)/admin/(work)/calendar/[id]/page.tsx"` returns 0.
    - `npm run build` and `npm run verify:routes` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:routes</automated>
  </verify>
  <done>`/admin/calendar/[id]` serves one night behind the same key, distinguishes a failed read from a missing night, and renders `PIECES` above `CHECKLIST` with both empty states written.</done>
</task>

<task type="auto">
  <name>Task 2: PiecesSection — every date through the one component that cannot render a bare one</name>
  <files>src/app/(admin)/admin/calendar/PiecesSection.tsx</files>
  <read_first>
    - `44-UI-SPEC.md` §9.2 in full — one row per owed piece, the kind in production's own
      word, the part marker rather than a name, the date through `PieceDate`, and the reason
      beside the state on the same row.
    - `44-UI-SPEC.md` §7 — the five variants and the three channels.
    - `src/app/(admin)/admin/calendar/PieceDate.tsx` (plan 44-05).
    - `src/lib/production/ics/vocabulary.ts` — `PIECE_KIND_LABELS`.
  </read_first>
  <action>
    Create `src/app/(admin)/admin/calendar/PiecesSection.tsx`.

    One row per **owed** piece, in the order the pipeline produces them — the owed set comes
    from `production_pipeline_rule`, so a piece that was never written is *missing* rather
    than being indistinguishable from *not needed* (D-44-09b part 2).

    Each row: the kind in the body role via `PIECE_KIND_LABELS`, then the date **through
    `PieceDate` and through nothing else**. No other component on this surface may render a
    piece's date; that is check U5 and the reason `PieceDate` takes a discriminated union.

    **`LiveCut`, never `Podcast`** (D-44-22). The string `Podcast` appears in no label, no
    comment, no component name, no variable name and no test string. A `Podcast` is a
    different thing that does not exist yet, and putting the word on a surface would announce
    a format the project has not built.

    **A LiveCut is identified by its part marker** — `PT1`, `PT2`, `PT3` — **and never by a
    dj's name.** The file carries a marker, not a name; associating a marker with a person
    would be a guess, and a guess about a line-up on a screen is a line-up on a screen.

    **The reason sits beside the state, on the same row, in the body role at `--muted`** —
    never behind an icon, never in a tooltip. A tooltip is not a channel on a touch device.

    Where the number of LiveCuts is not knowable, the section says `LiveCuts depend on the
    line-up` and prints **no figure** (D-44-13). Three is not a constant: one archived
    edition carries two. The count, where it is knowable, comes from `party_credits` — the
    structured line-up — and not from `event_parties.lineup`, which the migration states
    outright is *the communicated text*.

    Where a night has a listing and a timetable but no LiveCut and no after movie because
    the next edition is not in the calendar, the rows render `Waiting for <edition>` and
    **nothing on the surface calls that missing, late, incomplete or broken.** It is the rule
    behaving correctly.

    No sound allusion anywhere: no string, class name, component name or comment in this file
    says what a format sounds like. `RamaDub`, `MotionLab` and `re:sonate` have no written
    manifesto, and a surface may not allude to one.
  </action>
  <acceptance_criteria>
    - `grep -c "Podcast" "src/app/(admin)/admin/calendar/PiecesSection.tsx"` returns 0.
    - Every date goes through `PieceDate`: `grep -c "PieceDate" "src/app/(admin)/admin/calendar/PiecesSection.tsx"` returns 1 or more, and no `formatCivilDate` call renders a piece's date directly in this file.
    - `conforms_to_rule` reaches nothing: `grep -ci "conforms" "src/app/(admin)/admin/calendar/PiecesSection.tsx"` returns 0.
    - No line-up name is rendered: the component's props carry no artist or dj name field — confirmed by reading the prop type; only `part_marker` identifies a LiveCut.
    - `grep -c "depend on the line-up" "src/app/(admin)/admin/calendar/PiecesSection.tsx"` returns 1.
    - No word calls a waiting piece missing: `grep -ciE ">\s*(Missing|Incomplete|Broken)" "src/app/(admin)/admin/calendar/PiecesSection.tsx"` returns 0.
    - No date construction: `grep -cE "new Date|toISOString|toLocaleDateString|Intl\.DateTimeFormat" "src/app/(admin)/admin/calendar/PiecesSection.tsx"` returns 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Every owed piece has a row; every date renders through `PieceDate`; a LiveCut is identified by its marker; a waiting piece is never called missing; `Podcast` appears nowhere.</done>
</task>

<task type="auto">
  <name>Task 3: ChecklistSection — the pieces plus the production steps, with late readable from the row</name>
  <files>src/app/(admin)/admin/calendar/ChecklistSection.tsx</files>
  <read_first>
    - `44-UI-SPEC.md` §9.3 in full — the seven-row contract table: the real checkbox, the
      `htmlFor` label, the tick that warns and never blocks, the late row, the author line,
      the reversibility, and the sentence stating this is not a monotone guard.
    - `src/components/ui/Checkbox.tsx` — the primitive, 16px glyph inside a 44×44 hit area.
    - `44-CONTEXT.md` D-44-14, D-44-15, D-44-16.
    - `.claude/rules/meta-gates.md` §Verifica delle guardie monotone — the three one-way
      switches this phase does not touch.
  </read_first>
  <action>
    Create `src/app/(admin)/admin/calendar/ChecklistSection.tsx` as a client component
    rendering the checklist rows. **It renders and reports; the write is plan 44-12's.** Take
    the tick handler as a prop so this file compiles and renders before the action exists,
    and so the action's refusal copy lives with the action.

    Each row: a real `<input type="checkbox">` through the `Checkbox` primitive, its label
    associated by `htmlFor` and never by a sibling `<div>`, inside a **44 × 44** hit area.

    The five item kinds are the editorial pieces plus the production steps (D-44-14) — venue
    confirmed in writing, dj confirmed, photo arrived, and the exhibition space's approval of
    material where that applies. The checklist covers what can make a date fail, not only
    what can be drawn.

    **Late is computed at render from `ticked_at IS NULL AND due_date < today`**, never read
    from a stored flag — there is no such column, because a stored flag needs a cron to stay
    true and this project has four crons nobody is notified about. Today's civil date is
    passed in as a prop from the server component; this file constructs no `Date`.

    A late row carries `Badge tone="emphasis"` reading `Late` and its due date in `--ink` —
    the same `emphasis` fill the list uses, so the two agree. **`emphasis` is spent on `Late`
    and `Diverged` and on nothing else on this surface.**

    A ticked item shows **who ticked it and when**: `Ticked by <staff> · <date>`, label role,
    `--muted`, the date through `formatCivilDate`.

    **A tick warns but does not block** (D-44-16): ticking raises no dialog, gates nothing,
    and moves no focus. **And a tick is reversible** — unticking re-records the author.
    Write the sentence saying so, next to the code, because the wrong precedent is one
    directory away: unlike `venue_reveal_sent`, nothing leaves the building when a tick is
    made, and `meta-gates.md`'s three one-way switches are untouched by this phase. A tick
    that cannot be undone becomes a lie the first time somebody clicks the wrong row, and a
    checklist nobody trusts is a checklist nobody reads.

    **Two failures, two sentences** (§13.2): `You do not have permission to change this
    checklist.` and `We could not save that tick. The item is unchanged.` These are different
    facts with different next steps and they are never collapsed into one. Render whichever
    the handler returns, in a `role="alert"` region distinguishable from every other message
    on screen. The banned shape is named: the newsletter form's *"Qualcosa è andato storto"*,
    which collapses a network fault, a missing key and a duplicate address into one
    indebuggable message.
  </action>
  <acceptance_criteria>
    - A real checkbox: `grep -c "Checkbox" "src/app/(admin)/admin/calendar/ChecklistSection.tsx"` returns 1 or more, and no `role="checkbox"` div exists.
    - No stored lateness read: `grep -ci "is_late" "src/app/(admin)/admin/calendar/ChecklistSection.tsx"` returns 0.
    - No date construction: `grep -cE "new Date|toISOString|toLocaleDateString|Intl\.DateTimeFormat" "src/app/(admin)/admin/calendar/ChecklistSection.tsx"` returns 0.
    - `emphasis` is spent once: `grep -c "tone=\"emphasis\"" "src/app/(admin)/admin/calendar/ChecklistSection.tsx"` returns 1.
    - Two sentences, not one: `grep -cE "You do not have permission to change this checklist|We could not save that tick" "src/app/(admin)/admin/calendar/ChecklistSection.tsx"` returns 2.
    - No collapsed message: `grep -ciE "something went wrong|qualcosa" "src/app/(admin)/admin/calendar/ChecklistSection.tsx"` returns 0.
    - The alert region exists: `grep -c "role=\"alert\"" "src/app/(admin)/admin/calendar/ChecklistSection.tsx"` returns 1 or more.
    - `npm run build`, `npm run verify:touch-targets` and `npm run verify:tables` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:touch-targets</automated>
  </verify>
  <done>The checklist renders the pieces plus the production steps, computes lateness at render, shows the author of a tick, warns without blocking, is reversible, and keeps its two refusal sentences separate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the `[id]` route parameter → a query | Untrusted input crosses here; it is shape-checked before it reaches the database |
| a plan row → the rendered night page | `venue_word`, every piece date and every checklist label cross here, all behind the capability |
| a piece with no date → the reader | The point at which three different facts could collapse into one blank cell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Elevation of privilege | `/admin/calendar/[id]` | mitigate | the dynamic pattern is bound in the same map entry as its list; the page re-asks the key; the read uses the cookie-bound client so the policy is exercised |
| T-44-30 | Tampering | the `[id]` parameter | mitigate | shape-checked against a UUID pattern before any query; a failing shape is `notFound()`, not a query |
| T-44-20 | Repudiation | a proposed date read as settled | mitigate | every date goes through `PieceDate`'s five-variant union; the residual is a human judgement, booked as a written check with `Result: pending` in plan 44-13 |
| T-44-31 | Information disclosure | a LiveCut's identity | mitigate | the part marker renders, a person's name does not; the prop type carries no name field, so the association cannot be made by accident |
| T-44-28 | Repudiation | a failed read on a detail page | mitigate | three outcomes, never two; a failed read renders its own sentence and never `notFound()`, which on a page reached from a list reads as *the night was deleted* |
| T-44-32 | Repudiation | a checklist tick | mitigate | the author and the timestamp render on the row; the tick is reversible and says so, so nobody borrows the monotone-guard precedent from the neighbouring directory |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no package |
</threat_model>

<verification>
- `npm run build`, `npm run verify:routes`, `npm run verify:tables`,
  `npm run verify:touch-targets`, `npm run verify:dialogs`, `npm run verify:breakpoints`
  all exit 0.
- The grep assertions on each task.
- **What a green does NOT mean:** nothing here says the page refuses a door-assigned staff
  session, and nothing here says a proposal reads as unsettled. Both are written procedures
  in plan 44-13, and both carry `Result: pending` until a person runs them.
</verification>

<success_criteria>
- `/admin/calendar/[id]` serves one night behind the same key, with `PIECES` above
  `CHECKLIST`.
- Every piece date renders through `PieceDate` and through nothing else.
- `Podcast` appears nowhere; a LiveCut is identified by its marker; no name is rendered.
- Lateness is computed at render; the tick warns, does not block, and is reversible.
- The two refusal sentences stay two.
- No `Date` is constructed anywhere under the calendar surface.
</success_criteria>

<output>
Create `.planning/phases/44-the-production-calendar-comes-inside/44-11-SUMMARY.md` when done.
</output>
