---
phase: 58-il-calendario-e-uno-specchio
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-PROCEDURES.md
autonomous: true
requirements: [ICS-01, ICS-03, ICS-03b, ICS-05, ICS-07]

must_haves:
  truths:
    - "P-58-C, la procedura di ripristino, esiste PRIMA che qualunque piano possa lanciare un --apply"
    - "P-58-A e P-58-B esistono, portano ognuna la propria autorizzazione datata, e ogni Result e' pending"
    - "La finestra massima dell'aggancio (ICS-05) e' MISURATA sul catalogo vivo, non scelta"
    - "Le misure d'apertura sono prese PRIMA che lo specchio cancelli le righe su cui si misurano"
    - "I nomi dei vincoli CHECK sono letti da pg_constraint, mai dedotti"
  artifacts:
    - path: ".planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-PROCEDURES.md"
      provides: "P-58-A (la spunta sopravvive), P-58-B (il legame sopravvive), P-58-C (il ripristino)"
      contains: "P-58-C"
  key_links:
    - from: "58-PROCEDURES.md"
      to: ".planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md"
      via: "stessa forma: frontmatter-contratto, passi numerati, ruolo per passo, Result pending"
      pattern: "Result: pending"
---

<objective>
Scrivere le **tre procedure manuali** della fase e prendere le **misure
d'apertura** che lo specchio distruggera'.

Purpose: due ragioni d'ordine, entrambe irreversibili se sbagliate.
**(1)** `P-58-C` e' l'unico piano di rientro che esistera': non c'e' PITR, il
client Supabase non apre transazioni, e con il cron di `ICS-10` un processo puo'
morire fra la cancellazione e la riscrittura **di notte, senza nessuno che
guardi**. Va scritta **prima del primo `--apply`**, non dopo il primo incidente.
**(2)** La finestra massima dell'aggancio di `ICS-05` **si misura**, e l'unica
fonte su cui e' misurabile sono i 46 pezzi gia' agganciati in produzione — che
il primo specchio cancella. Misurarla dopo significa non misurarla piu'.

Output: `58-PROCEDURES.md` con tre procedure e ogni `Result` a `pending`; e un
blocco di misure nel SUMMARY, in numeri e nomi di vincolo, **senza una sola
parola di sede, una data o un nome di persona**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-CONTEXT.md
@.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-VALIDATION.md
@.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-RESEARCH.md
</context>

<constraints>
⚠ **`.planning/` e' tracciato e il repository e' PUBBLICO.** In `58-PROCEDURES.md`
e nel SUMMARY: **ruoli, mai persone**; nessuna sede, nessuna data di serata,
nessuna line-up. Un passo dice *«apri il calendario e leggi la prima riga»*, mai
cosa dice quella riga.

⚠ **`ticked_by_name` e' un nome di persona.** La migration lo vieta
esplicitamente in `.planning/` (`20260815120000_production_calendar.sql:706-713`).
Le procedure scrivono *«l'autore e' invariato»*, **mai il nome**.

⚠ **`party_series.ics_alias` porta parole per spazi.** Se ne conta il numero, non
se ne stampa mai un valore.

⚠ Tutte le letture di questo piano passano dalla Management API con
`read_only: true` (`scripts/rls-baseline.mjs:261-303`). **Nessuna scrittura in
produzione avviene in questo piano.**
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Le misure d'apertura, prese prima che lo specchio le cancelli</name>
  <files>(nessun file di prodotto — le misure atterrano nel SUMMARY di questo piano)</files>
  <read_first>
    - `scripts/rls-baseline.mjs` righe 150-303 (`loadEnvironment`, `registerSecret`, `redact`, `createManagementApiTarget`, `read_only`)
    - `supabase/migrations/20260815120000_production_calendar.sql` — le `CREATE TABLE` di `production_plan`, `production_piece`, `production_commitment`, `production_checklist_item`, `production_pipeline_rule`, `production_import_run`
    - `.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-RESEARCH.md` § *Assumptions Log* (A5) e § *ICS-05* (l'asimmetria fra derivare e agganciare) e § *ICS-07* (la query discriminante)
  </read_first>
  <action>
Eseguire **cinque letture** dal catalogo vivo, con `read_only: true`, e
registrarne l'esito nel SUMMARY. Sono letture, non scritture: nessun `--apply`,
nessun `INSERT`, nessun `DELETE`.

**M1 — la rimisura di A5.** `count(*)` su `production_checklist_item` con
`ticked_at IS NOT NULL`, e `count(*)` su `production_plan` con
`linked_party_id IS NOT NULL`. `58-RESEARCH.md` A5 dice zero e zero, misurati dal
proprietario e **non rimisurati dalla ricerca**. Se anche uno solo e' diverso da
zero, il riaggancio di `ICS-03` smette di essere una precauzione e diventa un
percorso critico con dati veri al primo giro: **scriverlo a caratteri grandi nel
SUMMARY**, perche' cambia il peso di `P-58-A` e `P-58-B`.

**M2 — la finestra massima dell'aggancio (`ICS-05`, domanda aperta 1).** Questa
misura chiude una delle tre domande che il piano deve chiudere, e la chiude con
un dato invece che con una scelta. Per ogni riga di `production_piece` che ha
`plan_id` non nullo **e** `date` non nulla, unita al suo `production_plan`,
calcolare la distanza in giorni fra la data del pezzo e la data del piano.
Riportare, **per coppia (serie, tipo di pezzo)**: quante righe, la distanza
massima e la distanza minima. Riportare poi il **massimo assoluto**.
Queste righe sono il **campione di verita'** della fase: sono i pezzi che il file
numerava, quindi il loro aggancio non e' dedotto — e' scritto.
Il numero che serve al piano 58-03 e' il massimo per (serie, tipo), non un unico
valore globale: una regola `next_edition` vive su una scala diversa da una
`self`. Riportare entrambe le forme e dichiarare quale il piano 58-03 dovra'
adottare.
⚠ **Se il campione e' troppo piccolo perche' una coppia abbia righe**, quella
coppia **non ha una finestra misurata**, e per essa la risposta corretta resta il
**rifiuto** (nessun aggancio). Dichiararlo per nome, coppia per coppia: una
tabella con delle celle vuote e' un dato; una finestra inventata per riempirle
non lo e'.

**M3 — la mappa alias, contata e non letta.** `count(*)` su `party_series` con
`ics_alias IS NOT NULL`. **Solo il conteggio.** `58-RESEARCH.md` § *Runtime State
Inventory* segnala che quella mappa vive **solo** nel database, non e' in git,
non e' in un seed, e nessun file la ricostruisce: se sparisse, l'import
tornerebbe a zero notti. Il numero e' il promemoria che quella riga esiste.

**M4 — i nomi dei vincoli, letti da `pg_constraint`.** `conname` di tutti i
vincoli `CHECK` (`contype = 'c'`) su `production_piece` e su
`production_pipeline_rule`. Servono ai piani 58-06 e 58-07, e
`20260808000500_staff_role.sql:34-40` registra perche' non si deducono: un nome
dedotto sbagliato produce un `DROP CONSTRAINT IF EXISTS` che **non fa nulla in
silenzio**, seguito da un `ADD CONSTRAINT` che fallisce per duplicato.

**M5 — il discriminante di `ICS-07`.** Interrogare `production_import_run`
cercando gli `id` il cui testo contiene un gruppo che l'audit legge come un anno
— la regex della ricerca e' `(^|-)(19|20)[0-9]{2}(-|$)` applicata a `id::text` —
e riportarne **il numero, mai il valore**. Poi la stessa regex sugli `id` delle
righe con `absent_since` non nullo delle tre tabelle dello specchio. L'esito
dice **quale delle tre righe candidate** ha fatto scattare l'audit: se il run
incriminato e' fra i primi, e' la riga `:1209`; altrimenti e' `:1117` o `:1103`.
Il piano 58-05 riparera' comunque tutte e tre, ma con la misura davanti invece
che dedotta.

**Redazione.** Ogni riga stampata passa da `say()` con `registerSecret` gia'
impostato su URL, host e chiave, come fa `rls-baseline.mjs`. Nel SUMMARY vanno
**conteggi, distanze in giorni e nomi di vincolo**. Non vanno: valori di
`ics_alias`, valori di `ticked_by_name`, `source_uid`, titoli, date di serata.
  </action>
  <verify>
    <automated>node -e "process.exit(0)" # le misure si verificano rileggendo il catalogo: vedi acceptance_criteria</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY riporta M1 come due numeri, con la data della lettura
    - Il SUMMARY riporta M2 come una tabella `(serie, tipo) → righe · distanza min · distanza max`, piu' il massimo assoluto, **e nomina le coppie senza righe come «non misurata → rifiuto»**
    - Il SUMMARY riporta M3 come un solo numero e **zero valori di alias**
    - Il SUMMARY riporta M4 come l'elenco dei `conname` letti, e dichiara che sono stati letti da `pg_constraint` e non dedotti
    - Il SUMMARY riporta M5 come conteggi, e nomina quale delle tre righe candidate di `ICS-07` risulta la causa
    - Nessuna delle cinque query esegue una scrittura: ognuna passa con `read_only: true`
    - Il SUMMARY non contiene: alcun valore di `ics_alias`, alcun `ticked_by_name`, alcun `source_uid`, alcuna data di serata, alcun nome di sede
  </acceptance_criteria>
  <done>Le cinque misure sono prese e scritte. La finestra dell'aggancio esiste come dato, con le sue celle vuote dichiarate; i nomi dei vincoli esistono come lettura; la causa di `ICS-07` non e' piu' una deduzione.</done>
</task>

<task type="auto">
  <name>Task 2: Le tre procedure — P-58-A, P-58-B e, prima di tutte, P-58-C</name>
  <files>.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-PROCEDURES.md</files>
  <read_first>
    - `.planning/phases/44-the-production-calendar-comes-inside/44-PROCEDURES.md` — il frontmatter-contratto (righe 1-10), le quattro regole di lettura (14-39), «Come si scrive un passo» (43-55), il preambolo di P3 che scrive in produzione (256-276), i passi 33-36 dell'istantanea sulla cascata (282-300), e P4.1 (415-430), che e' quasi letteralmente `P-58-A`
    - `.claude/rules/ai-engineering.md` — gate dell'istantanea, gate della rimozione per chiave, gate dell'autorizzazione a scrivere in produzione
    - `supabase/migrations/20260815120000_production_calendar.sql` — la cascata `production_checklist_item.plan_id → production_plan(id) ON DELETE CASCADE` (righe 648-661) e il divieto su `ticked_by_name` (706-713)
    - `.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-RESEARCH.md` § *ICS-01* (l'ordine di cancellazione) e § *ICS-03* (la procedura in sette passi e il punto esatto dove puo' perdere tutto)
  </read_first>
  <action>
Creare `58-PROCEDURES.md` sulla forma esatta di `44-PROCEDURES.md`: frontmatter
che e' un contratto (`phase`, `written`, `status: all pending`, `closes`,
`accounts` espressi come **ruoli**, `authorisation`, `phase_closes`), le regole
di lettura riportate in sostanza, e ogni passo numerato con il ruolo che lo
esegue e una riga `Result: pending`.

**Ordine nel file: `P-58-C` per prima**, con una riga in testa che dice perche':
e' l'unica delle tre che deve esistere prima che chiunque lanci un `--apply`, e
un lettore che scorre il file deve incontrarla prima delle altre due.

**`P-58-C` — il ripristino dopo uno specchio interrotto.** Precondizione: lo
specchio e' morto fra la cancellazione e la riscrittura. Passi, con il ruolo:
(1) **non rilanciare** l'import e non premere nulla sulla superficie — il primo
gesto sbagliato e' un secondo giro che cancella cio' che il primo aveva scritto a
meta'; (2) leggere dal **catalogo** (Management API, `read_only: true`) i
conteggi per chiave di calendario delle tre tabelle specchiate piu' la checklist
— e' la fonte diversa dallo strumento che ha causato l'effetto; (3) localizzare
l'**istantanea** che l'importatore ha scritto prima della cancellazione, nella
directory ignorata, e verificarne l'ora rispetto all'ora della corsa; (4)
rilanciare l'import con **la stessa chiave di calendario** e la stessa sorgente:
lo specchio e' idempotente per costruzione — cancella cio' che e' nello scopo e
riscrive l'intero contenuto del file, quindi una seconda corsa completa
ripristina tutto cio' che il file porta; (5) ripristinare **spunte e legami**
dall'istantanea con il percorso di ripristino dedicato, **non** con
`record_checklist_tick`, che riattribuirebbe la spunta a chi ha lanciato
l'import (Pitfall 8); (6) rileggere i conteggi dal catalogo e confrontarli con
il passo 2; (7) scrivere nel `Result` **cosa e' stato osservato**, incluso il
caso in cui l'istantanea non esistesse — che e' il ritrovamento peggiore e va
scritto per esteso invece che riprovato.
Chiudere `P-58-C` con la riga che manca oggi nello script: **le due righe di
consiglio di `failPartway` diventano false con lo specchio** — «rilancia con
`--dry-run`, il riconciliatore pianifica solo cio' che il primo giro non ha
finito» descriveva un aggiornamento campo per campo, non una cancellazione. Il
piano 58-09 le sostituira' con un rimando a questa procedura; qui si dichiara che
questa procedura e' il rimando.

**`P-58-A` — una spunta sopravvive a un import.** Sul modello di `44-PROCEDURES`
P4.1. Preambolo **⚠ QUESTA PROCEDURA SCRIVE IN PRODUZIONE** con la propria
autorizzazione datata, che non viaggia con quella delle altre. Passi: aprire la
superficie del calendario con il ruolo che possiede la chiave di sezione;
spuntare una voce; osservare che la casella resta spuntata e che compare la riga
d'autore; leggere dal catalogo `ticked_by` e `ticked_at` di quella voce e
**annotarne l'identificativo, non il nome**; lanciare l'import con `--apply` per
quella chiave di calendario; rileggere la stessa voce **per la chiave stabile**
`(production_plan.source_uid, kind, label)` e osservare che `ticked_at` e
`ticked_by` sono **gli stessi di prima** e non quelli di chi ha lanciato
l'import. Prima di tutto questo, i passi 0: l'autorizzazione datata, l'istantanea
su **ogni tabella raggiungibile per cascata** — enumerata **rileggendo i vincoli
al momento**, non copiata da qui — e la cattura degli identificativi delle righe
create, che e' l'unica lista che una eventuale rimozione potra' consultare, per
chiave primaria.

**`P-58-B` — un legame con una serata pubblicata sopravvive a un import.** Sul
modello di P3. Stessa struttura di autorizzazione e istantanea. Passi: creare
il legame fra una riga di piano e una serata; annotarne gli identificativi;
lanciare l'import con `--apply`; osservare che `linked_party_id` e' ancora li'.
Poi il caso di `ICS-03b` (D-58-02): lanciare l'import con un file di prova che
**non porta piu'** quel `source_uid` e osservare che **la riga di piano c'e'
ancora** e che il referto **conta** le righe sopravvissute a un'assenza.
E il caso di `ICS-01b` (D-58-01): lanciare l'import con un file di prova che
porta, per lo stesso `source_uid`, un **progressivo diverso**, e osservare che
l'import **rifiuta** con uscita `2`, nomina la serata e i due numeri, e
**non scrive niente** — verificato rileggendo i conteggi dal catalogo. Poi
rilanciarlo con l'**argomento di riautorizzazione esplicita** e osservare che
passa e che la riautorizzazione **compare nel referto**.
Chiudere con la rimozione **per chiave primaria** dalla lista catturata prima, e
il conteggio di controllo chiesto a una fonte diversa da quella su cui si e'
agito.

Ogni `Result` del file resta `pending`. Il file non nomina persone, sedi, date
di serata o line-up.
  </action>
  <verify>
    <automated>/usr/bin/grep -c "Result: pending" .planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-PROCEDURES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-PROCEDURES.md` esiste e contiene le tre intestazioni `P-58-C`, `P-58-A`, `P-58-B`, **in quest'ordine**
    - `/usr/bin/grep -c "Result: pending"` restituisce un numero pari al numero dei passi, e `/usr/bin/grep -cE "Result: (done|ok|passed)"` restituisce `0`
    - `P-58-A` e `P-58-B` portano ciascuna un preambolo che dichiara **⚠ QUESTA PROCEDURA SCRIVE IN PRODUZIONE** e una riga di autorizzazione datata propria, che non e' quella dell'altra
    - `P-58-A` e `P-58-B` contengono un passo che enumera la cascata **rileggendo i vincoli**, e un passo che cattura gli identificativi **al momento della creazione**
    - `P-58-B` contiene il caso `ICS-03b` (la riga con un legame non se ne va, il referto la conta) **e** i due casi di `ICS-01b` (rifiuto con uscita 2 e zero scritture; poi riautorizzazione esplicita registrata nel referto)
    - `P-58-C` contiene il passo «non rilanciare», il passo di lettura dei conteggi dal **catalogo**, il passo dell'istantanea, il passo di ripristino che **non** usa `record_checklist_tick`, e il passo di riconferma da una fonte diversa
    - Il file non contiene alcun nome di persona, alcuna sede, alcuna data di serata: `/usr/bin/grep -cE "(19|20)[0-9]{2}"` sul file restituisce `0` salvo le date di scrittura del frontmatter, che vanno dichiarate come tali
    - `npm run verify:persona` esce `0` (il file sta in `.planning/`, non in `.claude/`, ma il controllo F verifica che nulla di privato sia entrato nel repo)
  </acceptance_criteria>
  <done>Le tre procedure esistono, ogni `Result` e' `pending`, e `P-58-C` sta in testa al file perche' e' quella che deve esistere prima di tutte le altre.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalogo di produzione → SUMMARY tracciato | Una lettura che stampa un valore invece di un conteggio pubblica materiale su un repo pubblico |
| Management API → processo locale | Credenziali service-role / access token nell'ambiente |
| procedura scritta → repo pubblico | Un passo che nomina una sede o una persona e' una pubblicazione irreversibile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-01 | Information disclosure | M3 (`party_series.ics_alias`) | mitigate | Solo `count(*)`: nessun valore di alias raggiunge stdout ne' il SUMMARY. Criterio di accettazione esplicito |
| T-58-02-02 | Information disclosure | M1/M5 (`ticked_by_name`, `source_uid`, `absent_since`) | mitigate | Conteggi e nomi di vincolo soltanto; nessun identificativo, nessun nome, nessuna data |
| T-58-02-03 | Information disclosure | `58-PROCEDURES.md` | mitigate | Ruoli mai persone; un passo dice *leggi la prima riga*, mai cosa dice quella riga. Precedente di forma: `44-PROCEDURES.md` regola (b) |
| T-58-02-04 | Tampering | Management API | mitigate | Ogni query passa con `read_only: true`, che rifiuta un `INSERT` con `25006` — garanzia dura, non convenzione (`rls-baseline.mjs:64-66`) |
| T-58-02-05 | Information disclosure | log e messaggi d'errore | mitigate | `registerSecret` su URL, host e token prima di qualunque `say()`, come `rls-baseline.mjs:158-176` |
| T-58-02-SC | Tampering | npm install | accept | Nessun pacchetto nuovo in tutta la fase |
</threat_model>

<verification>
- Le cinque misure sono nel SUMMARY, in numeri
- `58-PROCEDURES.md` esiste, con tre procedure e ogni `Result` a `pending`
- `npm run build` esce `0` (nessun file di prodotto e' stato toccato)
- `npm run verify` esce `0`
</verification>

<success_criteria>
Il piano di rientro esiste prima della prima cancellazione. La finestra
dell'aggancio esiste come misura, con le sue celle vuote dichiarate come rifiuti
invece che riempite. I nomi dei vincoli sono letti. La causa di `ICS-07` e' un
fatto invece che una delle tre ipotesi.
</success_criteria>

<output>
Create `.planning/phases/58-il-calendario-si-legge-come-lo-si-scrive/58-02-SUMMARY.md` when done
</output>
