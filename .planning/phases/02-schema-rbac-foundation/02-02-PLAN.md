---
phase: 02-schema-rbac-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/rbac/roles.ts
  - src/lib/supabase/middleware.ts
  - src/components/layout/MobileNav.tsx
  - src/app/(members)/dashboard/page.tsx
  - src/app/(public)/events/[slug]/page.tsx
autonomous: true
requirements: [ROLE-04, ROLE-06]

must_haves:
  truths:
    - "Unauthenticated users are redirected to /login when accessing /dashboard, /membership-card, /attendance, /admin/*, /organizer/*"
    - "Pending/rejected members are redirected away from /membership-card and /attendance but can access /dashboard and /events"
    - "Only master role can access /admin/members"
    - "Both master and organizer roles can access /admin/scanner"
    - "Only master and organizer can access /organizer/* routes"
    - "Pending members see events-only navigation in MobileNav (Home, Events, Members/dashboard only)"
    - "Approved members see full navigation including Gallery and Members"
    - "Master sees an Admin nav item linking to /admin/members"
    - "Organizer sees an Organizer nav item linking to /organizer/members"
    - "Dashboard shows 'Your account is pending approval' message for pending members instead of the full dashboard cards"
    - "RSVP button on event detail page does NOT render for pending/rejected members -- section is absent, not grayed out"
  artifacts:
    - path: "src/lib/rbac/roles.ts"
      provides: "Role constants, type exports, route protection map, navigation filter function"
      exports: ["UserRole", "UserStatus", "ROUTE_ACCESS", "getVisibleNavItems"]
    - path: "src/lib/supabase/middleware.ts"
      provides: "Role-aware middleware with profile query, header injection, route protection"
      contains: "x-user-role"
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Role-aware bottom navigation that filters items by role and status"
      contains: "role"
    - path: "src/app/(members)/dashboard/page.tsx"
      provides: "Dashboard with pending member state display"
      contains: "pending"
    - path: "src/app/(public)/events/[slug]/page.tsx"
      provides: "Event detail page with RSVP section hidden for pending/rejected members"
      contains: "x-user-status"
  key_links:
    - from: "src/lib/supabase/middleware.ts"
      to: "profiles table"
      via: "supabase.from('profiles').select('role, status').eq('id', user.id).single()"
      pattern: "select.*role.*status"
    - from: "src/lib/supabase/middleware.ts"
      to: "route protection"
      via: "inlined route checks (not imported from roles.ts -- Edge runtime minimizes imports)"
      pattern: "pathname\\.startsWith.*admin|organizer"
    - from: "src/components/layout/MobileNav.tsx"
      to: "x-user-role header"
      via: "props passed from parent Server Component"
      pattern: "role.*UserRole"
    - from: "src/app/(members)/dashboard/page.tsx"
      to: "x-user-role header"
      via: "headers() API reading x-user-role and x-user-status"
      pattern: "headers.*x-user-role"
    - from: "src/app/(public)/events/[slug]/page.tsx"
      to: "x-user-status header"
      via: "headers() API reading x-user-status to conditionally render RSVP"
      pattern: "headers.*x-user-status"
---

<objective>
Implement middleware-level role enforcement, RBAC constants module, role-aware navigation, pending member dashboard state, and hide RSVP buttons for non-approved members.

Purpose: This plan wires the database role/status (from Plan 01) into the application layer. Middleware becomes the first line of defense for route access, and the UI adapts to show each role only what they should see. This is the bridge between data (Plan 01) and user-facing features (Plan 03).

Output: Working middleware with role resolution, RBAC constants module, role-filtered MobileNav, status-aware dashboard, and RSVP visibility gating on event detail page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schema-rbac-foundation/02-CONTEXT.md
@.planning/phases/02-schema-rbac-foundation/02-RESEARCH.md
@.planning/phases/02-schema-rbac-foundation/02-01-SUMMARY.md
@src/lib/supabase/middleware.ts
@src/middleware.ts
@src/components/layout/MobileNav.tsx
@src/app/(members)/dashboard/page.tsx
@src/app/(public)/events/[slug]/page.tsx
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RBAC constants module and middleware role enforcement</name>
  <files>src/lib/rbac/roles.ts, src/lib/supabase/middleware.ts</files>
  <action>
  **Create src/lib/rbac/roles.ts:**
  Central RBAC constants module. Export:
  - Re-export `UserRole` and `UserStatus` types from `@/types/database` (single source of truth)
  - `ROLES` constant object: `{ MASTER: "master", ORGANIZER: "organizer", MEMBER: "member" } as const`
  - `STATUSES` constant object: `{ PENDING: "pending", APPROVED: "approved", REJECTED: "rejected" } as const`
  - Navigation items array with role/status requirements per item (see MobileNav task for shape)
  - `getVisibleNavItems(role: UserRole | null, status: UserStatus | null)` function that filters nav items based on role and status. Logic per CONTEXT.md:
    - Pending/rejected: only see Home, Events, and Dashboard (dashboard shows pending state)
    - Approved member: see Home, Events, Gallery, Members (dashboard)
    - Organizer (approved): same as member PLUS "Organizer" item linking to /organizer/members
    - Master (approved): same as member PLUS "Admin" item linking to /admin/members
    - Unauthenticated (role=null): see Home, Events, Gallery only (no Members/dashboard)

  **Rewrite src/lib/supabase/middleware.ts:**
  Enhance `updateSession()` to resolve user role and enforce route access. Key changes:

  1. After `supabase.auth.getUser()`, if user exists, query their profile: `supabase.from("profiles").select("role, status").eq("id", user.id).single()`. Extract role (default "member") and status (default "pending").

  2. **Header injection:** Create new request headers containing `x-user-role`, `x-user-status`, and `x-user-id`. Create the response with these headers: `NextResponse.next({ request: { headers: requestHeaders } })`.

  3. **CRITICAL: Cookie preservation.** After creating the new response with injected headers, the Supabase cookies are lost because a new response object was created. Fix: Track all cookies set by Supabase's `setAll` callback in a local array. After creating the final response with headers, re-apply ALL tracked cookies to that response. This prevents users from being logged out on every navigation.

  Implementation approach for cookie preservation:
  ```
  let pendingCookies: Array<{name, value, options}> = []
  // In setAll callback: push to pendingCookies AND set on current response
  // After creating final response with headers: iterate pendingCookies and set on final response
  ```

  4. **Route protection logic** (order matters):
     - If user is NOT authenticated:
       - Protected routes (`/dashboard`, `/membership-card`, `/attendance`, `/admin`, `/organizer`) -> redirect to `/login?redirect={pathname}`
       - All other routes -> allow
     - If user IS authenticated:
       - `/admin/scanner` -> allow if role is master OR organizer; otherwise redirect to `/dashboard`
       - `/admin/*` (except scanner) -> allow only if role is master; otherwise redirect to `/dashboard`
       - `/organizer/*` -> allow if role is master OR organizer; otherwise redirect to `/dashboard`
       - `/membership-card`, `/attendance` -> allow only if status is "approved"; otherwise redirect to `/dashboard`
       - All other routes -> allow

  5. Do NOT import from `src/lib/rbac/roles.ts` in middleware (middleware runs in Edge runtime and should minimize imports). Inline the route checks directly as string comparisons and `startsWith` calls.

  6. Keep the existing Supabase client creation pattern and matcher config in `src/middleware.ts` (do not modify that file).
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -5</automated>
    <manual>Verify middleware.ts injects x-user-role/x-user-status headers and has route protection for /admin, /organizer, and approved-only routes. Verify cookie preservation pattern is present.</manual>
  </verify>
  <done>Middleware resolves user role/status from profiles table, injects x-user-role and x-user-status headers, protects routes based on role/status matrix, and preserves Supabase cookies when creating the header-injected response. RBAC module exports role constants and nav filtering function. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Role-aware navigation, pending member dashboard, and RSVP visibility gating</name>
  <files>src/components/layout/MobileNav.tsx, src/app/(members)/dashboard/page.tsx, src/app/(public)/events/[slug]/page.tsx</files>
  <action>
  **Rewrite src/components/layout/MobileNav.tsx:**
  The component needs to receive role and status as props (it is a client component, cannot read headers directly). Changes:

  1. Add props interface: `{ role: UserRole | null; status: UserStatus | null }` (import types from `@/types/database`).
  2. Import `getVisibleNavItems` from `@/lib/rbac/roles` to filter navigation items.
  3. Define the full nav items list with role/status requirements:
     - Home ("/", icon: home) -- visible to all
     - Events ("/events", icon: calendar) -- visible to all
     - Gallery ("/gallery", icon: image) -- visible to approved members and above (NOT pending/rejected)
     - Members ("/dashboard", icon: user) -- visible to authenticated users
     - Admin ("/admin/members", icon: shield) -- visible to master only
     - Organizer ("/organizer/members", icon: users) -- visible to organizer only
  4. Use `getVisibleNavItems(role, status)` to filter which items to render.
  5. Add a "shield" SVG icon (for Admin) and a "users" SVG icon (for Organizer) to the icons map. Use Heroicons outline style consistent with existing icons (24x24 viewBox, stroke, strokeWidth 1.5).
  6. Keep existing active state highlighting logic and Tailwind classes unchanged.
  7. Per CONTEXT.md locked decision: pending/rejected members see events-only navigation -- Home, Events, and Dashboard only. Gallery is hidden for pending/rejected.

  **Update all pages that render MobileNav:**
  Every page that currently renders `<MobileNav />` must now pass role and status props. The pages are Server Components and can read the headers set by middleware.

  For `src/app/(members)/dashboard/page.tsx`:
  1. Import `headers` from `next/headers`.
  2. Read `x-user-role` and `x-user-status` from headers.
  3. Pass as props to MobileNav: `<MobileNav role={role} status={status} />`.
  4. Add pending member state: if status is "pending" or "rejected", show a different dashboard view:
     - Header still shows "Hey, {fullName}"
     - Below header: a card with message "Your account is pending approval" (for pending) or "Your account has been reviewed" (for rejected)
     - The card text: "You can browse events while you wait. Once approved, you'll have full access to membership features."
     - Do NOT show the Membership Card, Attendance, or RSVP cards for pending/rejected -- simply omit them (per CONTEXT.md: section simply absent, not grayed out)
     - Still show a "Discover events" link
  5. For approved members, show the existing dashboard content unchanged.

  **Update other pages with MobileNav:**
  Search the codebase for all files importing MobileNav. Each must be updated to read headers and pass role/status props. Common pattern for Server Components:
  ```typescript
  import { headers } from "next/headers"
  import type { UserRole, UserStatus } from "@/types/database"
  // ...
  const headersList = await headers()
  const role = (headersList.get("x-user-role") as UserRole) || null
  const status = (headersList.get("x-user-status") as UserStatus) || null
  // ...
  <MobileNav role={role} status={status} />
  ```
  Apply this pattern to ALL pages that use MobileNav. This includes `src/app/(public)/events/[slug]/page.tsx` (see below), `src/app/page.tsx` (homepage), event listing pages, gallery page, etc.

  **Hide RSVP button for pending/rejected members on event detail page:**
  In `src/app/(public)/events/[slug]/page.tsx`:
  1. Import `headers` from `next/headers` and read `x-user-status` from the middleware-injected headers.
  2. Read `x-user-role` as well (needed for MobileNav props).
  3. Replace the existing `isMember` check (`!!user`) with a more nuanced approach:
     - `isAuthenticated = !!user`
     - `isApproved = status === "approved"`
  4. In the RSVP section (the `{/* RSVP */}` block around line 120-140), change the conditional render:
     - If `isAuthenticated && isApproved`: show the RSVP button (existing behavior for approved members)
     - If `isAuthenticated && !isApproved` (pending/rejected): do NOT render the RSVP section at all -- omit entirely per CONTEXT.md locked decision ("RSVP and ticket buttons do NOT render for pending/rejected members -- section simply absent, not grayed out")
     - If not authenticated: show the existing "Sign up to confirm attendance" link to /register
  5. The secret location section can stay as-is (showing for any authenticated user is fine -- pending members can see "You'll receive the address 24h before").
  6. Pass role/status props to the MobileNav at the bottom of the page.

  This is a ~5-line conditional render change in the RSVP section plus the standard MobileNav props update.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -5</automated>
    <manual>Check that MobileNav accepts role/status props. Check that dashboard renders pending state when status is pending. Check that all pages passing MobileNav provide role/status props. Check that event detail page hides RSVP button for pending/rejected users.</manual>
  </verify>
  <done>MobileNav renders different navigation items based on user role and status. Pending/rejected users see only Home, Events, and Dashboard. Master sees Admin link. Organizer sees Organizer link. Dashboard shows "pending approval" message for pending members and hides member-only cards. Event detail page hides RSVP section for pending/rejected members. All pages using MobileNav pass role/status from headers. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes with 0 errors
- `src/lib/rbac/roles.ts` exists and exports `getVisibleNavItems`, `ROLES`, `STATUSES`
- `src/lib/supabase/middleware.ts` queries profiles for role/status and injects headers
- `src/lib/supabase/middleware.ts` has route protection logic for /admin, /organizer, approved-only routes
- `src/lib/supabase/middleware.ts` does NOT import from `src/lib/rbac/roles.ts` (route checks are inlined)
- `src/components/layout/MobileNav.tsx` accepts role and status props
- `src/app/(members)/dashboard/page.tsx` reads x-user-role header and shows pending state
- `src/app/(public)/events/[slug]/page.tsx` reads x-user-status header and hides RSVP for pending/rejected
- No page renders `<MobileNav />` without role/status props (grep for `<MobileNav` should show props on every instance)
</verification>

<success_criteria>
Middleware enforces role-based route protection. Unauthenticated users cannot access protected routes. Pending members can access events and dashboard but not membership card or attendance. Master and organizer roles can access their respective admin sections. Navigation adapts to show only relevant items per role. Dashboard displays pending approval message for pending members. RSVP button on event detail page is hidden for pending/rejected members.
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-rbac-foundation/02-02-SUMMARY.md`
</output>
