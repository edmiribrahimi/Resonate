---
phase: 52.1
slug: chiusura-del-debito-della-fase-52
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-23
---

# Phase 52.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | nessun test runner (CLAUDE.md, Guardrail 1): i gate sono `npm run build`, `npm run verify` (VERIFY_OK, 24 gate), `npm run verify:persona`, `verify:media-strip`, `verify:capabilities`, `verify:refusal`, piu' procedure manuali sul laboratorio |
| **Config file** | `package.json` (script `verify*`), `scripts/verify-*.mjs` |
| **Quick run command** | `npm run build && npm run verify:media-strip` |
| **Full suite command** | `npm run build && npm run verify && npm run verify:persona` |
| **Estimated runtime** | ~52.1 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm run build && npm run verify:media-strip`
- **After every plan wave:** Run `npm run build && npm run verify && npm run verify:persona`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 52.1 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 52.1-01-01 | 01 | 1 | REQ-{XX} | T-52.1-01 / — | {expected secure behavior or "N/A"} | unit | `{command}` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `{tests/test_file.py}` — stubs for REQ-{XX}
- [ ] `{tests/conftest.py}` — shared fixtures
- [ ] `{framework install}` — if no framework detected

Existing infrastructure covers all phase requirements — nessun framework da installare: in un repo senza test runner la «Wave 0» e' la scrittura delle procedure manuali (`52.1-PROCEDURES.md`) PRIMA del codice, come nella fase 52. Il pianificatore compila la mappa per task dalla sezione «Validation Architecture» di `52.1-RESEARCH.md`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| {behavior} | REQ-{XX} | {reason} | {steps} |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 52.1s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** {pending / approved YYYY-MM-DD}
