# Phase 52.1: Chiusura del debito della fase 52 - Pattern Map

**Mapped:** 2026-09-23
**Files analyzed:** 34 (10 nuovi, 24 modificati — persona inclusa)
**Analogs found:** 31 / 34

> **Repo PUBBLICO.** Questo file non porta UUID, chiavi di oggetto, URL di media,
> nomi di persone ne' il ref del laboratorio. Dove un analogo scrive in chiaro il
> ref di produzione (`purge-media-orphans.mjs:179`, `restrip-event-media.mjs:197`,
> `seed-lab-media.mjs:93`, `probe-event-media-lab.mjs:71`) qui si cita la **riga**,
> non il valore.

---

## File Classification

| File nuovo/modificato | Ruolo | Flusso dati | Analogo piu' vicino | Qualita' |
|---|---|---|---|---|
| `src/lib/media/strip-video.ts` (NUOVO) | utility server-only | transform (bytes→bytes) | `src/lib/media/strip-metadata.ts` | exact |
| `src/lib/media/strip-metadata.ts` (ramo HEIC, `OUTPUT_MIME`) | utility server-only | transform | se stesso (`:206-220`, `:289-330`) | exact |
| `src/lib/media/finalize.ts` (fork immagine/HEIC/video, chiave finale) | service | file-I/O (download→strip→upload) | se stesso (`:283-374`) | exact |
| `src/app/api/media/finalize/route.ts` (ponte tolto, categorie nuove, `path` finale) | route | request-response | se stesso (`:228-315`, `:550-634`) | exact |
| `src/lib/media/upload-limits.ts` (`MAX_UPLOAD_BYTES`, HEIC) | config client-safe | — | se stesso | exact |
| `src/lib/media/rejection-window.ts` (NUOVO) | config | — | `src/lib/media/upload-limits.ts` | role-match |
| `src/lib/media/purge-rejected.ts` (NUOVO) | service | batch (delete per chiave) | `scripts/purge-media-orphans.mjs` + `deleteMedia` (`actions.ts:319-404`) | role-match |
| `src/app/api/cron/purge-rejected-media/route.ts` (NUOVO) | route cron | batch | `src/app/api/cron/refund-expired-tokens/route.ts` + auth di `production-mirror/route.ts:1632-1646` | exact |
| `vercel.json` | config | — | se stesso | exact |
| `supabase/migrations/2026092x_media_rejection_clock.sql` (NUOVO) | migration | DDL + trigger | `20260923180000_gallery_view_and_media_paths.sql` + `20260809004500_event_media_party_id.sql` §4 + `20260815120100_production_calendar_access.sql:328-355` | exact |
| `src/types/database.ts` (`EventMediaRow`) | model | — | se stesso (`:437-491`) | exact |
| `src/app/(public)/events/[slug]/actions.ts` (`updateMediaStatus` accetta `pending`; IN-02) | server action | CRUD | se stesso (`:242-274`) | exact |
| `src/app/(admin)/admin/(work)/events/[id]/media/page.tsx` (sezione Rejected, «N file rimossi», WR-01) | page RSC | request-response | se stesso (`:177-282`) | exact |
| `src/components/media/MediaReviewGrid.tsx` (Restore, WR-04, IN-01) | component client | event-driven | se stesso + `Lightbox.tsx:60-61` | exact |
| `src/components/media/MediaUpload.tsx` (accept HEIC, `File.type` vuoto, tetto vero, 413, compressione) | component client | streaming/file-I/O | se stesso (`:101-113`, `:280-297`, `:397-424`) | exact |
| `src/lib/media/compress-client.ts` (NUOVO) | utility client | transform | `upload-limits.ts` (modulo client-safe) | partial |
| worker di compressione video (NUOVO, se separato) | worker | streaming | `src/app/sw.ts` (unico worker del repo, dominio diverso) | **nessuno** |
| `src/app/(admin)/admin/visual/ArchiveUpload.tsx` (HEIC) | component client | file-I/O | `MediaUpload.tsx` (stessa lista importata) | exact |
| `scripts/verify-media-strip.mjs` (A esteso a `scripts/`, B su due stripper, controlli nuovi) | tooling/test | statico + comportamentale | se stesso (`:223-241`, `:393-512`, `:547-621`) + `restrip-event-media.mjs:172,366-376` (import del `.ts`) | exact |
| `scripts/fixtures/*.heic` (NUOVO, sintetico GPS 0,0) | fixture | — | — | **nessuno** |
| `scripts/restrip-event-media.mjs` (WR-07, WR-08, rifiuto HEIC) | script | batch | se stesso (`:340`, `:544-604`, `:361-362`) | exact |
| `scripts/seed-lab-media.mjs` (WR-06, WR-07) | script lab | batch | se stesso + `purge-media-orphans.mjs:336-339` (solo stato HTTP) | exact |
| `scripts/probe-event-media-lab.mjs` (IN-05, IN-08) | script lab | request-response | `purge-media-orphans.mjs:263-319` | role-match |
| `scripts/probe-cdn-lab.mjs` (NUOVO, DBT-08) | script lab | request-response (sonda) | `scripts/probe-event-media-lab.mjs:64-129` | role-match |
| script lab che arretra `rejected_at` (NUOVO o flag di `seed-lab-media`) | script lab | CRUD | `seed-lab-media.mjs:93-128` (rifiuto del ref) | role-match |
| `src/app/(admin)/admin/scanner/ScannerClient.tsx` (DBT-05, WR-05) | component client | event-driven | se stesso (`:831-870`, `:3258-3266`) | exact |
| `src/app/(public)/gallery/page.tsx` (WR-02) | page RSC | request-response | `media/page.tsx:60-72` | exact |
| `src/components/media/MediaGrid.tsx`, `src/app/(public)/events/[slug]/page.tsx` (WR-03, WR-04) | component/page | request-response | `Lightbox.tsx:60-61` | exact |
| IN-03/04/06/07 (`redirect("/dashboard")` ×32, `roles.ts:97`, `members/actions.ts:1606`, `account/page.tsx:263`) | vari | — | lettura a HEAD in RESEARCH §DBT-06/07 | role-match |
| `package.json` (`heic-decode`, `mediabunny`) | config | — | controllo E di `verify-media-strip.mjs:729-747` | exact |
| `.claude/rules/media-and-storage.md` (paths, tetto 50 MB, gate) | persona | — | se stesso (frontmatter `:1-6`) | exact |
| `.claude/rules/meta-gates.md` (`:117` «sei cron», tabella `:53-54`) | persona | — | se stesso | exact |
| `.claude/rules/checkin-offline.md` | persona | — | se stesso | exact |
| `CLAUDE.md` (indice `:249`) | persona | — | se stesso | exact |

---

## Pattern Assignments

### `src/lib/media/strip-video.ts` (utility server-only, transform) — NUOVO

**Analogo:** `src/lib/media/strip-metadata.ts` (361 righe, letto intero)

**Testa del file — `server-only` PRIMA del docblock** (`:1-2`, ragione in `:8-20`):
```ts
import "server-only";
// (nessuna dipendenza: il neutralizzatore e' codice nostro, RESEARCH §Don't Hand-Roll)
```
Il docblock deve rovesciare esplicitamente il paragrafo «Why `sharp` and not a
parser written by hand» (`:54-63`): per il video il TLV ISO-BMFF e' uniforme e
l'operazione non capisce il contenuto dei box, li spegne. E deve riscrivere la
sezione «WHAT THIS FILE DOES NOT COVER» (`:75-86`) — da quel commit **non e' piu'
vera** e va tolta da `strip-metadata.ts` nello stesso commit.

**Categorie come valori, record totale** (copiare la forma di `:131-153`):
```ts
export const MEDIA_STRIP_UNSUPPORTED_TYPE = "media_strip.unsupported_type";
export const MEDIA_STRIP_TOOL_UNAVAILABLE = "media_strip.tool_unavailable";
export const MEDIA_STRIP_FAILED = "media_strip.failed";

export type MediaStripRefusalReason =
  | typeof MEDIA_STRIP_UNSUPPORTED_TYPE
  | typeof MEDIA_STRIP_TOOL_UNAVAILABLE
  | typeof MEDIA_STRIP_FAILED;

const MEDIA_STRIP_REASONS: Record<MediaStripRefusalReason, true> = {
  [MEDIA_STRIP_UNSUPPORTED_TYPE]: true,
  [MEDIA_STRIP_TOOL_UNAVAILABLE]: true,
  [MEDIA_STRIP_FAILED]: true,
};
```
Le nuove (`media_strip.video_malformed`, `…video_unsupported_layout`,
`…video_unexpected_track`, `…video_timed_location`, `…video_residual_location`,
RESEARCH Pattern 2) **entrano nella stessa unione e nello stesso record**, oppure
in un'unione sorella importata da `strip-metadata.ts` — in entrambi i casi
`isMediaStripRefusal` (`:186-196`) deve riconoscerle, perche' `finalize.ts:296`
la usa per passare la `reason` senza ri-avvolgerla.

**Rifiuto lanciato, mai restituito; messaggio senza nome di file** (`:167-175`):
```ts
export class MediaStripRefusal extends Error {
  readonly reason: MediaStripRefusalReason;
  constructor(reason: MediaStripRefusalReason) {
    super(reason);
    this.name = "MediaStripRefusal";
    this.reason = reason;
  }
}
```

**Prima regola = il vecchio controllo del contenitore della route** — spostato
qui dentro (`route.ts:348-361`):
```ts
const ISO_BMFF_TOP_LEVEL_BOXES = new Set(["ftyp","moov","mdat","free","skip","wide","pnot"]);
function looksLikeVideoContainer(bytes: Buffer): boolean {
  if (bytes.length < 12) return false;
  return ISO_BMFF_TOP_LEVEL_BOXES.has(bytes.subarray(4, 8).toString("latin1"));
}
```

**Nucleo** — il prototipo misurato di RESEARCH Pattern 1 (ribattezza `udta`/`meta`/`uuid`
in `free` e azzera il contenuto, scende solo in `moov`/`trak`) + le sei regole
fail-closed di Pattern 2 + la post-condizione. **Nessun `catch` che degrada**:
copiare la chiusura di `:319-330`:
```ts
  } catch (cause) {
    if (isMediaStripRefusal(cause)) throw cause;
    console.error(`[${MEDIA_STRIP_FAILED}] … The upload is refused: an un-stripped file is never admitted. ${describe(cause)}`);
    throw new MediaStripRefusal(MEDIA_STRIP_FAILED);
  }
```
e `describe()` (`:341-344`). **Firma consigliata:** `stripVideoMetadata(input: Buffer, mime: string): Promise<Buffer>` —
il nome esatto `stripVideoMetadata(` e' quello che il controllo B esteso cerchera'
(RESEARCH §DBT-03 punto 5). Funzione **pura**: nessun client database importato
(regola di `:88-94`).

---

### `src/lib/media/strip-metadata.ts` (ramo HEIC) — MODIFICATO

**Analogo:** se stesso.

**Lista** (`:105-109`) — `image/heic` e `image/heif` **insieme**, e nello stesso
commit in `upload-limits.ts:34` (le due grafie si muovono insieme, `upload-limits.ts:17-21`).

**Record totali da estendere** (`:206-220`) — oggi la regola e' «stesso formato in
uscita». HEIC rompe la regola: va **dichiarato** (RESEARCH §HEIC (d)) con un terzo
record totale:
```ts
const REENCODE: Record<StrippableMime, (pipeline: Sharp) => Sharp> = { … };   // :206
const DETECTED_FORMAT: Record<StrippableMime, string> = { … };               // :216
// NUOVO, esportato: chi scrive (finalize, restrip) sa contentType ed estensione
export const OUTPUT_MIME: Record<StrippableMime, { mime: string; ext: string }> = { … };
```
Il ramo HEIC non passa da `REENCODE[mime](pipeline.rotate())` (`:316`): controllo
`format === "heif" && compression === "hevc"` (un AVIF riporta `heif`+`av1`),
decodifica con `heic-decode` (import **dinamico**, come `sharp` a `:264-287`, cosi'
un wasm che non si carica diventa `MEDIA_STRIP_TOOL_UNAVAILABLE` e non un crash),
pixel `raw` → `sharp` → `.jpeg()`. **Niente `.rotate()` da EXIF** (Pitfall 8).

**Firma invariata** `stripImageMetadata(input: Buffer, mime: string): Promise<Buffer>`
(`:246-249`) — `restrip-event-media.mjs:368` la importa dal `.ts` e `npm run build`
non guarda gli `.mjs` (Pitfall 6).

---

### `src/lib/media/finalize.ts` (service, file-I/O) — MODIFICATO

**Analogo:** se stesso (410 righe, letto intero).

**Il fork di oggi, da riscrivere** (`:285-335`): tre rami — immagine → strip;
`unstrippable !== null` → gate del chiamante con `bytesToWrite = sourceBytes`
(`:330`); altrimenti `MEDIA_FINALIZE_TYPE_NOT_ACCEPTED`. Il nuovo fork (RESEARCH
§DBT-03): immagine | HEIC | video (se il chiamante li ammette) | rifiuto, e **nessun
ramo scrive `sourceBytes`** — un controllo statico nuovo di `verify-media-strip`
pretende l'assenza di `bytesToWrite = sourceBytes`.

Copiare per il ramo video la stessa forma del ramo immagine (`:293-307`):
```ts
    try {
      bytesToWrite = await stripImageMetadata(sourceBytes, mimeType);
    } catch (cause) {
      if (isMediaStripRefusal(cause)) {
        return { ok: false, reason: cause.reason };
      }
      console.error(
        `[${MEDIA_FINALIZE_UNEXPECTED}] scope=${logScope} code=uncategorised ` +
          `message=the stripper threw something that is not a categorised refusal. ${describe(cause)}`
      );
      return { ok: false, reason: MEDIA_FINALIZE_UNEXPECTED };
    }
```

**La scrittura resta l'ultima istruzione** (`:348-353`) — cambia solo che chiave e
`contentType` vengono da `OUTPUT_MIME` (HEIC → `.jpg`, `image/jpeg`, RESEARCH Pattern 3):
```ts
  const { error: writeError } = await serviceClient.storage
    .from(destinationBucket)
    .upload(quarantinePath, bytesToWrite, {   // → finalPath derivato
      contentType: mimeType,                   // → OUTPUT_MIME[...].mime
      upsert: false,
    });
```
Il risultato `{ ok: true }` (`:374`) diventa `{ ok: true, path: finalPath }`; la
quarantena si rilascia con la chiave **originale** (`releaseQuarantineObject`, `:394-410`).

**Regole del file da NON rompere:** destinazione senza default (`:39-50`, controllo F);
log `code=… message=…` mai la chiave (`:69-79`); le sei categorie byte-identiche
(`:122-128`). Se `UnstrippableGate` (`:167-170`) perde ragione per la gallery, il
tipo si toglie o si restringe — ma l'archivio passa ancora `null` e continua a
rifiutare i video (`:145-151`).

---

### `src/app/api/media/finalize/route.ts` (route, request-response) — MODIFICATO

**Analogo:** se stesso.

**Da togliere nello stesso commit dello stripper** (Pitfall 4): la costante
`MEDIA_FINALIZE_VIDEO_ON_SECRET_NIGHT` (`:215-217`), la sua voce nell'unione
(`:239`), in `FINALIZE_HTTP` (`:268`), in `FINALIZE_QUARANTINE` (`:308`), il gate
`unstrippable` intero (`:565-625`) con la lettura di `venue_secret` (`:597-615`), e
`looksLikeVideoContainer` (`:348-361`, si sposta nello stripper).

**Da aggiungere** — ogni categoria nuova in **due** record totali (`:257-275`,
`:297-315`), che e' la ragione per cui un'omissione e' errore di build:
```ts
const FINALIZE_HTTP = {
  …
  [MEDIA_STRIP_UNSUPPORTED_TYPE]: 415,
  [MEDIA_STRIP_TOOL_UNAVAILABLE]: 503,
  [MEDIA_STRIP_FAILED]: 422,
} as const satisfies Record<FinalizeRefusal, number>;

const FINALIZE_QUARANTINE = {
  …
  [MEDIA_STRIP_TOOL_UNAVAILABLE]: "keep",   // «chiedi di nuovo» tiene i byte
  [MEDIA_STRIP_FAILED]: "remove",
} as const satisfies Record<FinalizeRefusal, "remove" | "keep">;
```
Le `media_strip.video_*` sono terminali → `"remove"`, 415/422.

**Risposta** (`:634`): oggi `path: quarantinePath`; diventa il `path` restituito da
`finalizeStrippedUpload`. Il client usa gia' `finalizedPath` (`MediaUpload.tsx:469-473`).

**Docblock:** «seventeen» (`:222-226`) e le frasi «public bucket» (`:31,110,114,163,172,576`,
RESEARCH lessico) si ricontano nello stesso commit.

---

### `src/lib/media/upload-limits.ts` e `src/lib/media/rejection-window.ts`

**Analogo:** `src/lib/media/upload-limits.ts` (57 righe, letto intero) — modulo
**senza import**, sicuro su entrambi i lati del confine (`:14-15`).

Forma della costante con fonte scritta accanto (`:39-50`):
```ts
/**
 * The ceiling for one photograph, in bytes.
 * … hoisted here rather than restated, so the archive's uploader could not
 * quietly pick a different one. …
 */
export const MAX_PHOTO_BYTES = 50 * 1024 * 1024;
```
→ `MAX_UPLOAD_BYTES = 50 * 1024 * 1024` con la fonte «`fileSizeLimit` globale del
piano free, letto il 2026-09-23», che **sostituisce** `MAX_PHOTO_BYTES` e
`MAX_VIDEO_SIZE` (`MediaUpload.tsx:111`, 500 MB falso). Consumatori da aggiornare:
`MediaUpload.tsx:102,110`, `ArchiveUpload.tsx:11,319,322,554`.

→ `rejection-window.ts`: stessa forma, **una** costante
`REJECTED_MEDIA_OBJECT_TTL_DAYS = 7` con la ragione di D-52.1-02 nel docblock;
letta dal cron **e** dalla pagina di moderazione, nessuna copia in SQL (il cron
passa la soglia come parametro).

---

### `src/lib/media/purge-rejected.ts` (service, batch) — NUOVO

**Analoghi:** `scripts/purge-media-orphans.mjs` (le sei regole, `:29-77`) e
`deleteMedia` (`actions.ts:358-388`) per la conferma per lista.

**La conferma e' la lista restituita, mai «nessun errore»** (`actions.ts:360-388`):
```ts
  const { data: removed, error: storageError } = await supabase.storage
    .from("event-media")
    .remove([media.storage_path]);
  if (storageError) { console.error(`[media.storage_delete_failed] …`); throw … }
  // No error is not the same as removed. Storage answers a DELETE the object
  // policy did not admit — or a key with no object behind it — with an EMPTY
  // list and no error.
  if (!Array.isArray(removed) || removed.length === 0) { … }
```
e il conteggio per nome esatto (`purge-media-orphans.mjs:383-394`):
```js
async function removeOne(bucket, name) {
  …
  if (out.error) return { ok: false, category: "storage_error", … };
  const removed = (out.data ?? []).filter((o) => o?.name === name).length;
  if (removed !== 1) return { ok: false, category: "storage_not_found", … };
  return { ok: true };
}
```
Qui **`storage_not_found` diventa `already_absent`** (Vercel puo' consegnare due
volte: idempotenza per costruzione con `object_removed_at IS NULL` nel filtro).

**Riconteggio da fonte diversa** (`purge-media-orphans.mjs:682-697`): lo script
riconta gli oggetti dal catalogo `storage.objects` via Management API; nel cron non
c'e' Management API → la funzione `SECURITY DEFINER` ristretta al service role
nella migration (sotto). **Istantanea:** strutturale, la riga resta — scriverlo nel
docblock (RESEARCH §DBT-01), nessun `.env*-snapshot`.

**Anti-pattern:** non riusare `deleteMedia` — cancella la riga (`actions.ts:390-398`),
D-52.1-04 lo esclude. Non leggere `created_at` come data del rifiuto (Pitfall 1).

⚠ **Trappola del controllo A di `verify-media-strip`:** `bindsPublicBucket`
(`verify-media-strip.mjs:321-323`) scatta su `const X = "event-media"`. Nel codice
sotto `src/` il bucket va scritto **inline** come fa `deleteMedia`
(`.from("event-media").remove(`) — `.remove(` non e' in `WRITE_CALLS` (`:383-386`),
quindi A resta verde. Una costante legata al nome lo farebbe diventare rosso.

---

### `src/app/api/cron/purge-rejected-media/route.ts` (route cron, batch) — NUOVO

**Analogo primario:** `src/app/api/cron/refund-expired-tokens/route.ts` (252 righe,
letto intero). **Auth:** `production-mirror/route.ts:1632-1646` (l'unico cron che
fallisce chiuso con `CRON_SECRET` assente).

**Imports** (`refund-expired-tokens/route.ts:1-2`):
```ts
import { NextResponse } from "next/server";
import { getServiceClient } from "@/lib/supabase/service";
```

**Auth fail-closed** (`production-mirror/route.ts:1632-1646`) — **non** la forma dei
cinque cron vecchi (`refund-expired-tokens:143-146`), che con la variabile assente
ammette `Bearer undefined`:
```ts
  const cronSecret = process.env.CRON_SECRET;
  if (typeof cronSecret !== "string" || cronSecret.length === 0) {
    console.error(
      "[production_mirror.cron_secret_absent] the route refused every request: " +
        "CRON_SECRET is not set in this environment"
    );
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }
  const authHeader = request.headers.get("authorization");
  if (authHeader !== `Bearer ${cronSecret}`) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }
```

**Esiti come unione + due record totali + `respond()`** (`refund-expired-tokens:19-110`):
```ts
const CRON_REFUND_OK = "cron_refund_ok";
…
const CRON_REFUND_HTTP = {
  [CRON_REFUND_OK]: 200,
  [CRON_REFUND_DELETE_REFUSED]: 500,
  [CRON_REFUND_DELETE_SHORT]: 500,
} as const satisfies Record<CronRefundOutcome, number>;

const CRON_REFUND_REPORT: Record<CronRefundOutcome, string> = { … };

function respond(outcome: CronRefundOutcome, counts: CronRefundCounts) {
  const status = CRON_REFUND_HTTP[outcome];
  const report = CRON_REFUND_REPORT[outcome];
  if (status !== 200) {
    console.error(`[${outcome}] ${report}`, counts);
  }
  return NextResponse.json({ ...counts, outcome, report }, { status });
}
```
Conteggi (RESEARCH §DBT-01): `candidates`, `removed`, `already_absent`, `refused`,
`residual`. Nessun id, nessuna chiave nel corpo (`:97-101`).

**Leggere l'errore della prima lettura** — il difetto da **non** copiare e'
`refund-expired-tokens:177-182` (`const { data: oldTokens } = …` scarta `error`).
Forma giusta da `retry-failed-orders/route.ts:45-63`:
```ts
  if (error) {
    console.error("[cron.retry_failed_orders.read_failed]", error.message);
    return NextResponse.json({ error: "read_failed" }, { status: 500 });
  }
```
→ categoria `media_purge.read_failed`.

**`count: "exact"` sull'UPDATE** (`refund-expired-tokens:220-234`) — «a null count
is not a measurement», nessun coalesce alla lunghezza attesa.

**Progresso per elemento** (`ticketing-payments.md`, gate *cron non atomico*):
marcare `object_removed_at` sulle chiavi confermate, non a fine batch.

**Docblock «Gira al mattino»** (`retry-failed-orders/route.ts:34-35`) con l'ora locale.

⚠ **Dominio che si carica:** `src/app/api/cron/**` carica `ticketing-payments`
(`meta-gates.md:54`); `media-and-storage.md` ha `paths` che **non** coprono ne' questo
file ne' `src/lib/media/**` (frontmatter `:1-6`). Il glob va aggiunto (RESEARCH
§DBT-06/07, persona).

---

### `vercel.json` (config) — MODIFICATO

**Analogo:** se stesso (`:5-34`). Aggiungere in coda, stessa forma:
```json
    {
      "path": "/api/cron/retry-failed-orders",
      "schedule": "15 7 * * *"
    }
```
→ `{"path": "/api/cron/purge-rejected-media", "schedule": "45 7 * * *"}` (09:45 estate /
08:45 inverno a Torino; fuori dalla notte 22→06). Ricontare: 7 directory in
`src/app/api/cron/` = 7 voci oggi → **8 e 8** dopo; `meta-gates.md:117` («sei») si
corregge nello stesso commit.

---

### `supabase/migrations/2026092x_media_rejection_clock.sql` (migration) — NUOVO

**Analogo di forma:** `20260923180000_gallery_view_and_media_paths.sql` (320 righe,
letto intero).

**Testata** (`:1-85`): decisioni citate, **verso del deploy** (qui: migration
PRIMA del codice — il codice nuovo legge `rejected_at`/`object_removed_at`),
**nessun `BEGIN;`** perche' l'endpoint della Management API avvolge in una
transazione (`:30-46`), «cosa questa migration NON fa», righe esistenti
(`:75-82`), e la chiusa:
```sql
-- ⚠ QUESTO FILE E' UNA PUBBLICAZIONE (repo pubblico): nessun path di oggetto,
-- nessun identificativo di riga, nessuna serata, nessuna sede.
```

**DDL idempotente** (`:145-150`, `:174-179`):
```sql
ALTER TABLE public.event_media
  ADD COLUMN IF NOT EXISTS storage_path text;

ALTER TABLE public.event_media
  DROP CONSTRAINT IF EXISTS event_media_storage_path_is_a_key;
ALTER TABLE public.event_media
  ADD CONSTRAINT event_media_storage_path_is_a_key
  CHECK (storage_path IS NULL OR (…));
```
→ `rejected_at`, `object_removed_at`, `event_media_removed_only_when_rejected`
(RESEARCH Pattern 4) e backfill `rejected_at = now()` per le `rejected` esistenti
(la finestra parte dall'atto, non dal caricamento).

**Trigger sulla stessa tabella — analogo esatto** `20260809004500_event_media_party_id.sql:233-272`:
```sql
CREATE OR REPLACE FUNCTION private.event_media_require_party()
RETURNS trigger
LANGUAGE plpgsql
SECURITY INVOKER
SET search_path = ''
AS $$
BEGIN
  IF NEW.party_id IS NULL THEN
    RAISE EXCEPTION
      'event_media.party_id is required: …'
      USING ERRCODE = '23514';
  END IF;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS event_media_require_party ON public.event_media;
CREATE TRIGGER event_media_require_party
  BEFORE INSERT ON public.event_media
  FOR EACH ROW
  EXECUTE FUNCTION private.event_media_require_party();
```
e il paragrafo sull'assenza della coppia REVOKE/GRANT per un trigger INVOKER
(`:253-262`). Per il rifiuto dell'uscita da `rejected` con oggetto rimosso, forma
del messaggio da `20260815120100_production_calendar_access.sql:334-340`
(solo l'`id`, `ERRCODE = 'restrict_violation'`), trigger
`BEFORE UPDATE OF status` (`:352-355`).

⚠ **Lezione registrata sulla stessa tabella** (`20260809004500:274-286`): un vincolo
applicato all'`UPDATE` di `event_media` rompe la moderazione delle righe legacy se
tocca righe esistenti. Il `CHECK (object_removed_at IS NULL OR status = 'rejected')`
e' innocuo solo perche' ogni riga esistente nasce con `object_removed_at` nullo —
**va scritto nella migration**, non lasciato dedurre.

**Funzione di riconteggio dal catalogo** (fonte diversa per il cron) — `SECURITY
DEFINER`, `search_path` vuoto, e revoke-then-grant da
`20260809001000_assignment_resolver.sql:478-485`:
```sql
REVOKE ALL ON FUNCTION public.my_access_context(uuid) FROM public, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.my_access_context(uuid) TO authenticated;
```
→ qui `GRANT … TO service_role` soltanto (Postgres concede `EXECUTE` a `PUBLIC` di
default su ogni funzione nuova, `:478-482`).

**Il `DO` che riconta prima del commit** (`20260923180000:266-302`):
```sql
DO $$
DECLARE
  v_senza integer;
BEGIN
  SELECT count(*) INTO v_senza
    FROM public.event_media WHERE storage_path IS NULL;
  IF v_senza > 0 THEN
    RAISE EXCEPTION '% righe di event_media restano senza storage_path dopo il backfill', v_senza
      USING HINT = '…';
  END IF;
END;
$$;
```
→ contare: `rejected` con `rejected_at` nullo = 0; righe con `object_removed_at`
non nullo = 0; trigger e funzione presenti nel catalogo. Solo numeri nel messaggio.

**`COMMENT ON COLUMN`** per le due colonne (`:319-320`), e `EventMediaRow`
(`src/types/database.ts:437-491`) aggiornato nello stesso commit con la stessa frase
(regola delle «tre posizioni» di `20260809004500:220-223`).

---

### `src/app/(public)/events/[slug]/actions.ts` — `updateMediaStatus` (server action, CRUD)

**Analogo:** se stesso (`:242-274`). Oggi `status: "approved" | "rejected"` (`:244`);
il ripensamento richiede anche `pending` o `approved` da `rejected`. Il controllo di
capacita' resta **dentro** l'azione (`:253-259`):
```ts
  if (!ctx.userId) { throw new Error("Not authenticated"); }
  if (!ctx.capabilities.has(CAP.STAFF_MANAGE)) {
    throw new Error("forbidden.staff_manage_required");
  }
```
Il rifiuto del trigger (oggetto gia' rimosso) arriva come errore PostgREST: va
riconosciuto per `code`, non per messaggio (Next redige i messaggi delle Server
Action, `MediaUpload.tsx:46-51`), e la UI non mostra «Restore» su quelle righe
(Pitfall 3). IN-02 (`:143,163`): «private bucket».

---

### `src/app/(admin)/admin/(work)/events/[id]/media/page.tsx` (page RSC) — MODIFICATO

**Analogo:** se stesso (396 righe, letto intero).

**Ogni lettura controlla l'errore con categoria** (`:218-229`):
```ts
  const { data: pendingMedia, error: pendingError } = await supabase
    .from("event_media")
    .select("id, storage_path, type, file_size, created_at, uploaded_by")
    .eq("event_id", eventId)
    .eq("status", "pending")
    .order("created_at", { ascending: true });
  if (pendingError) {
    console.error(
      `[media_review.pending_read_failed] code=${pendingError.code ?? "unknown"} message=${pendingError.message}`
    );
  }
```
→ una terza lettura, `status = 'rejected'` con `rejected_at`, `object_removed_at`,
categoria `[media_review.rejected_read_failed]`. **WR-01** (`:143-147`): stessa
forma sulla lettura dell'evento (`error: eventError`, `PGRST116` → ramo «not found»,
altro → `[media_review.event_read_failed]`).

**Tessere di conteggio** (`:304-323`) — una quarta, «N files removed», stessa
classe e `font-mono`; la data dell'ultima rimozione accanto. Solo i rifiutati
**nella finestra** si firmano (`signEventMedia`, `:269-272`), quelli con
`object_removed_at` non si firmano (non c'e' oggetto).

**Stati d'errore e vuoti in parole proprie** (`:330-393`); `dynamic = "force-dynamic"`
gia' dichiarato (`:72`).

---

### `src/components/media/MediaReviewGrid.tsx` (component client) — MODIFICATO

**Analogo:** se stesso (266 righe) + `Lightbox.tsx:60-61` per WR-04.

**Azione in transizione con errore mostrato** (`:94-113`):
```ts
  function handleAction(mediaId: string, status: "approved" | "rejected") {
    setPendingAction(`${status}-${mediaId}`);
    setError(null);
    startTransition(async () => {
      try {
        const result = await updateMediaStatus(mediaId, status);
        …
```
→ la sezione «Rejected» riusa lo stesso schema con un pulsante `secondary` «Restore»
(la scelta di varianti e' motivata a `:50-55`: niente semantica critica su un esito
ordinario), e una riga «removed automatically on <data>» calcolata da
`rejected_at + REJECTED_MEDIA_OBJECT_TTL_DAYS`.

**WR-04:** oggi `failed` e' per **id** (`:81-88`, `:187`, `:199`). `Lightbox.tsx:60-61`
lo tiene per **URL** (`failedUrl === item.url`), che e' la forma giusta perche' una
firma nuova deve poter riprovare. **IN-01** (`:57-64`): il docblock dice «public
bucket» e «l'oggetto resta» — riscrivere con la data e con DBT-01.

---

### `src/components/media/MediaUpload.tsx` (component client) — MODIFICATO

**Analogo:** se stesso (792 righe, letto intero).

**Punti d'innesto, riga per riga:**
- `:101-113` — import da `upload-limits`; `ALLOWED_VIDEO_TYPES` (`:109`) e
  `MAX_VIDEO_SIZE` (`:111`) spariscono in favore di `MAX_UPLOAD_BYTES`; `ACCEPT_STRING`
  (`:113`) guadagna `image/heic,image/heif`.
- `:231-244` — `getFileExtension` con `mimeMap`: aggiungere `image/heic→heic`,
  `image/heif→heif`. **E il rovescio** (Pitfall 7): una funzione che ricava il tipo
  dall'estensione quando `file.type === ""`, usata **prima** di `validateFile`
  (`:280-297`) **e** al deposito (`:405-409`, `contentType: file.type`) **e** nel POST
  (`:437`, `mimeType: selectedFile.file.type`).
- `:291-294` — il messaggio «10MB»/«100MB» e' falso: leggere da `formatFileSize(MAX_UPLOAD_BYTES)`,
  come fa gia' `ArchiveUpload.tsx:322`:
  ```ts
  message: `"${file.name}" is ${formatBytes(file.size)} and the ceiling is ${formatBytes(MAX_PHOTO_BYTES)}.`,
  ```
- `:411-424` — il deposito rifiutato ha una sola frase per ogni causa; separare il
  **413** (`media_upload.deposit_too_large`, con dimensione e tetto) dagli altri,
  leggendo lo stato dell'errore per forma come `finalize.ts:205-209`.
- `:136-178` — `FINALIZE_REASON_TEXT`: togliere `video_on_secret_night` (`:166-167`),
  aggiungere le `media_strip.video_*` e le `media_compress.*`; ricontare «seventeen»
  (`:41-42`, `:61`).
- `:622-624` — «Photos up to 50MB -- Videos up to 500MB» → il tetto vero, e i limiti
  non garantiti (durata) detti qui, prima.
- `:386-388`, `:724-742` — lo stato per file (`pending|uploading|done|error`) e'
  dove entra `compressing` con la percentuale (`onProgress` di mediabunny) e il
  pulsante Cancel.

**Controllo C invariato:** il componente nomina solo `QUARANTINE_BUCKET` (`:122`).

---

### `src/lib/media/compress-client.ts` (+ worker) — NUOVO, analogo parziale

**Analogo:** `upload-limits.ts` per la **forma** (modulo client-safe, docblock che
dice perche' esiste e cosa non e'). Il comportamento non ha analogo nel repo: usare
RESEARCH §«Compressione sul telefono» (foto: `createImageBitmap` +
`imageOrientation: "from-image"` + `toBlob` JPEG sotto 16,7 MP; video: `mediabunny`
importato **dinamicamente**, H.264/MP4, `canEncode` come cancello, 3 minuti).

Regole dal repo che si applicano:
- **non nomina alcun bucket** (controllo A cammina `src/`);
- **non importa** `strip-metadata.ts`/`strip-video.ts` e **non** e' importato da
  `finalize.ts` — controllo nuovo di `verify-media-strip` (RESEARCH §«Come continua a
  valere»);
- categorie come valori, una frase ciascuna (forma di `FINALIZE_REASON_TEXT`).

L'unico worker del repo e' `src/app/sw.ts` (service worker Serwist): **non** e' un
modello per un Dedicated Worker, e il gate *service worker* di `nextjs-architecture.md`
riguarda la cache, non questo.

---

### `scripts/verify-media-strip.mjs` (tooling) — MODIFICATO

**Analogo:** se stesso (799 righe; letti `:1-120`, `:210-512`, `:514-799`).

**La camminata** (`:223-241`) — `listScannableFiles(dir)` e' gia' parametrica: il
controllo A esteso la chiama anche su `${ROOT}/scripts`. **L'eccezione di oggi**
(`:549-552`):
```js
for (const rel of files) {
  if (rel === FINALIZE_ROUTE) continue;
  strayHits.push(...findPublicBucketWrites(rel));
}
```
→ per `scripts/` il disegno a quattro regole di RESEARCH §DBT-02 (misurato: la
estensione ingenua accende **cinque** file, fra cui questo stesso a `:196`).

**B per ordine di righe** (`:582-621`) — riusare `findStripLines`/`findWriteCallLines`
(`:488-512`) su `restrip-event-media.mjs` (strip `:651` sopra upload `:659`) e, con
`STRIP_CALL` diventato una lista (`stripImageMetadata(`, `stripVideoMetadata(`), su
`finalize.ts`.

**`WRITE_CALLS`** (`:393-404`) — aggiungere la forma grezza
`/storage/v1/object/` + `method: "POST"|"PUT"` (Pitfall 10: `seed-lab-media.mjs:174-175`).

**Controllo comportamentale** — nessun analogo dentro questo file (oggi «opens no
network connection… never executes anything», `:19-24`, `:95-98`: **la testata va
riscritta**). Il modo di importare un `.ts` server-only da uno script esiste:
`restrip-event-media.mjs:172` + `:366-376`:
```js
await import("./lib/server-only-shim.mjs");
…
  ({ stripImageMetadata, isMediaStripRefusal } = await import("../src/lib/media/strip-metadata.ts"));
  if (typeof stripImageMetadata !== "function") throw new TypeError("…");
```
⚠ Lo shim esige che sia **la prima riga eseguita** e niente import statici prima
(`restrip:174-176`) — ma questo file ha import statici (`:115-119`). Il controllo
comportamentale va quindi in un **file separato** invocato dal gate, o il gate
diventa a import dinamici. Decisione del piano, da dichiarare.

**Uscite 0/1/2 e `refuse()`** (`:213-217`) e il verdetto finale (`:787-799`) restano;
il conteggio «six checks» (`:38`, `:790`) si aggiorna.

---

### `scripts/restrip-event-media.mjs` (WR-07, WR-08) — MODIFICATO

**Analogo per WR-07:** `purge-media-orphans.mjs:336-339`:
```js
  if (!res.ok) {
    // Il corpo d'errore puo' ripetere la query, che porta chiavi: solo lo stato.
    fail("sql", `Management API HTTP ${res.status}`);
  }
```
contro l'attuale `restrip:340` (`(await res.text()).slice(0, 300)`) e
`seed-lab-media.mjs:161`.

**WR-08:** `restrip:544-588` scrive i byte (`writeFileSync(... got.bytes)`, `:572`)
**prima** del bivio `--dry-run` (`:598`). In dry-run solo `index.json` con formato,
dimensione, EXIF/XMP, `sha256` — la forma «solo metadati» di
`purge-media-orphans.mjs:578-595`.

**HEIC:** `MIME_BY_EXT`/`FORMAT_BY_MIME` (`:361-362`) non guadagnano HEIC; un `.heic`
si rifiuta con categoria (non esistono righe HEIC).

---

### `scripts/probe-cdn-lab.mjs` e lo script che arretra `rejected_at` — NUOVI

**Analogo:** `scripts/probe-event-media-lab.mjs:64-129` — il rifiuto del ref di
produzione **prima** di caricare l'ambiente e prima di qualunque rete:
```js
function refuse(message) {
  console.error(`\n  RIFIUTO: ${message}\n`);
  process.exit(2);
}
if (process.env.LAB_PROJECT_REF === PRODUCTION_REF) {
  refuse("LAB_PROJECT_REF e' il ref di PRODUZIONE. …");
}
```
seguito dal controllo che l'URL punti al ref di laboratorio (`:103-116`). Per IN-08
la forma con nomi `LAB_*` e ricaduta esplicita e' `purge-media-orphans.mjs:300-319`.
Il referto stampa **solo numeri, categorie e ore UTC** (`purge-media-orphans.mjs:129-136`).
DBT-08 usa un bucket di prova **dedicato**, mai `event-media` — cosi' il controllo A
esteso non ha nulla da dire.

---

### `src/app/(admin)/admin/scanner/ScannerClient.tsx` (DBT-05, WR-05) — MODIFICATO

**Analogo:** se stesso (3840 righe; letti `:370-399`, `:820-899`, `:3240-3269`).

**Il punto della correzione** (`:831-836`):
```ts
    const goOnline = () => {
      setIsOnline(true);
      resubscribe();
      requestReloadRef.current?.("online");
    };
    const goOffline = () => setIsOnline(false);
```
→ `goOffline` abbassa anche `setChannelLive(false)`. Rispetta la regola scritta a
`:854-861` («It only ever lowers `channelLive` … `SUBSCRIBED` is the only writer that
raises it»): il battito abbassa gia' allo stesso modo (`:864-870`):
```ts
    supabase.realtime.onHeartbeat((status) => {
      if (status === "disconnected" || status === "timeout") {
        setChannelLive(false);
        supabase.realtime.connect();
      }
    });
```
Il testo a radio spenta esiste gia' (`guestListWarningText`, `:389-395`). **La misura
sul telefono va prima del codice** (RESEARCH §DBT-05, Pitfall 9).

**WR-05:** oggi le regioni `aria-live="polite"` sono montate **solo quando c'e'
l'avviso** (`:3258-3266`, `:3701-3702`) — per VoiceOver una regione che nasce col
testo non annuncia. Serve una regione `role="status"` sempre montata, nascosta. Budget
persona: `DoorSurface.tsx` e' il caso peggiore, rimisurare.

---

### `src/app/(public)/gallery/page.tsx` (WR-02) e WR-03/04

**Analogo WR-02:** `media/page.tsx:60-72` — `export const dynamic = "force-dynamic";`
con il docblock che dice perche' non e' ridondante con `cookies()` (`:67-70`).

**WR-04 in `MediaGrid.tsx`:** `Lightbox.tsx:60-61`, `Set`/stato per **URL**.
**WR-03:** togliere `uploaded_by` da select, proiezione e tipo
(`events/[slug]/page.tsx:1036,1042,1071`, `MediaGrid.tsx:49`).

---

## Shared Patterns

### Categorie come valori, record totali (zero fallimenti silenziosi)
**Fonte:** `strip-metadata.ts:131-153`, `route.ts:257-315`, `refund-expired-tokens:55-80`
**Si applica a:** `strip-video.ts`, `finalize.ts`, `route.ts`, cron nuovo,
`compress-client.ts`, `MediaUpload.tsx`.
Ogni categoria nuova entra in **ogni** `Record<Union, …>` con `satisfies`: dimenticarla
e' un errore di `npm run build`, l'unico gate automatico del prodotto. Il client
mappa **stringhe esatte** (`MediaUpload.tsx:136-178`) con ricaduta che stampa la
categoria grezza (`:459-461`).

### Log senza contenuto
**Fonte:** `finalize.ts:69-79` (`code=… message=…`, mai la chiave),
`strip-metadata.ts:162-166` (mai nome di file), `refund-expired-tokens:97-101` (solo
conteggi), migration `20260815120100:316-319` (solo l'id nel `RAISE`).
**Si applica a:** tutto cio' che tocca media — un nome di file lo scrive un telefono
che era dentro la sede.

### Rimozione per chiave, conferma per lista, riconteggio da fonte diversa
**Fonte:** `purge-media-orphans.mjs:29-77` (le sei regole), `:383-394`, `:682-720`;
`actions.ts:375-388`.
**Si applica a:** `purge-rejected.ts`, cron, prova su lab di DBT-01.

### Rifiuto del ref di produzione come prima riga eseguita
**Fonte:** `probe-event-media-lab.mjs:66-80`, `purge-media-orphans.mjs:177-280`,
`seed-lab-media.mjs:93-128`; regola in `meta-gates.md` («Il gate della verifica»).
**Si applica a:** `probe-cdn-lab.mjs`, script che arretra `rejected_at`, ogni sonda
nuova — e il controllo A esteso lo misura (RESEARCH §DBT-02 regola 3).

### Auth di cron che fallisce chiusa
**Fonte:** `production-mirror/route.ts:1615-1646`.
**Si applica a:** solo il cron nuovo (gli altri cinque non si toccano da qui:
`:1628-1630` lo dichiara decisione).

### Server Action: controllo dentro, errore per posizione/codice
**Fonte:** `actions.ts:253-259`; `MediaUpload.tsx:46-51` (Next redige i messaggi).
**Si applica a:** `updateMediaStatus` esteso, «Restore».

### Persona nello stesso commit
**Fonte:** `media-and-storage.md` frontmatter `:1-6` (manca `src/lib/media/**`),
`meta-gates.md:53-54` (tabella), `:117` («sei cron»), `CLAUDE.md:249` (indice).
**Si applica a:** ogni onda che tocca media, cron o porta; `npm run verify:persona`
(controllo G: il modulo primario dichiarato deve caricarsi davvero) e budget
rimisurato.

---

## No Analog Found

| File | Ruolo | Flusso | Ragione |
|---|---|---|---|
| worker di compressione video | worker | streaming | L'unico worker e' `src/app/sw.ts` (Serwist, cache): nessun Dedicated Worker nel repo. Usare RESEARCH §«Video» (mediabunny, import dinamico, `AbortController`). |
| `src/lib/media/compress-client.ts` (comportamento) | utility client | transform | Nessuna ricodifica nel browser oggi. Solo la forma del modulo viene da `upload-limits.ts`. |
| `scripts/fixtures/*.heic` | fixture | — | Non esiste `scripts/fixtures/`. Generare con `sips` da un JPEG con GPS a (0°, 0°) — **pubblicabile per costruzione**; mai un file da telefono (RESEARCH §HEIC (e)). |

---

## Metadata

**Ambito di ricerca analoghi:** `src/lib/media/`, `src/app/api/media/`,
`src/app/api/cron/` (7 route), `src/components/media/`,
`src/app/(admin)/admin/(work)/events/[id]/media/`, `src/app/(admin)/admin/scanner/`,
`src/app/(public)/events/[slug]/actions.ts`, `scripts/` (purge, restrip, seed, probe,
verify-media-strip, lib/server-only-shim), `supabase/migrations/` (trigger e
revoke/grant), `src/types/database.ts`, `vercel.json`, `.claude/rules/`.
**File letti:** 22.
**Data di estrazione:** 2026-09-23.
