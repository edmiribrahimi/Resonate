---
phase: 41.2-public-member-and-money-surfaces
plan: 13
type: execute
wave: 6
depends_on: ["41.2-10"]
files_modified:
  - src/app/(members)/dashboard/page.tsx
  - src/app/(members)/dashboard/loading.tsx
  - src/app/(members)/dashboard/DashboardDrinkTokens.tsx
autonomous: true
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The member dashboard mounts the responsive navigation and declares the tablet-tier clearance, as a pair"
    - "A member's own token list still separates the active from the completed under the same conditions, and no token state is re-derived"
    - "The dashboard placeholder stands in front of a member's own list with counts that are literals and stand for nothing"
  artifacts:
    - path: "src/app/(members)/dashboard/page.tsx"
      provides: "the member dashboard on PageShell at default width, navigation pair, the phase's heaviest raw-palette file cleared"
      contains: "components/layout/AppNav"
    - path: "src/app/(members)/dashboard/loading.tsx"
      provides: "the dashboard placeholder on the Skeleton primitive at the page's own width"
      contains: "components/ui/Skeleton"
  key_links:
    - from: "src/app/(members)/dashboard/DashboardDrinkTokens.tsx"
      to: "src/app/(public)/events/[slug]/DrinkTokenCard.tsx"
      via: "the shared money core, converted one wave earlier so this plan does not open it"
      pattern: "DrinkTokenCard"
---

<objective>
Convert the member dashboard — the surface a member lands on — its placeholder,
and the token list it renders.

Purpose: the dashboard page is the **heaviest raw-palette file in the phase**, and
its placeholder holds **eleven** hand-rolled pulses, the most of the five. It also
reaches the shared money core, which is why that core was converted one wave
earlier: so this plan, the bar's plans and the public event page's plans stay
separate plans instead of being merged into one.

The token list is the one place a member sees their own drink tokens sorted into
active and completed. Nothing about which token is which is re-derived here — that
lifecycle is decided elsewhere, and a conversion that appeared to recompute it
would be changing what a member believes they still hold.

Output: three files, one placeholder off the pulse census, and the phase's largest
single palette debt cleared.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-10-SUMMARY.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). The surface is
   declared by the final reconciliation, 41.2-19.
2. **41.2 does not edit the spine** — `layout/AppNav.tsx`, `layout/MobileNav.tsx`,
   `ui/Button.tsx`, `ui/Typography.tsx`. Needing to is a **HALT**.
3. **No server action is opened.** Stop condition 2 — halt and report.
4. **Files in this closure that belong to other plans and are NOT opened here:**
   the media section and the account and authentication controls are plan
   41.2-14's; **the referral control was converted by plan 41.2-07** in wave 3,
   as spine, precisely so `/membership-card` and this surface stayed two plans —
   say so in the SUMMARY, in the *converted earlier as spine* position; and the
   four shared money-core files were converted by plan 41.2-10 in wave 5, for the
   same reason.
5. **No package is installed.**
6. **DEF-41-01** — never spell a bracketed arbitrary-value utility in prose. The
   clearance declaration is copied verbatim from
   `src/app/(public)/gallery/page.tsx:110`.
7. **`.planning/` is public.** No member, no referral value, no amount, no token
   identifier, no event and no date in the SUMMARY.
8. **No `npm run dev`, no headless render** (D-41.2-04).
9. **⚠ `scripts/verify-conversion.mjs` is invisible to the default `grep`.**
   `LC_ALL=C /usr/bin/grep` or the Read tool.
10. **macOS/BSD.** `grep -E`, `sed -i ''`. **No test runner exists.**
11. **The money grep runs on every file this plan opens.** Filter the diff for
    `status`, `refund`, `amount`, `idempot`, `webhook`, `price`, `token` and quote
    every matching line with one sentence.

**Quick run:** `npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`

**Clean-file greps** (matcher `scripts/verify-conversion.mjs:850-916`): raw palette
`(bg|text|border|ring|from|to|via|fill|stroke|shadow|outline|decoration)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose|black|white)(-[0-9]{1,3})?`;
legacy token, same prefixes against `card-border|card|background|foreground`.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: The dashboard page — the phase's heaviest palette debt, and the navigation pair</name>
  <files>src/app/(members)/dashboard/page.tsx</files>
  <read_first>
    - `src/app/(members)/dashboard/page.tsx` — 599 lines. Read it in full: it carries the phase's largest raw-palette count and its root, header and heading at `:287,294,298` are **byte-for-byte** the four-part deletion. It mounts the phone-locked wrapper at `:6` and `:589`, and it imports the media section at `:9` — a file this plan does not open
    - **The analog, exact:** `src/app/(admin)/admin/(work)/members/page.tsx`, converted by plan 41-10 — a long member-facing page with cards and a placeholder in front of it
    - **The public navigation analog:** `src/app/(public)/gallery/page.tsx:88-133`
    - `src/components/ui/PageShell.tsx:146,160`; `src/components/ui/Typography.tsx:53`; `src/components/ui/Card.tsx:47`; `src/components/ui/Chip.tsx:58,161`; `src/components/ui/Button.tsx:71,215`
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md` — this page file is expected to carry **no** own maximum; confirm against the findings table rather than assuming, and confirm the check-A and check-B hit lists the dry run recorded for this file, so the conversion can be checked against a number instead of a feeling
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.1, §3 rows 1, 2, 7, 8, 9, 10, 11
    - `.claude/rules/community-membership.md` — what a member sees on their own dashboard is the product's promise made concrete; a width changes how it sits, never what it is
  </read_first>
  <action>
Adopt `PageShell` at `width="default"`. This surface was the second candidate
considered for the wide form and it was **deferred, not rejected** — reversible in
one line of the shell's call. Say so, so the next reader sees a deferral.

Apply the four-part deletion: the outer root goes to the shell, the repeated
horizontal padding goes with it, the header's bottom padding becomes a margin, the
heading becomes the title primitive.

**Then clear the palette, and check it against a number.** Plan 41.2-01's dry run
recorded check A's and check B's hits **per file**; this file was the largest
contributor. Convert every one of them, then compare your result with that
recorded list and state in the SUMMARY whether the counts agree. A conversion
whose result does not match the measurement is either an incomplete conversion or
a stale measurement, and the difference matters.

Card shells become `Card`; a non-interactive mark becomes a badge and an
interactive one a `Chip` at the floor; focus suppression becomes the **imported**
focus-ring expression, never re-spelled; a hand-rolled status box becomes a status
region with its alert role in the semantic token.

Then apply **the navigation pair**: a direct import of `AppNav` replacing the
phone-locked wrapper, same four props in the same order, verbatim analog
`src/app/(public)/gallery/page.tsx:2` and `:125-132`; plus the declaring `div`
around the **shell**, copied verbatim from `src/app/(public)/gallery/page.tsx:110`,
with the navigation as its **sibling**, moved up a level out of the deleted page
root. **Mounting means importing `AppNav` directly.**

**Width may change layout, never membership**, and on the member dashboard that
sentence is the product rather than a formality: what a member can see here *is*
the community's promise. The same four props go in, so no entry appears or
disappears, and no capability check is touched.

**Do not open the media section or the account controls.** They are plan 41.2-14's
and this wave runs them in parallel.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-no-viewport-read.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'layout/AppNav' 'src/app/(members)/dashboard/page.tsx'` returns exactly **1**; `layout/MobileNav` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'nav-inset-inline-start' 'src/app/(members)/dashboard/page.tsx'` returns exactly **1**, byte-identical to `src/app/(public)/gallery/page.tsx:110` — both lines pasted
    - The declaring `div` wraps the shell and the navigation is its sibling — quote the JSX; the width is `default`; the four props are quoted before and after; the SUMMARY carries *no capability check touched*
    - The raw-palette grep returns **0** and the legacy-token grep returns **0**; the SUMMARY states the pre-conversion counts from plan 41.2-01's dry run beside them and says whether they agreed
    - `LC_ALL=C /usr/bin/grep -cE 'min-h-dvh|max-w-|focus:outline-none|sm:|animate-pulse' 'src/app/(members)/dashboard/page.tsx'` returns **0**
    - **The money grep is clean and quoted line by line**
    - `git status --porcelain` shows nothing for the media section, the account or authentication controls, the referral control, or any of the four shared money-core files
    - `node scripts/verify-no-viewport-read.mjs` exits 0
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The member dashboard is in the shell with the navigation pair, the phase's largest palette debt is cleared, and the result was checked against the number wave 0 measured rather than against a feeling.</done>
</task>

<task type="auto">
  <name>Task 2: The token list and the placeholder — eleven pulses, and a lifecycle nothing re-derives</name>
  <files>src/app/(members)/dashboard/DashboardDrinkTokens.tsx, src/app/(members)/dashboard/loading.tsx</files>
  <read_first>
    - `src/app/(members)/dashboard/DashboardDrinkTokens.tsx` — 204 lines, in full. It separates a member's active tokens from their completed ones, and it renders timestamps against the completed group
    - `src/app/(members)/dashboard/loading.tsx` — 69 lines, 19 legacy tokens, **eleven** hand-rolled pulses, the most of the five placeholders in the phase
    - **The placeholder analog:** `src/app/(admin)/admin/(work)/members/loading.tsx:36-42,70-75`, converted by plan 41-10 — the list form; and `src/components/ui/Skeleton.tsx:20-53,93,123,160,191`
    - **The card analogs:** `src/components/ui/Card.tsx:47` and `src/components/ui/Chip.tsx:58,161`
    - `src/app/(public)/events/[slug]/DrinkTokenCard.tsx` after plan 41.2-10's diff — **read it, do not open it.** It is the converted shape this list's rows should sit consistently with
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.2 (the five placeholders, with **the constraint that binds hardest on this one**), §3 rows 3, 7, 10, 11
    - `.claude/rules/ticketing-payments.md` — a token's lifecycle, and that a status corrects forward
  </read_first>
  <action>
**The token list.** Convert onto the primitives: card shells become `Card`, state
marks become badges or `Chip` at the floor, focus suppression becomes the
**imported** focus-ring expression. Sit the rows consistently with the token card
plan 41.2-10 converted, without opening that file.

**Nothing about a token's state is re-derived.** Which tokens are active and which
are completed, and what a completed one shows, is decided by a lifecycle this
component renders and does not own. Assert by filtered diff that no state
expression, no grouping predicate and no timestamp derivation moved, and quote the
grouping predicate before and after.

**The placeholder.** Take the list analog and delete all eleven hand-rolled
pulses. Three constraints travel with the primitive, and the second one **binds
hardest on this file of all five**:

1. **The placeholder's width must equal the page's** — `default` — or content
   jumps sideways when data lands.
2. **Every count is a literal and stands for nothing.** No query runs before a
   placeholder renders, and this placeholder stands in front of **a member's own
   token list**: a count chosen to look like a plausible number of tokens is a
   claim about that member's purchases, made before anything was read. Pick a
   shape, not a number, and say in the SUMMARY that the count is a literal.
3. **Do not append a radius to the line primitive** — DEF-41-05, still open,
   measured in the emitted sheet.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'animate-pulse' 'src/app/(members)/dashboard/loading.tsx'` returns **0**; `components/ui/Skeleton` returns at least **1**
    - The placeholder's shell width is `default`, equal to the page's — quote both lines
    - The SUMMARY states explicitly that every count in the placeholder is a literal standing for nothing, and names the count used
    - No radius is appended to a line primitive's call site — quote each call site
    - The raw-palette grep returns **0** and the legacy-token grep returns **0** for both files; `focus:outline-none`, `sm:`, `max-w-` each return **0**
    - The grouping predicate that separates active from completed is quoted before and after and asserted byte-identical
    - `git diff` filtered for a state expression, a grouping predicate or a timestamp derivation shows only rendering changes — the filter and its result are pasted
    - **The money grep is clean and quoted line by line** for both files
    - `git status --porcelain` shows exactly the three files this plan declares across its two tasks
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>A member's token list is on the primitives with its grouping byte-identical, and eleven hand-rolled pulses are gone in favour of a placeholder whose counts are admitted to stand for nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member → their own dashboard | what a member can see here is the community's promise made concrete |
| token lifecycle → the list | this component renders a state it does not own; recomputing it would change what a member believes they hold |
| placeholder → a member's expectations | a count that looks plausible is a claim made before anything was read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-80 | Elevation of privilege | a navigation entry appearing or disappearing with the width on the member surface | mitigate | UI-SPEC §0 rule 5; the four props quoted before and after; *no capability check touched* asserted |
| T-41.2-81 | Tampering | a token's state or grouping re-derived under a visual mandate | mitigate | the grouping predicate quoted before and after; the diff filtered for state expressions, grouping predicates and timestamp derivations, and its result pasted |
| T-41.2-82 | Repudiation | a conversion declared complete on a file whose debt was never counted | mitigate | the result is compared against plan 41.2-01's per-file check-A and check-B hit lists, and any disagreement is stated as either an incomplete conversion or a stale measurement |
| T-41.2-83 | Repudiation | a placeholder count read as a number of tokens | mitigate | the count is a literal, stated as such in the SUMMARY, and its width matched to the page's so nothing shifts when the real list lands |
| T-41.2-84 | Denial of service | content under a 224px column at 768px and above | mitigate | the pair lands in one commit; the declaration asserted byte-identical to the gallery specimen |
| T-41.2-85 | Repudiation | two plans in one wave opening one file | mitigate | the media section, the account and authentication controls, the referral control and the money core are all excluded by name and asserted unmodified by `git status` |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run verify` exits 0 with no refusal
- `git status --porcelain` shows exactly three modified files
- The declaration line is pasted against the gallery specimen
- The pre- and post-conversion palette counts are stated side by side
- The grouping predicate is quoted before and after
</verification>

<success_criteria>
The member dashboard is converted, mounts the responsive navigation with its
clearance declared, and its palette debt is cleared against a number rather than a
feeling; a member's token list groups exactly as it did; and eleven hand-rolled
pulses are replaced by a placeholder that admits its counts stand for nothing.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-13-SUMMARY.md`
when done, with the declaration line against the gallery specimen, the navigation
prop block, the pre/post palette counts side by side, the grouping predicate
before and after, the placeholder's literal count, and the money grep.
</output>
