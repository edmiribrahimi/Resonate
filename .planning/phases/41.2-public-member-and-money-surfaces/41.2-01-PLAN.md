---
phase: 41.2-public-member-and-money-surfaces
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md
  - .planning/phases/41.2-public-member-and-money-surfaces/deferred-items.md
autonomous: true
requirements: [DS-07, RESP-02, RESP-03]
must_haves:
  truths:
    - "The phase knows G1, G4 and G5's real numbers for its ten surfaces before any wave is cut"
    - "The own-maximum set is the measured set, not the research's set, and every page file in it has a written disposition"
    - "Criterion 3's scope is written down: check F is either specified or read out of scope, in a sentence somebody can disagree with"
    - "The four silent catches on the money path are recorded and routed, and none of them is fixed here"
  artifacts:
    - path: ".planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md"
      provides: "the dry run's exact gate output, the re-measured own-maximum census with a disposition per page file, and the check-F reading"
      contains: "touch-targets"
    - path: ".planning/phases/41.2-public-member-and-money-surfaces/deferred-items.md"
      provides: "the four silent catches on the money path, at file:line, recorded and routed"
      contains: "GuestTokenDisplay"
  key_links:
    - from: ".planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md"
      to: "scripts/conversion-manifest.mjs"
      via: "the ten CONVERTED entries appended on a throwaway branch and reverted"
      pattern: "CONVERTED"
---

<objective>
Learn this phase's own gate numbers before committing to anything, and close the
three readings that would otherwise strand a criterion at the end.

Purpose: the touch-target census this phase was scoped on is an **approximation
at LOW-MEDIUM confidence** (`41.2-RESEARCH.md` assumption A1) — 99 raw
interactive elements counted by a line-window heuristic, not by the gate's
parser. The gate resolves each element to a tag and applies eight exemptions. The
difference between those two numbers is the difference between a plan and a
phase: **a G5 red discovered here is a task; the same red discovered in wave 7 is
a re-plan.** The only thing that settles it is declaring the ten surfaces and
running the gate, which is a repository write and therefore a plan's first task
rather than research's.

Two of the three readings **block criterion 3**. The own-maximum set is not the
research's four — `41.2-PATTERNS.md` §7.2 re-measured it and two of the four are
different files — and one of the corrected members, `/artists/[slug]`, is in wave
1, the cheap wave, which would therefore ship a check-D red on its first
declaration unless the disposition is written before it.

Output: one findings document carrying real numbers instead of estimates, one
deferred-items file, and a tree byte-identical under `scripts/` to the one this
plan opened.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-VALIDATION.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
Standing rules for every plan in this phase. Repeated in each plan rather than
kept in a preamble, because a preamble is a place nobody reads.

1. **This plan leaves no gate edited.** The manifest change here is made on a
   throwaway branch and reverted. Plans 41.2-05, 41.2-09 and 41.2-19 are the
   phase's only *permanent* gate editors (D-41.1-22).
2. **41.2 does not edit the spine.** `src/components/layout/AppNav.tsx`,
   `src/components/layout/MobileNav.tsx`, `src/components/ui/Button.tsx`,
   `src/components/ui/Typography.tsx` are the only bridge to Phase 42. Needing to
   edit one is a **HALT**, decided here and not at execution time.
3. **No server action is opened.** Stop condition 2.
4. **No package is installed.** The inherited contract says *none*; needing one
   is a HALT and an owner decision.
5. **DEF-41-01 — never spell a bracketed arbitrary-value utility in prose.**
   Tailwind's source detection scans `.planning/`, and a document quoting one
   compiled a dead rule into the shipped stylesheet. Refer to the column-clearance
   declaration as `src/app/(public)/gallery/page.tsx:110`; do not retype it.
6. **`.planning/` is public.** Roles, never people. No venue under negotiation,
   no unannounced date, no line-up, no contact.
7. **No `npm run dev`, no headless render, no running application** (D-41.2-04).
   Every human observation in this phase is `pending`, batched to the end-of-v1.5
   sitting.
8. **⚠ `scripts/verify-conversion.mjs` is invisible to the shell's default
   `grep`.** It holds two literal NUL bytes and the default `grep` here is
   `ugrep -I`, which skips binary files **silently**. Use
   `LC_ALL=C /usr/bin/grep` or the Read tool. **A zero-hit grep there is not
   evidence of absence.**
9. **macOS/BSD.** `grep -E`, `sed -i ''`.
10. **There is no test runner.** Never write that something is verified because
    tests pass. The proofs available are a gate command with its exit code, a
    grep with an expected count, a source assertion, `npm run build`, and a
    written human procedure whose result is `pending` until a person looks.

**Quick run for this phase:**
`npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: The dry run — declare the ten on a throwaway branch, record G1/G4/G5, revert</name>
  <files>.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md</files>
  <read_first>
    - `scripts/conversion-manifest.mjs` — `CONVERTED` opens at `:442` and closes at `:674`. Read the last five entries at `:654-673`: the shape is `[route, pageFile, width, reason]` and they are the model for the ten temporary entries
    - `scripts/conversion-manifest.mjs` — `checkManifest()`, which refuses on an empty list and on a missing path, and compares a declared width against the closed lists
    - `scripts/verify-conversion.mjs` — **with the Read tool or `LC_ALL=C /usr/bin/grep`, never the default grep.** Check D at `:3134-3184` (the page-file-only maximum scan), check E's pairing at `:2628-2702` and `:2785-2820` (the responsive-mount and column-clearance sets, compared in both directions), `WIDE_ROUTES` `:1265`, `FOCUS_ROUTES` `:1293`, `TYPOGRAPHIC_MEASURES` `:1053`
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-RESEARCH.md` §2.3 — the census this task exists to replace, and its own label as an approximation
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §4.1 and §4.3 — what check E reads, and the ten mount sites with their page-file paths
  </read_first>
  <action>
Create a throwaway branch. On it, append **ten temporary `CONVERTED` entries** —
one per surface, each `[route, pageFile, width, reason]`, every width `default`
per D-41.2-02, every reason a one-line placeholder saying it is a dry-run entry
that will be reverted. The ten routes and their page files are enumerated in
`41.2-PATTERNS.md` §4.3; take the paths from there and **confirm each exists on
disk before writing it**, because `checkManifest()` refuses on a missing path and
a refusal is exit 2 with nothing measured — which is not the measurement this
task wants.

Then run `npm run verify` and **capture the output verbatim**, not summarised.
Record in the findings document, per gate:

- **G1/G4 (`verify-conversion`)** — surfaces declared, files reached by the walk,
  route-adjacent files added, files scanned by A/B/D, and **check A's and check
  B's hits per file**. This is how much raw palette and how many legacy tokens the
  ten closures really carry, as opposed to what a replica walker estimated.
- **Check D specifically** — which page files it names in `pagesWithOwnMaximum`.
  That list is task 2's input and it must come from the gate, not from a grep.
- **Check E specifically** — the declaring set and the mounting set, printed in
  both directions. With the ten declared and none of them yet moved to `AppNav`,
  this is expected to be quiet; record it anyway, because it is the baseline the
  ten navigation conversions will be diffed against.
- **G5 (`verify-touch-targets`)** — surfaces in scope, JSX files in scope,
  **elements measured**, elements failing, and the per-file breakdown of every
  failure with the exemption it missed. Today the gate measures 36 elements across
  77 files and passes; record the new pair.
- **G2 (`verify-dialogs`)** and **G6 (`verify-breakpoints`)** — unchanged by a
  manifest edit, so record them as the **control**: if either moves, the dry run
  measured something other than what it thought it did.

**Then revert, and prove the revert.** Restore
`scripts/conversion-manifest.mjs`, return to the working branch, delete the
throwaway branch, and assert `git status --porcelain -- scripts/` is empty.

**Write the consequence, not only the numbers.** Close the document with plain
sentences saying what the measured G5 number changes about the wave shape:
whether any surface needs a task nobody planned, and whether any red is a
*correct file the gate reddens on*. §0 rule 3 is standing — *a gate that reddens
on a correct file gets switched off* — and the precedent `DEF-41.1-24-01` was
closed by a signed contract edit, not by widening an exemption. If this run
produces that shape, **name it as an owner question rather than resolving it.**
  </action>
  <verify>
    <automated>npm run verify; echo "exit=$?"; git status --porcelain -- scripts/</automated>
  </verify>
  <acceptance_criteria>
    - `41.2-WAVE0-FINDINGS.md` exists and carries a section per gate: G1/G4, check D, check E, G5, G2, G6
    - The G5 section states **two integers** — elements measured and elements failing — and lists every failing element with its file and the exemption it missed. A zero is stated as a measured zero with the command beside it
    - The check-D section lists the page files the gate itself named, compared line by line against `41.2-PATTERNS.md` §7.2's re-measured set; any disagreement is recorded with **the gate's output winning**
    - `git status --porcelain -- scripts/` outputs nothing — the revert is proved, not claimed
    - `git branch --list` does not show the throwaway branch
    - `npm run verify` on the working branch exits 0 and `verify-conversion` reports **28** declared surfaces — the pre-existing number
    - `node scripts/verify-dialogs.mjs` still reports `REMAINING = 8` and `node scripts/verify-breakpoints.mjs` still reports 5 files / 16 uses — the control held
  </acceptance_criteria>
  <done>The phase knows its own G1, G4 and G5 numbers, `scripts/` is byte-identical to where this task started, and the consequence for the wave shape is written in sentences.</done>
</task>

<task type="auto">
  <name>Task 2: The own-maximum reading — the measured set, with a disposition each</name>
  <files>.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md</files>
  <read_first>
    - `scripts/verify-conversion.mjs` check D at `:3134-3184` and `TYPOGRAPHIC_MEASURES` at `:1053` — **Read tool, not the default grep.** An entry requires the page file, the exact token **and** a fragment present on the same line; a near-miss on any of the three refuses with *"matches NOTHING: stale"*
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §7.2 — the re-measurement, and why two of the research's four are different files
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §6.2 — why `TYPOGRAPHIC_MEASURES` has no adopter, and the recorded casualty at `src/app/(admin)/admin/(work)/formats/page.tsx:197-207` (DEF-41-04), where a reading measure on a paragraph read to the gate as a container maximum and had to go
    - `src/app/page.tsx`, `src/app/(public)/artists/[slug]/page.tsx`, `src/app/(public)/tickets/[id]/page.tsx`, `src/app/(public)/events/[slug]/menu/page.tsx` — each at its own `max-w-` site, read in place with the enclosing element
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §4 — the three container forms and the closed lists
  </read_first>
  <action>
**Re-derive the census first, then decide.** Run `LC_ALL=C /usr/bin/grep -n
'max-w-'` over each of the ten page files and put the result in the document as a
table: file, line, the token, and the enclosing element. `41.2-RESEARCH.md` §1.6
and `41.2-PATTERNS.md` §7.2 disagree about which four files are in the set;
**neither is the authority — the tree is**, and this task's output is what four
surface plans will act on.

For each hit write **one of two dispositions with its reason**:

- **Delete the width** — correct when the maximum is a *container* maximum the
  shell now owns; the page was writing what `PageShell` writes.
- **Declare a `TYPOGRAPHIC_MEASURES` entry** — correct when the maximum is a
  *reading measure* on a text element, where deletion would set a paragraph
  running the full width of the shell. The mechanism was built in 41.1 **before**
  its first red run precisely so this case adds a line rather than losing a
  measure, and it has **zero entries today**.

Two hits are known in advance to be the hard ones and each needs its own
paragraph:

- `src/app/(public)/artists/[slug]/page.tsx:126` is a reading measure on a
  paragraph, and it is in **wave 1** — so the phase's first `TYPOGRAPHIC_MEASURES`
  decision lands there, not in wave 6, and it cannot be a wave-0 abstraction.
  Write the entry's three parts here (page file, exact token, the fragment that
  must be present on the same line) so plan 41.2-04 **applies** a decision instead
  of taking one — and state what makes that fragment survive the conversion plan
  41.2-04 is about to perform on the same line.
- `src/app/(public)/tickets/[id]/page.tsx` writes the focus measure **twice**.
  Record that `focus` is **unavailable** to this surface: check E forbids it on
  any surface mounting a navigation and D-41.2-01 mounts one on all ten. The
  disposition may therefore not be *"declare it focus and the maximum goes away"*.

**Edit no page file in this task.** The dispositions are applied by the surface
plans that own those files; applying them here as well would put two authors on
one line.
  </action>
  <verify>
    <automated>LC_ALL=C /usr/bin/grep -rn 'max-w-' src/app/page.tsx "src/app/(public)/artists/[slug]/page.tsx" "src/app/(public)/tickets/[id]/page.tsx" "src/app/(public)/events/[slug]/menu/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The findings document carries a table with one row per `max-w-` occurrence across all ten page files, each with file, line, token and enclosing element
    - Every row carries a disposition — **delete** or **declare** — and a reason of at least one sentence
    - The row for `src/app/(public)/artists/[slug]/page.tsx:126` carries the three parts a `TYPOGRAPHIC_MEASURES` entry needs, spelled out, plus the sentence about fragment stability under conversion
    - The row set for `src/app/(public)/tickets/[id]/page.tsx` records that `focus` is unavailable and why, citing check E
    - The document states, as a measured fact with its command, that `src/app/(public)/events/[slug]/page.tsx` carries **zero** `max-w-` occurrences — so the wave-7 plan does not go looking for a maximum that is not there, and does not record a false *removed*
    - `git status --porcelain -- src/ scripts/` outputs nothing — no source or gate file was touched by this task
  </acceptance_criteria>
  <done>Every own-maximum occurrence in the ten page files has a written disposition, taken from the tree rather than from either upstream document, and no page file was edited to reach it.</done>
</task>

<task type="auto">
  <name>Task 3: Check F decided in writing, and the four silent catches recorded and routed</name>
  <files>.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md, .planning/phases/41.2-public-member-and-money-surfaces/deferred-items.md</files>
  <read_first>
    - `scripts/conversion-manifest.mjs:399-438` — the whole argument for why `/admin` is not declarable. It is the argument a check-F constant would need to read, and what it lacks today is a constant
    - `scripts/verify-tables.mjs:534-571` (`MIGRATION_CLOSED`) and `:721-742` (the emptiness guard) — the template for turning a ratchet into an absolute, and the shape of a decision written above a constant
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-RESEARCH.md` §2.2 — the third row of the table, the gap no gate closes today, and assumption A3, which labels this a **scope reading and not a measurement**
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-RESEARCH.md` §3.5 — the four silent catches, at `file:line`
    - `.planning/phases/41.1-work-surface-conversion/deferred-items.md` — the shape of a deferred entry in this project
    - `.claude/rules/meta-gates.md` — *zero silent failures*, and the standing fact that no production error reaches a human by itself
  </read_first>
  <action>
**Two written answers and one recording. None of the three edits a gate.**

**(a) Check F.** Criterion 3 says *"the gates become the absolutes the UI-SPEC
wrote them as"*. That is true of `verify-dialogs` and `verify-breakpoints` once
their lists empty. It is **not** true of the manifest: nothing in
`verify-conversion.mjs` counts the `page.tsx` files on disk and compares them to
`CONVERTED`, so a forty-second page added next month is invisible and the phase
stays green. Decide, in writing, **one** of:

- **Check F is in scope** — then specify it here: a tree-side census of every
  `page.tsx` under `src/app`, each of which must be declared in `CONVERTED`,
  behind the Phase 42 fence, or on a written non-declarable list whose sole
  member today is `/admin`. Name the constant the list becomes, say where it
  lives, and state that the implementation is **plan 41.2-19's**, not this one's.
- **Check F is out of scope** — then write the reading: criterion 3 covers the
  three `REMAINING` lists and not the manifest, and say plainly what stays true
  after this phase that a reader might otherwise assume is closed.

Either answer is acceptable. **Leaving it unwritten is not**, because an
unwritten reading is the one that gets read the convenient way at the end.

**(b) The four silent catches.** Create the phase's `deferred-items.md` and record
all four with their `file:line`, what each swallows, and the consequence for the
person on the other side of it — the guest who paid, at the bar, in front of a
queue. Route each one: *a separate, non-visual plan, owned by whoever owns what a
buyer is told when a purchase fails.* State, in the same entry, that this
repository has **no error tracking**, so a logged error is a place nobody looks
and a failure that matters needs an observable effect.

**They are recorded, not fixed.** A plan that "improves" one while converting a
class string has changed behaviour on the money path under a visual mandate,
which is stop condition 2 in a different costume. Write that sentence into the
file so the next reader does not have to re-derive it.

**(c) The wallet-pass exit.** `src/lib/apple-wallet.ts:141-145` writes a venue
field onto a pass — a file that leaves the product, syncs to a device, and is
recalled by nothing. It carries zero class strings, so no plan opens it, which is
exactly why it is easy to leave off the venue-secrecy enumeration for
`/tickets/[id]`. Record it in `deferred-items.md` as an **enumeration entry**, not
a defect: the point is that the enumeration for that surface is now complete.
  </action>
  <verify>
    <automated>ls -1 .planning/phases/41.2-public-member-and-money-surfaces/deferred-items.md && LC_ALL=C /usr/bin/grep -c "GuestTokenDisplay\|GuestDrinkMenu\|apple-wallet" .planning/phases/41.2-public-member-and-money-surfaces/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - The findings document carries a section headed with the check-F decision, stating **in scope** or **out of scope** and the reading behind it in at least one paragraph. If in scope, it names the constant and assigns the implementation to plan 41.2-19
    - `deferred-items.md` exists in the phase directory and contains **four** entries for the silent catches, each with a `file:line`, what is swallowed, and the consequence to the buyer
    - Each of the four entries names its route — a separate non-visual plan — and none is marked fixed
    - `deferred-items.md` contains the sentence that there is no error tracking in this repository, so a logged error is a place nobody looks
    - `deferred-items.md` contains the wallet-pass enumeration entry citing `src/lib/apple-wallet.ts:141-145`
    - `git status --porcelain -- src/ scripts/` outputs nothing
    - `npm run verify` exits 0 and `npm run build` exits 0 — the tree this wave hands on is the tree it received
  </acceptance_criteria>
  <done>Criterion 3's scope is a written sentence somebody can disagree with, the four silent catches are recorded with their consequence and their route, and nothing on the money path was changed to achieve it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → gate | a declared entry makes a gate assert about a named file; a temporary entry left behind makes it assert forever |
| findings document → nine later plans | every surface plan acts on this document's dispositions; a wrong disposition here is a red in nine places |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-01 | Tampering | the ten dry-run entries surviving the revert | mitigate | `git status --porcelain -- scripts/` asserted empty **after** the revert, and the throwaway branch asserted deleted; the revert is proved rather than claimed |
| T-41.2-02 | Repudiation | a dry run that measured nothing and printed a tick | mitigate | `checkManifest()` refuses on a missing path with exit 2; every page-file path is confirmed to exist before it is written, and G2/G6 are recorded as an unchanged control |
| T-41.2-03 | Repudiation | an own-maximum disposition taken from a stale document | mitigate | the census is re-derived from the tree with the command recorded, and where the two upstream documents disagree the tree wins in writing |
| T-41.2-04 | Information disclosure | a silent catch "improved" while it is being recorded | mitigate | recorded, not fixed, with the sentence saying why in the file itself; `git status` asserts no source file moved |
| T-41.2-05 | Information disclosure | the wallet-pass venue exit left off an enumeration | mitigate | recorded as an enumeration entry at `file:line`, because `venue-secrecy.md` requires the list rebuilt by reading the code and this is the moment somebody is looking at that surface |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed; `41.2-RESEARCH.md`'s Package Legitimacy Audit records **none proposed**, and a conversion that needs one is a HALT |
</threat_model>

<verification>
- `npm run verify` exits 0 with no refusal; `npm run build` exits 0
- `git status --porcelain -- src/ scripts/` outputs nothing at the end of the plan
- `41.2-WAVE0-FINDINGS.md` carries all three sections: the dry run's gate output, the own-maximum table with a disposition per row, the check-F decision
- `deferred-items.md` carries four silent-catch entries plus the wallet-pass enumeration entry
- Every figure this plan quotes was produced by a command run in this plan, and the command is beside it
</verification>

<success_criteria>
The phase can be executed on measured numbers instead of estimates: G5's real
element count is known, every own-maximum occurrence has a disposition its owning
plan will apply rather than invent, criterion 3's scope is a sentence somebody
can disagree with, and four failures on the money path are visible in writing to
whoever eventually owns them — with the tree unchanged.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-01-SUMMARY.md`
when done, with the verbatim gate output of the dry run, the own-maximum table,
the check-F decision, and the `git status --porcelain` capture proving the revert.
</output>
