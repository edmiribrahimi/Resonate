---
phase: 41.2-public-member-and-money-surfaces
plan: 06
type: execute
wave: 3
depends_on: ["41.2-05"]
files_modified:
  - src/app/(public)/events/page.tsx
  - src/app/(public)/events/EventTabs.tsx
  - src/app/(public)/events/FormatFilterRow.tsx
  - src/app/(public)/events/loading.tsx
autonomous: true
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The events listing still names a venue only on a night whose stored secrecy flag is explicitly false, and still stands in the secret label everywhere else"
    - "The listing still declares force-dynamic, so no address-adjacent listing reaches a shared cache"
    - "The listing mounts the responsive navigation and declares the tablet-tier clearance, as a pair"
    - "The placeholder in front of the grid is the primitive, at the same width as the page it precedes"
  artifacts:
    - path: "src/app/(public)/events/page.tsx"
      provides: "the public events listing on PageShell at default width, navigation pair, force-dynamic retained"
      contains: "force-dynamic"
    - path: "src/app/(public)/events/loading.tsx"
      provides: "the grid placeholder on the Skeleton primitive at the page's own width"
      contains: "components/ui/Skeleton"
  key_links:
    - from: "src/app/(public)/events/EventTabs.tsx"
      to: "the stored secrecy flag on a night"
      via: "the ternary that chooses the secret label over a venue name"
      pattern: "venue_secret \\?"
---

> **`PageTitle` renders the `<h1>`, so a converted route file contains ZERO of them.**
> Measured: `src/components/ui/Typography.tsx:61` emits the tag; the already-converted
> `src/app/(public)/gallery/page.tsx` returns **0** for `grep -c '<h1'`. **Never write a
> `<h1>` count as a threshold for a file that adopts the primitive** — the criterion is
> unsatisfiable and its likely outcomes are a silent reinterpretation or a skipped step.
> Assert the primitive's **call site** instead, and report the count rather than requiring
> one. And check the whole **surface** before adding a title: a form component in the
> closure may already carry the heading, in which case adopting `PageTitle` on the page
> file gives the surface a **second** one.

> ## ⚠ Check E uses two different meanings of "mounted", and one of them will mislead you
>
> **Measured in wave 0** (`41.2-WAVE0-FINDINGS.md`). `scripts/verify-conversion.mjs`
> reports the navigation question twice, and the two answers disagree today:
>
> - its **route table** already prints all ten of this phase's surfaces as *mounting* a
>   navigation — because they reach one through `MobileNav`;
> - its **pairing check** uses `importsDirectly`, so those same ten count as **not
>   mounting**.
>
> **The failure this sets up:** a plan that reads the route table concludes the surface
> already mounts a navigation and therefore needs no clearance declaration — then ships
> the loud red, which at 768px is **content sitting under a 224px column**.
>
> **Mounting, for every assertion in this plan, means importing `AppNav` DIRECTLY.**
> Reaching it through `MobileNav` does not count. The pair is two halves and check E
> fails on either half alone:
>
> 1. `LC_ALL=C /usr/bin/grep -c 'layout/AppNav'` on the page file → **1**
> 2. `LC_ALL=C /usr/bin/grep -c 'layout/MobileNav'` on the page file → **0**
> 3. the tablet-tier clearance utility present, copied byte-identically from
>    `src/app/(public)/gallery/page.tsx:110`, on a `div` wrapping the shell with the
>    navigation as its **sibling**
>
> Pairing baseline is **2/2** (the two surfaces that already declare); the target at the
> end of the phase is **12/12**.
>
> **Read `scripts/verify-conversion.mjs` with `LC_ALL=C /usr/bin/grep` or the Read tool** —
> it holds two NUL bytes and the shell's default `grep` skips it silently, returning
> nothing with no error.

> **`npm run verify` cannot exit 0 inside a worktree, and that is expected.**
> Measured in wave 0: `verify:capabilities` **refuses with exit 2** when `.env.local`
> is absent, printing *"Nothing was measured"* **before touching anything**. A
> worktree has no `.env.local`, and putting Supabase credentials into one is
> forbidden by **D-41.2-04** — the road that does not touch production. So the
> executor-side bar is:
>
> **all 15 gates that reach a verdict pass, 0 fail, and `verify:capabilities`
> refuses for want of credentials.** A refusal that names its missing input is a
> gate declining to measure, not a gate failing — and it is distinguishable from
> both a pass and a failure, which is the whole point. Quote the tail of the run
> showing the counts.
>
> **Do not make it green.** Not by adding credentials, not by skipping the gate,
> not by rewording the criterion. The full 16/16 suite is run by the orchestrator
> in the main checkout after this wave merges, where `.env.local` exists.

<objective>
Convert the public events listing — the funnel into the community, and the
largest public surface in the phase — with its venue predicate held rather than
assumed.

Purpose: this surface renders **venue names**, and the rule that decides which
ones is not `isVenueVisible` here: it is the **stored flag on the night**, tested
in a form that treats a missing row or a failed join as secret. That form was
chosen deliberately (D-36-11: *of two candidates the narrower wins, and the
predicate is declared in the code*) and it is exactly the kind of expression a
conversion tidies into something that reads better and defaults open. A listing
that names a venue for a night whose flag never arrived is a published address,
and `venue_reveal_sent` is monotone — there is no undo.

This surface also carries the file the UI contract was **copied from**.
`FormatFilterRow.tsx` holds the tree's only prior art at the 44px floor and
composes a border at run time from a catalogue row's colour. The ordinary
direction is inverted here: the primitive is downstream of this file, not
upstream of it, so this is a judgement rather than a copy and the plan says which
way it went.

Output: four files converted, one placeholder off the hand-rolled pulse census,
and a venue predicate proved unchanged rather than described as unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). The surface is
   declared by the reconciliation plan 41.2-09.
2. **41.2 does not edit the spine** — `layout/AppNav.tsx`, `layout/MobileNav.tsx`,
   `ui/Button.tsx`, `ui/Typography.tsx`. Needing to is a **HALT**.
3. **No server action is opened.** Stop condition 2.
4. **No package is installed.**
5. **DEF-41-01** — never spell a bracketed arbitrary-value utility in prose. The
   clearance declaration is copied verbatim from
   `src/app/(public)/gallery/page.tsx:110`.
6. **`.planning/` is public.** No venue name, no unannounced date, no line-up and
   no series progressivo in the SUMMARY — routes and file paths only. This is the
   one plan in the phase whose subject matter is literally the list of nights, so
   the rule bites here more than anywhere.
7. **No `npm run dev`, no headless render** (D-41.2-04).
8. **⚠ `scripts/verify-conversion.mjs` is invisible to the default `grep`.**
   `LC_ALL=C /usr/bin/grep` or the Read tool.
9. **macOS/BSD.** `grep -E`, `sed -i ''`.
10. **No test runner exists.**

**Quick run:** `npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`

**Clean-file greps** (matcher `scripts/verify-conversion.mjs:850-916`): raw palette
`(bg|text|border|ring|from|to|via|fill|stroke|shadow|outline|decoration)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose|black|white)(-[0-9]{1,3})?`;
legacy token, same prefixes against `card-border|card|background|foreground`.

## The venue assertions for this surface — after every diff that opens either file

Anchored on **predicate text**; the line numbers are locators, verified
2026-08-14, and a line that moved is not a failure — an assertion pinned to a
moved line is, because it fails by matching nothing, which prints green.

1. **`export const dynamic = "force-dynamic";` stays present** in
   `src/app/(public)/events/page.tsx` *(locator `:29`)*. A statically renderable
   listing puts venue names in a shared cache.
2. **The stored-flag test stays written as an explicit comparison against
   `false`** — `sorted.some((p) => p.venue_secret !== false)` *(locator `:440`)*.
   The file says why on the lines above it: **the predicate is the stored flag**,
   and a row that never arrived, or a join that failed, must count as secret.
   Rewriting it as a plain truthiness test inverts the default for exactly the
   rows nobody can see.
3. **The label ternary stays a ternary with the secret branch first** —
   `v.venue_secret ? … : v.venue_name ?? v.venue_text` in `EventTabs.tsx`
   *(locator `:277-280`)*. Collapsing it, or reordering the fallback chain, is how
   a name reaches a card that should carry the label.
4. **The night select's column list is unchanged** *(locator
   `page.tsx:270`)*, proved as a **multiset over comment-stripped source** rather
   than by reading a diff — the technique plan 41.1-22 established. A column
   dropping out of that list does not fail anything; it changes what a predicate
   sees.
5. **The qualified embed hint on the series relation survives.** `page.tsx:270`
   carries an explicitly named foreign key on the series embed. Unqualified, this
   embed answers `HTTP 300 PGRST201` because two relations exist, and **it fails
   silently** — the client returns no error and the page renders as though there
   were no events. Assert the qualifier present after the diff.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: The listing page — shell, navigation pair, and the venue predicate proved unmoved</name>
  <files>src/app/(public)/events/page.tsx</files>
  <read_first>
    - `src/app/(public)/events/page.tsx` — 578 lines. Read `:1-60` (the imports, the declared render mode, the row types), `:260-290` (the select and its qualified embed), `:390-450` (the grouping, the stored-flag predicate and the comment that states which of two candidates it is), and `:520-578` (the root, the header, the heading and the navigation mount)
    - **The analog:** `src/app/(admin)/admin/(work)/events/page.tsx`, converted by plan 41.1-07 — a listing page with a placeholder in front of it
    - **The public navigation analog:** `src/app/(public)/gallery/page.tsx:88-133`
    - `src/components/ui/PageShell.tsx:146,160`; `src/components/ui/Typography.tsx:53`
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.1 — this page is one of the files whose root, header and heading are **byte-for-byte** the four-part deletion; §3 rows 1, 2, 7, 8, 10, 11; §4
    - `.claude/rules/venue-secrecy.md` and `.claude/rules/meta-gates.md` — the monotone guard, and the rule that of two candidate predicates the narrower wins
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md` — this page file is expected to carry **no** own maximum; confirm against the findings table rather than assuming
  </read_first>
  <action>
Adopt `PageShell` at `width="default"`. **`default` is an answer, not a fallback**
(D-41.2-02). This surface was the one candidate considered for the wide form — a
multi-column grid of nights is §4's stated criterion — and it was **deferred, not
rejected**: it is reversible in one line of the shell's call, and revisiting it is
a decision for after this phase's numbers exist. Say so in the SUMMARY, so the
next reader sees a deferral rather than an oversight.

Apply the four-part deletion: the outer root goes to the shell; the repeated
horizontal padding goes with it; the header's bottom padding becomes a margin;
the heading becomes `PageTitle`.

Then apply **the navigation pair**: replace the phone-locked wrapper's import with
a direct import of `AppNav` and the wrapper element with `AppNav`, same four props
in the same order (role, status, capability set, live-assignment capability set),
verbatim analog `src/app/(public)/gallery/page.tsx:2` and `:125-132`; and add the
declaring `div` around the **shell**, copied verbatim from
`src/app/(public)/gallery/page.tsx:110`, with the navigation as its **sibling**.
The mount moves **up a level** out of the deleted page root, into a fragment.
**Mounting means importing `AppNav` directly** — reaching it through the wrapper
does not count to check E. Width may change layout, never membership: the same
four props go in, so no entry appears or disappears.

**Then assert the five venue assertions of the conversion rules, by predicate
text.** Every one of them is about a line this task did not intend to touch, which
is precisely why it is asserted rather than assumed: the failure this guards
against is a tidy, not a decision.

**The card and pill conversions come with a caution.** This page renders format
identification colour. `verify-semantic-separation` check C forbids a single line
carrying both a semantic colour utility and a format identifier; the check is
line-scoped, so a correct substitution stays green — but this is the first time in
the project a semantic substitution has landed in a file that also renders format
identity. Run that gate explicitly and paste its result.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-semantic-separation.mjs && node scripts/verify-sunset-gradient.mjs && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'force-dynamic' 'src/app/(public)/events/page.tsx'` returns **1**; quote the whole statement
    - `LC_ALL=C /usr/bin/grep -c 'venue_secret !== false' 'src/app/(public)/events/page.tsx'` returns at least **1**, and `LC_ALL=C /usr/bin/grep -cE 'venue_secret\)|!p\.venue_secret \?' 'src/app/(public)/events/page.tsx'` is compared against its pre-diff value and stated equal. Quote the predicate with the comment above it
    - The night select's column list is proved unchanged as a **multiset over comment-stripped source**; paste the before and after multisets and their equality
    - `LC_ALL=C /usr/bin/grep -c 'party_series!' 'src/app/(public)/events/page.tsx'` returns at least **1** — the qualified embed survived; quote the line
    - `LC_ALL=C /usr/bin/grep -c 'layout/AppNav' 'src/app/(public)/events/page.tsx'` returns exactly **1**; `layout/MobileNav` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'nav-inset-inline-start' 'src/app/(public)/events/page.tsx'` returns exactly **1**, byte-identical to `src/app/(public)/gallery/page.tsx:110` — both lines pasted
    - The declaring `div` wraps the shell and the navigation is its sibling — quote the JSX; the four props are quoted before and after; the SUMMARY carries *no capability check touched*
    - `LC_ALL=C /usr/bin/grep -cE 'min-h-dvh|max-w-' 'src/app/(public)/events/page.tsx'` returns **0**
    - The raw-palette grep returns **0**; the legacy-token grep returns **0**; `focus:outline-none`, `sm:`, `animate-pulse` each return **0**
    - `node scripts/verify-semantic-separation.mjs` exits 0 and its check C result is pasted
    - `npm run build` exits 0; `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan)
  </acceptance_criteria>
  <done>The listing is in the shell with the navigation pair, its render mode is still declared rather than derived, and its venue predicate, its select and its qualified embed are proved unmoved by text rather than described as unmoved.</done>
</task>

<task type="auto">
  <name>Task 2: EventTabs — the tab surface, and the label that stands in for an address</name>
  <files>src/app/(public)/events/EventTabs.tsx</files>
  <read_first>
    - `src/app/(public)/events/EventTabs.tsx` — 495 lines. Read `:18-45` (the docblock, which states in place that the **only** rendering of a venue on this page is the ternary below, and that the other props were dead and travelled anyway), `:270-290` (that ternary), and `:240-260` (the format marker and the draft pill eight lines below it)
    - **The analog:** `src/app/(admin)/admin/formats/FormatsCatalogue.tsx`, converted by plan 41-09 — a tabbed catalogue with format identification colour on its rows
    - `src/components/ui/Chip.tsx:58,161` — the floor and the export; `src/components/ui/Card.tsx:47`
    - `scripts/verify-semantic-separation.mjs` check C — no line under `src/` carries both a semantic colour utility and a format identifier. **The check is line-scoped**, which is what makes a correct substitution here safe and a careless one red
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §3 rows 7, 8, 9, 10, 11
    - `.claude/rules/venue-secrecy.md` — the editorial counterpart of the same rule: an address in a listing annuls a scheduled reveal
    - `.claude/rules/production-calendar.md` — the format sigla and the series progressivo are already on a poster; nothing here renumbers, re-sorts or re-derives one
  </read_first>
  <action>
Convert the tab strip, the cards and the pills onto the primitives: a card shell
becomes `Card`; a pill that is a link or a button becomes `Chip`, which carries
the 44px floor; focus suppression becomes the **imported** focus-ring expression.

**The venue label ternary is not touched, and that is the point of this task.**
The file's own docblock says the ternary is the only place a venue is rendered on
this page. Keep it a ternary, keep the secret branch first, and keep the fallback
chain in its order. Assert it by text after the diff and quote it in the SUMMARY,
with the docblock line that says it is the only such site.

**The draft pill's raw colour sits eight lines below a format marker.** Substitute
it to the semantic token, and then run `verify-semantic-separation` explicitly:
the check is line-scoped, so the substitution is correct — but this is the first
substitution in the project to land in a file that also renders format identity,
and the SUMMARY should say that it was checked rather than assumed.

**Nothing here renumbers, re-sorts or re-derives a series progressivo or a format
mark.** They are already on a poster; a progressivo is a monotone guard. State it
so it stays true.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-semantic-separation.mjs && node scripts/verify-sunset-gradient.mjs && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'venue_secret ?' 'src/app/(public)/events/EventTabs.tsx'` returns at least **1**, and the whole ternary with its fallback chain is quoted before and after and asserted byte-identical
    - `LC_ALL=C /usr/bin/grep -c 'venue' 'src/app/(public)/events/EventTabs.tsx'` is compared against its pre-diff value and stated equal — no new venue read was introduced
    - The raw-palette grep returns **0**; the legacy-token grep returns **0**; `focus:outline-none`, `sm:`, `max-w-`, `animate-pulse` each return **0**
    - `node scripts/verify-semantic-separation.mjs` exits 0 and its check C output is pasted
    - `node scripts/verify-sunset-gradient.mjs` exits 0 — the four-stop signature is still applied nowhere under `src/`
    - `git diff` filtered for a sort, a numbering or a series expression shows nothing — quote the filter used and its empty result
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The tab surface is on the primitives, the one place a venue is named still chooses the secret label first, and a semantic substitution has landed beside format identity with the gate run rather than trusted.</done>
</task>

<task type="auto">
  <name>Task 3: FormatFilterRow and the grid placeholder — the inversion, decided in writing</name>
  <files>src/app/(public)/events/FormatFilterRow.tsx, src/app/(public)/events/loading.tsx</files>
  <read_first>
    - `src/app/(public)/events/FormatFilterRow.tsx` — 184 lines, in full. Its two floor declarations are at `:115` and `:162`; its run-time border composition is at `:150-157`, with the comment above it saying what happens where that CSS function is unsupported
    - `src/components/ui/Chip.tsx:31` — **the primitive's own docblock names this file as the source of the floor.** Two occurrences of the minimum exist tree-wide and both are here
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §6.3 — **this is one of the map's three declared gaps.** The ordinary direction is inverted: the converted primitive is downstream of this file, not upstream of it, so replacing its floor declarations with the primitive is a judgement and not a copy
    - `scripts/verify-sunset-gradient.mjs` — its own header says it cannot see a border composed at run time. **Not a violation; recorded so a converter does not "simplify" it into a literal**
    - `src/app/(public)/events/loading.tsx` — 30 lines, 9 legacy tokens, 4 hand-rolled pulses
    - **The placeholder analog, and it is the grid one, not the list one:** `src/app/(public)/gallery/loading.tsx:35-38`, converted by plan 41-08. The list form is `src/app/(admin)/admin/(work)/members/loading.tsx:36-42,70-75`
    - `src/components/ui/Skeleton.tsx` — `SkeletonLine` `:93`, `SkeletonCard` `:123`, `SkeletonTile` `:160`, `SkeletonAvatar` `:191`, and `:20-53` for the three constraints that travel with them
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.2 — the five placeholders, their pulse counts and their widths
  </read_first>
  <action>
**FormatFilterRow — decide the inversion in writing before the diff.** Two
candidate readings, and the plan must choose one and say which:

- **Adopt `Chip`**, making this file the primitive's consumer instead of its
  ancestor. Almost certainly right, and it removes the last two hand-written floor
  declarations in the tree.
- **Leave the floor declarations in place** and convert only the colour and focus
  utilities, on the ground that this file is the contract's own specimen.

Whichever is chosen, **the run-time border composition stays**. Do not simplify it
into a literal: the border colour is mixed from a catalogue row's colour at run
time, the comment above it already states what happens where the CSS function is
unsupported, and `verify-sunset-gradient`'s own header says the gate cannot see
it. A literal would be a brand colour hard-coded from data — the opposite of what
the catalogue exists for.

**The placeholder takes the grid analog, not the list one.** `loading.tsx` stands
in front of a grid of nights, so `src/app/(public)/gallery/loading.tsx:35-38` is
the specimen. Three constraints travel with the primitive and a plan must not lose
them:

1. **The placeholder's width must equal the page's width** — `default` here — or
   content jumps sideways when data lands.
2. **Every count is a literal and stands for nothing.** No query runs before a
   placeholder renders, so the number of tiles is a shape and never a claim about
   how many nights exist.
3. **Do not append a radius to the line primitive** — DEF-41-05, still open,
   measured in the emitted sheet.

All four hand-rolled pulses in this file are deleted by the primitive.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-sunset-gradient.mjs && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY states which of the two readings was taken for `FormatFilterRow.tsx`, in at least a paragraph, citing `41.2-PATTERNS.md` §6.3 and `src/components/ui/Chip.tsx:31`
    - `LC_ALL=C /usr/bin/grep -c 'color-mix' 'src/app/(public)/events/FormatFilterRow.tsx'` returns the same value as before the diff — quote the line and its comment
    - `LC_ALL=C /usr/bin/grep -rc 'min-h-11' src/components/ui/Chip.tsx` is stated, and the SUMMARY says whether the floor now lives in the primitive alone or in both places, and why
    - `LC_ALL=C /usr/bin/grep -c 'animate-pulse' 'src/app/(public)/events/loading.tsx'` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'components/ui/Skeleton' 'src/app/(public)/events/loading.tsx'` returns at least **1**, and `components/ui/PageShell` returns at least **1** with `width` equal to the page's `default` — quote both lines
    - `LC_ALL=C /usr/bin/grep -cE 'rounded-' 'src/app/(public)/events/loading.tsx'` shows no radius appended to a line primitive's call site — quote each call site
    - The raw-palette grep returns **0** and the legacy-token grep returns **0** for both files; `focus:outline-none` and `sm:` each return **0**
    - `node scripts/verify-sunset-gradient.mjs` exits 0
    - `node scripts/verify-touch-targets.mjs` exits 0
    - `git status --porcelain` shows exactly the four files this plan declares
    - `npm run build` exits 0; `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan)
  </acceptance_criteria>
  <done>The filter row's inversion is a decision somebody wrote down, its run-time border survived as a composition rather than a literal, and the grid placeholder is the primitive at the page's own width.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous reader → the list of nights | this is the public funnel; every night's venue name on it is a publication |
| stored secrecy flag → the label | the flag, not a render-side verdict, decides what stands in for an address here |
| shared cache → a later reader | a statically renderable listing serves one reader's page to a different one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-29 | Information disclosure | the stored-flag test rewritten as plain truthiness | mitigate | asserted by predicate text — the explicit comparison against `false` must survive, with the file's own comment quoted; a missing row or failed join must keep counting as secret (D-36-11) |
| T-41.2-30 | Information disclosure | the label ternary collapsed or its fallback chain reordered | mitigate | the whole ternary quoted before and after and asserted byte-identical, with the docblock line that says it is the only venue-rendering site on this page |
| T-41.2-31 | Information disclosure | a listing of venue names reaching a shared cache | mitigate | `force-dynamic` asserted present after the diff, by statement text |
| T-41.2-32 | Information disclosure | a column dropping out of the night select | mitigate | multiset over comment-stripped source, before and after, the technique plan 41.1-22 established |
| T-41.2-33 | Denial of service | the qualified embed hint lost | mitigate | asserted present; unqualified it answers with an ambiguity error and **fails silently**, rendering the page as though there were no events |
| T-41.2-34 | Denial of service | content under a 224px column at 768px and above | mitigate | the navigation pair lands in one commit, the declaration asserted byte-identical to the gallery specimen |
| T-41.2-35 | Tampering | a format mark or series progressivo re-derived | mitigate | a filtered diff asserted empty; a progressivo is a monotone guard and is already on a poster |
| T-41.2-36 | Repudiation | a run-time colour composition simplified into a literal | mitigate | the composition asserted unchanged by count and quoted; the gate that would otherwise catch it says in its own header that it cannot see it |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan)
- `git status --porcelain` shows exactly four modified files
- All five venue assertions are quoted by predicate text, after the diff
- The select's column multiset is pasted before and after and asserted equal
- The declaration line is pasted beside the gallery specimen
</verification>

<success_criteria>
The public events listing is converted whole, mounts the responsive navigation
with its clearance declared, and its four venue-critical properties — the declared
render mode, the stored-flag comparison, the label ternary and the select's
columns — are proved unchanged by text rather than asserted in prose.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-06-SUMMARY.md`
when done, with the five venue assertions quoted, the select multiset before and
after, the declaration line against the gallery specimen, the navigation prop
block before and after, the written reading for `FormatFilterRow.tsx`, and the
semantic-separation gate output.
</output>
