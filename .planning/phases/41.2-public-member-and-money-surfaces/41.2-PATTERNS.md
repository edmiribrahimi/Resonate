# Phase 41.2: Public, Member and Money Surfaces — Pattern Map

**Mapped:** 2026-08-14
**Files classified:** 51 perimeter files (10 page files · 5 route-adjacent placeholders ·
36 components, actions and libs)
**Analogs found:** 48 of 51 — **3 gaps**, §6

> **DEF-41-01 — one class string is written broken on purpose.** Tailwind's source
> detection scans `.planning/`, and `DI-41.1-01` records a dead rule that shipped to every
> user because a document quoted a bracketed utility in prose. The **bracketed
> arbitrary-property utility** of §4 is therefore written broken and its two verbatim sites
> are cited by `file:line`. Ordinary utilities are quoted whole: every one of them is live
> in `src/` already, so compiling it from this page harvests nothing — which is the
> measurement `41.2-RESEARCH.md`'s DEF-41-01 note reports for the built stylesheet.
>
> **Read-only pass.** Nothing under `src/` or `scripts/` was modified. The only file
> written is this one. No `npm run dev`, no database read or write.
>
> **Every analog claim carries a `file:line`.** §7 records **six measurements that
> disagree** with `41.2-RESEARCH.md`, each with the command — four of them change what a
> plan asserts.
>
> Contains no venue under negotiation, no unannounced date, no line-up and no personal
> name: `.planning/` is tracked and this repository is **public**.

---

## 0. Why this map is short on invention and long on citation

`41.1-PATTERNS.md` §0 opened by saying the ordinary pattern-mapper question had no
purchase, because nothing in Phase 41.1 was a new file. **That is more true here, not
less.** Phase 41 converted 8 surfaces and published 13 primitives; Phase 41.1 converted 21
work pages, 7 placeholders, 5 tables and 8 dialogs. So for **every** file in this
perimeter there is a converted file in this tree that did the same thing, most of them
within the last two days, all of them under the same contract.

The one exception that matters is not a shape — it is a **decision that has exactly two
precedents** and needs both halves quoted: D-41.2-01's navigation move. That is §4, and it
is the section ten plans will each need.

**One arithmetic note before the roster.** The prompt asks for *51 perimeter files plus
five route-adjacent `loading.tsx`*. Measured, the five placeholders are **inside** the 51:
`41.2-RESEARCH.md` §1.3 says *"46 files reached by an import closure or sitting on a
ratchet list, plus 5 route-adjacent"*, and §1.4's group rows list each `loading.tsx` under
its own surface. 51 is the total. The five are broken out below anyway, because they are
reached by the ROUTER and not by any import walk, and a plan that forgets one now produces
a red (§1.5, closed by 41.1-04).

---

## 1. File Classification

Columns: **role** — server component (`svr`), client component (`cli`), route-adjacent
placeholder (`rt-adj`), non-visual module (`—`). **flow** — request-response (`req`),
event-driven (`evt`), money (`£`), venue (`◆`), static render (`stat`).

### 1a. Group A — public, no money, no venue *(3 surfaces · 6 files)*

| # | File | Lines | Role | Flow | Closest converted analog | Plan | Match |
|---|---|---|---|---|---|---|---|
| 1 | `src/app/page.tsx` | 78 | svr | req | `(public)/gallery/page.tsx` | 41-08 | **exact** (nav + shell) |
| 2 | `(public)/newsletter/page.tsx` | 27 | svr | req | `(public)/gallery/page.tsx` | 41-08 | **exact** |
| 3 | `components/newsletter/NewsletterForm.tsx` | 71 | cli | evt | `(auth)/login/page.tsx:141-185` | 41-06 | **exact** |
| 4 | `(public)/artists/[slug]/page.tsx` | 183 | svr | req | `(work)/venues/[slug]/page.tsx` | 41.1-07 | **exact** |
| 5 | `components/artists/EditArtistButton.tsx` | 205 | cli | evt | `components/venues/EditVenueButton.tsx` | 41.1-07 | **exact — §2.3** |
| 6 | `(admin)/admin/artists/actions.ts` | 241 | — | — | *(zero `className`; reached, nothing to convert)* | — | n/a |

### 1b. Group B — public and member, venue-adjacent *(4 surfaces · 12 files)*

| # | File | Lines | Role | Flow | Closest converted analog | Plan | Match |
|---|---|---|---|---|---|---|---|
| 7 | `(public)/events/page.tsx` | 578 | cli-hosting svr | req ◆ | `(work)/events/page.tsx` | 41.1-07 | **exact** |
| 8 | `(public)/events/EventTabs.tsx` | 495 | cli | evt ◆ | `admin/formats/FormatsCatalogue.tsx` | 41-09 | role-match |
| 9 | `(public)/events/FormatFilterRow.tsx` | 184 | svr | req | *(is itself UI-SPEC §8.2's source)* | — | **§6.3 — partial** |
| 10 | `(public)/events/loading.tsx` | 30 | rt-adj | stat | `(work)/events/loading.tsx` | 41.1-11 | **exact** |
| 11 | `(members)/attendance/page.tsx` | 74 | svr | req | `(work)/artists/page.tsx` | 41.1-06 | **exact** |
| 12 | `(members)/membership-card/page.tsx` | 80 | svr | req | `(work)/artists/page.tsx` | 41.1-06 | **exact** |
| 13 | `components/membership/MembershipCardView.tsx` | 57 | cli | evt | `ui/Card.tsx` + `ui/Button.tsx` | 41-03 | **exact** |
| 14 | `components/membership/CopyReferralLink.tsx` | 55 | cli | evt | `ui/Card.tsx` + `ui/Button.tsx` | 41-03 | **exact** — shared, §5.0 |
| 15 | `(members)/membership-card/loading.tsx` | 39 | rt-adj | stat | `(work)/members/loading.tsx` | 41-10 | **exact** |
| 16 | `(public)/tickets/[id]/page.tsx` | 223 | svr | req ◆ £ | `(public)/payment/callback/page.tsx` | 41-05 | role-match |
| 17 | `lib/apple-wallet.ts` | 154 | — | ◆ | *(zero `className`; §3.4's exit, read-only)* | — | n/a |
| 18 | `(public)/tickets/[id]/RefundRequestButton.tsx` | 90 | cli | evt £ | `admin/events/[id]/tickets/RefundActions.tsx` | 41.1-17 | **exact** |

### 1c. Group C — the bar and the money *(3 surfaces · 33 files)*

**C1 — `/events/[slug]/menu`, the QR bar surface** *(zero venue reads across all six of its own files)*

| # | File | Lines | Role | Flow | Closest converted analog | Plan | Match |
|---|---|---|---|---|---|---|---|
| 19 | `…/menu/page.tsx` | 242 | svr | req £ | `(work)/events/[id]/drinks/page.tsx` | 41.1-10 | **exact** |
| 20 | `…/menu/loading.tsx` | 40 | rt-adj | stat | `(work)/members/loading.tsx` | 41-10 | **exact** |
| 21 | `…/menu/PartyDrinkMenu.tsx` | 262 | cli | evt £ | `admin/…/drinks/DrinkMenuManager.tsx` | 41.1-10 | **exact** — its own import |
| 22 | `…/menu/GuestDrinkMenu.tsx` | 282 | cli | evt £ | `DrinkMenuManager.tsx` | 41.1-10 | role-match |
| 23 | `…/menu/GuestTokenDisplay.tsx` | 559 | cli | evt £ | `RefundActions.tsx` (dialog) + `Skeleton` | 41.1-17 | role-match |
| 24 | `…/menu/UserTokenDisplay.tsx` | 56 | cli | £ | `ui/Card.tsx` | 41-03 | **exact** |
| 25 | `…/menu/GuestLoginBanner.tsx` | 155 | cli | evt | `RefundActions.tsx` (dialog) | 41.1-17 | role-match — unreachable, D-41.2-03 |

**C2 — `/dashboard`**

| # | File | Lines | Role | Flow | Closest converted analog | Plan | Match |
|---|---|---|---|---|---|---|---|
| 26 | `(members)/dashboard/page.tsx` | 599 | svr | req £ | `(work)/members/page.tsx` | 41-10 | **exact** |
| 27 | `(members)/dashboard/loading.tsx` | 69 | rt-adj | stat | `(work)/members/loading.tsx` | 41-10 | **exact** |
| 28 | `(members)/dashboard/DashboardDrinkTokens.tsx` | 204 | cli | evt £ | `ui/Card.tsx` + `ui/Chip.tsx` | 41-03 | role-match |
| 29 | `components/media/MyMediaSection.tsx` | 206 | cli | evt | **`components/media/Lightbox.tsx`** | 41-08 | **exact — §6.1 has the tension** |
| 30 | `components/account/CollapsibleSection.tsx` | 49 | cli | evt | `analytics/ReferralChainTable.tsx` disclosure | 41.1-12 | partial |
| 31 | `components/account/ManagementSection.tsx` | 64 | cli | req | `ui/Card.tsx` | 41-03 | **exact** |
| 32 | `components/auth/ChangeEmailButton.tsx` | 79 | cli | evt | `EditVenueButton.tsx` status region | 41.1-07 | role-match |
| 33 | `components/auth/ResetPasswordButton.tsx` | 56 | cli | evt | as above | 41.1-07 | role-match |
| 34 | `components/auth/LogoutButton.tsx` | 24 | cli | evt | `ui/Button.tsx` `variant="secondary"` | 41-03 | **exact** |
| 35 | `components/analytics/PostHogIdentify.tsx` | 23 | cli | evt | *(zero `className`)* | — | n/a |
| 36 | `lib/routes/staff-tabs.ts` | 181 | — | — | *(zero `className`)* | — | n/a |

**C3 — `/events/[slug]`, the knot**

| # | File | Lines | Role | Flow | Closest converted analog | Plan | Match |
|---|---|---|---|---|---|---|---|
| 37 | `(public)/events/[slug]/page.tsx` | 1283 | svr | req £ ◆ | `(work)/events/[id]/edit/page.tsx` + `(work)/members/page.tsx` | 41.1-23 / 41-10 | **partial — §5.2** |
| 38 | `(public)/events/[slug]/loading.tsx` | 56 | rt-adj | stat | `(work)/members/loading.tsx` | 41-10 | **exact** |
| 39 | `[slug]/TierSelection.tsx` | 387 | cli | evt £ | `admin/…/tickets/*` + `ui/Input.tsx` | 41.1-22 | role-match |
| 40 | `[slug]/RedeemConfirmationModal.tsx` | 213 | cli | evt £ | `RefundActions.tsx:121-174` | 41.1-17 | **exact** |
| 41 | `[slug]/SumUpCheckoutModal.tsx` | 129 | cli | evt £ | `RefundActions.tsx:121-174` | 41.1-17 | **exact** |
| 42 | `[slug]/DrinkTokenCard.tsx` | 155 | cli | evt £ | `ui/Card.tsx` + `ui/Chip.tsx` + `Skeleton` | 41-03 | **exact** |
| 43 | `components/SumUpCardWidget.tsx` | 137 | cli | evt £ | *(zero raw / zero legacy — mount only)* | — | n/a |
| 44 | `[slug]/MyDrinks.tsx` | 70 | cli | £ | `ui/Card.tsx` | 41-03 | **exact** |
| 45 | `[slug]/SecretVenueDialog.tsx` | 176 | cli | evt ◆ | `admin/…/reveal/RevealVenueDialog.tsx` | 41.1-20 | **exact — §5.3** |
| 46 | `[slug]/MediaGallerySection.tsx` | 83 | cli | evt | `(public)/gallery/GalleryClient.tsx` | 41-08 | **exact** |
| 47 | `components/media/MediaUpload.tsx` | 715 | cli | evt | `components/media/MediaReviewGrid.tsx` | 41.1-09 | role-match |
| 48 | `[slug]/RsvpButton.tsx` | 80 | cli | evt | `ui/Button.tsx` | 41-03 | **exact** |
| 49 | `[slug]/PendingIntentHandler.tsx` | 102 | cli | evt £ | `ui/Button.tsx` + `Card` | 41-03 | role-match |
| 50 | `[slug]/ShareButton.tsx` | 55 | cli | evt | `ui/Button.tsx` | 41-03 | **exact** |
| 51 | `[slug]/rsvp-actions.ts` | 145 | — | — | *(`"use server"`, zero `className` — stop condition 2)* | — | n/a |
| — | `emails/rsvp-confirmation.tsx` | 94 | — | — | *(an e-mail template; inline styles, no Tailwind)* | — | n/a |

**Roster arithmetic.** 6 + 12 + 34 = 52 names above; `CopyReferralLink.tsx` (#14) is reached
by **both** `/membership-card` and `/dashboard` and is counted once in RESEARCH's 51. The
difference is a shared file, not a missing one.

---

## 2. Pattern Assignments — the five conversions every plan repeats

### 2.1 The `PageShell` adoption — eight of the ten page files, identically

**Analog:** `src/app/(admin)/admin/(work)/members/page.tsx:159` (`wide`) and
`(work)/formats/page.tsx:147` (`default`). For the *public* half of the tree the specimen is
`src/app/(public)/gallery/page.tsx:111`.

**The perimeter is unusually uniform — measured, all ten page files:**

```
src/app/(public)/events/page.tsx:527           <div className="min-h-dvh pb-24">
src/app/(public)/events/page.tsx:529             <header className="px-6 pt-12 pb-6">
src/app/(public)/events/page.tsx:530               <h1 className="text-3xl font-bold tracking-tight">Events</h1>
src/app/(members)/membership-card/page.tsx:36-38   — identical three lines
src/app/(members)/dashboard/page.tsx:287,294,298   — identical three lines
src/app/(public)/artists/[slug]/page.tsx:52        — the root; header differs (hero image)
src/app/(public)/events/[slug]/page.tsx:809,844    — the root and the h1
src/app/(members)/attendance/page.tsx:26-28        — identical three lines
```

That is **byte-for-byte the four-part deletion `41.1-PATTERNS.md` §2.1 extracted from the
`members/page.tsx` diff** (`git show f51eddd`). Nothing about the substitution is new:

1. the outer `min-h-dvh pb-24` root → the shell's outer element;
2. `px-6 pt-12` on the header and every repeated `px-6` on inner sections;
3. the header's `pb-6` → `mb-6`, a margin inside the shell;
4. `<h1 className="text-3xl font-bold tracking-tight">` → `<PageTitle>`,
   `src/components/ui/Typography.tsx:53`.

**`width` is always written.** All 37 current importers pass it, including those that could
have omitted `default` (`grep -rln 'components/ui/PageShell' src` → 37 files). D-41.2-02
makes all ten `default`.

**Three page files diverge and each needs its own sentence:**

| File | Root today | Why it is not the four-part deletion |
|---|---|---|
| `src/app/page.tsx:36-37` | `flex min-h-dvh flex-col` + `flex flex-1 … items-center justify-center` | a centred hero. `PageShell.tsx:125` names `FOCUS_ROOT = "flex min-h-dvh items-center justify-center p-6"` — **the shape this page hand-writes** — and check E forbids `focus` on a surface mounting a navigation (D-41.2-01 mounts one). It takes `default` and centres inside it |
| `(public)/newsletter/page.tsx:15` | `flex min-h-dvh flex-col items-center justify-center px-6 pb-24` | same shape, same answer |
| `…/menu/page.tsx:147,163` | `min-h-dvh bg-background` + `mx-auto max-w-lg px-4 …` inside a template literal | the only page file whose maximum is composed at run time. §3 row 5 |

### 2.2 The `loading.tsx` pattern — all five, one analog

**Analog:** `src/app/(admin)/admin/(work)/members/loading.tsx` (plan 41-10) and
`src/app/(public)/gallery/loading.tsx` (plan 41-08). The gallery one is the closer specimen
for three of the five, because it stands in front of a **grid** rather than a list.

```tsx
// (work)/members/loading.tsx:1-2, 36-42, 70-75
import { PageShell } from "@/components/ui/PageShell";
import { SkeletonCard, SkeletonLine } from "@/components/ui/Skeleton";

export default function MembersLoading() {
  return (
    <PageShell width="wide">                    // ← the SAME width as the page it precedes
      <header className="mb-6">
        <SkeletonLine className="h-9 w-40" />
      </header>
      …
      <div className="space-y-3">
        {Array.from({ length: 8 }).map((_, i) => <SkeletonCard key={i} />)}
      </div>
    </PageShell>
  );
}
```

`src/components/ui/Skeleton.tsx` exports four names: `SkeletonLine` (`:93`),
`SkeletonCard` (`:123`), `SkeletonTile` (`:160`), `SkeletonAvatar` (`:191`).

**The five, with the hand-rolled block each deletes and the count it must not claim:**

| File | `animate-pulse` | Width (D-41.2-02) | Analog |
|---|---|---|---|
| `(members)/dashboard/loading.tsx` | **11** | `default` | `(work)/members/loading.tsx` |
| `(public)/events/[slug]/loading.tsx` | **9** | `default` | same |
| `(public)/events/[slug]/menu/loading.tsx` | **6** | `default` | same |
| `(members)/membership-card/loading.tsx` | **5** | `default` | same |
| `(public)/events/loading.tsx` | **4** | `default` | `(public)/gallery/loading.tsx` (grid) |

**35 of the perimeter's 39 hand-rolled pulses are in these five files.** The other four are
in `GuestTokenDisplay.tsx` (2), `RedeemConfirmationModal.tsx` (1) and `DrinkTokenCard.tsx`
(1) — inline placeholders inside components, and they take `SkeletonLine` at their call
sites rather than a whole `loading.tsx` shape.

**Three constraints the analog carries and a plan must not lose** (`Skeleton.tsx:20-53`,
`(work)/members/loading.tsx:29-34`):

1. **`width` must equal the page's width**, or the content jumps sideways when data lands.
2. **Every count is a literal and stands for nothing.** No query runs before a `loading.tsx`
   renders. This binds hardest on `/dashboard`, which stands in front of a member's own
   token list.
3. **Do not append a radius to `SkeletonLine`** — DEF-41-05, still open, measured in the
   emitted sheet on 2026-08-12.

### 2.3 The form dialog — `EditArtistButton.tsx`, and this is the cheapest exact match in the phase

**Analog:** `src/components/venues/EditVenueButton.tsx`, converted by plan **41.1-07**.

The two files are the same file with different nouns. Measured side by side:

```
EditArtistButton.tsx:87   className="fixed inset-0 z-[60] flex items-center justify-center bg-black/60 backdrop-blur-sm px-4"
EditArtistButton.tsx:91     className="w-full max-w-md max-h-[85vh] overflow-y-auto rounded-2xl border border-card-border bg-card p-6"
EditArtistButton.tsx:94       <h3 className="text-lg font-semibold mb-4">Edit {artist.name}</h3>
EditArtistButton.tsx:97-98    border border-red-500/30 bg-red-500/10  …  text-sm text-red-400
EditArtistButton.tsx:102-103  border border-green-500/30 bg-green-500/10  …  text-sm text-green-400
```

`41.1-PATTERNS.md` §2.3a quotes `EditVenueButton.tsx:155-162` carrying **the same five
lines** before its conversion. What replaced them, verbatim from the converted file:

```tsx
// src/components/venues/EditVenueButton.tsx:214-256
<Dialog
  open={open}
  onClose={close}
  title={`Edit ${venue.name}`}
  size="lg"                                   // §8.3's closed lg list — EditArtistButton is ON it
  status={
    error   ? { tone: "crit", message: error }
    : success ? { tone: "done", message: "Saved!" }
    : null
  }
  actions={
    <div className="flex gap-3">
      <Button type="submit" form={FORM_ID} className="flex-1" disabled={isPending}>
        {isPending ? "Saving..." : "Save"}
      </Button>
      <Button variant="secondary" data-initial-focus onClick={close} disabled={isPending}>
        Cancel
      </Button>
    </div>
  }
>
  <form id={FORM_ID} onSubmit={handleSubmit} className="space-y-4">
```

`const FORM_ID = "edit-venue-form";` at `EditVenueButton.tsx:59`. The buttons live **outside**
the `<form>` and reach it through HTML's form-owner attribute, because the primitive puts
`actions` below the scroller.

**`EditArtistButton.tsx` is named on §8.3's closed `lg` list** — read it in the primitive
itself, `src/components/ui/Dialog.tsx:161-166`, which lists the six form dialogs by name.
So `size="lg"` here is checkable against the contract, not argued.

**What is different:** `EditArtistButton.tsx` carries **9 raw interactive elements** — the
heaviest single file in the touch-target census — five `<input>`, one `<textarea>` and
three `<button>`. Every one becomes `<Input>` / `<Textarea>` / `<Button>` (§3).

### 2.4 The money dialog — the four shells on the purchase path

**Analog:** `src/app/(admin)/admin/events/[id]/tickets/RefundActions.tsx:121-174`, converted
by plan **41.1-17** — and 41.1-17 is also the *scheduling* precedent, because it converted a
shared file once as spine so two plans in one wave would not both open it.

```tsx
// RefundActions.tsx:121-174 — a money confirmation, whole
{showConfirm && (
  <Dialog
    open
    onClose={closeConfirm}
    title="Confirm Refund"
    status={error ? { tone: "crit", message: error } : null}
    actions={
      <div className="flex gap-3">
        <Button variant="secondary" className="flex-1"
                data-initial-focus                     // ← Cancel, first in the DOM
                onClick={closeConfirm} disabled={isPending}>Cancel</Button>
        <Button variant="destructive" className="flex-1" disabled={isPending}
                onClick={() => { … }}>
          {isPending ? "Processing..." : "Refund"}
        </Button>
      </div>
    }
  >
    <Textarea id={refundReasonId} aria-label="Reason (optional)" … />
  </Dialog>
)}
```

Three properties to copy, each with its reason already written in the analog's own docblock:

- **`closeConfirm()` resets the refusal** (`:100-107`): *"Every route out of the confirmation
  runs through here — the close control, Escape, and Cancel — so none of them can leave a
  stale refusal behind."*
- **The dialog is mounted only while open** (`:51-53`), which keeps the DOM cost of a long
  list of rows at one confirmation, not one per row. This binds `DrinkTokenCard` →
  `RedeemConfirmationModal` directly.
- **The refusal is the primitive's `status`, never a toast.** A native `<dialog>` paints in
  the top layer, above the toast container's `z-[70]` (`Dialog.tsx:66-77`). `verify-dialogs`
  check C asserts it.

**What the four delete** — the shell, identically in all four:

```
[slug]/RedeemConfirmationModal.tsx:167  fixed inset-0 z-[60] flex items-end sm:items-center justify-center bg-black/60 backdrop-blur-sm
[slug]/RedeemConfirmationModal.tsx:168  w-full max-w-md rounded-t-2xl sm:rounded-2xl bg-card p-6 pb-[calc(1.5rem+5rem+env(safe-area-inset-bottom))] sm:pb-6
[slug]/SumUpCheckoutModal.tsx:50-51     the same two lines, plus max-h-[85dvh] overflow-y-auto and sm:max-h-[90vh]
menu/GuestTokenDisplay.tsx:222-223      the same two lines
menu/GuestLoginBanner.tsx:71,76         the same two lines
```

**These four shells are where 13 of the tree's 16 remaining `sm:` uses live**, and the
primitive **deletes** them rather than migrating them — §3 row 6 and pitfall P6.

### 2.5 The control ladder — every file

Unchanged from `41.1-PATTERNS.md` §2.5, which is still the table to copy. Re-measured today,
the primitives are 13 (`ls src/components/ui/` → `AutocompleteInput`, `Button`, `Card`,
`Checkbox`, `Chip`, `DataTable`, `Dialog`, `Icons`, `Input`, `PageShell`, `Skeleton`,
**`Switch`**, `Typography`). **`Switch.tsx` now exists** — published by plan 41.1-10 with
`DrinkMenuManager` as its first consumer — which closes `41.1-PATTERNS.md` §4.4 and §5.2.
`Switch({…})` is at `src/components/ui/Switch.tsx:165`.

The four call sites this perimeter has for it: none. **No file in this perimeter carries a
hand-written switch track** — verified, all four were inside 41.1's scope.

---

## 3. The recurring conversions, as a table

The perimeter's declared load: **183 raw palette utilities · 239 legacy token utilities ·
39 hand-rolled `animate-pulse` blocks · ~99 raw interactive elements · 16 `sm:` uses ·
8 hand-rolled dialog shells.**

| # | Family | The old shape, quoted from this perimeter | The new shape | Primitive that owns it | The one call site to copy |
|---|---|---|---|---|---|
| 1 | **Page root** | `<div className="min-h-dvh pb-24">` + `<header className="px-6 pt-12 pb-6">` — `(public)/events/page.tsx:527,529` | `<PageShell width="default">` + `<header className="mb-6">` | `ui/PageShell.tsx:146` | `(work)/members/page.tsx:159`; public specimen `(public)/gallery/page.tsx:111` |
| 2 | **Page title** | `<h1 className="text-3xl font-bold tracking-tight">` — 6 of the 10 page files carry it verbatim | `<PageTitle>` | `ui/Typography.tsx:53` | `(public)/gallery/page.tsx:114` |
| 3 | **Placeholder** | `animate-pulse rounded-… bg-card-border/50` — 39 blocks, 35 of them in the five `loading.tsx` | `<SkeletonLine>` / `<SkeletonCard>` / `<SkeletonTile>` | `ui/Skeleton.tsx:93,123,160` | `(work)/members/loading.tsx:41,72`; grid form `(public)/gallery/loading.tsx:35-38` |
| 4 | **Dialog shell** | `fixed inset-0 z-[60] flex items-end sm:items-center justify-center bg-black/60 backdrop-blur-sm` — 8 files | `<Dialog open onClose title size status actions>` | `ui/Dialog.tsx` | money: `RefundActions.tsx:121-174` · form: `EditVenueButton.tsx:214-256` · destructive: `RetireFormatDialog.tsx:266-306` |
| 5 | **Own container maximum** | `max-w-xs` `src/app/page.tsx:46` · `max-w-lg` `artists/[slug]/page.tsx:126` · `max-w-sm` ×2 `tickets/[id]/page.tsx:108,191` · `max-w-lg` `menu/page.tsx:163` | delete it, or declare a `TYPOGRAPHIC_MEASURES` entry | `verify-conversion.mjs:1053` (`[]` today), check D `:3157-3184` | no adopter exists — **§6.2** |
| 6 | **`sm:` prefix** | `items-end sm:items-center`, `rounded-t-2xl sm:rounded-2xl`, `sm:pb-6`, `sm:max-h-[90vh]` — **13 uses, all inside a dialog shell** | **deleted**, the primitive owns the sheet-vs-window pair | `ui/Dialog.tsx` | as row 4 |
| 6b | **`sm:` type size** | `text-4xl sm:text-5xl` — `RedeemConfirmationModal.tsx:136`, `GuestTokenDisplay.tsx:193` | **2 uses NOT absorbed** — migrate or delete, §7.4 | `ui/Typography.tsx` | — |
| 6c | **`sm:` grid axis** | `grid grid-cols-2 sm:grid-cols-3 gap-2` — `components/media/MediaUpload.tsx:624` | gains the middle step | — | `components/media/MediaReviewGrid.tsx:154` — `grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3` |
| 7 | **Interactive element** | `<button className="rounded-full bg-accent px-5 py-2 text-sm font-medium text-white …">` — `ChangeEmailButton.tsx:66` | `<Button>` at its size rung | `ui/Button.tsx:215` | `RefundActions.tsx:117` (`size="sm" variant="secondary"`) |
| 8 | **Focus suppression** | `focus:outline-none` + an accent ring — `NewsletterForm.tsx:56` and 30+ others | `${FOCUS_RING}`, imported never re-spelled | `ui/Button.tsx:71` | `MemberTable.tsx:1109-1118` |
| 9 | **Hand-rolled status box** | `border border-red-500/30 bg-red-500/10 … text-red-400` / the green twin — `EditArtistButton.tsx:97-103`, `ChangeEmailButton.tsx:32,60` | the dialog's `status` prop, or `role="alert" className="text-sm text-sem-crit"` outside one | `ui/Dialog.tsx:316-325` | dialog: `EditVenueButton.tsx:228-234` · page: `(auth)/login/page.tsx:170-174` |
| 10 | **Card shell** | `rounded-2xl border border-card-border bg-card p-5` — `CopyReferralLink.tsx:36`, `ManagementSection`, `MyDrinks`, `attendance/page.tsx:36` | `<Card>` | `ui/Card.tsx:47` | `(public)/events/[slug]/menu/EventQRCode.tsx:29` (already in this directory, converted 41.1-10) |
| 11 | **Interactive pill** | `rounded-full … py-2 text-sm` on a `<Link>` or `<button>` — `src/app/page.tsx:49,55`, `SecretVenueDialog.tsx:151,157` | `<Chip>` — 44px, `aria-current` when selected. **A `Badge` that is a `<Link>` or a `<button>` is a `Chip`** | `ui/Chip.tsx:161` (`min-h-11` at `:58`) | `(work)/analytics/compare` mode pills, plan 41.1-14 |
| 12 | **Full-bleed viewer** | `fixed inset-0 z-[60] … bg-black/80` + `role="dialog"` — `MyMediaSection.tsx:182-186` | adopt the existing viewer | `components/media/Lightbox.tsx` | `[slug]/MediaGallerySection.tsx:7` — a **perimeter sibling already does this** |

**Row 3 is the one D-41-04 is watching.** `Skeleton.tsx` was correct and had **zero
importers** for the whole of its existence while 102 hand-rolled pulses lived in 20 files.
It has 13 importers today (`grep -rln 'components/ui/Skeleton' src`), of which 9 are
`loading.tsx`. The five files in row 3 take it from 9 to 14 — and if a plan converts a page
and leaves its placeholder, check B now reddens (§1.5, closed by 41.1-04). That is the
improvement 41.1 bought, and it is worth exactly one line in each plan's task list.

---

## 4. The navigation conversion — the highest-value section, and it has two precedents

D-41.2-01 moves all ten surfaces from `MobileNav` to `AppNav`'s responsive form, and each
must **also** declare the tablet-tier column clearance, because check E asserts the two sets
are equal **in both directions**.

### 4.1 What check E actually reads — quoted from the gate

```js
// scripts/verify-conversion.mjs:2785-2786
const RESPONSIVE_NAV_MODULE  = 'src/components/layout/AppNav.tsx';
const PHONE_LOCKED_NAV_WRAPPER = 'src/components/layout/MobileNav.tsx';

// :2811-2820
const filesMountingResponsiveForm = allSrcFiles
  .filter((rel) => rel !== PHONE_LOCKED_NAV_WRAPPER && importsDirectly(rel, RESPONSIVE_NAV_MODULE))
  .sort();

const declaringWithoutMounting = filesDeclaringColumnClearance.filter(
  (rel) => !filesMountingResponsiveForm.includes(rel));
const mountingWithoutDeclaring = filesMountingResponsiveForm.filter(
  (rel) => !filesDeclaringColumnClearance.includes(rel));
```

Two things follow, and both bite:

- **Mounting means importing `AppNav` DIRECTLY** (`importsDirectly`, `:2803-2809`). Reaching
  it through `MobileNav` does not count. So the conversion is a **change of import
  specifier and component name in the page file**, nothing subtler.
- **Declaring means a bracketed arbitrary-property utility naming the inline-axis clearance,
  at the `md` tier and no other** (`:2724`, `:2760-2774`). A declaration at another variant
  is reported separately, not silently excluded — `:2771`, `declarationsOffTier`.

The gate's own failure directions, `:2657-2667`: *declares, does not mount* → a column
reserved on a route that has none, **silent**; *mounts, does not declare* → content sliding
**under** the column, **loud, at 768px and above**.

### 4.2 The two files that already do exactly this — both halves, quoted

Measured today, `LC_ALL=C /usr/bin/grep -rn "nav-inset-inline-start" src` returns **two**
declaring sites and they are the same two files that import `AppNav` directly:

```
src/app/(admin)/admin/(work)/layout.tsx:176   ← the work surface, one layout for 23 pages
src/app/(public)/gallery/page.tsx:110         ← a PUBLIC page, page-level, plan 41-08
```

**`/gallery` is the analog for all ten**, because the ten have no shared layout: they live
in `(public)`, `(members)` and at the app root, so each declares and mounts **in its own
page file**, exactly as `/gallery` does. The work layout is the wrong analog here — it does
it once for a whole route group.

**Half one — the declaration** (`src/app/(public)/gallery/page.tsx:110`, and the identical
line at `(work)/layout.tsx:176`):

> One `div` wrapping the shell, carrying **one arbitrary-property utility at the `md` tier,
> setting the inline-start navigation-inset custom property to fourteen rems.** The token is
> written broken here — `md:[--nav-inset-inline-…` — for DEF-41-01's reason. **Copy it
> verbatim from `src/app/(public)/gallery/page.tsx:110`**, which is the line the gate itself
> matched on its last green run.

```tsx
// (public)/gallery/page.tsx:110-123 — the shape, with the token elided
<div className="md:[--nav-inset-inline-…]">      {/* ← verbatim at :110 */}
  <PageShell width="wide">
    …
  </PageShell>
</div>
```

**Half two — the mount** (`src/app/(public)/gallery/page.tsx:125-132`, verbatim):

```tsx
<AppNav
  role={role as UserRole | null}
  status={status as UserStatus | null}
  capabilities={[...capabilities]}
  liveAssignmentCapabilities={
    liveAssignmentCapabilities ? [...liveAssignmentCapabilities] : null
  }
/>
```

with, at `:2`, `import AppNav from "@/components/layout/AppNav";`.

**And the diff is smaller than it looks.** All ten perimeter page files already pass those
exact four props to `MobileNav` — measured, e.g. `(members)/attendance/page.tsx:64-71`,
which is character-identical to the block above except for the component name. Plan 39-03
threaded the capability set through all thirteen mount sites, so the prop shape is already
the one `AppNav` takes.

### 4.3 The ten mount sites, by `file:line`

`grep -rln 'layout/MobileNav' src` → 11 files; the eleventh is
`(admin)/admin/scanner/DoorSurface.tsx`, which is **Phase 42's and stays on the phone form
by decision (D-41-21)**.

| # | Surface | Mount site today |
|---|---|---|
| 1 | `/` | `src/app/page.tsx:4` (import), `:68-75` (mount) |
| 2 | `/newsletter` | `(public)/newsletter/page.tsx:1`, `:17-24` |
| 3 | `/artists/[slug]` | `(public)/artists/[slug]/page.tsx:4`, `:173` |
| 4 | `/events` | `(public)/events/page.tsx:1`, `:568` |
| 5 | `/attendance` | `(members)/attendance/page.tsx:4`, `:64-71` |
| 6 | `/membership-card` | `(members)/membership-card/page.tsx:6`, `:70` |
| 7 | `/tickets/[id]` | `(public)/tickets/[id]/page.tsx:4`, `:213` |
| 8 | `/events/[slug]/menu` | `(public)/events/[slug]/menu/page.tsx:7`, `:231` |
| 9 | `/dashboard` | `(members)/dashboard/page.tsx:6`, `:589` |
| 10 | `/events/[slug]` | `(public)/events/[slug]/page.tsx:4`, `:1273` |

**Two placement rules the analog settles, and a plan that gets either wrong ships a red:**

1. **The declaring wrapper wraps the SHELL, not the nav.** In `/gallery` the `AppNav` sits
   **outside** the declaring `div`, as a sibling (`:110-132`). Putting the nav inside would
   still pass the textual pairing but would pad the column by its own clearance.
2. **The nav is a sibling of the shell, not a child of the old page root.** Eight of the ten
   currently render `MobileNav` **inside** `<div className="min-h-dvh pb-24">`. That root is
   deleted by §2.1, so the mount moves up a level into a fragment — the shape at
   `(public)/gallery/page.tsx:88-133`.

**Why `/gallery` and not the work layout, in one line:** the work layout declares once for a
directory; these ten declare ten times, one per page file, and check E counts **files**.

### 4.4 What D-41.2-01 does not change

`41-UI-SPEC.md` §0 rule 5, restated because a width change is where it gets broken: **width
may change layout, never membership.** The server decides which navigation entries exist;
CSS decides how they sit. `AppNav` receives the same four props `MobileNav` received, so no
entry appears or disappears — and that sentence belongs in each of the ten SUMMARYs, in the
`no capability check touched` position.

---

## 5. The three knots

`41.1-CONTEXT.md`'s knot treatment is: **its own plan, a written impact analysis before the
diff, and the stop conditions restated at the head.** The four plans that are the model are
41.1-17 (the refund controls), 41.1-18 / 41.1-19 (the event form and its satellites) and
41.1-20 (the venue reveal). What to copy from them is the *procedure*, not only the result.

### 5.0 First, the shared-file discharge — 41.1-17 is the precedent for the scheduling

Five files sit in more than one of the ten closures (§1.7). `RefundActions.tsx` had exactly
this topology and 41.1-17 solved it by **converting it once, as spine, in an earlier wave**,
and saying so in the file:

> `RefundActions.tsx:14-22` — *"D-41.1-15 says a shared component two plans would both touch
> is either converted in an earlier wave as spine or assigned to exactly one plan. It is
> converted here, ahead of both, so that the sales plan and the tickets plan stay two plans
> instead of being merged into one. Recorded so the next reader knows the merger was avoided
> deliberately rather than missed."*

Copy that paragraph's *shape* into the money-core plan and into whichever plan takes
`CopyReferralLink.tsx` (shared by `/membership-card` and `/dashboard`).

### 5.1 Knot 1 — the shared money core

**Files:** `[slug]/DrinkTokenCard.tsx` (3 surfaces) · `[slug]/RedeemConfirmationModal.tsx`
(3) · `[slug]/SumUpCheckoutModal.tsx` (2) · `components/SumUpCardWidget.tsx` (2).

**Analog:** `RefundActions.tsx` — for the dialog (§2.4), for the scheduling (§5.0), **and
for the impact analysis**. Its docblock is the template for what a money conversion writes
down before it diffs:

```
RefundActions.tsx:24-36  ── The money path is untouched, and that is the point of this plan ──
  "Three server actions are called from here and all three are called with the same
   arguments, in the same order, as before … No status transition, no refund amount, no
   idempotency key and no webhook path is written, read or reshaped by this file — it
   renders controls and reports what the action returned. The action module was read in
   order to be able to tell a rendering change from a payload change, and it was not edited."
```

**And the two findings it carried forward rather than fixed** (`:67-80`) are the template
for `41.2-RESEARCH.md` §3.5's four silent catches: named at `file:line`, with the reason the
fix is a different plan — *"Naming each cause is a rewrite of a money surface's copy and
belongs to a plan that owns that decision."*

**What is different here:** `SumUpCardWidget.tsx` carries **zero raw palette and zero legacy
tokens** — it loads the provider SDK. It is in the closure, has nothing to convert, and
saying so is the whole of its task. And `RedeemConfirmationModal.tsx` calls
`redeemDrinkToken` from `@/app/(admin)/admin/events/actions` — a **converted** work module
that this plan does not open. Stop condition 2 lives exactly there.

### 5.2 Knot 2 — `/events/[slug]`

**Analog for the split:** 41.1-18 (the 1669-line form) and 41.1-19 (its five satellites) on
**disjoint file sets**, declared by 41.1-23 which touched only the route file. The manifest
entry for `/admin/events/[id]/edit` (`conversion-manifest.mjs:670-673`) is the shape of the
declaration this surface will need:

> *"plan 41.1-23 — the route file only; the form (41.1-18), its five satellites (41.1-19)
> and the reveal panel (41.1-20) were converted before it. … THE CAUTION, because this is the
> one work page that reaches the venue reveal: the conditional deciding whether the panel is
> drawn at all, and all five props it is given, are byte-identical before and after …
> **Not drawing the panel was never the guard and this conversion did not make it one.**"*

**The four assertions, at their measured lines** — and three of the four line numbers in
`41.2-CONTEXT.md` and `41.2-RESEARCH.md` have drifted. **Re-measured 2026-08-14:**

| What must hold | CONTEXT/RESEARCH says | **Measured today** | Command |
|---|---|---|---|
| the reveal test stays positive | `:206-207` | **`:201-202`** | `grep -n "revealedByHand"` → `201`, `212` |
| `force-dynamic` stays present | `:243` | **`:249`** | `grep -n "force-dynamic"` → `249` |
| the venue ternary stays three branches | `:1075-1119` | **outer guard `:1075`, ternary `:1077-1119`** | Read |
| the night select's 17 columns | `:421` | **`:421` — correct** | `grep -n "venue_secret_hint"` → `421` |

The three branches, quoted so a plan can count them after its diff:

```tsx
// (public)/events/[slug]/page.tsx:1075-1119
{(venueRow || party.venue_text || party.venue_secret) && (
  <div className="mt-1">
    {venueVisible && venueRow ? (            // branch 1 — name, address, Maps link
      …
    ) : venueVisible && party.venue_text ? ( // branch 2 — free text on the night
      …
    ) : party.venue_secret ? (               // branch 3 — the HINT, via SecretVenueDialog
      <SecretVenueDialog … />
    ) : null}
  </div>
)}
```

and the positive test, with the file's own reason on the line above it:

```tsx
// (public)/events/[slug]/page.tsx:196-202
// Written as a positive test rather than `!== null` on purpose: `undefined !== null` is
// TRUE, so a column that stopped being selected would otherwise open the address on every
// secret night, silently.
const revealedByHand =
  typeof opts.revealedAt === "string" && !Number.isNaN(Date.parse(opts.revealedAt));
```

**The technique for the select:** 41.1-22 proved a **multiset over comment-stripped source**
rather than reading a diff — the manifest records it at `conversion-manifest.mjs:664`
(*"every price, quantity, discount value and usage ceiling keeps its type, its required flag,
its minimum and its step, proved as a multiset over comment-stripped source rather than by
reading a diff"*). The 17-column select at `:421` takes the same instrument.

**What has no analog:** the *size*. Nothing converted in this tree is 1283 lines with money
and an address in one file. `(work)/members/page.tsx` is the closest whole-page analog for
the shell work and knows nothing about a venue; `(work)/events/[id]/edit/page.tsx` is the
closest venue-adjacent analog and is 1/6th the size. **The plan's verification cannot rest
on resemblance** — it rests on the four assertions above, by count and by grep, and on H41-1
at three widths.

### 5.3 Knot 3 — `SecretVenueDialog.tsx`

**Analog:** `src/app/(admin)/admin/events/[id]/reveal/RevealVenueDialog.tsx`, converted by
plan **41.1-20**. It is the only venue dialog in the tree that has made this exact move, and
its docblock is written as an impact analysis. Three paragraphs to lift the shape of:

```
RevealVenueDialog.tsx:36-50   ── The shell is no longer in this file ──
  "What the primitive brought with it, and what this file therefore stopped carrying: the
   panel geometry, the scrim, the light-dismiss handler, the open/close effect, and the
   initial focus. What stays here is what this dialog IS — the question, the two answers,
   the eleven refusals, and the three acts."

RevealVenueDialog.tsx:85-89   ── The sentence the monotone guard needs ──
  "the reveal is at least as hard to trigger after this conversion as before, and harder in
   the one respect a person can check … Nothing here shortens the path between opening this
   dialog and sending."

RevealVenueDialog.tsx:138-142 ── The refusal moved OUT of the scroller ──
  "It keeps its alert role and it moves outside the scrolling body (Dialog.tsx:316-325), so
   a refusal can no longer appear below the fold of a long consequence list."
```

**What `SecretVenueDialog.tsx` deletes** — its whole shell, `:69-80` and `:164-170`:

```tsx
<div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/60 backdrop-blur-sm px-6"
     onClick={() => setOpen(false)}>                       // ← light dismiss, hand-rolled
  <div className="w-full max-w-sm rounded-2xl border border-card-border bg-card p-6"
       onClick={(e) => e.stopPropagation()}>
    <div className="flex items-center gap-2 mb-4">
      <LockClosedIcon />
      <h3 className="text-lg font-semibold">Secret Venue</h3>   // ← becomes `title`
…
<button … className={`${!isAuthenticated ? "mt-3" : "mt-5"} w-full rounded-full …`}>
  Close                                                     // ← becomes the primitive's
</button>                                                   //   close control + actions
```

**Four things this dialog carries that the money dialogs do not, and each is a stop
condition for its own task:**

1. **A three-branch unlock chain at `:91-143`** — `!isAuthenticated` → `!isApproved` →
   the approved case. Same shape, same hazard as §5.2's venue ternary: **three after the
   diff, counted**.
2. **A two-branch bullet at `:109-120`, deliberately asymmetric** (D-37-10): a ticket unlocks
   immediately only when `venue_reveal_on_purchase` is set; an RSVP unlocks regardless. The
   file says so at `:98-108`. **Not a tidy pair, and not to be tidied.**
3. **No promise of an e-mail** (D-37-05, stated at `:35-38` and again at `:134-137`). The
   copy at `:138-141` stays byte-identical. §11 of the UI-SPEC: *"none introduced."*
4. **The hint is never the address** (`:29-34`): *"The prose around the hint must not narrow
   it either — a hint made precise by its caption is a reveal with extra steps."* A
   conversion adds no caption.

**One thing the conversion adds, and it is a strengthening worth writing down:** the trigger
at `:60-66` is a raw `<button>` with no declared minimum, and the two Links at `:149-160` are
28px pills. Under §3 rows 7 and 11 they become `Button` / `Chip` at the 44px floor. Nothing
about *who* may open this dialog changes; only how easily a finger hits it.

**The `data-initial-focus` placement.** `Dialog.tsx:138-143`: the primitive focuses the
element carrying the marker if the caller declared one, otherwise the close control, *"which
is first in the DOM and is the least destructive control by construction."* This dialog has
no destructive control — its actions are `Sign up` / `Sign in` / `Close`. **The marker
therefore goes on Close**, or is omitted and the default does the same thing. Say which,
because 41.1-20's own correction (§7.1) is what happens when two mechanisms sit there for
one intent.

---

## 6. Gaps — three files with no good analog

`41.1-PATTERNS.md` declared 3 of 18 and said so. Three again, and none of them is a shape
this phase can copy its way out of.

### 6.1 `components/media/MyMediaSection.tsx` — the gate says convert, the tree says adopt

**It is not a dialog.** Read at `:179-202`, its overlay is a **lightbox**: a full-bleed image
at `bg-black/80`, a close cross, `stopPropagation` on the image. That is precisely what
`src/components/media/Lightbox.tsx` is, and `Lightbox.tsx` is the **declared permanent
exemption** from `verify-dialogs` check B — *"a full-bleed media viewer at every tier,
carrying a heavier scrim than a sheet … deliberately NOT on REMAINING: a file that will never
convert is not a debt"* (`verify-dialogs.mjs:474-478`).

And the gate names this file, separately, as **NOT exempt**:

```js
// scripts/verify-dialogs.mjs:468-472, :479-482
/** Exception 2 — NOT exempt. It is on `REMAINING`, and it is the reason this
 *  gate's signature is not keyed on `role="dialog"`. */
export const ROLE_DIALOG_OVERLAY = 'src/components/media/MyMediaSection.tsx';
  'NOT exempt, and on REMAINING — it is the tree\'s ONLY role="dialog" (one hit, measured)
   and is a hand-rolled overlay rather than a <dialog>. A signature keyed on that attribute
   would find this one file and miss every other copy in the tree, which is why the check is
   keyed on the shell instead'
```

**So there are two roads and the plan must choose one in writing.** Converting it to
`ui/Dialog` would put a sheet-and-window panel around a full-bleed photo — the exact
correct-file-goes-red shape §0 rule 3 forbids. **Adopting `Lightbox.tsx` instead is the
road the tree already walks**: `(public)/gallery/GalleryClient.tsx:5` and — inside this very
perimeter — `(public)/events/[slug]/MediaGallerySection.tsx:7` both import it. Delegating
removes the shell, which is what takes the file off `REMAINING`; the exemption is not widened
and no gate is edited.

**Why it is still a gap:** no converted file in this tree has *replaced its own overlay with
`Lightbox`*, so there is no diff to copy — and `Lightbox`'s props (`LightboxItem`, at
`Lightbox.tsx:39`) may not match what this section holds. **A read of both before the plan is
written, not during.**

### 6.2 The four own-maximum page files — `TYPOGRAPHIC_MEASURES` has no adopter

`verify-conversion.mjs:1053` — `export const TYPOGRAPHIC_MEASURES = [];`. Zero entries, zero
applications, and the run prints *"typographic measures declared: 0, applied 0 time(s)"*. The
mechanism was built in 41.1 **before** its first red run (D-41-16, D-41.1-27) precisely so a
plan wanting a reading measure adds a line rather than deleting a width — and **nobody has
added the first line.** Each entry requires the page file, the exact token **and** a fragment
present on the same line (`:3170-3172`); a near-miss on any of the three refuses with
*"matches NOTHING: stale"*.

The nearest thing to a precedent is a **casualty, not an adoption**:
`(work)/formats/page.tsx:197-207` records DEF-41-04 — a typographic measure on a `<p>` read
to the gate as a container maximum and **had to go**.

**And the four are not the four RESEARCH names.** Measured today — §7.2.

### 6.3 `(public)/events/FormatFilterRow.tsx` — the file the contract was copied *from*

UI-SPEC §8.2 took the phone navigation strip's shape from this file, and §2.3's touch-target
census records it as **the tree's only prior art at 44px** before Phase 41: `min-h-11` occurs
exactly twice tree-wide and both are here (`ui/Chip.tsx:31` says so in its own docblock).

So the ordinary direction is inverted: the converted primitive is downstream of this file,
not upstream of it. Two specific unknowns follow, and neither has an analog:

- `:157` composes a border at run time with `color-mix` from a catalogue row's colour.
  `verify-sunset-gradient`'s own header says it cannot see that. **Not a violation; recorded
  so a converter does not "simplify" it into a literal.**
- Its two `min-h-11` occurrences are the *source* of the floor. Replacing them with `<Chip>`
  is almost certainly right and would make the file the primitive's consumer instead of its
  ancestor — but no converted file in the tree has performed that inversion, so it is a
  judgement, not a copy.

---

## 7. Corrections measured today

Six figures in the upstream documents do not survive a re-measurement. Recorded with the
command, per this phase's own instruction — *a decision built on a wrong measurement is worth
more corrected than obeyed*. Four of the six change what a plan asserts.

### 7.1 `41.1-PATTERNS.md` §5.3 is already withdrawn in the tree — do not carry it forward

§5.3 of the previous map claimed `RevealVenueDialog.tsx:426`'s `autoFocus` was inert and the
focus placement therefore *"unspecified in practice"*. **The converted file corrects it, with
its measurement**, at `RevealVenueDialog.tsx:59-75`:

> *"the effect in this file then called `.focus()` on the cancel ref immediately after
> `showModal()`, which is exactly the moment that lands. So `Cancel` **was** focused. It
> simply was not visible … **This corrects D-41.1-24 and `41.1-PATTERNS.md` §5.3** … The
> conclusion about the attribute is right; the conclusion about this file is not, because the
> imperative call one screen above it was not read."*

A plan for `SecretVenueDialog` that cites §5.3 as an argument would be citing a withdrawn
claim. **The live argument is `Dialog.tsx:117-147`**, about the attribute, not about any file.

### 7.2 The four page files writing their own maximum are a **different four**

```bash
for f in <the ten page files>; do LC_ALL=C /usr/bin/grep -n 'max-w-' "$f"; done
```

| Page file | `41.2-RESEARCH.md` §1.6 | **Measured 2026-08-14** |
|---|---|---|
| `src/app/page.tsx` | `max-w-xs` ×1 | **`max-w-xs` ×1 at `:46`** ✓ |
| `(public)/tickets/[id]/page.tsx` | `max-w-sm` ×2 | **`max-w-sm` at `:108` and `:191`** ✓ |
| `(public)/events/[slug]/menu/page.tsx` | `max-w-lg` ×1 | **`max-w-lg` ×1 at `:163`** ✓ |
| `(public)/events/[slug]/page.tsx` | `max-w-lg` ×1 | **ZERO. The file carries no `max-w-` at all** |
| `(public)/artists/[slug]/page.tsx` | *not listed* | **`max-w-lg` ×1 at `:126`** — a paragraph measure on a `<p>` |

Still four files; **two of the four are different**. The consequences are both real:

- The `/events/[slug]` plan (the phase's most expensive) would go looking for a maximum that
  is not there, and either invent a task or record a false "removed".
- **`/artists/[slug]` is in wave 1, the cheap one**, and would ship a **check-D red** on its
  first declaration. `:126` is `<p className="mt-6 max-w-lg text-muted …">` — a *typographic
  measure*, the exact case `TYPOGRAPHIC_MEASURES` was built for and DEF-41-04 recorded as a
  casualty. So the phase's first own-maximum decision arrives in wave 1, not wave 6.

Check D reads the **page file only** (`verify-conversion.mjs:3161`), so a maximum inside a
component — e.g. `NewsletterForm.tsx:43`'s `max-w-sm` — is invisible to it. **A limit, not a
permission.**

### 7.3 `/newsletter` does not lack an `<h1>` — its form carries two

`41.2-RESEARCH.md` §7 item 12 names three pages with no `<h1>` and says adopting `PageTitle`
*"gives each one"*. Measured across the surface rather than the page file:

```
src/app/(public)/newsletter/page.tsx                h1=0
src/components/newsletter/NewsletterForm.tsx        h1=2      ← :36 (success branch), :44 (form branch)
```

So `/newsletter`'s surface has a heading in **both** branches, and adopting `PageTitle` on the
page file would give it a **second** one. The conversion here is *substitution inside the
form*, one per branch — not an addition. Only **two** pages genuinely have none:
`src/app/page.tsx` and `(public)/events/[slug]/menu/page.tsx`, and RESEARCH's copy caution
about a heading a guest reads standing at the bar applies to the second alone.

### 7.4 The `sm:` split is 13 / 2 / 1, not 12 / — / 1

`npm run verify:breakpoints` → 5 files, 16 uses. Enumerated by hand:

| Kind | Count | Sites |
|---|---|---|
| **Dialog shell** — absorbed and **deleted** by the primitive | **13** | `RedeemConfirmationModal.tsx:167,168` (3) · `SumUpCheckoutModal.tsx:50,51` (4) · `GuestTokenDisplay.tsx:222,223` (3) · `GuestLoginBanner.tsx:71,76` (3) |
| **Type size** — *not* absorbed; migrate or delete | **2** | `RedeemConfirmationModal.tsx:136` · `GuestTokenDisplay.tsx:193`, both `text-4xl sm:text-5xl` |
| **Grid axis** — gains the middle step | **1** | `MediaUpload.tsx:624`, `grid-cols-2 sm:grid-cols-3` |

`41.2-RESEARCH.md` §9 P6 says *"Twelve of them are absorbed and deleted by the Dialog
primitive"*. Thirteen. And the **two type-size uses have no primitive to absorb them** — they
are a live decision for the money-core plan, not a by-product of the dialog swap.
`MediaReviewGrid.tsx:154` is the analog for the third kind.

### 7.5 `Switch.tsx` exists — `41.1-PATTERNS.md` §4.4 and §5.2 are closed

```
ls src/components/ui/  →  13 files, including Switch.tsx
grep -n "^export function" src/components/ui/Switch.tsx  →  165:export function Switch({
```

Published by plan 41.1-10 with `DrinkMenuManager` as its first consumer in the same commit,
per D-41-04. **No file in this perimeter carries a hand-written switch track**, so it is
recorded as closed rather than inherited.

### 7.6 `Skeleton.tsx` has 13 importers, not 5

`grep -rln 'components/ui/Skeleton' src` → **13**: 9 `loading.tsx`, plus
`admin/TransactionList.tsx`, `ui/Button.tsx`, `ui/DataTable.tsx`, `ui/Input.tsx`. D-41-04's
recorded failure — *a primitive that is correct and has zero importers* — is **paid**. What is
still true is the volume: the five files in §2.2 are 35 of the tree's 39 remaining hand-rolled
pulses, and after this phase the only ones left are the primitive's own four and the
scanner's four.

---

## 8. What the wave cut inherits from this map

Five findings change a plan's shape rather than its content.

| Finding | Wave it binds |
|---|---|
| `/artists/[slug]:126` writes a container maximum (§7.2) | **wave 1** — the phase's first `TYPOGRAPHIC_MEASURES`-or-delete decision arrives here, not in wave 6. It cannot be a wave-0 abstraction |
| `/events/[slug]` writes **no** maximum (§7.2) | **wave 7** — one task fewer, and no false "removed" in the SUMMARY |
| Three line numbers in the venue assertions have drifted (§5.2) | **wave 7** — the impact analysis re-measures before it quotes, or asserts nothing |
| `MyMediaSection` is a lightbox, not a dialog (§6.1) | **wave 6c** — the disposition is decided in writing before the diff; `Lightbox`'s props are read first |
| Two `sm:` type-size uses have no primitive (§7.4) | **wave 6** — a decision, not a by-product of the shell swap |
| The nav mount moves **out** of the deleted page root (§4.3) | **every wave** — eight of the ten render it inside `min-h-dvh pb-24` today |

---

## Metadata

**Analog search scope:** `src/components/ui/` (13 files) · `src/components/media/` ·
`src/components/venues/` · `src/components/artists/` · `src/app/(admin)/admin/(work)/` ·
`src/app/(admin)/admin/events/[id]/{tickets,reveal}/` · `src/app/(admin)/admin/formats/` ·
`src/app/(auth)/` · `src/app/(public)/{gallery,payment/callback}/` ·
`scripts/{conversion-manifest,verify-conversion,verify-dialogs,verify-breakpoints}.mjs`.

**Read but not modified:** every file cited. No source, gate or manifest edit. No
`npm run dev`. No database read or write. The gates run were read-only:
`npm run verify:dialogs` (DIALOGS_OK, REMAINING = 8) and `npm run verify:breakpoints`
(BREAKPOINTS_OK, 5 files / 16 uses), both this session.

**`scripts/verify-conversion.mjs` was read with the Read tool and with
`LC_ALL=C /usr/bin/grep`**, never with the shell's default `grep`: it holds two literal NUL
bytes and the default is `ugrep -I`, which skips binary files silently. A zero-hit grep there
is not evidence of absence.

**Figures inherited rather than re-derived:** `41.2-RESEARCH.md` §1.3 (the perimeter
totals), §1.4 (the three group tables), §1.7 (the five shared files), §2.1 (the eight dialogs
and five breakpoint entries), §3.3 (the venue exit enumeration), §3.5 (the four silent
catches). Each is cited at the point of use. §7 records where a re-measurement disagreed.

**Re-measured today (2026-08-14):** the ten page files' roots, `<h1>`s and `max-w-` hits; the
`animate-pulse` census in the five placeholders; the 16 `sm:` uses by kind; the two
declaring sites and the eleven `MobileNav` mount sites; the primitive importer counts
(`PageShell` 37, `Skeleton` 13, `Dialog` 9, `AppNav` 3); `ls src/components/ui/` (13 files,
`Switch` present); the venue assertion line numbers in `(public)/events/[slug]/page.tsx`.

**Valid until:** wave 0 lands. Every count here is on a tree this phase is about to move —
and `41.2-RESEARCH.md`'s own note applies with more force to this document than to that one:
this tree moved four times on 2026-08-14 alone. **Re-measure before quoting, with the
command.**

---

*Phase 41.2 — pattern map written 2026-08-14. Contains no venue under negotiation, no
unannounced date, no line-up and no personal name: `.planning/` is tracked and this
repository is public.*
