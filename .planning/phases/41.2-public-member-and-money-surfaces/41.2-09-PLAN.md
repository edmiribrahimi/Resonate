---
phase: 41.2-public-member-and-money-surfaces
plan: 09
type: execute
wave: 4
depends_on: ["41.2-06", "41.2-07", "41.2-08"]
files_modified:
  - scripts/conversion-manifest.mjs
  - .planning/phases/41.2-public-member-and-money-surfaces/41.2-HUMAN-ROWS.md
autonomous: true
requirements: [DS-07, DS-08, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "Four more surfaces are declared, and check A walks their closures instead of trusting a claim"
    - "Every entry on both open ratchet lists is re-derived against the tree with a second instrument, including the ones nothing was expected to move"
    - "Check E's declaring set and mounting set are equal in both directions with seven pairs in each"
  artifacts:
    - path: "scripts/conversion-manifest.mjs"
      provides: "four new declared surfaces, each with a reason written from the files that render"
      contains: "/tickets/[id]"
    - path: ".planning/phases/41.2-public-member-and-money-surfaces/41.2-HUMAN-ROWS.md"
      provides: "wave 3's human rows appended, every Result pending"
      contains: "pending"
  key_links:
    - from: "scripts/conversion-manifest.mjs"
      to: "scripts/verify-conversion.mjs"
      via: "the declared list both G1 and G4 read, and the pairing check E performs over it"
      pattern: "CONVERTED"
---

> ## ⚠ Check E's pairing does NOT consult `CONVERTED` — do not read it as proof your declaration landed
>
> **Measured in wave 2** (`41.2-05-FINDINGS.md` F-41.2-05-01), and re-derived at source:
> `filesMountingResponsiveForm` is computed from **`allSrcFiles`** — the whole `src/` walk —
> not from the manifest. So the pairing number rises when the **conversion commits** land,
> in the wave that edits the page files, and is **already at its new value before a
> reconciliation runs**. Wave 2 saw this concretely: its pairing was 5/5 both before and
> after its own declarations, having gone 2/2 → 5/5 during wave 1.
>
> **A reconciliation that reads its own pairing number as evidence that its declaration
> landed is crediting a counter to the wrong cause** — the same shape as the four recorded
> recurrences of a debt going quiet. Prove the declaration by what actually changes with
> it: the **declared surface count**, the **files scanned by checks A/B/D**, and the two
> difference lists check E prints. Report the pairing; do not lean on it.

> **`npm run verify` cannot exit 0 inside a worktree, and that is expected.**
> Measured in wave 0: `verify:capabilities` **refuses with exit 2** when `.env.local`
> is absent, printing *"Nothing was measured"* **before touching anything**. A
> worktree has no `.env.local`, and putting Supabase credentials into one is
> forbidden by **D-41.2-04** — the road that does not touch production. So the
> executor-side bar is:
>
> **all 15 gates that reach a verdict pass, 0 fail, and `verify:capabilities`
> refuses for want of credentials.** A refusal that names its missing input is a
> gate declining to measure, not a gate failing — and it is distinguishable from
> both a pass and a failure, which is the whole point. Quote the tail of the run
> showing the counts.
>
> **Do not make it green.** Not by adding credentials, not by skipping the gate,
> not by rewording the criterion. The full 16/16 suite is run by the orchestrator
> in the main checkout after this wave merges, where `.env.local` exists.

<objective>
Reconcile wave 3 against the tree: declare its four surfaces, re-derive every
entry on both open ratchet lists, and append the human rows it owes.

Purpose: **this is the wave that is expected to delete nothing**, and that is
exactly why the re-derivation still happens in full. A reconciliation that only
runs when a number is expected to move is a reconciliation that cannot detect the
failure it exists for — a count falling for the wrong reason. Four recorded
mechanisms in this phase family have already produced that: a never-measured map,
a fence match, an unguarded walk-membership check, and a comment stripper going
blind inside a file. **A wave that touched no entry should be able to prove it,
and proving it is the work.**

Check E now carries seven pairs. The declaring set and the mounting set must be
equal **in both directions**, read out of the gate rather than asserted from the
plans, because the failure direction that is *silent* — declaring without mounting
— is the one no person will notice at any width.

Output: a manifest with four more entries, two ratchet lists proved unmoved, and
twelve more human rows written `pending`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-VALIDATION.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-06-SUMMARY.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-07-SUMMARY.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-08-SUMMARY.md
</context>

<conversion_rules>
1. **This plan is one of the phase's three declared gate editors** — 41.2-05,
   41.2-09, 41.2-19 — and the only one in this wave (D-41.1-22).
2. **41.2 does not edit the spine.**
3. **No server action is opened.** **No package is installed.**
4. **DEF-41-01** — never spell a bracketed arbitrary-value utility in a manifest
   reason, a gate comment or a SUMMARY. Cite `src/app/(public)/gallery/page.tsx:110`.
5. **`.planning/` is public.** No venue name, no date, no line-up, no member, no
   ticket identifier, no amount.
6. **No `npm run dev`, no headless render** (D-41.2-04). Every human row is
   `pending`; **deferred is not verified.**
7. **⚠ `scripts/verify-conversion.mjs` holds two literal NUL bytes and is
   invisible to the shell's default `grep`** (`ugrep -I` skips binary files
   silently). Read it with the Read tool; search it with `LC_ALL=C /usr/bin/grep`.
8. **macOS/BSD.** `grep -E`, `sed -i ''`. **No test runner exists.**

**Quick run:** `npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: Declare the four surfaces, with their cautions carried</name>
  <files>scripts/conversion-manifest.mjs</files>
  <read_first>
    - `scripts/conversion-manifest.mjs` — `CONVERTED` from `:442`, and the five most recent entries at `:654-673`, whose reasons are the model. Note especially the entry that carries a **THE CAUTION** paragraph for the one work page reaching the venue reveal: that is the shape `/events` and `/tickets/[id]` both need
    - `scripts/conversion-manifest.mjs` — `checkManifest()`, and that it compares a declared width against the closed lists
    - `scripts/verify-conversion.mjs` check D `:3134-3184`, check E `:2628-2702` and `:2785-2820` — **Read tool, not the default grep**
    - The three wave-3 SUMMARYs, for the entry texts they were asked to report and the assertions they quoted
  </read_first>
  <action>
Add **four `CONVERTED` entries** — `/events`, `/attendance`, `/membership-card`,
`/tickets/[id]` — each at `default`, **taking the reason text from the SUMMARY of
the plan that did the work.**

Every reason carries the three standard things: the plan number and what *whole*
turned out to mean in files; the width with its reason — *not on the closed wide
list and therefore default, which is not a fallback*; and the no-behaviour-change
sentence, *no query changed, no column added, no capability check touched, no
action payload altered.*

Then carry the cautions, because these four are not all the same kind of surface:

- **`/events`** — **THE CAUTION, because this is the public funnel and it names
  venues:** the declared render mode is still declared and not derived; the
  stored-flag comparison is still written explicitly against `false`, so a row
  that never arrived or a join that failed still counts as secret; the label
  ternary still puts the secret branch first; and the night select's column
  multiset is unchanged, proved over comment-stripped source rather than by
  reading a diff. Record that the wide form was **deferred and not rejected**,
  reversible in one line of the shell's call.
- **`/tickets/[id]`** — **THE CAUTION, because this surface holds the same secret
  as the public event page under a different predicate:** the venue line, its
  assignment and its comment are byte-identical; the stored secrecy flag is still
  selected and still read nowhere; and the narrow form is **unavailable** on this
  surface rather than deferred, because check E forbids it on any surface mounting
  a navigation.
- **`/membership-card`** — the conditional guarding what a person holds up at a
  door is byte-identical.
- **`/attendance`** — the phase's control surface: one file, no money, no venue,
  no dialog.

All four also carry the navigation sentence: they mount the responsive form and
declare the tablet-tier clearance, **width may change layout and never
membership**, and the same four props go in so no entry appears or disappears.

**Read check E's output and paste both sets.** Seven files should now appear in
both. Both difference lists must be empty.
  </action>
  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-conversion.mjs` exits 0 and reports **35** declared surfaces (31 + 4), all five checks passing
    - `checkManifest()` raises no width disagreement
    - The scanned-file count rises by the four closures **plus the two route-adjacent placeholders** beside `/events` and `/membership-card`; state the number and how it was derived
    - Check E's two sets are pasted in full; the four new page files appear in **both**, and `declaringWithoutMounting` and `mountingWithoutDeclaring` are both **empty**
    - Check D names **no** page file in `pagesWithOwnMaximum` — pasted
    - `LC_ALL=C /usr/bin/grep -c 'THE CAUTION' scripts/conversion-manifest.mjs` returns at least **2** more than before this plan; state both numbers
    - `LC_ALL=C /usr/bin/grep -c 'no query changed' scripts/conversion-manifest.mjs` returns at least **4** more than before; state both numbers
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Four surfaces are declared with the cautions their own plans reported, check A walks four more closures, and check E's two sets are equal in both directions with seven pairs in each.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive every entry — including in a wave where nothing was expected to move</name>
  <files>.planning/phases/41.2-public-member-and-money-surfaces/41.2-HUMAN-ROWS.md</files>
  <read_first>
    - `scripts/verify-dialogs.mjs:1197-1310` — the entries as they stand after plan 41.2-05, and `:1471` — the emptiness refusal
    - `scripts/verify-breakpoints.mjs:347+` — the five entries with their declared counts; `:196-217` — the four rules that travel with the constant, including that a **lowered** count is reported STALE loudly and never as a failure
    - `scripts/lib/comments.mjs` — `liveLines` / `liveLinesFrom`. **This is the instrument the second measurement must NOT use**
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-RESEARCH.md` §9 P1 — the four recorded mechanisms
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-05-SUMMARY.md` — the previous diff table, so the two are comparable row for row
  </read_first>
  <action>
**Diff entries, not counts — and do it in full even though nothing is expected to
move.** For **every** entry on both open lists, re-derive from the tree whether it
is still owed, and produce the same four-column table plan 41.2-05 produced:
entry, the gate's current verdict, what the tree says, and **what the second
instrument says**.

**The second instrument does not use the gates' shared comment stripper.**
`scripts/lib/comments.mjs` is what both gates use, and a round of this phase
family measured it going blind inside a file while the gate printed *converted;
remove this entry* and exited 0. The second measurement is a **raw needle count on
the raw file** — `LC_ALL=C /usr/bin/grep -c` for the shell signature and for the
breakpoint prefix. Where the raw count and the stripped count disagree, say which
disagreed and in which direction: a raw count that is **higher** because the needle
survives inside a comment is explainable; a raw count that is **lower** is not, and
it stops any deletion on that row.

**The expected outcome of this wave is zero deletions.** None of wave 3's files
carries a hand-rolled dialog shell, and none carries a third breakpoint prefix.
**Prove both as measured zeros**, per file, and record the proof. A wave that
merely says *nothing moved* has asserted the thing this table exists to check.

**Do not delete the two STALE orphan entries** — `GuestLoginBanner.tsx` and
`RefundRequestButton.tsx`, converted in wave 0. Both gates report them STALE and
exit 0; they are deleted by 41.2-19 alongside the closure decision, because
emptying a list without a `MIGRATION_CLOSED` above the constant breaks the gate on
purpose with exit 2 and nothing measured. Restate the decision here so it is
visible twice rather than remembered once.

**Then append wave 3's human rows** to `41.2-HUMAN-ROWS.md`: one row per surface
per width at **390 / 768 / 1440** for `/events`, `/attendance`,
`/membership-card` and `/tickets/[id]`; plus a row for the events listing's
placeholder — that content does not shift sideways when data lands, which is what
a placeholder width mismatch looks like. Every criterion is a **number with a
tolerance**; every Result reads `pending`.
  </action>
  <verify>
    <automated>node scripts/verify-dialogs.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-tables.mjs && npm run verify; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-dialogs.mjs` exits 0 with `REMAINING = 7` — **unchanged**, and stated as a measured zero movement with the per-file proof
    - `node scripts/verify-breakpoints.mjs` exits 0 with **5 files / 16 uses** — unchanged, same proof
    - `node scripts/verify-tables.mjs` exits 0, `REMAINING = 0` with its closure set — untouched
    - The four-column diff table covers **all twelve** remaining entries across both lists, every row carrying the second instrument's raw count
    - Every row where the raw and stripped counts disagree carries one sentence naming the direction; no row has a raw count lower than its stripped count without the plan stopping and reporting it
    - The SUMMARY restates, with its reason, that the two STALE orphan entries stay until 41.2-19
    - `41.2-HUMAN-ROWS.md` gains at least **twelve** width rows plus the placeholder row, and its `pending` count equals its Result-cell count — state both numbers
    - Every appended *what must be true* cell contains a number with a tolerance; quote three as evidence
    - `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan); `npm run build` exits 0
  </acceptance_criteria>
  <done>Twelve entries were checked against the tree with an instrument the gates do not share, a wave that moved nothing proved it moved nothing, and the phase's human debt grew by thirteen rows that all read pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → gate | a declared entry makes the gate assert about a named file |
| debt list → reader | an unchanged count is a claim as much as a changed one |
| ledger → the sitting | a row that reads as measured when it was inferred closes a requirement nobody exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-50 | Repudiation | a ratchet count that did not move for the wrong reason | mitigate | all twelve entries re-derived in a wave where nothing was expected to move, each with a second instrument that does not share the gates' comment stripper; the unchanged counts are proved per file rather than asserted |
| T-41.2-51 | Repudiation | a surface declared converted that is not | mitigate | check A walks each new closure the moment the entry lands; the route-adjacent extension means a placeholder cannot pass by absence |
| T-41.2-52 | Denial of service | check E's silent failure direction — declaring without mounting | mitigate | both sets pasted from the gate's own output and both difference lists asserted empty; the silent direction is the one no person will catch at any width |
| T-41.2-53 | Information disclosure | a venue-critical caution lost between a SUMMARY and the manifest | mitigate | the caution paragraphs are required in the entries for `/events` and `/tickets/[id]`, and their presence is asserted by count |
| T-41.2-54 | Denial of service | a list emptied in a wave with no closure decision to write | mitigate | the two STALE orphan entries deliberately left; the decision restated so it is visible twice |
| T-41.2-55 | Repudiation | a human row ticked without a person | mitigate | every Result written `pending`, the count asserted equal to the row count, every criterion a number with a tolerance |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan); `npm run build` exits 0
- `verify-conversion` reports 35 surfaces; `verify-dialogs` 7; `verify-breakpoints` 5 files / 16 uses; `verify-tables` 0 with its closure set
- The twelve-entry diff table is recorded with its second-instrument column
- Check E's two sets are pasted and both difference lists are empty
- The ledger's `pending` count equals its Result-cell count
</verification>

<success_criteria>
Seven of this phase's ten surfaces are declared and walked; the two open ratchet
lists are proved unmoved rather than asserted unmoved; check E's two sets are
equal in both directions with seven pairs in each; and the human debt of four more
surfaces is written as numbers with tolerances, every Result pending.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-09-SUMMARY.md`
when done, with the twelve-entry diff table and its second-instrument column, the
per-file proof of the zero movements, check E's two sets pasted, and the ledger's
two equal counts.
</output>
