---
phase: 41.2-public-member-and-money-surfaces
plan: 20
written: 2026-08-14
corrected: 2026-08-19 — dall'audit di milestone della v1.5. Quattro righe nominavano indirizzi rimossi lo stesso giorno in cui questo file e' stato scritto (§2a); sei superfici costruite dalle fasi 44 e 45 dopo quella data non erano nell'unione (§2b, osservazione O14). Un documento che si dichiara l'unione di un requisito e non lo e' spunta quel requisito su un insieme sbagliato
status: pending — written BEFORE the sitting that fills it in. Every Result below reads `pending`, and an empty Result is an UNRUN procedure, never a verified-by-inspection in disguise
ticks: >-
  RESP-01. This is the document that ticks the requirement, and it is the only
  one. `REQUIREMENTS.md:254` says RESP-01 *"says **every** surface, so it closes
  only in 41.2, and by a written human pass; no script can close it"* — so the
  rows here are the UNION of that requirement's evidence across the whole
  milestone, not this phase's share of it
closes: >-
  RESP-01, and it is **NOT CLOSABLE BY A BLANKET REPLY**. A single word covering
  fifty-seven rows would tick a requirement on evidence nobody produced, which is
  the failure this document was split off to prevent: Phase 41 was closed by one
  word — `approved` — with no itemised findings, and four criteria stayed
  unticked underneath it. Each row below is ticked by its own recorded
  observation or it is not ticked. **Closing `41.2-RELEASE-PASS.md` does not
  close this**, and if only one of the two documents comes back filled in, that
  is visible from both
batch: >-
  the end-of-v1.5 human sitting — the SAME sitting as `41.2-RELEASE-PASS.md`, the
  one `41-RELEASE-PASS.md`'s `batch:` field already commits, alongside
  `40-RELEASE-PASS.md`, `39-DOOR-PASS.md` and `38-PROCEDURES.md`. NOT A SECOND
  SITTING. Two documents, one sitting: a second sitting would not add attention,
  it would split it
devices: >-
  three — a phone, a **LARGE TOUCH SCREEN** (a tablet, not a narrow phone), and a
  laptop with a mouse. **The tablet may not exist.** §5 says in advance what
  happens then, and it is the same thing `41-RELEASE-PASS.md` said in advance and
  then had to do
accounts: >-
  four sessions, described by the capability each holds and never by whose it is
  — no session; the member role at approved status; `organizer.access` together
  with `catalogue.manage` and `admin.access`; and **a session entitled to see a
  night's address, plus one that is not** (§6 needs both, and it needs them to be
  real sessions). Roles, never people
requires: >-
  a checkout that holds `.env.local`, and a running application. **NO ROW BELOW
  WAS MEASURED DURING EXECUTION, and that is a decision rather than an omission**:
  D-41.2-04 forbade this phase's worktrees running the application, no worktree
  held `.env.local`, and supplying it would point a running application at
  production — which this repository has incident D12 behind. The decision is
  standing for the phase, not permanent
widths: 390 / 768 / 1440 for this phase's ten (see `41.2-RELEASE-PASS.md` §0.3). **The thirteen owed rows keep the widths their own verifications wrote — 390 / 768 / 1280 — because a reworded row is a new row**
role: the owner, on their own machine
---

# Phase 41.2 — The RESP-01 Closing Pass

> **(a) Every `Result` below reads `pending`, and an empty Result is an UNRUN
> PROCEDURE.** Never a verified-by-inspection in disguise. A tick nobody earned is
> worse than an empty line, because it closes a requirement.
>
> **(b) Every criterion is a NUMBER WITH A TOLERANCE**, except where a row is
> carried verbatim from a verification that wrote it as prose — and where that is
> so, it is marked, because **rewriting an owed row into this document's voice
> would replace it**.
>
> **(c) This document TICKS THE REQUIREMENT and is not closable by a blanket
> reply.** Read the `closes:` field before reading a row.
>
> **(d) Roles, never people.** `.planning/` is tracked and this repository is
> public. A person here is *a session holding a named capability*. **No venue, no
> unannounced date, no line-up, no member, no amount.**
>
> **(e) Class strings are described, never spelled** (DEF-41-01): the navigation
> column's clearance is **224px, by its measurement**, and the declaration is cited
> at `src/app/(public)/gallery/page.tsx:110` rather than written out.
>
> **(f) Sixteen gates run in one command and NOT ONE OF THEM HAS SEEN A PIXEL.**
> They read class strings, import clauses and declarations. **There is no test
> runner in this repository**, none was introduced, and nothing below is covered by
> one. RESP-01's whole content — *every surface usable on phone, tablet and
> desktop, with the layout chosen for the device the surface is actually worked on*
> — is a sentence no script in this repository can evaluate. That is why this
> document exists and why it cannot be closed by a word.

---

## The union, stated as arithmetic

**A document that ticks a requirement must contain the UNION of that requirement's
evidence, or it ticks something it did not measure.** RESP-01 says *every* surface.
So:

| Part | Count | Where its rows are |
|---|---|---|
| surfaces declared converted **before** this phase | **28** dichiarate, di cui **4 poi RIMOSSE** (2026-08-14) -> **24 vive** — 8 in Phase 41, 16 vive in Phase 41.1 | **§2**, con §2a per le quattro rimosse |
| this phase's surfaces | **10** | §3, at three widths — 30 rows |
| superfici costruite **dopo** questo documento, dalle fasi 44 e 45 | **6** | **§2b** — aggiunte il 2026-08-19 |
| **superfici in totale** | ~~38~~ -> **40 dichiarate, 4 rimosse, 36 da osservare** | — |
| observations **already owed** | **14** — **7** da `41-VERIFICATION.md`, **6** da `41.1-VERIFICATION.md`, **1** (`O14`) aperta il 2026-08-19 per le sei superfici di 44/45 | §1 per le tredici, §2b per O14 |
| the navigation form, per surface of this phase | 10 | §4 |
| the touch-screen observation for this phase's ten | 1 | §5 |
| the venue observation, **Critical** | 1 | §6 |
| preconditions | 2 | §7 |

**Fifty-seven rows. Fifty-seven unrun procedures.**

> **Why 28 and not a quarter of it.** Ticking RESP-01 on this phase's ten alone
> would close it on **roughly a quarter of its own scope** — ten surfaces out of
> thirty-eight. That is the shape this project has already had to correct more than
> once, and the correction is cheaper before the tick than after it.

**Measured against the tree at the time of writing:** `CONVERTED` holds **35**
entries — the 28 declared before this phase, plus seven of this phase's ten. **The
remaining three** — the bar's QR surface, the member dashboard and the public event
page — are declared by plan 41.2-19 in this same wave. The count of ten comes from
`41.2-CONTEXT.md`'s Phase Boundary, which is the scope statement, and not from a
manifest that was still being written beside this document.

---

## §1 — The thirteen already owed, carried VERBATIM

**These thirteen are not this document's rows. They are two other verifications'
rows, and they are reproduced word for word with their original identifiers so a
reader can trace each one back to the document that raised it.** Where the source
gave a row no identifier, that is recorded as such and the source's own section
number and line are given instead — **inventing an identifier would be the same
error as rewording the row.**

**Nothing below was rewritten into this document's voice.** Several are prose
rather than numbers, and that is how their verifications wrote them; making them
numeric here would produce a new row wearing an old row's name.

### §1a — Seven from `41-VERIFICATION.md` (`human_verification:`, lines 42–62)

| # | Identifier | Test — verbatim | Expected — verbatim | Result |
| --- | --- | --- | --- | --- |
| O1 | **H41-1** | "H41-1 — the eight surfaces this phase declares, at 390px / 768px / 1280px, on a device" | "nothing clipped, nothing stretched, no horizontal scroll; the navigation column clears the content at 768px and up" | pending |
| O2 | **`41-CR01-PASS.md` rows 7–13** | "41-CR01-PASS.md rows 7–13 — /set-password and /payment/callback at three widths, and /register at 390px scrolled to its last field" | "card centre within 4px of the viewport centre at 768 and 1440; symmetric space at 390; no navigation bar; no horizontal scroll; at 390 the form ends with a visible gap below its last element" | pending |
| O3 | **H41-2** | "H41-2 — a converted dialog on a phone and on a laptop, including whether the page behind the open panel scrolls (assumption A2)" | "sheet at 390px, window at 1280px, Escape closes, the background does not scroll" | pending |
| O4 | **H41-3** | "H41-3 — the densest converted table on a phone" | "cards at 390px, no sideways scroll, the columns that mattered still present" | pending |
| O5 | **H41-4** | "H41-4 — the smallest interactive control on a LARGE TOUCH SCREEN, measured" | "at least 44×44 CSS px in both axes as rendered, with the one declared exception" | pending |
| O6 | **H41-5** | "H41-5 — a desktop with a mouse only: the row-action pills at their reduced height, and only those" | "the pointer variant applies where D-41-07 says it does and nowhere else" | pending |
| O7 | **H41-6a / H41-6b** | "H41-6a — eight tabs at 768px without scrolling the strip, and H41-6b — the door unchanged at both of its addresses" | "eight tabs visible without scrolling; the door is the bottom bar at every width" | pending |

### §1b — Six from `41.1-VERIFICATION.md` (`human_verification:`, lines 22–40; body §"Human Verification Required", numbered 1–6)

| # | Identifier | Test — verbatim | Expected — verbatim | Result |
| --- | --- | --- | --- | --- |
| O8 | **H41-1** *(41.1's own H41-1 — a different scope from O1's; both keep their name)* | "H41-1 — every surface this phase converted, at 390px / 768px / 1280px" | "nothing clipped, nothing stretched, no horizontal scroll; the navigation column clears the content at 768px and up" | pending |
| O9 | **H41-3** *(41.1's)* | "H41-3 — the six card-rendering tables on a phone" | "the card carries the columns that mattered; no sideways scroll at 390px" | pending |
| O10 | **H41-4** *(41.1's)* | "H41-4 — the smallest interactive control, on a LARGE touch screen, measured" | "at least 44px in both axes as rendered" | pending |
| O11 | **no identifier in the source** — `41.1-VERIFICATION.md:32-34`, body §"Human Verification Required" row **4**, marked **Critical** there | "The venue-reveal confirmation still refuses the accidental path" | "opening the dialog puts focus on Cancel visibly; Enter performs nothing; the confirming control must be pressed deliberately" | pending |
| O12 | **no identifier in the source** — `41.1-VERIFICATION.md:35-37`, body row **5** | "A refund control's confirmation is unchanged in observable behaviour" | "same two answers, same refusals, the reason field still optional, the amount unchanged" | pending |
| O13 | **no identifier in the source** — `41.1-VERIFICATION.md:38-40`, body row **6** | "The toast clears the navigation column at 768px on a work surface" | "a raised toast does not sit under the column" | pending |

> **Two of the thirteen have NEVER ONCE BEEN PRODUCED, and they are O5 and O10 —
> both of them the touch-screen row, one raised by each verification.** This is
> written here, in the document, and not only in a summary. `41-RELEASE-PASS.md`
> records O5 as `human_needed` because the owner's approval never stated that a
> large touch screen was available, and that document declines to read a silence as
> a device. `41.1-VERIFICATION.md` says of O10, in its own words, *"This is the only
> thing in the repository that proves anything is 44px."* **Between them, nothing in
> this repository has ever asserted that any control is physically 44px on the
> device that criterion names.** See §5.
>
> **O3 carries an assumption that has been open since Phase 41's research.**
> Assumption **A2** — whether the page behind an open dialog scrolls — was open when
> the primitive was written, was open when the phase closed, and is open now.
> `41-RELEASE-PASS.md` §9 records it as **STILL OPEN, nothing reported**. The
> primitive deliberately writes no scroll lock of its own, because writing one
> before observing whether the platform already does it would be a second author for
> a behaviour nobody has measured. **O3 is where A2 closes.**
>
> **O11 is Critical in its source and stays Critical here.** It is the venue-reveal
> confirmation, and the reveal is the one act in this product that cannot be undone.
> Its source's own words: *"That focus **lands** and is **seen** is an observation."*
>
> **O2's rows 1–6 were measured headless on 2026-08-13 and rows 7–13 were not.**
> That is why the owed row is *rows 7–13* and not *rows 1–13*: `/set-password` needs
> a token and `/payment/callback` needs a payment id, and neither is reachable
> without the owner. **A headless render is also not a device render** — it says
> nothing about fonts on a real screen, the safe-area inset, pinch-zoom, or whether
> a thumb can hit anything.

---

## §2 — The twenty-eight surfaces declared before this phase, and where their rows are

**Their rows are O1 and O8, carried verbatim in §1.** They are **not**
re-enumerated as eighty-four rows here, and the reason is the rule this document
runs on: **O1 and O8 already say, in their own verifications' words, *every surface
this phase declares, at three widths*.** Writing them out again as per-surface rows
would be rewording two owed observations into this document's voice — which is
exactly what "a reworded row is a new row" forbids, and it would also create two
owners for one observation.

What the table below does is **prove the coverage is complete**: every one of the
twenty-eight is named, with the owed row that covers it. **It carries no Result
column, because it is a coverage proof and not a set of rows.**

| # | Route | Declared by | Covered by |
|---|---|---|---|
| 1 | `/payment/callback` | Phase 41 | **O1**, and additionally **O2** (rows 10–12) |
| 2 | `/login` | Phase 41 | **O1** |
| 3 | `/register` | Phase 41 | **O1**, and additionally **O2** (row 13) |
| 4 | `/set-password` | Phase 41 | **O1**, and additionally **O2** (rows 7–9) |
| 5 | `/gallery` | Phase 41 | **O1** |
| 6 | `/admin/formats` | Phase 41 | **O1**, and **O3** (its dialog) |
| 7 | `/admin/members/register` | Phase 41 | **O1** |
| 8 | `/admin/members` | Phase 41 | **O1**, and **O4** (its table) |
| 9 | `/admin/artists` | Phase 41.1 | **O8** |
| 10 | `/admin/venues` | Phase 41.1 | **O8** |
| 11 | `/admin/newsletter` | Phase 41.1 | **O8** |
| 12 | `/admin/venues/[slug]` | Phase 41.1 | **O8** |
| 13 | `/admin/events` | Phase 41.1 | **O8** |
| 14 | `/admin/events/[id]/guest-list` | Phase 41.1 | **O8**, and **O13** (the toast) |
| 15 | `/admin/events/[id]/assignments` | Phase 41.1 | **O8** |
| 16 | `/admin/events/[id]/media` | Phase 41.1 | **O8** |
| 17 | `/admin/events/[id]/review` | Phase 41.1 | **O8** |
| 18 | `/admin/events/[id]/drinks` | Phase 41.1 | **O8** |
| 19 | ~~`/admin/analytics/members`~~ | Phase 41.1 | **SUPERFICIE RIMOSSA** — vedi §2a |
| 20 | `/admin/events/[id]/analytics` | Phase 41.1 | **O8**, and **O9** |
| 21 | ~~`/admin/analytics`~~ | Phase 41.1 | **SUPERFICIE RIMOSSA** — vedi §2a |
| 22 | ~~`/admin/analytics/compare`~~ | Phase 41.1 | **SUPERFICIE RIMOSSA** — vedi §2a |
| 23 | `/admin/members/growth` | Phase 41.1 | **O8**, and **O9** |
| 24 | ~~`/admin/finance`~~ | Phase 41.1 | **SUPERFICIE RIMOSSA** — vedi §2a. *La conferma di rimborso che O12 osserva vive anche a `/admin/events/[id]/tickets`, riga 26, che esiste: O12 non resta senza soggetto.* |
| 25 | `/admin/events/[id]/sales` | Phase 41.1 | **O8** |
| 26 | `/admin/events/[id]/tickets` | Phase 41.1 | **O8**, and **O12** (a refund confirmation) |
| 27 | `/admin/events/new` | Phase 41.1 | **O8** |
| 28 | `/admin/events/[id]/edit` | Phase 41.1 | **O8**, and **O11** — *the venue-reveal confirmation, Critical* |

**Ventotto righe. Ventiquattro coperte, quattro rimosse.** Ogni riga viva e'
raggiungibile da un'osservazione dovuta.

---

## §2a — Quattro superfici sono state rimosse, e questo documento non lo sapeva

**Corretto il 2026-08-19, dall'audit di milestone della v1.5.**

Le righe 19, 21, 22 e 24 nominano indirizzi che **non esistono nell'albero**:
`/admin/analytics`, `/admin/analytics/compare`, `/admin/analytics/members` e
`/admin/finance`. Sono stati eliminati — non nascosti, rimossi — insieme alle
loro voci in `CAPABILITY_ROUTES` (`src/lib/routes/capability-routes.ts:292-293`)
e a otto componenti orfani, per **decisione del proprietario del 2026-08-14**: la
finance si monitora sul cruscotto SumUp, non nell'app, perche' due posti che
raccontano lo stesso incasso sono due posti che possono divergere.

**La rimozione e' avvenuta lo stesso giorno in cui questo documento e' stato
scritto.** Nessuno se n'e' accorto per cinque giorni, e la prova non e' un
ragionamento: `find src/app -name page.tsx` non trova nessuno dei quattro
indirizzi, mentre trova i due vicini che le stesse righe avrebbero potuto far
confondere — `/admin/events/[id]/analytics` (riga 20) e `/admin/members/growth`
(riga 23), che **esistono e restano coperti**.

**Le righe non sono state cancellate, ma barrate.** Cancellarle avrebbe fatto
sparire la ragione insieme all'errore, e la prossima persona che rilegge questo
documento avrebbe trovato una tabella con ventiquattro righe e nessun modo di
sapere perche' non sono ventotto.

> **Perche' questa correzione doveva precedere la seduta, non seguirla.**
> Chiedere a una persona di osservare `/admin/finance` a tre larghezze significa
> chiederle di verificare un indirizzo cancellato. Il suo esito sarebbe stato
> `pending` — indistinguibile da una procedura semplicemente non eseguita — e
> avrebbe consumato attenzione in una serata in cui l'attenzione e' la risorsa
> scarsa.

---

## §2b — Sei superfici sono nate DOPO questo documento, e non erano contate

**Aggiunte il 2026-08-19, dallo stesso audit.**

Questo documento si dichiara *«l'UNIONE delle prove di RESP-01 su tutto il
milestone»* e *«l'unico documento che spunta il requisito»*. E' stato scritto il
**2026-08-14**. Le fasi **44** e **45** hanno costruito **sei pagine dopo quella
data**, e nessuna riga di questo file le nominava — cercare `calendar`,
`location`, `manifesto` o `visual` nelle sue 487 righe originali dava **zero
occorrenze**.

RESP-01 dice **ogni** superficie. Sei superfici fuori dall'unione significano che
il requisito si sarebbe spuntato su un insieme che non era piu' l'insieme.

| # | Route | Costruita da | Coperta da |
|---|---|---|---|
| 29 | `src/app/(admin)/admin/(work)/calendar/page.tsx` | Phase 44 | **O14** |
| 30 | `src/app/(admin)/admin/(work)/calendar/[id]/page.tsx` | Phase 44 | **O14** |
| 31 | `src/app/(admin)/admin/(work)/location/page.tsx` | Phase 45 | **O14** |
| 32 | `src/app/(admin)/admin/(work)/location/[id]/page.tsx` | Phase 45 | **O14** |
| 33 | `src/app/(admin)/admin/(work)/manifesto/page.tsx` | Phase 45 | **O14** |
| 34 | `src/app/(admin)/admin/(work)/visual/page.tsx` | Phase 45 | **O14** |

### O14 — l'osservazione che queste sei richiedono, e che nessuno deve ancora

| # | Identificatore | Prova | Atteso | Result |
| --- | --- | --- | --- | --- |
| O14 | **H44/45-1** *(nuovo: non esisteva quando questo documento e' stato scritto)* | Le sei superfici di produzione delle fasi 44 e 45, a **390px / 768px / 1280px**, su un dispositivo | niente tagliato, niente stirato, nessuno scorrimento orizzontale; la colonna di navigazione libera il contenuto da 768px in su — **gli stessi criteri di O1 e O8**, perche' il requisito e' lo stesso e cambiare il metro fra due gruppi di superfici renderebbe l'unione non confrontabile | pending |

> **Cio' che gia' si sa, e che O14 NON sostituisce.** Un controllo meccanico
> eseguito il 2026-08-19 dice che le sei importano `PageShell`, `Card`,
> `Typography` e `Chip` e **non contengono un solo colore letterale** — zero
> occorrenze di `#[0-9a-fA-F]{6}` nelle directory che le ospitano. **Il livello
> e' stato adottato.** Ma RESP-01 non chiede quali classi porta un file: chiede
> che una persona guardi la pagina a tre larghezze. Una stringa di classe non
> dice che nulla e' tagliato.

> **Nota per chi legge `verify:conversion`.** Le stesse sei pagine risultano
> «pending another phase's conversion» nel check F — 43 pagine, 36 dichiarate, 6
> in attesa. **Quella «altra fase» non esiste**: 44 e 45 sono le ultime del
> milestone. E' il recinto del gate a essere fermo al 41.2, non il livello a
> mancare: buco di copertura, non layer rotto.

---

## §3 — This phase's ten surfaces, at three widths

**Thirty rows.** Nothing owes these yet: this is where they are first written down.
The criteria are the measurements `41.2-RELEASE-PASS.md` §1 sets out — leading
clearance 224px ±8px from 768px up and 0px ±4px below it, a 1024px ±8px content
column, side spaces differing by ≤8px, a page that moves 0px when dragged sideways,
a bottom clearance of ≥16px ±4px on a phone, and a 44px minimum on every
interactive box.

> **These thirty and `41.2-RELEASE-PASS.md` §1's thirty are the SAME thirty
> observations, deliberately.** They are not duplicated work: they are one set of
> observations serving two different acts, and the two acts are separable. If a
> person takes them once, both documents' rows are filled from the same numbers.
> **What is not shared is the consequence**: filling them in the other document
> closes a phase gate; filling them here contributes to ticking a requirement, and
> ticking it also needs §1's thirteen, §5 and §6. **A blanket reply on the other
> document does not carry across to these**, and that asymmetry is the whole reason
> there are two documents.

| # | Route | Width | What must be true | Result |
| --- | --- | --- | --- | --- |
| Q1 | `/` | 390 | leading clearance 0px ±4px; page moves 0px when dragged sideways; last element clears the bar's top edge by ≥16px ±4px; the three action controls equal in width within 4px and each ≥44px tall | pending |
| Q2 | `/` | 768 | leading clearance 224px ±8px, nothing left of it; side spaces differ by ≤8px; page moves 0px; the three action controls ≥44px tall | pending |
| Q3 | `/` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; side spaces differ by ≤8px; page moves 0px | pending |
| Q4 | `/newsletter` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; exactly 1 top-level heading (count = 1, tolerance 0); field and submit each ≥44px tall | pending |
| Q5 | `/newsletter` | 768 | leading clearance 224px ±8px; the entry column 384px ±8px and its two side spaces differ by ≤8px; page moves 0px | pending |
| Q6 | `/newsletter` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; the entry column is still 384px ±8px and does NOT widen to 1024px; page moves 0px | pending |
| Q7 | `/artists/[slug]` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; the bio's rendered line breaks equal the count in the stored text (tolerance 0) | pending |
| Q8 | `/artists/[slug]` | 768 | leading clearance 224px ±8px; the bio ≤512px wide (+8px); page moves 0px; every link control ≥44px tall | pending |
| Q9 | `/artists/[slug]` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; **the bio is 512px ±8px**; side spaces differ by ≤8px; page moves 0px | pending |
| Q10 | `/events` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; exactly 1 top-level heading (count = 1, tolerance 0); each tab, chip and night card ≥44px tall | pending |
| Q11 | `/events` | 768 | leading clearance 224px ±8px; side spaces differ by ≤8px; page moves 0px; nights render ONE per row (count = 1, tolerance 0) | pending |
| Q12 | `/events` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; page moves 0px; nights still ONE per row (count = 1, tolerance 0) | pending |
| Q13 | `/attendance` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; the empty-state card's padding 24px ±2px on all four sides | pending |
| Q14 | `/attendance` | 768 | leading clearance 224px ±8px; side spaces differ by ≤8px; page moves 0px; padding still 24px ±2px | pending |
| Q15 | `/attendance` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; page moves 0px; the card spans the column within 8px, padding 24px ±2px | pending |
| Q16 | `/membership-card` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; **the code's drawn box 192px ±4px square**; wallet control and referral input each ≥44px tall | pending |
| Q17 | `/membership-card` | 768 | leading clearance 224px ±8px; side spaces differ by ≤8px; page moves 0px; **the code's box STILL 192px ±4px square** | pending |
| Q18 | `/membership-card` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; page moves 0px; **the code's box STILL 192px ±4px square** | pending |
| Q19 | `/tickets/[id]` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; **card and wallet control equal in width within 4px**; each ≥44px tall | pending |
| Q20 | `/tickets/[id]` | 768 | leading clearance 224px ±8px; side spaces differ by ≤8px; page moves 0px; **card and wallet control STILL equal within 4px** | pending |
| Q21 | `/tickets/[id]` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; page moves 0px; **still equal within 4px**; the cover reaches the card's edge with 0px ±2px of inset | pending |
| Q22 | `/events/[slug]/menu` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; every drink row's primary control ≥44px tall; price and control overlap 0px, tolerance 0 | pending |
| Q23 | `/events/[slug]/menu` | 768 | leading clearance 224px ±8px; side spaces differ by ≤8px; page moves 0px; every primary control ≥44px tall | pending |
| Q24 | `/events/[slug]/menu` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; page moves 0px; the list spans the column within 8px | pending |
| Q25 | `/dashboard` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; exactly 1 top-level heading (count = 1, tolerance 0); the two quick-action tiles ≥44px tall and equal in height within 4px | pending |
| Q26 | `/dashboard` | 768 | leading clearance 224px ±8px; side spaces differ by ≤8px; page moves 0px; each section card's padding 24px ±2px | pending |
| Q27 | `/dashboard` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; page moves 0px; each section card spans the column within 8px, padding 24px ±2px | pending |
| Q28 | `/events/[slug]` | 390 | leading clearance 0px ±4px; page moves 0px; bottom clearance ≥16px ±4px; the cover's leading and trailing inset 0px ±2px; back and share controls each ≥44px tall | pending |
| Q29 | `/events/[slug]` | 768 | leading clearance 224px ±8px; side spaces differ by ≤8px; page moves 0px; each lineup pill ≥44px tall | pending |
| Q30 | `/events/[slug]` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; page moves 0px; the cover reaches the column's edges with 0px ±2px of inset | pending |

---

## §4 — The navigation row, per surface, and why RESP-01's own words land here

**RESP-01 asks that every surface be usable *with the layout chosen for the device
the surface is actually worked on*.** After **D-41.2-01** that sentence is a
judgement about exactly this: all ten surfaces stopped presenting a phone bar on a
desktop and took the responsive form instead. **It is the largest visible change
this phase makes**, and the gate that guards it reads a class string on one side
and an import graph on the other — **it renders nothing.**

**The row asks what form the navigation takes at each width AND whether the content
clears it, in numbers**, because the two failures look nothing alike:

| Failure | The number | How it is discovered |
|---|---|---|
| content starts under the column | leading clearance **0px** at 768px and above | somebody reports it the same day |
| the clearance is reserved twice | leading clearance **448px**, not 224px | nobody reports it, because the page looks intentional |

| # | Route | What must be true | Result |
| --- | --- | --- | --- |
| M1 | `/` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M2 | `/newsletter` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M3 | `/artists/[slug]` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M4 | `/events` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M5 | `/attendance` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M6 | `/membership-card` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M7 | `/tickets/[id]` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M8 | `/events/[slug]/menu` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M9 | `/dashboard` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |
| M10 | `/events/[slug]` | at 390: a bar at the bottom edge, leading clearance 0px ±4px. At 768 and 1440: no bar, a column at the leading edge, leading clearance **224px ±8px — not 0px, not 448px**; at 1440 the column is still 224px ±8px and does not widen | pending |

> **The pre-41.2 surfaces' navigation is covered by O1, O8 and O7** — O7 is the
> eight-tab count at 768px, which is RESP-04's observable half and was never
> reported. No row is duplicated here for them.

---

## §5 — The touch-screen row, which has NEVER ONCE BEEN PRODUCED

**This is the only thing in this repository that would prove anything renders at
44px on a large touch screen, and it has never once been produced.** Not in Phase
41, not in Phase 41.1, not here.

- `verify:touch-targets` **estimates from a class string**. Its own header says it
  cannot see a flex stretch, an icon height, an ancestor's padding, or a class
  built by concatenation. A green from it is a statement about text.
- **A phone is not a substitute.** RESP-03's words are *large touch screens
  included*, and a tablet is precisely the device on which a layout designed for a
  mouse arrives and a finger cannot hit anything.
- **A headless render is not a substitute either.** `41-CR01-PASS.md` §1a says so
  in its own words about its own measured rows.

**Said in advance, so the row is not silently dropped on the day** — and this is
the same sentence `41-RELEASE-PASS.md` wrote in advance and then had to apply:

> **If no large touch screen is available, this row and O5 and O10 are recorded
> `human_needed` with the reason, and RESP-03 is NOT ticked. A silence about the
> device is not a statement that the device was there.** No green from
> `verify:touch-targets` substitutes for it, and this document does not claim it
> does. **`human_needed` is a legitimate value in the Result column; a tick is
> not.**

| # | Where | What must be true | Result |
| --- | --- | --- | --- |
| K1 | this phase's ten surfaces, on a **large touch screen** — a tablet, not a narrow phone | the **smallest** interactive box on each of the ten measures **≥44px in both axes as rendered**, with the one declared exception. Record the smallest box found, on which surface, and **how it was measured** — the device's own tooling if it has any, a thumb if it does not | pending |

> **RESP-03 is ticked by K1 together with O5 and O10, or it is not ticked.** K1
> alone covers this phase's ten; the other twenty-eight are O5's and O10's, and
> those two are the ones that have never been produced.

---

## §6 — The venue row — **Critical**

**The venue block on `/events/[slug]` shows the address to an entitled reader and
NOTHING to everybody else.** This row exercises that with **two real role
sessions**, and it must be two.

**Why no tool here can take it, and why an observation taken any other way would
prove the opposite of what it claims:**

- **No tool in this repository can authenticate as a role.** Not a gate, not a
  script, not the build.
- **The management path bypasses row-level security.** An observation taken through
  it would show what a **privileged** reader sees — on a surface whose entire
  question is what an **un**privileged one sees. It would look like evidence and be
  the inverse of it.
- **The middleware is UX and the row-level policies are the security boundary.** A
  redirect proves a reader did not *arrive*; it proves nothing about what a reader
  could *read*.

**And the reveal is monotone.** Once the mail has gone the address is public and
there is no undo — the mail has left, the screenshot exists. **This row reads the
guard. It does not operate it.**

| # | Where | What must be true | Result |
| --- | --- | --- | --- |
| V1 | `/events/[slug]`, for a night whose address is not yet revealed, read **twice**: once with a session entitled to see it and once with a session that is not | with the **unentitled** session: occurrences of the address on the page = **0**, tolerance 0 — in the visible text, in the page source, and in the social preview tags. With the **entitled** session: occurrences ≥ 1. **Record which capability each session held, never whose it was.** Change nothing: nights whose reveal flag changed during this row = **0**, tolerance 0 | pending |

> **V1 is Critical and its failure direction is asymmetric.** A `0` where a `1`
> belongs is an entitled reader who cannot find where they are going — annoying,
> and reversible. A `1` where a `0` belongs is **an address published to somebody
> not entitled to it**, and there is no version of that which is reversible. **If
> V1 cannot be taken with two real sessions, it is `human_needed` and it is not
> ticked** — the domain gate is not satisfied by an inference.

---

## §7 — Preconditions, read ON THE DAY

| # | What to record | Result |
| --- | --- | --- |
| W1 | run `npm run verify`: the verdict line verbatim, the exit code, and the counts of gates **passed / FAILED / REFUSED / not run**. Run `npm run build`: the exit code. **And which machine this was** — with or without `.env.local`, since exit 2 for want of credentials is the command working and not the tree failing | pending |
| W2 | the capability set of **each** of the four sessions §"accounts" names — including, for §6, that **two** sessions were available and which of them was the unentitled one. Where a session was unavailable, name every row that consequently reads `human_needed` | pending |

> **W2 is the precondition Phase 41 lost.** `41-RELEASE-PASS.md` §0.2 reads
> `human_needed` — *"the owner's approval did not state which capabilities the
> account held, or whether one was used at all"* — and the consequence landed on
> O7, whose count of eight tabs has no recorded capability set behind it. **A row
> taken by an unrecorded session is a row nobody can re-take.**

---

## §8 — What this document refuses to do

**It does not accept a blanket reply.** Fifty-seven rows, each ticked by its own
recorded observation or not ticked. A single word is a real authorisation to close
a **phase gate** — that is `41.2-RELEASE-PASS.md`'s business — and it is not a
measurement of fifty-seven things.

**It does not tick RESP-01 partially.** Either the union is filled in or the
requirement stays `PARTIAL`. Ticking it on this phase's ten would close it on ten
of thirty-eight surfaces.

**It does not tick RESP-03 without a device**, and it does not let a class-string
green stand in for one. See §5.

**It does not close the phase gate.** That is the other document's, and a reply
here does not carry across to it either. The two acts are separable in both
directions, on purpose.

**It claims nothing.** Fifty-seven rows, fifty-seven unrun procedures. The word
*approved* is an authorisation, the word *verified* is a claim about files, and the
word *observed* belongs to a person who has looked. **None of the three applies to
any row above.**

---

## Closing block

### What this pass creates in production: nothing

Every row opens a page and reads it. The rows that could have written are written
not to: **O11** opens the venue-reveal confirmation and presses Cancel; **O12**
opens a refund confirmation and cancels it, on its source's own instruction — *"Do
not complete a refund to test this"*; **V1** reads the venue block twice and
changes nothing.

**If any row ever finds itself needing to create a row, it has drifted.** This
project lost 63 production rows across seven tables to a verification script, with
no PITR, and authorisation to write to production is an act spent once and
recorded — never a standing permission.

### Nothing here reveals a venue

**V1 reads the guard from both sides and operates neither.** No row sends mail, and
none touches the reveal flag. **O11 opens the reveal confirmation and cancels it.**
The reveal is monotone: it can only go one way, and a pass that reads layouts has
no business making it go.

### No real credential is used, and no class string is spelled

No row asks for a key or a token to be written down anywhere. Per DEF-41-01 no
class string appears above: every criterion of this document's own rows is a
measurement in pixels or a count with a tolerance, and the clearance declaration is
cited at `src/app/(public)/gallery/page.tsx:110` rather than written out.

### Why not one of these fifty-seven was taken during execution

**D-41.2-04**, and it is a decision. No worktree in this phase held `.env.local`;
the middleware reads Supabase credentials on **every** request, so nothing renders
without them; supplying them would point a running application at production, which
this repository has incident D12 behind — 63 rows across seven tables, none
recovered, PITR not enabled. **The safe default was also the road that does not
touch production.** On `/dashboard` and `/membership-card` it would additionally
have meant rendering real members' personal data in order to look at a card.

**It is standing for this phase, not permanent.** If the owner authorises a run
later, that is a new authorisation, spent once, for what was described when it was
asked.

### Approved is not verified

Sixteen gates prove the **files**. Not one of them has seen a pixel, and **there is
no test runner in this repository** — nothing above is covered by a test, and
nothing above may be called verified because something passed.

**RESP-01 is ticked by fifty-seven filled-in Results and by nothing else.** Until
then it stays `PARTIAL`, and no VERIFICATION document, no green gate, no
`npm run verify` table, no build, and no approval on the other document may say
otherwise.

**Approved is not verified.**
