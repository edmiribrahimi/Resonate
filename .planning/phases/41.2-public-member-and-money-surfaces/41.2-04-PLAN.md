---
phase: 41.2-public-member-and-money-surfaces
plan: 04
type: execute
wave: 1
depends_on: ["41.2-01"]
files_modified:
  - src/app/(public)/artists/[slug]/page.tsx
  - src/components/artists/EditArtistButton.tsx
  - .planning/phases/41.2-public-member-and-money-surfaces/41.2-04-FINDINGS.md  # conditional: written only if this plan records a finding
autonomous: true
requirements: [DS-07, DS-08, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The phase's first dialog conversion lands on a surface carrying no money and no address"
    - "The artist surface mounts the responsive navigation and declares the tablet-tier clearance, as a pair"
    - "The paragraph at the artist page's reading measure is disposed of exactly as wave 0 decided, and the decision is applied rather than re-taken"
  artifacts:
    - path: "src/app/(public)/artists/[slug]/page.tsx"
      provides: "the public artist surface on PageShell at default width, responsive navigation mounted and its clearance declared"
      contains: "components/layout/AppNav"
    - path: "src/components/artists/EditArtistButton.tsx"
      provides: "the artist edit form on the Dialog primitive at the closed lg size, its nine raw controls on the primitives"
      contains: "components/ui/Dialog"
  key_links:
    - from: "src/components/artists/EditArtistButton.tsx"
      to: "src/components/ui/Dialog.tsx"
      via: "the form dialog at the size named on the primitive's own closed lg list"
      pattern: "size=\"lg\""
---


> **Findings go in this plan's own file, not in the shared one.** This plan may run
> in parallel with another plan in its wave, in a separate worktree. Two worktrees
> appending to one shared file is a merge conflict on an append-only document — the
> exact hazard the intra-wave overlap rule exists to prevent, and the previous phase
> avoided it only by luck (its one recorded finding came from a plan that happened to
> be alone in its wave). Write to `.planning/phases/41.2-public-member-and-money-surfaces/41.2-04-FINDINGS.md`, which this plan owns
> exclusively. **Plan 41.2-20 consolidates every `41.2-*-FINDINGS.md` into
> `deferred-items.md` in wave 8**, where nothing runs beside it.

<objective>
Convert the public artist surface and the edit dialog it mounts.

Purpose: this is **the phase's first dialog conversion**, and it lands on the
cheapest correct place for it — a surface with no money on it and no address on
it. `EditArtistButton.tsx` is the cheapest exact match in the whole phase: it and
`src/components/venues/EditVenueButton.tsx`, converted by plan 41.1-07, are the
same file with different nouns, carrying the same five class strings before the
conversion. It is also named by the primitive itself on the closed `lg` list, so
its size is checkable against the contract rather than argued.

It is also where the phase's **first own-maximum decision** is applied.
`41.2-RESEARCH.md` §1.6 does not list this page; the re-measurement in
`41.2-PATTERNS.md` §7.2 found a reading measure on a paragraph at `:126`. Without
plan 41.2-01's disposition this cheap surface would ship a check-D red on its
first declaration — which is exactly why the disposition was taken in wave 0 and
is only **applied** here.

Output: two files converted, one dialog off `REMAINING`, nine raw controls on the
primitives — the heaviest single contribution to the touch-target scope in the
phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). It declares no
   surface and it adds no `TYPOGRAPHIC_MEASURES` entry — the reconciliation plan
   41.2-05 does both, in one commit, on the authority of the tree. If wave 0's
   disposition for `:126` is *declare*, this plan leaves that line
   **byte-identical** and says so; if it is *delete*, this plan deletes it.
2. **41.2 does not edit the spine** — `layout/AppNav.tsx`, `layout/MobileNav.tsx`,
   `ui/Button.tsx`, `ui/Typography.tsx`. Needing to is a **HALT**.
3. **No server action is opened.** `src/app/(admin)/admin/artists/actions.ts` is
   in this closure and carries **zero** class strings: it is read to tell a
   rendering change from a payload change, and it is not edited. Needing to edit
   it is stop condition 2 — halt and report.
4. **No package is installed.**
5. **DEF-41-01** — never spell a bracketed arbitrary-value utility in prose. The
   clearance declaration is copied verbatim from
   `src/app/(public)/gallery/page.tsx:110`.
6. **`.planning/` is public.** No artist name, handle or contact in the SUMMARY —
   roles and routes only.
7. **No `npm run dev`, no headless render** (D-41.2-04).
8. **⚠ `scripts/verify-conversion.mjs` is invisible to the default `grep`.**
   `LC_ALL=C /usr/bin/grep` or the Read tool.
9. **macOS/BSD.** `grep -E`, `sed -i ''`.
10. **No test runner exists.**

**Quick run:** `npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`

**Clean-file greps** (matcher: `scripts/verify-conversion.mjs:850-916`):
raw palette `(bg|text|border|ring|from|to|via|fill|stroke|shadow|outline|decoration)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose|black|white)(-[0-9]{1,3})?`;
legacy token, same prefixes against `card-border|card|background|foreground`.

## The navigation pair — both halves, in one commit

**Half one — the mount.** Replace the phone-locked wrapper's import with a direct
import of `AppNav` and the wrapper element with `AppNav`, passing the **same four
props in the same order** — role, status, capability set, live-assignment
capability set. Verbatim analog: `src/app/(public)/gallery/page.tsx:2` and
`:125-132`. **Mounting means importing `AppNav` DIRECTLY**
(`scripts/verify-conversion.mjs:2803-2809`); reaching it through the wrapper does
not count.

**Half two — the declaration.** One `div` wrapping the **shell**, carrying the
single bracketed arbitrary-property utility that sets the inline-start navigation
inset at the `md` tier and no other. **Copy the line verbatim from
`src/app/(public)/gallery/page.tsx:110`.**

**Placement:** the declaring wrapper wraps the shell and the navigation is its
**sibling** (`gallery/page.tsx:110-132`); and the navigation moves **up a level**
out of the deleted page root, into a fragment (`gallery/page.tsx:88-133`).

**What it does not change** (UI-SPEC §0 rule 5): width may change layout, never
membership. The same four props go in, so no entry appears or disappears. That
sentence goes into the SUMMARY.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: The artist surface — shell, hero header, navigation pair, and the reading measure applied</name>
  <files>src/app/(public)/artists/[slug]/page.tsx</files>
  <read_first>
    - `src/app/(public)/artists/[slug]/page.tsx` — 183 lines, in full. Its root is at `:52`; its header differs from the phase's common shape because it carries a hero image; it writes a reading measure on a paragraph at `:126`; it mounts the phone-locked wrapper at `:4` and `:173`
    - **The analog, exact:** `src/app/(admin)/admin/(work)/venues/[slug]/page.tsx`, converted by plan 41.1-07 — the detail-page shape with a header that is not the plain three-line header
    - **The public navigation analog:** `src/app/(public)/gallery/page.tsx:88-133`
    - `src/components/ui/PageShell.tsx:146,160` and `src/components/ui/Typography.tsx:53`
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md` — **the disposition for `:126`, and if it is *declare*, the three parts of the entry plan 41.2-05 will write.** Apply it; do not take it
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.1, §3 rows 1, 2, 5, 7, 8, 10, 11, §4, §7.2
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §2, §4, §8.2
  </read_first>
  <action>
Adopt `PageShell` at `width="default"` — not on the closed `wide` list, and
`focus` is unavailable because the surface mounts a navigation. **`default` is an
answer, not a fallback.**

Apply the four-part deletion where it applies: the outer root goes to the shell,
the repeated horizontal padding goes with it, the header's bottom padding becomes
a margin. **The hero header is the divergence** — it is not the plain
title-only header the other surfaces carry, so keep its structure and convert only
its class strings and its heading element.

The heading becomes `PageTitle`. Card shells become `Card`. Interactive pills that
are links or buttons become `Chip`, which carries the 44px floor. Focus
suppression becomes the imported focus-ring expression, never re-spelled.

**The reading measure at `:126`.** Apply plan 41.2-01's disposition exactly:

- if the disposition is **delete**, delete it and say in the SUMMARY what the
  paragraph's measure now is — the shell's;
- if the disposition is **declare**, the line stays **byte-identical**, and the
  SUMMARY quotes it and names the fragment plan 41.2-05 will key the entry on.
  **Then check that your own conversion did not move that fragment.** A
  `TYPOGRAPHIC_MEASURES` entry requires the page file, the exact token **and** a
  fragment present on the same line, and a near-miss on any of the three refuses
  with *matches NOTHING: stale*. If the conversion changes anything else on that
  line, the entry planned in wave 0 will not match — **say so loudly in the
  SUMMARY rather than adjusting the entry from here**, because plan 41.2-05 owns
  the gate file.

Then apply **the navigation pair** — both halves, in this commit.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-no-viewport-read.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'layout/AppNav' 'src/app/(public)/artists/[slug]/page.tsx'` returns exactly **1**; `layout/MobileNav` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'nav-inset-inline-start' 'src/app/(public)/artists/[slug]/page.tsx'` returns exactly **1**, byte-identical to `src/app/(public)/gallery/page.tsx:110` — both lines pasted
    - The declaring `div` wraps the shell, the navigation is its sibling — quote the JSX
    - The shell width is `default` — quote the line
    - The four navigation props are the same four in the same order — quoted before and after; the SUMMARY carries *no capability check touched*
    - `LC_ALL=C /usr/bin/grep -c 'min-h-dvh' 'src/app/(public)/artists/[slug]/page.tsx'` returns **0**
    - `LC_ALL=C /usr/bin/grep -n 'max-w-' 'src/app/(public)/artists/[slug]/page.tsx'` returns the count the wave-0 disposition requires — **0 if delete, 1 if declare** — and the SUMMARY quotes the findings line that ordered it. If the disposition is *declare*, the whole line is pasted before and after and asserted byte-identical
    - The raw-palette grep returns **0**; the legacy-token grep returns **0**; `focus:outline-none`, `sm:`, `animate-pulse` each return **0**
    - `git status --porcelain -- 'src/app/(admin)/admin/artists/actions.ts'` outputs nothing — the action was read, not edited
    - `node scripts/verify-no-viewport-read.mjs` exits 0
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The artist surface is in the shell with the navigation pair, its hero header survived as a hero header, and its reading measure is in exactly the state wave 0 specified.</done>
</task>

<task type="auto">
  <name>Task 2: EditArtistButton — the form dialog, at the size the primitive already names</name>
  <files>src/components/artists/EditArtistButton.tsx</files>
  <read_first>
    - `src/components/artists/EditArtistButton.tsx` — 205 lines, in full. Its hand-rolled shell is at `:87` and `:91`, its heading at `:94`, its two status boxes at `:97-98` and `:102-103`. It carries **9 raw interactive elements** — five inputs, one textarea, three buttons — the heaviest single file in the touch-target census
    - **The analog, exact match — these two files are the same file with different nouns:** `src/components/venues/EditVenueButton.tsx`, converted by plan 41.1-07. Read `:214-256` (the whole converted call: `open`, `onClose`, `title`, `size`, `status` with its two tones, `actions` with the submit and the cancel), `:59` (the form identifier constant), and `:228-234` (the status region in a dialog)
    - `src/components/ui/Dialog.tsx:161-166` — **the closed `lg` list, which names this file.** So `size="lg"` here is checkable against the contract rather than argued
    - `src/components/ui/Dialog.tsx:66-77` (why a native dialog paints above the toast container), `:117-147` (the initial-focus contract), `:316-325` (the status region and its two roles), `:89` (actions sit below the scroller — which is why the buttons live outside the form and reach it through the form-owner attribute)
    - `src/components/ui/Input.tsx`, `src/components/ui/Textarea.tsx`, `src/components/ui/Button.tsx:215`
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.3 — the five lines this file carries and the verbatim replacement; §3 rows 4, 7, 8, 9
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §8.3 (the dialog and its closed size list), §11 (copy: *none introduced*)
    - `src/app/(admin)/admin/artists/actions.ts` — **read it, do not edit it.** Knowing what the action does is how you tell a rendering change from a payload change
  </read_first>
  <action>
Adopt `Dialog` at `size="lg"`, which is not a judgement: this file is named on the
primitive's own closed `lg` list at `src/components/ui/Dialog.tsx:161-166`.

The hand-rolled overlay, its panel and its own heading element go — the primitive
owns the shell and the `title` prop **is** the heading. The two bordered status
boxes become the `status` prop with its two tones, which renders the alert role in
the semantic tokens; the raw red and green palette utilities disappear with them.

**The buttons live outside the `<form>`**, in the actions region, reaching the
form through HTML's form-owner attribute — the primitive puts `actions` below the
scroller, so a control inside the form would scroll away. Declare the form
identifier as a constant, exactly as the analog does at
`EditVenueButton.tsx:59`.

**All nine raw controls take their primitive** — five `Input`, one `Textarea`,
three `Button` at their size rungs. None of them declares a minimum today, and
this file alone is the largest single contribution this phase makes to the
touch-target scope. Compare the result against plan 41.2-01's measured G5 number
for this file and state whether it matched.

**The dialog does not report through the toast.** A native dialog paints in the
top layer, above the toast container; a toast raised from inside one is raised
invisibly. `verify-dialogs` check C asserts this.

**The copy does not change** — not the title, not the labels, not the refusal
sentences. §11: *none introduced*. If a refusal sentence collapses two distinct
causes, record it as a finding in this plan's `41.2-04-FINDINGS.md` and carry it
forward; do not rewrite it.

**The action payload is untouched.** Read the action so you can tell a rendering
change from a payload change, then change nothing about what is sent. If the
conversion appears to need a payload change, **halt and return to the owner** —
stop condition 2.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-dialogs.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'components/ui/Dialog' src/components/artists/EditArtistButton.tsx` returns at least **1**, and `LC_ALL=C /usr/bin/grep -c 'size="lg"' src/components/artists/EditArtistButton.tsx` returns exactly **1**
    - `LC_ALL=C /usr/bin/grep -cE 'fixed inset-0|backdrop-blur|z-\[60\]' src/components/artists/EditArtistButton.tsx` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'useToast' src/components/artists/EditArtistButton.tsx` returns **0**
    - The raw-palette grep returns **0**; the legacy-token grep returns **0**; `focus:outline-none`, `sm:`, `max-w-`, `animate-pulse` each return **0**
    - `LC_ALL=C /usr/bin/grep -cE '<input|<textarea|<button' src/components/artists/EditArtistButton.tsx` returns **0** — all nine are primitives now; quote each replacement site
    - `node scripts/verify-touch-targets.mjs` exits 0, and the SUMMARY states this file's element count as the gate measured it and compares it with plan 41.2-01's dry-run figure for the same file
    - **The copy is byte-identical**: `git diff` on the file shows no line changing a user-visible string; the filtered diff is pasted
    - `git status --porcelain -- 'src/app/(admin)/admin/artists/actions.ts'` outputs nothing
    - `node scripts/verify-dialogs.mjs` exits 0 and reports this entry **STALE** rather than owed; the SUMMARY pastes that line. **The entry is not deleted here** — plan 41.2-05 deletes it on the authority of the tree
    - `git status --porcelain` shows exactly the two **source** files this plan declares, **plus, if a finding was recorded, this plan's `41.2-04-FINDINGS.md`** (the record is conditional, so its absence is also correct)
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The artist edit form is the platform's dialog at the size the contract already named for it, its nine controls are primitives at the floor, and its copy and payload are byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → artist record | this dialog writes a public-facing record; its refusal path must stay reachable and readable |
| page file → check E | the mount and the declaration mean nothing apart |
| page file → check D | any maximum in a declared page file is a failure unless it is a declared measure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-17 | Denial of service | content under a 224px column at 768px and above | mitigate | the pair lands in one commit; the declaration asserted byte-identical to `src/app/(public)/gallery/page.tsx:110`, the mount asserted a direct import |
| T-41.2-18 | Repudiation | a check-D red on the phase's cheap wave | mitigate | the `:126` disposition is applied from plan 41.2-01, and the plan asserts the resulting `max-w-` count against it. If the conversion moves the fragment the planned entry keys on, the SUMMARY says so instead of the plan editing a gate |
| T-41.2-19 | Tampering | an artist record's action payload | mitigate | the action is read and not edited; `git status` asserts it unmodified; a needed payload change is stop condition 2 and halts |
| T-41.2-20 | Repudiation | a refusal reported by a toast a native dialog paints over | mitigate | the refusal is the primitive's `status`; `useToast` asserted absent; `verify-dialogs` check C enforces it |
| T-41.2-21 | Elevation of privilege | a navigation entry changing with the width | mitigate | the four props quoted before and after; UI-SPEC §0 rule 5 restated in the SUMMARY |
| T-41.2-22 | Repudiation | a `REMAINING` entry deleted by the plan claiming to have paid it | mitigate | this plan deletes no entry; the gate reports STALE and exits 0, and plan 41.2-05 deletes on the authority of the tree |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run verify` exits 0 with no refusal
- `git status --porcelain` shows exactly two modified **source** files, plus this plan's `41.2-04-FINDINGS.md` if a finding was recorded
- The declaration line is pasted beside the gallery specimen and is byte-identical
- The `max-w-` count matches the wave-0 disposition, with the findings line quoted
- `verify-dialogs` reports the entry STALE and the line is pasted
</verification>

<success_criteria>
The public artist surface is converted whole and mounts the responsive navigation
with its clearance declared; the phase's first dialog conversion is on the
primitive at the size the contract already named; nine raw controls carry the
floor; and the phase's first own-maximum decision was applied rather than taken a
second time.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-04-SUMMARY.md`
when done, with the declaration line pasted against the gallery specimen, the
navigation prop block before and after, the `max-w-` state with the findings line
that ordered it, the nine control replacement sites, the byte-identical copy diff,
and the gate's STALE line.
</output>
