---
phase: 41.2-public-member-and-money-surfaces
plan: 05
wave: 2
subsystem: gates-and-reconciliation
tags: [reconciliation, conversion-manifest, typographic-measure, debt-lists, human-rows]
requires:
  - "41.2-03 — / and /newsletter, converted and reported"
  - "41.2-04 — /artists/[slug] and its edit dialog, converted and reported"
  - "41.2-01 — wave 0's dispositions, and the entry §7.3 spelled out"
provides:
  - "three declared surfaces, so check A walks their closures and G5 measures them for the first time"
  - "the project's first TYPOGRAPHIC_MEASURES entry, shipped in the same commit as its declaration"
  - "a thirteen-entry diff of both open debt lists against the tree, each row with a second instrument"
  - "41.2-HUMAN-ROWS.md — the phase's running ledger of human observations, every Result pending"
affects:
  - "41.2-19 — reads this SUMMARY's decision on the two STALE orphans, and inherits both halves of GuestLoginBanner's staleness"
  - "41.2-20 — consolidates 41.2-05-FINDINGS.md and turns 41.2-HUMAN-ROWS.md into the two pass documents"
tech-stack:
  added: []
  patterns:
    - "a debt entry is deleted only after the tree AND a second instrument that does not share the gates' comment stripper both agree it is paid"
    - "a declaration and the measure entry that forgives its width ship in one commit, never two"
key-files:
  created:
    - .planning/phases/41.2-public-member-and-money-surfaces/41.2-HUMAN-ROWS.md
    - .planning/phases/41.2-public-member-and-money-surfaces/41.2-05-FINDINGS.md
  modified:
    - scripts/conversion-manifest.mjs
    - scripts/verify-conversion.mjs
    - scripts/verify-dialogs.mjs
decisions:
  - "D-41.2-05-A — GuestLoginBanner.tsx and RefundRequestButton.tsx stay on their lists until 41.2-19, because a deletion belongs in the same wave that writes the gate's closure decision. NOT because deleting them would empty a list: it would not, and both lists stay non-empty either way"
  - "D-41.2-05-B — the TYPOGRAPHIC_MEASURES entry was written here and not in 41.2-04, on D-41.1-22, and it landed in the same commit as the CONVERTED entry, on D-41-16"
  - "D-41.2-05-C — no breakpoints entry was touched, and the zero movement is proved by a byte-identical gate output rather than asserted"
  - "D-41.2-05-D — the rung for a filled internal-navigation action was NOT built: the count is two, the trigger is three, and a new rung is a spine edit, which is a halt"
metrics:
  tasks: 3
  commits: 4
  duration: one session
  completed: 2026-08-14
---

# Phase 41.2 Plan 05: The First Reconciliation — Summary

Three surfaces are declared on the authority of the tree, the project's first
typographic measure ships in the same commit as the declaration that needs it,
**one** debt entry is deleted because two instruments agreed it was paid, and the
phase's human debt is written down as numbers with tolerances rather than as
adjectives.

---

## 1. The gate numbers, before and after

| Gate | Before | After | Verdict |
|---|---|---|---|
| `verify-conversion` — surfaces declared | 28 | **31** | exit 0 |
| — files reached by the walk | 158 | **163** | |
| — files scanned by A, B and D | 157 | **162** | |
| — route-adjacent files added | 9 | **9** | unmoved |
| — typographic measures | `declared: 0, applied 0 time(s)` | **`declared: 1, applied 1 time(s)`** | |
| — check E, the pairing | 5 declaring / 5 mounting | **5 / 5 — unmoved** | see §4 |
| — check E, the route table | 24 of 28 mount a navigation | **27 of 31** | |
| `verify-dialogs` — `REMAINING` declared | 8 | **7** | exit 0 |
| — `REMAINING` measured | 5 | **5 — unmoved** | |
| — entries printed STALE | 3 | **2** | deliberate, §5 |
| `verify-breakpoints` | 5 declared / 16 declared uses; 4 files / 13 uses measured | **identical, byte for byte** | exit 0 |
| `verify-tables` | `REMAINING = 0`, `MIGRATION_CLOSED` set | **untouched** | exit 0 |
| `verify-touch-targets` | 28 surfaces, 115 JSX files, 36 measured across 77 | **31 surfaces, 120 JSX files, 41 measured across 82** | `PASSED` |
| `grep -c 'no query changed'` on the manifest | **13** | **16** | +3 |

**The scanned-file count rose by 5, not by 3, and here is how that is derived.**
The three closures are `/` — 12 files, `/newsletter` — 14, `/artists/[slug]` — 18,
and almost all of what each reaches was already scanned for another surface (the
primitives, the capability modules, the Supabase clients). The five files that
were **not** already in the scanned set are the three page files plus
`src/components/newsletter/NewsletterForm.tsx` and
`src/components/artists/EditArtistButton.tsx` — the two components the two new
non-trivial closures reach and nothing else declared did. **Route-adjacent stayed
at 9 because there is none to add:** `ls` on the three route directories returns
`page.tsx` and nothing else — a measured zero, listed rather than assumed.

---

## 2. The `TYPOGRAPHIC_MEASURES` entry, verbatim

Written into `scripts/verify-conversion.mjs` in **the same commit** as the
`CONVERTED` entry for `/artists/[slug]` — commit `19749ae` — per D-41-16:

```js
  [
    'src/app/(public)/artists/[slug]/page.tsx',
    'max-w-lg',
    'whitespace-pre-line',
    "the artist bio is prose and reads at a measure; the shell's 1024px is a line length nobody finishes",
  ],
```

Its three parts are wave 0 §7.3's, unchanged. What this plan added was the check
wave 0 could not make: **the line moved.** `41.2-04-FINDINGS.md` F-41.2-04-02
records that the conversion pushed it from `:126` to `:229` and deepened its
indentation by two spaces, so the line was **re-read on the converted tree before
the entry was written** rather than trusted from a line number in a document:

```
$ LC_ALL=C /usr/bin/grep -n 'max-w-' 'src/app/(public)/artists/[slug]/page.tsx'
229:              <p className="mt-6 max-w-lg text-muted whitespace-pre-line text-left">
```

One `max-w-lg`, one `whitespace-pre-line`, both on that line. The only other
occurrence of the fragment in the file is inside a JSX comment at `:222`, which
the shared stripper blanks — so the entry can match neither zero times (stale) nor
twice (ambiguous).

**The gate's own line, pasted:**

```
      surfaces whose width was compared against §4 : 31
      typographic measures declared : 1, applied 1 time(s)   (D-41.1-27)

  ! D  1 width(s) forgiven as a DECLARED typographic measure:

       src/app/(public)/artists/[slug]/page.tsx:229   max-w-lg     (/artists/[slug])
         the artist bio is prose and reads at a measure; the shell's 1024px is a line length
         nobody finishes

       Not a failure and not a narrowing: a measure is a property of a block of TEXT,
       which no tier owns, and it is forgiven here only because a person wrote the line
       above. Every width NOT on that list still fails, including one on a paragraph.

  ✓ D  the shell declares §4's three maxima and only those; all 31 converted
       page(s) import it, write no undeclared maximum of their own, and carry the width
       §4 assigns
```

**M is 1.** No fragment was adjusted to make it match, and none needed to be.

---

## 3. Check D green across all five own-maximum occurrences

Wave 0 §7.2 measured five occurrences in four files, one disposition each. All
five are now discharged, four by deletion and one by declaration:

| # | File | Disposition | State today | By |
|---|---|---|---|---|
| 1 | `src/app/page.tsx:46` | DELETE | gone — `grep -c 'max-w-'` returns **0** | plan 41.2-03 |
| 2 | `(public)/artists/[slug]/page.tsx:126` → `:229` | **DECLARE** | **declared here**, forgiven once | this plan |
| 3 | `(public)/tickets/[id]/page.tsx:108` | DELETE | **still standing — surface not yet declared** | a later wave |
| 4 | `(public)/tickets/[id]/page.tsx:191` | DELETE | **still standing — surface not yet declared** | a later wave |
| 5 | `(public)/events/[slug]/menu/page.tsx:163` | DELETE | **still standing — surface not yet declared** | a later wave |

**`pagesWithOwnMaximum` names no page file, and that is the honest reading of it:**
rows 3–5 are invisible to check D because their surfaces are not in `CONVERTED`
yet, not because they were paid. The gate's line, pasted:

```
  ✓ D  the shell declares §4's three maxima and only those; all 31 converted
       page(s) import it, write no undeclared maximum of their own, and carry the width
       §4 assigns
```

Of the **five occurrences wave 0 dispositioned, one was deleted and one declared —
the other three belong to surfaces this wave did not declare.** Stating that as
"four deleted, one declared" would be a claim about work nobody has done.

---

## 4. Check E — both sets, pasted in full, and a correction

```
          bracketed declaration(s) of the inline axis found : 5   (264 file(s) read, comments blanked)
              src/app/(admin)/admin/(work)/layout.tsx:176   md:[…]
              src/app/(public)/artists/[slug]/page.tsx:135  md:[…]
              src/app/(public)/gallery/page.tsx:110         md:[…]
              src/app/(public)/newsletter/page.tsx:55       md:[…]
              src/app/page.tsx:108                          md:[…]
          file(s) DECLARING the column clearance            : 5
              src/app/(admin)/admin/(work)/layout.tsx
              src/app/(public)/artists/[slug]/page.tsx
              src/app/(public)/gallery/page.tsx
              src/app/(public)/newsletter/page.tsx
              src/app/page.tsx
          file(s) MOUNTING the responsive form              : 5   (import AppNav.tsx directly, and are not the phone-locked wrapper)
              src/app/(admin)/admin/(work)/layout.tsx
              src/app/(public)/artists/[slug]/page.tsx
              src/app/(public)/gallery/page.tsx
              src/app/(public)/newsletter/page.tsx
              src/app/page.tsx
```

*(The declared utility is elided as `md:[…]` in this document and nowhere else —
DEF-41-01: Tailwind's source detection scans `.planning/`. The value is at
`src/app/(public)/gallery/page.tsx:110`, which is the line the gate itself matched.)*

**Both difference lists are empty, asserted rather than inferred.** The gate emits
a `✗ E` line for each direction when either is non-empty
(`verify-conversion.mjs:3673` and `:3697`); neither appears in the run:

```
$ LC_ALL=C /usr/bin/grep -c '✗ E  .* file(s) DECLARE the column clearance' <run>   → 0
$ LC_ALL=C /usr/bin/grep -c '✗ E  .* file(s) MOUNT the responsive navigation' <run> → 0
```

and the equality is printed as a tick:

```
  ✓ E  … and the 5 file(s) declaring the column clearance at the md tier are
       EXACTLY the 5 file(s) mounting the responsive navigation form
```

**The correction, and it matters to seven later plans.** The three files were in
**both** sets **before this plan's first edit**. The pairing is computed from two
tree-wide reads and never consults `CONVERTED`, so it moved 2/2 → 5/5 in **wave
1's own commits**, and this plan could not have moved it. The figures a
declaration actually moves are the route-table partition (24 of 28 → **27 of 31**)
and the width-against-§4 comparison (28 → **31**). A reconciliation that reads its
own pairing number as evidence its declaration landed is crediting a counter to
the wrong cause — the debt-goes-quiet shape one step removed. **F-41.2-05-01.**

---

## 5. The thirteen-entry diff, with the second instrument

**Diffed against the tree, never against wave 1's counts.** Every entry on both
open lists was re-derived — not only the one expected to move.

**The two instruments, and why the second one is the point.** The first is the
gates' own matcher reading through the **shared comment stripper**
(`scripts/lib/comments.mjs`), which is what `verify-dialogs` and
`verify-breakpoints` see. The second is a **raw needle count over the un-stripped
file** — `LC_ALL=C /usr/bin/grep` chained for all three parts of the hand-rolled
shell on one line, for the native shell, and for the migrating prefix — sharing no
line of code with either gate. Round 5 of this phase family measured that stripper
going blind inside a file while the gate printed *converted; remove this entry* and
exited 0, which is why asking the gate what it thinks is not a check on the gate.

### 5.1 `verify-dialogs` — all eight entries

| # | Entry | Gate's verdict | What the tree says | Second instrument (raw) |
|---|---|---|---|---|
| 1 | `(public)/tickets/[id]/RefundRequestButton.tsx` | **STALE** — *converted; remove this entry* | converted by plan 41.2-02; no shell | overlay **0**, native **0** — agrees |
| 2 | `(public)/events/[slug]/RedeemConfirmationModal.tsx` | carries a shell | unconverted, still owed | overlay **3**, native **0** — agrees |
| 3 | `(public)/events/[slug]/SumUpCheckoutModal.tsx` | carries a shell | unconverted, still owed | overlay **1**, native **0** — agrees |
| 4 | `(public)/events/[slug]/SecretVenueDialog.tsx` | carries a shell | unconverted, still owed | overlay **1**, native **0** — agrees |
| 5 | `(public)/events/[slug]/menu/GuestLoginBanner.tsx` | **STALE** | converted by plan 41.2-02; no shell | overlay **0**, native **0** — agrees |
| 6 | `(public)/events/[slug]/menu/GuestTokenDisplay.tsx` | carries a shell | unconverted, still owed | overlay **3**, native **0** — agrees |
| 7 | **`components/artists/EditArtistButton.tsx`** | **STALE** | **converted by plan 41.2-04 task 2; mounts the primitive — `grep -c 'components/ui/Dialog'` → 1** | overlay **0**, native **0** — agrees → **DELETED** |
| 8 | `components/media/MyMediaSection.tsx` (`ROLE_DIALOG_OVERLAY`) | carries a shell | unconverted, still owed | overlay **1**, native **0** — agrees |

### 5.2 `verify-breakpoints` — all five entries

| # | Entry | Declared | Gate measured (stripped) | Second instrument (raw) | What the tree says |
|---|---|---|---|---|---|
| 9 | `components/media/MediaUpload.tsx` | 1 | **1** | **1** — agrees | still owed |
| 10 | `(public)/events/[slug]/RedeemConfirmationModal.tsx` | 4 | **4** | **4** — agrees | still owed |
| 11 | `(public)/events/[slug]/SumUpCheckoutModal.tsx` | 4 | **4** | **4** — agrees | still owed |
| 12 | `(public)/events/[slug]/menu/GuestLoginBanner.tsx` | 3 | **0 — STALE** | **0** — agrees | converted by 41.2-02; **left standing, §6** |
| 13 | `(public)/events/[slug]/menu/GuestTokenDisplay.tsx` | 4 | **4** | **4** — agrees | still owed |

**Thirteen rows, thirteen agreements.** **No row disagreed in either direction**,
so no row needed the explanatory sentence, and — the load-bearing half — **no row
had a raw count LOWER than its stripped count**, which is the direction that would
have stopped a deletion. The instrument is not vacuously zero: it returned 3, 1, 1,
3 and 1 on the five live overlays and 1, 4, 4, 4 on the four live prefix files.

**Nothing on the breakpoints list moved, and that is a measured zero.** `diff` on
the gate's full output before and after this plan's edits **exits 0** — the run is
byte-identical. The declared 5 / 16 is intact.

**One thing the tree says that no wave-1 SUMMARY does.** Wave 0's control recorded
the breakpoints gate at *"16 declared, 16 measured"*; today it measures **13**, and
the mover is plan **41.2-02**, not wave 1. Read as a wave-1 regression it would be
a false alarm; read as a stale entry it is exactly what row 12 above says it is.
**F-41.2-05-03.**

### 5.3 The three negative obligations

- **No entry was added**, to either list.
- **No entry was moved between groups.** The dialogs tag balance went
  `public-member-money` 8 → 7 and `work` 0 → 0.
- **The declared permanent exemption was not tidied onto any list**, asserted by
  grep **after** the edit, because the failure mode here is a tidy rather than a
  decision:

```
$ LC_ALL=C /usr/bin/grep -n 'Lightbox' scripts/verify-dialogs.mjs
115: *   1. `src/components/media/Lightbox.tsx` — **exempt from check B, for good.**
466:export const FULL_BLEED_VIEWER = 'src/components/media/Lightbox.tsx';
501: * declared entries — while `Lightbox.tsx:82` still carries a native shell.
1351: *   - `exempt`               — declared correct as what it is. `Lightbox.tsx` is

$ LC_ALL=C /usr/bin/grep -n 'Lightbox' scripts/verify-breakpoints.mjs scripts/verify-tables.mjs scripts/conversion-manifest.mjs
                                        ← no output, exit 1
```

**Four hits, all in the exemption constants and the prose explaining them: the
declaration at `:466`, and three sentences about it.** Not one is a `REMAINING`
entry, and it appears on no other debt list at all.

---

## 6. The two STALE orphans stay, and it is a decision

`GuestLoginBanner.tsx` and `RefundRequestButton.tsx` are converted, and both gates
print them STALE. **They were not deleted here.**

**The reason is not that deleting them would empty a list.** It would not:
`verify-dialogs`' list would go 7 → 5 and `verify-breakpoints`' 5 → 4, both
non-empty, so neither emptiness refusal — `verify-dialogs.mjs:1471`,
`verify-breakpoints.mjs:661` — would fire and nothing would break.

**The reason is that a deletion belongs in the same wave that writes the gate's
closure decision**, which is plan 41.2-19. Split across two waves, a deletion is a
tidy-up that happens to shrink a counter; kept beside `MIGRATION_CLOSED`, it is an
answer with its reasoning next to it. This repository has **four recorded
recurrences** of a debt that went quiet because a counter fell for a reason nobody
wrote down, and the defence is that **a number only ever moves in the same commit
as the sentence explaining why**.

**For 41.2-19, so it is not re-derived:** `GuestLoginBanner.tsx` is stale on
**both** gates — a dialogs entry and a breakpoints entry of 3 declared against 0
measured — and both halves belong in that commit.

---

## 7. The human-row ledger

`41.2-HUMAN-ROWS.md` is a **running ledger**, not a pass document: each
reconciliation wave appends its own section, and plan 41.2-20 turns the
accumulated rows into the two documents of D-41.2-05.

**Twelve rows opened**: nine width rows — `/`, `/newsletter` and `/artists/[slug]`
at **390 / 768 / 1440**, the same three numbers `41-CR01-PASS.md` used — plus three
dialog rows for `/artists/[slug]`.

**The two counts, asserted equal:**

```
$ LC_ALL=C /usr/bin/grep -c '^| [0-9]'                   → 12    ← Result cells (data rows)
$ LC_ALL=C /usr/bin/grep -c '^| [0-9].*| pending |$'     → 12    ← Result cells reading pending
$ LC_ALL=C /usr/bin/grep -c 'pending'                    → 12
```

**12 = 12.** Every Result cell is unrun.

**Three rows quoted as evidence that each criterion is a number with a tolerance,
not an adjective:**

> | 3 | `/` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px wide; the space each side of it differs by ≤8px; page moves 0px when dragged sideways; the three action controls are as wide as the longest of the three within 4px, NOT 320px and NOT the full column | pending |

> | 9 | `/artists/[slug]` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; **the bio paragraph is 512px ±8px wide — half the column, which is the whole content of the declared typographic measure**; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways | pending |

> | 12 | `/artists/[slug]` | 390 | with the edit panel open, scroll the page behind it by three wheel notches or one full finger drag: the content behind moves **0px**, tolerance 0. Then dismiss with Escape: visible panels after = 0, tolerance 0 | pending |

**The header states, in its own words, that no row was measured during execution
because D-41.2-04 forbids this worktree running the application, and that
*deferred is not verified*.** The batch is named: the end-of-v1.5 human sitting,
the one `41-RELEASE-PASS.md`'s `batch:` field already commits alongside
`40-RELEASE-PASS.md`, `39-DOOR-PASS.md` and `38-PROCEDURES.md`.

---

## 8. What the tree said that a wave-1 SUMMARY did not

1. **G5 confirmed plan 41.2-04's largest contribution, which that plan could only
   claim from source.** F-41.2-04-05 states it plainly: the gate never opened
   `EditArtistButton.tsx`, because `/artists/[slug]` was not declared. It is now.
   Wave 0's dry run measured **12** elements on that surface and **all** of them
   red, nine of them in that one file — the heaviest in the phase's census. The
   gate, with the surface declared:

   ```
   /                           13 file(s) reached,   8 element(s) found,   3 measured,   5 exempt
   /newsletter                 15 file(s) reached,  10 element(s) found,   0 measured,  10 exempt
   /artists/[slug]             19 file(s) reached,  21 element(s) found,   2 measured,  19 exempt

   measured 41 element(s) across 82 file(s).
   PASSED — every measured element declares an unprefixed minimum of at least 44px
   ```

   **Zero failures.** The nine raw controls are gone into primitives and land as
   exemption 2b — a primitive rendered at a call site — which is why the file's
   `found` is high and its `measured` is not. That is the gate's confirmation
   arriving exactly where F-41.2-04-05 predicted it would.

2. **The breakpoints gate's measured figure fell from 16 to 13 before this wave**,
   moved by plan 41.2-02 and reported by nobody. F-41.2-05-03.

3. **Wave 0's spine-exclusion row is wrong in its baseline cell** — it records 13
   where the gate prints 9, on an unchanged gate over an unchanged closure, and its
   Δ of 0 across ten added surfaces is the tell. F-41.2-05-02.

4. **Check E's pairing was already equal before this plan ran.** §4 above, and
   F-41.2-05-01.

---

## 9. Deviations from Plan

### 1. [Rule 3 — blocking, self-inflicted, caught before staging] A whole-file substitution edited two entries this plan does not own

- **Found during:** Task 1, reading `git diff` before staging.
- **Issue:** the acceptance criterion asks that `grep -c 'no query changed'` rise by
  at least 3; the three new entries had the sentence capitalised, so the count did
  not move. A `sed` over the file matched **two pre-existing entries as well** —
  `/admin/artists` and `/admin/newsletter`, both plan 41.1-06's — rewriting reason
  text this plan has no authority over. The block this plan appends to forbids
  exactly that: *"an editorial fix to somebody else's sentence is exactly the move
  this block's opening paragraph refuses."*
- **Fix:** a second `sed` restricted to lines naming that plan restored both.
  `git diff --stat` on the manifest reports **57 insertions, 0 deletions** before
  anything was staged, and the diff carries **no `-` line**.
- **Recorded:** F-41.2-05-04, because the near-miss is the interesting part.
- **Commit:** `19749ae`

### 2. [Refused] Building the rung for a filled internal-navigation action

- **Asked implicitly by** F-41.2-03-04's *"a third occurrence is the case for a
  rung, not a third copy"*, and by the execution brief's invitation to judge
  whether it belongs here.
- **Not done, and the count is why.** Re-derived from the tree, the accent-filled
  `<Link>` on a converted surface exists **twice** — `src/app/page.tsx:144` and
  `src/app/(admin)/admin/(work)/events/page.tsx:193`. The trigger the finding sets
  is a **third**. Building on two is deciding on an argument rather than on a
  measurement.
- **And it is a halt either way:** a new rung is an edit to `Button.tsx` or
  `Chip.tsx`, and **41.2 does not edit the spine.**
- **Recorded:** F-41.2-05-05, routed to whichever plan next needs one.

### 3. [Recorded] The plan's breakpoints criterion is true of the declared figures and false of the measured ones

- *"5 files / 16 uses — unchanged"* holds for `REMAINING entries declared : 5` and
  `sm: uses declared : 16`, both intact. The gate **measures** 4 files / 13 uses,
  and has since plan 41.2-02. Stated rather than rounded to the criterion's
  wording. F-41.2-05-03.

### 4. [Recorded] Check D's five occurrences are not "four deleted, one declared"

- One deleted, one declared, **three still standing on surfaces this wave did not
  declare**. §3 above. Writing four-and-one would claim work nobody has done.

### 5. No architectural change, no package, no spine, no server action

- **Zero packages** proposed or installed (T-41.2-SC).
- **No spine file opened.** `AppNav.tsx`, `MobileNav.tsx`, `Button.tsx` and
  `Typography.tsx` were neither read for edit nor modified;
  `git status --porcelain` never showed one.
- **No server action opened.** No file under `src/` was modified at all: this
  plan's three source-tree commits touch only `scripts/` and `.planning/`.
- **No Supabase read or write, no migration, no dev server, no headless render**
  (D-41.2-04).
- **No `STATE.md`, no `ROADMAP.md`** — the orchestrator's.

---

## 10. Threat register — dispositions discharged

| Threat ID | Disposition | How it was discharged |
|---|---|---|
| T-41.2-23 | mitigate | **All thirteen** entries across both open lists re-derived from the tree and tabulated in §5 before any deletion, each with a **second instrument that does not share the gates' comment stripper**. Thirteen agreements, no row with a raw count below its stripped count, and the one deletion made only where both instruments read zero |
| T-41.2-24 | mitigate | Check A walked all three new closures the moment the entries landed — 162 files scanned against 157 — and the route-adjacent extension was checked by listing the three route directories rather than assumed: `page.tsx` and nothing else |
| T-41.2-25 | mitigate | Both sets pasted in full from the gate's own output in §4, and both difference lists asserted empty by grepping for the two `✗ E` lines the gate emits when either is non-empty. Both returned **0** |
| T-41.2-26 | mitigate | The permanent exemption's absence from every debt list asserted by grep **after** the edit — §5.3. Four hits, all in the exemption constants and their prose |
| T-41.2-27 | mitigate | The two STALE orphans deliberately left, with the reason written as a **decision** in §6 and the false reason explicitly refused |
| T-41.2-28 | mitigate | Every Result written `pending`; Result-cell count **12** asserted equal to the `pending` count **12**; every criterion a number with a tolerance, three quoted in §7 as evidence |
| T-41.2-SC | mitigate | Zero packages proposed, zero installed |

**Threat flags:** none. No file under `src/` was modified. No network endpoint,
auth path, file-access pattern or schema touch was introduced anywhere.

---

## 11. Known Stubs

None. Nothing was wired to an empty array, a placeholder or a mock. The one list
that is deliberately incomplete — `41.2-HUMAN-ROWS.md`'s Results — is incomplete
**by design and says so**: twelve unrun procedures, and the header states why no
row could be measured here.

---

## 12. Verification

**There is no test runner in this repository.** Nothing below is verified because
tests pass; each line is a command with its exit code, a `grep` with its count, or
a source assertion.

```
$ npm run build
build exit=0        ← run twice, once after task 1 and once after task 2
```

```
$ node scripts/verify-conversion.mjs      → 0    31 surfaces, 162 files, all five checks
$ node scripts/verify-dialogs.mjs         → 0    REMAINING declared 7, measured 5
$ node scripts/verify-breakpoints.mjs     → 0    output byte-identical to before
$ node scripts/verify-tables.mjs          → 0    REMAINING = 0, MIGRATION_CLOSED set
$ node scripts/verify-touch-targets.mjs   → 0    41 measured across 82, PASSED
```

**`npm run verify` — the expected verdict on a worktree, quoted from the tail:**

```
    package.json declares           17  verify:* entr(y/ies)
    run here                        16
      of which passed               15
      of which FAILED                0
      of which REFUSED               1  — nothing was measured by these
    needs a server, not run          1
    declared absent                  0
    MISSING                          0
                                   ───
    accounted for                   17

  ═══ verify:capabilities — REFUSED (exit 2) ═══
FATAL: missing environment variable(s): SUPABASE_ACCESS_TOKEN, NEXT_PUBLIC_SUPABASE_URL.
Set them in .env.local (gitignored) or in the environment. Nothing was measured.
```

**15 gates reached a verdict, 0 failed, and `verify:capabilities` refused for want
of `.env.local`.** The suite exits 2, and that is its honest state here rather
than a failure of this work. **It was not made green** — no credentials added, no
gate skipped, no criterion reworded. Supplying those credentials would point a
gate at production, which **D-41.2-04 forbids** and which this repository has an
incident behind: **D12**, 63 rows deleted across seven tables, none recovered,
PITR not enabled. The full 16/16 suite is the orchestrator's, in the main
checkout, after this wave merges.

---

## 13. What this plan did NOT measure, said plainly

- **Nothing rendered.** No dev server, no headless render, no box measured. Every
  figure here is a class string, an import clause, a declaration or a gate's own
  printed line.
- **`typographic measures declared: 1, applied 1` says a class string was found on
  a line.** It does not say the bio paragraph reads at 512px on a screen. That is
  row 9 of the ledger, and it is unrun.
- **`PASSED` on 41 measured elements says nothing renders at 44px.** H41-4 is the
  only thing that would, and it has still never been produced.
- **The capability model is unverified on this checkout**, in every run above — an
  environment fact reported, not a gap filed.

---

## Commits

| Task | Commit | Subject |
|---|---|---|
| 1 | `19749ae` | `feat(41.2-05)` — three surfaces declared, and the project's first typographic measure |
| 2 | `8a467b0` | `chore(41.2-05)` — one entry deleted because the tree changed, thirteen checked |
| 3 | `3943f02` | `docs(41.2-05)` — the human-row ledger, twelve unrun procedures |
| — | *(this commit)* | `docs(41.2-05)` — the summary and the findings |

---

## Self-Check: PASSED

Files claimed created or modified, checked on disk:

```
FOUND: scripts/conversion-manifest.mjs
FOUND: scripts/verify-conversion.mjs
FOUND: scripts/verify-dialogs.mjs
FOUND: .planning/phases/41.2-public-member-and-money-surfaces/41.2-HUMAN-ROWS.md
FOUND: .planning/phases/41.2-public-member-and-money-surfaces/41.2-05-FINDINGS.md
```

Commits claimed, checked in the log:

```
FOUND: 19749ae  feat(41.2-05): tre superfici dichiarate, e la prima misura tipografica del progetto
FOUND: 8a467b0  chore(41.2-05): una voce cancellata perche' l'albero e' cambiato, tredici verificate
FOUND: 3943f02  docs(41.2-05): il registro delle righe umane, aperto con dodici procedure non eseguite
```

---

*Plan 41.2-05, executed 2026-08-14 on base commit `5204d8d`, in an isolated
worktree. `STATE.md` and `ROADMAP.md` are the orchestrator's and were not touched.
Contains no venue under negotiation, no unannounced date, no line-up and no
personal name: `.planning/` is tracked and this repository is public.*
