---
phase: 41.2-public-member-and-money-surfaces
plan: 09
wave: 4
subsystem: gates-and-reconciliation
tags: [reconciliation, conversion-manifest, debt-lists, human-rows, second-instrument, check-e]
requirements: [DS-07, DS-08, RESP-01, RESP-02, RESP-03]
requires:
  - "41.2-06 — /events converted and reported"
  - "41.2-07 — /attendance and /membership-card converted and reported"
  - "41.2-08 — /tickets/[id] converted and reported"
  - "41.2-05 — the first reconciliation, whose findings are this plan's most important input"
provides:
  - "four declared surfaces, so checks A, B and D walk their closures for the first time — 31 → 35"
  - "a twelve-entry diff of both open debt lists against the tree, each row with a second instrument"
  - "the zero movement of both ratchets proved per file rather than asserted"
  - "the own-maximum picture re-measured: three standing occurrences are now one"
  - "thirteen more human rows, every Result pending — the ledger holds 25"
affects:
  - "41.2-19 — reads the decision on the two STALE orphans, and inherits the rule that a zero from the second instrument counts only if the run printed its self-check"
  - "41.2-20 — consolidates 41.2-09-FINDINGS.md and settles the ledger's per-wave totals once"
  - "whichever plan declares /events/[slug]/menu — it inherits a check-D red unless the own-maximum line goes in the same commit"
tech-stack:
  added: []
  patterns:
    - "a second instrument REFUSES unless it first reproduces known answers on two fixtures; a bare zero is a measurement that may not have happened"
    - "an expected gate figure written before a wave ran is a prediction, because the wave's worktrees cannot see each other; the tree is the measurement"
    - "a declaration is proved by the declared-surface count and the scanned-file set, never by check E's pairing"
key-files:
  created:
    - .planning/phases/41.2-public-member-and-money-surfaces/41.2-09-FINDINGS.md
    - .planning/phases/41.2-public-member-and-money-surfaces/41.2-09-SUMMARY.md
  modified:
    - scripts/conversion-manifest.mjs
    - .planning/phases/41.2-public-member-and-money-surfaces/41.2-HUMAN-ROWS.md
decisions:
  - "D-41.2-09-A — the two STALE orphan entries stay until 41.2-19, restated as a choice: a deletion belongs in the same wave that writes the gate's closure decision, NOT because deleting them would empty a list — it would not"
  - "D-41.2-09-B — the plan's expected pairing of seven was not met and not made to be met: the tree says nine, the gate is green, and the divergence is recorded rather than reconciled by adjusting an expectation"
  - "D-41.2-09-C — wave 2's stale ledger sentence was left standing and the correct total stated in a new section, rather than rewritten — the rule that F-41.2-05-04's near-miss exists to enforce"
  - "D-41.2-09-D — the second instrument gained a mandatory self-check after returning a vacuous zero on its first run; a zero it prints without one is not evidence"
metrics:
  tasks: 2
  commits: 3
  duration: one session
  completed: 2026-08-14
---

# Phase 41.2 Plan 09: The Second Reconciliation — Summary

Four surfaces are declared on the authority of the tree, **twelve debt entries
are re-derived in a wave that was expected to move none of them and moved none
of them — proved rather than asserted** — and the instrument that proves it was
caught returning a vacuous zero before it was believed.

**This is the wave that deleted nothing, and the work was proving it.**

---

## 1. The gate numbers, before and after

| Figure | Before | After | Verdict |
|---|---|---|---|
| `verify-conversion` — **surfaces declared** | 28→31 | **35** | exit 0 |
| — **files scanned by checks A, B and D** | 162 | **173** | +11, derived in §2 |
| — route-adjacent files added | 9 | **11** | +2, both listed |
| — excluded as converted spine | 9 | **10** | +1, F-41.2-09-03 |
| — surfaces whose width was compared against §4 | 31 | **35** | |
| — route table: surfaces mounting a navigation | 27 of 31 | **31 of 35** | |
| — typographic measures | `declared: 1, applied 1` | **unchanged** | |
| — **check E, the pairing** | **9 / 9** | **9 / 9 — UNMOVED** | §4 |
| `verify-dialogs` — `REMAINING` declared | 7 | **7 — unchanged** | exit 0 |
| — `REMAINING` measured | 5 | **5 — unchanged** | |
| — entries printed STALE | 2 | **2 — deliberate**, §6 | |
| `verify-breakpoints` | 5 declared / 16 uses; 4 files / 13 measured | **byte-identical** | exit 0 |
| `verify-tables` | `REMAINING = 0`, closure set | **untouched** | exit 0 |
| `grep -c 'THE CAUTION'` on the manifest | **15** | **17** | +2 |
| `grep -c 'no query changed'` on the manifest | **16** | **20** | +4 |

**The two figures that prove the declaration landed are the declared-surface
count and the scanned-file set.** Not the pairing — §4.

---

## 2. The scanned-file count rose by ELEVEN, and here is how it is derived

The plan predicts *"the four closures plus the two route-adjacent placeholders"*,
which is six. **The tree says eleven**, and the derivation is a set difference
over the two runs' own printed paths rather than a sum of the SUMMARYs' file
lists:

| Kind | Count | Files |
|---|---|---|
| page files | 4 | `(public)/events/page.tsx`, `(members)/attendance/page.tsx`, `(members)/membership-card/page.tsx`, `(public)/tickets/[id]/page.tsx` |
| route-adjacent placeholders | 2 | `(public)/events/loading.tsx`, `(members)/membership-card/loading.tsx` |
| components no declared closure already reached | 4 | `EventTabs.tsx`, `FormatFilterRow.tsx`, `CopyReferralLink.tsx`, `MembershipCardView.tsx` |
| a library ditto | 1 | `src/lib/apple-wallet.ts` |
| **total** | **11** | 162 → **173** |

**Route-adjacent went 9 → 11 because the directories were LISTED, not assumed:**
`/events` and `/membership-card` each hold one placeholder; `/attendance` and
`/tickets/[id]` hold none. A measured zero on two of the four.

**A twelfth path appears in the printed listing and is NOT scanned** —
`src/components/ui/Icons.tsx`, newly *excluded* as converted spine. That is the
whole of the eleven-against-twelve discrepancy, and it is written down rather
than left for a later reader to trip on. F-41.2-09-03.

---

## 3. The four entries, and what they carry

Each reason is the text of the plan that did the work, joined into the one string
this list's shape requires — never re-composed here. **The plan that walked the
closure is the one that knows what *whole* turned out to mean.**

| Route | Width | Whole is | Carries |
|---|---|---|---|
| `/events` | `default` | FOUR files | **THE CAUTION** — the public funnel, and it names venues |
| `/attendance` | `default` | ONE file | the control surface, and its stub declared |
| `/membership-card` | `default` | FOUR files | the door conditional, byte-identical |
| `/tickets/[id]` | `default` | ONE file | **THE CAUTION** — the same secret under a different predicate |

**`/events` — the wide form was DEFERRED and not rejected.** It is the phase's
one candidate, and the criterion is not met on the current markup: the nights
render as a single column. The change is one word in the shell's call.

**`/tickets/[id]` — the narrow form is UNAVAILABLE and not deferred.** Check E
fails any surface declaring `focus` while mounting a navigation, and one is
mounted here. *Deferring implies it could be picked up later; it cannot be picked
up at all.*

**The diff is pure insertion, and that was checked before staging.**

```
$ git diff --stat -- scripts/conversion-manifest.mjs
 1 file changed, 62 insertions(+)

$ git diff -U0 -- scripts/conversion-manifest.mjs | grep -c '^-[^-]'
0
```

### 3.1 The near-miss that did not become one

The acceptance criterion asks `grep -c 'no query changed'` to rise by four. **It
did not move at all — 16 → 16** — because all four new sentences were capitalised
at the start of a sentence. **This is the identical trap F-41.2-05-04 recorded**,
where the repair was a whole-file `sed` that rewrote two entries its plan did not
own and was caught only by reading the diff.

**The `sed` was not used.** Four separate edits, each anchored on text unique to
one of this plan's own four entries, took the count 16 → **20** — and the diff
stayed **62 insertions, 0 deletions**, which is the assertion that the repair
stayed inside this plan's authority.

---

## 4. Check E — both sets pasted, and the pairing reported rather than leaned on

> **The pairing does NOT consult `CONVERTED`.** Re-derived at source and confirmed
> by measurement: it was **9 / 9 before this plan's first edit and 9 / 9 after**.
> It moved in **wave 3's own conversion commits**. A reconciliation reading its
> own pairing as evidence its declaration landed is crediting a counter to the
> wrong cause.

**The declaring set — 9:**

```
    src/app/(admin)/admin/(work)/layout.tsx
    src/app/(members)/attendance/page.tsx
    src/app/(members)/membership-card/page.tsx
    src/app/(public)/artists/[slug]/page.tsx
    src/app/(public)/events/page.tsx
    src/app/(public)/gallery/page.tsx
    src/app/(public)/newsletter/page.tsx
    src/app/(public)/tickets/[id]/page.tsx
    src/app/page.tsx
```

**The mounting set — 9:** the same nine paths, in the same order, printed
separately by the gate.

*(The declared utility is elided everywhere in this document — DEF-41-01,
Tailwind's source detection scans `.planning/`. The value is at
`src/app/(public)/gallery/page.tsx:110`, the line the gate itself matched.)*

**Both difference lists are empty, asserted from the gate's own emission rather
than inferred:**

```
$ LC_ALL=C /usr/bin/grep -c '✗ E' <after-run>   → 0
$ LC_ALL=C /usr/bin/grep -cE '✗ '  <after-run>   → 0   (no failure line of any check)
```

and the equality is printed as a tick:

```
  ✓ E  … and the 9 file(s) declaring the column clearance at the md tier are
       EXACTLY the 9 file(s) mounting the responsive navigation form
```

**Nine, not the seven the plan expects — and that is the tree, not a defect.**
Three wave-3 worktrees each measured a base the other two had not moved: 41.2-06
reported 6/6, 41.2-07 reported 7/7, 41.2-08 landed a ninth without printing the
figure. On the merged tree 5 + 4 = **9**. The alternative reading — two files
declaring a clearance nobody mounts — is check E's **silent** failure direction,
and it was ruled out by reading the two sets rather than the count: same nine
paths on both sides, every one accounted for by name. **F-41.2-09-01.**

**The phase's target is 12 / 12. Three surfaces remain, not five.**

---

## 5. The twelve-entry diff, with the second instrument

**Diffed against the tree, never against wave 3's counts.** Every entry on both
open lists was re-derived — including the eleven nothing was expected to touch.

**The second instrument does not use `scripts/lib/comments.mjs`**, which is the
stripper *both* gates read through. It is a raw needle count over the un-stripped
file: the three parts of the hand-rolled shell chained on one line, the native
element, and the migrating prefix counted as occurrences.

**And it refuses unless it first reproduces known answers**, which is new in this
wave and is §7's subject:

```
self-check PASSED — positive fixture 1/1/1, negative fixture 0/0/0
```

### 5.1 `verify-dialogs` — all seven entries

| # | Entry | Gate's verdict | What the tree says | Second instrument (raw) |
|---|---|---|---|---|
| 1 | `(public)/tickets/[id]/RefundRequestButton.tsx` | **STALE** — *converted; remove this entry* | converted by 41.2-02; no shell | overlay **0**, native **0** — agrees |
| 2 | `(public)/events/[slug]/RedeemConfirmationModal.tsx` | carries a shell | unconverted, still owed | overlay **3**, native **0** — agrees |
| 3 | `(public)/events/[slug]/SumUpCheckoutModal.tsx` | carries a shell | unconverted, still owed | overlay **1**, native **0** — agrees |
| 4 | `(public)/events/[slug]/SecretVenueDialog.tsx` | carries a shell | unconverted, still owed | overlay **1**, native **0** — agrees |
| 5 | `(public)/events/[slug]/menu/GuestLoginBanner.tsx` | **STALE** | converted by 41.2-02; no shell | overlay **0**, native **0** — agrees |
| 6 | `(public)/events/[slug]/menu/GuestTokenDisplay.tsx` | carries a shell | unconverted, still owed | overlay **3**, native **0** — agrees |
| 7 | `components/media/MyMediaSection.tsx` (`ROLE_DIALOG_OVERLAY`) | carries a shell | unconverted, still owed | overlay **1**, native **0** — agrees |

### 5.2 `verify-breakpoints` — all five entries

| # | Entry | Declared | Gate measured (stripped) | Second instrument (raw) | What the tree says |
|---|---|---|---|---|---|
| 8 | `components/media/MediaUpload.tsx` | 1 | **1** | **1** — agrees | still owed |
| 9 | `(public)/events/[slug]/RedeemConfirmationModal.tsx` | 4 | **4** | **4** — agrees | still owed |
| 10 | `(public)/events/[slug]/SumUpCheckoutModal.tsx` | 4 | **4** | **4** — agrees | still owed |
| 11 | `(public)/events/[slug]/menu/GuestLoginBanner.tsx` | 3 | **0 — STALE** | **0** — agrees | converted by 41.2-02; **left standing**, §6 |
| 12 | `(public)/events/[slug]/menu/GuestTokenDisplay.tsx` | 4 | **4** | **4** — agrees | still owed |

**Twelve rows, twelve agreements.** **No row disagreed in either direction**, so
no row needed the explanatory sentence — and the load-bearing half: **no row had
a raw count LOWER than its stripped count**, which is the direction that stops a
deletion.

**The instrument is not vacuously zero.** It returned 3, 1, 1, 3 and 1 on the
five live overlays and 1, 4, 4, 4 on the four live prefix files **in the same run
session** as the zeros. And it is independently corroborated: **these twelve
figures are identical, file for file, to the twelve plan 41.2-05 recorded on a
different base** — two measurements a wave apart, neither having seen the other.

### 5.3 The zero movement, proved PER FILE

**The tree's own answer to which files wave 3 touched**, taken from git rather
than from three SUMMARYs:

```
$ git diff --name-only 64eb82e e2bd64c -- src
src/app/(members)/attendance/page.tsx
src/app/(members)/membership-card/loading.tsx
src/app/(members)/membership-card/page.tsx
src/app/(public)/events/EventTabs.tsx
src/app/(public)/events/FormatFilterRow.tsx
src/app/(public)/events/loading.tsx
src/app/(public)/events/page.tsx
src/app/(public)/tickets/[id]/page.tsx
src/components/membership/CopyReferralLink.tsx
src/components/membership/MembershipCardView.tsx
```

**Ten files — and they are exactly the ten the three SUMMARYs declare.** No
eleventh, none missing. Where the instruction is to diff against the tree
*rather than* the SUMMARYs, the honest outcome here is agreement.

**All ten, measured with the second instrument:**

```
overlay=0   native=0   sm:uses=0    src/app/(members)/attendance/page.tsx
overlay=0   native=0   sm:uses=0    src/app/(members)/membership-card/loading.tsx
overlay=0   native=0   sm:uses=0    src/app/(members)/membership-card/page.tsx
overlay=0   native=0   sm:uses=0    src/app/(public)/events/EventTabs.tsx
overlay=0   native=0   sm:uses=0    src/app/(public)/events/FormatFilterRow.tsx
overlay=0   native=0   sm:uses=0    src/app/(public)/events/loading.tsx
overlay=0   native=0   sm:uses=0    src/app/(public)/events/page.tsx
overlay=0   native=0   sm:uses=0    src/app/(public)/tickets/[id]/page.tsx
overlay=0   native=0   sm:uses=0    src/components/membership/CopyReferralLink.tsx
overlay=0   native=0   sm:uses=0    src/components/membership/MembershipCardView.tsx
```

**Thirty measured zeros across ten files** — no hand-rolled shell, no native
element outside the primitive, no third breakpoint prefix. **That is why neither
ratchet moved, and it is a measurement rather than an inference.**

**And the gates agree, byte for byte:**

```
$ diff dialogs-before.txt dialogs-after.txt  ; echo $?   → 0
$ diff bp-before.txt      bp-after.txt       ; echo $?   → 0
$ diff tables-before.txt  tables-after.txt   ; echo $?   → 0
```

**Three gate outputs, three empty diffs.** A wave that touched no entry can prove it.

### 5.4 The three negative obligations

- **No entry was added**, to either list.
- **No entry was moved between groups.** The dialogs tag balance is
  `public-member-money` 7 → 7, `work` 0 → 0; breakpoints 4 files / 13 uses → same.
- **No entry was deleted.** Zero, which was the expectation and is now the
  measurement.

---

## 6. The two STALE orphans stay, and it is a choice

`GuestLoginBanner.tsx` and `RefundRequestButton.tsx` are converted, and both
gates print them STALE. **They were not deleted here.**

**The reason is not that deleting them would empty a list.** It would not:
`verify-dialogs`' list would go 7 → 5 and `verify-breakpoints`' 5 → 4, both
non-empty, so neither emptiness refusal would fire and nothing would break.

**The reason is that a deletion belongs in the same wave that writes the gate's
closure decision**, which is plan 41.2-19. Split across two waves a deletion is a
tidy-up that happens to shrink a counter; kept beside `MIGRATION_CLOSED` it is an
answer with its reasoning beside it. This repository has **four recorded
recurrences** of a debt that went quiet because a counter fell for a reason nobody
wrote down, and the defence is that **a number only ever moves in the same commit
as the sentence explaining why**.

**For 41.2-19, so it is not re-derived twice:** `GuestLoginBanner.tsx` is stale on
**both** gates — a dialogs entry, and a breakpoints entry of 3 declared against 0
measured — and both halves belong in that commit. **This is the second wave to
state it; it has now been visible three times rather than remembered once.**

**And one rule that is new:** a zero from the second instrument counts **only if
the run that produced it printed its self-check**. §7 is why.

---

## 7. The instrument returned a vacuous zero, and it would have authorised twelve deletions

**On its first run the second instrument returned `0 / 0 / 0` on all eight
files** — including four the gates report as carrying 3, 1, 1 and 3 overlays.

The defect was the instrument's, not the tree's: a word-character class was
defined **with** its brackets and then used **inside** brackets, nesting a bracket
expression in a bracket expression. It was caught because a plain
`grep -c 'fixed'` on the same file returned **3**: the instrument disagreed with
the crudest possible reading of the same bytes.

**Why this is the finding of the wave.** An instrument that returns zero on every
file is **indistinguishable from a tree with no debt left**. Believed, the rule
this plan operates under — *delete when the gate and a second instrument both read
zero* — would have authorised deleting **all twelve entries**, in a wave whose
expectation is zero deletions, and every deleted row would have carried a second
instrument's agreement. **That is a fifth mechanism** alongside the phase family's
four: a never-measured map, a fence match, an unguarded walk-membership check, a
comment stripper going blind — and now **a corroborating instrument that measures
nothing and agrees with everything.**

**The guard that outlives the repair:** the instrument now **refuses (exit 2)**
unless it first reproduces 1/1/1 on a positive fixture and 0/0/0 on a negative
one, and prints `self-check PASSED` before any measurement. Every figure in §5
comes from a run that printed it. **F-41.2-09-02.**

---

## 8. The own maximum, re-measured now that more surfaces are declared

Wave 2 corrected wave 0 to *one deleted, one declared, **three still standing***
— and said plainly that check D named none of the three **by invisibility, not by
payment**. Two of those three belong to a surface this plan declares, so the
question was live here and not at wave 8.

| # | File | Wave 0 | Wave 2 | **Now** |
|---|---|---|---|---|
| 1 | `src/app/page.tsx:46` | DELETE | gone | gone |
| 2 | `(public)/artists/[slug]/page.tsx` | DECLARE | declared | unchanged |
| 3 | `(public)/tickets/[id]/page.tsx:108` | DELETE | standing, invisible | **PAID** |
| 4 | `(public)/tickets/[id]/page.tsx:191` | DELETE | standing, invisible | **PAID** |
| 5 | `(public)/events/[slug]/menu/page.tsx:163` | DELETE | standing, invisible | **standing, still invisible** |

**Measured with a raw grep BEFORE the declaration landed** — six files, six
zeros: the four page files and the two placeholders. Rows 3 and 4 were paid by
plan 41.2-08, **together**, which is what the disposition demanded.

**Check D's line, pasted:**

```
  ✓ D  the shell declares §4's three maxima and only those; all 35 converted
       page(s) import it, write no undeclared maximum of their own, and carry the width
       §4 assigns
```

**`pagesWithOwnMaximum` names no page file — and this time that is payment rather
than invisibility for two of the three.** Row 5 belongs to
`/events/[slug]/menu`, which no plan has declared: a sibling route directory is
not an import, so declaring `/events` did not make it visible. **Whichever plan
declares that surface inherits a check-D red unless the line goes in the same
commit.** F-41.2-09-04.

**There was no newly-visible red to resolve here.** That is the answer to the
question the brief asked, and it is a measurement, not a hope.

---

## 9. The human-row ledger

**Thirteen rows appended**: twelve width rows — `/events`, `/attendance`,
`/membership-card` and `/tickets/[id]` at **390 / 768 / 1440** — plus the
placeholder row for the events listing.

**The two counts, asserted equal over the whole document:**

```
$ LC_ALL=C /usr/bin/grep -c '^| [0-9]'                → 25    ← Result cells
$ LC_ALL=C /usr/bin/grep -c '^| [0-9].*| pending |$'  → 25    ← reading pending
```

**25 = 25.** Every Result cell is unrun.

**Three rows quoted as evidence that each criterion is a number with a tolerance:**

> | 15 | `/events` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px wide; the space each side of it differs by ≤8px; page moves 0px when dragged sideways; **the nights still render ONE per row (count = 1, tolerance 0)**, and each night card spans the content column within 8px rather than sitting at a narrower measure of its own | pending |

> | 20 | `/membership-card` | 768 | leading clearance 224px ±8px, nothing left of it; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways; **the code's drawn box is STILL 192px ±4px square — it does not scale with the wider column** | pending |

> | 25 | `/events` | 768 | with the network throttled so the placeholder is visible for at least two seconds, note the distance from the viewport's leading edge to the leading edge of the FIRST placeholder card; when the nights land, note the same distance to the leading edge of the first night card. **The two distances differ by 0px, tolerance 0.** The same comparison on the trailing edge: **0px, tolerance 0** | pending |

**Rows 19–21 are the door rows and they are the strongest in the batch.** The
conversion asserted 192px in both branches by reading a class string; a class
string is not a drawn box, and a code that shrinks because a column got wider
fails to scan in front of a queue.

**Wave 2's twelve rows were not touched** — zero data rows appear as `-` in the
diff. The only two deleted lines are the document's own provenance footer, now
naming both plans. **§3's sentence *"Twelve rows, twelve unrun procedures"* was
left standing**, because it is wave 2's and it was true when written; the correct
total is stated in a new §7 of the ledger instead. F-41.2-09-07.

---

## 10. What the tree said that no wave-3 SUMMARY did

1. **The pairing is 9 / 9, not 6 / 6 or 7 / 7.** No wave-3 SUMMARY could have
   said so: three isolated worktrees, each on a base the other two had not moved.
   F-41.2-09-01.
2. **The scanned set grew by eleven, not six** — five of the eleven are
   components and a library no declared closure already reached. §2.
3. **A tenth spine file is now reached** — `src/components/ui/Icons.tsx`, absent
   from the before run entirely. It is what makes twelve printed paths into
   eleven scanned files, and it is the first movement of a row wave 2 argued
   *ought* to move. F-41.2-09-03.
4. **Two of the three standing own-maximum occurrences are paid**, and became
   visible to check D already at zero rather than as a red. §8.
5. **And one agreement:** the three SUMMARYs' `files_modified` are exactly the
   ten paths git reports. Diffing against the tree sometimes returns a
   confirmation, and saying so is part of the same discipline.

---

## 11. Deviations from Plan

### 1. [Recorded, not reconciled] The plan expects seven pairs; the tree has nine

- **Found during:** Task 1, on the baseline run before any edit.
- **What the plan says:** *"Check E now carries seven pairs"*, and the acceptance
  criterion *"Seven files should now appear in both"*.
- **What the tree says:** **nine**, before and after this plan's declaration.
- **Not made to be seven.** No expectation was reworded into agreement and no
  gate was touched. Both sets are pasted in §4 and every one of the nine is
  accounted for by name, which is what rules out check E's silent direction.
- **Recorded:** F-41.2-09-01. **Commit:** `ab964c0`.

### 2. [Rule 3 — blocking, self-inflicted, caught before any entry was touched] The second instrument returned a vacuous zero

- **Found during:** Task 2, on the instrument's first run.
- **Issue:** all zeros on all eight files, including four the gates report as
  live. A nested bracket expression in the instrument, not a paid debt.
- **Fix:** the class corrected, **and a mandatory two-fixture self-check added**
  that refuses with exit 2 rather than reporting. Every figure in §5 comes from a
  run that printed `self-check PASSED`.
- **Why it is written up rather than quietly fixed:** believed, it would have
  authorised deleting **all twelve entries** with a second instrument's
  agreement on every row.
- **Recorded:** F-41.2-09-02. **Commit:** `7cfa637`.

### 3. [Rule 1 — caught before staging] Four capitalised sentences left a required count unmoved

- `grep -c 'no query changed'` did not move — 16 → 16 — because all four new
  entries capitalised the sentence. **The identical trap F-41.2-05-04 recorded.**
- **The whole-file `sed` that produced that near-miss was not used.** Four edits
  anchored on text unique to this plan's own entries took it to **20**, and the
  diff stayed **62 insertions, 0 deletions**. §3.1.
- **Commit:** `ab964c0`.

### 4. [Rule 1 — reverted immediately] One edit touched an entry this plan does not own

- **Found during:** Task 1, while looking for an anchor. A single edit altered a
  word inside plan 41.2-04's `/artists/[slug]` reason text.
- **Reverted before anything was staged**, and `git diff --stat` on the manifest
  returned **no output** — byte-for-byte restored. The committed diff carries
  **zero `-` lines**.
- **Recorded here rather than absorbed**, because it is the third time in two
  waves that this file's shared-document hazard has been touched, and the defence
  each time was the same: read the diff, never the count.
- **Commit:** `ab964c0`.

### 5. [Decided] The two STALE orphan entries stay

- Restated as a **choice** with the false reason explicitly refused. §6 and
  **D-41.2-09-A**. Plan 41.2-19 reads this SUMMARY as input.

### 6. [Refused] Nothing that would have needed a halt was needed

- **No spine file opened.** `AppNav.tsx`, `MobileNav.tsx`, `Button.tsx` and
  `Typography.tsx` were neither read for edit nor modified.
- **No file under `src/` was modified at all.** This plan's commits touch only
  `scripts/conversion-manifest.mjs` and `.planning/`.
- **No server action opened. Zero packages** proposed or installed.
- **No Supabase read or write, no migration, no seeding, no dev server, no
  headless render** (D-41.2-04).
- **`npm run verify` was NOT made green.** No credentials added, no gate skipped,
  no criterion reworded. §12.
- **No `STATE.md`, no `ROADMAP.md`** — the orchestrator's.

### Auto-fixed

Deviations 2, 3 and 4 above. No other bug, missing critical functionality or
blocking issue was found.

---

## 12. Verification

**There is no test runner in this repository.** Nothing below is verified because
tests pass; each line is a command with its exit code, a `grep` with its count, or
a source assertion.

```
$ npm run build
build_exit=0        ← run twice, once after each task
```

```
$ node scripts/verify-conversion.mjs   → 0   35 surfaces, 173 files, all five checks
$ node scripts/verify-dialogs.mjs      → 0   REMAINING declared 7, measured 5
$ node scripts/verify-breakpoints.mjs  → 0   5 declared / 16 uses; 4 files / 13 measured
$ node scripts/verify-tables.mjs       → 0   REMAINING = 0, with its closure set
```

**`npm run verify` — the expected verdict on a worktree, quoted from the tail:**

```
      of which passed               15
      of which FAILED                0
      of which REFUSED               1  — nothing was measured by these
    needs a server, not run          1
    declared absent                  0
    MISSING                          0
                                   ───
    accounted for                   17

  ═══ verify:capabilities — REFUSED (exit 2) ═══
FATAL: missing environment variable(s): SUPABASE_ACCESS_TOKEN, NEXT_PUBLIC_SUPABASE_URL.
Set them in .env.local (gitignored) or in the environment. Nothing was measured.

  VERIFY_REFUSED — 1 gate(s) could not measure: verify:capabilities
```

**15 gates reached a verdict, 0 failed, and `verify:capabilities` refused for want
of `.env.local`.** The suite exits 2, and that is its honest state here rather
than a failure of this work. **It was not made green.** Supplying those
credentials would point a gate at production, which **D-41.2-04 forbids** and
which this repository has an incident behind: **D12**, 63 rows deleted across
seven tables, none recovered, PITR not enabled. The full 16/16 suite is the
orchestrator's, in the main checkout, after this wave merges.

---

## 13. Threat register — dispositions discharged

| Threat ID | Disposition | How it was discharged |
|---|---|---|
| T-41.2-50 | mitigate | **All twelve** entries re-derived in a wave expected to move none, §5, each with a second instrument that shares no line with the gates' comment stripper — **and that instrument was proved non-vacuous by two fixtures after it failed once**. The unchanged counts are proved **per file** (thirty measured zeros across ten files) and by three byte-identical gate outputs, never asserted |
| T-41.2-51 | mitigate | Checks A, B and D walked all four new closures the moment the entries landed — 173 files against 162, derived as a set difference — and the route-adjacent extension was checked by **listing** the four route directories: two placeholders, two measured zeros |
| T-41.2-52 | mitigate | Both sets pasted in full from the gate's own output in §4; both difference lists asserted empty by grepping for the failure lines the gate emits — **0**, and **0** for any `✗` line at all. The count differing from the plan's expectation was resolved by reading the **sets**, which is the only thing that separates a stale prediction from the silent failure direction |
| T-41.2-53 | mitigate | THE CAUTION carried in the entries for `/events` and `/tickets/[id]`, presence asserted by count **15 → 17**; the venue predicates, the label ternary, the select multiset, the byte-identical venue line and the flag-selected-but-never-read state all named in the reason text |
| T-41.2-54 | mitigate | The two STALE orphans deliberately left, the reason written as a **decision** in §6 and the false reason explicitly refused; the decision now visible three times rather than remembered once |
| T-41.2-55 | mitigate | Every Result written `pending`; Result-cell count **25** asserted equal to the `pending` count **25**; every criterion a number with a tolerance, three quoted in §9 |
| T-41.2-SC | mitigate | Zero packages proposed, zero installed |

**Threat flags:** none. **No file under `src/` was modified.** No network endpoint,
auth path, file-access pattern or schema touch was introduced anywhere.

---

## 14. Known Stubs

**None introduced.** One is **inherited and now declared**: `/attendance` renders
a stub — a `TODO` and a hardcoded empty array — so the empty branch is the only
branch a member can reach. It was inherited unchanged by plan 41.2-07 (fetching
rows is a query, and no query changed) and **this plan declares the surface
converted anyway**, because the conversion is real: the shell, the pair and the
placeholder all landed.

**The qualification is written into the `CONVERTED` entry itself, not only here**,
so the next reader of that list meets it where the claim is made. **A surface can
be converted and still show a member nothing, and *converted* does not
distinguish the two.** F-41.2-09-06, routed to 41.2-20.

The ledger's 25 empty Results are incomplete **by design and say so**: unrun
procedures, with the header stating why none could be measured here.

---

## 15. What this plan did NOT measure, said plainly

- **Nothing rendered.** No dev server, no headless render, no box measured, no
  database read or write. Every figure is a class string, a path, a `grep` count
  or a gate's own printed line.
- **`verify:capabilities` measured nothing**, in every run — an environment fact
  reported, not a gap filed.
- **H41-4 has still never been produced.** Thirteen more rows are written; all
  thirteen are unrun, and the ledger holds **25** unrun procedures.
- **Nothing was proved RIGHT, only UNMOVED.** The venue predicates, the door
  conditional and the code's box were asserted by their own plans to be the text
  they were. Whether they are the *correct* predicates is D-36-11's and D-37-09's
  answer, taken in earlier phases.
- **The render-mode question on `/tickets/[id]` is still open.** That route is
  dynamic **by derivation** and does not say so. Declaring it in `CONVERTED` does
  not answer it, and no gate here would redden if the derivation stopped holding.
- **`/events/[slug]/menu`'s own maximum is still standing and still invisible.**
  Not paid — merely not yet in scope. §8.

---

## Commits

| Task | Commit | Subject |
|---|---|---|
| 1 | `ab964c0` | `feat(41.2-09)` — four surfaces declared, with the cautions their own plans reported |
| 2 | `7cfa637` | `docs(41.2-09)` — twelve entries re-derived in a wave that moved nothing, thirteen human rows |
| — | *(this commit)* | `docs(41.2-09)` — the summary and the findings |

Base: `e2bd64c`.

---

## Self-Check: PASSED

Files claimed created or modified, checked on disk:

```
FOUND: scripts/conversion-manifest.mjs
FOUND: .planning/phases/41.2-public-member-and-money-surfaces/41.2-HUMAN-ROWS.md
FOUND: .planning/phases/41.2-public-member-and-money-surfaces/41.2-09-FINDINGS.md
FOUND: .planning/phases/41.2-public-member-and-money-surfaces/41.2-09-SUMMARY.md
```

Commits claimed, checked in the log:

```
FOUND: ab964c0  feat(41.2-09): quattro superfici dichiarate, con le cautele che i loro piani hanno riportato
FOUND: 7cfa637  docs(41.2-09): dodici voci ri-derivate in un'onda che non doveva muovere nulla, tredici righe umane
```

---

*Plan 41.2-09, executed 2026-08-14 on base commit `e2bd64c`, in an isolated
worktree. `STATE.md` and `ROADMAP.md` are the orchestrator's and were not
touched. Contains no venue under negotiation, no unannounced date, no line-up, no
member, no ticket identifier and no amount: `.planning/` is tracked and this
repository is public.*
