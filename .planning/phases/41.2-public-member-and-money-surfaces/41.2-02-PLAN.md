---
phase: 41.2-public-member-and-money-surfaces
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx
  - src/app/(public)/tickets/[id]/RefundRequestButton.tsx
autonomous: true
requirements: [DS-07, DS-08, RESP-01, RESP-03]
must_haves:
  truths:
    - "Both files that no closure reaches carry the primitives, so two ratchets can reach zero without an exemption and without a deletion"
    - "The re-enable path for the guest banner lands already converted rather than reopening a gate that had closed"
    - "Neither file was made reachable: no import was restored, no render site uncommented"
  artifacts:
    - path: "src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx"
      provides: "the guest sign-in invite on the Dialog primitive, its three sm: uses deleted rather than migrated"
      contains: "components/ui/Dialog"
    - path: "src/app/(public)/tickets/[id]/RefundRequestButton.tsx"
      provides: "the refund-request confirmation on the Dialog primitive, Cancel first and focused"
      contains: "data-initial-focus"
  key_links:
    - from: "src/app/(public)/tickets/[id]/RefundRequestButton.tsx"
      to: "src/components/ui/Dialog.tsx"
      via: "the extracted dialog shell and its imperative initial focus"
      pattern: "data-initial-focus"
---

<objective>
Convert the two files that sit on a ratchet list and that **no closure reaches**.

Purpose: this is the finding most likely to strand criterion 3. Check A and check
B only ever open a *declared surface's closure*, and neither of these files is in
one — so **no surface conversion in this phase clears either as a by-product**,
and two `REMAINING` lists cannot reach zero without an explicit disposition.
D-41.2-03 took that disposition: **converted**, not deleted and not permanently
exempted, because it is the only answer that is reversible and decides nothing
outside this phase. Deleting would answer a product question that is not this
phase's, and on a public repository a deletion is a publication of the decision.
A permanent exemption would be a lie about a file whose own import comment says
*"re-enable by restoring this import"* — the exemption's rule is *a file that will
never convert is not a debt*, and this one is meant to come back.

It is in wave 0 for the same reason the dry run is: scoped where it can still
change the plan rather than at the end, where it strands a criterion.

Output: 245 lines converted, two ratchets unblocked, and the re-enable path left
warm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). It converts the two
   files; the `REMAINING` entries they sit on are deleted by a **reconciliation**
   plan, on the authority of the tree, never on the authority of this plan's
   claim. Both gates report a converted-but-still-listed file as `STALE` and exit
   0, so leaving the entries is the correct intermediate state.
2. **41.2 does not edit the spine** — `layout/AppNav.tsx`, `layout/MobileNav.tsx`,
   `ui/Button.tsx`, `ui/Typography.tsx`. Needing to is a **HALT**.
3. **No server action is opened.** Stop condition 2 — halt and report.
4. **No package is installed.**
5. **DEF-41-01** — never spell a bracketed arbitrary-value utility in prose or in
   a SUMMARY.
6. **`.planning/` is public.** Roles, never people; no real ticket, amount,
   member or address in the SUMMARY.
7. **No `npm run dev`, no headless render** (D-41.2-04).
8. **⚠ `scripts/verify-conversion.mjs` is invisible to the default `grep`** — two
   literal NUL bytes, and the default is `ugrep -I`, which skips binary files
   silently. `LC_ALL=C /usr/bin/grep` or the Read tool.
9. **macOS/BSD.** `grep -E`, `sed -i ''`.
10. **No test runner exists.** Verification is the gates, `npm run build`, and a
    written procedure whose result is `pending`.
11. **Neither file is made reachable by this plan.** The commented import at
    `src/app/(public)/events/[slug]/menu/page.tsx:12-13` and the commented render
    at `:178-182` stay commented; `GuestDrinkMenu.tsx:11` stays commented.
    Restoring a feature is a product decision and it is not this one.

**Quick run:** `npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`

**The clean-file greps this phase uses, so every plan means the same thing by
"zero raw palette" and "zero legacy token"** — the matcher is
`scripts/verify-conversion.mjs:850-916`:

- raw palette: `LC_ALL=C /usr/bin/grep -cE '(bg|text|border|ring|from|to|via|fill|stroke|shadow|outline|decoration)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose|black|white)(-[0-9]{1,3})?' <file>` — expect **0**, with the single tolerated exception of a translucent black scrim (`bg-black` followed by an opacity), which a file on the Dialog primitive no longer writes at all
- legacy token: the same prefix list against `card-border|card|background|foreground` — expect **0**
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: GuestLoginBanner onto the Dialog primitive — three sm: uses deleted, not migrated</name>
  <files>src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx</files>
  <read_first>
    - `src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx` — 155 lines, in full. It is dead markup with live debt: 9 raw palette utilities, 8 legacy tokens, **3 `sm:` uses**, and 8 raw interactive elements
    - **The analog:** `src/app/(admin)/admin/events/[id]/tickets/RefundActions.tsx:121-174`, converted by plan 41.1-17 — the dialog call shape, the status region instead of a toast, and the mount-only-while-open pattern at `:51-53`
    - `src/components/ui/Dialog.tsx` — `:52` (this file is named in the primitive's own docblock as one of the shells the phone form was taken from), `:66-77` (why a native dialog paints above the toast container), `:117-147` (the initial-focus contract and why the React autofocus prop never reaches the DOM), `:161-166` (the closed `lg` list), `:316-325` (the status region and its two roles)
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.4 (the four money shells and what they delete), §3 rows 4, 6, 7, 8, 9, 11, and §7.4 — **the `sm:` split is 13 shell uses / 2 type-size uses / 1 grid axis, not 12**
    - `src/lib/routes/next-redirect.ts:73` — it names this file's two redirect sites by line. Read it so a renumbering here is noticed rather than discovered later
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §8.3 (the dialog and its closed size list), §11 (the copywriting contract — *none introduced*)
  </read_first>
  <action>
Adopt `Dialog`. The hand-rolled overlay at `:71` and `:76`, its panel, and its own
heading element all go: the primitive owns the shell and the `title` prop **is**
the heading.

**The three `sm:` uses are deleted, not migrated.** They are the sheet-vs-window
pair — bottom-anchored below the boundary, centred above it, with the phone
sheet's bottom padding — and the primitive owns that pair. Migrating them to `md:`
would leave a second author for a decision the primitive already makes. This is
pitfall P6 and it is the single most likely error in this file.

Every interactive element takes its primitive: a control that submits or
navigates becomes `Button` at its size rung; an interactive pill on a `<Link>` or
a `<button>` becomes `Chip`, which carries the 44px floor at
`src/components/ui/Chip.tsx:58`. This file carries **8** of them and none declares
a minimum today, so it is one of the heavier single contributions to G5 in the
phase.

Any hand-rolled status box — the bordered red or green pair — becomes the
dialog's `status` prop, which renders the alert role in the semantic token. **A
dialog never reports its own outcome through the toast**: a native dialog paints
in the top layer, above the toast container, so a toast raised from inside one is
raised invisibly. `verify-dialogs` check C asserts this.

**The copy does not change.** Not the invitation's wording, not the button
labels. §11: *none introduced*.

**The two redirect targets do not change.** `next-redirect.ts:73` documents the
shape this file produces; the destination string, its encoding and its parameter
name stay byte-identical, and the SUMMARY quotes both call sites before and after.
If the conversion appears to need a different redirect, that is a behaviour
change on the sign-in path and it is not this plan's — halt and report.

**Do not restore the feature.** The import at `menu/page.tsx:12-13` and the render
at `:178-182` stay commented, and `GuestDrinkMenu.tsx:11` stays commented. Assert
it after the diff rather than assuming it: the temptation while converting a file
that is *"temporarily disabled"* is to finish the job somebody started, and
finishing it is a product decision.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-dialogs.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'components/ui/Dialog' 'src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx'` returns at least 1
    - `LC_ALL=C /usr/bin/grep -cE 'fixed inset-0|backdrop-blur|z-\[60\]' 'src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx'` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'sm:' 'src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx'` returns **0**, and the SUMMARY states the three uses were **deleted by the primitive**, not migrated to `md:`
    - The raw-palette grep from the conversion rules returns **0**; the legacy-token grep returns **0**
    - `LC_ALL=C /usr/bin/grep -cE 'focus:outline-none|max-w-|animate-pulse' 'src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx'` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'useToast' 'src/app/(public)/events/[slug]/menu/GuestLoginBanner.tsx'` returns **0** — the refusal is the primitive's `status`
    - **The feature is still disabled:** `LC_ALL=C /usr/bin/grep -n 'GuestLoginBanner' 'src/app/(public)/events/[slug]/menu/page.tsx'` shows both hits still inside comments, and `LC_ALL=C /usr/bin/grep -n 'GuestLoginBanner' 'src/app/(public)/events/[slug]/menu/GuestDrinkMenu.tsx'` shows its one hit still commented. Paste all three lines
    - `git diff` filtered for the two redirect call sites shows the destination string, its encoding and its parameter name unchanged — quoted before and after
    - `node scripts/verify-dialogs.mjs` exits 0; the entry for this file is now reported **STALE** rather than owed, and the SUMMARY pastes that line
    - `node scripts/verify-breakpoints.mjs` exits 0; the file's declared count is now above its live count, reported STALE, and the SUMMARY pastes that line
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The guest invite is on the platform's dialog, its three sm: uses are gone because the primitive owns that boundary, its eight controls carry the floor, and the feature is exactly as disabled as it was.</done>
</task>

<task type="auto">
  <name>Task 2: RefundRequestButton — a confirmation for money leaving, on a control nothing renders</name>
  <files>src/app/(public)/tickets/[id]/RefundRequestButton.tsx</files>
  <read_first>
    - `src/app/(public)/tickets/[id]/RefundRequestButton.tsx` — 90 lines, in full. 9 raw palette, 8 legacy tokens, 8 raw interactive elements, and **no importer at all**: `LC_ALL=C /usr/bin/grep -rn 'RefundRequest' src` returns only this file's own two lines
    - **The analog, exact match:** `src/app/(admin)/admin/events/[id]/tickets/RefundActions.tsx` — `:121-174` for the money confirmation whole, `:100-107` for why every route out of the confirmation runs through one reset, `:51-53` for mounting the dialog only while open
    - `src/components/ui/Dialog.tsx:117-147` — the initial-focus contract. **The live argument is about the attribute, not about any file**: `41.1-PATTERNS.md` §5.3's claim about an inert autofocus was **withdrawn** by the converted `RevealVenueDialog.tsx:59-75`, so do not cite it
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §11 — the destructive-confirmation rule: Cancel first in the DOM, first in the tab order, carrying the initial-focus marker; the confirming control on the destructive variant; no Enter-to-confirm
    - `.claude/rules/ticketing-payments.md` and `.claude/rules/meta-gates.md` — the monotone guard on a payment reaching completion
    - `scripts/verify-dialogs.mjs:1197-1310` — this file is **entry 1** on `REMAINING`, and its entry reads *"a hand-rolled overlay on the ticket surface — a refund request, which is money leaving"*
  </read_first>
  <action>
Adopt `Dialog`, with §11 applied verbatim because this control asks for money to
come back out: **Cancel is first in the DOM, first in the tab order, carries the
initial-focus marker and is the secondary variant. The confirming control is the
destructive variant. There is no Enter-to-confirm and no autofocus on the
confirming control.**

Record the before and after focus target in one line each, **read out of the file
rather than assumed**. If the file carries the React autofocus prop, it is
**removed** rather than kept beside the marker — two mechanisms for one intent
leave a reader unable to tell which is load-bearing.

The refusal becomes the primitive's `status`, not a toast, and it is reset by
whatever function closes the dialog — so the close control, Escape and Cancel
cannot leave a stale refusal behind. The analog states that rule in its own
docblock at `:100-107`.

Every interactive element takes its primitive; this file carries 8 and none
declares a minimum.

**The copy does not change.** A refund request's wording is what a ticket holder
reads before asking for money back, and rewording it is a decision, not a
conversion. If a refusal sentence collapses two distinct causes, **record it as a
finding carried forward** in the SUMMARY and in this phase's `deferred-items.md` —
there is no error tracking here, so a refusal nobody can read is a refusal nobody
ever reads — but do not rewrite it.

**No payload moves.** Read whatever action or route this control calls **so you
can tell a rendering change from a payload change**, and then change nothing about
it: not the ticket identifier, not the amount, not the reason, not an idempotency
key. If the conversion appears to need a payload change, **halt and return to the
owner** — that is stop condition 2 and it is not a judgement to make at execution
time.

**Do not give the file an importer.** It has none, and adding one restores a
feature. That is D-41.2-03's deferred half and it stays deferred.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-dialogs.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'components/ui/Dialog' 'src/app/(public)/tickets/[id]/RefundRequestButton.tsx'` returns at least 1
    - `LC_ALL=C /usr/bin/grep -cE 'fixed inset-0|backdrop-blur|z-\[60\]' 'src/app/(public)/tickets/[id]/RefundRequestButton.tsx'` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'data-initial-focus' 'src/app/(public)/tickets/[id]/RefundRequestButton.tsx'` returns exactly **1**, and it is on the Cancel control — quote the surrounding lines
    - `LC_ALL=C /usr/bin/grep -c 'autoFocus' 'src/app/(public)/tickets/[id]/RefundRequestButton.tsx'` returns **0**
    - The raw-palette grep returns **0**; the legacy-token grep returns **0**; `focus:outline-none`, `max-w-`, `sm:` and `animate-pulse` each return **0**
    - `LC_ALL=C /usr/bin/grep -c 'useToast' 'src/app/(public)/tickets/[id]/RefundRequestButton.tsx'` returns **0**
    - **The copy is byte-identical**: `git diff` on the file shows no line changing a user-visible string; the filtered diff is pasted
    - **The money grep is clean and quoted line by line**: the diff filtered for `status`, `refund`, `amount`, `idempot`, `webhook` shows only rendering changes, each with one sentence saying why
    - `LC_ALL=C /usr/bin/grep -rn 'RefundRequest' src` still returns **only this file's own lines** — it gained no importer
    - `git status --porcelain` shows exactly the two files this plan declares, and **nothing** under `src/app/api/`, `scripts/`, or any `actions.ts`
    - `node scripts/verify-dialogs.mjs` exits 0 and reports this entry STALE; the SUMMARY pastes that line
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The refund-request confirmation is the platform's modal, measurably harder to trigger by accident than it was, its copy untouched, its payload untouched, and it still has no importer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ticket holder → refund request | pressing this control asks for money to move back out of the business |
| guest → sign-in redirect | the banner's redirect decides where an unauthenticated buyer lands after signing in |
| dialog → keyboard | a confirmation that submits on Enter is one keystroke from an accident |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-06 | Tampering | a refund request triggered by an accidental Enter | mitigate | §11 verbatim: Cancel first, Cancel carrying the initial-focus marker the primitive honours imperatively after the modal opens, destructive variant on the confirming control, no Enter-to-confirm |
| T-41.2-07 | Tampering | a status transition, refund amount, idempotency key or webhook path | mitigate | stop condition 2 stated; the diff is filtered for all five terms and every match quoted with a sentence; a conversion that cannot proceed without one **halts** |
| T-41.2-08 | Repudiation | a reworded money confirmation | mitigate | the copy is byte-identical and the filtered diff proves it; a collapsed-cause refusal is carried forward as a finding rather than rewritten |
| T-41.2-09 | Elevation of privilege | a disabled feature re-enabled under cover of a conversion | mitigate | all three commented references asserted still commented **after** the diff, pasted line by line; and the refund control asserted still importerless |
| T-41.2-10 | Spoofing | a changed sign-in redirect target | mitigate | both redirect call sites quoted before and after; `next-redirect.ts:73` names them by line, so a renumbering is noticed here rather than discovered later |
| T-41.2-11 | Repudiation | a `REMAINING` entry deleted by the plan that claims to have paid it | mitigate | this plan deletes no entry. Both gates report a converted-but-listed file as STALE and exit 0; the deletion belongs to a reconciliation plan and happens on the authority of the tree |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed; needing one is a HALT |
</threat_model>

<verification>
- `npm run verify` exits 0 with no refusal; `npm run build` exits 0
- `git status --porcelain` shows exactly two modified files
- Both gates report both files STALE rather than owed, and both lines are pasted
- The money grep is quoted line by line for the refund control
- All three commented references to the guest banner are pasted, still commented
</verification>

<success_criteria>
Two files that no closure reaches carry the primitives, so `verify-dialogs` and
`verify-breakpoints` can reach zero later without an exemption and without a
deletion; the re-enable path lands already converted; and neither file became
reachable, gained an importer, or moved a payload to get there.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-02-SUMMARY.md`
when done, with the before/after focus target, the byte-identical copy diff, the
money grep quoted line by line, the three still-commented references pasted, and
both gates' STALE lines.
</output>
