---
phase: 41.2-public-member-and-money-surfaces
plan: 11
type: execute
wave: 6
depends_on: ["41.2-10"]
files_modified:
  - src/app/(public)/events/[slug]/menu/page.tsx
  - src/app/(public)/events/[slug]/menu/loading.tsx
  - src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx
  - src/app/(public)/events/[slug]/menu/UserTokenDisplay.tsx
autonomous: true
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The bar's QR surface still reads no venue at all — the measured zero is still zero after the diff"
    - "The surface mounts the responsive navigation and declares the tablet-tier clearance, as a pair"
    - "The one page file whose maximum is composed at run time ends in the state wave 0 specified"
    - "Whether this surface gains a heading a guest reads standing at the bar is a stated copy decision, not a by-product"
  artifacts:
    - path: "src/app/(public)/events/[slug]/menu/page.tsx"
      provides: "the bar's QR surface on PageShell at default width, navigation pair, zero venue reads"
      contains: "components/layout/AppNav"
    - path: "src/app/(public)/events/[slug]/menu/loading.tsx"
      provides: "the bar placeholder on the Skeleton primitive at the page's own width"
      contains: "components/ui/Skeleton"
  key_links:
    - from: "src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx"
      to: "an already-converted work module"
      via: "an import of the drinks manager, converted by plan 41.1-10 and not opened here"
      pattern: "DrinkMenuManager"
---

> **`npm run verify` cannot exit 0 inside a worktree, and that is expected.**
> Measured in wave 0: `verify:capabilities` **refuses with exit 2** when `.env.local`
> is absent, printing *"Nothing was measured"* **before touching anything**. A
> worktree has no `.env.local`, and putting Supabase credentials into one is
> forbidden by **D-41.2-04** — the road that does not touch production. So the
> executor-side bar is:
>
> **all 15 gates that reach a verdict pass, 0 fail, and `verify:capabilities`
> refuses for want of credentials.** A refusal that names its missing input is a
> gate declining to measure, not a gate failing — and it is distinguishable from
> both a pass and a failure, which is the whole point. Quote the tail of the run
> showing the counts.
>
> **Do not make it green.** Not by adding credentials, not by skipping the gate,
> not by rewording the criterion. The full 16/16 suite is run by the orchestrator
> in the main checkout after this wave merges, where `.env.local` exists.

<objective>
Convert the bar's own QR surface: the page a guest opens standing at a bar, its
placeholder, the organizer-facing menu it mounts, and the signed-in token display.

Purpose: **the roadmap's premise about this surface was wrong, and the correction
changes what the plan guards.** The sentence said the bar *"reveals an address on
the same pages"* as money. Measured, the bar's QR surface reads **no venue at
all** — zero occurrences across all six of its own files, and no venue column in
any of its five queries. So the guard this plan carries is not the venue guard: it
is the **money** guard, and the observation that the failure here happens *at a
bar, at night, in front of a queue*. `checkin-offline.md`'s asymmetry has its
analogue: a guest who cannot find or use a token they paid for is a worse failure
than an ugly panel.

This surface is also the one page file in the phase whose container maximum is
**composed at run time**, inside a template literal, which is why its disposition
was taken in wave 0 and is only applied here. And it is one of only two pages in
the phase with no heading — the other being the landing page — so adopting the
title primitive would add a heading somebody reads while standing at a bar. That
is a copy decision and the plan states it rather than discovering it.

Output: four files, one placeholder off the pulse census, and a measured zero
still measured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-10-SUMMARY.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). The surface is
   declared by the final reconciliation, 41.2-19.
2. **41.2 does not edit the spine** — `layout/AppNav.tsx`, `layout/MobileNav.tsx`,
   `ui/Button.tsx`, `ui/Typography.tsx`. Needing to is a **HALT**.
3. **No server action is opened.** This directory has its own action module and
   the organizer-facing menu calls one of them. **Read it; do not edit it.**
   Needing to edit it is stop condition 2 — halt and report.
4. **Four files in this directory belong to other plans and are not opened here:**
   the guest menu and the guest token display are plan 41.2-12's, and the
   already-disabled guest banner was converted by plan 41.2-02. Two plans in one
   wave may not both open one file.
5. **The shared money core is not opened.** The token card, the redemption
   confirmation, the checkout panel and the provider mount were converted by plan
   41.2-10 in the wave before this one, precisely so this plan and two others stay
   separate plans. **Say so in the SUMMARY**, in the *converted earlier as spine*
   position.
6. **The already-converted work module this directory imports is not opened.** The
   organizer-facing menu imports a drinks manager from the work surface, converted
   by plan 41.1-10. That is the harmless direction — the bar inherits an
   already-converted component — and it stays that way.
7. **No package is installed.**
8. **DEF-41-01** — never spell a bracketed arbitrary-value utility in prose. The
   clearance declaration is copied verbatim from
   `src/app/(public)/gallery/page.tsx:110`.
9. **`.planning/` is public.** No venue name, no date, no amount, no drink price,
   no member and no token identifier in the SUMMARY.
10. **No `npm run dev`, no headless render** (D-41.2-04).
11. **⚠ `scripts/verify-conversion.mjs` is invisible to the default `grep`.**
    `LC_ALL=C /usr/bin/grep` or the Read tool.
12. **macOS/BSD.** `grep -E`, `sed -i ''`. **No test runner exists.**
13. **The money grep runs on every file this plan opens.** Filter the diff for
    `status`, `refund`, `amount`, `idempot`, `webhook`, `price`, `token` and quote
    every matching line with one sentence.

**Quick run:** `npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`

**Clean-file greps** (matcher `scripts/verify-conversion.mjs:850-916`): raw palette
`(bg|text|border|ring|from|to|via|fill|stroke|shadow|outline|decoration)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose|black|white)(-[0-9]{1,3})?`;
legacy token, same prefixes against `card-border|card|background|foreground`.

## The venue assertion for this surface, and it is a zero

`venue-secrecy.md` requires an exit enumeration rebuilt by **reading the code**,
because it is dated by construction. For this surface the enumeration is empty and
that is the finding: **re-measure it after the diff and record the zero with its
command.** A zero that is asserted from a document is worth nothing; a zero
re-measured on the tree this plan just moved is the whole guard.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: The QR page and its placeholder — shell, navigation pair, run-time maximum, and the heading question</name>
  <files>src/app/(public)/events/[slug]/menu/page.tsx, src/app/(public)/events/[slug]/menu/loading.tsx</files>
  <read_first>
    - `src/app/(public)/events/[slug]/menu/page.tsx` — 242 lines, in full. Its root is at `:147`; **its container maximum is composed inside a template literal at `:163`**, which makes it the one page file in the phase whose maximum is built at run time; it has **no** heading; its commented-out banner import sits at `:12-13` and the commented render at `:175-182`; it mounts the phone-locked wrapper at `:7` and `:231`
    - `src/app/(public)/events/[slug]/menu/loading.tsx` — 40 lines, 12 legacy tokens, **6** hand-rolled pulses
    - **The analog:** `src/app/(admin)/admin/(work)/events/[id]/drinks/page.tsx`, converted by plan 41.1-10 — the work-side twin of this surface
    - **The placeholder analog:** `src/app/(admin)/admin/(work)/members/loading.tsx:36-42,70-75`, and `src/components/ui/Skeleton.tsx:20-53,93,123,160,191`
    - **The public navigation analog:** `src/app/(public)/gallery/page.tsx:88-133`
    - **The already-converted sibling in this very directory:** `src/app/(public)/events/[slug]/menu/EventQRCode.tsx:29`, converted by plan 41.1-10 — the card primitive's call site, in this directory, already
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md` — the disposition for the run-time maximum. **Apply it; do not take it**
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.1 (the third divergent root), §2.2, §3 rows 1, 2, 3, 5, 10
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-RESEARCH.md` assumption **A6** — that adding a heading here is a copy decision and not a defect, and that a deliberately headless reading would need a declared exception
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md` §2, §4, §11
  </read_first>
  <action>
Adopt `PageShell` at `width="default"`.

**The maximum is composed at run time and that is what makes it different.** Check
D reads a page file's live lines and any maximum token it finds that is not a
declared measure is a failure — and a token inside a template literal is still a
token in the file. Apply plan 41.2-01's disposition exactly, and then **assert the
resulting count**. If the disposition is *delete*, the shell owns the measure and
the template literal loses that fragment; if it is *declare*, the entry's fragment
must survive this conversion, and if it does not, **say so loudly rather than
adjusting anything** — plan 41.2-19 owns the gate file.

Apply the shell adoption's deletions: the root, the repeated horizontal padding,
and the header's bottom padding to a margin.

**The heading is a decision and this plan takes it in writing.** This surface has
none today. Two readings:

- **Adopt the title primitive**, which gives the surface a heading a guest reads
  while standing at a bar. Then **state the exact string in the SUMMARY**, and
  take it from what the page already displays as its dominant line rather than
  inventing one. §11 owns the copy contract: *none introduced*.
- **Keep it headless**, on the ground that this is a scan-and-act surface rather
  than a page somebody reads. Then say so **as a declared exception**, in a
  sentence, so the next reader finds a decision instead of an omission.

Either is acceptable; **choosing silently is not.**

Then apply **the navigation pair**: a direct import of `AppNav` replacing the
phone-locked wrapper, same four props in the same order, verbatim analog
`src/app/(public)/gallery/page.tsx:2` and `:125-132`; plus the declaring `div`
around the **shell**, copied verbatim from `src/app/(public)/gallery/page.tsx:110`,
with the navigation as its **sibling**, moved up a level out of the deleted page
root. **Mounting means importing `AppNav` directly.** Width may change layout,
never membership.

**Leave the disabled banner disabled.** The import at `:12-13` and the render at
`:175-182` stay commented. The file it refers to was converted in wave 0 precisely
so that the re-enable path lands already converted; **restoring it is a product
decision and it is not this plan's.** Assert all the commented references are
still commented after the diff.

**The placeholder** takes the list analog and deletes all six hand-rolled pulses.
Its width must equal the page's — `default` — or content jumps sideways when data
lands; every count in it is a literal that stands for nothing, because no query
runs before a placeholder renders; and no radius is appended to the line primitive
(DEF-41-05, still open).
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-no-viewport-read.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'layout/AppNav' 'src/app/(public)/events/[slug]/menu/page.tsx'` returns exactly **1**; `layout/MobileNav` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'nav-inset-inline-start' 'src/app/(public)/events/[slug]/menu/page.tsx'` returns exactly **1**, byte-identical to `src/app/(public)/gallery/page.tsx:110` — both lines pasted
    - The declaring `div` wraps the shell and the navigation is its sibling — quote the JSX; the width is `default`; the four props are quoted before and after; the SUMMARY carries *no capability check touched*
    - `LC_ALL=C /usr/bin/grep -c 'max-w-' 'src/app/(public)/events/[slug]/menu/page.tsx'` matches the wave-0 disposition exactly, with the findings line quoted; the template literal that composed it is quoted before and after
    - **The heading decision is stated in the SUMMARY in at least one paragraph**, with the exact string if one was added, or as a declared exception if the surface stays headless. `LC_ALL=C /usr/bin/grep -c '<h1'` is reported either way
    - `LC_ALL=C /usr/bin/grep -n 'GuestLoginBanner' 'src/app/(public)/events/[slug]/menu/page.tsx'` shows every hit still inside a comment — paste the lines
    - `LC_ALL=C /usr/bin/grep -ci 'venue\|address' 'src/app/(public)/events/[slug]/menu/page.tsx'` returns **0**, re-measured after the diff with the command recorded
    - `LC_ALL=C /usr/bin/grep -c 'animate-pulse' 'src/app/(public)/events/[slug]/menu/loading.tsx'` returns **0**; `components/ui/Skeleton` returns at least **1**; the placeholder's shell width is `default`, equal to the page's — quote both lines; no radius is appended to a line primitive's call site
    - The raw-palette grep returns **0** and the legacy-token grep returns **0** for both files; `focus:outline-none`, `sm:` each return **0**
    - **The money grep is clean and quoted line by line** for the page file
    - `npm run build` exits 0; `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan)
  </acceptance_criteria>
  <done>The bar's QR page is in the shell with the navigation pair, its run-time maximum is in the state wave 0 specified, its heading is a written decision either way, and the surface still reads no venue — re-measured on the tree this task just moved.</done>
</task>

<task type="auto">
  <name>Task 2: The organizer-facing menu and the signed-in token display</name>
  <files>src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx, src/app/(public)/events/[slug]/menu/UserTokenDisplay.tsx</files>
  <read_first>
    - `src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx` — 262 lines, in full. It imports the drinks manager from the work surface at `:6` — **an already-converted work module, converted by plan 41.1-10, and it is not opened here** — and it calls an action from this directory's own module
    - `src/app/(public)/events/[slug]/menu/UserTokenDisplay.tsx` — 56 lines, in full. Measured: zero raw palette, zero legacy tokens on its own lines. Verify, then say so
    - **The analog:** `src/app/(admin)/admin/events/[id]/drinks/DrinkMenuManager.tsx`, converted by plan 41.1-10 — **its own import**, which makes it the closest analog any file in this phase has
    - `src/components/ui/Card.tsx:47`, `src/components/ui/Button.tsx:215`, `src/components/ui/Chip.tsx:58,161`, `src/components/ui/Switch.tsx:165`
    - `src/app/(public)/events/[slug]/menu/actions.ts` — **read it, do not edit it.** Knowing what the action does is how you tell a rendering change from a payload change. It is already inside another declared surface's closure and it carries no class strings
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §1c rows 21 and 24, §2.5 (the control ladder, and that **no file in this perimeter carries a hand-written switch track** — all four were inside the previous phase's scope), §3 rows 7, 8, 9, 10
    - `.claude/rules/ticketing-payments.md` — the closing time of a menu decides when a token can still be bought, and a grace period decides when one can still be redeemed
  </read_first>
  <action>
Convert both onto the primitives: card shells become `Card`, controls become
`Button` at their size rungs, interactive pills become `Chip` at the floor, focus
suppression becomes the **imported** focus-ring expression, and any hand-rolled
status box becomes a status region with its alert role in the semantic token.

**The switch primitive exists and this perimeter has no consumer for it.** All
four hand-written switch tracks in the tree were inside the previous phase's
scope. If this file turns out to carry one after all, adopt the primitive rather
than converting the hand-written track — and **say that the measurement had
drifted**, with the command and the count.

**The closing time is a payload, not a decoration.** The organizer-facing menu
sets when the menu closes, which decides when a token can still be bought and,
through the grace period, when one can still be redeemed. **Read the action so you
can tell a rendering change from a payload change, then change nothing about what
is sent** — not the field, not its units, not its default, not the grace window. If
the conversion appears to need any of that, **halt and return to the owner**:
stop condition 2.

**The signed-in token display carries nothing to convert if the measurement
holds.** Run both greps, record both counts, and if both are zero assert an empty
diff for that file. If either is non-zero, convert what the grep found and **state
that the prior measurement no longer held**, with the command and the count.

**Neither file reads a venue.** Re-measure and record the zero after the diff.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-dialogs.mjs && node scripts/verify-breakpoints.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The raw-palette grep returns **0** and the legacy-token grep returns **0** for `PartyDrinkMenu.tsx`; `focus:outline-none`, `sm:`, `max-w-`, `animate-pulse` each return **0**
    - For `UserTokenDisplay.tsx`: both greps are run and **both counts recorded**. If both are 0, `git diff` on the file is asserted **empty**; if either is non-zero, the drift is stated with its command
    - `LC_ALL=C /usr/bin/grep -c 'DrinkMenuManager' 'src/app/(public)/events/[slug]/menu/PartyDrinkMenu.tsx'` returns at least **1**, and `git status --porcelain` shows **no** modification to that work module — the harmless direction held
    - `git status --porcelain -- 'src/app/(public)/events/[slug]/menu/actions.ts'` outputs nothing
    - `git diff` filtered for the closing-time field, its units and the grace window shows **no** change — the filter used and its empty result are pasted
    - **The money grep is clean and quoted line by line** for both files
    - `LC_ALL=C /usr/bin/grep -ci 'venue\|address'` returns **0** in both, re-measured after the diff
    - `git status --porcelain` shows exactly the four files this plan declares across its two tasks, and **nothing** for the guest menu, the guest token display, the guest banner, or any of the four shared money-core files
    - `npm run build` exits 0; `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan)
  </acceptance_criteria>
  <done>The organizer-facing menu is on the primitives with its closing-time payload untouched, the signed-in token display is measured rather than assumed, and the already-converted work module it imports was not opened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest at a bar → the QR surface | opened on a phone, at night, with a queue behind; a failure here is expensive in a way an ugly panel is not |
| organizer → the closing time | this field decides when a token can still be bought and, through the grace period, when one can still be redeemed |
| this directory → an already-converted work module | the harmless direction: the bar inherits a converted component and does not open it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-65 | Information disclosure | a venue read arriving on the bar's surface | mitigate | the measured zero is **re-measured after the diff** on all four files with the command recorded, because a zero asserted from a document is worth nothing |
| T-41.2-66 | Tampering | the closing time, its units, its default or the grace window | mitigate | the action is read and not edited; the diff is filtered for all four and the empty result pasted; a needed change is stop condition 2 and halts |
| T-41.2-67 | Repudiation | a check-D red from a maximum composed inside a template literal | mitigate | the wave-0 disposition applied and the resulting count asserted; a fragment the conversion moved is reported rather than repaired from a plan that does not own the gate |
| T-41.2-68 | Repudiation | a heading appearing on a bar surface without anybody deciding it | mitigate | the decision is written either way — the exact string, or a declared headless exception |
| T-41.2-69 | Elevation of privilege | a disabled guest feature re-enabled under cover of a conversion | mitigate | every commented reference asserted still commented after the diff and pasted |
| T-41.2-70 | Denial of service | content under a 224px column at 768px and above | mitigate | the pair lands in one commit; the declaration asserted byte-identical to the gallery specimen |
| T-41.2-71 | Repudiation | two plans in one wave opening one file | mitigate | the guest path, the money core and the work module are all excluded by name; `git status --porcelain` asserts exactly four files |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run verify` reaches a verdict on 15 gates with **0 failures**, and `verify:capabilities` refuses for want of `.env.local` (expected in a worktree — see the note at the top of this plan)
- `git status --porcelain` shows exactly four modified files
- The venue zero is re-measured after the diff on all four files, with the command
- The money grep is quoted line by line
- The declaration line is pasted against the gallery specimen
</verification>

<success_criteria>
The bar's QR surface is converted whole except for its guest path, mounts the
responsive navigation with its clearance declared, still reads no venue after the
diff, has a written decision about its heading, and its closing-time payload is
byte-identical.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-11-SUMMARY.md`
when done, with the declaration line against the gallery specimen, the navigation
prop block, the maximum's state with the findings line, the heading decision, the
re-measured venue zeros with their command, the money grep, and the commented
banner references pasted.
</output>
