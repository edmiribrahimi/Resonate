---
phase: 41.2-public-member-and-money-surfaces
plan: 18
type: execute
wave: 7
depends_on: ["41.2-10", "41.2-11", "41.2-12", "41.2-13", "41.2-14"]
files_modified:
  - src/components/media/MediaUpload.tsx
  - src/app/(public)/events/[slug]/MediaGallerySection.tsx
  - src/app/(public)/events/[slug]/RsvpButton.tsx
  - src/app/(public)/events/[slug]/ShareButton.tsx
  - src/app/(public)/events/[slug]/loading.tsx
autonomous: true
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "The heaviest single component in the perimeter is converted, and the bucket it must never name is still unnamed"
    - "The share control still shares what it shared — no venue, no address, no map link reaches a clipboard or a share sheet that did not before"
    - "The last remaining grid-axis breakpoint use gains the middle step rather than being deleted"
    - "The placeholder in front of the event page is the primitive, at the page's own width"
  artifacts:
    - path: "src/components/media/MediaUpload.tsx"
      provides: "the upload surface converted, its bucket still unnamed, its grid axis migrated rather than deleted"
    - path: "src/app/(public)/events/[slug]/loading.tsx"
      provides: "the event page placeholder on the Skeleton primitive at the page's own width"
      contains: "components/ui/Skeleton"
  key_links:
    - from: "src/app/(public)/events/[slug]/MediaGallerySection.tsx"
      to: "src/components/media/Lightbox.tsx"
      via: "the declared permanent exemption, which this file already delegates to"
      pattern: "media/Lightbox"
---

<objective>
Convert the remaining satellites of the public event page: the upload surface, the
gallery section, the RSVP and share controls, and the placeholder.

Purpose: **the upload surface is the heaviest single component in the perimeter**
— 715 lines, the largest raw-palette count of any component, and the one most
likely to be under-scoped in a plan. It is also the subject of a gate outside the
conversion family: a check asserting that this file **names the public bucket
nowhere.** That is trivially true of a class-string change and it is stated here
so it stays true.

It also holds the **last of the tree's sixteen third-breakpoint uses that the
dialog primitive does not absorb**: a grid axis. The other fifteen are a dialog
boundary the primitive deletes, or a type size. This one is a genuine layout
decision and it **gains the middle step** rather than being deleted — there is a
converted grid in this tree that shows exactly what that looks like.

The share control is the quiet risk. It is the one file in this satellite set that
can put a string on a clipboard or into a system share sheet, and a share sheet is
a publication. **What it shares must be what it shared.**

Output: five files, one placeholder off the pulse census, and the phase's last
loose breakpoint use resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-CONTEXT.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md
@.planning/phases/41.2-public-member-and-money-surfaces/41.2-WAVE0-FINDINGS.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
</context>

<conversion_rules>
1. **This plan edits no file under `scripts/`** (D-41.1-22). The surface is
   declared by the final reconciliation, 41.2-19.
2. **41.2 does not edit the spine.** Needing to is a **HALT**.
3. **No server action is opened.** The RSVP control calls the one file in the whole
   fifty-one carrying the server directive, and that file has **zero class
   attributes**. **Read it; do not edit it.** Needing to is stop condition 2 —
   halt and report.
4. **`src/components/media/Lightbox.tsx` is READ, not edited.** It is the declared
   permanent exemption; the gallery section already delegates to it, and that
   delegation is the shape plan 41.2-14 was asked to copy.
5. **The RSVP confirmation e-mail template is not opened.** It uses inline styles
   rather than utilities and is not a conversion target; its palette is deferred by
   an explicit owner decision from a previous phase.
6. **Files in this directory belonging to other plans and NOT opened here:** the
   page file is plan 41.2-15's, the secret-venue dialog is plan 41.2-16's, and the
   money satellites are plan 41.2-17's. All three run **in this same wave, on
   disjoint file sets.** The four shared money-core files were converted by plan
   41.2-10 in wave 5 as spine.
7. **No package is installed.**
8. **DEF-41-01** — never spell a bracketed arbitrary-value utility in prose.
9. **`.planning/` is public.** No venue, no address, no map link, no unannounced
   date, no line-up, no member and no media file name in the SUMMARY.
10. **No `npm run dev`, no headless render** (D-41.2-04).
11. **⚠ `scripts/verify-conversion.mjs` is invisible to the default `grep`.**
    `LC_ALL=C /usr/bin/grep` or the Read tool.
12. **macOS/BSD.** `grep -E`, `sed -i ''`. **No test runner exists.**

**Quick run:** `npm run verify:conversion && npm run verify:dialogs && npm run verify:breakpoints && npm run verify:touch-targets`

**Clean-file greps** (matcher `scripts/verify-conversion.mjs:850-916`): raw palette
`(bg|text|border|ring|from|to|via|fill|stroke|shadow|outline|decoration)-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose|black|white)(-[0-9]{1,3})?`
— with the one tolerated exception, a **translucent** black scrim through the
background prefix, which a full-bleed viewer legitimately uses;
legacy token, same prefixes against `card-border|card|background|foreground`.
</conversion_rules>

<tasks>

<task type="auto">
  <name>Task 1: The upload surface — the heaviest component, the unnamed bucket, and the grid axis</name>
  <files>src/components/media/MediaUpload.tsx</files>
  <read_first>
    - `src/components/media/MediaUpload.tsx` — 715 lines, in full. It carries 22 raw palette utilities, 4 legacy tokens and **the tree's last third-breakpoint use that no primitive absorbs**: a grid axis at `:624`
    - **The analog:** `src/components/media/MediaReviewGrid.tsx`, converted by plan 41.1-09 — the closest converted component, and **`:154` is the exact specimen for the grid axis**: a one-column base gaining the middle step and then the wide step, with no third-breakpoint prefix anywhere
    - `scripts/verify-media-strip.mjs` **check C** — *this file names the public bucket nowhere.* Read the check so the assertion after the diff is the gate's own, not a paraphrase
    - `src/lib/media/strip-metadata.ts` — **read it, do not edit it.** It is what the upload path runs before anything leaves the browser
    - `src/components/ui/Button.tsx:71,215`, `src/components/ui/Input.tsx`, `src/components/ui/Card.tsx:47`, `src/components/ui/Skeleton.tsx:93`
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §1c row 47, §3 rows 7, 8, 9, **6c**, and §7.4 — **the split is 13 shell uses / 2 type-size uses / 1 grid axis, and this is the grid axis**
    - `.claude/rules/media-and-storage.md` if present, and `.claude/rules/meta-gates.md`
  </read_first>
  <action>
**Scope it honestly first.** This is 715 lines and the largest raw-palette count of
any component in the phase; plan 41.2-01's dry run recorded check A's and check
B's hits **per file**. Take that list, convert every one of them, and then compare
your result with the recorded number in the SUMMARY. **A conversion whose result
does not match the measurement is either incomplete or the measurement was
stale**, and the difference is worth stating.

Convert onto the primitives: fields become `Input` and its siblings, controls
become `Button` at their size rungs, card shells become `Card`, an inline
placeholder becomes the line primitive at its call site, focus suppression becomes
the **imported** focus-ring expression, and any hand-rolled status box becomes a
status region with its alert role in the semantic token.

**The grid axis gains the middle step; it is not deleted.** The other fifteen
third-breakpoint uses in this tree are a dialog boundary the primitive owns, or a
type size. This one is a real layout decision about how many columns a preview
grid has. Take the converted grid's shape verbatim — a one-column base, the middle
step, then the wide step, no third-breakpoint prefix anywhere — and say in the
SUMMARY that this use was **migrated** rather than absorbed, which is the
distinction §7.4 corrects.

**The bucket stays unnamed.** A gate asserts this file names the public bucket
nowhere. That is trivially preserved by a class-string change, and it is asserted
after the diff by running the gate rather than by reasoning about it.

**The metadata-stripping path is not touched.** It is what runs before anything a
member uploads leaves their browser. Read it; change nothing that calls it, and
nothing about when it is called. Assert by filtered diff.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-media-strip.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY states the pre-conversion check-A and check-B counts for this file from plan 41.2-01's dry run beside the post-conversion counts, and says whether they agreed
    - The raw-palette grep returns **0**; the legacy-token grep returns **0**; `focus:outline-none`, `max-w-`, `animate-pulse` each return **0**
    - `LC_ALL=C /usr/bin/grep -c 'sm:' src/components/media/MediaUpload.tsx` returns **0**, and the SUMMARY states the use was **migrated to the middle step**, not deleted, quoting the before and after class strings
    - `node scripts/verify-media-strip.mjs` exits 0, and its **check C output is pasted** — the bucket is still named nowhere, asserted by the gate rather than by reasoning
    - `git status --porcelain -- src/lib/media/strip-metadata.ts` outputs nothing, and the diff filtered for the stripping call and its call site shows no change — the filter and its empty result are pasted
    - `node scripts/verify-breakpoints.mjs` exits 0 and reports this file below its declared count, STALE; the line is pasted
    - `LC_ALL=C /usr/bin/grep -ci 'venue\|address' src/components/media/MediaUpload.tsx` returns **0**
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The perimeter's heaviest component is converted against a measured number rather than a feeling, the tree's last unabsorbed breakpoint use gained a middle step, and the bucket the gate watches is still unnamed — asserted by the gate.</done>
</task>

<task type="auto">
  <name>Task 2: The gallery section, the RSVP control and the share control</name>
  <files>src/app/(public)/events/[slug]/MediaGallerySection.tsx, src/app/(public)/events/[slug]/RsvpButton.tsx, src/app/(public)/events/[slug]/ShareButton.tsx</files>
  <read_first>
    - `src/app/(public)/events/[slug]/MediaGallerySection.tsx` — 83 lines, in full. **It already delegates its viewer to the declared permanent exemption**, and it is the specimen plan 41.2-14 was asked to copy for the dashboard's media section
    - `src/app/(public)/events/[slug]/RsvpButton.tsx` — 80 lines, in full. It carries six raw palette utilities and calls the one server-directive file in the perimeter
    - `src/app/(public)/events/[slug]/ShareButton.tsx` — 55 lines, in full. **Read every string it can put on a clipboard or into a system share sheet, and where each comes from**
    - `src/app/(public)/events/[slug]/rsvp-actions.ts` — **read it, do not edit it.** It is the only file among the fifty-one carrying the server directive and it has **zero class attributes**; needing to edit it is stop condition 2
    - `src/components/media/Lightbox.tsx` — **read it, do not edit it**
    - **The analogs:** `src/app/(public)/gallery/GalleryClient.tsx`, converted by plan 41-08, for the gallery section; `src/components/ui/Button.tsx:215` for both controls
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §1c rows 46, 48, 50, §3 rows 7, 8, 12
    - `.claude/rules/venue-secrecy.md` — **a share sheet is a publication**, and the editorial counterpart of the same rule: an address in a story annuls a scheduled reveal
  </read_first>
  <action>
Convert all three onto the primitives: controls become `Button` at their size
rungs, card shells become `Card`, focus suppression becomes the **imported**
focus-ring expression, and any hand-rolled status box becomes a status region with
its alert role in the semantic token.

**The gallery section keeps its delegation.** It already hands its full-bleed
viewer to the exempt component, which is the correct road; the conversion is
around that, not through it. Quote the delegation before and after.

**The RSVP control's payload does not move.** Read the action so a rendering
change can be told from a payload change, then change nothing about what is sent
and nothing about the conditions under which the control is drawn or refuses.
Note the domain fact while you are there: **an RSVP unlocks a venue hint
regardless of the reveal-on-purchase flag**, which is a deliberate asymmetry
recorded elsewhere in this tree — so a change to when this control is drawn is a
change to who is told where a night is. It is not this plan's, and the plan asserts
it did not happen.

**The share control is the quiet risk and it gets its own assertion battery.**
Enumerate **every** string it can put on a clipboard or into a share sheet, and
where each one comes from. Assert, after the diff, that:

- the set of strings is the same set, quoted before and after;
- **no venue name, no address and no map link is among them**, by grep on the
  file and by reading each source;
- nothing was added to the share payload — not a title, not a description, not an
  image — that reads from a night's venue fields.

A share sheet is a publication, and a publication of an address is not
recallable. If the conversion appears to need any change to what is shared, **stop
and report.**
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-touch-targets.mjs && node scripts/verify-dialogs.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The raw-palette grep returns **0** and the legacy-token grep returns **0** in all three files; `focus:outline-none`, `sm:`, `max-w-`, `animate-pulse` each return **0**
    - `LC_ALL=C /usr/bin/grep -c 'media/Lightbox' 'src/app/(public)/events/[slug]/MediaGallerySection.tsx'` returns at least **1**, and the delegation is quoted before and after
    - `git status --porcelain -- src/components/media/Lightbox.tsx 'src/app/(public)/events/[slug]/rsvp-actions.ts'` outputs nothing
    - The RSVP control's action call and the conditions under which it is drawn or refuses are quoted before and after and asserted byte-identical
    - **The share control's assertion battery is complete:** every shareable string enumerated with its source, the set quoted before and after and asserted equal, and `LC_ALL=C /usr/bin/grep -ciE 'venue|address|maps|location'` on the file returning **0**
    - Nothing was added to the share payload — the diff of the payload construction is quoted whole
    - `git status --porcelain` shows exactly the files this plan declares, and nothing for the page, the secret-venue dialog, the money satellites, any money-core file, the e-mail template, or anything under `scripts/` and `src/app/api/`
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The gallery keeps delegating to the exempt viewer, the RSVP control's payload and its drawing conditions are byte-identical, and every string the share control can publish is enumerated and proved to carry no place.</done>
</task>

<task type="auto">
  <name>Task 3: The event page's placeholder — nine pulses, at the page's own width</name>
  <files>src/app/(public)/events/[slug]/loading.tsx</files>
  <read_first>
    - `src/app/(public)/events/[slug]/loading.tsx` — 56 lines, 15 legacy tokens, **nine** hand-rolled pulses
    - **The analog:** `src/app/(admin)/admin/(work)/members/loading.tsx:36-42,70-75`, converted by plan 41-10 — the list form; and `src/components/ui/Skeleton.tsx:20-53,93,123,160,191` for the four names and the three constraints that travel with them
    - `.planning/phases/41.2-public-member-and-money-surfaces/41.2-PATTERNS.md` §2.2 — the five placeholders, their pulse counts and their widths; and §3 row 3, which records that the primitive was correct and had **zero importers** for the whole of its existence while a hundred hand-rolled pulses lived in twenty files
    - The page file after plan 41.2-15's diff — **read it for its declared width, do not open it**
  </read_first>
  <action>
Take the list analog and delete all nine hand-rolled pulses. Three constraints
travel with the primitive:

1. **The placeholder's width must equal the page's** — `default`, as plan 41.2-15
   declared it — or content jumps sideways when data lands. Read the page's width
   from the converted page file rather than assuming it.
2. **Every count is a literal and stands for nothing.** No query runs before a
   placeholder renders, so the number of rows is a shape and never a claim about
   how many tiers, drinks or media items a night has.
3. **Do not append a radius to the line primitive** — DEF-41-05, still open,
   measured in the emitted sheet.

**The placeholder shows no venue, and it must not begin to.** A placeholder that
stands in front of a venue block should show a neutral shape, never a labelled one
that says an address is coming — that is a statement about a night made before
anything was read. Assert by grep that the file names no venue.

**This is the phase's last placeholder.** After it, the only hand-rolled pulses
left in the tree are the primitive's own and the scanner's, which is Phase 42's.
State that arithmetic in the SUMMARY with the command that produced it, so the
next phase inherits a number rather than a claim.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `LC_ALL=C /usr/bin/grep -c 'animate-pulse' 'src/app/(public)/events/[slug]/loading.tsx'` returns **0**
    - `LC_ALL=C /usr/bin/grep -c 'components/ui/Skeleton'` returns at least **1**, and the placeholder's shell width equals the page's — quote both lines, the page's from the converted page file
    - No radius is appended to a line primitive's call site — quote each call site
    - `LC_ALL=C /usr/bin/grep -ci 'venue\|address'` returns **0**
    - The raw-palette grep returns **0**; the legacy-token grep returns **0**; `focus:outline-none`, `sm:`, `max-w-` each return **0**
    - **The tree-wide arithmetic is stated with its command:** the remaining count of hand-rolled pulses under `src/`, and the statement that the survivors are the primitive's own and the scanner's
    - `git status --porcelain` shows exactly the five files this plan declares across its three tasks
    - `npm run build` exits 0; `npm run verify` exits 0 with no refusal
  </acceptance_criteria>
  <done>The event page's placeholder is the primitive at the page's own width, its counts stand for nothing, it names no venue, and the tree's remaining hand-rolled pulse count is a measured number the next phase inherits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member's device → the storage bucket | the upload path strips metadata before anything leaves the browser, and a gate asserts the bucket is named nowhere in this component |
| share sheet / clipboard → the public | a share is a publication and a published address is not recallable |
| RSVP → a venue hint | an RSVP unlocks a hint regardless of the reveal-on-purchase flag, so when this control is drawn is a secrecy question |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.2-117 | Information disclosure | a venue name, address or map link entering a share payload | mitigate | every shareable string enumerated with its source, the set asserted equal before and after, a place-word grep asserted **0**, and the payload construction quoted whole |
| T-41.2-118 | Information disclosure | the public bucket named in the upload component | mitigate | asserted by **running the gate** and pasting its check C output, not by reasoning about a class-string change |
| T-41.2-119 | Tampering | the metadata-stripping call or its call site | mitigate | the module asserted unmodified and the call filtered out of the diff with an empty result pasted |
| T-41.2-120 | Information disclosure | the conditions under which the RSVP control is drawn | mitigate | quoted before and after and asserted byte-identical, with the domain fact stated: an RSVP unlocks a hint regardless of the reveal-on-purchase flag |
| T-41.2-121 | Repudiation | a breakpoint use deleted where it should have been migrated | mitigate | the grid axis migrated to the middle step with the before and after class strings quoted, and the distinction stated against the corrected 13/2/1 split |
| T-41.2-122 | Repudiation | a conversion declared complete on the perimeter's heaviest file | mitigate | the result compared against plan 41.2-01's per-file measured counts, with any disagreement stated as either an incomplete conversion or a stale measurement |
| T-41.2-123 | Information disclosure | a placeholder announcing that an address is coming | mitigate | the placeholder's shapes are neutral and a place-word grep is asserted **0** |
| T-41.2-124 | Repudiation | two plans in one wave opening one file | mitigate | the page, the secret-venue dialog, the money satellites, the exempt viewer, the server action and the e-mail template are excluded by name and asserted unmodified |
| T-41.2-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages proposed or installed |
</threat_model>

<verification>
- `npm run build` exits 0; `npm run verify` exits 0 with no refusal
- `git status --porcelain` shows exactly five modified files
- The media-strip gate's check C output is pasted
- The share control's enumeration is complete, with the place-word grep at zero
- The grid axis's before and after class strings are quoted
- The tree-wide remaining pulse count is stated with its command
</verification>

<success_criteria>
The public event page's remaining satellites are converted; the perimeter's
heaviest component is cleared against a measured number with its bucket still
unnamed; the tree's last unabsorbed breakpoint use gained a middle step instead of
disappearing; every string the share control can publish is proved to carry no
place; and the phase's last placeholder is the primitive at the page's own width.
</success_criteria>

<output>
Create `.planning/phases/41.2-public-member-and-money-surfaces/41.2-18-SUMMARY.md`
when done, with the pre/post counts for the heaviest component, the media-strip
check C output, the grid axis's class strings before and after, the share
control's full enumeration with its place-word grep, the delegation quoted, the
RSVP conditions before and after, and the tree-wide remaining pulse count.
</output>
