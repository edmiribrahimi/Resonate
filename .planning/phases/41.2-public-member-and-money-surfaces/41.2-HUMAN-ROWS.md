---
phase: 41.2-public-member-and-money-surfaces
kind: ledger
opened: 2026-08-14
opened_by: plan 41.2-05, the wave-2 reconciliation
status: open — a RUNNING LEDGER, appended to by each reconciliation wave. Not a pass document, and it closes nothing
covers: the surfaces each reconciliation wave has declared, at three widths, plus the dialog behaviours those surfaces mount
closes: nothing. It ticks no requirement, closes no criterion, and replaces no observation already owed
requires: a checkout holding `.env.local`, and `npm run dev`. No worktree in this phase holds one — D-41.2-04
batch: the end-of-v1.5 human sitting — the one `41-RELEASE-PASS.md`'s `batch:` field already commits, alongside `40-RELEASE-PASS.md`, `39-DOOR-PASS.md` and `38-PROCEDURES.md`. Not a second sitting
role: the owner, on their own machine. Roles, never names — this repository is public and `.planning/` is tracked
---

# Phase 41.2 — The Human-Row Ledger

> **(a) NO ROW BELOW WAS MEASURED DURING EXECUTION, and the reason is a decision
> rather than an omission.** **D-41.2-04** forbids this worktree running the
> application: no worktree in this phase holds `.env.local`, the middleware reads
> Supabase credentials on every request so nothing renders without them, and
> supplying them would point a running application at production. This repository
> has an incident behind that sentence — **D12**, in which a verification deleted
> 63 rows across seven tables, none of which came back because PITR is not
> enabled. The safe default is also the road that does not touch production, and
> when those two coincide the decision is not close.
>
> **(b) DEFERRED IS NOT VERIFIED.** An empty Result is an **unrun procedure**, and
> never a verified-by-inspection in disguise. The words *approved*, *verified* and
> *observed* appear in this document only in prose about what they are **not**.
>
> **(c) Every criterion is a NUMBER WITH A TOLERANCE.** A row written as *looks
> right* is a row that will be ticked without being looked at. Where a criterion
> below could have been an adjective it is a measurement instead, on
> `41-CR01-PASS.md`'s own rule — *"how to measure, so the result is a number"*.
>
> **(d) It is a LEDGER, not a pass.** Each reconciliation wave appends its wave's
> rows; **plan 41.2-20 turns the accumulated rows into the two documents of
> D-41.2-05** — `41.2-RELEASE-PASS.md`, which closes the phase gate on this
> phase's own work, and the RESP-01 closing pass, whose rows are the union of the
> thirteen already owed plus this phase's surfaces. This document is neither of
> them and closes neither.
>
> **(e) Roles, never names.** Surfaces are named by their **route**. No account is
> described by whose it is, no real credential appears, and no artist, venue or
> date is named: `.planning/` is tracked and this repository is public.
>
> **(f) Class strings are described, never spelled.** DEF-41-01: Tailwind's source
> detection scans `.planning/` and compiles a class string out of prose into the
> shipped stylesheet. So the leading column clearance below is **224px**, by its
> measurement, and the declaration itself is cited at
> `src/app/(public)/gallery/page.tsx:110` rather than written out.

---

## The three widths, and why these three

**390 / 768 / 1440** — the same three numbers `41-CR01-PASS.md` used, so a result
here is comparable with a result there without anybody converting between two
sets of viewports.

> **A divergence, recorded rather than reconciled by choosing quietly.**
> `41.2-VALIDATION.md`'s Manual-Only table names **390 / 768 / 1280**. This ledger
> uses 1440, on the plan's instruction and for comparability with the pass
> document that already exists. The two are not in conflict about anything
> measurable — every desktop criterion below is stated against the **viewport
> width in the row**, so it evaluates at either number — but the divergence is
> written down here so plan 41.2-20 settles it once for both output documents
> instead of meeting it twice.

---

## How to measure, so the result is a number

**The leading column clearance.** From 768px up the responsive navigation is a
**side column**, not a bottom bar, and the shell reserves **224px** on the leading
edge for it. Read the distance from the viewport's leading edge to the leftmost
pixel of any content. It must be **224px, within 8px**. Below 768px that same
distance must be **0px, within 4px** — a column reserved on a phone is the silent
half of the defect check E pairs against.

**Content centring inside the reserved area.** The shell centres a **1024px**
content column inside whatever is left after the clearance, with **24px** of its
own gutter each side. So read the space between the navigation column's trailing
edge and the content's leading edge, and the space between the content's trailing
edge and the viewport's trailing edge: **the two must differ by no more than
8px**, which puts the column's centre within 4px of the centre of the space it
lives in.

**Horizontal scroll.** Put a finger, or the trackpad, on the page body and drag
sideways. The page must move **0px**. Not *barely*, not *a little*: zero.

**The bottom clearance on a phone.** The shell reserves the navigation bar's own
height plus **16px**. Scroll to the end of the page: the last element's bottom
edge must clear the bar's top edge by **at least 16px**, within 4px.

**A control's height.** Read the box, not the ink. **44px is a MINIMUM, not a
fixed height** — a control whose label wraps to two lines is taller and is
correct; a control at 43px is not.

**Reaching the three surfaces.** `/` and `/newsletter` are public and need no
session. `/artists/[slug]` is public too, but its edit affordance is drawn only
for an account holding the organizer or master role — the dialog rows need such a
session, and **no row here creates, edits or deletes anything**: the dialog is
opened, looked at, and dismissed with Escape.

---

## §1 — Wave 1's rows: three surfaces × three widths

| # | Route | Width | What must be true | Result |
| --- | --- | --- | --- | --- |
| 1 | `/` | 390 | leading clearance 0px ±4px (a bottom bar, not a side column); page moves 0px when dragged sideways; the last element clears the bar's top edge by ≥16px ±4px; the three action controls are equal in width within 4px of the widest, and each is ≥44px tall; the wordmark's top edge sits 48px ±8px below the shell's top edge and is NOT vertically centred in the viewport | pending |
| 2 | `/` | 768 | leading clearance 224px ±8px, and nothing renders left of it; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways; the three action controls stay equal in width within 4px and ≥44px tall | pending |
| 3 | `/` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px wide; the space each side of it differs by ≤8px; page moves 0px when dragged sideways; the three action controls are as wide as the longest of the three within 4px, NOT 320px and NOT the full column | pending |
| 4 | `/newsletter` | 390 | leading clearance 0px ±4px; page moves 0px when dragged sideways; the last element clears the bar's top edge by ≥16px ±4px; exactly 1 top-level heading is rendered (count = 1, tolerance 0); the email field and the submit control are each ≥44px tall | pending |
| 5 | `/newsletter` | 768 | leading clearance 224px ±8px, nothing left of it; the form's entry column is 384px ±8px wide and its two side spaces inside the content column differ by ≤8px; page moves 0px when dragged sideways; exactly 1 top-level heading (count = 1, tolerance 0) | pending |
| 6 | `/newsletter` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; the form's entry column is 384px ±8px and centred inside that column within 4px — it does NOT widen to 1024px; page moves 0px when dragged sideways | pending |
| 7 | `/artists/[slug]` | 390 | leading clearance 0px ±4px; page moves 0px when dragged sideways; the last element clears the bar's top edge by ≥16px ±4px; the bio paragraph's line breaks are preserved exactly as typed (count of rendered breaks equals count in the stored text, tolerance 0); every link control is ≥44px tall | pending |
| 8 | `/artists/[slug]` | 768 | leading clearance 224px ±8px, nothing left of it; the bio paragraph is ≤512px wide (+8px) and does NOT run the full content column; page moves 0px when dragged sideways; every link control ≥44px tall | pending |
| 9 | `/artists/[slug]` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; **the bio paragraph is 512px ±8px wide — half the column, which is the whole content of the declared typographic measure**; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways | pending |

**Row 9 is the one that would catch the measure going wrong.** It is the only row
in this ledger whose number is asserted by a gate as well as by a person: check D
forgives that width because `TYPOGRAPHIC_MEASURES` names it, and if the paragraph
renders at 1024px the entry is forgiving something that is not there.

**Rows 2, 3, 5, 6, 8 and 9 are the ones that would catch check E's loud
direction** — content sliding **under** a 224px column at 768px and above. The
gate's pairing reads a class string and an import graph; it renders nothing, and
these six rows are the only thing that would show the clearance actually arriving.

---

## §2 — The dialog rows, asked of `/artists/[slug]` only

The phase's first dialog conversion is the artist edit form. **DS-08's three
behaviours are H41-2's question — a person, at two widths — and no gate in this
repository can see any of them.**

| # | Route | Width | What must be true | Result |
| --- | --- | --- | --- | --- |
| 10 | `/artists/[slug]` | 390 | with the edit panel open: its bottom edge sits within 4px of the viewport's bottom edge and its top edge is >0px below the viewport's top — it RISES FROM THE BOTTOM as a sheet, it is not a centred window | pending |
| 11 | `/artists/[slug]` | 768 | with the edit panel open: it is a WINDOW above the boundary — the space above it and the space below it differ by ≤8px, and its bottom edge is >8px clear of the viewport's bottom edge. One press of Escape dismisses it: visible panels after the press = 0, tolerance 0 | pending |
| 12 | `/artists/[slug]` | 390 | with the edit panel open, scroll the page behind it by three wheel notches or one full finger drag: the content behind moves **0px**, tolerance 0. Then dismiss with Escape: visible panels after = 0, tolerance 0 | pending |

**Row 12 is the one the platform is supposed to answer for.** The panel is on the
`Dialog` primitive now, so background inertness arrives from the platform's own
modal behaviour rather than from a copy of a scroll lock. If the page behind
moves, the primitive is not doing what the conversion claimed it does, and that
is a finding about the primitive rather than about this surface.

**No row above submits the form.** The panel is opened, measured, and dismissed
with Escape. Nothing is saved, so nothing is written — the same rule
`41-CR01-PASS.md` states as *"nothing here creates a row in production"*.

---

## §3 — What this ledger refuses to do

**It does not tick RESP-01, RESP-02 or RESP-03.** RESP-01 says *every* surface and
closes only across the whole of this phase, by the closing pass D-41.2-05 names;
RESP-03 is H41-4's question and needs a large touch screen, which no row here
assumes exists.

**It does not tick DS-08.** Rows 10–12 are three of that requirement's
observations on one surface. The requirement spans every dialog this phase
converts, and the rest arrive in later waves' appendices below.

**It does not close the phase gate.** That is `41.2-RELEASE-PASS.md`'s, and it
does not exist yet.

**It claims nothing.** Twelve rows, twelve unrun procedures. The word *approved*
is an authorisation, the word *verified* is a claim about files, and the word
*observed* belongs to a person who has looked. **None of the three applies to any
row above**, and an empty Result stays an unrun procedure until somebody writes a
number into it.

---

## §4 — Appendices, one per reconciliation wave

| Wave | Reconciliation | Surfaces appended | Rows added | Where |
| --- | --- | --- | --- | --- |
| wave 2 | plan 41.2-05 | `/`, `/newsletter`, `/artists/[slug]` | 12 | §1 and §2 above |
| wave 4 | plan 41.2-09 | `/events`, `/attendance`, `/membership-card`, `/tickets/[id]` | 13 | §5 and §6 below |

Later waves append their own section here rather than editing §1, so a row's
provenance stays readable: the wave that declared a surface is the wave that owes
its observation.

---

## §5 — Wave 3's rows: four surfaces × three widths

> **Appended by plan 41.2-09, the wave-4 reconciliation.** These four surfaces
> were converted by plans 41.2-06, 41.2-07 and 41.2-08 and declared in
> `CONVERTED` by this plan. **Not one row below was measured**, for the reason
> §(a) gives: D-41.2-04 forbids this worktree running the application.
>
> **Reaching them.** `/events` and `/tickets/[id]` are public and need no
> session — the ticket route needs a ticket identifier, which is **not written
> here**: this repository is public. `/attendance` and `/membership-card` are
> member surfaces and need a session holding the member role at approved
> status. **No row below creates, edits, purchases, refunds or deletes
> anything**, and no row scans a code at a door: rows 19–21 measure the box the
> code is drawn in, which is not the same statement as a phone having read it.

| # | Route | Width | What must be true | Result |
| --- | --- | --- | --- | --- |
| 13 | `/events` | 390 | leading clearance 0px ±4px (a bottom bar, not a side column); page moves 0px when dragged sideways; the last element clears the bar's top edge by ≥16px ±4px; exactly 1 top-level heading is rendered (count = 1, tolerance 0); each tab control, each format chip and each night card is ≥44px tall | pending |
| 14 | `/events` | 768 | leading clearance 224px ±8px, and nothing renders left of it; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways; **the nights still render ONE per row (count = 1, tolerance 0)** | pending |
| 15 | `/events` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px wide; the space each side of it differs by ≤8px; page moves 0px when dragged sideways; **the nights still render ONE per row (count = 1, tolerance 0)**, and each night card spans the content column within 8px rather than sitting at a narrower measure of its own | pending |
| 16 | `/attendance` | 390 | leading clearance 0px ±4px; page moves 0px when dragged sideways; the last element clears the bar's top edge by ≥16px ±4px; exactly 1 top-level heading (count = 1, tolerance 0); the empty-state card's inner padding is 24px ±2px on all four sides | pending |
| 17 | `/attendance` | 768 | leading clearance 224px ±8px, nothing left of it; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways; the empty-state card's inner padding stays 24px ±2px | pending |
| 18 | `/attendance` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; the space each side differs by ≤8px; page moves 0px when dragged sideways; the empty-state card spans the content column within 8px and its padding is still 24px ±2px | pending |
| 19 | `/membership-card` | 390 | leading clearance 0px ±4px; page moves 0px when dragged sideways; the last element clears the bar's top edge by ≥16px ±4px; **the code's drawn box is 192px ±4px square**; the wallet control and the referral input are each ≥44px tall | pending |
| 20 | `/membership-card` | 768 | leading clearance 224px ±8px, nothing left of it; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways; **the code's drawn box is STILL 192px ±4px square — it does not scale with the wider column** | pending |
| 21 | `/membership-card` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; the space each side differs by ≤8px; page moves 0px when dragged sideways; **the code's drawn box is STILL 192px ±4px square**; the instruction card's padding is 24px ±2px | pending |
| 22 | `/tickets/[id]` | 390 | leading clearance 0px ±4px; page moves 0px when dragged sideways; the last element clears the bar's top edge by ≥16px ±4px; **the ticket card and the wallet control are equal in width within 4px**; the wallet control and the back link are each ≥44px tall | pending |
| 23 | `/tickets/[id]` | 768 | leading clearance 224px ±8px, nothing left of it; the space each side of the content column differs by ≤8px; page moves 0px when dragged sideways; **the ticket card and the wallet control widened together and are STILL equal in width within 4px** | pending |
| 24 | `/tickets/[id]` | 1440 | leading clearance 224px ±8px; content column 1024px ±8px; the space each side differs by ≤8px; page moves 0px when dragged sideways; **the ticket card and the wallet control are still equal in width within 4px**, and the cover image reaches the card's own edge with 0px ±2px of inset on the leading and trailing sides | pending |

**Rows 14 and 15 are the two that decide whether a deferral was right.**
`/events` is the phase's one candidate for the wide form, and it was **deferred
and not rejected** on a measurement of the current markup: the nights render as
a single column, so §4's multi-column criterion is not met. If a person reads
more than one night per row at either width, the measurement the deferral rests
on has changed and the width is owed a second look — the change is one word in
the shell's call.

**Rows 19–21 are the door rows, and they are the strongest thing in this
appendix.** A code that shrinks because a column got wider is a code that fails
to scan at an entrance, and `checkin-offline.md` records the asymmetry: refusing
a valid guest is worse than admitting a duplicate, because the first error
happens in front of a queue. The conversion asserted 192px in **both** branches
by reading a class string; **a class string is not a drawn box**, and these three
rows are the only thing that would say otherwise.

**Rows 22–24 are H-41.2-08-1, written as numbers.** Both hand-written maxima
were deleted from that surface, together, on wave 0's disposition — *"both go
together or the button stops matching the card"*. Equality within 4px at all
three widths is that sentence made measurable; a difference is the disposition
having gone half-applied somewhere no gate can see.

---

## §6 — The placeholder row: does content shift sideways when the data lands?

**One row, and it is the only row in this ledger that measures a transition
rather than a state.** A placeholder at a different measure from the page it
precedes makes the content jump the moment the data arrives — the defect the
equal-width constraint exists to prevent, and one that no gate in this
repository can see, because both files are correct on their own.

| # | Route | Width | What must be true | Result |
| --- | --- | --- | --- | --- |
| 25 | `/events` | 768 | with the network throttled so the placeholder is visible for at least two seconds, note the distance from the viewport's leading edge to the leading edge of the FIRST placeholder card; when the nights land, note the same distance to the leading edge of the first night card. **The two distances differ by 0px, tolerance 0.** The same comparison on the trailing edge: **0px, tolerance 0** | pending |

**Why this row is `/events` and not `/membership-card`.** Both surfaces have a
placeholder, and both pass the same word to the shell. This one is chosen
because its placeholder took the **card** form rather than the tile grid its
analog assigns — a decision recorded as D-41.2-06-D, made because the nights
render in a single column and a tile grid would have guaranteed the very jump
this row measures. **A decision taken to prevent a jump is the decision that
should be asked to prove it.**

---

## §7 — The ledger's two counts, and what wave 2's §3 said about them

**§3 above says *"Twelve rows, twelve unrun procedures."* That sentence is wave
2's, and it was true of wave 2's ledger.** It is left standing rather than
rewritten, on the same rule that governs the manifest's reason texts: the wave
that wrote a sentence is the wave that owns it, and an editorial fix to somebody
else's sentence is the move that produced this phase's one recorded near-miss
(F-41.2-05-04, a whole-file substitution that rewrote two entries its plan did
not own). **The correct total is stated here instead, and plan 41.2-20 — which
turns this ledger into two documents — is the plan that will meet it:**

**Twenty-five rows, twenty-five unrun procedures.** Every Result in this
document reads `pending`. Nothing above is approved, verified or observed, and
**deferred is not verified**.

---

*Opened 2026-08-14 by plan 41.2-05; §5, §6 and §7 appended 2026-08-14 by plan
41.2-09. No row was measured; D-41.2-04 is the reason, and deferred is not
verified.*
