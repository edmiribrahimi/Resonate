# 41.2-16 — Findings: the dialog that stands where the address does not

> **Owned exclusively by plan 41.2-16.** Plan 41.2-20 consolidates in wave 8.
>
> **This file is published.** `.planning/` is tracked on a public repository, and
> this plan's subject is venue secrecy. **No place name, no address, no hint text,
> no unannounced date and no sample data appears anywhere below.** Every string
> quoted here is a literal that lives in the component's source and carries no
> place information; the one runtime value that could — the night's own hint — is
> a prop and is quoted **only** as its interpolation, never as content.

---

## 0. The two timestamps, in the order the knot rule requires

| Moment | UTC | Evidence |
|---|---|---|
| **Impact analysis + before-image written** | **2026-08-14T10:56:09Z** | this file, committed **before** the first edit to any file under `src/` |
| **The diff** | see §7 | the conversion commit, whose parent is this file's commit |

The ordering is not a claim: it is the shape of the history. This document's
commit has no `src/` change in it, and the conversion commit's parent is this one.

---

## 1. Classification, and why it is not the phase's default

```
Classification: Critical
Domain:         venue-secrecy (primary) · access-gating · community-membership
                meta-gates.md's monotone guard binds
```

Every other file in this wave is a visual conversion. This one is named by the
gate that carries it as *"venue-secrecy primary, and the one on this list whose
conversion is Critical rather than visual"*
(`scripts/verify-dialogs.mjs:1220-1224`).

`venue_reveal_sent` is one of the project's three **monotone** guards. Once the
mail has left, the address is public; there is no annulment, because the
screenshot exists. A conversion on this boundary may only make the act **harder**
to reach — never easier, never earlier, never by one fewer deliberate press.

**What is opened: exactly one file** —
`src/app/(public)/events/[slug]/SecretVenueDialog.tsx`, 176 lines.

**What is not opened, and must not be:** the page that mounts this component
(plan 41.2-15 owns it and is running in this same wave on a disjoint file set);
the other two satellite plans of this wave; the reveal library; the reveal cron;
the reveal e-mail; any server action; anything under `src/app/api/`; anything
under `scripts/`; any `.sql`. The four spine files —
the two navigation files, the button ladder and the typography primitive — are
**read where needed and never edited**; needing to edit one is a halt.

---

## 2. Cross-domain impact, written before the diff

- **Venue secrecy.** This component is the **third branch** of the public event
  page's venue chain — the branch that renders the hint and never the address.
  Everything it renders is chosen so as **not** to be the address. A conversion
  may restyle it and may not narrow it. The rule the file states in place is that
  *the prose around the hint must not narrow it either — a hint made precise by
  its caption is a reveal with extra steps.* **A conversion adds no caption.**
- **Access and gating.** The three branches of the unlock chain correspond to
  three states of a reader — not signed in, signed in and not approved, approved.
  A merged branch is a reader shown a path that is not theirs, or shown one that
  is. `member` is not `approved`; the chain reads the second, and it keeps
  reading the second.
- **Community and membership.** The two bullets describe the two ways in. **They
  are not the same way**, and the file says so in place. Making them read the
  same would be a product statement about how somebody gets into a night, made
  by a visual conversion.
- **Next.js architecture.** The component is a client component today and stays
  one. Nothing is added to the client bundle that was not there: the two
  primitives it adopts are already in the bundle of every converted surface.

---

## 3. The three stop conditions, verbatim from the plan

1. **`venue_reveal_sent` is monotone.** Work may only make a reveal **harder** to
   trigger, never easier and never earlier. **Nothing in this file reaches the
   reveal** — asserted by grep, not assumed — and nothing in it may be made to
   narrow the hint.
2. **A payment reaching completion corrects forward.** This file touches no
   money. Asserted by grep.
3. **A series progressivo is already on a poster.** Nothing here renumbers,
   re-sorts or re-derives one.

**Both greps, run on the pre-diff file at 2026-08-14T10:56Z:**

```
$ LC_ALL=C /usr/bin/grep -cE 'venue_reveal_sent|RevealVenueDialog|VenueRevealPanel|sendVenueReveal' <the file>
0
$ LC_ALL=C /usr/bin/grep -ciE 'sumup|refund|amount|idempot|webhook|price' <the file>
0
```

Both were **already** zero before the diff. That is the point of running them
before as well as after: a zero after a diff proves nothing unless the before is
on the record too.

---

## 4. The before-image — read out of the file, not inferred

### 4.1 The unlock chain: **three branches**, at `:91-143`

```tsx
{!isAuthenticated ? (                                         // branch 1
  <p className="mb-2">Sign up or sign in to access secret venues.</p>
) : !isApproved ? (                                           // branch 2
  <p>Your account needs to be approved first.</p>
) : (                                                         // branch 3
  <>
    …the two bullets and the paragraph…
  </>
)}
```

Three arms of one chained ternary. Same shape and same hazard as the page's own
venue chain. **Three before; to be counted again after.**

### 4.2 The two bullets, and the paragraph that explains their asymmetry

The explanatory comment at `:98-108`, quoted whole:

```
Level 1 — a ticket or an RSVP, at once.

The two are NOT symmetric, and the asymmetry is the
reason this is one conditional bullet and not a tidy
pair: a ticket unlocks immediately only when the night
has `venue_reveal_on_purchase` set, while an RSVP
unlocks regardless of that flag (D-37-10) — because an
RSVP is not a purchase, and the reveal cron mails an
RSVP holder without consulting the flag either.
```

The two arms it explains, at `:109-120`:

```tsx
{revealOnPurchase ? (
  <li>
    Buy a ticket, or confirm your RSVP — the address appears
    straight away
  </li>
) : (
  <li>
    Confirm your RSVP and the address appears straight away.
    On this night a ticket alone does not: ticket holders
    wait for the window like everyone else
  </li>
)}
```

And the second bullet, the widening of phase 37, at `:128-132`:

```tsx
<li>
  Or buy nothing at all: as an approved member you will see
  the address here {revealHours} hours before the night
  starts — sooner, if we reveal it by hand
</li>
```

**They look like an unbalanced pair and they are correct.** This is a recorded
decision (D-37-10 for the asymmetry, D-37-02 for the widening) and not an
untidiness.

### 4.3 The decision that no e-mail is promised — stated twice

Once in the docblock at `:35-38`:

```
- **Promise an email.** The mail is a NOTIFICATION, not the reveal (D-37-05):
  the page opens at the instant of the window, the mail leaves on the next
  useful run of a once-a-day cron, and the two can be hours apart. "You will
  receive the address by email at the window" would be false by construction.
```

Once again as a comment at `:134-137`, immediately above the copy it governs:

```
No promise of an email, on purpose (D-37-05). The mail is a
notification that follows the page, not the reveal itself.
```

And the copy itself, at `:138-141`:

```tsx
<p className="text-xs">
  The address opens on this page. Any email is a notification
  that follows it, and it can arrive hours later.
</p>
```

### 4.4 The current focus target, read out of the file rather than assumed

```
$ LC_ALL=C /usr/bin/grep -n 'focus' <the file>
(no match — exit 1)
```

**Nothing.** The file declares no focus mechanism of any kind: no React focus
prop, no ref, no effect, no imperative call, and no marker. The shell at `:69-80`
is a plain `div` overlay — not the platform's modal element — so nothing performs
a focusing step when it appears. **On open, focus stays where it already was: on
the trigger that opened it.**

This is a *stronger* statement than the one the previous phase's reveal
confirmation could make. That file's focus placement had to be re-derived,
because an imperative call one screen below an inert attribute made the naive
reading wrong. **Here there is nothing to re-derive: the census is empty.**

### 4.5 The controls, and their declared minimums

```
$ LC_ALL=C /usr/bin/grep -c 'min-h-' <the file>
0
```

| Control | Site | Declared minimum | Computed from its own classes |
|---|---|---|---|
| the trigger | `:60-66` | **none** | no padding at all; a 14px type ramp at a 20px line box → **≈20px** |
| Sign up | `:149-154` | **none** | 10px + 20px + 10px → **40px** |
| Sign in | `:155-160` | **none** | 10px + 20px + 10px → **40px** |
| Close | `:164-170` | **none** | 10px + 20px + 10px → **40px** |

**A correction to `41.2-PATTERNS.md` §5.3, recorded rather than obeyed.** That
section calls the two inner links *"28px pills"*. Re-measured from the class
strings on the file today, the vertical padding step and the type ramp compute to
**40px**, not 28. The direction of the finding is unchanged — all four controls
are below the 44px floor, and the trigger is the worst of them — so the
conversion the map prescribes is right; only the number was wrong. Recorded here
because a figure in prose that nobody re-checks is how a document starts
describing a file it no longer resembles.

### 4.6 The token census before the diff

```
$ raw-palette matcher, -o                       →  2 occurrences   (:70, :151)
$ legacy-token matcher, -o                      →  9 occurrences on 4 lines (:74, :90, :157, :167)
$ LC_ALL=C /usr/bin/grep -c 'focus:outline-none' →  0
$ LC_ALL=C /usr/bin/grep -c 'sm:'                →  0
$ LC_ALL=C /usr/bin/grep -c 'max-w-'             →  1   (:74)
$ LC_ALL=C /usr/bin/grep -c 'animate-pulse'      →  0
$ LC_ALL=C /usr/bin/grep -c 'components/ui/Dialog' → 0
$ overlay-shell matcher                          →  1   (:70)
$ LC_ALL=C /usr/bin/grep -c 'stopPropagation'    →  1   (:75)
$ LC_ALL=C /usr/bin/grep -c 'data-initial-focus' →  0
$ LC_ALL=C /usr/bin/grep -c 'autoFocus'          →  0
$ LC_ALL=C /usr/bin/grep -c 'useToast'           →  0
```

*(The class strings behind the two matchers are not spelled in this document —
DEF-41-01, and the same discipline the primitives' own docblocks keep: to a
census counting occurrences, a literal in prose is an occurrence.)*

### 4.7 The prop list before the diff

```tsx
interface SecretVenueDialogProps {
  hint: string | null;
  isAuthenticated: boolean;
  isApproved: boolean;
  /** The EFFECTIVE window, already resolved by the server. Never the raw column. */
  revealHours: number;
  revealOnPurchase: boolean;
}
```

Five props. **None may be added, and none may change type** — a new prop is a new
source of place information, which is threat T-41.2-105.

### 4.8 The gate's state before the diff

```
$ node scripts/verify-dialogs.mjs   → DIALOGS_OK, REMAINING = 3 file(s)
```

This file is entry `:1220` of that list. **It is not deleted by this plan** —
plan 41.2-19 does that, on the authority of the tree. Until then the gate reports
the entry STALE and exits 0, and a converted-but-listed file printing STALE is
the correct intermediate state.

---

## 5. What may change, and the one direction that is safe

**The conversion is a strengthening in exactly one measurable respect**: four
controls with no declared minimum reach the 44px floor. **Nothing about who may
open this dialog changes; only how easily a finger hits it.**

**And the rung follows the act, not the file type.** In the previous phase's
reveal confirmation the confirming control moved to the *destructive* rung,
because revealing an address **is** the irreversible act and a heavier confirming
control makes it harder to press by accident. **Here there is no such control at
all**: this dialog's three answers are *a way in*, *a second way in*, and *a way
out*. None of them performs an irreversible act, and none of them is destructive.
Assigning the destructive rung to any of them would be a colour lying about a
consequence.

---

## 6. Recorded, not repaired

Nothing on the money path is reachable from this file — the money grep is zero
before and after — so this plan met no silent catch of the kind this phase has
recorded six of. The one correction it found is §4.5's, and it is a figure in a
planning document, not a defect in the product.

---

## 7. The diff, and what it must still prove

Filled in by the conversion commit and by task 2. The six questions the knot rule
puts, each answered separately and each with quoted text before and after:

1. weaker predicate?
2. fewer presses between the control and the act?
3. default-open?
4. removed confirmation / a refusal set that stopped being total?
5. Enter-to-confirm — **re-derived from the primitive's source, not cited**?
6. focus — the marker's count, its placement, and the DOM order?

*(Answers in §8, written after the diff.)*

**The diff's timestamp: 2026-08-14T11:04Z**, commit `7271ad6`, whose parent
`8003a85` is this document's first commit. The analysis precedes the diff, and
the history is the evidence rather than the claim.

---

## 8. The six answers, each separately, each with quoted text

### The instruments, and why any of their zeros may be believed

Wave 4's corroborating instrument returned `0/0/0` on eight files including four
with visible overlays, because of a defect in itself; believed, it would have
authorised twelve wrong deletions each carrying a second instrument's agreement.
**So every instrument built for this plan proves itself before it reports, in the
same run, and exits 2 without printing a result if it cannot.**

Three were built, none of them a regular expression. All three parse the file
with **the compiler the build itself runs**, so a text node is a text node
because the parser said so.

| Instrument | Negative fixture | Positive fixture(s) | Verdict |
|---|---|---|---|
| **the copy census** — visible runs, attributes, props, the two conditionals | a file against itself → **0 findings** ✓ expected 0 | one word of copy changed → **1** ✓ · an attribute added → **18** ✓ · a branch condition rewritten → **chain text differs** ✓ | self-proved |
| **the multiset** — every string a reader can be shown, order discarded, text-bearing attributes included | a file against itself → **identical** ✓ | one visible string cut → **differs** ✓ | self-proved |
| **the comment-blind diff** — comment *ranges* removed, indentation normalised | a file against itself → **identical** ✓ | a code change planted → **survives** ✓ · a comment change planted → **removed** ✓ | self-proved |

The third exists because a **line**-based comment filter cannot answer this
question: the interior lines of a block comment do not look like comments, and a
first attempt with one produced 177 "non-comment" changed lines, most of them
prose. Removing comment *ranges* instead gives the answer below. **The wrong
instrument was discarded, not reported.**

---

### 1. Weaker predicate? **No — not one conditional line changed.**

The comment-blind diff — comment **ranges** removed, indentation normalised,
blank lines dropped — is **111 code lines before, 98 after**, and its shape is
the finding. **Every conditional in the file appears as a context line**, which
is the diff's own statement that it did not change:

```
 {open && (
 {hint && isAuthenticated && (
 {!isAuthenticated ? (
 ) : !isApproved ? (
 ) : (
 {revealOnPurchase ? (
 {!isAuthenticated && (
```

The changed lines are, exhaustively: three import lines, the element names and
attributes of five controls, and **one** token migration on a label
(`text-foreground` → `text-ink`). No conditional, no comparison, no guard.

The diff whole, so the claim can be checked rather than believed:

```diff
 "use client";
 import { useState } from "react";
-import Link from "next/link";
+import { Button } from "@/components/ui/Button";
+import { Chip } from "@/components/ui/Chip";
+import { Dialog } from "@/components/ui/Dialog";
 import { LockClosedIcon } from "@/components/ui/Icons";
 …
 const [open, setOpen] = useState(false);
 return (
 <>
-<button
-type="button"
-onClick={() => setOpen(true)}
-className="…"
->
+<Button variant="secondary" size="sm" onClick={() => setOpen(true)}>
 <LockClosedIcon /> Secret Venue
-</button>
+</Button>
 {open && (
-<div
-className="…"
+<Dialog
+open
+onClose={() => setOpen(false)}
+title="Secret Venue"
+actions={
+<Button
+variant="secondary"
+className="w-full"
+data-initial-focus
 onClick={() => setOpen(false)}
 >
-<div
-className="…"
-onClick={(e) => e.…()}
+Close
+</Button>
+}
 >
-<div className="…">
-<LockClosedIcon />
-<h3 className="…">Secret Venue</h3>
-</div>
 <div className="space-y-3 text-sm text-muted">
 {hint && isAuthenticated && (
 <p className="italic">
 …
 </p>
 )}
 <div className="space-y-2">
-<p className="font-medium text-foreground">How to unlock:</p>
+<p className="font-medium text-ink">How to unlock:</p>
 {!isAuthenticated ? (
 <p className="mb-2">Sign up or sign in to access secret venues.</p>
 ) : !isApproved ? (
 …
 </div>
 {!isAuthenticated && (
 <div className="mt-5 flex gap-3">
-<Link
-href="/register"
-className="…"
->
+<Chip href="/register" className="flex-1">
 Sign up
-</Link>
-<Link
-href="/login"
-className="…"
->
+</Chip>
+<Chip href="/login" className="flex-1">
 Sign in
-</Link>
+</Chip>
 </div>
 )}
-<button
-type="button"
-onClick={() => setOpen(false)}
-className={`…`}
->
-Close
-</button>
-</div>
-</div>
+</Dialog>
 )}
 </>
 );
```

*(the deleted class strings are elided as `…` — DEF-41-01, and the light-dismiss
handler's method name likewise, because this document asserts its count is zero
and a document that spells the needle destroys its own measurement. The full,
unelided text is in the commit.)*

Corroborated independently by the copy census, which does not read lines at all:

```
THE UNLOCK CHAIN
  arms before: 3   arms after: 3
  text identical after normalising indentation: true
```

**Three branches before, three after**, and the chain's *source text* is
byte-identical once indentation is normalised away — a stronger claim than a
count, because a count survives two branches being swapped.

The previous phase's equivalent conversion set the bar at *not one conditional
line changed*. **This meets it.**

### 2. Fewer presses between the control and the act? **No — the count is unchanged, and there is no act.**

Before and after, reaching the hint costs **one press** on the trigger. Reaching
the address costs **nothing this dialog can offer**: it has no control that
reveals one, and the reveal grep is zero on both sides of the diff. The two
controls that go anywhere navigate to the sign-up and sign-in pages — the same
two addresses, byte-identical, proved by the copy census's attribute list, where
`href="/register"` and `href="/login"` appear on neither the removed nor the
added side.

### 3. Default-open? **No.**

```tsx
const [open, setOpen] = useState(false);
```

Unchanged — the line is a context line in the comment-blind diff. The panel is
mounted only while that state is true (`{open && (`), and the only thing that
sets it true is the trigger's own handler.

### 4. Removed confirmation, or a refusal set that stopped being total? **Neither applies, and the reason is not an evasion.**

**This component has no confirmation and no refusal set.** It performs no act, so
there is nothing to confirm; it calls no server action, so there is nothing that
can refuse. The primitive's status region — where the staff-side reveal
confirmation renders its eleven refusals from a *total* `Record`, so that a
member added upstream turns that file red at build time rather than rendering a
blank at two in the morning — is **not used here**, and the `status` prop is not
passed.

That is the correct disposition and not an omission: a status region on a dialog
with no outcome would be a region that can never fill.

### 5. Enter-to-confirm? **No — and re-derived from the primitives' source, not cited.**

The previous phase proved this by reading the primitive. This plan **re-read it**
rather than quoting that reading:

```
$ LC_ALL=C /usr/bin/grep -cE '<form|</form>'                        src/components/ui/Dialog.tsx  →  0
$ LC_ALL=C /usr/bin/grep -c  'method='                              src/components/ui/Dialog.tsx  →  0
$ LC_ALL=C /usr/bin/grep -c  'onSubmit'                             src/components/ui/Dialog.tsx  →  0
$ LC_ALL=C /usr/bin/grep -c  'type='                                src/components/ui/Dialog.tsx  →  0
$ LC_ALL=C /usr/bin/grep -cE 'onKeyDown|onKeyUp|onKeyPress|addEventListener|keydown|\bkey\b'
                                                                    src/components/ui/Dialog.tsx  →  2
```

**The two key matches are prose, and both are quoted so the number can be read
rather than trusted:**

```
Dialog.tsx:45   * So this file writes no key handler, no focus-cycling code and no
Dialog.tsx:145  * **A confirmation whose Enter key performs the act is a confirmation that did
```

Neither is code. So the primitive renders a `<dialog>` with **no form inside it,
no form method, no submit handler, no key handler and no `type` attribute at
all**.

The ladder supplies the missing half:

```
src/components/ui/Button.tsx:158  type="button"     ← IconButton, written BEFORE the caller's spread
src/components/ui/Button.tsx:236  type="button"     ← Button,     written BEFORE the caller's spread
src/components/ui/Chip.tsx:195    type="button"     ← the chip's button arm
```

*(the other two matches in `Button.tsx`, at `:146` and `:207`, are the prose
explaining why — quoted here so the count of four is accounted for and not
rounded)*

**Therefore no key press performs anything inside this panel.** Its three
controls are one button of the labelled rung and two chips rendering typed
`next/link` anchors; Enter on a focused anchor follows a link, which is
navigation away from this page and not the confirming of an act.

### 6. Focus. **The marker occurs exactly once, on the way out.**

```
$ LC_ALL=C /usr/bin/grep -c 'data-initial-focus'  <the file>   →  1
$ LC_ALL=C /usr/bin/grep -c 'autoFocus'           <the file>   →  0
```

Quoted whole, and it is the whole of the actions region:

```tsx
actions={
  <Button
    variant="secondary"
    className="w-full"
    data-initial-focus
    onClick={() => setOpen(false)}
  >
    Close
  </Button>
}
```

**Before, nothing was focused at all** — §4.4: the focus census on the pre-diff
file was empty, the shell was a plain `div`, and no focusing step ran when it
appeared.

**After, the way out holds it.** This dialog has no destructive control and no
control that performs an act: its three answers are a way in, a second way in,
and a way out. §11's rule for a confirmation — *the cancel is the initial-focus
target, and the confirming control is focused by nothing* — reads here as *the
way out carries the marker, and neither road in may hold it*. `Close` is the
**only** control in the actions region, so it is first there; the primitive's own
close control precedes it in the DOM and is also a way out, so **no reading of
DOM order puts the initial focus on a road in.**

The marker is declared rather than left to the primitive's default because a
declared marker forces the author to say which control is the least destructive
and is the thing a person can grep. **One mechanism, not two** — the precedent
this project already corrected once is two of them sitting there for one intent,
and the inert framework attribute that made that pair ambiguous is absent here
and was absent before.

---

## 9. Every string this dialog can render, branch by branch

`.planning/` is public. **The night's own hint is a prop and its content is never
quoted** — only its interpolation. Every literal below is a constant in the
source and carries no place information.

| # | Branch | String | What it tells a reader about the place |
|---|---|---|---|
| 0 | always (the trigger, outside the panel) | `Secret Venue` | that there is one, and that it is secret. **Nothing about where** |
| 1 | always (the heading, now the primitive's `title` and the panel's accessible name) | `Secret Venue` | the same two words, in the same role |
| 2 | signed in **and** the night stored a hint | `Hint:` + `{hint}` | **exactly the night's own hint, unchanged.** The prefix adds no word. No caption, no label and no description narrows it |
| 3 | always | `How to unlock:` | that unlocking is a thing one does. Nothing about where |
| 4 | **branch 1** — not signed in | `Sign up or sign in to access secret venues.` | that the road starts at an account. Nothing about where |
| 5 | **branch 2** — signed in, not approved | `Your account needs to be approved first.` | that approval gates it. Nothing about where |
| 6 | **branch 3**, reveal-on-purchase set | `Buy a ticket, or confirm your RSVP — the address appears straight away` | *when* the address appears, never *what* it is |
| 7 | **branch 3**, reveal-on-purchase not set | `Confirm your RSVP and the address appears straight away. On this night a ticket alone does not: ticket holders wait for the window like everyone else` | the same, plus which of the two roads is conditional |
| 8 | **branch 3** | `Or buy nothing at all: as an approved member you will see the address here ` + `{revealHours}` + ` hours before the night starts — sooner, if we reveal it by hand` | a number of hours. **A duration is not a location** |
| 9 | **branch 3** | `The address opens on this page. Any email is a notification that follows it, and it can arrive hours later.` | where the address will appear — *this page* — and that no mail is promised |
| 10 | not signed in | `Sign up` | a destination inside this application |
| 11 | not signed in | `Sign in` | a destination inside this application |
| 12 | always | `Close` | the way out |

**Every row says the same thing: nothing beyond the hint the night's own author
wrote.** The one row that carries place information at all is row 2, and it
carries *exactly* what the night stored — no more, and with no prose around it
that narrows it.

**The multiset, checked mechanically rather than by this table:**

```
before: 14 string(s)
after : 14 string(s)
multiset identical: true
```

Fourteen and fourteen, order discarded, text-bearing attributes included — which
is how the heading's move from a text node into the `title` attribute is
accounted for instead of appearing as a loss.

## 10. Every attribute added, and each names an action rather than a place

Twelve removed, twelve added — the copy census's own output, pasted:

| Added attribute | Names |
|---|---|
| `variant="secondary"` (trigger) | a rung on the control ladder |
| `size="sm"` | a rung |
| `open` | the panel's own showing state |
| `onClose={() => setOpen(false)}` | an **action**: closing |
| `title="Secret Venue"` | **the only added attribute carrying a string a reader sees**, and it is byte-identical to the deleted heading's text. It names the *absence* of a place, which is the point of the phrase |
| `actions={…}` | the region the way out moved into |
| `variant="secondary"` (Close) | a rung |
| `className="w-full"` | width |
| `data-initial-focus` | the focus marker |
| `className="font-medium text-ink"` | a token migration off a legacy alias |
| `className="flex-1"` ×2 | layout on the two chips |

**No `aria-label`, no `aria-describedby`, no `aria-description`, no tooltip, no
placeholder and no helper line was added.** The panel did gain an accessible
name it never had — the primitive labels the `<dialog>` by its own heading — and
that name is the heading's own two words. **A conversion adds no caption, and
this one added none.**

## 11. The props — no new source of place information

```
before (5): ["hint: string | null","isAuthenticated: boolean","isApproved: boolean",
             "revealHours: number","revealOnPurchase: boolean"]
after  (5): ["hint: string | null","isAuthenticated: boolean","isApproved: boolean",
             "revealHours: number","revealOnPurchase: boolean"]
identical in names and types: true
```

**No prop added, none removed, none re-typed.** T-41.2-105 is closed by
measurement.

## 12. The touch-floor delta — the one measurable strengthening

| Control | Declared minimum before | Declared minimum after | By |
|---|---|---|---|
| the trigger | **none** (≈20px from its own classes) | **44px** | the labelled rung, small step |
| `Sign up` | **none** (40px) | **44px** | the chip rung |
| `Sign in` | **none** (40px) | **44px** | the chip rung |
| `Close` | **none** (40px) | **44px** | the labelled rung, default step |

`min-h-` occurrences in the file: **0 before, 0 after** — and that is correct
rather than a miss. The floor is declared *inside the primitives*, which is why
the gate lists both of them under *"declares the minimum"* and resolves their
elements through exemption 2a. The gate's own scan does not yet reach this file,
because the public event surface is not a declared surface until plan 41.2-19;
stated rather than implied, because a green that never looked at a file is not a
green about that file.

**Nothing about who may open this dialog changed. Only how easily a finger hits
it.**

## 13. The negative-space check, pasted whole

```
$ git status --porcelain
(empty)

$ git diff --name-only <the plan's base> HEAD
.planning/phases/41.2-public-member-and-money-surfaces/41.2-16-FINDINGS.md
src/app/(public)/events/[slug]/SecretVenueDialog.tsx

$ git diff --name-only <base> HEAD -- scripts/ supabase/ src/app/api/ src/lib/venue-reveal/ \
      'src/app/(public)/events/[slug]/page.tsx' '*actions.ts' '*.sql' \
      .planning/STATE.md .planning/ROADMAP.md src/components/layout/ \
      src/components/ui/Button.tsx src/components/ui/Typography.tsx
(empty)
```

**Exactly one file under `src/`.** Nothing under `scripts/`, nothing under
`supabase/`, nothing under `src/app/api/`, no action module, no `.sql`, nothing
in the reveal library, **nothing in the page that mounts this component** — which
plan 41.2-15 owns and is editing in this same wave — and no other satellite. No
spine file. No `STATE.md`, no `ROADMAP.md`.

## 14. No role-session observation was attempted, and the reason

**None was attempted, and none may be inferred from anything above.** No tool in
this repository can authenticate as a role, and the management path bypasses
row-level security — so an observation taken that way would prove the opposite of
what it claims: it would show what a privileged reader sees, on a surface whose
whole question is what an *un*privileged one sees.

That row belongs to a person, in the batch, and plan 41.2-19 writes it `pending`.
A deferred observation is honest; this project has already written down that
deferred is not verified, and that is the correct state to leave a row in.

Nor was the application run, rendered headless, or pointed at Supabase —
D-41.2-04, the road that does not touch production. Incident D12 is why that road
is not reopened for convenience.

## 15. The gate line, pasted, and the entry NOT deleted

```
! B  5 REMAINING entr(y/ies) are STALE — the file no longer carries a shell:
     src/app/(public)/events/[slug]/SecretVenueDialog.tsx  → converted; remove this entry

✓ C  none of the 16 file(s) rendering Dialog imports the toast, and
     neither does the primitive

DIALOGS_OK — all three checks passed. REMAINING = 2 file(s) still declare
```

`REMAINING` went **3 → 2**. The entry is **not deleted here** — that belongs to
plan 41.2-19, on the authority of the tree rather than on the authority of this
plan's claim. A converted-but-listed file printing STALE while the gate exits 0
is the correct intermediate state.

## 16. The full run, and the bar it was measured against

```
    package.json declares           17  verify:* entr(y/ies)
    run here                        16
      of which passed               15
      of which FAILED                0
      of which REFUSED               1  — nothing was measured by these
    needs a server, not run          1

  ═══ verify:capabilities — REFUSED (exit 2) ═══
FATAL: missing environment variable(s): SUPABASE_ACCESS_TOKEN, NEXT_PUBLIC_SUPABASE_URL.
Set them in .env.local (gitignored) or in the environment. Nothing was measured.
```

**15 gates reached a verdict, 0 failed, and one refused for want of credentials a
worktree does not hold and must not be given** (D-41.2-04). `npm run verify`
exits 2 by construction so that a refusal cannot be read as a pass. **It was not
made green**, and making it green was not attempted.

```
$ npm run build   →  ✓ Compiled successfully · Running TypeScript · exit=0
```

## 17. The monotone sentence, earned rather than asserted

Every check above passed, so it may be written:

> **The address is at least as hard to reach after this conversion as before, and
> nothing here shortens the path between opening this surface and reading one —
> because this dialog never tells one.**

And the one respect in which it is *harder*, stated so the claim is not merely
defensive: the panel now traps focus, closes on Escape and makes the document
behind it inert, all three by specification rather than by a copy — so the page
underneath, which is the surface that *can* show an address, cannot be operated
by accident while this panel is open.
