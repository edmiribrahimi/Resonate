---
phase: 03-referral-approval-system
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(admin)/admin/members/actions.ts
  - src/app/(admin)/admin/members/page.tsx
  - src/app/(organizer)/organizer/members/page.tsx
  - src/components/admin/MemberTable.tsx
autonomous: true
requirements: [APPR-01, APPR-02, APPR-03]

must_haves:
  truths:
    - "Both master AND organizers can approve or reject pending members"
    - "The MemberTable has status tabs (All / Pending / Approved / Rejected) that filter members by status"
    - "The Pending tab shows a badge count of pending members"
    - "When the Pending tab is active, each member row has a checkbox for bulk selection"
    - "A bulk action toolbar appears when members are selected, with 'Approve selected' and 'Reject selected' buttons"
    - "Clicking a member row expands an accordion detail section showing: referred by, referral count, and events attended"
    - "The expanded detail shows 'Direct signup' when referred_by is null and the referrer's name when present"
    - "The organizer members page passes showActions=true so organizers can approve/reject pending members"
    - "Organizer approve/reject server actions use a service-role client to bypass RLS"
    - "After bulk approve/reject, selected checkboxes are cleared"
    - "The admin members page fetches referred_by data via a self-referencing join on profiles"
    - "Pending members can browse events but cannot RSVP, buy tickets, or upload media (enforced by existing Phase 2 RLS)"
  artifacts:
    - path: "src/app/(admin)/admin/members/actions.ts"
      provides: "Server actions for approve/reject (single and bulk), updated auth check to allow organizers"
      exports: ["approveMember", "rejectMember", "bulkApproveMember", "bulkRejectMember", "updateMemberRole", "deactivateMember", "reactivateMember"]
    - path: "src/components/admin/MemberTable.tsx"
      provides: "Enhanced MemberTable with status tabs, bulk checkboxes, expandable detail rows, referral data display"
      contains: "statusTab"
    - path: "src/app/(admin)/admin/members/page.tsx"
      provides: "Admin members page fetching referred_by data, passing enhanced props to MemberTable"
      contains: "referred_by"
    - path: "src/app/(organizer)/organizer/members/page.tsx"
      provides: "Organizer members page with showActions=true for approve/reject"
      contains: "showActions={true}"
  key_links:
    - from: "src/app/(admin)/admin/members/actions.ts"
      to: "profiles table"
      via: "serviceClient.from('profiles').update({ status }).in('id', memberIds) for bulk operations"
      pattern: "\\.in\\(.*id.*memberIds"
    - from: "src/app/(admin)/admin/members/actions.ts"
      to: "service-role client"
      via: "createSupabaseClient with SUPABASE_SERVICE_ROLE_KEY for organizer approve/reject"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: "src/app/(admin)/admin/members/page.tsx"
      to: "profiles table"
      via: "supabase.from('profiles').select('...referred_by, referrer:profiles!referred_by(full_name)') for referral data"
      pattern: "referrer.*profiles.*referred_by"
    - from: "src/components/admin/MemberTable.tsx"
      to: "src/app/(admin)/admin/members/actions.ts"
      via: "Server Action imports for approve/reject (single + bulk)"
      pattern: "import.*approveMember.*bulkApproveMember"
---

<objective>
Enhance the admin member management UI with status tab filtering, bulk approve/reject for pending members, expandable detail rows showing referral and attendance data, and enable organizer approve/reject capabilities.

Purpose: This completes the trust-gated community loop. Admins and organizers can review and approve pending members, see who referred whom, and manage the approval queue efficiently with bulk operations. Combined with Plans 01 and 02, this creates the full referral-to-approval pipeline.

Output: Enhanced MemberTable with tabs/bulk/expandable rows, updated server actions supporting organizer auth, updated admin and organizer pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-referral-approval-system/03-CONTEXT.md
@.planning/phases/03-referral-approval-system/03-RESEARCH.md
@.planning/phases/03-referral-approval-system/03-01-SUMMARY.md
@src/components/admin/MemberTable.tsx
@src/app/(admin)/admin/members/actions.ts
@src/app/(admin)/admin/members/page.tsx
@src/app/(organizer)/organizer/members/page.tsx
@src/app/api/auth/callback/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server actions -- add approve/reject (single + bulk) with organizer support</name>
  <files>src/app/(admin)/admin/members/actions.ts</files>
  <action>
  Modify `src/app/(admin)/admin/members/actions.ts` to add new server actions and update auth verification:

  1. **Add a service-role client helper** (same pattern as auth/callback/route.ts):
     ```
     import { createClient as createSupabaseClient } from "@supabase/supabase-js";

     function getServiceClient() {
       return createSupabaseClient(
         process.env.NEXT_PUBLIC_SUPABASE_URL!,
         process.env.SUPABASE_SERVICE_ROLE_KEY!
       );
     }
     ```

  2. **Create `verifyAdminOrOrganizer` function** alongside the existing `verifyMaster`:
     - Same pattern as `verifyMaster` but checks `profile.role === "master" || profile.role === "organizer"`
     - Returns the authenticated user
     - Used for approve/reject actions (both master and organizer can approve/reject)

  3. **Add `approveMember(memberId: string)` action:**
     - Verify caller via `verifyAdminOrOrganizer`
     - Use `getServiceClient()` to bypass RLS for the update (organizers don't have RLS write permission on profiles)
     - Update: `serviceClient.from("profiles").update({ status: "approved" }).eq("id", memberId)`
     - `revalidatePath("/admin/members")` and `revalidatePath("/organizer/members")`
     - Return `{ success: true }`

  4. **Add `rejectMember(memberId: string)` action:**
     - Same as approveMember but sets `status: "rejected", role: "member"` (demote + reject in one, matching deactivateMember pattern)
     - Use service-role client

  5. **Add `bulkApproveMember(memberIds: string[])` action:**
     - Verify caller via `verifyAdminOrOrganizer`
     - Validate `memberIds` is non-empty
     - Use service-role client: `serviceClient.from("profiles").update({ status: "approved" }).in("id", memberIds)`
     - Revalidate both paths
     - Return `{ success: true, count: memberIds.length }`

  6. **Add `bulkRejectMember(memberIds: string[])` action:**
     - Same as bulkApproveMember but sets `status: "rejected", role: "member"`
     - Use service-role client

  7. **Keep existing actions unchanged:** `verifyMaster`, `updateMemberRole`, `deactivateMember`, `reactivateMember` all remain as-is. The master-only role management (promote/demote) stays master-only. Only approve/reject is shared with organizers.

  8. **Export all new actions** from the file.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -5</automated>
    <manual>Check that actions.ts exports approveMember, rejectMember, bulkApproveMember, bulkRejectMember. Check that verifyAdminOrOrganizer allows both master and organizer. Check that service-role client is used for approve/reject operations. Check that existing master-only actions are unchanged.</manual>
  </verify>
  <done>Server actions file exports approveMember, rejectMember, bulkApproveMember, bulkRejectMember using verifyAdminOrOrganizer (master + organizer). Service-role client bypasses RLS for profile updates. Existing master-only actions (updateMemberRole, deactivateMember) unchanged. Both /admin/members and /organizer/members paths revalidated. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced MemberTable with status tabs, bulk actions, expandable detail rows; updated admin/organizer pages</name>
  <files>src/components/admin/MemberTable.tsx, src/app/(admin)/admin/members/page.tsx, src/app/(organizer)/organizer/members/page.tsx</files>
  <action>
  **Enhance src/components/admin/MemberTable.tsx:**

  This is a significant enhancement to the existing component. Extend (do not replace) the component to add three new features: status tabs, bulk selection/actions, and expandable detail rows.

  **Updated MemberRow interface:**
  ```typescript
  interface MemberRow {
    id: string;
    email: string;
    full_name: string;
    role: UserRole;
    status: UserStatus;
    membership_code: string;
    created_at: string;
    referred_by: string | null;
    referrer_name: string | null;  // From the self-join
  }
  ```

  **New prop:** Add `callerRole: UserRole` to MemberTableProps. This determines which actions are visible:
  - Master sees ALL actions: promote, demote, deactivate, reactivate, approve, reject
  - Organizer sees ONLY: approve, reject (for pending members)

  **Feature 1: Status tabs**
  Add a tab bar above the existing filter controls:
  - Tabs: All / Pending / Approved / Rejected
  - Visual: styled buttons (rounded-lg px-3 py-1.5 text-sm font-medium) with active state (bg-accent text-white) and inactive state (bg-card text-muted hover:text-foreground)
  - The "Pending" tab shows a count badge: `Pending ({pendingCount})`
  - Selecting a tab sets `statusTab` state which pre-filters the member list
  - When a specific status tab is selected, HIDE the status dropdown filter (it would conflict). Show it again when "All" tab is active.
  - The `statusTab` state overrides `statusFilter` when set to anything other than "all"

  **Feature 2: Bulk selection and actions**
  - Only visible when the "Pending" tab is active
  - Add `selectedIds: Set<string>` state
  - Header checkbox (select all visible pending members) in the table header
  - Per-row checkbox in each member row
  - Bulk action toolbar: appears when `selectedIds.size > 0`, positioned above the table:
    ```
    <div className="mb-4 flex items-center gap-3 rounded-xl border border-accent/30 bg-accent/5 px-4 py-3">
      <span className="text-sm font-medium">{selectedIds.size} selected</span>
      <button onClick={handleBulkApprove}>Approve selected</button>
      <button onClick={handleBulkReject}>Reject selected</button>
    </div>
    ```
  - After bulk action completes, clear `selectedIds` via `setSelectedIds(new Set())`
  - Use `startTransition` for bulk actions to batch UI updates with server revalidation
  - Import `bulkApproveMember` and `bulkRejectMember` from actions

  **Feature 3: Expandable detail rows**
  - Add `expandedId: string | null` state (only one row expanded at a time)
  - Clicking a member row (not on a button or checkbox) toggles the expanded detail
  - The expanded section renders as an additional `<tr>` below the member's row with `colSpan` spanning all columns
  - Content in the expanded section (grid with 3 columns):
    - **Referred by:** Show `member.referrer_name` if present, or "Direct signup" if `referred_by` is null
    - **Referred members:** Compute referral count from the member list: count how many members in the full list have `referred_by === member.id`. This is client-side computation from already-loaded data -- no extra query needed.
    - **Events attended:** Show `member.attendance_count` if available, or "0" as placeholder (attendance data populated in Phase 5). For now, pass `0` from the page -- the expandable row displays it.
  - Style the expanded section with a slightly different background: `bg-card/20 border-t border-card-border/30`
  - Add a visual expand/collapse indicator on each row: a small chevron icon (rotate on expand)
  - On mobile card layout: add the expandable detail section inside the card (tap to expand)

  **Update MemberActions component:**
  Update the existing MemberActions to use `callerRole` to determine which actions to show:
  - If `callerRole === "master"`: show all actions (promote, demote, deactivate, reactivate, approve, reject) -- existing behavior
  - If `callerRole === "organizer"`: show only approve/reject for pending members. No promote/demote/deactivate/reactivate.
  - Import `approveMember` and `rejectMember` from actions for the new action functions
  - For organizer: use the new `approveMember`/`rejectMember` actions instead of `reactivateMember`/`deactivateMember`

  **Remove placeholder columns:**
  The existing MemberTable has placeholder "Referred By" and "Events" columns showing "--". Replace:
  - "Referred By" column: Remove from the main table. This data is now shown in the expandable detail row (per CONTEXT.md: "Referred by info shown in an expandable member detail row, not as a column in the main table").
  - "Events" column: Same -- move to expandable detail row.
  - This reduces the table width and makes it cleaner. The main table columns become: Name, Email, Role, Status, Joined, [Actions].

  **Update src/app/(admin)/admin/members/page.tsx:**
  1. Update the profile query to include `referred_by` and the referrer's name via a self-referencing join:
     ```typescript
     const { data: members } = await supabase
       .from("profiles")
       .select(`
         id, email, full_name, role, status, membership_code, created_at, referred_by,
         referrer:profiles!referred_by(full_name)
       `)
       .order("created_at", { ascending: false });
     ```
     If the self-referencing join syntax doesn't work (see Open Question 1 in research), fall back to passing `referred_by` raw and computing referrer names client-side from the member list.
  2. Transform the data to flatten the referrer name: `referrer_name: member.referrer?.full_name || null`
  3. Pass `callerRole="master"` to MemberTable.
  4. The `showActions` prop stays `true`.

  **Update src/app/(organizer)/organizer/members/page.tsx:**
  1. Same query update as admin page (include referred_by and referrer join).
  2. Change `showActions={false}` to `showActions={true}` -- organizers can now approve/reject.
  3. Pass `callerRole="organizer"` to MemberTable.
  4. The organizer page title stays "Members" (not "Member Management").

  **APPR-01 verification:**
  APPR-01 (pending members cannot RSVP, buy tickets, upload media) is already enforced by Phase 2:
  - RLS policy `rsvps_insert_approved` requires `status='approved'`
  - Middleware blocks pending members from /membership-card and /attendance
  - Event detail page hides RSVP button for non-approved members
  No new work needed for APPR-01 -- just verify the existing enforcement is intact. Add a comment in the plan summary noting this.
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -5</automated>
    <manual>Check MemberTable has status tabs (All/Pending/Approved/Rejected). Check Pending tab shows badge count. Check checkboxes appear when Pending tab is active. Check bulk action toolbar appears when items selected. Check expanded detail row shows referred by, referral count, events attended. Check organizer page passes showActions=true and callerRole="organizer". Check admin page fetches referred_by data. Check MemberTable no longer has "Referred By" and "Events" as main table columns (moved to expandable detail).</manual>
  </verify>
  <done>MemberTable enhanced with status tabs (All/Pending/Approved/Rejected with count badge), bulk selection checkboxes on Pending tab with approve/reject toolbar, and expandable detail rows showing referral and attendance data. Admin page fetches referred_by via self-join. Organizer page passes showActions=true with callerRole="organizer" for approve/reject only. Server actions support both master and organizer for approve/reject. Placeholder table columns removed and data moved to expandable rows. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes with 0 errors
- `src/app/(admin)/admin/members/actions.ts` exports approveMember, rejectMember, bulkApproveMember, bulkRejectMember
- `src/app/(admin)/admin/members/actions.ts` has verifyAdminOrOrganizer function allowing both master and organizer
- `src/app/(admin)/admin/members/actions.ts` uses service-role client for approve/reject operations
- `src/components/admin/MemberTable.tsx` renders status tab buttons (All, Pending, Approved, Rejected)
- `src/components/admin/MemberTable.tsx` shows checkboxes and bulk action toolbar when Pending tab is active
- `src/components/admin/MemberTable.tsx` has expandable detail rows with referred by, referral count, events attended
- `src/components/admin/MemberTable.tsx` no longer renders "Referred By" and "Events" as main table columns
- `src/components/admin/MemberTable.tsx` accepts `callerRole` prop and filters actions accordingly
- `src/app/(admin)/admin/members/page.tsx` query includes `referred_by` and referrer join
- `src/app/(organizer)/organizer/members/page.tsx` passes `showActions={true}` and `callerRole="organizer"`
- Organizer can only see approve/reject actions (no promote/demote/deactivate)
- Bulk selection clears after bulk action completes
- Existing master-only actions (promote, demote, deactivate) still work and are still master-only
</verification>

<success_criteria>
Master and organizer users can efficiently manage the pending member queue through status tabs with badge counts, bulk approve/reject with checkboxes, and detailed member information in expandable rows. The approval workflow is complete: pending members appear in the Pending tab, can be individually or bulk approved/rejected, and the referral chain (who referred whom) is visible in the detail view. Pending members remain restricted to browse-only access (Phase 2 enforcement intact).
</success_criteria>

<output>
After completion, create `.planning/phases/03-referral-approval-system/03-03-SUMMARY.md`
</output>
