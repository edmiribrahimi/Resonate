---
phase: 03-referral-approval-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/(members)/dashboard/page.tsx
  - src/app/(members)/membership-card/page.tsx
autonomous: true
requirements: [REFR-02]

must_haves:
  truths:
    - "Approved members see their referral link on the dashboard page with a copy button"
    - "Approved members see their referral link on the membership card page with a copy button"
    - "Pending and rejected members do NOT see a referral link anywhere"
    - "The referral link format is {origin}/register?ref={membership_code}"
    - "The membership card page fetches the real membership_code from the profiles table instead of using hardcoded RSN-DEMO1234"
    - "Clicking the copy button copies the full referral link to the clipboard and shows 'Copied!' feedback for 2 seconds"
    - "The referral link section is labeled 'Invite a friend'"
  artifacts:
    - path: "src/app/(members)/dashboard/page.tsx"
      provides: "Dashboard with referral link card for approved members"
      contains: "Invite a friend"
    - path: "src/app/(members)/membership-card/page.tsx"
      provides: "Membership card page with real membership_code from DB and referral link section"
      contains: "register?ref="
  key_links:
    - from: "src/app/(members)/membership-card/page.tsx"
      to: "profiles table"
      via: "supabase.from('profiles').select('membership_code').eq('id', user.id).single()"
      pattern: "membership_code"
    - from: "src/app/(members)/dashboard/page.tsx"
      to: "profiles table"
      via: "supabase.from('profiles').select('membership_code').eq('id', user.id).single()"
      pattern: "membership_code"
---

<objective>
Display the referral link with copy-to-clipboard functionality on the dashboard and membership card pages for approved members. Fix the hardcoded membership_code on the membership card page by fetching it from the database.

Purpose: This gives approved members the ability to invite friends by sharing their unique referral link. The link is the primary growth mechanism for the trust-gated community. Fixing the hardcoded membership_code also resolves a Phase 2 TODO.

Output: Dashboard and membership card pages with referral link display and clipboard copy.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-referral-approval-system/03-CONTEXT.md
@.planning/phases/03-referral-approval-system/03-RESEARCH.md
@.planning/phases/03-referral-approval-system/03-01-SUMMARY.md
@src/app/(members)/dashboard/page.tsx
@src/app/(members)/membership-card/page.tsx
@src/components/membership/MembershipCardView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard referral link and membership card page referral link + real membership_code</name>
  <files>src/app/(members)/dashboard/page.tsx, src/app/(members)/membership-card/page.tsx</files>
  <action>
  **Create a shared CopyReferralLink client component:**
  Since both pages need the same referral link copy UI, create a reusable client component. It can live inline in one of the pages or be extracted to a shared location like `src/components/membership/CopyReferralLink.tsx`. The component:
  - Accepts `membershipCode: string` as a prop
  - Constructs the referral link: uses `window.location.origin` for the base URL + `/register?ref={membershipCode}`
  - Renders a card with:
    - Label: "Invite a friend" (text-sm text-muted)
    - A read-only input showing the referral link (truncated with Tailwind `truncate` class, font-mono for the code portion)
    - A "Copy" button that calls `navigator.clipboard.writeText(referralLink)`, then shows "Copied!" for 2 seconds via setTimeout
    - Fallback for clipboard API failure: document.execCommand("copy") with a temporary input element
  - Styled consistently with existing cards: `rounded-2xl border border-card-border bg-card p-5`

  **Modify src/app/(members)/dashboard/page.tsx:**
  1. Fetch the current user's `membership_code` from the profiles table. After the existing `getUser()` call, add:
     ```
     const { data: profile } = await supabase
       .from("profiles")
       .select("membership_code")
       .eq("id", user.id)
       .single();
     ```
  2. In the approved state section (the else branch after `isPendingOrRejected`), add the referral link card. Place it AFTER the Membership Card link and Attendance link, but BEFORE the "Upcoming confirmed events" section. This gives it visual prominence without displacing the existing cards.
  3. Only render the referral link if the user is approved (already in the approved branch, so this is automatic).
  4. Pass `membershipCode={profile?.membership_code || ""}` to the CopyReferralLink component.
  5. If `profile?.membership_code` is falsy (shouldn't happen for approved users), do not render the referral link section.

  **Modify src/app/(members)/membership-card/page.tsx:**
  1. **Fix the hardcoded membership_code:** Replace `const membershipCode = "RSN-DEMO1234"` with a database fetch:
     ```
     const { data: profile } = await supabase
       .from("profiles")
       .select("membership_code, status")
       .eq("id", user.id)
       .single();
     const membershipCode = profile?.membership_code || "RSN-UNKNOWN";
     ```
  2. Add the referral link section AFTER the MembershipCardView component and BEFORE the "How to use your card" section.
  3. Only show the referral link if `profile?.status === "approved"`. (Pending/rejected members shouldn't reach this page due to middleware, but defense in depth.)
  4. Pass `membershipCode` to the CopyReferralLink component.
  5. Remove the `// TODO: fetch membership_code from profiles table` comment.

  **Important per CONTEXT.md decisions:**
  - Only approved members get a referral link. Pending/rejected cannot invite.
  - Referral link visible in TWO places: dashboard AND membership card page.
  - No intermediate landing page -- link goes straight to /register?ref=...
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && npx next build 2>&1 | tail -5</automated>
    <manual>Check dashboard page shows referral link card for approved users (not for pending/rejected). Check membership card page fetches real membership_code from DB (no more RSN-DEMO1234). Check both pages have "Invite a friend" label and copy button. Check referral link format is /register?ref={code}.</manual>
  </verify>
  <done>Dashboard shows referral link card with copy button for approved members, positioned after membership and attendance cards. Membership card page fetches real membership_code from database (RSN-DEMO1234 hardcode removed) and shows referral link section for approved members. CopyReferralLink component handles clipboard copy with "Copied!" feedback. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` completes with 0 errors
- Dashboard page (`src/app/(members)/dashboard/page.tsx`) contains "Invite a friend" text
- Dashboard page fetches membership_code from profiles table
- Dashboard page only shows referral link in the approved branch (not for pending/rejected)
- Membership card page (`src/app/(members)/membership-card/page.tsx`) no longer contains "RSN-DEMO1234"
- Membership card page fetches membership_code from profiles table
- Membership card page shows referral link section with copy button
- Referral link format uses `/register?ref={membership_code}`
- Copy button uses `navigator.clipboard.writeText` with 2-second "Copied!" feedback
</verification>

<success_criteria>
Every approved member can see their unique referral link on both the dashboard and membership card pages and copy it to share with friends. The membership card page uses the real database-stored membership code instead of a hardcoded placeholder. Pending and rejected members do not see any referral link.
</success_criteria>

<output>
After completion, create `.planning/phases/03-referral-approval-system/03-02-SUMMARY.md`
</output>
