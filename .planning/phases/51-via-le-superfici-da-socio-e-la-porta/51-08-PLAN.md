---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 08
type: execute
wave: 2
depends_on: ["51-05"]
files_modified:
  - supabase/migrations/<timestamp>_role_attendee_and_capability_keys.sql
  - src/lib/capabilities/keys.ts
  - src/lib/routes/capability-routes.ts
  - scripts/verify-capabilities.mjs
  - scripts/verify-refusal.mjs
  - scripts/rls-baseline.mjs
  - scripts/container/seed.mjs
  - scripts/seed-lab-door.mjs
autonomous: true
requirements: [MEM-01]

must_haves:
  truths:
    - "Il database accetta il valore di ruolo 'attendee' e le righe esistenti lo portano gia' (D-51-06)"
    - "Le due chiavi membership.active e membership.card.view non sono piu' nel catalogo ne' nella costante TypeScript (D-51-07)"
    - "La migration e' stata applicata al LABORATORIO e riletta dal catalogo, non dalla risposta del POST"
    - "Nessun gate resta rosso alla fine del piano: le quattro costanti e le query vive si muovono nello stesso atto della migration"
  artifacts:
    - path: "supabase/migrations/<timestamp>_role_attendee_and_capability_keys.sql"
      provides: "i due CHECK riscritti, le righe mosse, il DEFAULT, le due chiavi cancellate — un file solo, una transazione sola"
      contains: "profiles_role_check"
    - path: "src/lib/capabilities/keys.ts"
      provides: "il catalogo TypeScript senza le due chiavi"
  key_links:
    - from: "supabase/migrations/<timestamp>_role_attendee_and_capability_keys.sql"
      to: "private.role_capabilities"
      via: "il join su valore fra role_capabilities.role e profiles.role, che si muove nella stessa transazione"
      pattern: "role_capabilities"
    - from: "src/lib/capabilities/keys.ts"
      to: "scripts/verify-capabilities.mjs"
      via: "le quattro costanti numeriche e ROLE_GRANTS, confrontati con il catalogo vivo in entrambe le direzioni"
      pattern: "EXPECTED_KEY_COUNT"
---

<objective>
Scrivere la prima migration della fase — il ruolo `member` diventa `attendee` e
le due chiavi di capability del socio escono dal catalogo — applicarla al
**laboratorio**, rileggerla dal catalogo, e muovere nello stesso atto le costanti
e le query vive che altrimenti diventerebbero rosse.

Purpose: D-51-06 e D-51-07. La chiave `membership.active` era stata lasciata
indietro dalla fase 50 **con il nome della fase che la chiude scritto accanto**:
questa.
Output: un file di migration applicato al laboratorio, il catalogo riletto, e
zero gate rossi lasciati indietro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md
@./CLAUDE.md
@.claude/rules/supabase-data.md
@.claude/rules/access-gating.md
@.claude/rules/ai-engineering.md

<interfaces>
Endpoint e regole, gia' misurate dalla fase 50 e da non rimisurare:
- schema: `POST /v1/projects/{ref}/database/migrations` — avvolge il corpo in UNA transazione da solo, quindi **niente `BEGIN;` nel file**
- lettura: `POST /v1/projects/{ref}/database/query` — solo SELECT
- laboratorio: `$LAB_PROJECT_REF` da `.env.lab.local`; produzione `cjsfocnhfzycbbgkwocx`, **fuori dal perimetro di questo piano**
- ogni script di laboratorio rifiuta il ref di produzione come prima riga eseguita

Vincoli vivi, da riconfermare su `51-CATALOG.md` prima di scrivere:
  profiles_role_check            CHECK (role IN ('master','organizer','staff','member'))
  role_capabilities_role_check   stesso insieme
  public.profiles.role           DEFAULT 'member'   (posato nel 2026-02 e mai rimosso)

`scripts/verify-capabilities.mjs`
  const EXPECTED_KEY_COUNT = 17;      // :232  → 15
  const EXPECTED_PAIR_COUNT = 68;     // :700  → 60   (4 ruoli × 15)
  const EXPECTED_GRANT_COUNT = 32;    // :701  → 28   (−4: membership.card.view GRANTED su 4 ruoli)
  const EXPECTED_REFUSAL_COUNT = 36;  // :702  → 32   (−4: membership.active REFUSED su 4 ruoli)
  const ROLE_GRANTS = { … }           // :290 — le chiavi di primo livello sono i ruoli
`scripts/verify-refusal.mjs:459`  .eq("role", "member")   — query VIVA
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere la migration del ruolo e delle due chiavi</name>
  <files>supabase/migrations/<timestamp>_role_attendee_and_capability_keys.sql</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md` — quali vincoli, funzioni, policy e DEFAULT nominano davvero il ruolo: **vince il catalogo**, non l'elenco di `51-RESEARCH.md`
    - `supabase/migrations/20260808000500_staff_role.sql:70-101` — il modello: i due `CHECK` riscritti nello stesso file, e `:79-94`, il paragrafo che spiega perche' non possono essere due migration
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:1-115` — la forma dell'intestazione (verso del deploy, «cosa questa migration NON fa», «l'ordine dei passi», e `:42-55`, perche' non c'e' `BEGIN;`)
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:820-880` — `handle_new_user` viva, con il ruolo **cablato** e il suo `COMMENT`
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:947-980` — la sezione 5b (perche' la chiave era rimasta) e il blocco `DO` che rilegge `pg_policies` prima di cancellare una capability
    - `supabase/migrations/20260807000000_capability_model.sql:118-125` — `private.role_capabilities.capability` con `references private.capabilities(key) on delete cascade`
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:366-392, 419-437` — l'ordine interno e il valore transitorio; le due chiavi e i numeri del gate
  </read_first>
  <action>
    Creare un file di migration il cui nome e' `<timestamp UTC>_role_attendee_and_capability_keys.sql`
    — timestamp nel formato della casa, strettamente maggiore della migration piu'
    alta presente in `supabase/migrations/`.
    Intestazione nella forma della fase 50: titolo in cornice, fase e decisioni
    nominate (D-51-06, D-51-07), il **verso del deploy** (per il ruolo il verso
    e' l'opposto di un `DROP COLUMN`: il `CHECK` deve accettare il valore nuovo
    **prima** che qualcuno lo scriva, quindi in produzione questa migration entra
    subito dopo il deploy del codice, e la finestra fra i due e' dichiarata nel
    piano 51-13), il blocco «cosa questa migration NON fa», il blocco «l'ordine
    dei passi, che non e' estetico», e il paragrafo che dichiara l'assenza di
    `BEGIN;` con la sua ragione misurata.
    Ordine interno, che e' l'unico punto capace di fallire per una ragione di
    **dati**:
    1. `profiles_role_check` allargato a **cinque** valori (i quattro nuovi piu'
       `'member'`), con `DROP CONSTRAINT IF EXISTS` seguito da `ADD CONSTRAINT`
       con lo stesso nome — un `ADD CONSTRAINT` valida le righe esistenti, e le
       righe portano ancora `'member'`;
    2. `role_capabilities_role_check` allargato allo stesso modo;
    3. `UPDATE private.role_capabilities SET role = 'attendee' WHERE role = 'member';`
    4. `UPDATE public.profiles SET role = 'attendee' WHERE role = 'member';`
    5. i due `CHECK` ristretti a quattro valori, con un secondo `ALTER` per
       vincolo (e' la forma che questo repo gia' scrive, ed e' leggibile da chi
       non conosce `NOT VALID`);
    6. `ALTER TABLE public.profiles ALTER COLUMN role SET DEFAULT 'attendee';`
    7. `handle_new_user` ridefinita con `CREATE OR REPLACE` su firma identica —
       il ruolo li' e' **cablato**, non letto dai metadati. Ridefinire il corpo
       **per intero** (il `REPLACE` lo sostituisce), cambiando **solo** il
       letterale del ruolo: il conio del codice socio esce nella migration 2, e
       anticiparlo qui spezzerebbe l'ordine dichiarato;
    8. il blocco `DO` che rilegge `pg_policies` cercando i due letterali
       `membership.active` e `membership.card.view` e **solleva** se una policy
       li nomina ancora — le concessioni non si cancellano finche' una policy le
       legge;
    9. `DELETE` delle concessioni da `private.role_capabilities` e poi delle
       chiavi da `private.capabilities`, in due passi espliciti anche se la
       cascata dichiarata li farebbe in uno: e' il pattern della casa, e si legge.
    Ogni `CHECK` ricreato, il `DEFAULT` e la funzione portano il proprio
    `COMMENT ON …` che dice quanti valori, da dove vengono, cosa resta leggibile
    senza essere piu' scrivibile e quale fase l'ha deciso.
    Il file **non si applica** in questo task.
  </action>
  <verify>
    <automated>ls supabase/migrations/*_role_attendee_and_capability_keys.sql &amp;&amp; test $(/usr/bin/grep -ci "^BEGIN;" supabase/migrations/*_role_attendee_and_capability_keys.sql) -eq 0 &amp;&amp; /usr/bin/grep -q "attendee" supabase/migrations/*_role_attendee_and_capability_keys.sql &amp;&amp; /usr/bin/grep -q "pg_policies" supabase/migrations/*_role_attendee_and_capability_keys.sql</automated>
  </verify>
  <acceptance_criteria>
    - Il file esiste, con timestamp strettamente maggiore di ogni migration presente
    - Zero `BEGIN;` e zero `COMMIT;`
    - I nove passi sono presenti e nell'ordine dichiarato; i due `CHECK` passano da cinque a quattro valori in due `ALTER` distinti
    - `SET DEFAULT 'attendee'` presente
    - Il blocco `DO` su `pg_policies` precede la cancellazione delle chiavi
    - Ogni oggetto toccato porta il proprio `COMMENT`
    - Gli oggetti nominati sono quelli di `51-CATALOG.md`; ogni divergenza dall'elenco della ricerca e' scritta nel file
  </acceptance_criteria>
  <done>La migration esiste, e' leggibile, e non e' ancora stata applicata a nulla.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Applicarla al LABORATORIO e rileggere il catalogo</name>
  <files>supabase/migrations/<timestamp>_role_attendee_and_capability_keys.sql</files>
  <read_first>
    - `scripts/seed-lab-door.mjs:70-92` — il rifiuto del ref di produzione come **prima riga eseguita**: uno script nuovo senza quel blocco non e' uno script di laboratorio
    - `scripts/dev-lab.sh:1-23` — il caricamento di `.env.local` e poi `.env.lab.local`
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:357-364` — l'endpoint da usare e' `migrations`, non `/database/query`, o la history del progetto mente
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md` — i valori «prima», per poter dire cosa e' cambiato
  </read_first>
  <action>
    Applicare il file al **laboratorio** con
    `POST /v1/projects/$LAB_PROJECT_REF/database/migrations`. Se si scrive uno
    script per farlo, la sua prima riga eseguita rifiuta il ref di produzione.
    La produzione e' **fuori dal perimetro di questo piano**: la tocca il 51-13,
    sotto autorizzazione datata.
    Rileggere poi **dal catalogo**, non dalla risposta del `POST`:
    (a) `pg_get_constraintdef` dei due `CHECK` — devono elencare quattro valori,
    con `attendee` e senza `member`;
    (b) il `DEFAULT` di `public.profiles.role`;
    (c) il conteggio delle righe per valore di ruolo in `public.profiles` e
    `private.role_capabilities` — nessuna riga deve portare `member`;
    (d) `select count(*) from private.capabilities` e l'assenza delle due chiavi;
    (e) `pg_get_functiondef` di `handle_new_user`, per vedere il letterale nuovo
    e che `prosecdef` e `proconfig` sono sopravvissuti al `REPLACE`.
    Scrivere i cinque risultati nel SUMMARY con l'ora UTC e la versione
    registrata dalla history. Se uno solo di essi non corrisponde, **fermarsi**:
    una migration che riesce e un catalogo che dice un'altra cosa e' il caso in
    cui si smette, non quello in cui si prosegue.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - La migration risulta nella history del progetto di laboratorio, con la versione registrata scritta nel SUMMARY
    - Le cinque riletture (a)-(e) sono nel SUMMARY, prese dal catalogo e con l'ora UTC
    - Zero righe con ruolo `member` su entrambe le tabelle del laboratorio
    - Le due chiavi non esistono piu' in `private.capabilities`, e nemmeno le loro concessioni
    - Nessuna scrittura sul progetto di produzione: dichiarato nel SUMMARY
  </acceptance_criteria>
  <done>Il laboratorio conosce `attendee`, e lo dice il catalogo.</done>
</task>

<task type="auto">
  <name>Task 3: Le costanti del gate e le query vive si muovono nello stesso atto</name>
  <files>src/lib/capabilities/keys.ts, src/lib/routes/capability-routes.ts, scripts/verify-capabilities.mjs, scripts/verify-refusal.mjs, scripts/rls-baseline.mjs, scripts/container/seed.mjs, scripts/seed-lab-door.mjs</files>
  <read_first>
    - `src/lib/capabilities/keys.ts:295-320` — le due chiavi, e `:390-405` — le loro descrizioni in `CAP_DESCRIPTIONS`
    - `src/lib/routes/capability-routes.ts:405-415` (la voce con `routes: ["/membership-card","/attendance"]`) e `:480-492` (la voce `scope: "table"` di `membership.active`)
    - `scripts/verify-capabilities.mjs:202-232` — il docblock che pretende che **ogni movimento di `EXPECTED_KEY_COUNT` porti la propria riga di storia**; `:290-600` (`ROLE_GRANTS`, quattro blocchi di ruolo, con le righe `membership.active: REFUSED` e `membership.card.view: GRANTED` in ciascuno); `:700-702` (le altre tre costanti)
    - `scripts/verify-refusal.mjs:450-470` — la query viva `.eq("role","member")` e cosa fa il suo `refuse()`
    - `scripts/rls-baseline.mjs:665-745` — `PERSONA_ROLES`, le persone di produzione, il `where role in (…)`
    - `scripts/rls-baseline-compare.mjs:215-225` — **da NON riscrivere**: quel file confronta due artefatti su disco, e la stringa e' la forma con cui `pg_policies` stampava quel predicato prima della fase 32 (D-50-02)
  </read_first>
  <action>
    Togliere `MEMBERSHIP_ACTIVE` e `MEMBERSHIP_CARD_VIEW` da `CAP` in `keys.ts`,
    con le loro voci in `CAP_DESCRIPTIONS`, e **le due voci corrispondenti** in
    `capability-routes.ts`: la mappa e' un `Record` totale sulle chiavi, quindi
    la voce esce con la chiave — togliere le rotte lasciando la chiave
    produrrebbe un'entry che mente.
    In `verify-capabilities.mjs`: portare `EXPECTED_KEY_COUNT` a 15,
    `EXPECTED_PAIR_COUNT` a 60, `EXPECTED_GRANT_COUNT` a 28,
    `EXPECTED_REFUSAL_COUNT` a 32, togliere le otto righe dai quattro blocchi di
    `ROLE_GRANTS` e rinominare la chiave di ruolo `member` in `attendee`. Il
    docblock chiede **la riga di storia accanto al numero**: scriverla, con la
    data e la fase, non solo il numero nuovo.
    In `verify-refusal.mjs:459`: `"member"` diventa `"attendee"`. Senza questo, il
    gate fa `refuse()` con uscita 2 appena la migration passa — ed e' il motivo
    per cui sta in questo piano e non in un altro.
    In `rls-baseline.mjs`: `PERSONA_ROLES`, le personas e il `where role in (…)`
    passano ad `attendee`. **Non toccare** `rls-baseline-compare.mjs`.
    In `scripts/container/seed.mjs` e `scripts/seed-lab-door.mjs`: i banchi
    seminano `attendee`.
    Lanciare i gate vivi **contro il laboratorio**, caricando `.env.local` e poi
    `.env.lab.local` come fa `scripts/dev-lab.sh`, perche' e' li' che la
    migration e' applicata; dichiarare nel SUMMARY che contro la **produzione**
    quei due gate restano rossi fino al piano 51-13, con il piano che li chiude
    nominato — un gate rosso senza il nome di chi lo chiude e' un gate che
    nessuno rilancia.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c "MEMBERSHIP_ACTIVE\|MEMBERSHIP_CARD_VIEW" src/lib/capabilities/keys.ts src/lib/routes/capability-routes.ts | /usr/bin/grep -c ":[1-9]") -eq 0 &amp;&amp; /usr/bin/grep -q "EXPECTED_KEY_COUNT = 15" scripts/verify-capabilities.mjs &amp;&amp; test $(/usr/bin/grep -c '"role", "member"' scripts/verify-refusal.mjs) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero `MEMBERSHIP_ACTIVE` / `MEMBERSHIP_CARD_VIEW` in `keys.ts` e in `capability-routes.ts`
    - Le quattro costanti valgono 15 / 60 / 28 / 32, ognuna con la propria riga di storia datata
    - `ROLE_GRANTS` ha quattro blocchi di ruolo, quello di `attendee` al posto di quello di `member`, senza le otto righe
    - `verify:capabilities` e `verify:refusal` **verdi contro il laboratorio**; il loro stato contro la produzione e' dichiarato nel SUMMARY con il piano che lo chiude (51-13)
    - `rls-baseline-compare.mjs` non compare nel diff
  </acceptance_criteria>
  <done>Il catalogo, la costante TypeScript e i gate dicono la stessa cosa — e il debito che la fase 50 aveva dichiarato e' chiuso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → laboratorio | qui si scrive schema: l'unico progetto che questo piano puo' toccare |
| catalogo dei permessi → RLS | le concessioni sono il modello d'accesso: cancellarne una mentre una policy la legge e' un'elevazione o un blocco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-32 | Elevation of privilege | rinomina del ruolo mentre una policy ne legge il letterale | mitigate | `51-CATALOG.md` ha misurato funzioni e policy sul catalogo vivo; il join e' su **valore** (`role_capabilities.role = profiles.role`) e i due `UPDATE` stanno nella stessa transazione |
| T-51-33 | Denial of Service | un ruolo che non tiene alcuna capability | mitigate | i due `CHECK` e i due `UPDATE` in un file solo: se uno rifiuta, la transazione aborta e **nulla** e' applicato — che e' l'esito voluto |
| T-51-34 | Denial of Service | `ADD CONSTRAINT` rifiutato con 23514 sulle righe esistenti | mitigate | il `CHECK` si allarga a cinque valori **prima** degli `UPDATE` e si stringe a quattro dopo; e' l'unico punto che puo' fallire per una ragione di dati, ed e' per questo che si prova sul laboratorio |
| T-51-35 | Elevation of privilege | chiave cancellata mentre una policy la nomina | mitigate | il blocco `DO` rilegge `pg_policies` e solleva prima di ogni `DELETE` |
| T-51-36 | Repudiation | gate rosso lasciato indietro | mitigate | le quattro costanti, `verify:refusal` e i banchi si muovono nello stesso piano della migration; lo stato contro la produzione e' dichiarato con il piano che lo chiude |
| T-51-37 | Tampering | scrittura accidentale in produzione | mitigate | solo `$LAB_PROJECT_REF`; ogni script di laboratorio rifiuta il ref di produzione come prima riga eseguita; il SUMMARY dichiara zero scritture in produzione |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- Il catalogo del laboratorio dice `attendee` sui due `CHECK`, sul `DEFAULT` e su tutte le righe
- 15 chiavi in `private.capabilities` e 15 in `CAP`
- `verify:capabilities` e `verify:refusal` verdi contro il laboratorio; rossi dichiarati contro la produzione fino al 51-13
- `npm run build` verde
</verification>

<success_criteria>
D-51-06 e D-51-07 sono veri sul laboratorio, letti dal catalogo, e nessun gate
resta rosso senza il nome di chi lo chiude.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-08-SUMMARY.md` when done
</output>
