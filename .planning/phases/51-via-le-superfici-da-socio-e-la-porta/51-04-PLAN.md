---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(members)/membership-card/page.tsx
  - src/app/(members)/membership-card/loading.tsx
  - src/components/membership/MembershipCardView.tsx
  - src/app/(members)/attendance/page.tsx
  - src/app/(members)/dashboard/page.tsx
  - src/utils/qr.ts
  - src/lib/routes/next-redirect.ts
  - next.config.ts
  - scripts/conversion-manifest.mjs
  - scripts/probe-forged-identity.sh
autonomous: true
requirements: [MEM-01, MEM-02]

must_haves:
  truths:
    - "La membership card non ha piu' superficie ne' rotta: /membership-card non esiste (MEM-01)"
    - "Lo storico delle presenze non ha piu' superficie: /attendance non esiste, e nemmeno il suo alias italiano /presenze (MEM-02)"
    - "Nessuna superficie del prodotto linka i due indirizzi"
    - "L'elenco di cio' che e' ammesso e i prefissi protetti sono cambiati insieme, nello stesso commit"
  artifacts:
    - path: "src/lib/routes/next-redirect.ts"
      provides: "NEXT_ALLOW_LIST e PROTECTED_PREFIXES senza i due indirizzi"
      contains: "PROTECTED_PREFIXES"
    - path: "scripts/conversion-manifest.mjs"
      provides: "censimento senza le due voci, con l'aritmetica riscritta e datata"
  key_links:
    - from: "src/lib/routes/next-redirect.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "PROTECTED_PREFIXES importato, non ricopiato"
      pattern: "PROTECTED_PREFIXES"
    - from: "scripts/conversion-manifest.mjs"
      to: "npm run verify:conversion"
      via: "i controlli D/E/F, che leggono il file di pagina dichiarato"
      pattern: "membership-card"
---

<objective>
Cancellare le due superfici da socio — la card e lo storico presenze — e ogni
posto che le nomina come indirizzo.

Purpose: MEM-01 (superficie e rotta) e MEM-02 lato superficie. Lo storico non ha
mai letto nulla: `attendance/page.tsx:67` e' un `TODO` con un array vuoto
costante, quindi qui non si perde alcun dato — il lato dati di MEM-02 e' dei
piani 51-05 e 51-12.
Output: quattro file cancellati, cinque posti che smettono di nominarli, e un
gate del censimento che punta ancora al file giusto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@./CLAUDE.md
@.claude/rules/nextjs-architecture.md
@.claude/rules/access-gating.md

<interfaces>
`src/lib/routes/next-redirect.ts`
  const NEXT_ALLOW_LIST: readonly RegExp[]   // :91-126 — /^\/membership-card$/ :125, /^\/attendance$/ :126
  export const PROTECTED_PREFIXES = [...]    // :145-151 — "/membership-card" :147, "/attendance" :148
  export const DEFAULT_NEXT = "/dashboard";  // :38 — NON si tocca qui: lo muove il piano 51-06
`next.config.ts`
  async redirects()                          // :88-92 — { source: "/presenze", destination: "/attendance" } :90
`scripts/conversion-manifest.mjs`
  entry "/attendance"      :1022
  entry "/membership-card" :1026
`src/utils/qr.ts`
  export async function generateMembershipQR(membershipCode: string): Promise<string>  // :33 — unico chiamante MembershipCardView.tsx:73
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cancellare le quattro superfici e l'unico link che le apre</name>
  <files>src/app/(members)/membership-card/page.tsx, src/app/(members)/membership-card/loading.tsx, src/components/membership/MembershipCardView.tsx, src/app/(members)/attendance/page.tsx, src/app/(members)/dashboard/page.tsx, src/utils/qr.ts</files>
  <read_first>
    - `src/app/(members)/membership-card/page.tsx:85-130` — legge `profiles.membership_code` (`:90`), ripiego `"RSN-UNKNOWN"` (`:94`), monta `MembershipCardView` (`:124-126`)
    - `src/app/(members)/attendance/page.tsx:60-75` — il `TODO` e l'array vuoto costante a `:67`: la superficie non ha mai letto nulla
    - `src/app/(members)/dashboard/page.tsx:499-521` — il blocco «My Stuff», unico posto che linka i due indirizzi (`:511`, `:517`)
    - `src/utils/qr.ts:30-40` — `generateMembershipQR`, e `src/components/membership/MembershipCardView.tsx:4,73` — il suo unico chiamante
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:102-123` — l'inventario, e perche' MEM-02 non perde dati lato superficie
  </read_first>
  <action>
    Cancellare `src/app/(members)/membership-card/page.tsx`, il suo
    `loading.tsx`, `src/components/membership/MembershipCardView.tsx` (unico
    importatore: la pagina) e `src/app/(members)/attendance/page.tsx`. Se
    `src/components/membership/` resta vuota, cancellare anche la directory.
    Togliere da `src/app/(members)/dashboard/page.tsx` l'intero blocco «My
    Stuff» (`:499-521`): con `typedRoutes` un `Link` verso una pagina cancellata
    non compila, quindi il link e la pagina escono nello stesso commit. Non
    toccare altro di quella pagina — «My Tickets», «My Drinks», «Settings» e
    `ManagementSection` li lavora il piano 51-06, che la sposta.
    Togliere `generateMembershipQR` da `src/utils/qr.ts` e verificare che non
    resti alcun chiamante. Non toccare il resto di `qr.ts`: la generazione del QR
    del biglietto e' il percorso della porta. Correggere nello stesso file il
    commento a `:55`, che descrive il conio del codice socio come una cosa viva:
    un commento che spiega una regola che non c'e' piu' e' documentazione datata.
    Nota per chi legge dopo: `src/utils/qr.ts` resta dentro i `paths:` della
    persona (`checkin-offline.md`), e quel glob non diventa morto — il file
    esiste ancora.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test ! -d "src/app/(members)/membership-card" &amp;&amp; test ! -f "src/app/(members)/attendance/page.tsx" &amp;&amp; test $(/usr/bin/grep -rl "generateMembershipQR" src/ | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0 (il gate vero: con `typedRoutes` un link residuo sarebbe un errore di tipo)
    - Le directory `src/app/(members)/membership-card/` e `src/components/membership/` non esistono
    - `src/app/(members)/attendance/page.tsx` non esiste
    - Zero occorrenze di `generateMembershipQR` in `src/`
    - Il diff su `dashboard/page.tsx` tocca **solo** il blocco «My Stuff»
  </acceptance_criteria>
  <done>Le due superfici non esistono, e nessuna pagina prova ad aprirle.</done>
</task>

<task type="auto">
  <name>Task 2: I due indirizzi escono dalle liste d'accesso e dall'alias italiano</name>
  <files>src/lib/routes/next-redirect.ts, next.config.ts</files>
  <read_first>
    - `src/lib/routes/next-redirect.ts:66-90` — il criterio scritto dalla fase 50: togliere una voce da questa lista e' una decisione d'accesso quanto aggiungerne una; `:91-126` (`NEXT_ALLOW_LIST`), `:139-151` (`PROTECTED_PREFIXES`)
    - `next.config.ts:63-92` — il docblock della fase 50 sul 308 che resta in cache, e i tre redirect
    - `scripts/verify-routes.mjs:516-560` — il controllo [3/3]: ogni `PROTECTED_PREFIXES` deve risolvere via `resolveNext` senza sostituzione
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:124-165` — la regola meccanica: le due liste cambiano insieme, e il gemello `/presenze` esce con la pagina
  </read_first>
  <action>
    Togliere `/^\/membership-card$/` e `/^\/attendance$/` da `NEXT_ALLOW_LIST`, e
    `"/membership-card"` e `"/attendance"` da `PROTECTED_PREFIXES`, nello stesso
    commit: toglierli dai prefissi soltanto lascerebbe l'elenco sporco,
    toglierli dall'elenco soltanto renderebbe rosso il controllo [3/3].
    Scrivere accanto la ragione, nella forma che la fase 50 ha gia' usato in
    questo file: qui non e' «il traffico che e' finito», e' l'indirizzo che non
    esiste piu', ed e' una decisione d'accesso dichiarata.
    Togliere da `next.config.ts` la riga con `source: "/presenze"`, che punta a
    una pagina cancellata. Scrivere accanto, nella forma del docblock esistente,
    il fatto che va dichiarato e non scoperto: chi ha gia' seguito `/presenze`
    continuera' a essere mandato su `/attendance` dalla propria cache e ricevera'
    un 404. E' la proprieta' di un 308, non un difetto di questa modifica —
    disposizione `accept`, come `T-50-28`.
    Non aggiungere ancora `/account`: lo fa il piano 51-06, insieme alla pagina
    che serve quell'indirizzo.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:routes &amp;&amp; test $(/usr/bin/grep -c "membership-card" src/lib/routes/next-redirect.ts) -eq 0 &amp;&amp; test $(/usr/bin/grep -c "presenze" next.config.ts) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:routes` verde (in particolare il controllo [3/3])
    - Zero `membership-card` e zero `"/attendance"` in `next-redirect.ts`
    - Zero `presenze` in `next.config.ts`, con il paragrafo di disposizione `accept` scritto accanto
    - `PROTECTED_PREFIXES` resta esportato e importato dal middleware, non ricopiato
  </acceptance_criteria>
  <done>Nessuna lista d'accesso nomina piu' due indirizzi che non esistono.</done>
</task>

<task type="auto">
  <name>Task 3: Il censimento della conversione e la sonda perdono le due voci</name>
  <files>scripts/conversion-manifest.mjs, scripts/probe-forged-identity.sh</files>
  <read_first>
    - `scripts/conversion-manifest.mjs:985-1000` — il paragrafo di aritmetica del censimento, con la data accanto al numero (forma della fase 50)
    - `scripts/conversion-manifest.mjs:1018-1030` — le due voci `/attendance` e `/membership-card`
    - `scripts/probe-forged-identity.sh:15-25` — i cinque indirizzi provati
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md:721-750` — la forma del movimento (diff `74a577f`): la voce si muove nello stesso commit della pagina e l'aritmetica si riscrive con la data
    - `scripts/verify-routes.mjs:51-63` — il docblock che dichiara «a pattern with no page is NOT an error», che e' cio' che rende lecito lasciare in piedi l'entry di `capability-routes.ts` fino al piano 51-08
  </read_first>
  <action>
    Cancellare dal manifest le due voci `/attendance` (`:1022`) e
    `/membership-card` (`:1026`), e riscrivere il paragrafo di aritmetica del
    censimento: il totale cambia, e la prosa porta la propria data accanto al
    numero, perche' il numero si rilegge dal gate a ogni corsa mentre la prosa
    no. Nominare la fase e i requisiti (MEM-01, MEM-02) come fa il precedente.
    Non rinominare ancora la voce `/dashboard` (`:1076`): la rinomina in
    `/account` appartiene al piano 51-06, che sposta il file di pagina.
    Togliere i due indirizzi dall'elenco di `scripts/probe-forged-identity.sh:20`.
    Scrivere nel SUMMARY, come nota d'ordine e non come riparazione da fare qui:
    la voce `[CAP.MEMBERSHIP_CARD_VIEW]` di `src/lib/routes/capability-routes.ts:411-413`
    resta a nominare i due indirizzi fino al piano 51-08, e non rompe il gate
    perche' un pattern senza pagina non e' un errore e il censimento vede solo
    `src/app/(admin)`. Esce insieme alla chiave, perche' la mappa e' un `Record`
    totale sulle chiavi: toglierle le rotte senza togliere la chiave
    produrrebbe un'entry che mente.
  </action>
  <verify>
    <automated>npm run verify:conversion &amp;&amp; test $(/usr/bin/grep -c '"/membership-card"' scripts/conversion-manifest.mjs) -eq 0 &amp;&amp; test $(/usr/bin/grep -c "membership-card" scripts/probe-forged-identity.sh) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:conversion` verde
    - Zero voci `/attendance` e `/membership-card` nel manifest, e l'aritmetica del censimento riscritta **con la data**
    - `scripts/probe-forged-identity.sh` prova tre indirizzi invece di cinque
    - Il SUMMARY dichiara la nota d'ordine su `capability-routes.ts`
  </acceptance_criteria>
  <done>Il gate del censimento misura il prodotto che esiste, e nessuna entry punta a un file cancellato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonimo → rotte protette | chi digita un indirizzo che non esiste piu' |
| gate del censimento → file di pagina | un gate che asserisce la cosa giusta sul file sbagliato e' peggio di nessun gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-14 | Spoofing | `NEXT_ALLOW_LIST` | mitigate | si tolgono due pattern gia' ancorati a entrambi i capi; nessun pattern nuovo, nessun `.*`; `verify:routes` [3/3] e' il gate |
| T-51-15 | Information disclosure | `profiles.membership_code` letto dalla pagina della card | mitigate (in sottrazione) | l'unica superficie che rendeva il codice socio a schermo esce; la colonna la toglie il piano 51-12 |
| T-51-16 | Tampering | entry di manifest che punta a un file cancellato | mitigate | le due entry escono nello stesso commit delle pagine; `/dashboard` resta intatta fino al piano che sposta il file |
| T-51-17 | Denial of Service | 308 su `/presenze` memorizzato dai browser | accept | dichiarato nel docblock: chi l'ha seguito ricevera' un 404 dalla propria cache — proprieta' del 308, non difetto della modifica (come `T-50-28`) |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build` verde dopo ogni task
- `npm run verify:routes` e `npm run verify:conversion` verdi
- `curl -s -o /dev/null -w '%{http_code}' https://lab.resonatemotion.com/membership-card` → 404 dopo il deploy del ramo di fase sul laboratorio (registrato, non preteso qui)
</verification>

<success_criteria>
MEM-01 lato superficie e MEM-02 lato superficie: i due indirizzi non esistono,
nessuna pagina li apre, nessuna lista li ammette, nessun gate li censisce.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-04-SUMMARY.md` when done
</output>
