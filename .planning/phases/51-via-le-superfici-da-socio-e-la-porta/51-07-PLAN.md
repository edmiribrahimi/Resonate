---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 07
type: execute
wave: 2
depends_on: ["51-02"]
files_modified:
  - src/components/scanner/ScanFlash.tsx
  - src/app/(admin)/admin/scanner/ScannerClient.tsx
autonomous: true
requirements: [MEM-03]

must_haves:
  truths:
    - "Lo schermo dell'esito mostra esito e tipo di biglietto, e nessun nome (D-51-05)"
    - "La zona dell'esito e' opaca: niente lista e niente «Scanner paused» che traspaiono (D-51-05)"
    - "Alla seconda lettura resta il FATTO — ora e dispositivo — che e' l'unico appiglio per distinguere due letture ravvicinate; esce solo il nome (D-51-05)"
    - "L'avviso della lista dice «guest list» e si accende solo quando e' vero, perche' e' derivato dall'eta' della lista e non da un array appiccicoso (D-51-10)"
    - "Il titolo della serata e il pulsante di scansione restano in vista mentre la lista scorre (D-51-05)"
  artifacts:
    - path: "src/components/scanner/ScanFlash.tsx"
      provides: "tre sfondi opachi, titolo = esito, sottotitolo = tipo e provenienza"
      contains: "bg-green-500"
    - path: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      provides: "testata spezzata in barra fissa e resto che scorre, avviso derivato dall'eta' della lista"
      contains: "listIsStale"
  key_links:
    - from: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      to: "src/components/scanner/ScanFlash.tsx"
      via: "showFlash(type, title, subtitle) — il nome non passa piu' come titolo"
      pattern: "showFlash"
    - from: "lastFetchAtRef / listAgeMs"
      to: "l'avviso della guest list"
      via: "uno stato calcolato, che non puo' restare acceso quando cio' che descrive e' falso"
      pattern: "listAgeMs"
---

<objective>
Le tre osservazioni del proprietario sulle schermate del laboratorio, riparate:
l'esito opaco e senza nome, l'avviso che dice la verita', la testata che resta
dov'e'.

Purpose: D-51-05 e D-51-10. Il biglietto e' al portatore (D-49-03): un nome sullo
schermo fa respingere un ospite valido, e rifiutare un ospite valido e' peggio
che ammetterne uno due volte.
Output: uno schermo che si legge in un colpo d'occhio, alle due di notte,
davanti a una fila.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@.planning/phases/50-via-le-iscrizioni/deferred-items.md
@./CLAUDE.md
@.claude/rules/checkin-offline.md

<interfaces>
`src/components/scanner/ScanFlash.tsx`
  export type ScanFlashType = "success" | "already_recorded" | "error";   // :22
  interface ScanFlashProps { type; title: string; subtitle?: string; onDismiss }   // :24-30
  FLASH_STATES[...]  bg "bg-green-500/90" :97 · "bg-sem-done/90" :110 · "bg-red-600/90" :125
  contenitore: fixed inset-0 z-[70] ... ${state.bg}                       // :154
  dwell: 1500 / 2500 / 2000 ms — `already_recorded` dura di piu' perche' porta un fatto da leggere

`src/app/(admin)/admin/scanner/ScannerClient.tsx`
  const NOT_VALID_MESSAGE: Record<DoorNotValidReason, string>             // :83 — vocabolario TOTALE dei rifiuti, si riusa
  function readSubjectLabel(body: unknown): string | null                 // :204
  showFlash(type, title, subtitle)                                        // chiamanti a :1760, :1775, :1784, :1809, :1818, :1861, :1885, :1892, :1929, :1962, :1977, :2058, :2097, :2114, :2153, :2186, :2226
  const lastFetchAtRef = useRef<number | null>(null);                     // :1042 — performance.now(), monotono
  const listAgeMs = ...                                                   // :2643
  const listIsStale = ...                                                 // :2665
  testata: <div className="sticky top-0 z-10 bg-ground px-6 pt-6 pb-3">   // :2802, che si chiude a :3156
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: L'esito diventa opaco, e il titolo smette di essere un nome</name>
  <files>src/components/scanner/ScanFlash.tsx, src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `src/components/scanner/ScanFlash.tsx:88-130` — i tre stati con i loro sfondi al 90% e le tre durate, e il commento `:88-93` che dichiara la disciplina del file (i numeri sono stampati dal gate, non asseriti da un commento; il dwell e' informazione)
    - `src/components/scanner/ScanFlash.tsx:150-180` — il contenitore, il titolo e il sottotitolo
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:83-110` (`NOT_VALID_MESSAGE`), `:204-215` (`readSubjectLabel`), `:1855-1870` (il flash dell'annullamento, che passa `record.name`), `:2040-2120` (i chiamanti online), `:2140-2230` (i chiamanti offline, fra cui `:2186` che `deferred-items.md` §7 nomina)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:480-518` — dove tagliare e perche' li': non nella rotta, non nella coda, ma nel titolo
    - `.planning/phases/50-via-le-iscrizioni/deferred-items.md` §7 — la decisione del proprietario e il costo dichiarato
  </read_first>
  <action>
    In `ScanFlash.tsx`: togliere `/90` dai tre sfondi. A 90% il testo «Scanner
    paused» e le righe della lista traspaiono, ed e' esattamente cio' che il
    proprietario ha visto. Non toccare le tre durate: il dwell e' informazione, e
    `already_recorded` dura di piu' perche' porta un fatto che si legge mentre
    qualcuno aspetta.
    In `ScannerClient.tsx`: nessun chiamante di `showFlash` passa piu' un nome
    come **titolo**. Il titolo diventa **l'esito** — ammesso, gia' registrato, o
    il motivo del rifiuto — e il sottotitolo **il tipo di biglietto piu' la
    provenienza** (per esempio tipo e «Offline»), con `ticketType` che vale gia'
    `purchased`, `guest_list` o l'etichetta del tier. Il vocabolario dei rifiuti
    si **riusa**: `NOT_VALID_MESSAGE` e' un `Record` totale su
    `DoorNotValidReason`, e una stringa nuova creerebbe una seconda verita' che
    diverge al primo membro aggiunto.
    Due cose che **restano**: (1) alla seconda lettura il **fatto** — ora e
    dispositivo o operatore — resta nel sottotitolo: e' l'unico appiglio dello
    staff per capire se due letture ravvicinate sono la stessa persona o due, e
    `deferred-items.md` §7 lo nomina fra i costi; esce il nome, non il fatto.
    (2) Il nome resta nella **lista** della serata e nella cronologia delle
    scansioni (`:3384`, `:3435`), che sono elenchi consultati, non un verdetto
    che lampeggia su una persona davanti a una fila — e la ricerca per nome e'
    proprio come entra un invitato di guest list senza QR (D-51-10).
    Anche il flash dell'annullamento (`:1861`) smette di passare `record.name`.
    Se `readSubjectLabel` e i lettori di `member_name` restano senza consumatori,
    escono con i chiamanti.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:scan-legibility &amp;&amp; test $(/usr/bin/grep -c "/90" src/components/scanner/ScanFlash.tsx) -eq 0 &amp;&amp; test $(/usr/bin/grep -c "member_name" "src/app/(admin)/admin/scanner/ScannerClient.tsx") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0 e `npm run verify:scan-legibility` e' verde (i numeri li stampa il gate)
    - Zero `/90` in `ScanFlash.tsx`; le tre durate invariate nel diff
    - Nessun chiamante di `showFlash` passa come titolo `cached.name`, `record.name`, un `readSubjectLabel(...)` o un `member_name`
    - Il sottotitolo di `already_recorded` porta ancora ora e dispositivo/operatore
    - Zero stringhe di rifiuto nuove: `NOT_VALID_MESSAGE` resta l'unico vocabolario
  </acceptance_criteria>
  <done>Lo schermo dice cosa e' successo e che biglietto era, e non dice chi e'.</done>
</task>

<task type="auto">
  <name>Task 2: L'avviso della lista diventa uno stato calcolato, e dice «guest list»</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:1121-1180` — `fetchAttendance` e i due rami che escono prima (`fetch` fallito con `navigator.onLine` falso, e risposta non-`ok`), ognuno con il proprio `return`
    - `:1320-1345` — l'unica chiamata a `setCacheNotices`, dopo tutti i rami di fallimento, e la scrittura di `lastFetchAtRef`
    - `:1042, :2643, :2665` — `lastFetchAtRef` (monotono, con il commento che spiega perche' non `Date.now()`), `listAgeMs`, `listIsStale`
    - `:3215-3226` — la fascia che gia' si disegna a partire da uno stato calcolato: e' la forma da seguire
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:202-217` — perche' l'avviso resta acceso col badge «Online», e perche' la riparazione derivata e' corretta in entrambi i casi (assunzione A4)
  </read_first>
  <action>
    Sostituire l'avviso appiccicoso con uno **derivato**. La condizione e' l'eta'
    della lista o la sua assenza — `listAgeMs === null` (mai scaricata) oppure
    `listIsStale` — non un elemento che qualcuno ha spinto dentro un array e che
    nessun ramo di uscita anticipata toglie. Un avviso derivato non puo' restare
    acceso quando cio' che descrive e' falso, perche' non e' uno stato: e' una
    funzione.
    Il testo dice **guest list**, non «member list»: cio' che conta ora e' che un
    invitato senza email si trova per nome nella lista scaricata, e se la lista e'
    vecchia o assente puo' non esserci. Restare nella stessa forma di frase del
    precedente — dire cosa rischia di mancare e **cosa fare invece di
    rifiutare** — perche' l'asimmetria della porta e' quella: rifiutare un ospite
    valido e' peggio che ammetterne uno due volte.
    Non aggiungere alcun flag booleano nuovo: la macchina giusta esiste gia' ed e'
    `lastFetchAtRef` / `listAgeMs` / `listIsStale`. Se serve distinguere «mai
    scaricata» da «vecchia», distinguerlo **nel testo**, non in un secondo stato.
    Il download della lista resta quello che c'e': `GET /api/tickets/attendance`,
    una chiamata sola (D-51-10, e la ricerca lo ha misurato). Non si aggiunge una
    rotta, non si aggiunge un pulsante.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -ci "member list" "src/app/(admin)/admin/scanner/ScannerClient.tsx") -eq 0 &amp;&amp; /usr/bin/grep -qi "guest list" "src/app/(admin)/admin/scanner/ScannerClient.tsx" &amp;&amp; /usr/bin/grep -q "listIsStale" "src/app/(admin)/admin/scanner/ScannerClient.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero occorrenze di «member list» nel file; l'avviso dice «guest list»
    - L'avviso e' calcolato da `listAgeMs` / `listIsStale`, e non compare in alcun array di notice spinto a mano
    - Nessun flag booleano nuovo, nessuna rotta nuova, nessun pulsante nuovo
  </acceptance_criteria>
  <done>L'avviso si accende quando la lista e' davvero vecchia o assente, e si spegne quando non lo e'.</done>
</task>

<task type="auto">
  <name>Task 3: La testata si spezza in una barra fissa e un resto che scorre</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:2802` — il `div` gia' `sticky top-0`, e `:3156`, dove si chiude: 355 righe di JSX dentro un solo elemento appiccicato
    - `:2804-2899` (titolo della serata e pulsante di scansione), `:2903-2916` (avviso di serata finita), `:2945-2995` (pastiglie di stato), `:3004` (deriva dell'orologio), `:3046-3066` (contatore), `:3115` (ricerca), `:3140-3155` (le tre linguette di filtro)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:520-536` — la misura: un elemento `sticky` piu' alto della finestra pinna il proprio bordo, non il proprio contenuto; e le due cause alternative gia' escluse (nessun antenato con `overflow`, nessun problema di safe area)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md:947-953` — «No Analog Found»: nessun altro punto del prodotto ha una testata `sticky` a due livelli; la causa e' sostenuta dal codice ma si conferma **sullo schermo**
  </read_first>
  <action>
    Spezzare il blocco in due: una **barra sottile fissa** che porta il titolo
    della serata e il pulsante di scansione e **nient'altro**, e un **resto che
    scorre** con avvisi, pastiglie, contatore, ricerca e linguette. Il criterio
    che decide cosa sta nella barra non e' estetico ed e' uno solo: **la parte
    fissa deve stare comodamente dentro la finestra di un telefono**, o si
    riproduce lo stesso difetto con meno righe.
    Scrivere accanto, come commento, la misura che spiega il difetto — un
    elemento `sticky` piu' alto della finestra pinna il proprio bordo superiore,
    e tutto cio' che sta sopra il bordo della finestra scorre via — cosi' chi
    legge dopo non «ripara» rimettendo dentro le pastiglie.
    Non cambiare alcuna decisione di ammissione: e' presentazione pura. Non
    toccare le linguette, il contatore o la ricerca: si spostano, non cambiano.
    **Dichiarare nel SUMMARY che questa e' l'assunzione A2 della ricerca**, e che
    la conferma non e' questo build ma il passo 6 di `P-51-1` nella corsa
    «dopo», sullo schermo di un telefono vero. Se quel passo la smentisse, la
    riparazione e' da rifare e la ragione sara' un'altra.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:touch-targets; npm run verify:breakpoints &amp;&amp; test $(/usr/bin/grep -c "sticky top-0" "src/app/(admin)/admin/scanner/ScannerClient.tsx") -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Esiste una barra fissa che contiene **solo** titolo della serata e pulsante di scansione; avvisi, pastiglie, contatore, ricerca e linguette sono fuori da essa
    - Il commento che spiega la causa (altezza del blocco `sticky`) e' scritto accanto
    - `npm run verify:breakpoints` verde; `verify:touch-targets` non peggiora rispetto al suo stato pre-esistente, che e' gia' rosso ed e' dichiarato nella voce 12 di `deferred-items` della fase 58
    - Il SUMMARY dichiara che la conferma e' il passo 6 di `P-51-1` nella corsa «dopo»
  </acceptance_criteria>
  <done>Titolo e pulsante restano in vista mentre la lista scorre — e la prova finale e' su un telefono, non in un file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schermo della porta → persona in fila | l'unico posto in cui un dato si trasforma in un giudizio su qualcuno, in pubblico |
| stato dell'interfaccia → verita' del dispositivo | un avviso che mente e' peggio di nessun avviso, perche' viene creduto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-27 | Information disclosure | `ScanFlash` titolo | mitigate | il nome esce dal verdetto lampeggiante e resta solo nelle liste consultate; il taglio e' nel titolo, non nella rotta ne' nella coda, o si romperebbe D-51-10 |
| T-51-28 | Denial of Service | ospite valido respinto perche' lo schermo non si legge | mitigate | sfondi opachi (niente `/90`), `verify:scan-legibility` dopo; il fatto di `already_recorded` resta leggibile |
| T-51-29 | Repudiation | avviso appiccicoso che resta acceso col badge «Online» | mitigate | avviso derivato da `listAgeMs`/`listIsStale`: uno stato calcolato non puo' restare acceso falso |
| T-51-30 | Denial of Service | testata che scorre via e nasconde il pulsante di scansione | mitigate | barra fissa con il criterio dell'altezza; conferma su schermo al passo 6 di `P-51-1` (assunzione A2) |
| T-51-31 | Tampering | vocabolario dei rifiuti duplicato | mitigate | si riusano `NOT_VALID_MESSAGE` e `NOT_VALID_REASONS`, `Record` totali su `DoorNotValidReason` |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build` verde dopo ogni task
- `npm run verify:scan-legibility` verde dopo il task 1
- Nessuna decisione di ammissione modificata: il diff non tocca i percorsi di check-in ne' la coda
</verification>

<success_criteria>
Le tre osservazioni del proprietario sono riparate nel codice; la loro conferma
e' lo schermo di un telefono vero, al passo 3 e al passo 6 di `P-51-1`.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-07-SUMMARY.md` when done
</output>
