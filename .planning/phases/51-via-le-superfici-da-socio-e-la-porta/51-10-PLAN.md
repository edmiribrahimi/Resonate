---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 10
type: execute
wave: 3
depends_on: ["51-06", "51-08"]
files_modified:
  - src/types/database.ts
  - src/lib/rbac/roles.ts
  - src/lib/supabase/middleware.ts
  - src/app/(members)/account/page.tsx
  - src/app/(public)/tickets/refund-actions.ts
  - src/app/api/webhooks/sumup/route.ts
  - src/app/api/cron/event-reminders/route.ts
  - src/lib/errors/redact.ts
  - src/lib/failure/money-path.ts
  - src/lib/tickets/guest-identity.ts
  - src/app/api/tickets/attendance/route.ts
  - src/app/(public)/events/[slug]/page.tsx
autonomous: true
requirements: [MEM-01]

must_haves:
  truths:
    - "Il tipo UserRole ha quattro valori e il quarto e' attendee (D-51-06)"
    - "Ogni ripiego che risolveva un ruolo sconosciuto risolve ad attendee, e nessuna risoluzione di ripiego e' diventata piu' permissiva"
    - "La pagina dell'account non seleziona piu' profiles.membership_code (D-51-02)"
    - "Nessun testo verso una persona la chiama «Member» come ripiego"
  artifacts:
    - path: "src/types/database.ts"
      provides: "UserRole a quattro valori, Profile senza membership_code"
      contains: "attendee"
    - path: "src/lib/rbac/roles.ts"
      provides: "la costante del ruolo, rinominata"
  key_links:
    - from: "src/types/database.ts"
      to: "profiles_role_check"
      via: "lo specchio dichiarato: il CHECK esiste perche' rispecchia UserRole"
      pattern: "UserRole"
    - from: "src/lib/supabase/middleware.ts"
      to: "private.has_capability"
      via: "le risoluzioni di ripiego, che decidono cosa vale un utente senza riga di profilo"
      pattern: "attendee"
---

<objective>
Il tipo, le risoluzioni di ripiego del middleware, la pagina dell'account e il
lessico verso le persone smettono di dire «member».

Purpose: D-51-06 e D-51-02. Il `CHECK` esiste perche' rispecchia `UserRole`, e
uno specchio aggiornato a meta' non e' piu' uno specchio.
Output: quattro valori in un posto solo, e nessuna frase che chiami «Member» chi
ha comprato un biglietto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@./CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/comms-analytics.md

<interfaces>
`src/types/database.ts`
  export type UserRole = "master" | "organizer" | "staff" | "member";   // :99
  export interface Profile { … membership_code: string; role: UserRole; … }  // :110-116
`src/lib/rbac/roles.ts`
  MEMBER: "member",                                                     // :105 — zero consumatori misurati di ROLES.MEMBER
`src/lib/supabase/middleware.ts`
  il ripiego a ruolo/stato quando non c'e' riga di profilo                // :395-420
`src/app/(members)/account/page.tsx` (spostata dal piano 51-06)
  .select("membership_code, role, created_at")                           // era :187
  const isMemberRole = !profile?.role || profile.role === "member";      // era :208
  role={role ?? "member"}                                                // era :350
Ripieghi testuali «Member» verso una persona:
  src/app/(public)/tickets/refund-actions.ts:376, :467
  src/app/api/webhooks/sumup/route.ts:239
  src/app/api/cron/event-reminders/route.ts:175, :188
  src/app/(members)/account/page.tsx (era :183, :205)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Il tipo, la costante e le risoluzioni di ripiego</name>
  <files>src/types/database.ts, src/lib/rbac/roles.ts, src/lib/supabase/middleware.ts</files>
  <read_first>
    - `src/types/database.ts:90-120` — `UserRole` e il paragrafo che la fase 50 ha lasciato dov'era l'unione dello stato, con la ragione d'ordine scritta accanto; `Profile`, che dichiara `membership_code`
    - `src/lib/rbac/roles.ts:95-115` — le costanti dei ruoli, e il blocco dove stavano quelle di stato uscite nella fase 50
    - `src/lib/supabase/middleware.ts:390-425` — il ripiego quando non c'e' riga di profilo, i due commenti che lo spiegano e la riga `:417` che nomina il ruolo
    - `supabase/migrations/20260808000500_staff_role.sql:79-94` — il commento del `CHECK`, che dichiara di **rispecchiare `UserRole`**
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:394-406` — i nove file, e la misura che `ROLES.MEMBER` ha zero riscontri nel repo
  </read_first>
  <action>
    Portare `UserRole` a `"master" | "organizer" | "staff" | "attendee"` e
    togliere `membership_code` da `Profile` — l'interfaccia descrive colonne, e
    quella colonna cade nel piano 51-12; il file stesso dichiara altrove che
    un'interfaccia che descrive colonne inesistenti e' peggio di nessuna
    interfaccia. Scrivere accanto, nella forma che la fase 50 ha usato qui, cosa
    e' uscito e perche'.
    In `roles.ts`: `MEMBER: "member"` diventa `ATTENDEE: "attendee"`. La costante
    non ha consumatori misurati, quindi il build non la difende: se ne compare
    uno, si muove con lei.
    In `middleware.ts`: ogni risoluzione di ripiego che scriveva `member` scrive
    `attendee`, e i commenti che la spiegano si aggiornano. **Il ripiego non
    diventa piu' permissivo**: un utente autenticato senza riga di profilo deve
    continuare a valere esattamente quanto valeva — nessuna capability. Il
    middleware e' UX, il confine e' la RLS, e questo piano non tocca una sola
    policy.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; /usr/bin/grep -q 'UserRole = "master" | "organizer" | "staff" | "attendee"' src/types/database.ts &amp;&amp; test $(/usr/bin/grep -c "membership_code" src/types/database.ts) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `UserRole` ha quattro valori e il quarto e' `attendee`
    - `Profile` non dichiara piu' `membership_code` (l'unica occorrenza residua ammessa nel file e' quella del tipo di riga del registro, che muove il piano 51-11)
    - Nessuna risoluzione di ripiego concede piu' di prima: il diff del middleware non tocca alcuna decisione di capability
    - Nessuna policy toccata in questo piano
  </acceptance_criteria>
  <done>Lo specchio fra il tipo e il `CHECK` e' di nuovo intero.</done>
</task>

<task type="auto">
  <name>Task 2: La pagina dell'account smette di leggere il codice socio</name>
  <files>src/app/(members)/account/page.tsx</files>
  <read_first>
    - `src/app/(members)/account/page.tsx` — la `select` del profilo, il calcolo del ramo per chi non ha un ruolo di lavoro, e il ruolo passato a `PostHogIdentify` (nel file spostato dal piano 51-06: le righe corrispondono a `:186-189`, `:208`, `:350` del file d'origine)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:180-183` — i due punti: `membership_code` esce dalla `select`, e i due letterali passano ad `attendee`
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md` — D-51-02
  </read_first>
  <action>
    Togliere `membership_code` dalla `select` del profilo: nessun blocco della
    pagina lo usa piu' dopo il piano 51-06, e una lettura che non alimenta nulla
    e' peso su un percorso, non una cosa innocua.
    Portare ad `attendee` i due letterali: la condizione che distingue chi non ha
    un ruolo di lavoro, e il ripiego del ruolo passato all'identificazione
    analitica. Mantenere **identico** il comportamento del ramo: chi non ha riga
    di profilo continua a essere trattato come prima.
    Portare il ripiego del nome visibile fuori dalla parola «Member»: chi legge
    quella pagina ha comprato un biglietto, non e' un socio, e il prodotto non ha
    piu' soci.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c "membership_code" "src/app/(members)/account/page.tsx") -eq 0 &amp;&amp; test $(/usr/bin/grep -c '"member"' "src/app/(members)/account/page.tsx") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero `membership_code` e zero `"member"` nella pagina
    - Il ramo per chi non ha ruolo di lavoro si comporta come prima (dichiarato nel SUMMARY leggendo il diff)
  </acceptance_criteria>
  <done>La pagina dell'account non chiede piu' al database una credenziale che sta per sparire.</done>
</task>

<task type="auto">
  <name>Task 3: Il lessico verso le persone, e i commenti che descrivono una credenziale viva</name>
  <files>src/app/(public)/tickets/refund-actions.ts, src/app/api/webhooks/sumup/route.ts, src/app/api/cron/event-reminders/route.ts, src/lib/errors/redact.ts, src/lib/failure/money-path.ts, src/lib/tickets/guest-identity.ts, src/app/api/tickets/attendance/route.ts, src/app/(public)/events/[slug]/page.tsx</files>
  <read_first>
    - `src/app/(public)/tickets/refund-actions.ts:370-380` e `:460-470`, `src/app/api/webhooks/sumup/route.ts:235-245`, `src/app/api/cron/event-reminders/route.ts:170-192` — i ripieghi `full_name || "Member"`, che finiscono **dentro una mail**
    - `src/lib/errors/redact.ts:5-20` — la redazione della riga che fallisce, che nomina `membership_code` come **unica credenziale d'ingresso**
    - `src/lib/failure/money-path.ts:90-100` e `src/lib/tickets/guest-identity.ts:14-22` — i due paragrafi che descrivono il conio del codice a ogni nuovo account
    - `src/app/api/tickets/attendance/route.ts:140-150` e `src/app/(public)/events/[slug]/page.tsx:325-335` — i due commenti che chiamano il codice socio «la credenziale della porta»
    - `.claude/rules/comms-analytics.md` — una lingua sola per percorso, e cosa puo' comparire in un testo verso una persona
  </read_first>
  <action>
    Sostituire il ripiego `"Member"` con una parola che sia vera per chi la
    ricevera': chi legge quelle mail ha **comprato un biglietto** o e' stato
    invitato. Sceglierne **una sola** e usarla in tutti e cinque i siti: cinque
    ripieghi diversi per la stessa situazione sono cinque verita' che divergono.
    Non toccare nient'altro di quei tre percorsi — rimborso, webhook dei
    pagamenti e cron dei promemoria sono percorsi di denaro e di consegna, e qui
    si cambia una stringa di ripiego, non una decisione.
    Correggere i commenti che descrivono `membership_code` come una credenziale
    **viva**: dal piano 51-12 non esistera'. In `redact.ts` la **regola** resta
    (un messaggio di errore Postgres puo' portare i valori della riga che
    fallisce, e vanno redatti): cambia l'esempio, non il comportamento. In
    `guest-identity.ts` e `money-path.ts` il paragrafo dice che ogni account
    nuovo porta con se' un codice: dopo questa fase non lo porta piu'.
    Non toccare le parole «member/membership» nei documenti e nella persona: la
    riscrittura del lessico li' e' della **fase 57**, ed e' una Deferred Idea di
    questa.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c '|| "Member"' src/app/\(public\)/tickets/refund-actions.ts src/app/api/webhooks/sumup/route.ts src/app/api/cron/event-reminders/route.ts | /usr/bin/grep -vc ":0$") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero ripieghi `"Member"` nei cinque siti, e la parola scelta e' **la stessa** in tutti
    - Nessuna modifica di comportamento nei percorsi di rimborso, webhook e cron: il diff tocca solo la stringa di ripiego
    - `redact.ts` conserva la regola e cambia l'esempio
    - Nessun file sotto `.claude/` o `docs/` toccato (la fase 57 resta intatta)
  </acceptance_criteria>
  <done>Nessun testo verso una persona la chiama con un nome che il prodotto non usa piu'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| utente autenticato senza riga di profilo → middleware | il ripiego decide cosa vale chi non ha un profilo |
| errore Postgres → log → screenshot | la riga che fallisce puo' portare valori di una persona |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-42 | Elevation of privilege | risoluzione di ripiego del middleware | mitigate | il ripiego cambia **nome**, non permessi: nessuna capability concessa in piu', e il confine resta la RLS, che questo piano non tocca |
| T-51-43 | Tampering | specchio `UserRole` ↔ `CHECK` | mitigate | i quattro valori si muovono nello stesso atto del `CHECK` (piano 51-08), e il commento del vincolo dichiara lo specchio |
| T-51-44 | Information disclosure | redazione dei messaggi d'errore | mitigate | `redact.ts` conserva la regola: cambia l'esempio, non il comportamento |
| T-51-45 | Denial of Service | percorsi di denaro toccati per una stringa | mitigate | il diff su rimborso, webhook e cron tocca **solo** il ripiego testuale, e l'acceptance lo asserisce |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build` verde dopo ogni task
- `npm run verify` verde salvo i rossi gia' dichiarati contro la produzione fino al piano 51-13
- Nessun file `.claude/**` nel diff
</verification>

<success_criteria>
Il prodotto ha un solo nome per chi partecipa, ed e' quello che il database
accetta.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-10-SUMMARY.md` when done
</output>
