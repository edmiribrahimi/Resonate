---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 11
type: execute
wave: 4
depends_on: ["51-09", "51-10"]
files_modified:
  - src/lib/membership/acts.ts
  - src/lib/account/acts.ts
  - src/types/database.ts
  - src/app/(admin)/admin/members/actions.ts
  - src/app/(admin)/admin/(work)/members/register/page.tsx
  - src/app/api/auth/callback/route.ts
  - src/lib/email-delivery/categories.ts
  - scripts/rls-baseline.mjs
  - scripts/container/seed.mjs
  - scripts/seed-lab-door.mjs
autonomous: true
requirements: [MEM-01]

must_haves:
  truths:
    - "Il registro degli atti si chiama account_acts nel codice, e la funzione che lo scrive si chiama record_account_act (D-51-08)"
    - "Il modulo del vocabolario del registro vive in src/lib/account/, e src/lib/membership/ non esiste piu'"
    - "Nessuno script di semina inserisce piu' profiles.membership_code (D-51-02)"
    - "I valori storici dell'unione degli atti restano leggibili e nessuno li riscrive"
  artifacts:
    - path: "src/lib/account/acts.ts"
      provides: "il vocabolario del registro, spostato con la sua storia"
      contains: "AccountAct"
    - path: "src/types/database.ts"
      provides: "il tipo di riga del registro, rinominato"
  key_links:
    - from: "src/app/(admin)/admin/members/actions.ts"
      to: "public.record_account_act"
      via: "la chiamata rpc, che cambia nome nello stesso atto della funzione SQL"
      pattern: "record_account_act"
    - from: "src/lib/account/acts.ts"
      to: "il CHECK SQL su account_acts.act"
      via: "lo specchio dichiarato dal file: modificare un insieme di letterali significa modificare entrambi"
      pattern: "AccountAct"
---

<objective>
Il registro degli atti cambia nome nel codice — `membership_acts` diventa
`account_acts`, `record_membership_act` diventa `record_account_act`, e il modulo
del vocabolario si sposta in `src/lib/account/` — e gli script smettono di
seminare il codice socio.

Purpose: D-51-08, deciso dal proprietario sapendo che costa una migration in
piu'. Questo e' **il codice**: la migration che rinomina davvero gli oggetti e'
il piano 51-12, e il verso dichiarato e' codice prima, schema dopo.
Output: un modulo spostato, un tipo rinominato, tre chiamanti allineati e due
banchi che seminano cio' che lo schema avra'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md
@./CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/supabase-data.md

<interfaces>
`src/lib/membership/acts.ts` — il modulo e' **la sorgente** dei letterali: non
importa nulla, nemmeno `@/types/database`, che importa da lui.
  export type MembershipAct = "created" | "approved" | ... | "deleted";   // dieci valori
  export type MembershipActorKind = "user" | "system";
I consumatori misurati:
  src/types/database.ts:31-33 (import), :908-1000 (`MembershipActRow`)
  src/app/(admin)/admin/members/actions.ts:9 (import del tipo), :794 (`rpc("record_membership_act", …)`)
  src/app/(admin)/admin/(work)/members/register/page.tsx:10 (import del tipo), :87-95 (le colonne nominate una volta), :226 (`from("membership_acts")`), :282-296 (la lettura di `membership_code` per l'etichetta)
  src/app/api/auth/callback/route.ts:190-230 (due commenti)
  src/lib/email-delivery/categories.ts:77 (commento)
  scripts/rls-baseline.mjs:1368-1380 (la voce `membership_acts` del probe), :1552 (le colonne, fra cui `membership_code`)
  scripts/container/seed.mjs:485-520 e scripts/seed-lab-door.mjs:220-230 (gli `insert` che scrivono `membership_code`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spostare il modulo e rinominare i due tipi</name>
  <files>src/lib/membership/acts.ts, src/lib/account/acts.ts, src/types/database.ts</files>
  <read_first>
    - `src/lib/membership/acts.ts` per intero — in particolare `:1-20` (perche' questo modulo non importa nulla), `:10-19` (la regola dello specchio: i due insiemi si modificano nello stesso commit), `:55-105` (i valori storici che restano leggibili e nessuno scrive piu')
    - `src/types/database.ts:28-35` (l'import), `:900-1000` (`MembershipActRow` e i suoi commenti, che nominano `record_membership_act` e `public.membership_acts`)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md:947-953` — «No Analog Found»: nessun modulo di `src/lib/` e' mai stato spostato in questo repo; non c'e' un redirect a coprire uno spostamento di modulo, quindi si spostano **il file e ogni importatore nello stesso commit**, e il build e' l'unico gate
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md` — D-51-08
  </read_first>
  <action>
    Spostare il modulo in `src/lib/account/acts.ts` con `git mv`, e rinominare i
    due tipi in `AccountAct` e `AccountActorKind`. Il modulo resta **la
    sorgente**: continua a non importare nulla, per la ragione gia' scritta nel
    suo docblock.
    Aggiornare il docblock dove nomina la tabella: l'insieme dei letterali e'
    specchiato da un `CHECK` SQL, e **quel CHECK non cambia valori** in questa
    fase — cambia solo il nome della tabella su cui vive (piano 51-12). Scriverlo
    esplicitamente, o il prossimo lettore credera' che lo specchio sia stato
    rotto.
    I dieci valori **restano tutti**, compresi i quattro che nessuno scrive piu'
    dalla fase 50: sono valori da leggere, il registro e' append-only, e
    un'unione che non sapesse nominare una riga esistente renderebbe illeggibile
    la storia che il registro esiste per conservare.
    In `database.ts`: `MembershipActRow` diventa `AccountActRow`, l'import segue
    il modulo, e i commenti che nominano `public.membership_acts` e
    `record_membership_act` nominano i nomi nuovi. Se `src/lib/membership/` resta
    vuota, **cancellarla**: una directory vuota nominata da un glob della persona
    diventerebbe un path morto al prossimo `verify:persona`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f src/lib/account/acts.ts &amp;&amp; test ! -d src/lib/membership &amp;&amp; /usr/bin/grep -q "AccountActRow" src/types/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `src/lib/account/acts.ts` esiste, `src/lib/membership/` non esiste
    - I due tipi si chiamano `AccountAct` e `AccountActorKind`, e l'unione ha ancora **dieci** valori
    - `AccountActRow` in `database.ts`, con i commenti allineati ai nomi nuovi
    - Il docblock dichiara che il `CHECK` cambia tabella e **non** valori
  </acceptance_criteria>
  <done>Il vocabolario del registro vive dove vive il modello nuovo, e non ha perso un valore.</done>
</task>

<task type="auto">
  <name>Task 2: I tre chiamanti — creazione account, pagina del registro, commenti</name>
  <files>src/app/(admin)/admin/members/actions.ts, src/app/(admin)/admin/(work)/members/register/page.tsx, src/app/api/auth/callback/route.ts, src/lib/email-delivery/categories.ts</files>
  <read_first>
    - `src/app/(admin)/admin/members/actions.ts:5-15` (l'import del tipo) e `:785-800` (la chiamata `rpc("record_membership_act", …)`, che e' il percorso di `createAccount`)
    - `src/app/(admin)/admin/(work)/members/register/page.tsx:85-100` (le colonne nominate una volta), `:180-200` (il commento che nomina la policy `membership_acts_select_register_read`), `:220-230` (`from("membership_acts")`), `:275-300` (la lettura di `full_name, membership_code` per l'etichetta del soggetto)
    - `src/app/api/auth/callback/route.ts:190-230` — i due commenti che nominano la funzione e la tabella
    - `src/lib/email-delivery/categories.ts:70-80` — il commento
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:455-477` — l'elenco lato TypeScript, e il vincolo: gli oggetti veri li dice `51-CATALOG.md`
  </read_first>
  <action>
    Portare la chiamata `rpc` al nome nuovo, `record_account_act`, e le letture
    `from("membership_acts")` a `from("account_acts")`. La policy nominata nei
    commenti della pagina del registro cambia nome con la tabella (piano 51-12):
    nominarla come si chiamera'.
    Nella pagina del registro, togliere la lettura di `membership_code` usata per
    costruire l'etichetta del soggetto: dal piano 51-12 l'etichetta la calcola la
    funzione SQL (D-51-15, prime 8 cifre dell'identificativo) e le righe storiche
    continuano a portare i codici gia' emessi. La pagina **mostra** cio' che la
    riga porta: non ricostruisce un'etichetta per conto proprio, o esisterebbero
    due verita' che divergono sulle righe vecchie.
    Aggiornare i commenti di `auth/callback/route.ts` e di
    `email-delivery/categories.ts`.
    **La finestra, da dichiarare nel SUMMARY e non da scoprire:** da questo
    commit fino all'applicazione della migration del piano 51-12, il codice
    chiama una funzione SQL che non esiste ancora con quel nome, e la creazione
    di un account fallisce. E' il verso dichiarato — codice prima, schema dopo —
    ed e' la ragione per cui in produzione i due passi stanno **dentro un solo
    atto autorizzato**, uno dopo l'altro (piano 51-13). Sul laboratorio la
    finestra dura quanto il piano 51-12.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -rc "record_membership_act\|membership_acts" src/ | /usr/bin/grep -vc ":0$") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero occorrenze di `record_membership_act` e di `membership_acts` sotto `src/`
    - La pagina del registro non legge piu' `membership_code` e non ricostruisce alcuna etichetta
    - Il SUMMARY dichiara la finestra e nomina il piano che la chiude
  </acceptance_criteria>
  <done>Il codice chiama il registro con il nome che avra', e lo dice invece di lasciarlo scoprire.</done>
</task>

<task type="auto">
  <name>Task 3: I banchi e la linea di base smettono di seminare il codice socio</name>
  <files>scripts/rls-baseline.mjs, scripts/container/seed.mjs, scripts/seed-lab-door.mjs</files>
  <read_first>
    - `scripts/rls-baseline.mjs:1360-1385` — la voce `membership_acts` del probe e il commento sulla sentinella; `:1545-1560` — le colonne interrogate, fra cui `membership_code`
    - `scripts/container/seed.mjs:485-520` — i due `insert into public.profiles (…, membership_code, role)`
    - `scripts/seed-lab-door.mjs:70-92` (il rifiuto del ref di produzione) e `:220-235` (l'`insert` con `membership_code`)
    - `scripts/rls-baseline-compare.mjs:215-225` — **da NON toccare**, per la ragione gia' registrata (D-50-02): confronta due artefatti su disco, non un database
  </read_first>
  <action>
    In `rls-baseline.mjs`: la voce del probe punta a `account_acts`, e le colonne
    interrogate perdono `membership_code`. Se la sentinella del probe era
    costruita sulla forma del codice, sostituirla con una sentinella che non
    somigli a una credenziale, e dirlo accanto.
    In `container/seed.mjs` e `seed-lab-door.mjs`: gli `insert` smettono di
    scrivere `membership_code`, che dal piano 51-12 non esiste; il ruolo
    seminato e' gia' `attendee` dal piano 51-08, quindi qui si tocca **solo** la
    colonna. `seed-lab-door.mjs` conserva il rifiuto del ref di produzione come
    prima riga eseguita: uno script che lo perdesse non sarebbe piu' uno script
    di laboratorio.
    Non toccare `rls-baseline-compare.mjs`.
  </action>
  <verify>
    <automated>test $(/usr/bin/grep -c "membership_code" scripts/rls-baseline.mjs scripts/container/seed.mjs scripts/seed-lab-door.mjs | /usr/bin/grep -vc ":0$") -eq 0 &amp;&amp; /usr/bin/grep -q "account_acts" scripts/rls-baseline.mjs &amp;&amp; /usr/bin/grep -q "PRODUCTION_REF" scripts/seed-lab-door.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Zero `membership_code` nei tre script
    - Il probe della linea di base nomina `account_acts`
    - `seed-lab-door.mjs` porta ancora il rifiuto del ref di produzione come prima riga eseguita
    - `rls-baseline-compare.mjs` non compare nel diff
  </acceptance_criteria>
  <done>I banchi seminano cio' che lo schema avra', e la linea di base interroga la tabella con il nome nuovo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codice applicativo → funzione `SECURITY DEFINER` del registro | la creazione di un account passa da li' |
| registro → lettura storica | un'unione che non sa nominare una riga esistente rende illeggibile la storia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-46 | Denial of Service | `createAccount` durante la finestra codice→schema | accept, dichiarata | il verso e' codice prima e schema dopo; in produzione i due passi stanno dentro un solo atto autorizzato, uno dopo l'altro (piano 51-13), e la finestra dura minuti su un progetto senza traffico |
| T-51-47 | Repudiation | valori storici dell'unione | mitigate | i dieci valori restano; nessuna riga del registro si riscrive |
| T-51-48 | Tampering | specchio unione ↔ `CHECK` | mitigate | il `CHECK` cambia **tabella** e non valori, e il docblock lo dichiara |
| T-51-49 | Information disclosure | etichetta del soggetto ricostruita lato applicazione | mitigate | l'etichetta la calcola la funzione SQL; la pagina mostra cio' che la riga porta |
| T-51-50 | Tampering | script di laboratorio senza rifiuto del ref di produzione | mitigate | il rifiuto resta la prima riga eseguita, ed e' asserito |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build` verde dopo ogni task
- Zero `membership_acts` / `record_membership_act` / `membership_code` sotto `src/` e negli script toccati
- La finestra codice→schema e' dichiarata nel SUMMARY con il piano che la chiude
</verification>

<success_criteria>
D-51-08 e' vero lato codice, e nulla ricostruisce un'etichetta che la funzione
SQL calcolera' da se'.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-11-SUMMARY.md` when done
</output>
