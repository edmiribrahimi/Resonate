# Fase 51 — punto di ripresa, 2026-09-22 ~17:30 Torino

**Fermata dal proprietario** (doveva staccare la rete), non da un errore.
Albero pulito su `main` a `be51951`. Nessun push: `origin/main` e' 73 commit
indietro, `origin/lab` serve ancora `03e443e` (il codice della corsa «prima»).

## Dove siamo

| Onda | Piani | Stato |
|---|---|---|
| 1 | 51-01 … 51-05 | chiusi e uniti |
| 2 | 51-06, 51-07, 51-08 | chiusi e uniti — migration 1 (`attendee`) applicata al **solo lab** |
| 3 | 51-09, 51-10, 51-15 | chiusi e uniti (51-15 = piano di chiusura scritto in corsa: check-in per nome offline) |
| 4 | 51-11 | chiuso, su `main` |
| 5 | **51-12** | **da ricominciare da zero**: l'esecutore e' stato fermato prima di scrivere il file di migration; nessuna scrittura sul lab (`profiles.membership_code` c'e' ancora, `membership_acts` e `attendances` esistono) |
| 6 | 51-13 | produzione, checkpoint del proprietario |
| 7 | 51-14 | corsa «dopo» + VERIFICATION |

## Ordine deciso per la ripresa (diverso dalle onde 6/7 del piano)

1. `/gsd-execute-phase 51` → riparte da 51-12 (migration 2 sul lab). Al suo
   esecutore vanno ripetuti i fatti: nomi fissati da 51-11 (`record_account_act`,
   `account_acts`, `account_acts_act_check`, `account_acts_actor_attributed`,
   `account_acts_select_register_read`); `reconcile_master` si ridefinisce
   partendo dal corpo di `20260922120000_role_attendee_and_capability_keys.sql`
   §7b; 7 vincoli + 3 indici di `membership_acts` che `RENAME` non muove;
   `attendances` vuota ovunque, cascata vuota; **non usare `gsd-sdk query state.*`**
   (corrompe STATE.md — 51-11 l'ha misurato), tracking a mano.
2. **Deploy del lab PRIMA della produzione**: `git push origin main:lab`
   (fast-forward, `lab` e' antenato di `main`). Poi il proprietario percorre la
   corsa «dopo» di `P-51-1` sul lab, nove passi, stesso telefono.
3. Solo dopo: 51-13 (produzione, autorizzazione datata), poi 51-14.

## Precondizioni vive, da non perdere

- **Il telefono del proprietario porta UNA voce `membership` in coda** (14:32Z,
  account master del lab). **Non aprire la porta su quel telefono** finche' il
  lab non serve il codice della fase: la coda si drena solo a pagina aperta.
- Invitato di guest list senza email seminato sul lab, free night:
  `guest_list_entries.id = 63ebd88f-d2a7-4c65-81bf-afca5d52c350`, ancora
  `invited`. Serve al passo 5 della corsa «dopo»; a corsa finita si cancella
  **per quella chiave**.
- Biglietti validi non scansionati sulla free night: i due dell'account
  `p50-free-a` («1 di 2», «2 di 2»), per il passo 3.
- Credenziali del lab: in `.env.lab.local` (`LAB_ACCOUNT_PASSWORD`), account
  `door@`, `master@`, `member@`, `p50-free-a@` `lab.invalid`.

## Debiti aperti, tutti registrati

- `STATE.md`: il corpo ha un frammento orfano dopo «Plan: 1 of 14» e conta i
  piani a mano; da riallineare a fine fase, non prima.
- Todo `door-and-login-viewport-crops-under-keyboard.md` (fase 52).
- `deferred-items.md`: D-51-08-A/B (rls-baseline etichetta, verify:refusal sul lab);
  `SetPasswordForm.tsx:343` e `organizer-redirects.ts:41` di nessuno;
  `verify:touch-targets` rosso preesistente (3 elementi); G2 `verify:venue-surfaces`.
- Il prefisso del ref del lab e' nei criteri di 51-01-PLAN (pubblicato): decisione
  del proprietario se ruotare il progetto lab; accettato per ora.

## Critical Anti-Patterns

| Pattern | Severity | Nota |
|---|---|---|
| Aprire la porta sul telefono prima del deploy del lab | blocking | drena la coda: la precondizione del passo 9 non e' piu' fabbricabile |
| Produzione prima della corsa «dopo» | blocking | l'atto e' irreversibile; la verifica alla porta viene prima |
| `gsd-sdk query state.*` | high | corrompe STATE.md; tracking a mano |
