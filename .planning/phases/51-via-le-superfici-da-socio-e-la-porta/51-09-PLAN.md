---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 09
type: execute
wave: 3
depends_on: ["51-08"]
files_modified:
  - src/components/admin/MemberTable.tsx
  - src/app/(admin)/admin/members/CreateAccountForm.tsx
  - src/app/(admin)/admin/members/actions.ts
  - src/app/(admin)/admin/(work)/members/page.tsx
  - src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx
  - src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx
  - src/emails/account-invitation.tsx
autonomous: true
requirements: [MEM-01]

must_haves:
  truths:
    - "Le superfici di lavoro creano, elencano e filtrano account con il ruolo attendee, mai member (D-51-06)"
    - "Nessuna superficie di lavoro legge o mostra profiles.membership_code (D-51-02)"
    - "Chi sceglie a chi assegnare una serata ha ancora un disambiguatore fra due omonimi, e non e' una credenziale"
  artifacts:
    - path: "src/components/admin/MemberTable.tsx"
      provides: "tabella senza la colonna del codice socio, con il valore di ruolo nuovo"
    - path: "src/app/(admin)/admin/members/actions.ts"
      provides: "creazione account su ruolo attendee, senza leggere il codice socio"
  key_links:
    - from: "src/app/(admin)/admin/members/CreateAccountForm.tsx"
      to: "profiles_role_check"
      via: "il valore scritto deve essere fra i quattro che il CHECK ammette"
      pattern: "attendee"
    - from: "src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx"
      to: "profiles.id"
      via: "le prime 8 cifre dell'identificativo al posto del codice socio"
      pattern: "slice"
---

<objective>
Le superfici di lavoro smettono di dire «member» e smettono di leggere il codice
socio.

Purpose: D-51-06 e D-51-02. Il valore di ruolo lo decide il `CHECK`, e dal piano
51-08 il `CHECK` dice `attendee`: un form che scrive `member` da qui in poi
riceve un 23514. E la colonna del codice socio cade nel piano 51-12 — il codice
che la legge deve uscire **prima**, o il `DROP COLUMN` lascia dietro di se'
superfici che interrogano una colonna inesistente.
Output: sei superfici di lavoro allineate, e un disambiguatore che non e' una
credenziale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@./CLAUDE.md
@.claude/rules/access-gating.md
@.claude/rules/comms-analytics.md

<interfaces>
Dopo il piano 51-08 il database accetta questi quattro valori, e nessun altro:

  profiles_role_check           CHECK (role IN ('master','organizer','staff','attendee'))
  role_capabilities_role_check  stesso insieme
  public.profiles.role          DEFAULT 'attendee'

I siti da muovere, letti il 2026-09-21 e **da rileggere prima di citarli**:
  src/components/admin/MemberTable.tsx                     :72 (tipo), :405, :453, :481, :501, :625 (colonna codice), :742 (option)
  src/app/(admin)/admin/members/CreateAccountForm.tsx      :165 (valore e label), :182, :199
  src/app/(admin)/admin/members/actions.ts                 :610 (commento), :927 e :946, :965 (WritableRole), :1670 e :1680
  src/app/(admin)/admin/(work)/members/page.tsx            :105 (select), :142
  src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx     :63 (tipo), :171 (select)
  src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx :55 (tipo), :283, :361 (il codice accanto al nome)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: La tabella dei membri e il form di creazione</name>
  <files>src/components/admin/MemberTable.tsx, src/app/(admin)/admin/members/CreateAccountForm.tsx</files>
  <read_first>
    - `src/components/admin/MemberTable.tsx:65-80` (il tipo di riga, con `membership_code`), `:400-510` (i filtri e i conteggi per ruolo), `:615-635` (la colonna che mostra il codice, con il ripiego `"--"`), `:735-750` (l'`option` del menu)
    - `src/app/(admin)/admin/members/CreateAccountForm.tsx:155-205` — le opzioni di ruolo, il valore e l'etichetta
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:394-417` — i nove file e le cinque righe di `MemberTable`, e la distinzione fra **valore** (dato: `attendee`) ed **etichetta** (discrezione del piano)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md` — D-51-06 (perche' `attendee` e non `guest` ne' `buyer`) e D-51-02
  </read_first>
  <action>
    In `MemberTable.tsx`: il **valore** di ruolo diventa `attendee` ovunque —
    tipo, filtri, conteggi, `option`. L'**etichetta** che un organizer legge e'
    discrezione: scegliere una sola parola e usarla in tutte e due le superfici,
    senza inventare un sinonimo per ciascuna. Cancellare la colonna che mostra il
    codice socio e il suo ripiego: la colonna non avra' piu' una fonte dopo il
    piano 51-12, e una colonna che mostra sempre `--` e' rumore.
    In `CreateAccountForm.tsx`: l'opzione scrive `attendee`. Attenzione a non
    lasciare il valore vecchio in un ramo di default: dal piano 51-08 in poi il
    `CHECK` rifiuta `member` con `23514`, e un form che lo scrive fallisce a
    creazione avviata — la causa va restituita come **valore** distinto, mai come
    messaggio generico, perche' Next redige i messaggi delle Server Action in
    produzione.
    Nota, da scrivere accanto e non da scoprire: il valore e' dato dal `CHECK`,
    l'etichetta no. Chi cambiera' l'etichetta domani non deve credere di poter
    cambiare anche il valore.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c '"member"' src/components/admin/MemberTable.tsx "src/app/(admin)/admin/members/CreateAccountForm.tsx" | /usr/bin/grep -vc ":0$") -eq 0 &amp;&amp; test $(/usr/bin/grep -c "membership_code" src/components/admin/MemberTable.tsx) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero `"member"` come **valore** nei due file; l'etichetta visibile e' una sola parola, la stessa nei due
    - Zero `membership_code` in `MemberTable.tsx`
    - Il rifiuto di una creazione con ruolo non ammesso torna come valore distinto, non come messaggio generico
  </acceptance_criteria>
  <done>Chi crea e chi elenca un account lavora sul valore che il database accetta.</done>
</task>

<task type="auto">
  <name>Task 2: Le azioni dei membri, la pagina di elenco, e la mail d'invito</name>
  <files>src/app/(admin)/admin/members/actions.ts, src/app/(admin)/admin/(work)/members/page.tsx, src/emails/account-invitation.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/members/actions.ts:600-620` (il commento che nomina `role: 'member'`), `:920-950` (la `select` con `membership_code` e il `membershipCode` passato piu' sotto), `:960-975` (`WritableRole`), `:1660-1690` (la seconda lettura del codice), `:780-800` (la chiamata `rpc` al registro degli atti — **non si tocca qui**: la rinomina e' del piano 51-11)
    - `src/app/(admin)/admin/(work)/members/page.tsx:100-150` — la `select` e la proiezione verso la tabella
    - `src/emails/account-invitation.tsx` per intero, e `.claude/CHANGELOG.md` voce 1.23.0, che assegna **a questa fase** la sua traduzione e dichiara perche': e' l'ultimo template nella lingua vecchia e promette un invito personale che la fase 50 ha tolto e un ingresso per nome che questa fase riscrive
    - `.claude/rules/comms-analytics.md` — il gate «una lingua sola per percorso»
  </read_first>
  <action>
    In `actions.ts`: il valore di ruolo scritto e tipizzato diventa `attendee`
    (`WritableRole` compreso), e le due letture di `membership_code` escono
    insieme a cio' che le consumava. Dove il codice serviva come **etichetta del
    soggetto** verso il registro degli atti, non inventare qui un sostituto: la
    funzione SQL lo calcolera' da se' dal piano 51-12 (D-51-15, prime 8 cifre
    dell'identificativo), e duplicarlo lato applicazione creerebbe due verita'
    che divergono.
    In `(work)/members/page.tsx`: togliere `membership_code` dalla `select` e
    dalla proiezione verso la tabella.
    In `account-invitation.tsx`: riscriverlo in **inglese**, come i dodici
    template fratelli, e togliere cio' che non e' piu' vero — l'invito personale
    che la fase 50 ha tolto e l'ingresso per nome che questa fase riscrive.
    L'oggetto vive in `actions.ts` e si muove con il template, nello stesso
    commit. Aggiornare il docblock del template, che oggi **dichiara** di essere
    l'eccezione in italiano: se resta, diventa documentazione datata.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c "membership_code" "src/app/(admin)/admin/members/actions.ts" "src/app/(admin)/admin/(work)/members/page.tsx" | /usr/bin/grep -vc ":0$") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero `membership_code` nei due file di superficie
    - `WritableRole` e ogni scrittura di ruolo usano `attendee`
    - `account-invitation.tsx` e' in inglese, non promette un invito personale ne' un ingresso per nome, e il suo docblock non si dichiara piu' l'eccezione italiana
    - La chiamata `rpc` al registro degli atti e' **invariata** (la rinomina e' del piano 51-11)
  </acceptance_criteria>
  <done>Le azioni sui membri scrivono il ruolo giusto e non leggono piu' una credenziale che sta per sparire.</done>
</task>

<task type="auto">
  <name>Task 3: Le assegnazioni, e il disambiguatore che sostituisce il codice</name>
  <files>src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx, src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx:55-75` (il tipo con `membership_code`) e `:165-180` (la `select`)
    - `src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx:50-60` (il tipo), `:275-290` e `:355-370` — il codice socio mostrato **accanto al nome**, che oggi e' l'unico modo per distinguere due omonimi quando si sceglie chi lavora una serata
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:450-454` — la ricaduta dichiarata: quelle superfici perdono l'appiglio, e il sostituto naturale (le prime cifre dell'identificativo) non e' PII e non e' una credenziale
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md` — D-51-15, che sceglie **le prime 8 cifre di `subject_id`** per il registro: qui si usa la stessa forma, o il prodotto avrebbe due disambiguatori diversi per la stessa persona
  </read_first>
  <action>
    Togliere `membership_code` dal tipo e dalla `select` della pagina, e dal tipo
    del client. Al suo posto, accanto al nome, mostrare **le prime 8 cifre
    dell'identificativo del profilo** — la stessa forma che D-51-15 sceglie per
    l'etichetta del registro, cosi' che la stessa persona si disambigui allo
    stesso modo nei due posti. Scrivere accanto il limite dichiarato: non e' una
    credenziale, non e' un dato personale, e **non sopravvive alla cancellazione
    del soggetto** — a differenza del codice che c'era prima.
    Non aggiungere una seconda lettura al database per ottenerlo: l'identificativo
    e' gia' nella riga che la pagina seleziona.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c "membership_code" "src/app/(admin)/admin/(work)/events/[id]/assignments/page.tsx" "src/app/(admin)/admin/events/[id]/assignments/AssignmentsClient.tsx" | /usr/bin/grep -vc ":0$") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero `membership_code` nei due file
    - Il disambiguatore mostrato e' la stessa forma scelta da D-51-15 (prime 8 cifre dell'identificativo), con il limite scritto accanto
    - Nessuna interrogazione nuova al database
  </acceptance_criteria>
  <done>Chi assegna una serata distingue ancora due omonimi, senza mostrare una credenziale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superficie di lavoro → `public.profiles` | qui si creano account e si assegnano persone a una serata |
| schermo di un organizer → screenshot | cio' che si mostra accanto a un nome puo' finire in un'immagine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-38 | Denial of Service | creazione account con ruolo rifiutato dal `CHECK` | mitigate | il valore scritto e' `attendee` ovunque; il rifiuto torna come valore distinto, mai come messaggio generico che Next rediga |
| T-51-39 | Information disclosure | codice socio mostrato accanto a un nome | mitigate | la colonna e il disambiguatore escono; al loro posto 8 cifre di un identificativo, che non sono una credenziale |
| T-51-40 | Denial of Service | superficie che interroga una colonna droppata | mitigate | il codice smette di leggerla **prima** che il piano 51-12 la cancelli: e' il verso del deploy dichiarato |
| T-51-41 | Tampering | due disambiguatori diversi per la stessa persona | mitigate | si usa la stessa forma di D-51-15, e la ragione e' scritta accanto |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build` verde dopo ogni task
- `/usr/bin/grep -rn "membership_code" src/app/\(admin\)` → zero righe (salvo la pagina del registro, che e' del piano 51-11)
- `npm run verify` verde salvo i rossi dichiarati contro la produzione fino al 51-13
</verification>

<success_criteria>
Le superfici di lavoro parlano il linguaggio del database nuovo e non leggono
piu' una credenziale che sta per essere cancellata.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-09-SUMMARY.md` when done
</output>
