---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 06
type: execute
wave: 2
depends_on: ["51-04"]
files_modified:
  - src/app/(members)/account/page.tsx
  - src/app/(members)/account/loading.tsx
  - src/app/(members)/dashboard/page.tsx
  - src/app/(members)/dashboard/loading.tsx
  - src/app/(members)/dashboard/DashboardDrinkTokens.tsx
  - next.config.ts
  - src/lib/routes/next-redirect.ts
  - src/lib/supabase/middleware.ts
  - src/lib/rbac/roles.ts
  - src/components/layout/AppNav.tsx
  - scripts/conversion-manifest.mjs
  - scripts/probe-forged-identity.sh
autonomous: true
requirements: [MEM-01]

must_haves:
  truths:
    - "La pagina dell'account vive a /account, e /dashboard risponde 308 verso di essa (D-51-09b)"
    - "La pagina mostra solo i biglietti — le serate in arrivo con il QR e i passati in una sezione chiusa «Past» — piu' cambio password ed email (D-51-09)"
    - "Niente drink, niente card, niente storico, niente caricamento media su quella pagina (D-51-09)"
    - "Nessuno dei circa quaranta rifiuti delle superfici di lavoro che scrivono redirect verso /dashboard e' stato toccato"
  artifacts:
    - path: "src/app/(members)/account/page.tsx"
      provides: "la pagina dell'account: biglietti in arrivo, «Past» chiusa, impostazioni"
      contains: "My Tickets"
    - path: "next.config.ts"
      provides: "il 308 da /dashboard a /account"
      contains: "/account"
  key_links:
    - from: "next.config.ts"
      to: "src/app/(members)/account/page.tsx"
      via: "redirects(), che gira PRIMA del middleware e tiene /dashboard un Route valido sotto typedRoutes"
      pattern: "destination"
    - from: "src/lib/routes/next-redirect.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "PROTECTED_PREFIXES e NEXT_ALLOW_LIST, che devono ammettere /account insieme"
      pattern: "account"
---

<objective>
Far nascere `/account` da `/dashboard`: stessa pagina, ridotta a cio' che D-51-09
lascia, all'indirizzo che la barra usa gia', con `/dashboard` che resta vivo come
redirect permanente.

Purpose: D-51-09 e D-51-09b. Il valore di questa pagina per chi ha comprato e' il
biglietto con il suo QR; tutto il resto era del socio.
Output: `/account` servita, `/dashboard` 308, e nessuno dei quaranta rifiuti
delle superfici di lavoro toccato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@./CLAUDE.md
@.claude/rules/nextjs-architecture.md
@.claude/rules/access-gating.md

<interfaces>
Misurato sui tipi generati, non dedotto dalla documentazione
(`.next/types/routes.d.ts:8-9`, `.next/types/link.d.ts:73,79`):

  type RedirectRoutes = "/eventi/[[...path]]" | "/galleria" | "/presenze"
  type Routes = AppRoutes | PageRoutes | LayoutRoutes | RedirectRoutes | ...

I source dei redirect dichiarati in `next.config.ts` entrano nell'union dei
percorsi validi di `typedRoutes`. Conseguenza operativa: aggiungendo la voce con
source `/dashboard` e destination `/account`, i circa 40 `redirect("/dashboard")`
sotto `src/app/(admin)/admin/(work)/` continuano a compilare e non si toccano.

`src/lib/routes/next-redirect.ts`
  export const DEFAULT_NEXT = "/dashboard";      // :38
  const NEXT_ALLOW_LIST: readonly RegExp[]       // :91-126
  export const PROTECTED_PREFIXES = [...]        // :145-151
`src/lib/supabase/middleware.ts`
  const bounceToDashboard = (cause) -> { url.pathname = "/dashboard"; ... }   // :495-497
`src/lib/rbac/roles.ts`
  la voce Account della barra, href "/dashboard"                                 // :332
`src/components/layout/AppNav.tsx`
  lo stato attivo della voce                                                     // :253-254
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spostare la pagina a /account e ridurla a cio' che D-51-09 lascia</name>
  <files>src/app/(members)/account/page.tsx, src/app/(members)/account/loading.tsx, src/app/(members)/dashboard/page.tsx, src/app/(members)/dashboard/loading.tsx, src/app/(members)/dashboard/DashboardDrinkTokens.tsx</files>
  <read_first>
    - `src/app/(members)/dashboard/page.tsx` per intero, e in particolare: `:149-152` e `:448` (il notice per chi arriva senza password), `:186-189` (la select con `membership_code, role, created_at`), `:208` e `:211` e `:225` (`isMemberRole` e i suoi rami), `:227-249` (la query su `drink_tokens`), `:282-290` (le due liste `upcoming` e `past`, gia' separate), `:349-357` (header e `PostHogIdentify`), `:525-640` (il blocco dei biglietti), `:641-643` (il blocco dei drink), `:663-678` (le impostazioni), `:684-686` (`ManagementSection`), `:690-700` (`AppNav`)
    - `src/app/(members)/dashboard/loading.tsx` — il segnaposto, che si sposta con la pagina
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:166-186` — la tabella blocco per blocco: cosa resta, cosa esce
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md:652-676` — l'analogo dello spostamento (commit `24b511e`, `ab0c0c4`): ogni link entrante si corregge nello stesso commit
  </read_first>
  <action>
    Spostare `src/app/(members)/dashboard/page.tsx` e `loading.tsx` in
    `src/app/(members)/account/`, con `git mv` cosi' che la storia resti
    leggibile, e cancellare `DashboardDrinkTokens.tsx` insieme alla query su
    `drink_tokens` che lo alimentava: una lettura che non alimenta piu' nulla e'
    peso sul percorso, non una cosa innocua.
    Dentro la pagina spostata, il blocco dei biglietti resta ed e' il cuore; le
    due liste `upcoming` e `past` esistono gia' separate, quindi «Past» diventa
    una sezione chiusa — usando il modo in cui la casa gia' chiude una sezione,
    senza introdurre un componente nuovo se ne esiste uno. Restano: il notice per
    chi arriva senza password, l'header con `PostHogIdentify`, le impostazioni
    (`ChangeEmailButton`, `ResetPasswordButton`, `LogoutButton`) e
    `ManagementSection` — quest'ultima resta in 51: la toglie NAV-03, fase 52, e
    anticiparla qui allargherebbe la fase.
    Non toccare in questo piano ne' `membership_code` nella select (`:187`) ne' i
    due letterali `"member"` (`:208`, `:350`): sono del piano 51-10, che li muove
    insieme al tipo e alla migration del ruolo. Qui si sposta e si riduce.
    Se `src/app/(members)/dashboard/` resta vuota, cancellarla.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f "src/app/(members)/account/page.tsx" &amp;&amp; test ! -d "src/app/(members)/dashboard" &amp;&amp; test $(/usr/bin/grep -c "drink_tokens" "src/app/(members)/account/page.tsx") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `src/app/(members)/account/page.tsx` e `loading.tsx` esistono; `src/app/(members)/dashboard/` non esiste
    - Zero `drink_tokens` e zero `DashboardDrinkTokens` nella pagina
    - «Past» e' una sezione chiusa per default; i biglietti in arrivo restano aperti con il QR
    - `ManagementSection` e' ancora renderizzata (la toglie la fase 52)
  </acceptance_criteria>
  <done>La pagina dell'account mostra i biglietti e le impostazioni, e nient'altro.</done>
</task>

<task type="auto">
  <name>Task 2: /dashboard diventa un 308 verso /account, e le liste d'accesso ammettono il nuovo indirizzo</name>
  <files>next.config.ts, src/lib/routes/next-redirect.ts, src/lib/supabase/middleware.ts</files>
  <read_first>
    - `next.config.ts:63-92` — il docblock della fase 50 sul 308 che il browser memorizza, e l'array dei redirect
    - `src/lib/routes/next-redirect.ts:31-38` (`DEFAULT_NEXT`), `:66-90` (il criterio: aggiungere una voce e' una decisione d'accesso, e i pattern sono ancorati a entrambi i capi senza `.*`), `:91-126`, `:145-151`
    - `src/lib/supabase/middleware.ts:490-500` (il rimbalzo per capability) e `:578-600` (il rimbalzo anonimo, che importa `PROTECTED_PREFIXES` invece di ricopiarlo)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:139-165` — la strada misurata, e la nota d'ordine: i redirect di `next.config` girano prima del middleware, quindi un anonimo su `/dashboard` riceve 308 verso `/account` e poi il rimbalzo a `/login` con `next=/account`
  </read_first>
  <action>
    In `next.config.ts` aggiungere ai `redirects()` la voce con source
    `/dashboard`, destination `/account`, `permanent: true`, e scriverle accanto
    le due cose che vanno dichiarate e non scoperte: (1) e' questa voce a tenere
    `/dashboard` dentro l'union di `typedRoutes`, ed e' la ragione per cui i
    circa 40 rifiuti delle superfici di lavoro non si toccano; (2) un 308 lo
    memorizza il browser — chi lo segue una volta finira' su `/account` dalla
    propria cache anche se un giorno si volesse tornare indietro. Disposizione
    `accept`, come `T-50-28`.
    In `next-redirect.ts`: aggiungere il pattern ancorato per `/account` a
    `NEXT_ALLOW_LIST` — ancorato a entrambi i capi, nessun `.*`, come la nota del
    file impone — e `"/account"` a `PROTECTED_PREFIXES`. Portare `DEFAULT_NEXT` a
    `/account`. Lasciare dov'e' la voce di `/dashboard` in entrambe le liste:
    l'indirizzo esiste ancora come sorgente di redirect, e togliere una voce e'
    una decisione d'accesso quanto aggiungerla. Scrivere accanto perche' resta.
    In `middleware.ts`: il rimbalzo per capability punta a `/account`, e il nome
    della funzione segue l'indirizzo. L'import di `PROTECTED_PREFIXES` resta un
    import: le due liste non possono divergere senza che `verify:routes` diventi
    rosso, ed e' la proprieta' per la cui mancanza quel difetto era esistito.
    Non toccare i letterali `"member"` di `:399`, `:405`, `:417`: sono del piano
    51-10.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:routes &amp;&amp; grep -q "/account" next.config.ts &amp;&amp; grep -q "/account" src/lib/routes/next-redirect.ts &amp;&amp; test $(/usr/bin/grep -c 'pathname = "/dashboard"' src/lib/supabase/middleware.ts) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0 e `npm run verify:routes` e' verde, controllo [3/3] compreso
    - `next.config.ts` porta il redirect permanente con il paragrafo di disposizione `accept`
    - `/account` e' in `NEXT_ALLOW_LIST` con un pattern ancorato a entrambi i capi, e in `PROTECTED_PREFIXES`
    - `DEFAULT_NEXT` e' `/account`; le voci di `/dashboard` restano, con la ragione scritta accanto
    - Zero `redirect("/dashboard")` toccati sotto `src/app/(admin)` (`git diff --stat` non nomina quei file)
  </acceptance_criteria>
  <done>`/dashboard` porta a `/account` prima ancora del middleware, e il nuovo indirizzo e' ammesso da entrambe le liste.</done>
</task>

<task type="auto">
  <name>Task 3: La barra, il censimento e la sonda seguono l'indirizzo</name>
  <files>src/lib/rbac/roles.ts, src/components/layout/AppNav.tsx, scripts/conversion-manifest.mjs, scripts/probe-forged-identity.sh</files>
  <read_first>
    - `src/lib/rbac/roles.ts:320-339` — la voce Account della barra, con `href: "/dashboard"` a `:332`
    - `src/components/layout/AppNav.tsx:248-258` — lo stato attivo, che confronta `item.href` e `pathname` con `/dashboard`
    - `scripts/conversion-manifest.mjs:1070-1080` — la entry `/dashboard`, con il percorso del file di pagina dichiarato
    - `scripts/conversion-manifest.mjs` docblock del censimento — la forma: il numero si rilegge dal gate, la prosa porta la data
    - `scripts/probe-forged-identity.sh:15-25` — gli indirizzi provati e i codici attesi
  </read_first>
  <action>
    Portare `href` della voce Account a `/account` in `roles.ts`, e allineare lo
    stato attivo in `AppNav.tsx`: il confronto deve riconoscere `/account`. Se il
    prodotto deve continuare a evidenziare la voce anche per chi arriva da un
    vecchio link, ricordare che il 308 cambia gia' il `pathname` prima che la
    pagina renda — quindi non serve un secondo confronto su `/dashboard`, e
    aggiungerlo sarebbe una seconda verita' che diverge.
    Rinominare nel manifest la entry `/dashboard` in `/account`, **con il nuovo
    percorso del file di pagina**: il controllo D legge il file dichiarato, e una
    entry che punta a un file inesistente fa un gate che asserisce la cosa giusta
    sul file sbagliato. Aggiornare il paragrafo di aritmetica con la data.
    In `scripts/probe-forged-identity.sh`, aggiungere `/account` all'elenco degli
    indirizzi provati e dichiarare accanto che `/dashboard` ora risponde **308**
    prima di qualunque giudizio d'identita': se lo script confronta codici
    attesi, quello di `/dashboard` va aggiornato, o la sonda misurera' un
    reindirizzamento di rotta credendo di misurare un rifiuto d'accesso — che e'
    esattamente l'errore che la v1.5 ha gia' commesso con l'apice del dominio.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify:conversion &amp;&amp; npm run verify:routes &amp;&amp; grep -q "/account" src/lib/rbac/roles.ts &amp;&amp; grep -q "/account" scripts/conversion-manifest.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:conversion` verde con la entry `/account` che punta al file esistente
    - La voce Account della barra porta a `/account` e si evidenzia li'
    - `scripts/probe-forged-identity.sh` prova `/account` e dichiara il 308 di `/dashboard`
    - Il paragrafo di aritmetica del manifest porta la data della rimisura
  </acceptance_criteria>
  <done>Ogni posto che nominava l'indirizzo vecchio nomina quello nuovo, e i gate lo confermano.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonimo → `/account` | una pagina con sessione: se il redirect la raggiungesse senza rimbalzo, mostrerebbe biglietti a chi non li ha comprati |
| browser → 308 in cache | un redirect permanente non si ritira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-22 | Spoofing | `NEXT_ALLOW_LIST` | mitigate | il pattern di `/account` e' ancorato a entrambi i capi e senza `.*`: un pattern largo su un flusso autenticato riaprirebbe un open redirect |
| T-51-23 | Information disclosure | `/account` raggiunta senza sessione | mitigate | `/account` entra in `PROTECTED_PREFIXES` **insieme** all'allow-list; il middleware importa la lista, non la ricopia; `verify:routes` [3/3] e' il gate |
| T-51-24 | Denial of Service | 308 su `/dashboard` non ritirabile dalle cache | accept | dichiarato nel docblock di `next.config.ts`, come `T-50-28` |
| T-51-25 | Tampering | entry di manifest che punta al file sbagliato | mitigate | la entry si rinomina nello stesso commit dello spostamento, con il nuovo percorso |
| T-51-26 | Repudiation | sonda che misura un 308 credendo di misurare un rifiuto | mitigate | il codice atteso di `/dashboard` si aggiorna e la ragione si scrive accanto |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build`, `npm run verify:routes`, `npm run verify:conversion` verdi
- Dopo il deploy del ramo sul laboratorio: `curl -s -o /dev/null -w '%{http_code}' https://lab.resonatemotion.com/dashboard` → **308**, e `/account` da anonimo → **307** verso `/login`
</verification>

<success_criteria>
Chi ha comprato apre `/account` e vede i propri biglietti; i vecchi link non
muoiono; nessun rifiuto delle superfici di lavoro e' stato toccato.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-06-SUMMARY.md` when done
</output>
