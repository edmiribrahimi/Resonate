---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sw.ts
  - src/app/api/membership/verify/route.ts
  - src/app/api/membership/list/route.ts
  - CLAUDE.md
  - .claude/rules/checkin-offline.md
  - .claude/rules/access-gating.md
  - .claude/rules/meta-gates.md
  - .claude/CHANGELOG.md
autonomous: true
requirements: [MEM-03]

must_haves:
  truths:
    - "/api/membership/verify e /api/membership/list non esistono piu' (MEM-03)"
    - "Il service worker non porta piu' alcuna regola per quelle due rotte, e le due che restano vincono ancora sulla regola ereditata /api/* (MEM-03)"
    - "La persona non dichiara piu' un glob che non matcha alcun file, e la tabella di routing di meta-gates.md descrive un routing reale"
  artifacts:
    - path: "src/app/sw.ts"
      provides: "doorRuntimeCaching con due regole, e il docblock riscritto"
      contains: "api/tickets/attendance"
    - path: ".claude/CHANGELOG.md"
      provides: "la voce semver con lo scenario di carico-e-scatto e il context budget rimisurato"
      contains: "51"
  key_links:
    - from: "src/app/sw.ts"
      to: "public/sw.js"
      via: "il build, che e' dove la regola si verifica — non il diff"
      pattern: "api/tickets/attendance"
    - from: ".claude/rules/checkin-offline.md"
      to: "CLAUDE.md riga d'indice"
      via: "controllo B di verify:persona — stesso insieme di glob nei due posti"
      pattern: "src/lib/offline"
---

<objective>
Cancellare le due rotte d'API del socio, togliere le loro due regole dal service
worker, e chiudere nello stesso commit il glob della persona che senza quella
directory diventa un path morto.

Purpose: MEM-03. E la regola della casa: una rotta che esce lascia insieme alla
pagina il manifest, le mappe e i `paths:` della persona — altrimenti
`verify:persona` controllo A diventa rosso e resta rosso.
Output: due rotte in meno, due regole `NetworkOnly` in meno, e una persona che
descrive il prodotto che esiste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@./CLAUDE.md
@.claude/rules/ai-engineering.md
@.claude/rules/checkin-offline.md
@.claude/rules/meta-gates.md

<interfaces>
`src/app/sw.ts`
  const doorRuntimeCaching: RuntimeCaching[] = [ …4 elementi… ];   // :32-48
  // :34 /api/tickets/attendance   RESTA
  // :38 /api/tickets/checkin      RESTA
  // :42 /api/membership/list      ESCE
  // :46 /api/membership/verify    ESCE
  precacheEntries: self.__SW_MANIFEST                              // :114 — generato dal build, nessuna voce scritta a mano
La regola ereditata di `defaultCache` e' un `NetworkFirst` su ogni `GET /api/*`
same-origin, `cacheName: "apis"`, `maxAgeSeconds: 86400`: Serwist prende la
**prima** regola che matcha, quindi l'ordine e' portante.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Le due regole escono dal service worker, e il docblock smette di descriverle</name>
  <files>src/app/sw.ts</files>
  <read_first>
    - `src/app/sw.ts:14-30` — il docblock, incluso il paragrafo che motiva `/api/membership/list` («every full name and membership code») e il paragrafo «Order matters»
    - `src/app/sw.ts:32-48` — l'array `doorRuntimeCaching`, quattro elementi
    - `src/app/sw.ts:110-120` — `precacheEntries: self.__SW_MANIFEST`
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:219-234` — perche' l'ordine e' portante e perche' la verifica va fatta sull'output
  </read_first>
  <action>
    Togliere dall'array `doorRuntimeCaching` il terzo e il quarto elemento — i
    matcher su `/api/membership/list` e `/api/membership/verify`. I primi due
    restano **nello stesso ordine**: Serwist prende la prima regola che matcha, e
    se `/api/tickets/attendance` perdesse la sua regola esplicita la lista della
    porta comincerebbe a risolversi dalla copia `NetworkFirst` ereditata, vecchia
    fino a un giorno, mentre il badge dice «Online».
    Riscrivere il docblock `:14-30`: il paragrafo che motiva la regola sul roster
    descrive una rotta che non esistera' piu' — si sostituisce dicendo che il
    roster dei soci e' uscito con la fase 51 e che cio' che resta a viaggiare
    verso il telefono e' la lista della serata, che porta nomi ma **non** un
    codice socio. Il paragrafo «Order matters» **resta e diventa piu'
    importante**, non meno.
    Non toccare `precacheEntries`: il manifest e' generato dal build sugli asset,
    non c'e' alcuna voce scritta a mano da editare.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; /usr/bin/grep -c "api/membership" src/app/sw.ts | grep -qx 0 &amp;&amp; /usr/bin/grep -c "api/membership" public/sw.js | grep -qx 0 &amp;&amp; /usr/bin/grep -c "api/tickets/attendance" public/sw.js | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `public/sw.js` — l'**output**, non il diff — contiene ancora i matcher su `/api/tickets/attendance` e `/api/tickets/checkin`, e zero occorrenze di `api/membership`
    - Il docblock non nomina piu' una rotta inesistente, e conserva il paragrafo sull'ordine
  </acceptance_criteria>
  <done>La porta cachea due rotte, quelle giuste, e nell'ordine che le fa vincere.</done>
</task>

<task type="auto">
  <name>Task 2: Cancellare le due rotte d'API</name>
  <files>src/app/api/membership/verify/route.ts, src/app/api/membership/list/route.ts</files>
  <read_first>
    - `src/app/api/membership/verify/route.ts:520-570` — l'unico scrittore di `public.attendances` in tutto il prodotto (`INSERT` a `:528`, `SELECT` sul conflitto a `:562`): va letto **prima** di cancellare, perche' e' il fatto su cui il piano 51-05 conta le righe
    - `src/app/api/membership/list/route.ts:80-95` — cosa restituiva (nome intero + codice socio)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:292-306` — chi scrive e chi legge `attendances`, misurato
  </read_first>
  <action>
    Cancellare i due file e le due directory `src/app/api/membership/verify/` e
    `src/app/api/membership/list/`, lasciando `src/app/api/membership/` vuota e
    quindi cancellata anch'essa. Prima di cancellare, riportare nel SUMMARY il
    fatto che il task 1 di 51-05 usera': **l'unico scrittore di
    `public.attendances` scriveva sempre `party_id` valorizzato**, mai NULL
    (`verify/route.ts:528-536`) — e' cio' che distingue le righe da porta dal
    residuo pre-porta.
    Nessun ramo del prodotto chiama piu' queste rotte dopo il piano 51-02: il
    ramo dello scanner e il `case` del drenaggio sono gia' usciti li'. Se il
    build trovasse un chiamante residuo, **fermarsi e dichiararlo** invece di
    aggiungere una compatibilita'.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test ! -d src/app/api/membership &amp;&amp; /usr/bin/grep -rc "api/membership" src/ | grep -v ":0$" | grep -qx "" </automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `src/app/api/membership/` non esiste
    - `/usr/bin/grep -rn "api/membership" src/` → zero righe
    - Il SUMMARY riporta il fatto misurato su `party_id` per il piano 51-05
  </acceptance_criteria>
  <done>La verifica per codice socio non ha piu' un indirizzo.</done>
</task>

<task type="auto">
  <name>Task 3: La persona smette di dichiarare una directory che non esiste</name>
  <files>CLAUDE.md, .claude/rules/checkin-offline.md, .claude/rules/access-gating.md, .claude/rules/meta-gates.md, .claude/CHANGELOG.md</files>
  <read_first>
    - `.claude/rules/checkin-offline.md:1-10` — il frontmatter `paths:`, che dichiara `src/app/api/membership/**` alla riga 5, e `:56` — il gate «rate limit sulla verifica», che nomina una rotta in uscita
    - `CLAUDE.md:239` — la riga d'indice «Check-in & Offline», che deve dichiarare **lo stesso insieme** di glob del frontmatter (controllo B)
    - `.claude/rules/meta-gates.md:61` — la riga di routing `src/app/api/membership/**` → checkin-offline (controllo G)
    - `.claude/rules/access-gating.md:66,69` — le due citazioni in prosa di `api/membership/verify` dentro il gate del rate limiting
    - `.claude/rules/ai-engineering.md` — semver, changelog, scenario di carico-e-scatto, context budget
    - `.claude/CHANGELOG.md:1-45` — la forma della voce (versione, «Cosa e' cambiato», «Cosa lo ha fatto scattare», «Scenario di carico e scatto»); l'ultima versione e' 1.23.0
    - `scripts/verify-persona.mjs` — leggere con `/usr/bin/grep -a` o con Read: il file porta byte NUL e `grep` nudo e' cieco su di esso
  </read_first>
  <action>
    Togliere `src/app/api/membership/**` da **tre** posti, nello stesso commit:
    il frontmatter `paths:` di `.claude/rules/checkin-offline.md`, la riga
    d'indice di `CLAUDE.md:239`, e la riga della tabella di priorita' di
    `.claude/rules/meta-gates.md:61`. I due insiemi (frontmatter e indice) devono
    restare identici o il controllo B arrossisce.
    Correggere le tre citazioni in prosa che nominano `api/membership/verify`
    come endpoint vivo — `checkin-offline.md:56` e `access-gating.md:66,69`. Il
    **gate** del rate limiting resta vero e resta scritto (questo repo non ha
    rate limiting); cambia l'**esempio**, perche' un gate che cita un endpoint
    inesistente e' documentazione datata. Non toccare la parola «member» altrove
    nei documenti: la riscrittura del lessico e' della fase 57 (Deferred Ideas),
    e anticiparla qui allargherebbe la fase.
    Scrivere la voce di `.claude/CHANGELOG.md` con un **minor bump** (1.23.0 →
    1.24.0, «Changed»): cosa e' cambiato, cosa lo ha fatto scattare, e lo
    **scenario di carico e scatto** — file reale nel suo scope
    (`src/lib/offline/checkin-store.ts`), moduli attesi (`checkin-offline.md`
    primario + `meta-gates.md`), modifica-tipo che deve far scattare il gate.
    Riportare nella voce il **context budget rimisurato** dal controllo E: la
    rimozione restringe, e il numero va scritto, non dedotto.
    Lanciare `npm run verify:persona` **dopo** la cancellazione delle rotte
    (task 2), mai prima: un modulo aggiornato prima della cancellazione descrive
    un futuro, e un glob tolto prima che la directory sparisca lascerebbe scoperti
    i file ancora presenti.
  </action>
  <verify>
    <automated>npm run verify:persona &amp;&amp; /usr/bin/grep -c "api/membership" CLAUDE.md .claude/rules/checkin-offline.md .claude/rules/meta-gates.md | grep -qv ":[1-9]" &amp;&amp; grep -q "1.24.0" .claude/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:persona` verde, 7/7 controlli (in particolare A, B, E, G)
    - Zero occorrenze di `api/membership` in `CLAUDE.md`, `checkin-offline.md`, `meta-gates.md`
    - `.claude/CHANGELOG.md` porta la voce `1.24.0` con lo scenario di carico e scatto e il context budget rimisurato
    - Nessuna riscrittura del lessico «member/membership» oltre alle tre citazioni di rotta (la 57 resta intatta)
  </acceptance_criteria>
  <done>La persona descrive il prodotto che esiste, e il gate lo dimostra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rete → service worker sul telefono dello staff | cio' che il telefono considera «la lista» quando la rete e' incerta |
| repo pubblico → persona | un modulo che descrive un prodotto che non esiste guida ogni risposta successiva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-10 | Tampering | `doorRuntimeCaching` | mitigate | la verifica e' su `public/sw.js` dopo il build, non sul diff: i due matcher che restano devono comparire nell'output, o la lista della porta cadrebbe sulla regola ereditata `NetworkFirst` a 86400 s |
| T-51-11 | Denial of Service | un precache che risponde ancora per una rotta rimossa | mitigate | `precacheEntries` e' generato dal build: la rotta sparisce dal manifest da sola; asserito con zero occorrenze di `api/membership` in `public/sw.js` |
| T-51-12 | Information disclosure | roster sul telefono | mitigate (in sottrazione) | la rotta che portava nome intero e codice socio su ogni telefono che poteva lavorare una porta non esiste piu' |
| T-51-13 | Repudiation | persona con path morto | mitigate | `verify:persona` A/B/E/G verde **dopo** la cancellazione; changelog con semver e scenario di carico-e-scatto |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build` verde; `public/sw.js` senza `api/membership` e con i due matcher dei biglietti
- `npm run verify:persona` 7/7, lanciato dopo la cancellazione
- `npm run verify` verde salvo i rossi dichiarati altrove in questa fase
</verification>

<success_criteria>
MEM-03 lato server e lato cache: la rotta non esiste, il service worker non la
nomina, e la persona non dichiara una directory sparita.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-03-SUMMARY.md` when done
</output>
