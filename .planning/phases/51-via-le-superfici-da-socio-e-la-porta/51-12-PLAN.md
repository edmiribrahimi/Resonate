---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 12
type: execute
wave: 5
depends_on: ["51-05", "51-11"]
files_modified:
  - supabase/migrations/<timestamp>_drop_membership_code_and_rename_acts.sql
  - scripts/purge-attendances.mjs
  - .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md
autonomous: true
requirements: [MEM-01, MEM-02]

must_haves:
  truths:
    - "profiles.membership_code non esiste piu', e nessuna funzione plpgsql si e' rotta in silenzio con lei (D-51-02)"
    - "Il registro si chiama account_acts, con i suoi vincoli, indici, policy e privilegi riletti dal catalogo (D-51-08)"
    - "subject_label sulle righe nuove porta le prime 8 cifre dell'identificativo del soggetto (D-51-15)"
    - "public.attendances e' stata svuotata per chiave e poi tolta, e la tabella non si droppa se non e' vuota (D-51-04, D-51-14)"
    - "Tutto questo e' stato fatto prima sul LABORATORIO, con la procedura scritta"
  artifacts:
    - path: "supabase/migrations/<timestamp>_drop_membership_code_and_rename_acts.sql"
      provides: "un file, una transazione: funzioni ridefinite, tabella rinominata, colonna tolta, tabella vuota droppata"
      contains: "account_acts"
    - path: "scripts/purge-attendances.mjs"
      provides: "conteggio, cascata, istantanea, cattura degli id e cancellazione per chiave — con il rifiuto del ref di produzione come prima riga eseguita"
      contains: "PRODUCTION_REF"
  key_links:
    - from: "supabase/migrations/<timestamp>_drop_membership_code_and_rename_acts.sql"
      to: "public.record_account_act"
      via: "la ridefinizione della funzione nella stessa transazione del DROP COLUMN che la romperebbe"
      pattern: "record_account_act"
    - from: "scripts/purge-attendances.mjs"
      to: "public.attendances"
      via: "DELETE ... WHERE id = ANY(lista catturata), mai un predicato ricomposto al momento dell'atto"
      pattern: "= ANY"
---

<objective>
Scrivere la seconda migration — via il codice socio, il registro rinominato,
`attendances` svuotata e tolta — e provarla **tutta** sul laboratorio, compresa
la cancellazione per chiave.

Purpose: D-51-02, D-51-08, D-51-14, D-51-15. Un `DROP COLUMN` da solo
**riesce** e rompe in silenzio `record_membership_act`, che e' `plpgsql` e non
registra la dipendenza; con essa si rompe la creazione degli account. La fase 50
ha pagato esattamente questo errore e lo ha scritto dentro il proprio file.
Output: un file di migration applicato al laboratorio, uno strumento di
cancellazione per chiave provato li', e il catalogo riletto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md
@./CLAUDE.md
@.claude/rules/supabase-data.md
@.claude/rules/ai-engineering.md
@.claude/rules/meta-gates.md

<interfaces>
Regole gia' misurate, da non rimisurare:
- `POST /v1/projects/{ref}/database/migrations` avvolge il corpo in UNA transazione: **niente `BEGIN;`**
- `CREATE OR REPLACE` su firma identica conserva ACL, `SECURITY DEFINER` e `proconfig`; sostituisce il corpo **per intero**
- `ALTER FUNCTION … RENAME TO` conserva ACL e `SECURITY DEFINER`; `DROP` + `CREATE` no
- `ALTER TABLE … RENAME TO` **non** rinomina vincoli, indici e policy: servono `RENAME CONSTRAINT`, `ALTER INDEX … RENAME TO`, `ALTER POLICY … RENAME TO`, e i privilegi si **rileggono dal catalogo dopo**

Oggetti da toccare: **l'elenco vero e' in `51-CATALOG.md`**, non negli elenchi
letti dai file. Quelli previsti:
  public.record_membership_act(8 arg)  → ridefinire e rinominare
  public.handle_new_user()             → ridefinire per intero, senza il conio
  public.record_party_assignment_act   → chiama la precedente
  public.reconcile_master(text)        → idem, gira a ogni deploy
  public.membership_acts               → tabella, 2 CHECK, 2 indici, 1 policy, privilegi, COMMENT
  public.profiles.membership_code      → colonna + vincolo UNIQUE
  public.attendances                   → tabella, 2 policy, 3 indici
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere la migration — un file, una transazione</name>
  <files>supabase/migrations/<timestamp>_drop_membership_code_and_rename_acts.sql</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md` — gli oggetti veri, con firma, `prosecdef` e `proconfig`: **vince il catalogo**
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:1-115` — l'intestazione: verso del deploy, «un'eccezione dentro l'eccezione», «una transazione sola e non c'e' `BEGIN;`», «cosa questa migration NON fa», «l'ordine dei passi»
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:551-615` — il corpo vivo di `record_membership_act`, incluso il `SELECT … FOR UPDATE` e il `RAISE EXCEPTION` che nomina **solo** l'identificativo
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:820-880` — `handle_new_user` viva, il conio con `extensions.gen_random_bytes(10)`, i cinque tentativi sulla sola collisione, e il `COMMENT` che descrive quel conio
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:1003-1012` — la forma del `DROP COLUMN` in coda, con la dichiarazione «IRREVERSIBILE, e questo repository non ha PITR»
    - `supabase/migrations/20260808002000_membership_register.sql:195-300` — la tabella, `subject_label NOT NULL` e il commento che vieta nome, indirizzo e contatto
    - `supabase/migrations/20260808005000_membership_acts_append_only.sql:135-150` — `REVOKE`/`GRANT` e il `COMMENT ON TABLE`
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md:99-230` — il pattern 1, con la forma dell'intestazione e del blocco `DO` di controllo
  </read_first>
  <action>
    Creare `<timestamp UTC>_drop_membership_code_and_rename_acts.sql`, timestamp
    strettamente maggiore di ogni migration presente, **compresa quella del piano
    51-08**.
    Intestazione nella forma della fase 50, con il contenuto di questa: il verso
    del deploy (**codice prima, migration dopo** — la colonna e la funzione sono
    lette da file di prodotto, e applicarla prima aprirebbe una finestra
    `42703`), il paragrafo «un'eccezione dentro l'eccezione» riscritto su
    `record_membership_act` invece che sul trigger, il paragrafo che dichiara
    l'assenza di `BEGIN;` con la sua misura, «cosa questa migration NON fa»,
    «l'ordine dei passi».
    L'ordine, che non e' estetico:
    1. `record_membership_act` ridefinita con `CREATE OR REPLACE` su **firma
       identica**, corpo per intero, con due sole differenze: non legge piu'
       `p.membership_code`, e scrive `subject_label` come **le prime 8 cifre di
       `p_subject_id`** (D-51-15). Accanto, il limite dichiarato: e' un'etichetta
       che **non sopravvive alla cancellazione del soggetto**, a differenza del
       codice che c'era prima — ed e' il mestiere che la colonna aveva. Restano
       intatti il `SELECT … FOR UPDATE` e il `RAISE EXCEPTION` che nomina solo
       l'identificativo: mai un nome, mai un indirizzo, perche' un messaggio
       sollevato raggiunge un log e in questo progetto un log raggiunge uno
       screenshot;
    2. `handle_new_user` ridefinita **per intero**, senza il conio del codice: via
       `extensions.gen_random_bytes(10)`, via i cinque tentativi sulla collisione,
       via la colonna dall'`INSERT`. Riscrivere anche il suo `COMMENT`, che oggi
       descrive quel conio;
    3. `record_party_assignment_act` e `reconcile_master` ridefinite, perche'
       **chiamano** la funzione che sta per cambiare nome; `reconcile_master`
       gira a ogni deploy, quindi un suo riferimento rotto si manifesterebbe al
       primo;
    4. `ALTER FUNCTION public.record_membership_act(<firma dal catalogo>) RENAME TO record_account_act;`
    5. `ALTER TABLE public.membership_acts RENAME TO account_acts;` seguito dai
       **satelliti, uno per uno**: `RENAME CONSTRAINT` sui due `CHECK`,
       `ALTER INDEX … RENAME TO` sui due indici, `ALTER POLICY … RENAME TO` sulla
       policy di lettura del registro, e il `COMMENT ON TABLE` riscritto. Un
       `RENAME TO` non muove i suoi satelliti, e i privilegi si rileggono **dopo**;
    6. `ALTER TABLE public.profiles DROP COLUMN IF EXISTS membership_code;` — in
       coda, con la propria dichiarazione: **irreversibile, e questo repository
       non ha PITR**; il vincolo `UNIQUE` cade con la colonna;
    7. un blocco `DO` che **solleva** se `public.attendances` contiene anche una
       sola riga, seguito da `DROP TABLE public.attendances;` con le sue policy.
       La guardia non e' una cortesia: rende **meccanico** l'ordine che D-51-04
       impone — prima la cancellazione per chiave, con il suo conteggio e la sua
       istantanea, poi il `DROP` di una tabella vuota. Un `DROP` su una tabella
       piena cancellerebbe righe **senza** che nessuno le abbia contate.
    Ogni oggetto toccato porta il proprio `COMMENT ON …`, che dice cosa resta
    leggibile senza essere piu' scrivibile e quale fase l'ha deciso.
    Il file **non si applica** in questo task.
  </action>
  <verify>
    <automated>ls supabase/migrations/*_drop_membership_code_and_rename_acts.sql &amp;&amp; test $(/usr/bin/grep -ci "^BEGIN;" supabase/migrations/*_drop_membership_code_and_rename_acts.sql) -eq 0 &amp;&amp; /usr/bin/grep -q "RENAME TO record_account_act" supabase/migrations/*_drop_membership_code_and_rename_acts.sql &amp;&amp; /usr/bin/grep -q "DROP TABLE public.attendances" supabase/migrations/*_drop_membership_code_and_rename_acts.sql</automated>
  </verify>
  <acceptance_criteria>
    - Il file esiste, timestamp maggiore di quello del piano 51-08, zero `BEGIN;` e zero `COMMIT;`
    - I sette passi ci sono e sono nell'ordine dichiarato
    - Le quattro funzioni sono ridefinite con la firma che dice `51-CATALOG.md`, e la rinomina usa `ALTER FUNCTION … RENAME TO`
    - I satelliti della tabella sono rinominati uno per uno
    - Il `DROP TABLE public.attendances` e' preceduto dalla guardia che solleva se non e' vuota
    - Ogni oggetto porta il proprio `COMMENT`
  </acceptance_criteria>
  <done>La migration esiste, dice cosa fa e cosa non fa, e non puo' cancellare righe che nessuno ha contato.</done>
</task>

<task type="auto">
  <name>Task 2: Lo strumento della cancellazione, e la prova per chiave sul LABORATORIO</name>
  <files>scripts/purge-attendances.mjs, .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md</files>
  <read_first>
    - `.claude/rules/ai-engineering.md` — i gate: conteggio prima, istantanea per cascata, rimozione **per chiave**, contatore da **fonte diversa**, autorizzazione come atto che si consuma una volta
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:341-352` — i sette passi della forma dell'atto, e il perche' della chiave: una chiave sbagliata non trova nulla, un predicato sbagliato cancella di piu'
    - `scripts/seed-lab-door.mjs:70-92` — il rifiuto del ref di produzione come prima riga eseguita, e il commento che spiega perche' il ref di produzione sta in chiaro e quello del laboratorio no
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md` — i conteggi e la cascata gia' letti dal piano 51-05
  </read_first>
  <action>
    Scrivere `scripts/purge-attendances.mjs`, lo strumento che il piano 51-13
    usera' in produzione e che **qui si prova sul laboratorio**. Deve:
    (a) **rifiutare il ref di produzione come prima riga eseguita**, a meno che
    non riceva un argomento esplicito che nomina il file di autorizzazione e la
    sua data; in quel caso legge `51-AUTHORISATION.md`, e rifiuta comunque se il
    documento non e' concesso o e' gia' dichiarato esaurito. Un'autorizzazione e'
    un atto che si consuma una volta, e lo strumento lo fa rispettare invece di
    ricordarlo;
    (b) contare **per provenienza**: `party_id` valorizzato (cio' che D-51-04
    autorizza) e `party_id` nullo (il residuo pre-porta, che D-51-04 **non
    nomina**), e stampare i due numeri separati;
    (c) enumerare la cascata **dal catalogo** (`pg_constraint`, `confrelid`), non
    da un documento;
    (d) scrivere un'**istantanea** di `public.attendances` intera in un file
    datato, prima di qualunque scrittura, piu' il conteggio righe delle tabelle
    raggiungibili;
    (e) **catturare la lista degli `id`** con la stessa interrogazione che li
    conta, e stamparla nel referto;
    (f) cancellare con `DELETE … WHERE id = ANY(<lista catturata>)` — **mai** per
    interfaccia, **mai** con un predicato ricomposto al momento dell'atto;
    (g) rileggere il conteggio **da una fonte diversa** da quella su cui ha
    agito: se l'atto e' passato dal Management API, il controllo si prende da
    PostgREST con la chiave di servizio.
    Con `--dry-run` deve fare (b) (c) (d) (e) e fermarsi. In assenza di righe da
    cancellare deve dirlo ed uscire con successo: **una decisione senza soggetti
    non si esegue**, e va dichiarata invece di lasciarla scoprire a meta'
    procedura.
    Provarlo sul laboratorio: prima `--dry-run`, poi l'atto. Scrivere in
    `51-CATALOG.md`, in una sezione «la prova sul laboratorio», i numeri prima,
    la cascata letta, il percorso dell'istantanea, quante righe sono state
    cancellate e il conteggio dopo, con la fonte di ciascuno e l'ora UTC.
  </action>
  <verify>
    <automated>node scripts/purge-attendances.mjs --dry-run; test $? -le 1 &amp;&amp; /usr/bin/grep -q "PRODUCTION_REF" scripts/purge-attendances.mjs &amp;&amp; /usr/bin/grep -q "= ANY" scripts/purge-attendances.mjs &amp;&amp; /usr/bin/grep -q "laboratorio" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md</automated>
  </verify>
  <acceptance_criteria>
    - Lo script rifiuta il ref di produzione come **prima riga eseguita**, e senza autorizzazione valida non prosegue
    - `--dry-run` produce conteggio per provenienza, cascata dal catalogo, istantanea e lista di `id`, senza scrivere nulla
    - La cancellazione e' per chiave (`= ANY`), e il conteggio dopo viene da una fonte diversa da quella dell'atto
    - Sul laboratorio: `public.attendances` risulta vuota dopo l'atto, con i numeri scritti in `51-CATALOG.md`
    - Le due popolazioni restano separate nel referto
  </acceptance_criteria>
  <done>Esiste uno strumento che cancella per chiave, e lo ha gia' fatto una volta dove non costa nulla.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Applicare la migration al LABORATORIO e rileggere il catalogo</name>
  <files>supabase/migrations/<timestamp>_drop_membership_code_and_rename_acts.sql</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:357-364` — l'endpoint `migrations`, non `/database/query`, o la history del progetto mente
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md` — i valori «prima», per poter dire cosa e' cambiato
    - `scripts/dev-lab.sh:1-23` — come si carica l'ambiente del laboratorio sopra quello di produzione
  </read_first>
  <action>
    Applicare il file al **laboratorio** con
    `POST /v1/projects/$LAB_PROJECT_REF/database/migrations`. La produzione e'
    fuori dal perimetro: la tocca il piano 51-13, sotto autorizzazione datata.
    Rileggere **dal catalogo**:
    (a) `public.profiles` non ha piu' la colonna, e il vincolo `UNIQUE` non c'e'
    piu' (`information_schema.columns`, `pg_constraint`);
    (b) `public.account_acts` esiste, `public.membership_acts` no
    (`to_regclass` su entrambi);
    (c) i due `CHECK`, i due indici e la policy portano i nomi nuovi
    (`pg_constraint`, `pg_indexes`, `pg_policies`);
    (d) i privilegi sulla tabella rinominata, **riletti** e confrontati con
    quelli che `51-CATALOG.md` aveva registrato prima;
    (e) `pg_get_functiondef` delle quattro funzioni: nomi, firme, `prosecdef`,
    `proconfig` sopravvissuti;
    (f) `to_regclass('public.attendances')` e' nullo;
    (g) **una prova funzionale**: creare un account di prova sul laboratorio e
    verificare che la riga di registro esista con `subject_label` uguale alle
    prime 8 cifre dell'identificativo. E' l'unico modo di dimostrare che la
    funzione `plpgsql` non si e' rotta in silenzio — un `SELECT` sul catalogo non
    lo direbbe.
    Rilanciare poi `npm run build`, `npm run verify`, e i due gate vivi
    (`verify:capabilities`, `verify:refusal`) **con l'ambiente del laboratorio
    caricato**. Se uno solo dei sette controlli non corrisponde, **fermarsi**.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - La migration risulta nella history del laboratorio, con la versione registrata scritta nel SUMMARY
    - Le sette riletture (a)-(g) sono nel SUMMARY con l'ora UTC, prese dal catalogo e non dalla risposta del `POST`
    - La prova funzionale (g) ha prodotto una riga di registro con l'etichetta attesa
    - `verify:capabilities` e `verify:refusal` verdi contro il laboratorio; lo stato contro la produzione resta dichiarato con il piano che lo chiude (51-13)
    - Nessuna scrittura sul progetto di produzione, dichiarato nel SUMMARY
  </acceptance_criteria>
  <done>Il laboratorio non ha piu' il codice socio, ha il registro con il nome nuovo, e sa ancora creare un account.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → funzioni `plpgsql` | una dipendenza non registrata si rompe **dopo**, in silenzio, sul percorso di chi ha appena pagato |
| strumento di cancellazione → righe di persone | qui si cancellano righe che dicono che qualcuno e' entrato a una serata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-51 | Denial of Service | `record_account_act` / `createAccount` | mitigate | `DROP COLUMN`, ridefinizione e rinomina in **un file e una transazione**; la prova funzionale (g) crea davvero un account sul laboratorio, perche' il catalogo non direbbe che la funzione si e' rotta |
| T-51-52 | Tampering | `DELETE` piu' largo della lista catturata | mitigate | `DELETE … WHERE id = ANY(<lista>)`; nessun predicato ricomposto al momento dell'atto; il verso dell'errore e' il punto |
| T-51-53 | Repudiation | righe cancellate che nessuno ha contato | mitigate | la guardia `DO` rifiuta il `DROP TABLE` se `attendances` non e' vuota: l'ordine e' meccanico, non ricordato |
| T-51-54 | Tampering | `RENAME` che lascia indietro vincoli, indici, policy o privilegi | mitigate | satelliti rinominati uno per uno; privilegi **riletti** dal catalogo dopo, non dedotti |
| T-51-55 | Information disclosure | `subject_label` | mitigate | 8 cifre di un identificativo: non un nome, non un indirizzo, non una credenziale; il limite (non sopravvive al soggetto) e' scritto nella migration |
| T-51-56 | Tampering | scrittura accidentale in produzione | mitigate | solo `$LAB_PROJECT_REF`; lo strumento rifiuta il ref di produzione come prima riga eseguita e pretende un'autorizzazione non esaurita |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- Sul laboratorio: colonna assente, registro rinominato con i suoi satelliti, `attendances` inesistente, quattro funzioni vive
- Un account creato sul laboratorio dopo la migration produce una riga di registro con l'etichetta attesa
- `npm run build` e `npm run verify` verdi; i due gate vivi verdi contro il laboratorio
</verification>

<success_criteria>
Tutto cio' che la produzione dovra' subire e' gia' successo una volta, dove non
costa nulla, con i numeri scritti.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-12-SUMMARY.md` when done
</output>
