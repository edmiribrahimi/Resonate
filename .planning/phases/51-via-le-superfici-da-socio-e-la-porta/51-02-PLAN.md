---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/scanner/ScannerClient.tsx
  - src/lib/offline/checkin-store.ts
  - src/lib/offline/sync-manager.ts
  - src/lib/door/outcome.ts
  - src/lib/door/classify.ts
  - src/app/api/tickets/checkin/undo/route.ts
autonomous: true
requirements: [MEM-03]

must_haves:
  truths:
    - "Nessun percorso del prodotto scansiona, verifica o mette in coda un codice socio (MEM-03, D-51-01)"
    - "Un codice della forma RSN-… riceve lo stesso rifiuto di un codice sconosciuto, senza messaggio dedicato (D-51-01)"
    - "Un telefono che arriva con una coda NON vuota perde solo le voci membership: quelle ticket e guest restano (D-51-11)"
    - "Lo scarto lascia una riga di log con categoria e conteggio, e nessuna superficie (D-51-11)"
    - "'membership' resta leggibile in DoorSubjectType e nel CHECK SQL come valore storico (D-51-13)"
    - "L'annullamento alla porta non ha piu' un ramo socio, quindi public.attendances resta senza scrittori E senza lettori nel prodotto (MEM-02)"
  artifacts:
    - path: "src/lib/offline/checkin-store.ts"
      provides: "coda a due tipi, store members rimosso, passo di aggiornamento v6"
      contains: "DB_VERSION = 6"
    - path: "src/lib/offline/sync-manager.ts"
      provides: "drenaggio su due rami soltanto"
    - path: "src/lib/door/outcome.ts"
      provides: "DoorSubjectType con 'membership' dichiarato storico e non piu' scrivibile"
      contains: "membership"
  key_links:
    - from: "src/lib/offline/checkin-store.ts"
      to: "src/lib/offline/sync-manager.ts"
      via: "QueuedSubjectType che si restringe e rende il case un errore di tipo"
      pattern: "QueuedSubjectType"
    - from: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      to: "src/lib/offline/checkin-store.ts"
      via: "gli import di findMember / checkInMemberLocally / rosterPredatesRole che spariscono"
      pattern: "checkin-store"
---

<objective>
Togliere il ramo socio dalla porta lato client: lo scanner smette di riconoscere
un codice `RSN-…`, la coda offline perde il suo terzo tipo, e lo store dei soci
esce dal telefono.

Purpose: MEM-03. E' la parte irreversibile davanti a una fila: una rimozione
parziale si manifesta alle due di notte, non alla scrivania.
Output: scanner e coda a due rami — `ticket` e `guest` — intatti nei due rami che
funzionano, e un passo di aggiornamento IndexedDB che scarta solo cio' che deve.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@./CLAUDE.md
@.claude/rules/checkin-offline.md
@.claude/rules/meta-gates.md

<interfaces>
I contratti che restano, e che questo piano non deve toccare:

`src/lib/door/outcome.ts`
  export type DoorSubjectType = "ticket" | "guest_list_entry" | "membership";  // :56 — resta intera (D-51-13)
  export type DoorNotValidReason = …;                                          // :92-97 — Record totali a valle
`src/lib/offline/checkin-store.ts`
  export type QueuedSubjectType = "ticket" | "guest" | "membership";           // :105 — perde il terzo
  const DB_VERSION = 5;                                                        // :57 → 6
`src/lib/offline/sync-manager.ts`
  function targetFor(entry: PendingCheckin): Target                            // :352 — switch su union che si restringe
Il ramo `guest` drena verso `POST /api/tickets/attendance` e **non si tocca**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lo scanner smette di riconoscere un codice socio</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx:16,18,33` (import di `findMember`, `checkInMemberLocally`, `rosterPredatesRole`), `:69,71` (i due pattern), `:678,784-796` (`rosterPredatesRoleRef` e l'effetto che lo popola), `:1288-1343` (il blocco roster dentro `fetchAttendance`, l'avviso a `:1311` e la scrittura di `lastFetchAtRef` a `:1343`), `:2244-2330` e `:2358-2400` (`membershipOnline` / `membershipOffline`), `:2415` e `:2448` (il dispatch sui due pattern)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:276-289` — la tabella sito per sito
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md` — D-51-01 (nessun messaggio dedicato) e D-51-03 (staff e organizer non scansionano)
  </read_first>
  <action>
    Cancellare da `ScannerClient.tsx` tutto cio' che esiste per il ramo socio:
    `MEMBERSHIP_PATTERN`, `BARE_MEMBERSHIP_PATTERN`, gli import di `findMember`,
    `checkInMemberLocally` e `rosterPredatesRole`, `rosterPredatesRoleRef` con
    l'effetto che lo popola, le funzioni `membershipOnline` e
    `membershipOffline`, il blocco che scarica il roster dentro
    `fetchAttendance` (compresi la `fetch("/api/membership/list")` e l'avviso
    appiccicoso «The member list on this device was NOT refreshed»), e il ramo
    del dispatch che riconosce i due pattern. L'union locale del tipo di scan si
    restringe a `"ticket" | "guest"`.
    **D-51-01, e va scritto come commento accanto al dispatch:** un codice della
    forma `RSN-…` non riceve un messaggio proprio — cade nel rifiuto del codice
    sconosciuto, e il vocabolario del rifiuto e' quello che gia' esiste
    (`NOT_VALID_MESSAGE`, `Record` totale su `DoorNotValidReason`), non una
    stringa nuova.
    **Cio' che NON si tocca in questo task:** `lastFetchAtRef` (`:1042`),
    `listAgeMs` (`:2643`) e `listIsStale` (`:2665`) restano dove sono — il
    piano 51-07 ci costruisce sopra l'avviso derivato di D-51-10. La scrittura
    di `lastFetchAtRef.current = performance.now()` a `:1343` deve restare
    raggiungibile dopo la rimozione del blocco roster: e' la sola cosa che dice
    quanto e' vecchia la lista.
    Nessun nome di persona nei commenti; `re:sonate` con la e normale.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; /usr/bin/grep -c "MEMBERSHIP_PATTERN\|membershipOnline\|membershipOffline\|api/membership/list\|rosterPredatesRole" "src/app/(admin)/admin/scanner/ScannerClient.tsx" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero occorrenze di `MEMBERSHIP_PATTERN`, `membershipOnline`, `membershipOffline`, `api/membership/list`, `rosterPredatesRole` nel file
    - `/usr/bin/grep -n "lastFetchAtRef.current = performance.now()" ScannerClient.tsx` trova ancora una riga raggiungibile dal percorso riuscito di `fetchAttendance`
    - Nessuna stringa di rifiuto nuova introdotta: `NOT_VALID_MESSAGE` resta l'unico vocabolario
  </acceptance_criteria>
  <done>Lo scanner non conosce piu' un codice socio, e non gli dedica un messaggio.</done>
</task>

<task type="auto">
  <name>Task 2: La coda passa a v6 e scarta le voci membership, per chiave</name>
  <files>src/lib/offline/checkin-store.ts</files>
  <read_first>
    - `src/lib/offline/checkin-store.ts:33-38` (la disciplina dei passi cumulativi), `:56-57` (`DB_NAME`/`DB_VERSION`), `:63-72` (`ROSTER_PREDATES_ROLE_KEY`), `:105` (`QueuedSubjectType`), `:160-180` (`MemberRecord`), `:233-236` (`entryRole`), `:264-266` e `:309` (lo store `members`), `:313-322` (`QUEUE_TYPE_BY_SUBJECT` / `SUBJECT_BY_QUEUE_TYPE`), `:437-461` (il passo v3 e le due regole: copia prima, solo promesse `idb` attese), `:487-489` (creazione dello store `members`), `:539-542` (la chiave meta del passo v4), `:545-596` (il passo v5 e la proprieta' che dichiara), `:954-1020` (`checkInMemberLocally`), `:1288-1303` (`bumpAttempts`, la forma «apri una readwrite, leggi, cancella per chiave»), `:1329-1370` (`cacheMembers`, la forma «conta mentre scorri»), `:1374-1389` (`rosterPredatesRole`), `:1471-1477` (`findMember`)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:236-267` — cosa esce, la forma del passo v6, le quattro regole che restano intatte
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md:291-378` — l'analogo (se stesso) e il vincolo sul commento del passo v5
    - `.claude/rules/checkin-offline.md` — la coda non si cancella, l'annullamento si registra
  </read_first>
  <action>
    Portare `DB_VERSION` da 5 a 6 e aggiungere un blocco `if (oldVersion < 6)`
    proprio, che e' **l'unico passo di questo store che tocca la coda** — e va
    scritto dichiarandolo, citando accanto la proprieta' che il passo v5
    enunciava (`:545-560`) e che qui si rompe deliberatamente per D-51-11.
    Il passo v6: apre una `readwrite` su `pendingCheckins` (e, se la casa lo
    prevede per i falliti, anche `failedCheckins`), scorre le voci, **cancella
    per chiave** solo quelle con `type === "membership"` contando quante,
    lascia intatte `ticket` e `guest`, e **solo dopo** cancella lo store
    `members` con `deleteObjectStore`. Restano valide, e vanno rispettate: copia
    prima e cancella poi; **dentro la callback si attendono solo promesse
    `idb`** — un `await` su altro lascerebbe chiudere la transazione
    `versionchange` a meta' migrazione, e in questo repository non esiste alcun
    test runner che lo coglierebbe.
    Emettere **una sola** riga di log, nella convenzione della casa
    `categoria:evento` + oggetto di contesto:
    `console.warn("checkin-store:v6_dropped_membership_entries", { count })`.
    Nessuna superficie, come D-51-11 impone, e mai un messaggio generico.
    Restringere `QueuedSubjectType` a `"ticket" | "guest"`, e cancellare:
    `MemberRecord`, lo store `members` dalla lista degli store, la voce
    `membership` dai due `Record` totali `QUEUE_TYPE_BY_SUBJECT` /
    `SUBJECT_BY_QUEUE_TYPE`, `ROSTER_PREDATES_ROLE_KEY` e `rosterPredatesRole()`,
    `cacheMembers()`, `findMember()`, `checkInMemberLocally()` con
    `MembershipLocalResult`, e `entryRole` dai record di coda.
    Scrivere accanto al passo v6 che **nomina l'identificativo dello store della
    coda** — cosa che il passo v5 aveva il divieto di fare perche' la sua
    asserzione era un grep — e perche' qui e' corretto.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -q "DB_VERSION = 6" src/lib/offline/checkin-store.ts &amp;&amp; grep -q "oldVersion < 6" src/lib/offline/checkin-store.ts &amp;&amp; /usr/bin/grep -c "checkin-store:v6_dropped_membership_entries" src/lib/offline/checkin-store.ts | grep -qx 1 &amp;&amp; /usr/bin/grep -c "cacheMembers\|findMember\|checkInMemberLocally\|rosterPredatesRole" src/lib/offline/checkin-store.ts | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - `DB_VERSION = 6` e un blocco `if (oldVersion < 6)` esistono; i blocchi v3, v4 e v5 sono invariati (`git diff` non li tocca)
    - La riga di log compare **una sola volta** nel file
    - Zero occorrenze di `cacheMembers`, `findMember`, `checkInMemberLocally`, `rosterPredatesRole`
    - Dentro la callback `upgrade` non c'e' alcun `await` su una promessa che non sia `idb` (lettura del diff, dichiarata nel SUMMARY)
  </acceptance_criteria>
  <done>Un telefono che arriva con una coda non vuota perde solo il terzo tipo, e lo dice una volta in console.</done>
</task>

<task type="auto">
  <name>Task 3: Il drenaggio e l'annullamento perdono il ramo socio, e 'membership' resta un valore storico</name>
  <files>src/lib/offline/sync-manager.ts, src/lib/door/outcome.ts, src/lib/door/classify.ts, src/app/api/tickets/checkin/undo/route.ts</files>
  <read_first>
    - `src/lib/offline/sync-manager.ts:352-400` — `targetFor()`, il `case "membership"` con i suoi quattro paragrafi di commento, e il `case "guest"` che resta identico
    - `src/lib/door/outcome.ts:17-21` (la regola dello specchio: modificare uno dei due insiemi di letterali significa modificare entrambi, nello stesso commit), `:56` (`DoorSubjectType`), `:92-97` (`DoorNotValidReason` e i `Record` totali)
    - `src/lib/door/classify.ts:79,82,144-146,267-273` — i punti che descrivono il ramo membership
    - `src/lib/membership/acts.ts:55-105` — l'analogo di D-51-13: un'unione che continua a nominare righe storiche che nessuno scrive piu'
    - `supabase/migrations/20260805120000_door_scan_events.sql:69-70` — il `CHECK` specchiato, che **non si tocca**
    - `src/app/api/tickets/checkin/undo/route.ts:30-55` (il docblock che distingue i tre rami dell'annullamento e dice che sul ramo socio **l'ammissione stessa smette di esistere**), `:465-480` e `:525-570` — il ramo che legge e cancella da `public.attendances`, unico lettore rimasto di quella tabella
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md` — D-51-13
  </read_first>
  <action>
    In `sync-manager.ts`: cancellare il `case "membership"` di `targetFor()` con
    i suoi paragrafi di commento. `case "ticket"` e `case "guest"` non si
    toccano — il ramo `guest` drena verso `POST /api/tickets/attendance` e resta
    identico. Il `switch` e' su un'union che si e' ristretta nel task 2: il
    build e' il gate.
    In `outcome.ts`: `DoorSubjectType` **resta a tre membri** e il `CHECK` SQL
    **non si tocca** — D-51-13. Riscrivere il commento a `:56` nella forma gia'
    usata da `src/lib/membership/acts.ts:55-105`: `'membership'` e' un valore da
    **leggere**, non piu' da scrivere; le righe storiche di `door_scan_events`
    lo portano, il registro delle convalide non si riscrive, e D-51-04 non
    autorizza a toccarlo. Dire accanto che la regola dello specchio non e'
    violata perche' nessuno dei due insiemi cambia.
    In `classify.ts`: potare i commenti che descrivono un ramo che non esiste
    piu', lasciando la lettura del valore storico intatta.
    In `checkin/undo/route.ts`: togliere il **ramo di annullamento della
    presenza da socio** — la `SELECT` e la `DELETE` su `public.attendances` e il
    proprio messaggio d'errore — e i paragrafi di docblock che lo descrivono. I
    rami **biglietto** e **guest list** non si toccano: sono l'annullamento che
    lo staff usa davvero, e il loro comportamento e' esercitato da `P-51-1`.
    Dichiarare nel SUMMARY la conseguenza che il piano 51-12 usera': dopo questo
    task `public.attendances` non ha piu' **ne' scrittori ne' lettori** nel
    prodotto — lo scrittore e' uscito con la rotta (piano 51-03), il lettore
    esce qui.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test $(/usr/bin/grep -c 'case "membership"' src/lib/offline/sync-manager.ts) -eq 0 &amp;&amp; /usr/bin/grep -q "membership" src/lib/door/outcome.ts &amp;&amp; test $(/usr/bin/grep -rc 'from("attendances")' src/) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` esce 0
    - Zero `case "membership"` in `sync-manager.ts`; i rami `ticket` e `guest` invariati nel diff
    - `DoorSubjectType` ha ancora tre membri e porta il commento che dichiara il terzo come storico
    - `supabase/migrations/20260805120000_door_scan_events.sql` non compare nel diff di questo piano
    - `/usr/bin/grep -rn 'from("attendances")' src/` → **zero righe**: la tabella non ha piu' ne' scrittori ne' lettori nel prodotto
    - I rami biglietto e guest list dell'annullamento sono invariati nel diff
  </acceptance_criteria>
  <done>Il drenaggio conosce due rami; la storia della porta resta leggibile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telefono dello staff (offline) → IndexedDB | righe di ammissione di persone che hanno pagato, su un telefono che nessuno puo' ispezionare |
| coda locale → API di drenaggio | l'unico percorso che riporta in banca dati cio' che e' successo alla porta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05 | Repudiation | passo di aggiornamento v6 di `checkin-store.ts` | mitigate | si cancella **per chiave** solo `type === "membership"`; `ticket` e `guest` non si leggono per riscriverli; `deleteObjectStore("members")` avviene dopo; provato su dispositivo con coda non vuota (`P-51-1` passo 9) |
| T-51-06 | Denial of Service | callback `versionchange` | mitigate | dentro la callback si attendono **solo** promesse `idb`; un `await` su altro chiuderebbe la transazione a meta' e nessun test runner lo coglierebbe |
| T-51-07 | Tampering | `DoorSubjectType` ↔ `CHECK` SQL | mitigate | D-51-13: nessuno dei due insiemi si tocca, quindi lo specchio resta intero; il commento dichiara il valore storico |
| T-51-08 | Denial of Service | un ospite valido respinto | mitigate | i rami `ticket` e `guest` non si toccano; l'unico percorso rimosso e' quello che nessun ospite puo' piu' presentare (nessuno ha una card) |
| T-51-09 | Information disclosure | log del passo v6 | mitigate | una riga sola, categoria + conteggio, **nessun identificativo di persona**; nessuna superficie (D-51-11) |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato in questa fase; `package.json` non cambia |
</threat_model>

<verification>
- `npm run build` verde dopo ogni task
- `npm run verify:scan-legibility` invariato (questo piano non tocca `ScanFlash.tsx`)
- Nessuna migration nel diff
</verification>

<success_criteria>
MEM-03 lato client: nessun percorso mette in coda, verifica o scansiona un
codice socio; la coda scarta solo il terzo tipo e lo dichiara una volta.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-02-SUMMARY.md` when done
</output>
