---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md
autonomous: true
requirements: [MEM-01, MEM-02]

must_haves:
  truths:
    - "Prima che esista un file di migration, si sa dal CATALOGO quali vincoli, funzioni, policy e indici nominano davvero il ruolo, il codice socio e il registro degli atti (S7)"
    - "La cascata di public.attendances e' enumerata da pg_constraint, non ricordata da un documento (D-51-04)"
    - "Le righe di attendances sono contate per provenienza — party_id valorizzato contro party_id nullo — su laboratorio E produzione, in sola lettura"
  artifacts:
    - path: ".planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md"
      provides: "cio' che il catalogo dice, con la query accanto a ogni numero e la data della lettura"
      contains: "pg_constraint"
      min_lines: 80
  key_links:
    - from: "51-CATALOG.md"
      to: "51-08-PLAN.md e 51-12-PLAN.md"
      via: "gli oggetti veri che le due migration devono toccare"
      pattern: "pg_proc"
    - from: "51-CATALOG.md"
      to: "51-AUTHORISATION.md (piano 51-13)"
      via: "i conteggi da rimettere davanti al proprietario, ripresi il giorno dell'atto"
      pattern: "attendances"
---

<objective>
Interrogare il catalogo — laboratorio per lo schema, laboratorio **e** produzione
per i conteggi — prima che esista una riga di migration.

Purpose: la fase 50 ha pagato questa lezione e l'ha scritta dentro il proprio
file di migration: *«un elenco letto dai file trova cio' che una migration ha
scritto; solo il catalogo trova cio' che c'e'»*. Il suo elenco diceva quattro
funzioni; il catalogo ne ha date sei, e una era `plpgsql`. Gli elenchi di
`51-RESEARCH.md` §4.2, §4.4 e §4.5 sono letti dai file: qui si misura.
Output: `51-CATALOG.md`, con la query accanto a ogni numero e la data della
lettura.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md
@./CLAUDE.md
@.claude/rules/supabase-data.md
@.claude/rules/ai-engineering.md

<interfaces>
Accesso, e la regola che lo governa:
- lettura: `POST /v1/projects/{ref}/database/query` con `SUPABASE_ACCESS_TOKEN` — **solo SELECT**, mai una migration
- scrittura di schema: `POST /v1/projects/{ref}/database/migrations` — non usato in questo piano
- produzione: `cjsfocnhfzycbbgkwocx` (gia' pubblico: viaggia nel bundle dentro `NEXT_PUBLIC_SUPABASE_URL`)
- laboratorio: `$LAB_PROJECT_REF` da `.env.lab.local` — **il valore non si scrive mai in `.planning/`**
Questo piano **non scrive una sola riga** in nessuno dei due progetti.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cosa nomina il ruolo — vincoli, funzioni, policy, DEFAULT</name>
  <files>.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:366-392` — l'elenco letto dai file: due `CHECK` vivi (`profiles_role_check`, `role_capabilities_role_check`), due storici, il `DEFAULT 'member'` mai rimosso, e l'assunzione A5 («nessuna funzione viva contiene il letterale `'member'` fuori da `handle_new_user`»)
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:94-112` — il paragrafo che dichiara la regola: il catalogo, non i file
    - `supabase/migrations/20260921120000_drop_status_and_referral.sql:460-470` — la forma della query sulle funzioni, e la nota che i commenti vanno spogliati prima della regex o si raccolgono falsi positivi (D-50-02)
    - `scripts/dev-lab.sh:1-23` — come si caricano `.env.local` e poi `.env.lab.local`
  </read_first>
  <action>
    Contro il **laboratorio**, in sola lettura, interrogare e trascrivere:
    (a) `pg_constraint` per i vincoli `CHECK` che nominano `'member'` —
    `conrelid::regclass`, `conname`, `pg_get_constraintdef(oid)` — su
    `public.profiles` e `private.role_capabilities` e ovunque altro compaiano;
    (b) `pg_proc` con `pg_get_functiondef(oid)`, `prokind = 'f'`, corpo che
    contiene il letterale `'member'`, **spogliato dei commenti prima del
    confronto**, con schema, nome, firma, `prosecdef` e `proconfig`;
    (c) `pg_policies` il cui `coalesce(qual,'') || coalesce(with_check,'')`
    contiene `member`, cercando su tabella **e** predicato — non su un nome di
    funzione, che era gia' cieco prima;
    (d) il `DEFAULT` corrente di `public.profiles.role` e di ogni altra colonna
    con default `'member'` (`information_schema.columns`);
    (e) il conteggio delle righe per valore di ruolo in `public.profiles` e in
    `private.role_capabilities`.
    Scrivere in `51-CATALOG.md` una sezione per ogni lettera, **con la query
    accanto al risultato** e l'ora UTC. Dove il numero vero diverge dall'elenco
    di `51-RESEARCH.md`, scriverlo come divergenza e dire quale vince: il
    catalogo. Nessun ref di laboratorio nel file.
  </action>
  <verify>
    <automated>grep -q "pg_constraint" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md &amp;&amp; grep -q "pg_proc" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md &amp;&amp; test $(/usr/bin/grep -c "mgkkbd" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Le cinque letture (a)-(e) sono nel file, ognuna con la propria query e l'ora UTC
    - I due `CHECK` vivi sono nominati con `conname` e `pg_get_constraintdef` per esteso
    - L'assunzione A5 e' confermata **o smentita** con l'elenco delle funzioni trovate
    - Zero occorrenze del ref di laboratorio o di una chiave nel file
  </acceptance_criteria>
  <done>Si sa quali oggetti la migration del ruolo deve toccare, perche' il catalogo li ha nominati.</done>
</task>

<task type="auto">
  <name>Task 2: Cosa nomina il codice socio e il registro degli atti</name>
  <files>.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:439-477` — l'elenco letto dai file: `record_membership_act` (plpgsql, dipendenza NON registrata), `handle_new_user`, `profiles_membership_code_key`, e i satelliti di `membership_acts` (due CHECK, due indici, una policy, i privilegi, il COMMENT)
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-PATTERNS.md:924-943` — S6 (un `ALTER TABLE` non muove i suoi satelliti) e S7 (prima di scrivere una migration si interroga il catalogo)
    - `supabase/migrations/20260808002000_membership_register.sql:195-300` — la tabella, `subject_label NOT NULL` e il suo commento
    - `supabase/migrations/20260808005000_membership_acts_append_only.sql:135-150` — `REVOKE`/`GRANT` e il `COMMENT ON TABLE`
  </read_first>
  <action>
    Contro il laboratorio, in sola lettura, interrogare e trascrivere:
    (a) `pg_proc` + `pg_get_functiondef` per ogni funzione il cui corpo nomina
    `membership_code`, `membership_acts` o `record_membership_act` — con schema,
    firma completa (`pg_get_function_identity_arguments`), `prosecdef`,
    `proconfig`, e chi chiama chi;
    (b) `pg_constraint` su `public.membership_acts` e su `public.attendances`:
    nome, tipo, definizione;
    (c) `pg_indexes` su entrambe;
    (d) `pg_policies` su entrambe: `policyname`, `cmd`, `qual`, `with_check`;
    (e) i privilegi su `public.membership_acts`
    (`information_schema.role_table_grants`), che dopo un `RENAME` vanno
    **riletti** e non dedotti;
    (f) il vincolo `UNIQUE` sulla colonna `profiles.membership_code` e ogni altro
    oggetto che la nomina (`pg_attribute` + `pg_constraint` + `pg_depend`).
    Scrivere ogni risultato in `51-CATALOG.md` con la query accanto. Dichiarare
    esplicitamente quante funzioni sono state trovate contro le quante l'elenco
    dei file prevedeva: e' il numero che la fase 50 ha sbagliato, ed e' il numero
    che rompe `createAccount` se e' sbagliato di nuovo.
  </action>
  <verify>
    <automated>grep -q "pg_policies" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md &amp;&amp; grep -q "record_membership_act" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md &amp;&amp; grep -q "role_table_grants" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md</automated>
  </verify>
  <acceptance_criteria>
    - Le sei letture (a)-(f) sono nel file con la propria query
    - Ogni funzione trovata porta firma completa, `prosecdef` e `proconfig` — cio' che serve per ridefinirla senza perdere ACL e `search_path`
    - Il file dichiara il **numero trovato** contro il numero previsto, e nomina la divergenza se c'e'
  </acceptance_criteria>
  <done>La migration 2 potra' essere scritta sugli oggetti che esistono, non su quelli che un documento ricorda.</done>
</task>

<task type="auto">
  <name>Task 3: Le righe di attendances — conteggio per provenienza e cascata, su laboratorio e produzione</name>
  <files>.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:308-352` — lo schema di `attendances`, come distinguere le righe (`party_id IS NOT NULL` = scritta da una scansione socio), la cascata misurata vuota dai file, e i sette passi della forma dell'atto
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md` — D-51-04 e D-51-14 (la tabella si svuota e si toglie), e la seconda domanda che le righe `party_id IS NULL` pongono al proprietario
    - `.claude/rules/ai-engineering.md` — conteggio prima, istantanea, rimozione per chiave, contatore da fonte diversa, autorizzazione come atto
    - `.planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md:70-95` — §1.0: la forma di un conteggio messo davanti al proprietario **prima** della domanda, e la dichiarazione che una decisione senza soggetti non si esegue
  </read_first>
  <action>
    In sola lettura, su **entrambi** i progetti — laboratorio e produzione —
    eseguire e trascrivere:
    (a) `select count(*) filter (where party_id is not null) as da_porta,
    count(*) filter (where party_id is null) as pre_porta,
    count(*) as totale from public.attendances;`
    (b) l'enumerazione della cascata **dal catalogo vivo**:
    `pg_constraint` con `confrelid = 'public.attendances'::regclass` — cioe' chi
    pende da `attendances` — e, separatamente, `conrelid = 'public.attendances'::regclass`
    — da cosa pende lei. La ricerca dice che il primo insieme e' vuoto: qui si
    misura, perche' un documento letto dai file trova cio' che una migration ha
    scritto;
    (c) l'elenco delle tabelle raggiungibili che l'istantanea del piano 51-12
    dovra' coprire, derivato da (b) e non da memoria;
    (d) per la produzione soltanto: il totale righe per tabella sulle tabelle
    raggiungibili, che e' la base dell'istantanea e del contatore «prima».
    Scrivere i risultati in `51-CATALOG.md` in una sezione «i conteggi, e da
    dove vengono», con l'ora UTC di ciascuno e **le due popolazioni tenute
    separate**: `party_id IS NOT NULL` e' cio' che D-51-04 autorizza a
    cancellare; `party_id IS NULL` e' un residuo pre-porta che D-51-04 **non
    nomina** e che va davanti al proprietario come seconda domanda, con il
    numero vero. Se uno dei due numeri e' zero, dirlo: una decisione senza
    soggetti non si esegue, e la fase 50 lo ha gia' dichiarato invece di
    lasciarlo scoprire a meta' runbook.
    Chiudere la sezione dichiarando che i conteggi **si riprendono il giorno
    dell'atto**: questi sono per scrivere la domanda, non per eseguirla.
  </action>
  <verify>
    <automated>grep -q "da_porta" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md &amp;&amp; grep -q "confrelid" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md &amp;&amp; grep -qi "produzione" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md</automated>
  </verify>
  <acceptance_criteria>
    - I conteggi compaiono per **entrambi** i progetti, con l'ora UTC, e le due popolazioni sono separate
    - La cascata e' enumerata da `pg_constraint` con la query accanto, non citata da `51-RESEARCH.md`
    - L'elenco delle tabelle da coprire con l'istantanea e' derivato dalla lettura
    - Il file dichiara che i conteggi si riprendono il giorno dell'atto
    - Nessuna scrittura e' stata eseguita su nessuno dei due progetti (dichiarato nel SUMMARY)
  </acceptance_criteria>
  <done>I numeri che la domanda al proprietario dovra' portare esistono, e vengono dal catalogo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → produzione | qui si legge soltanto, e la distinzione va asserita: una query di lettura passa da `/database/query`, una migration no |
| documento di fase → migration | un elenco letto dai file che guida un `DROP` e' il modo in cui una funzione `plpgsql` si rompe in silenzio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-18 | Tampering | produzione | mitigate | solo `SELECT`; nessun `INSERT`/`UPDATE`/`DELETE`/DDL; l'endpoint `migrations` non viene usato in questo piano e il SUMMARY lo dichiara |
| T-51-19 | Denial of Service | `record_membership_act` / `createAccount` | mitigate | la dipendenza `plpgsql` non registrata si trova **qui**, prima che una migration la rompa; il numero di funzioni trovato si confronta con quello previsto |
| T-51-20 | Tampering | cascata sottostimata | mitigate | enumerata da `pg_constraint` in entrambe le direzioni, non dal documento di ricerca |
| T-51-21 | Information disclosure | `.planning/` pubblico | mitigate | nel file entrano numeri e nomi di oggetti di schema; **mai** il ref di laboratorio, mai una chiave, mai una riga di dati o un identificativo di persona |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- `51-CATALOG.md` esiste, con una query accanto a ogni numero e l'ora UTC
- Zero scritture su entrambi i progetti, dichiarato
- Le divergenze fra catalogo ed elenchi letti dai file sono nominate, e vince il catalogo
</verification>

<success_criteria>
Le due migration di questa fase possono essere scritte su oggetti misurati, e la
domanda al proprietario potra' portare numeri veri invece di `N`.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-05-SUMMARY.md` when done
</output>
