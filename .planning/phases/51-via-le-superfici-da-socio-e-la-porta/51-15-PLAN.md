---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 15
type: execute
wave: 3
depends_on: ["51-02", "51-07"]
files_modified:
  - src/app/(admin)/admin/scanner/ScannerClient.tsx
autonomous: true
requirements: [MEM-04]
gap_closure: true

must_haves:
  truths:
    - "A radio spenta, il pulsante «Check in» accanto a un invitato di guest list lo AMMETTE e lo mette in coda: schermo verde, pastiglia dei pendenti che sale (P-51-1 passo 5, D-51-10)"
    - "Alla riaccensione della rete la voce si drena per la strada che esiste gia' — `sync-manager.ts` caso `guest`, POST /api/tickets/attendance con `guestListEntryId` — e la riga risulta `checked_in`"
    - "Una seconda pressione sullo stesso invitato a radio spenta non rifiuta e non conta due volte: mostra il fatto (ora + dispositivo), non il nome (D-51-05)"
    - "Il percorso online non cambia di una riga: con la rete il pulsante fa quello che faceva"
  artifacts:
    - path: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      provides: "handleGuestCheckIn con il ramo offline, costruito su checkInLocally(partyId, 'guest_list_entry', id, { token: null, name })"
      contains: "guest_list_entry"
  key_links:
    - from: "src/app/(admin)/admin/scanner/ScannerClient.tsx"
      to: "src/lib/offline/checkin-store.ts"
      via: "checkInLocally con subjectType guest_list_entry, che la mappa QUEUE_TYPE_BY_SUBJECT traduce in una voce `guest` della coda"
      pattern: "checkInLocally"
    - from: "src/lib/offline/sync-manager.ts"
      to: "src/app/api/tickets/attendance/route.ts"
      via: "il caso `guest` gia' esistente, che manda guestListEntryId e accetta 200 e 409"
      pattern: "case \"guest\""
---

> **Piano di chiusura, scritto il 2026-09-22 durante l'esecuzione della fase.**
> La corsa «prima» di `P-51-1` (`51-ESITI.md`, passo 5) ha misurato che il
> check-in per nome a radio spenta oggi finisce in **«Connection error — The
> guest was not checked in»**, senza accodare nulla; `51-07` ha confermato che
> `handleGuestCheckIn` non ha mai avuto un ramo offline e che nessun piano
> della fase lo assegnava, mentre `51-14` lo pretende al passo 5 della corsa
> «dopo» (*«entra per nome, dalla lista, sempre a radio spenta → accettato,
> entra in coda»*). Un piano di verifica che misura un comportamento che nessun
> piano costruisce troverebbe il «dopo» identico al «prima». Questo piano
> chiude quel buco, e lo chiude con il minimo: la coda, il tipo `guest` e il
> drenaggio esistono gia' (`51-02`, D-51-11); manca il solo ramo che li usa.

<objective>
Dare al pulsante «Check in» accanto a un invitato di guest list il ramo
offline che il biglietto ha da sempre: a radio spenta si ammette, si accoda, si
mostra il verde; la rete, quando torna, drena per la strada gia' scritta.

Purpose: alle due di notte l'invitato senza email e' l'unico soggetto che
dipende dalla lista scaricata (D-51-10). Rifiutarlo davanti a una fila per un
errore di rete e' esattamente l'asimmetria che `checkin-offline.md` vieta.
Output: `handleGuestCheckIn` con due rami — online invariato, offline che
accoda — e la corsa «dopo» di `P-51-1` che al passo 5 ha qualcosa da misurare.
</objective>

<context>
Cosa esiste gia', e va USATO, non rifatto:

- `src/lib/offline/checkin-store.ts` — `checkInLocally(partyId, subjectType, subjectId, { token, name })`
  accetta `subjectType = "guest_list_entry"` (`QueueableSubjectType`) e lo
  mappa in una voce `guest` della coda (`QUEUE_TYPE_BY_SUBJECT`). Restituisce
  `wasCached`, `alreadyRecorded` e `at`: sono i tre valori che decidono lo
  schermo. `token` e' `null` per costruzione: un invitato per nome non ha una
  firma da portare.
- `src/lib/offline/sync-manager.ts` — `case "guest"` manda
  `POST /api/tickets/attendance` con `guestListEntryId` e tratta 200 e 409
  come successo (`guestLegacySuccess`). Non si tocca.
- `ScannerClient.tsx` — `ticketOffline(code, partyId)` e' il modello del ramo:
  `checkInLocally` → `showFlash` → `addScanRecord` → `refreshQueueCounts`.
  `handleGuestCheckIn(guestListEntryId)` oggi fa solo `fetch` e, se la
  richiesta non parte, finisce in `scanner:guest_checkin_unreachable` con lo
  schermo rosso.
- `51-07` ha riscritto `showFlash(type, title, subtitle)`: titolo = esito,
  sottotitolo = tipo + provenienza tramite `ticketKindLabel`. Il ramo nuovo
  usa **quella** convenzione: mai il nome nel titolo.

Cosa NON e' in perimetro: la lista scaricata (gia' contiene gli invitati senza
email, `P-51-1` passo 1), l'avviso della guest list (51-07), il drenaggio
(51-02), qualunque rotta.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Il ramo offline di handleGuestCheckIn</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `src/app/(admin)/admin/scanner/ScannerClient.tsx` — `handleGuestCheckIn` per intero, `ticketOffline` per intero (il modello), le chiamate a `showFlash` riscritte da 51-07 e `ticketKindLabel`
    - `src/lib/offline/checkin-store.ts:1005-1110` — `findBySubject`, `checkInLocally`, `markCheckedInLocally` e i loro tipi
    - `src/lib/offline/sync-manager.ts:375-415` — il caso `guest`, per non duplicarlo
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-ESITI.md` — passo 5 della corsa «prima»: cio' che oggi si vede
    - `.claude/rules/checkin-offline.md` — l'asimmetria del rifiuto
  </read_first>
  <action>
    In `handleGuestCheckIn`, PRIMA del `fetch`: se `!navigator.onLine`, prendere
    il ramo offline. Il ramo offline chiama
    `checkInLocally(selectedPartyId, "guest_list_entry", guestListEntryId, { token: null, name })`
    dove `name` viene dalla riga della lista (l'invitato e' per definizione
    nella lista scaricata: e' da li' che si e' premuto il pulsante). Poi:
    - se `alreadyRecorded` e' `false`: `showFlash("success", <esito>, <tipo · Offline>)`
      nella convenzione di 51-07 (titolo = esito, sottotitolo = «Guest List · Offline»,
      nessun nome), `addScanRecord({ id, type: "guest", name, ticketType: "Guest List", status: "success", canUndo: true })`,
      aggiornare la riga nella lista locale come entrata (stesso meccanismo del
      biglietto offline), `await refreshQueueCounts()` cosi' la pastiglia
      «Pending» sale subito;
    - se `alreadyRecorded` e' `true`: `showFlash("already_recorded", <esito>, recordedFact(at, THIS_DEVICE_LABEL))`
      e `addScanRecord` con `status: "already_recorded"` e `canUndo: false`.
      Non e' un rifiuto e non conta due volte.
    Il `catch` esistente (`scanner:guest_checkin_unreachable`) resta per il caso
    in cui `navigator.onLine` dice `true` ma la richiesta non parte lo stesso:
    in quel caso, invece del solo schermo rosso, prendere lo STESSO ramo
    offline — la rete che mente e' il caso reale della porta — e loggare con
    una categoria distinta (`scanner:guest_checkin_queued_after_unreachable`),
    perche' i due eventi vanno distinti nei log (meta-gates, zero fallimenti
    silenziosi). Il ramo online resta byte per byte com'e'.
    Il commento sopra `handleGuestCheckIn` che dice *«Online only — it has no
    offline branch and never had one»* va riscritto, perche' da questo commit e'
    falso: descrivere i due rami e la ragione (asimmetria del rifiuto).
  </action>
  <verify>
    <automated>cd /Users/etiesse/Resonate && grep -c '"guest_list_entry"' 'src/app/(admin)/admin/scanner/ScannerClient.tsx' | grep -qvx 0 &amp;&amp; ! grep -q "never had one" 'src/app/(admin)/admin/scanner/ScannerClient.tsx' &amp;&amp; grep -q "guest_checkin_queued_after_unreachable" 'src/app/(admin)/admin/scanner/ScannerClient.tsx' &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `handleGuestCheckIn` chiama `checkInLocally` con `"guest_list_entry"` quando `navigator.onLine` e' `false` e quando il `fetch` non raggiunge un server
    - Lo schermo verde non porta il nome nel titolo; il sottotitolo dice tipo e «Offline»
    - La seconda pressione mostra `already_recorded` con il fatto, non un rifiuto
    - Il ramo online e' invariato (diff del blocco `if (res.ok)` e `if (res.status === 409)`: zero righe)
    - `npm run build` esce 0
  </acceptance_criteria>
  <done>A radio spenta il pulsante accoda e mostra il verde; la coda porta una voce `guest` che il drenaggio esistente sa spedire.</done>
</task>

<task type="auto">
  <name>Task 2: La prova a tavolino, scritta, di cio' che il telefono misurera'</name>
  <files>src/app/(admin)/admin/scanner/ScannerClient.tsx</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-14-PLAN.md` — il passo 5 e l'8 della corsa «dopo»
  </read_first>
  <action>
    Non esiste un test runner. Scrivere nel SUMMARY, in una sezione «Cosa
    misura la corsa dopo», la sequenza attesa al passo 5 e all'8 di `P-51-1` —
    verde con «Guest List · Offline», «Pending (n)» che sale, alla riaccensione
    la riga `63ebd88f-…` in `guest_list_entries` che passa a `checked_in` con
    `checked_in_by` valorizzato — e cio' che invece NON e' verificabile qui:
    il drenaggio reale della voce `guest` da un telefono, che solo `51-14`
    vede. Nessuna cella di `51-ESITI.md` va toccata da questo piano.
  </action>
  <verify>
    <automated>test -f .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-15-SUMMARY.md &amp;&amp; grep -q "Cosa misura la corsa" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-15-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY dichiara cosa il passo 5 e l'8 della corsa «dopo» devono mostrare, e cosa questo piano non ha potuto verificare
  </acceptance_criteria>
  <done>Chi percorre la corsa «dopo» sa cosa aspettarsi al passo 5 e da dove viene.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Category | Mitigation |
|---|---|---|---|
| T-51-15-01 | Un invitato accodato offline due volte da due telefoni | Repudiation | il server risponde 409 al secondo drenaggio e `guestLegacySuccess` lo tratta come successo: una riga sola, nessun rifiuto |
| T-51-15-02 | Il ramo offline ammette un nome che non e' in lista | Elevation | il pulsante esiste solo accanto a una riga della lista scaricata: non c'e' input libero, l'id viene dalla lista |
| T-51-15-03 | Un errore di rete con `navigator.onLine === true` produce un rosso invece di una coda | Tampering | il `catch` prende lo stesso ramo offline con categoria di log distinta |
</threat_model>

<verification>
`npm run build` esce 0. Il ramo offline si vede solo al telefono: e' il passo 5 della corsa «dopo» di `P-51-1` (piano 51-14), sul laboratorio, e la rilettura dal catalogo della riga `63ebd88f-…` (passo 8).
</verification>

<success_criteria>
- [ ] `handleGuestCheckIn` ha due rami e il ramo online e' invariato
- [ ] La voce accodata e' di tipo `guest` e il drenaggio esistente la spedisce
- [ ] Nessun nome nel titolo dello schermo di esito
- [ ] SUMMARY con la sezione «Cosa misura la corsa dopo»
</success_criteria>
