---
phase: 51-via-le-superfici-da-socio-e-la-porta
plan: 13
type: execute
wave: 6
depends_on: ["51-06", "51-07", "51-09", "51-10", "51-11", "51-12"]
files_modified:
  - .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md
  - .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-ESITI.md
autonomous: false
requirements: [MEM-01, MEM-02, MEM-03]

must_haves:
  truths:
    - "L'autorizzazione a scrivere in produzione e' stata scritta PRIMA che la domanda venisse posta, con i numeri veri e non con N"
    - "Le righe di attendances sono state contate, fotografate e cancellate per chiave, e il conteggio dopo viene da una fonte diversa da quella su cui si e' agito (D-51-04)"
    - "Le due migration sono in produzione nell'ordine dichiarato, dopo il deploy del codice"
    - "L'autorizzazione e' dichiarata ESAURITA con l'ora, e da li' in poi non autorizza piu' niente"
  artifacts:
    - path: ".planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md"
      provides: "il perimetro, i conteggi presi prima della domanda, la seconda domanda sulle righe pre-porta, e il registro d'uso"
      contains: "ESAURITA"
      min_lines: 90
  key_links:
    - from: "51-AUTHORISATION.md"
      to: "scripts/purge-attendances.mjs"
      via: "lo strumento legge il documento e rifiuta se non e' concesso o e' gia' esaurito"
      pattern: "purge-attendances"
    - from: "51-AUTHORISATION.md"
      to: "51-ESITI.md"
      via: "il registro d'uso, con l'ora di ogni passo"
      pattern: "UTC"
---

<objective>
Portare la fase in produzione dentro **un solo atto autorizzato e datato**: il
deploy del codice, le due migration nell'ordine, e la cancellazione delle righe
di presenza per chiave.

Purpose: D-51-04 e D-51-14 sono scritture irreversibili su un database senza
PITR. `ai-engineering.md` dice come si fanno, e la fase 50 ha gia' dimostrato che
scrivere l'autorizzazione **prima** della domanda cambia cosa viene autorizzato:
il suo §1.0 ha scoperto, contando, che la decisione da eseguire non aveva
soggetti.
Output: una produzione allineata al laboratorio, e un documento che dichiara
quando e' stato speso il permesso e quando si e' esaurito.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CONTEXT.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md
@.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md
@.planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md
@./CLAUDE.md
@.claude/rules/ai-engineering.md
@.claude/rules/meta-gates.md
@.claude/rules/supabase-data.md

<interfaces>
Perimetro possibile, e non un byte oltre:
  (a) `git push origin main` → deploy Vercel di produzione
  (b) migration del piano 51-08 — ruolo `attendee` e due chiavi di capability
  (c) cancellazione delle righe di `public.attendances`, per chiave
  (d) migration del piano 51-12 — codice socio, registro rinominato, `attendances` tolta
  (e) la rilettura di controllo, in sola lettura

Produzione: `cjsfocnhfzycbbgkwocx` — gia' pubblico per costruzione, viaggia nel
bundle dentro `NEXT_PUBLIC_SUPABASE_URL`. Il laboratorio non compare in questo
documento.

Le finestre che l'ordine (a)→(d) apre, da dichiarare **prima** e non da scoprire:
- fra (a) e (b): un organizer che creasse un account riceverebbe `23514` — il
  codice scrive gia' `attendee`, il `CHECK` lo ammette solo dopo (b)
- fra (a) e (d): la creazione di un account chiama `record_account_act`, che
  esiste con quel nome solo dopo (d)
Entrambe durano il tempo fra due passi consecutivi dello stesso atto, su un
progetto che oggi non ha serate in vendita.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere l'autorizzazione PRIMA di chiedere, con i numeri veri</name>
  <files>.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md</files>
  <read_first>
    - `.planning/phases/50-via-le-iscrizioni/50-AUTHORISATION.md` per intero — frontmatter, il riquadro «scritto PRIMA che la domanda venisse posta», §1 (il perimetro alla lettera), §1.0 (il conteggio che ha tolto un passo dal perimetro), il registro d'uso e la chiusura
    - `.claude/rules/ai-engineering.md` — l'autorizzazione e' un atto che si consuma una volta e copre esattamente cio' che e' stato descritto quando e' stata chiesta
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-CATALOG.md` — i conteggi gia' presi dal piano 51-05, **che qui si riprendono**: quelli servivano a scrivere la domanda, questi a porla
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-RESEARCH.md:341-352` — i sette passi della forma dell'atto
  </read_first>
  <action>
    Riprendere **oggi**, in sola lettura, i conteggi di produzione: righe di
    `public.attendances` con `party_id` valorizzato e con `party_id` nullo, la
    cascata da `pg_constraint`, il conteggio righe delle tabelle raggiungibili, e
    il numero di profili per ruolo.
    Scrivere `51-AUTHORISATION.md` nella forma di quella della fase 50:
    frontmatter (`phase`, `document`, `written`, `granted`, `granted_by`,
    `scope`, `status`), il riquadro che dichiara che il testo e' stato scritto
    **prima** che la domanda venisse posta, §1 con il perimetro passo per passo —
    (a) il deploy con il numero di commit misurato oggi, (b) e (d) le due
    migration **nominate per file**, (c) la cancellazione con **il numero vero**
    di righe e lo strumento che la esegue — e l'elenco esplicito di **cio' che
    resta fuori perimetro**: ogni altra scrittura, ogni sessione coniata, ogni
    altro campo di configurazione. Per quelle serve un atto nuovo, con la sua
    data.
    Due cose che questo documento deve fare, e non sono formalita':
    1. **La seconda domanda.** Le righe con `party_id IS NULL` sono un residuo
       pre-porta che D-51-04 **non nomina**. Vanno messe davanti al proprietario
       separatamente, con il numero vero, come domanda distinta: si cancellano
       anch'esse, o restano? D-51-14 toglie la tabella, quindi una risposta serve
       — e se il numero e' zero la domanda si chiude da sola e va dichiarato.
    2. **Le due finestre** fra i passi (a)→(b) e (a)→(d), scritte con la loro
       conseguenza: durante la prima una creazione di account riceve `23514`,
       durante la seconda fallisce sul nome della funzione. Chi autorizza deve
       sapere cosa e' temporaneamente rotto mentre l'atto e' in corso.
    Dichiarare infine lo stato dei gate che restano rossi contro la produzione
    fino a questo atto — `verify:capabilities` e `verify:refusal` — e che questo
    atto e' cio' che li chiude.
    **Non porre ancora la domanda**: questo task scrive, non chiede.
  </action>
  <verify>
    <automated>test -f .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md &amp;&amp; /usr/bin/grep -q "Fuori perimetro" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md &amp;&amp; test $(/usr/bin/grep -c "party_id" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md) -ge 2 &amp;&amp; test $(/usr/bin/grep -c "mgkkbd" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Il documento esiste con il frontmatter completo e `status` non ancora concesso
    - Il perimetro nomina i cinque passi, i due file di migration e lo strumento della cancellazione
    - I conteggi sono **di oggi**, con l'ora UTC, e le due popolazioni sono separate
    - La seconda domanda sulle righe `party_id IS NULL` e' posta esplicitamente, con il numero vero
    - Le due finestre e la loro conseguenza sono scritte
    - Nessun ref di laboratorio, nessuna chiave, nessun nome di persona
  </acceptance_criteria>
  <done>La domanda che sara' posta ha un perimetro scritto e dei numeri veri accanto.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Il proprietario autorizza — o non autorizza</name>
  <files>.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md` — il documento scritto dal task 1, con il perimetro e i numeri di oggi
    - `.claude/rules/ai-engineering.md` — l'autorizzazione e' un atto che si consuma una volta
  </read_first>
  <action>
    Porre la domanda al proprietario mettendogli davanti **i numeri veri** —
    quante righe, su quali tabelle, quali file di migration, quante finestre e
    quanto durano — e le quattro opzioni qui sotto, senza raccomandarne una come
    se fosse gia' decisa. Registrare la risposta **letterale** nel frontmatter di
    `51-AUTHORISATION.md` (`granted`, `granted_by`, `answer`) e la risposta alla
    seconda domanda nel corpo. Nessun passo si esegue prima che questa
    registrazione esista.
  </action>
  <decision>Se eseguire oggi i cinque passi di `51-AUTHORISATION.md` sulla produzione, e cosa fare delle righe di presenza pre-porta</decision>
  <context>
    La fase e' pronta e provata sul laboratorio: due migration applicate li',
    la cancellazione per chiave gia' eseguita una volta, i gate verdi contro il
    laboratorio. In produzione restano da fare cinque passi, due dei quali sono
    **irreversibili** e questo progetto **non ha PITR**: la cancellazione delle
    righe di presenza e i due `DROP` (la colonna del codice socio e la tabella
    delle presenze). I numeri veri, presi oggi, sono in
    `51-AUTHORISATION.md` §1. Mentre l'atto e' in corso ci sono due finestre in
    cui la creazione di un account fallisce: sono scritte nel documento con la
    loro causa.
  </context>
  <options>
    <option id="tutto">
      <name>Autorizza tutti e cinque i passi, oggi, nell'ordine</name>
      <pros>La produzione si allinea al laboratorio in un atto solo; i due gate rossi si chiudono; le finestre durano il tempo fra due passi consecutivi</pros>
      <cons>Due scritture irreversibili senza PITR, nello stesso giorno</cons>
    </option>
    <option id="senza-cancellazione">
      <name>Autorizza il deploy e le due migration, NON la cancellazione</name>
      <pros>Niente righe cancellate oggi</pros>
      <cons>Non e' eseguibile: la guardia della migration rifiuta di togliere `public.attendances` se contiene righe. Il passo (d) si fermerebbe, e la fase resterebbe a meta' con una finestra aperta</cons>
    </option>
    <option id="solo-deploy">
      <name>Autorizza solo il deploy, le migration un altro giorno</name>
      <pros>Il codice arriva in produzione senza toccare lo schema</pros>
      <cons>Le due finestre restano **aperte** invece di durare minuti: fino alla migration, creare un account in produzione fallisce. E' l'esito peggiore dei tre</cons>
    </option>
    <option id="niente">
      <name>Non autorizzare oggi</name>
      <pros>Nessuna scrittura</pros>
      <cons>La fase resta chiusa sul laboratorio; `P-51-1` nella sua corsa «dopo» si puo' comunque percorrere li'</cons>
    </option>
  </options>
  <resume-signal>Scegli: `tutto`, `senza-cancellazione`, `solo-deploy` o `niente` — e rispondi alla seconda domanda sulle righe pre-porta</resume-signal>
  <verify>
    <automated>/usr/bin/grep -q "granted:" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md &amp;&amp; /usr/bin/grep -q "answer:" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Il frontmatter porta `granted`, `granted_by` e `answer` con la risposta letterale del proprietario
    - La risposta alla seconda domanda e' registrata nel documento, non riassunta
    - Se la risposta e' `niente` o `solo-deploy`, il task 3 esegue **solo** cio' che e' stato concesso e lo dichiara
  </acceptance_criteria>
  <done>Esiste un permesso datato, con un perimetro, e una risposta alla domanda sulle righe pre-porta.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Spendere l'autorizzazione, nell'ordine, e dichiararla esaurita</name>
  <files>.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md, .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-ESITI.md</files>
  <read_first>
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md` — il perimetro concesso, e **solo** quello
    - `.planning/STATE.md` — le condizioni che questo repo misura prima di una spedizione: niente file privati nel diff, zero ref di laboratorio, zero segreti, un giorno senza serata, produzione inerte
    - `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-12-SUMMARY.md` — la stessa sequenza, gia' percorsa sul laboratorio, con i suoi numeri
    - `scripts/purge-attendances.mjs` — lo strumento, e cosa pretende per accettare il ref di produzione
  </read_first>
  <action>
    Eseguire i passi concessi, **in questo ordine**, registrando l'ora UTC di
    ciascuno in `51-ESITI.md` mentre accadono:
    (a) **Precondizioni della spedizione, misurate e non ricordate:** zero file
    di `docs/` e `.firecrawl/` nel diff, zero `.env*`, zero ref di laboratorio,
    zero segreti; nessuna serata nella finestra ieri/oggi/domani; produzione
    inerte (zero serate in vendita, zero scansioni in corso). Poi
    `git push origin main` e il deploy Vercel, atteso fino a stato `success`.
    (b) Applicare la migration del piano 51-08 dall'endpoint `migrations`, e
    rileggere dal catalogo i due `CHECK`, il `DEFAULT` e il conteggio righe per
    ruolo.
    (c) Lanciare `scripts/purge-attendances.mjs` **prima con `--dry-run`**,
    mettere i numeri davanti al proprietario se differiscono da quelli scritti
    nell'autorizzazione, poi eseguire l'atto: istantanea, cattura degli `id`,
    `DELETE … WHERE id = ANY(<lista>)`, e conteggio dopo **da una fonte diversa**
    da quella su cui si e' agito.
    (d) Applicare la migration del piano 51-12 dall'endpoint `migrations`, e
    rileggere dal catalogo: colonna assente, `account_acts` presente e
    `membership_acts` assente, satelliti rinominati, privilegi riletti, quattro
    funzioni vive, `attendances` inesistente.
    (e) La prova funzionale: creare un account di prova in produzione **solo se
    il perimetro la nomina**; se non la nomina, dirlo e verificare invece che
    `reconcile_master`, che gira a ogni deploy, non abbia riportato errori.
    Poi: `npm run verify:capabilities` e `npm run verify:refusal` contro la
    **produzione**, che da questo momento devono essere verdi — e' l'atto che li
    chiude.
    Infine, chiudere l'autorizzazione: `status: ESAURITA` con l'ora esatta,
    quanti passi su quanti, quanti falliti, e la riga che dichiara che **da qui
    in poi questo documento non autorizza piu' niente**. Se un passo non e' stato
    eseguito, dirlo **al suo posto** invece di riscrivere il perimetro: una
    smentita sta nella nota d'uso, non al posto della riga originale.
    **Se un controllo non corrisponde, fermarsi e riportarlo.** Un atto che
    prosegue su un catalogo che dice un'altra cosa e' il caso in cui si smette.
  </action>
  <verify>
    <automated>npm run verify:capabilities &amp;&amp; npm run verify:refusal &amp;&amp; /usr/bin/grep -q "ESAURITA" .planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-AUTHORISATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Ogni passo concesso ha la sua ora UTC in `51-ESITI.md`, e nessun passo fuori perimetro e' stato eseguito
    - La cancellazione e' avvenuta **per chiave**, con istantanea presa prima e conteggio dopo da una fonte diversa
    - Le due migration risultano nella history di produzione, con le versioni registrate scritte
    - `verify:capabilities` e `verify:refusal` verdi **contro la produzione**
    - `51-AUTHORISATION.md` dichiara `ESAURITA` con l'ora, i passi eseguiti e quelli non eseguiti
  </acceptance_criteria>
  <done>La produzione e' allineata, i due gate rossi sono chiusi, e il permesso e' speso e dichiarato tale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chi esegue → produzione | scritture irreversibili su un database senza PITR |
| permesso → perimetro | «mi autorizzi ad applicare la fase?» non e' un perimetro, e' una delega |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-57 | Tampering | `DELETE` su `public.attendances` | mitigate | per chiave su lista catturata, dopo istantanea; conteggio dopo da fonte diversa; lo strumento rifiuta il ref di produzione senza autorizzazione valida e non esaurita |
| T-51-58 | Repudiation | autorizzazione riscritta a posteriori | mitigate | il testo e' scritto prima della domanda; le smentite stanno nella nota d'uso, mai al posto della riga originale; `status: ESAURITA` con l'ora |
| T-51-59 | Denial of Service | finestra fra deploy e migration | accept, dichiarata | le due finestre e la loro conseguenza sono scritte nell'autorizzazione; i passi sono consecutivi nello stesso atto; nessuna serata in vendita |
| T-51-60 | Information disclosure | spedizione che porta materiale privato su un repo pubblico | mitigate | precondizioni misurate prima del push: zero `docs/`, zero `.firecrawl/`, zero `.env*`, zero ref di laboratorio, zero segreti |
| T-51-61 | Denial of Service | `reconcile_master` rotta al primo deploy | mitigate | e' ridefinita nella migration del piano 51-12 e riletta dal catalogo; il passo (e) ne controlla l'esito |
| T-51-62 | Tampering | un passo fuori perimetro eseguito perche' «gia' che ci siamo» | mitigate | il perimetro elenca cio' che resta fuori, esplicitamente; per quello serve un atto nuovo con la sua data |
| T-51-SC | Tampering | npm install | accept | nessun pacchetto installato; `package.json` non cambia |
</threat_model>

<verification>
- Catalogo di produzione: quattro valori di ruolo con `attendee`, 15 chiavi di capability, colonna del codice socio assente, `account_acts` presente, `attendances` inesistente
- `verify:capabilities` e `verify:refusal` verdi contro la produzione
- `51-AUTHORISATION.md` `ESAURITA`, con il registro d'uso
</verification>

<success_criteria>
La produzione e' allineata al laboratorio dentro un solo atto datato, e nessuna
riga e' stata cancellata senza essere prima contata e fotografata.
</success_criteria>

<output>
Create `.planning/phases/51-via-le-superfici-da-socio-e-la-porta/51-13-SUMMARY.md` when done
</output>
