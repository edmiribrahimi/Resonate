---
phase: 37-manual-venue-reveal
plan: 03
type: execute
wave: 2
depends_on: ["37-01", "37-02"]
files_modified:
  - src/types/database.ts
autonomous: false
requirements: [VENUE-01, VENUE-02]

must_haves:
  truths:
    - "Le due migration sono applicate in produzione, in ordine di timestamp, e la history del progetto le registra"
    - "Il catalogo TypeScript delle capability e le righe nel database concordano: verify:capabilities e' verde"
    - "src/types/database.ts nomina venue_revealed_at, venue_reveal_acts e le due funzioni nuove"
    - "Nessuna riga di dati e' stata creata, modificata o rimossa da questo piano"
  artifacts:
    - path: "src/types/database.ts"
      provides: "I tipi allineati allo schema vivo, nello stesso commit del cambiamento"
      contains: "venue_revealed_at"
  key_links:
    - from: "supabase/migrations/20260810160000_manual_venue_reveal.sql"
      to: "il database di produzione"
      via: "endpoint migrations della Management API, non /database/query"
      pattern: "record_venue_reveal_act"
---

<objective>
Applicare le due migration in produzione, allineare i tipi e far tornare verde
l'anello che nessun compilatore tiene.

Purpose: **`npm run build` passa senza la push.** I tipi TypeScript vengono da un
file committato, non dal database vivo, e nessuno dei quattro client Supabase e'
parametrizzato con `Database` — quindi nessun nome di colonna in nessuna query e'
controllato dal build. Un verde prima della push e' uno stato di verifica
falsamente positivo, e l'unico controllo che se ne accorge e'
`npm run verify:capabilities`, che ha bisogno di un database vivo.

Ogni piano da qui in poi scrive codice contro colonne e funzioni che devono
esistere. **Questo passo e' `[BLOCKING]`.**

Output: due versioni nella history delle migration, i tipi allineati, e un
`verify:capabilities` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-manual-venue-reveal/37-01-SUMMARY.md
@.planning/phases/37-manual-venue-reveal/37-02-SUMMARY.md

<interfaces>
Come si applica una migration in questo progetto, misurato e registrato in
`.planning/STATE.md:219-224`:

  POST /v1/projects/{ref}/database/migrations   con SUPABASE_ACCESS_TOKEN

**L'endpoint migrations, NON `/database/query`**, o la history del progetto
smette di essere veritiera. La CLI `supabase` **non e' installata su questa
macchina** (registrato il 2026-08-06 e di nuovo il 2026-08-10): se
`supabase db push` non esiste, non e' un ostacolo da aggirare, e' la strada gia'
documentata da prendere.

Deriva preesistente, da non "riparare" per iniziativa propria: alcune migration
applicate in produzione mancano dalla history. Ripararle **e' una chiamata del
proprietario**, e `PUT` sullo stesso endpoint fa upsert **senza applicare**.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Autorizzazione a scrivere in produzione, e l'istantanea prima</name>
  <what-built>
    Due file di migration, scritti e non applicati: `20260810160000_manual_venue_reveal.sql`
    (chiave, colonna, traccia, scrittore) e `20260810161000_venues_read_narrowed.sql`
    (revoca della lettura anonima di `public.venues`, funzione di titolo).
  </what-built>
  <how-to-verify>
    Prima di autorizzare, l'esecutore presenta e il proprietario legge:

    1. **Cosa cambia per chi guarda oggi.** La seconda migration **toglie ad
       `anon` ogni lettura di `public.venues`**. Da quel momento in poi le due
       pagine pubbliche degli eventi dipendono dalla funzione nuova, e i piani
       che le riscrivono (37-05 e 37-06) **non sono ancora eseguiti**. Fra questa
       push e quei due piani, la lista eventi e il dettaglio serata mostrano il
       nome del locale **vuoto** anche per le serate non segrete.
       **E' una regressione temporanea, visibile, sul negozio.** Va detta prima,
       non scoperta dopo, e il proprietario decide se accettarla o se questo
       piano attende che 37-05 e 37-06 siano pronti al commit successivo.
    2. **Cosa non cambia:** nessuna riga di dati viene creata, modificata o
       rimossa. Le uniche scritture sono DDL.
    3. **L'istantanea, in sola lettura, con la service key:** conteggi e hash per
       tabella su `venues`, `event_parties`, `events`, `tickets`, `rsvps`. E'
       il punto di confronto per il piano 37-13, e si prende **adesso**, prima
       del cambiamento: un baseline preso dopo non e' un baseline.
    4. **Non esiste PITR** su questo database (decisione del proprietario,
       `STATE.md` blocco D12). Non c'e' un annulla.

    L'autorizzazione **si consuma una volta** e copre esattamente cio' che e'
    stato descritto qui. L'esecutore dichiara nel SUMMARY quando l'ha usata e
    quando l'ha esaurita.
  </how-to-verify>
  <action>
    Presentare al proprietario i quattro punti sopra — la regressione temporanea
    sulle pagine pubbliche, l'assenza di scritture di dati, l'istantanea in sola
    lettura e l'assenza di PITR — e **attendere**. Prendere l'istantanea **prima**
    di chiedere, cosi' che la risposta non ne ritardi la cattura. Non inviare
    alcuna migration prima della risposta.
  </action>
  <resume-signal>Scrivere "autorizzato" per applicare, oppure "attendi 37-05 e 37-06" per rimandare la push al commit successivo</resume-signal>
  <read_first>
    - `.claude/rules/ai-engineering.md` — i quattro gate scritti dopo l'incidente della fase 36
    - `.planning/STATE.md:159-165` — i blocchi D7 e D12
    - `.planning/phases/37-manual-venue-reveal/37-RESEARCH.md` § Runtime State Inventory (righe 1369-1382)
  </read_first>
  <acceptance_criteria>
    - L'istantanea e' presa **prima** di qualunque `POST`, e i suoi numeri sono nel SUMMARY
    - Ogni comando dell'istantanea e' una lettura: nessun `INSERT`, `UPDATE`, `DELETE`, nessun controllo di cancellazione premuto
    - La risposta del proprietario e' riportata verbatim nel SUMMARY
  </acceptance_criteria>
  <done>Autorizzazione data o rimandata, e l'istantanea esiste.</done>
</task>

<task type="auto">
  <name>Task 2: Applicare le due migration, in ordine</name>
  <files>supabase/migrations/20260810160000_manual_venue_reveal.sql, supabase/migrations/20260810161000_venues_read_narrowed.sql</files>
  <read_first>
    - `.planning/STATE.md:205-224` — come e' stata applicata la migration della fase 31, e la deriva della history da non riparare
    - `.planning/phases/36-formats-series-numbering/36-05-SUMMARY.md` — la corsa gia' eseguita con lo stesso endpoint, versione `20260810144239`
    - I due file di migration, riletti per intero prima di inviarli
  </read_first>
  <action>
    Applicare **prima** `20260810160000_manual_venue_reveal.sql` e **poi**
    `20260810161000_venues_read_narrowed.sql`: la seconda legge
    `event_parties.venue_revealed_at`, che la prima crea, e Postgres rifiuta una
    funzione che nomina una colonna inesistente.

    Provare `supabase db push`. Se la CLI non e' installata — lo e' stato due
    volte finora — usare `POST /v1/projects/{ref}/database/migrations` con
    `SUPABASE_ACCESS_TOKEN` da `.env.local`, **una chiamata per file**, e
    **mai** `/database/query`: la history deve registrare le due versioni.

    Dopo ciascuna, verificare **dal database e non dalla risposta HTTP** che
    l'oggetto esista: la riga `venue.reveal` in `private.capabilities`, la colonna
    `venue_revealed_at` su `event_parties`, la tabella `public.venue_reveal_acts`
    con RLS attiva e **una** policy, l'assenza di `venues_select_public` fra le
    policy di `public.venues`, e l'esistenza delle tre funzioni nuove. La misura
    non si prende con lo strumento che ha causato l'effetto: la conferma si chiede
    ai cataloghi (`pg_policies`, `information_schema`), non al corpo della
    risposta dell'endpoint.
  </action>
  <acceptance_criteria>
    - Le due versioni compaiono nella history delle migration, nell'ordine dei loro timestamp
    - `SELECT count(*) FROM pg_policies WHERE tablename = 'venues' AND policyname = 'venues_select_public'` restituisce 0
    - `SELECT count(*) FROM pg_policies WHERE tablename = 'venue_reveal_acts'` restituisce 1
    - `SELECT key FROM private.capabilities WHERE key = 'venue.reveal'` restituisce una riga
    - I conteggi delle cinque tabelle dell'istantanea sono **identici** a prima: una DDL non muove righe, e se ne ha mosse va detto immediatamente
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:capabilities</automated>
  </verify>
  <done>Lo schema vivo porta i sei oggetti nuovi e non porta piu' la policy anonima.</done>
</task>

<task type="auto">
  <name>Task 3: I tipi allineati allo schema, nello stesso commit</name>
  <files>src/types/database.ts</files>
  <read_first>
    - `src/types/database.ts` — l'intestazione (`:1-30`, la direzione invertita degli import) e il blocco `event_parties`, piu' un blocco di tabella recente come `membership_acts` da usare come forma
    - `.claude/rules/supabase-data.md` — gate dei tipi allineati nello stesso commit
    - I due file di migration
  </read_first>
  <action>
    Il file **non e' generato**: porta prosa e tre import a direzione invertita, e
    si modifica a mano. Aggiungere:
    `venue_revealed_at: string | null` a `Row`, `Insert` e `Update` di
    `event_parties`; il blocco della tabella `venue_reveal_acts` con le sue
    colonne; e, se il file tipizza le funzioni, le firme di
    `record_venue_reveal_act`, `venue_for_parties` e `party_start_instant`.

    Il vocabolario di `act` — `'revealed' | 're_hidden'` — segue la stessa
    disciplina degli altri tre import invertiti del file: **una sola
    definizione**, esportata da dove i chiamanti TypeScript la leggono (il modulo
    dei tipi della rivelazione, che il piano 37-10 usera'), non re-dichiarata qui
    come coppia di stringhe. Se il tipo non esiste ancora, dichiararlo qui e
    lasciare scritto che il prossimo lettore lo sposti invece di duplicarlo.

    Scrivere accanto a `venue_revealed_at` la stessa distinzione che porta la
    migration: **non e' `venue_reveal_email_sent` con un nome nuovo.**
  </action>
  <acceptance_criteria>
    - `npm run build` esce con 0
    - `grep -c "venue_revealed_at" src/types/database.ts` restituisce almeno 3
    - `grep -c "venue_reveal_acts" src/types/database.ts` restituisce almeno 1
    - Nessuna colonna `address` o `google_maps_url` compare nel blocco `venue_reveal_acts`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run verify:capabilities && npm run verify:routes</automated>
  </verify>
  <done>I tipi dicono quello che il database dice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macchina di sviluppo → database di produzione | `SUPABASE_ACCESS_TOKEN` e service key vivono in `.env.local` e bypassano ogni RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-13 | Tampering | applicazione della DDL con lo strumento sbagliato | mitigate | endpoint migrations, mai `/database/query`; una chiamata per file, in ordine di timestamp |
| T-37-14 | Denial of Service | la finestra fra la push e i piani 37-05/37-06, in cui le pagine pubbliche perdono il nome del locale | mitigate | dichiarata al proprietario **prima** della push, nel checkpoint, con la scelta di rimandare come opzione reale |
| T-37-15 | Repudiation | un'autorizzazione a scrivere in produzione riusata per altro | mitigate | si consuma una volta, copre cio' che e' stato descritto, e chi la riceve dichiara quando l'ha esaurita |
| T-37-SC | Tampering | installazioni npm/pip/cargo | accept | nessun pacchetto nuovo |
</threat_model>

<verification>
- `npm run verify:capabilities` verde — e' l'unico anello che vede il database
- `npm run build` verde
- `npm run verify:routes` verde e **identico** a prima: questo piano non aggiunge rotte
- I conteggi delle cinque tabelle dell'istantanea, riletti dopo, identici a prima
</verification>

<success_criteria>
- Due versioni nella history, nell'ordine giusto
- `verify:capabilities` verde per la prima volta da quando esiste la tredicesima chiave
- Zero righe di dati mosse
</success_criteria>

<output>
Create `.planning/phases/37-manual-venue-reveal/37-03-SUMMARY.md` when done
</output>
