---
phase: 37-manual-venue-reveal
plan: 14
type: execute
wave: 8
gap_closure: true
depends_on: ["37-02", "37-05", "37-06", "37-10", "37-13"]
files_modified:
  - supabase/migrations/20260810161000_venues_read_narrowed.sql
  - src/app/(admin)/admin/events/[id]/reveal/actions.ts
  - src/app/(admin)/admin/events/actions.ts
autonomous: true
requirements: [VENUE-01, VENUE-02]

must_haves:
  truths:
    - "Una serata senza sede collegata non puo' essere rivelata: il rifiuto e' lato server, non nel componente"
    - "Collegare una sede a una serata gia' rivelata non pubblica un indirizzo senza atto"
    - "Il ramo 3 di venue_for_parties applica venue_reveal_on_purchase, come il ramo di pagina che dichiara di rispecchiare"
    - "Su una bozza, chi ha staff.manage ottiene il nome del locale — che gia' legge per un'altra strada"
  artifacts:
    - path: "supabase/migrations/20260810161000_venues_read_narrowed.sql"
      provides: "I rami allineati al predicato di pagina, e is_published spostato dentro i rami"
      contains: "venue_reveal_on_purchase"
    - path: "src/app/(admin)/admin/events/[id]/reveal/actions.ts"
      provides: "La precondizione sulla sede, prima dell'atto"
      contains: "venue_id"
  key_links:
    - from: "src/app/(admin)/admin/events/actions.ts"
      to: "venue_secret_locked"
      via: "venue_id entra nel campo bloccato quando la serata e' rivelata"
      pattern: "venue_secret_locked"
---

<objective>
Chiudere il percorso che pubblica un indirizzo senza lasciare traccia, e riallineare
i rami di `venue_for_parties` al predicato di pagina che dichiarano di rispecchiare.

Purpose: `37-REVIEW.md` ha trovato un buco nella **giuntura** fra 37-02 e 37-10 —
nessuno dei due piani poteva vederlo da solo. La correzione vive nella migration
**non ancora applicata**: oggi costa la modifica di un file, dopo il deploy costa
una migration in piu' e un percorso gia' aperto. E' l'unica ragione per cui questo
piano esiste adesso invece che dopo.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 — una serata senza sede non si rivela, e una sede non si collega a una serata rivelata</name>
  <files>src/app/(admin)/admin/events/[id]/reveal/actions.ts, src/app/(admin)/admin/events/actions.ts</files>
  <read_first>
    - `.planning/phases/37-manual-venue-reveal/37-REVIEW.md` — **CR-01 per intero**, con i quattro passi dello scenario e le righe che cita
    - `src/app/(admin)/admin/events/[id]/reveal/actions.ts` — `revealVenueNow` e le sue precondizioni attuali; l'unione dei dieci rifiuti tipizzati
    - `src/app/(admin)/admin/events/[id]/reveal/VenueRevealPanel.tsx` — `:200-201`, la guardia che oggi vive **solo** nel client
    - `src/app/(admin)/admin/events/actions.ts` — `nightFields` e `venue_secret_locked` (`:955` circa): quali campi chiude oggi, e perche' `venue_id` non c'e'
    - `supabase/migrations/20260810161000_venues_read_narrowed.sql` — il ramo 5, che guarda solo `venue_revealed_at IS NOT NULL`
    - `.claude/rules/venue-secrecy.md` e `.claude/rules/meta-gates.md` — le guardie monotone
  </read_first>
  <action>
    Il percorso da chiudere ha due estremita', e vanno chiuse **entrambe**: chiuderne
    una sola sposta il buco invece di toglierlo.

    **Estremita' A — non si rivela cio' che non c'e'.** `revealVenueNow` deve
    rifiutare quando la serata non ha una sede collegata, **prima** di scrivere
    l'atto. Il rifiuto e' un valore di ritorno con la sua categoria, come gli altri
    dieci: aggiungi un membro all'unione esportata — il nome naturale e'
    `venue_not_set` — e la sua frase nel `Record` totale che 37-11 rende. Il
    compilatore pretendera' la frase: e' il punto di quel `Record`.
    **La guardia in `VenueRevealPanel.tsx:200-201` resta** — non e' ridondanza, e'
    UX: il pannello non arma un bottone che il server rifiutera'. Ma non e' piu' la
    sola.

    **Estremita' B — non si collega una sede a una serata gia' rivelata.**
    `venue_id` deve entrare nell'insieme dei campi che `venue_secret_locked` chiude
    quando la serata porta un `venue_revealed_at`. Oggi `venue_secret_locked` chiude
    `venue_secret` ma non `venue_id`, che sta nello stesso `nightFields`: e' la
    ragione per cui il passo 3 dello scenario non incontra nessuna opposizione.

    Il rifiuto dell'estremita' B usa la forma gia' presente in quel file per i campi
    bloccati — non introdurne una nuova — e il messaggio deve dire **la causa**: la
    sede di una serata gia' rivelata non si cambia, perche' l'indirizzo nuovo
    diventerebbe pubblico senza un atto che lo registri.
  </action>
  <acceptance_criteria>
    - `revealVenueNow` rifiuta con una categoria propria quando la serata non ha `venue_id`, e il rifiuto avviene **prima** di qualunque scrittura
    - Il nuovo membro dell'unione compare nel `Record` dei rifiuti reso dall'interfaccia — verificato dal fatto che `npm run build` esce 0 (un `Record` totale rende una frase mancante un errore di compilazione)
    - `venue_id` e' fra i campi che `venue_secret_locked` chiude su una serata con `venue_revealed_at` non nullo
    - Il messaggio del rifiuto nomina la causa, non solo il divieto
    - `grep -c "error.details" src/app/\(admin\)/admin/events/\[id\]/reveal/actions.ts` resta `0`
    - `npm run build` esce 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: WR-01 — il ramo 3 applica la regola che dichiara di rispecchiare</name>
  <files>supabase/migrations/20260810161000_venues_read_narrowed.sql</files>
  <read_first>
    - `.planning/phases/37-manual-venue-reveal/37-REVIEW.md` — **WR-01**, e il commento della migration che il review cita come falso
    - `supabase/migrations/20260810161000_venues_read_narrowed.sql` — i cinque rami di `venue_for_parties`, il ramo 3 in particolare
    - `src/app/(public)/events/[slug]/page.tsx` — `isVenueVisible`, il ramo del biglietto: **e' il predicato che il ramo 3 dichiara di rispecchiare**, ed e' la fonte
    - `supabase/migrations/20260305200000_venue_reveal_on_purchase.sql` — cosa governa quella colonna e qual e' il suo default
    - `.planning/phases/37-manual-venue-reveal/37-CONTEXT.md` — D-37-02, livello 1
  </read_first>
  <action>
    Il ramo 3 lascia cadere il congiunto `venue_reveal_on_purchase`. Su una serata
    configurata `false`, un possessore di biglietto che chiama la RPC direttamente
    legge l'indirizzo — mentre la pagina, sullo stesso caso, non glielo mostra. Una
    server function e' un endpoint: «passa dalla pagina» non e' una protezione.

    Rimetti il congiunto, nella stessa forma in cui il ramo di pagina lo valuta.
    **Leggi il ramo di pagina come fonte** invece di ricostruirlo a memoria: se i
    due divergono ancora, e' il ramo SQL a doversi allineare, non il contrario.

    **Questa migration NON e' applicata** — la modifichi come file. Non aggiungere
    una migration nuova, non rinominarla, non cambiarne il timestamp: la history
    resta in ordine e la prima applicazione sara' quella corretta.

    Aggiorna il commento che dichiarava l'equivalenza: un commento che afferma una
    proprieta' che il codice non ha e' peggio di nessun commento, perche' il
    prossimo lettore lo credera'.
  </action>
  <acceptance_criteria>
    - Il ramo 3 di `venue_for_parties` valuta `venue_reveal_on_purchase` con la stessa semantica del ramo biglietto di `isVenueVisible`
    - Il commento del ramo descrive cio' che il ramo fa
    - `supabase/migrations/20260810161000_venues_read_narrowed.sql` resta **un solo file**, con il timestamp invariato, e nessuna migration nuova compare in `supabase/migrations/`
    - `grep -c "venue_reveal_on_purchase" supabase/migrations/20260810161000_venues_read_narrowed.sql` e' maggiore di 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -c "venue_reveal_on_purchase" supabase/migrations/20260810161000_venues_read_narrowed.sql</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: is_published entra nei rami invece di stare davanti</name>
  <files>supabase/migrations/20260810161000_venues_read_narrowed.sql</files>
  <read_first>
    - `.planning/phases/37-manual-venue-reveal/deferred-items.md` — **voce 4**, trovata indipendentemente da 37-05 e 37-06 e misurata da 37-13
    - `supabase/migrations/20260810161000_venues_read_narrowed.sql` — dove `AND e.is_published` sta oggi, cioe' **prima** dei cinque rami
    - `.planning/phases/37-manual-venue-reveal/37-13-SUMMARY.md` — la misura: su una bozza la funzione non restituisce nulla a nessuno, mentre lo stesso master leggendo `public.venues` direttamente ottiene le righe
  </read_first>
  <action>
    Oggi `AND e.is_published` e' valutato **prima** dei rami, quindi su una bozza la
    funzione non risponde a nessuno — nemmeno a chi ha `staff.manage`. Lo staff le
    bozze le vede, e da oggi le vedrebbe senza il nome del locale.

    Sposta la condizione **dentro i rami**: i rami del pubblico (biglietto, RSVP,
    membro approvato, esterno) continuano a richiedere `is_published`; il ramo dello
    **staff** no.

    **Perche' non e' un allargamento, e va scritto accanto alla modifica:** chi ha
    `staff.manage` legge gia' `public.venues` direttamente, per la policy
    `venues_select_staff` creata da questa stessa migration. La funzione che gli
    rifiuta il nome su una bozza non lo protegge da niente — gli nega una strada
    mentre un'altra e' aperta. Toglie un'incoerenza, non aggiunge un lettore.
    *(Decisione presa in fase di chiusura, 2026-08-11, sulla misura di 37-13; la
    voce 4 di `deferred-items.md` va marcata chiusa con questa ragione.)*

    Vale il vincolo del Task 2: **stesso file, stesso timestamp, nessuna migration
    nuova.**
  </action>
  <acceptance_criteria>
    - `is_published` non compare piu' come congiunto unico davanti ai rami
    - Il ramo dello staff non richiede `is_published`; i rami del pubblico si'
    - La ragione — lo staff legge gia' le sedi per un'altra strada — e' scritta come commento accanto alla modifica, non solo nel SUMMARY
    - `deferred-items.md` voce 4 marcata chiusa, con la data e la ragione
    - Nessuna migration nuova in `supabase/migrations/`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && ls supabase/migrations/ | wc -l</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: WR-07 — la lettura che scarta l'errore, e il quarto fallimento silenzioso</name>
  <files>src/app/(admin)/admin/events/[id]/reveal/actions.ts</files>
  <read_first>
    - `.planning/phases/37-manual-venue-reveal/37-REVIEW.md` — **WR-07** e **WR-05**
    - `src/app/(admin)/admin/events/[id]/reveal/actions.ts` — `eventQuery.single()`, due letture sopra quelle gia' corrette
    - `.claude/rules/meta-gates.md` — zero fallimenti silenziosi, e il precedente del newsletter
  </read_first>
  <action>
    `eventQuery.single()` scarta il proprio errore: un rifiuto del database si legge
    come 404. E' il quarto della stessa famiglia in questa fase — dopo il lotto
    contato come consegnato, la lettura dei destinatari, e il conteggio a zero. Sulla
    stessa pagina, due letture piu' su di quelle gia' corrette.

    Ramifica sull'errore invece di ignorarlo, con la stessa forma tipizzata usata
    dalle letture corrette in questo file: «non trovato» e «non si e' potuto sapere»
    sono due frasi diverse e non devono diventare una.

    **Su WR-05, non correggere il codice: correggi il docblock.** Il commento di
    `party_not_in_event` dichiara di impedire la pubblicazione dell'indirizzo di una
    serata altrui. Entrambi gli id arrivano dal corpo della richiesta e la chiave non
    e' per-evento: e' una **verifica di coerenza**, non un confine di sicurezza.
    Riscrivi il docblock perche' dica cio' che il controllo fa — un gate che dichiara
    un confine che non presidia e' peggio di nessun gate, perche' fa credere che
    qualcuno stia controllando (`ai-engineering.md`, gate *un gate deve poter
    fallire*).
  </action>
  <acceptance_criteria>
    - `eventQuery.single()` non scarta piu' il proprio errore; un rifiuto del database e' distinguibile da «non trovato»
    - Il docblock di `party_not_in_event` descrive una verifica di coerenza, non un confine di sicurezza
    - `npm run build` esce 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| CR-01 | Information Disclosure | collegare una sede a una serata gia' rivelata pubblica l'indirizzo senza atto | **mitigate** | precondizione lato server prima dell'atto + `venue_id` fra i campi bloccati |
| WR-01 | Elevation of Privilege | il ramo 3 della RPC concede piu' del ramo di pagina che rispecchia | **mitigate** | congiunto `venue_reveal_on_purchase` rimesso, letto dalla fonte |
| WR-07 | Repudiation | un rifiuto del database letto come «non trovato» | **mitigate** | ramificazione tipizzata sull'errore |
| WR-05 | Repudiation | un docblock che dichiara un confine che il controllo non presidia | **mitigate** | docblock riscritto sul comportamento reale |
| T-37-31 | Information Disclosure | `is_published` spostato dentro i rami allarga chi legge | **accept** | non allarga: `staff.manage` legge gia' `public.venues` per `venues_select_staff` — misurato da 37-13 |
</threat_model>

<verification>
- `npm run build`, `npm run verify:routes`, `npm run verify:capabilities`, `npm run verify:persona` verdi
- **Nessuna migration nuova**: la 20260810161000 resta un file solo, non applicato
- La prova comportamentale del SQL e' un container Postgres usa-e-getta con entrambe le migration, sul modello di 37-02 — **mai la produzione**
</verification>

<success_criteria>
- I quattro reperti chiusi, o dichiarati non chiudibili con la ragione
- Nessuna scrittura in produzione, nessuna migration applicata
- `deferred-items.md` voce 4 marcata chiusa
</success_criteria>

<output>
Create `.planning/phases/37-manual-venue-reveal/37-14-SUMMARY.md` when done
</output>
