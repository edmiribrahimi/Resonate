---
phase: 37-manual-venue-reveal
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/routes/next-redirect.ts
  - src/app/api/auth/callback/route.ts
  - src/app/(auth)/login/page.tsx
autonomous: true
requirements: [VENUE-01]

must_haves:
  truths:
    - "La destinazione dopo il login passa da una allow-list prima di raggiungere il browser"
    - "Esiste una sola allow-list nel repository, e i due percorsi la leggono"
    - "Il nome del parametro NON viene allineato in questo piano, e il perche' e' scritto"
  artifacts:
    - path: "src/lib/routes/next-redirect.ts"
      provides: "La allow-list e il risolutore, estratti dal callback e condivisi"
      contains: "resolveNext"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "La destinazione validata prima di finire in window.location.href"
      contains: "resolveNext"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/lib/routes/next-redirect.ts"
      via: "la stessa lista che gia' protegge il callback"
      pattern: "resolveNext"
---

<objective>
Chiudere il todo del redirect dopo il login — **e tenerlo separato**, nei piani e
nei commit.

Purpose: `src/app/(auth)/login/page.tsx:11` legge `?next=` e `:52` lo scrive in
`window.location.href`. **Fra le due righe non c'e' alcuna validazione.** La
difesa gemella esiste gia' e funziona: `src/app/api/auth/callback/route.ts:44-49`
(`NEXT_ALLOW_LIST`, quattro pattern ancorati) e `:52-90` (`resolveNext`, con i
cinque rifiuti nominati uno per uno).

**Nota sul perimetro, dichiarata e non nascosta:** questo piano **non** copre
VENUE-01 ne' VENUE-02. Chiude un todo che viaggia con la fase **per scelta del
proprietario**, non perche' sia la stessa materia — non tocca il venue. Il campo
`requirements` porta `VENUE-01` perche' lo schema del frontmatter non ammette un
elenco vuoto: e' una convenzione, non una copertura, e questa riga esiste per
dirlo. Il piano va tenuto **separato nei commit**: mescolarlo significa che
nessuno dei due lavori viene guardato per quello che e'.

Output: una allow-list sola, letta da due percorsi, e un blocco D7 che resta
aperto con la sua ragione.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
**Il vincolo d'ordine, che viene dal todo stesso e non e' una preferenza:**

  il middleware scrive   `?redirect=`   (src/lib/supabase/middleware.ts:466)
  la pagina di login legge `?next=`     (src/app/(auth)/login/page.tsx:11)

I due nomi non coincidono, quindi **la riga non validata non e' raggiungibile da
quel percorso** — ed e' anche il blocco **D7** di `STATE.md`: la destinazione
dopo il login si perde su ogni indirizzo protetto.

**Allineare i nomi senza aggiungere la allow-list ATTIVA l'apertura.** Prima la
allow-list, poi — in un lavoro suo, che non e' questo — l'allineamento.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Una sola allow-list, estratta invece che riscritta</name>
  <files>src/lib/routes/next-redirect.ts, src/app/api/auth/callback/route.ts</files>
  <read_first>
    - `src/app/api/auth/callback/route.ts:40-95` — `NEXT_ALLOW_LIST`, i quattro pattern ancorati, `resolveNext` e i cinque rifiuti nominati
    - `src/lib/media/may-upload.ts:1-34` — la forma «un modulo, due chiamanti» e perche' non e' un export di un file `"use server"`
    - `src/lib/routes/capability-routes.ts:1-60` — la convenzione di questa directory
  </read_first>
  <action>
    Spostare `NEXT_ALLOW_LIST` e `resolveNext` in
    `src/lib/routes/next-redirect.ts`, **senza cambiarne il comportamento**, e far
    importare il callback da li'. Una seconda lista scritta a mano divergerebbe al
    primo indirizzo nuovo: e' l'unico motivo per cui questo passo esiste come
    passo separato.

    Portare con il codice **i cinque rifiuti nominati uno per uno** e il commento
    che spiega perche' i pattern sono **ancorati**: una lista di prefissi non
    ancorati e' una allow-list che accetta `/dashboard.attacker.example`.

    Il modulo non e' `"use server"` e non deve diventarlo: e' una funzione pura su
    una stringa, usata da un route handler e da un componente client.

    Verificare che il callback si comporti **identico**: e' un percorso di
    autenticazione, e un'estrazione che cambia un verdetto qui non e' un
    refactoring.
  </action>
  <acceptance_criteria>
    - `grep -c "NEXT_ALLOW_LIST" src/app/api/auth/callback/route.ts` mostra solo l'import, non la definizione
    - I pattern nel modulo nuovo sono **byte-identici** a quelli di prima (diff riportato nel SUMMARY)
    - Il modulo non contiene `"use server"`
    - `npm run build` esce con 0
    - Osservazione manuale: un giro di login completo dal callback continua a portare dove portava prima
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>La lista vive in un posto solo e il callback si comporta come prima.</done>
</task>

<task type="auto">
  <name>Task 2: La pagina di login smette di fidarsi del parametro</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <read_first>
    - `src/app/(auth)/login/page.tsx` — `:11` (la lettura di `?next=`), `:52` (`window.location.href`), `:99` (la propagazione verso `/register`)
    - `src/lib/routes/next-redirect.ts` — appena creato
    - `.planning/todos/pending/login-client-redirect-not-allow-listed.md` — cosa chiede a chi lo chiude
  </read_first>
  <action>
    Far passare il valore letto a `:11` da `resolveNext` **prima** che raggiunga
    `window.location.href` a `:52`, e prima che venga propagato verso `/register`
    a `:99`. Un valore rifiutato ricade su `/dashboard`, che e' gia' il ripiego
    scritto in quella riga.

    **Non** allineare il nome del parametro nel middleware. Allineare senza la
    allow-list attiva l'apertura; allineare **con** la allow-list la chiuderebbe in
    sicurezza, ma e' il blocco **D7**, che non e' assegnato a questa fase e ha una
    sua analisi da fare (quali percorsi costruiscono un URL di login, e quale dei
    due nomi tenere — il todo dichiara quel censimento **non verificato**).
    Scrivere accanto al codice che D7 resta aperto e che **adesso la guardia c'e'
    prima**, cosi' chi lo chiudera' trovera' il terreno pronto invece di aprire una
    falla chiudendo un difetto.

    Il todo chiede esplicitamente che **chi lo chiude lo provi con una richiesta
    reale prima di dichiararlo risolto, e lo provi anche dopo il rimedio**. Il
    codice letto dice di si': non e' un'osservazione.
  </action>
  <acceptance_criteria>
    - `resolveNext` compare fra `:11` e `:52`, e sul percorso verso `/register`
    - Un valore assoluto verso un altro host, provato con una richiesta reale su `/login?next=https://example.org`, porta a `/dashboard` e **non** all'host esterno — osservato prima e dopo il rimedio, e riportato
    - Un valore interno legittimo continua a funzionare, provato con una richiesta reale
    - Il codice porta la riga che dichiara D7 ancora aperto
    - `npm run build` esce con 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Il valore non validato non raggiunge piu' il browser, e D7 resta aperto per iscritto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string → `window.location.href` | un parametro di URL e' scritto da chi manda il link, non dal prodotto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-50 | Spoofing | open redirect dopo il login: un link che porta un membro autenticato su un host altrui | mitigate | `resolveNext` applicato prima della navigazione e prima della propagazione a `/register` |
| T-37-51 | Tampering | una seconda allow-list che diverge dalla prima | mitigate | estrazione, non riscrittura: un modulo, due chiamanti |
| T-37-52 | Elevation of Privilege | allineare i nomi del parametro senza la guardia, attivando la falla | mitigate | l'ordine e' un vincolo dichiarato: la allow-list qui, l'allineamento in D7, non in questa fase |
| T-37-SC | Tampering | installazioni npm/pip/cargo | accept | nessun pacchetto nuovo |
</threat_model>

<verification>
- `npm run build` verde
- Due richieste reali su `/login?next=…`, una interna e una verso un host esterno,
  osservate **prima e dopo** il rimedio e riportate con l'esito
- Un giro di login dal callback, per accertare che l'estrazione non abbia cambiato
  un verdetto
</verification>

<success_criteria>
- Una sola allow-list nel repository
- Il valore non validato non raggiunge piu' `window.location.href`
- Commit separati da quelli del venue
</success_criteria>

<output>
Create `.planning/phases/37-manual-venue-reveal/37-12-SUMMARY.md` when done
</output>
