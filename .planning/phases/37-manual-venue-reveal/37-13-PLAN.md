---
phase: 37-manual-venue-reveal
plan: 13
type: execute
wave: 7
depends_on: ["37-05", "37-06", "37-07", "37-08", "37-09", "37-11"]
files_modified: []
autonomous: false
requirements: [VENUE-01, VENUE-02]

must_haves:
  truths:
    - "Con la sola chiave anonima, chiesto per chiave primaria, l'indirizzo di una sede segreta non torna"
    - "Nel sorgente delle pagine pubbliche — payload RSC compreso — nessuno degli aghi dichiarati compare"
    - "Una serata NON segreta mostra ancora nome e indirizzo del locale a un lettore senza login"
    - "La stessa pagina, letta in finestra pulita e in finestra gia' visitata, non mostra l'indirizzo a chi non ne ha titolo"
    - "Cio' che questa procedura NON prova e' scritto, non evocato"
  artifacts:
    - path: ".planning/phases/37-manual-venue-reveal/37-13-SUMMARY.md"
      provides: "Le sonde, i loro esiti letterali, i conteggi prima e dopo, e le voci human_needed"
      contains: "human_needed"
  key_links:
    - from: "la sonda anonima"
      to: "public.venue_for_parties"
      via: "la strada positiva di D-37-24 misurata nella stessa corsa della negativa"
      pattern: "venue_for_parties"
---

<objective>
Guardare il risultato con la chiave anonima, contro le pagine vere — perche' il
guasto che questa fase deve evitare **non da' errore: restituisce vuoto**, e
nessun build lo vede.

Purpose: D-37-25 lo chiede alla lettera, e la ragione e' misurata: PostgREST
risponde a un embed malformato o rifiutato con `data: null` e **nessuna
eccezione**, quindi la pagina rende «nessun evento» e nulla protesta — una bugia
dall'aria sana, sulla vetrina. E la fuga chiusa dal piano 37-05 **non viveva nel
rendering ma nel payload RSC**: nessuno sguardo alla pagina l'avrebbe vista.

D-37-09 aggiunge la seconda meta': il predicato ora ha una componente temporale
che **scatta da sola a un istante preciso**, e il service worker cachea queste
pagine `NetworkFirst` per 24 ore. E' un **requisito di verifica di fase**, non un
dettaglio di implementazione.

Output: un SUMMARY con le sonde e i loro esiti **letterali**, i conteggi prima e
dopo, la strada positiva misurata nella stessa corsa, e le voci `human_needed`
riportate come tali.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-manual-venue-reveal/37-VALIDATION.md

<interfaces>
Il precedente eseguito, da **ripetere** invece che ricordare:
`.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md:195-270` — la
procedura V3 con la chiave anonima.

I quattro principi che governano ogni misura di questo piano:

  1. la misura non si prende con lo strumento che ha causato l'effetto;
  2. le righe create per una prova si catturano **per chiave primaria alla
     creazione** e si rimuovono per chiave primaria — mai cliccando un controllo
     di cancellazione, mai risalendo un albero di elementi;
  3. l'istantanea copre **le cascate**, tutte, lette dai vincoli;
  4. l'autorizzazione a scrivere in produzione **si consuma una volta**.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: L'istantanea, e le sonde chieste per chiave primaria</name>
  <files>nessuno — solo letture</files>
  <read_first>
    - `.planning/phases/36-formats-series-numbering/36-13-SUMMARY.md:195-270` — la corsa gia' eseguita, con la forma delle sonde e la ragione per cui si chiede **per chiave primaria** e non per conteggio
    - `.planning/phases/37-manual-venue-reveal/37-RESEARCH.md` § Validation Architecture (righe 1405-1504)
    - `.planning/phases/37-manual-venue-reveal/37-03-SUMMARY.md` — i numeri dell'istantanea presa prima della push
  </read_first>
  <action>
    **Prima:** hash per tabella e conteggi di riferimento su `venues`,
    `event_parties`, `events`, `tickets`, `rsvps`, `venue_reveal_acts` — con la
    service key, **in sola lettura**. Confrontarli con quelli presi dal piano
    37-03 prima della push: una DDL non muove righe, e se ne ha mosse va detto
    subito.

    **Poi le sonde, con la sola `NEXT_PUBLIC_SUPABASE_ANON_KEY` e nessuna
    sessione**, chiedendo **per chiave primaria** — non per conteggio, perche' un
    totale stabile non distingue un rifiuto da una coincidenza:

      GET /rest/v1/venues?select=id,name,address,google_maps_url&id=eq.<sede di una serata segreta>
        → deve rispondere con un insieme VUOTO

      GET /rest/v1/venues?select=id,name,address&id=eq.<sede di una serata NON segreta>
        → deve rispondere con un insieme VUOTO anch'esso: dopo il piano 37-02
          `anon` non legge piu' la tabella, e la strada di D-37-24 e' la funzione

      GET /rest/v1/event_parties?select=id,venue_secret,venue_id&id=eq.<serata segreta>
        → risponde: `venue_id` resta leggibile, ed e' la ragione per cui la
          concessione e' per serata e non per sede. Registrarlo come **fatto
          atteso**, non come fuga

      POST /rest/v1/rpc/venue_for_parties  { "p_party_ids": ["<serata segreta>"] }
        → deve restituire un insieme VUOTO

      POST /rest/v1/rpc/venue_for_parties  { "p_party_ids": ["<serata NON segreta>"] }
        → **deve restituire nome e indirizzo**: e' la strada positiva di D-37-24,
          e senza questa misura il rimedio e' indistinguibile da una rottura

    Riportare **l'esito letterale** di ogni sonda — codice HTTP e corpo — non un
    «ok». Gli id delle serate e delle sedi **non entrano nel SUMMARY**: si nomina
    «la serata segreta A», «la sede non segreta B». `.planning/` e' pubblicato.
  </action>
  <acceptance_criteria>
    - Le cinque sonde sono state eseguite con la sola chiave anonima e i loro esiti letterali sono nel SUMMARY
    - Le due sonde su `venues` restituiscono insiemi vuoti
    - La sonda `venue_for_parties` sulla serata **non** segreta restituisce nome e indirizzo
    - Nessun id reale, nessun indirizzo reale, nessun nome di sede in trattativa compare nel SUMMARY
    - I conteggi delle sei tabelle sono identici a quelli del piano 37-03
  </acceptance_criteria>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/venues?select=id&limit=1" -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY"</automated>
  </verify>
  <done>La porta anonima e' chiusa e la strada positiva e' aperta, misurate nella stessa corsa.</done>
</task>

<task type="auto">
  <name>Task 2: Il sorgente, non il rendering</name>
  <files>nessuno — solo letture</files>
  <read_first>
    - `.planning/phases/37-manual-venue-reveal/37-05-SUMMARY.md` e `37-06-SUMMARY.md` — cosa e' stato tolto e da dove
    - `.planning/phases/37-manual-venue-reveal/37-RESEARCH.md` § B.2 (righe 416-455) — la catena della fuga nel payload
  </read_first>
  <action>
    `curl` **senza sessione** su `/events`, su `/events/<slug di una serata
    segreta>` e su `/venues/<slug>` — quest'ultimo deve rispondere **404** dopo lo
    spostamento del piano 37-08.

    Su ogni documento **intero**, payload RSC compreso, cercare gli **aghi
    dichiarati**: l'indirizzo della sede segreta, il suo link Maps, il suo nome,
    e il suo id. Dichiarare gli aghi **prima** di cercarli, e riportarli nel
    SUMMARY per **forma** («l'indirizzo della sede A», «l'id della sede A»), mai
    per valore.

    E' il passo che intercetta la fuga di § B.2, che nessuno sguardo al rendering
    avrebbe visto: i due campi non erano renderizzati da nessuna parte e
    viaggiavano comunque.

    Sulla stessa corsa, la **strada positiva**: `/events` e la pagina di una
    serata **non** segreta devono mostrare nome e indirizzo del locale a un
    lettore anonimo. Senza questa misura, un rimedio riuscito e una rottura
    silenziosa sono indistinguibili.

    Poi la stessa lettura **con una sessione master**, per accertare che le
    superfici di lavoro non abbiano perso le sedi: `/admin/venues`,
    `/admin/venues/<slug>` e il form di una serata devono elencare e mostrare.
    Una scheda vuota senza errore e' il modo peggiore di fallire, ed e' il rischio
    dichiarato dal piano 37-08.
  </action>
  <acceptance_criteria>
    - Zero occorrenze di ciascun ago dichiarato nei documenti anonimi, riportate con il comando e il conteggio
    - `/venues/<slug>` risponde 404 senza sessione
    - La serata **non** segreta mostra nome e indirizzo del locale nel documento anonimo
    - `/admin/venues` e `/admin/venues/<slug>` mostrano le sedi con sessione master
    - Nel SUMMARY, la frase che dichiara **cosa la procedura non prova**: che *N aghi dichiarati non compaiono in M documenti letti in un momento preciso*, e che **nessun meccanismo qui puo' asserire l'assenza di un canale**
  </acceptance_criteria>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "$NEXT_PUBLIC_APP_URL/events"</automated>
  </verify>
  <done>Il sorgente e' stato letto, non guardato, e la strada positiva e' misurata accanto a quella negativa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: La cache attraverso l'istante, e il debito che resta</name>
  <what-built>
    Le tre pagine sono `force-dynamic` dichiarate (piani 37-05 e 37-06) e il
    dettaglio serata e' escluso dalle tre cache di pagina del service worker
    (piano 37-07).
  </what-built>
  <how-to-verify>
    **Due letture, dichiarate come tali** (D-37-09):

    1. **Finestra privata** — service worker pulito. Aprire la pagina di una
       serata segreta **prima** dell'apertura della finestra: deve mostrare
       l'indizio. Poi, con la stessa sessione, **dopo** che la finestra si e'
       aperta (o dopo un atto manuale): deve mostrare l'indirizzo.
    2. **Finestra gia' visitata** — il dispositivo ha aperto la pagina prima del
       deploy. Le voci gia' in Cache Storage sopravvivono fino a 24 ore anche se
       il worker si aggiorna alla prima visita (`skipWaiting`, `clientsClaim`):
       **la prima misura va presa in finestra privata, o si misura il worker
       vecchio.** Dichiarare quale delle due si sta guardando, ogni volta.

    Con la rete disattivata, la pagina della serata **non deve aprirsi affatto**:
    e' il costo dichiarato della regola `NetworkOnly`, ed e' l'opposto della porta.
    Se si apre, la regola non sta funzionando.

    **Poi il debito, che si scrive e non si aggira.** Riportare come
    `human_needed`, con la ragione, tutto cio' che oggi non e' provabile:

    - un organizer **approvato e non proprietario** che rivela con successo;
    - un organizer **non approvato** che viene **rifiutato**;
    - il cron che **completa** una serata rivelata a mano.

    In produzione non esistono sessioni `organizer` ne' `staff`, e nessuno
    strumento di questo repository puo' autenticarsi come un ruolo: sono la stessa
    specie delle **32 voci `human_needed`** gia' aperte fra `43-`, `35-` e
    `34-VERIFICATION.md`. **Va detto** che questa fase costruisce un percorso
    irreversibile sopra un modello di permessi che nessuno ha ancora visto
    rifiutare qualcuno.
  </how-to-verify>
  <action>
    Eseguire le due letture di cache descritte sopra, dichiarando ogni volta in
    quale finestra si sta guardando, e la prova a rete disattivata. Poi raccogliere
    nel SUMMARY le tre voci `human_needed` con la loro ragione, e dichiarare se
    `nyquist_compliant` puo' passare a `true`.
  </action>
  <resume-signal>Scrivere "approvato" oppure indicare quale delle due letture non ha dato l'esito atteso</resume-signal>
  <read_first>
    - `.planning/phases/37-manual-venue-reveal/37-07-SUMMARY.md` — la regola aggiunta e il suo costo
    - `.planning/phases/37-manual-venue-reveal/37-VALIDATION.md` — le tre voci gia' dichiarate non provabili
    - `.planning/STATE.md:76-84` — il debito di verifica aperto, 32 voci
  </read_first>
  <acceptance_criteria>
    - Le due letture sono riportate **separatamente**, ciascuna dichiarando in quale finestra e' stata presa
    - Con la rete disattivata la pagina della serata non si apre, e l'osservazione e' riportata
    - Le tre voci `human_needed` compaiono nel SUMMARY con la loro ragione, non silenziate
    - Il SUMMARY dichiara se `nyquist_compliant` di `37-VALIDATION.md` puo' passare a `true` e, se non puo', perche'
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chiave anonima → PostgREST | e' il lettore che conta, e non e' quello che si ha in sviluppo |
| Cache Storage → lettore | una copia locale attraversa l'istante di rivelazione |
| documento di verifica → repo pubblico | ogni SUMMARY e' una pubblicazione |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-53 | Information Disclosure | un rimedio che sembra completo perche' e' stato guardato il rendering | mitigate | si legge il **sorgente**, documento intero, payload RSC compreso, con aghi dichiarati prima |
| T-37-54 | Information Disclosure | una rottura silenziosa scambiata per un rimedio riuscito | mitigate | la strada positiva di D-37-24 si misura nella **stessa corsa** della negativa |
| T-37-55 | Information Disclosure | id, indirizzi e nomi di sede in un artefatto di verifica | mitigate | nel SUMMARY entrano forme e conteggi, mai valori |
| T-37-56 | Repudiation | un debito di verifica silenziato invece che dichiarato | mitigate | tre voci `human_needed` riportate con la loro ragione, e la frase su cosa la procedura non puo' provare |
| T-37-SC | Tampering | installazioni npm/pip/cargo | accept | nessun pacchetto nuovo |
</threat_model>

<verification>
- `npm run build`, `npm run verify:routes`, `npm run verify:capabilities`,
  `npm run verify:persona` — tutti e quattro verdi prima di dichiarare chiusa la
  fase
- Le sonde anonime e le letture di sorgente eseguite e riportate letteralmente
- Le due letture di cache, dichiarate una per una
</verification>

<success_criteria>
- Nessun ago dichiarato compare in nessun documento anonimo
- La strada positiva di D-37-24 risponde
- Le voci `human_needed` sono scritte, con la ragione
- Zero righe di produzione mosse da questo piano
</success_criteria>

<output>
Create `.planning/phases/37-manual-venue-reveal/37-13-SUMMARY.md` when done
</output>
