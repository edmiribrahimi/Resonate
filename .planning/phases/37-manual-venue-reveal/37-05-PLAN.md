---
phase: 37-manual-venue-reveal
plan: 05
type: execute
wave: 3
depends_on: ["37-03"]
files_modified:
  - src/app/(public)/events/page.tsx
  - src/app/(public)/events/EventTabs.tsx
autonomous: true
requirements: [VENUE-01]

must_haves:
  truths:
    - "L'indirizzo e il link Maps non compaiono piu' nel payload della lista eventi, per nessun visitatore"
    - "Una serata NON segreta continua a mostrare il nome del locale a un lettore senza login"
    - "Una serata segreta mostra Secret Venue e nient'altro, e non porta con se' l'id della sede risolto in nome"
    - "Se la strada verso i nomi dei locali fallisce, la pagina lo dice invece di rendere una lista silenziosamente monca"
  artifacts:
    - path: "src/app/(public)/events/page.tsx"
      provides: "La lista che risolve i nomi dei locali per titolo invece che per embed, e la sua dichiarazione di dinamicita'"
      contains: "venue_for_parties"
    - path: "src/app/(public)/events/EventTabs.tsx"
      provides: "L'interfaccia client senza i due campi che non rendeva"
      contains: "venue_name"
  key_links:
    - from: "src/app/(public)/events/page.tsx"
      to: "public.venue_for_parties"
      via: "una sola chiamata rpc per l'intera lista, con gli id raccolti dalla query precedente"
      pattern: "venue_for_parties"
---

<objective>
Chiudere la fuga che nessun todo aveva registrato, e ricostruire il nome del
locale sulla strada nuova.

Purpose: misurato durante la ricerca — `src/app/(public)/events/page.tsx:212`
seleziona `venues(name, address, google_maps_url)` per **ogni** serata, segrete
comprese; `:268-275` costruisce `venue_address` e `venue_google_maps_url` **senza
guardare `venue_secret`**; `:350` mette l'array nella card; e `EventTabs.tsx` e'
`"use client"`. Next serializza **ogni** prop di un componente client nel payload
RSC. I due campi **non sono renderizzati da nessuna parte** — l'unica altra
occorrenza in tutto `src/` e' la loro dichiarazione di tipo — quindi sono prop
morte che viaggiano comunque, leggibili da chiunque apra `/events`.

E' una fuga nel bundle, non nel database: **il rimedio RLS del piano 37-02 non la
chiude**, e lasciarla aperta significa che il rimedio sembrera' completo mentre
non lo e'.

Output: due campi che non vengono piu' selezionati ne' costruiti ne' dichiarati,
i nomi dei locali che arrivano dalla funzione di titolo, e nessun pixel cambiato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-manual-venue-reveal/37-02-SUMMARY.md

<interfaces>
La funzione che sostituisce l'embed (piano 37-02, gia' applicata dal 37-03):

  public.venue_for_parties(p_party_ids uuid[])
    → (party_id, venue_id, name, slug, address, google_maps_url)

Una serata assente dal risultato significa **nessun titolo**. Per questa pagina
significa: nessun nome da mostrare, e `venue_secret` decide cosa rendere al suo
posto. La pagina **non usa** `address` ne' `google_maps_url` — e non deve
selezionarli nemmeno dalla funzione, se la forma del client lo consente.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: La lista risolve i nomi per titolo invece che per embed</name>
  <files>src/app/(public)/events/page.tsx</files>
  <read_first>
    - `src/app/(public)/events/page.tsx` — `:196-225` (l'embed, il commento sulle due relazioni e la gestione dell'errore che questa pagina ha gia' costruito), `:255-289` (la costruzione di `VenueInfo` e il gate del nome), `:340-355` (dove l'array finisce nella card)
    - `.planning/phases/37-manual-venue-reveal/37-RESEARCH.md` § B.2 (righe 416-455) e § B.4 (righe 488-520)
    - `.planning/phases/37-manual-venue-reveal/37-PATTERNS.md` § 13 (righe 1320-1380)
  </read_first>
  <action>
    Togliere `venues(name, address, google_maps_url)` dalla `select` di `:212`.
    L'embed non serve piu': dopo il piano 37-02 un lettore anonimo non ha alcuna
    lettura su `public.venues`, e un embed rifiutato **non da' errore, restituisce
    vuoto** — la serata perderebbe il nome del locale in silenzio, che e'
    esattamente la specie di guasto che la fase 36 ha misurato (`PGRST201`,
    `data: null`, la pagina che rende «nessun evento»). E' la forma di guasto che
    **D-37-25** nomina per nome, e la strada verso il nome di un locale pubblico
    che **D-37-24** pretende va **costruita**, non sperata.

    Al suo posto: raccogliere gli id di tutte le serate della lista e fare **una
    sola** chiamata `supabase.rpc("venue_for_parties", { p_party_ids: ids })`,
    poi indicizzare il risultato per `party_id`. Una chiamata per l'intera pagina,
    non una per serata.

    **L'errore della chiamata si controlla e non si scarta.** Questa pagina ha
    gia' costruito il rilevamento a `:219-225` e `:280-285`, e ha gia' scritto
    perche': un rifiuto del database porta un codice e viene lanciato verso
    l'error boundary; un guasto di trasporto no. La `rpc` eredita quel
    trattamento — un `error` non nullo si lancia, con la stessa forma. Una lista
    che perde tutti i nomi dei locali senza che nulla protesti e' «una bugia
    dall'aria sana, sulla vetrina», ed e' il precedente che quel commento
    descrive.

    Nella costruzione di `VenueInfo` (`:268-275`): **eliminare `venue_address` e
    `venue_google_maps_url`**, e prendere `venue_name` dalla mappa della `rpc`
    invece che dall'embed. `venue_secret` e `venue_secret_hint` restano come sono:
    il gate del nome sul marker usa la **bandiera memorizzata** `venue_secret`,
    non un predicato calcolato — e' la scelta gia' presa dalla fase 36 (D-36-11),
    con `!== false` cosi' che una riga mancante o un join fallito valgano come
    segreto.

    Aggiungere infine `export const dynamic = "force-dynamic";`, con una riga che
    dice perche': oggi la pagina e' `ƒ` **per derivazione** da `cookies()`, non
    per dichiarazione, e nessuna riga esprime l'intenzione. Il predicato del venue
    ora ha una componente temporale che scatta da sola a un istante preciso: una
    pagina servita statica attraverso quell'istante mostra l'indirizzo a chi non
    deve. La dichiarazione non cambia il comportamento misurato oggi — **lo rende
    una decisione invece che un effetto collaterale**, e rende visibile chi la
    rimuove.
  </action>
  <acceptance_criteria>
    - `grep -c "google_maps_url" src/app/\(public\)/events/page.tsx` restituisce 0
    - `grep -c "venue_address" src/app/\(public\)/events/page.tsx` restituisce 0
    - Il file contiene una sola occorrenza di `venue_for_parties` (una chiamata, non una per serata)
    - Il risultato della `rpc` e' destrutturato con `{ data, error }` e l'`error` porta a un lancio, come gia' fa la query degli eventi
    - Il file contiene `export const dynamic = "force-dynamic";`
    - `npm run build` esce con 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -c "google_maps_url" "src/app/(public)/events/page.tsx"</automated>
  </verify>
  <done>La lista non seleziona piu' un indirizzo, e i nomi che mostra arrivano dalla strada che decide per titolo.</done>
</task>

<task type="auto">
  <name>Task 2: L'interfaccia client perde i due campi che non rendeva</name>
  <files>src/app/(public)/events/EventTabs.tsx</files>
  <read_first>
    - `src/app/(public)/events/EventTabs.tsx` — `:1` (`"use client"`), `:8-20` (il tipo `VenueInfo`), `:244-262` (l'unico punto che rende il venue)
    - `.claude/rules/nextjs-architecture.md` — gate *segreti nel bundle*
  </read_first>
  <action>
    Rimuovere `venue_address` e `venue_google_maps_url` dall'interfaccia delle
    prop. Sono dichiarati e mai usati: il rendering a `:246-259` legge solo
    `venue_secret`, `venue_name` e `venue_text`. **Nessun pixel cambia.**

    Scrivere accanto all'interfaccia la ragione, perche' la prossima persona che
    vorra' mostrare un indirizzo nella lista deve trovarla qui: tutto cio' che sta
    in un componente client finisce nel browser, quindi un campo dichiarato «per
    quando servira'» e' gia' pubblicato. Se un giorno servira' l'indirizzo di una
    serata **non segreta** nella lista, arriva da `venue_for_parties` come il
    nome, e passa dallo stesso controllo.
  </action>
  <acceptance_criteria>
    - `grep -c "venue_address\|venue_google_maps_url" src/app/\(public\)/events/EventTabs.tsx` restituisce 0
    - Il blocco di rendering del venue e' invariato riga per riga (dichiarato nel SUMMARY con il diff)
    - `npm run build` esce con 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Il componente client non riceve piu' campi che non rende.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server RSC → browser | ogni prop di un componente client attraversa il confine, renderizzata o no |
| chiave anonima → PostgREST | la pagina e' aperta senza sessione dalla maggior parte dei visitatori |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-19 | Information Disclosure | `venue_address` e `venue_google_maps_url` nel payload RSC di `/events` | mitigate | non si selezionano, non si costruiscono, non si dichiarano: tre punti, non uno |
| T-37-20 | Denial of Service | l'embed rifiutato che restituisce vuoto e fa sparire i nomi in silenzio | mitigate | l'embed viene rimosso; la `rpc` che lo sostituisce ha il suo `error` controllato e lanciato |
| T-37-21 | Information Disclosure | una copia statica della lista servita attraverso l'istante di rivelazione | mitigate | `export const dynamic = "force-dynamic"` dichiarato invece che derivato |
| T-37-SC | Tampering | installazioni npm/pip/cargo | accept | nessun pacchetto nuovo |
</threat_model>

<verification>
- `npm run build` verde
- **La prova che conta e' nel piano 37-13**: `curl` su `/events` con nessuna
  sessione e ricerca degli aghi dichiarati sull'**intero documento, payload RSC
  compreso**. Nessuno sguardo al rendering avrebbe visto questa fuga, e nessun
  build la vede.
</verification>

<success_criteria>
- Zero occorrenze di `venue_address` e `venue_google_maps_url` nei due file
- Una serata non segreta mostra ancora il nome del locale senza login
- La dinamicita' della pagina e' dichiarata
</success_criteria>

<output>
Create `.planning/phases/37-manual-venue-reveal/37-05-SUMMARY.md` when done
</output>
