---
phase: 41-shared-primitives-three-tier-layout
plan: 16
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - scripts/verify-dialogs.mjs
autonomous: true
requirements: [DS-08]
must_haves:
  truths:
    - "The scanner and the door are outside this gate's check B by path, declared before the matcher widens, and the fence is printed on every run as unmeasured rather than approved"
    - "This gate's Phase 42 fence and the conversion manifest's are the same fence, or the run refuses having measured nothing"
    - "A hand-rolled overlay written at any z rung is seen by check B, not only one written at the rung the eleven incumbents happen to carry"
    - "The positioning utility is matched as a token, so a longer word containing it does not produce a red on a correct file"
    - "The matcher proves itself on every run against three fixed probes, and disagreeing with its own description is a refusal"
    - "REMAINING still reconciles on the live tree: the number of files carrying a shell is stated, and any change from 14 is a decision recorded with its reason"
  artifacts:
    - path: "scripts/verify-dialogs.mjs"
      provides: "check B's overlay matcher keyed on the shape of an overlay rather than on one literal rung, behind a Phase 42 path fence"
      contains: "PHASE_42_EXEMPT_PATHS"
    - path: "scripts/verify-dialogs.mjs"
      provides: "the overlay signature as three boundary-guarded regexes, proving themselves on three fixed probes each run"
      contains: "OVERLAY_PARTS"
  key_links:
    - from: "scripts/verify-dialogs.mjs"
      to: "src/components/ui/Dialog.tsx"
      via: "check B counts every shell declared outside the primitive; the primitive is the one implementation DS-08 asks for"
      pattern: "REMAINING"
    - from: "scripts/verify-dialogs.mjs"
      to: "scripts/conversion-manifest.mjs"
      via: "the fence declared here is compared glob-for-glob with the manifest's, and a drift refuses the run"
      pattern: "PHASE_42_PATHS"
---

<objective>
Close the fourth finding named in the gap-closure scope: `scripts/verify-dialogs.mjs:344`
recognises a hand-rolled overlay only when three literal substrings appear on one line, and the
third of them is one specific z rung. A nineteenth copy written at any other rung passes check B
in silence.

Purpose: check B is *"no second shell, except on the list"* — the mechanical half of DS-08's
claim that a dialog comes from **one** implementation. The eleven incumbent overlays happen to
carry the same rung, but the rung is not what makes something an overlay: `41-UI-SPEC.md` §10
keeps that rung declared for anything that must clear the navigation **without being modal**,
which is precisely why it is a poor signature. A copy written one rung up or down, or with a
different arbitrary rung, is invisible — and an invisible copy is the *"gate that went quiet
while the thing it tracked is still there"* shape this file's own header warns about, absent
from the four things it lists a green as not meaning.

There is a second, cheaper defect in the same three lines and it points the other way:
`line.includes` on the positioning utility also matches that utility inside a longer word, which
is a red on a correct file — the failure mode §0 rule 3 says gets a gate switched off.

**The fence comes first, and it is why this plan has three tasks instead of two.** Widening the
rung to a family, on this tree, reddens exactly one correct file: `src/components/scanner/ScanFlash.tsx:135`
carries the three parts on one line at the rung one above the incumbents'. It is the door's
accept/refuse flash — `role="status"`, `aria-live="assertive"`, dismissed by its own timer — and
it is **not a dialog**: there is nothing to trap focus for, nothing for Escape to close, and
converting it to the primitive would be wrong. It is also **Phase 42's**, fenced by
`41-CONTEXT.md` and by the manifest, and this gap-closure round opens no Phase 42 file for
anything beyond a read. So the honest order is: declare the fence, then widen the matcher behind
it. `scripts/verify-touch-targets.mjs:499-512` already carries exactly this fence, exactly this
way; this gate simply did not have one.

**What the fence is, and what it is not.** It is a **scope boundary, not an approval.** After
this plan, five files are unmeasured by check B — not declared correct. If a real hand-rolled
dialog is written behind the fence, this gate will be silent about it, and that silence is
deliberate and printed. The distinction is the same one this phase's gates already make between
a refusal and a pass: *not measured* is not *fine*.

Output: a Phase 42 path fence cross-checked against the manifest; behind it a matcher keyed on
the shape of an overlay rather than on one rung, bounded so it cannot fire inside a longer word,
proving itself on three fixed probes on every run; and three mutation cycles proving the fence,
the list and the self-check each still able to fail.

**What this does not close.** DS-08 remains PARTIAL and is not ticked. This gate reads class
strings and import clauses, never behaviour: whether Escape closes the panel, whether the sheet
rises from the bottom edge below 768px and whether the page behind it scrolls are **H41-2**, a
person at two widths, and research assumption **A2** — background scroll lock under
`showModal()` — is still explicitly open with nothing reported. Fourteen copies still stand on
`REMAINING`, and `RevealVenueDialog.tsx` — the interface of a monotone, irreversible guard — is
among them. Nothing here converts one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-shared-primitives-three-tier-layout/41-REVIEW.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-VERIFICATION.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-CONTEXT.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-09-SUMMARY.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
@CLAUDE.md
@.claude/rules/meta-gates.md
@.claude/rules/ai-engineering.md
</context>

<interfaces>
**The numbers this tree produces today, measured by running the gate before touching it:**

- `REMAINING entries declared` : **14**
- `files measured carrying a shell` : **14**
- `exempt from this check` : **1** — `src/components/media/Lightbox.tsx`, a full-bleed media
  viewer, declared an exception by §8.3 *before* this gate existed
- verdict: `DIALOGS_OK`, exit **0**

**What is behind the Phase 42 fence today, measured on this tree — five files:**

| File | What it is | What check B sees today | What it would see after the rung widens |
|---|---|---|---|
| `src/app/(admin)/admin/scanner/ScannerClient.tsx` | the scanner surface | nothing | nothing |
| `src/app/(admin)/admin/scanner/DoorSurface.tsx` | the door's surface | nothing | nothing |
| `src/app/(admin)/admin/scanner/page.tsx` | the door's first address | nothing | nothing |
| `src/app/(admin)/door/page.tsx` | the door's second address (STAFF-04) | nothing | nothing |
| `src/components/scanner/ScanFlash.tsx` | the accept/refuse flash at the door | nothing — it is written one rung above the incumbents' | **the three parts on line 135**, which is the red this fence exists to prevent |

No native `<dialog>` exists behind the fence, and **no `REMAINING` entry lives behind it** — all
fourteen are under the public surfaces, the admin event pages, and `components/{admin,venues,artists,media}`.
So the fence removes nothing from today's count: after the fence alone the measured count is
still **14** and no entry goes stale. That is the property task 1's acceptance asserts.

**`ScanFlash.tsx` is not a `REMAINING` candidate, and writing it onto the list would be false in
three ways.** `REMAINING`'s own header states that every entry *"is a dialog without a focus
trap"* — this one is a status flash with no modal semantics. Its target column would have to name
a Phase 42 surface, which is scope this round does not have. And the entry would read as a debt
somebody owes, when the correct outcome is that it stays exactly as it is.

**The mechanism to copy, and where it already lives.** `scripts/verify-touch-targets.mjs`
carries the identical fence: the declared constant with `[glob, reason]` at `:499-512`, the
`globToRegExp` helper at `:863-880`, the drift refusal against the manifest at `:936-945`, and
the printed block at `:951-959` that ends by naming what the silence costs. This gate copies all
four. The helper is copied rather than imported — *"self-contained is the house shape for a
gate"*, and the header's argument at `:147-155` is about importing a module that exits at module
scope, which `scripts/conversion-manifest.mjs` does not do (verify it by reading before relying
on it; `verify-touch-targets.mjs:892` already imports it).

**The convention this file already keeps, and which the new matcher must keep.** Utility class
names are never written as complete literals in the source — the incumbent builds them by
concatenation for a reason that is not style: a complete utility literal sitting in a scanned
file is a class string Tailwind can compile, and this phase has already emitted a malformed rule
from a comment (DEF-41-01). Every fragment of the new regexes is assembled the same way. The
same applies to the SUMMARY: `.planning/` is scanned too, so describe `ScanFlash.tsx:135` by
citing it, never by pasting its class string.

**Two declared exceptions, neither of which moves:** `Lightbox.tsx` is exempt from check B;
`MyMediaSection.tsx` is **not** exempt and is on `REMAINING`, and it is the reason the signature
is keyed on the shell rather than on the accessibility role.

**A limit this plan declares rather than closes.** A class string split across two lines by a
formatter is invisible to a line-oriented matcher, before and after this change. Matching across
lines is a different change with a different risk; the honest move is to add it to the list of
things a green does not mean, not to leave it unsaid.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fence Phase 42 by path, before anything widens</name>
  <files>scripts/verify-dialogs.mjs</files>
  <read_first>
    - `scripts/verify-touch-targets.mjs` — `PHASE_42_EXEMPT_PATHS` and the docblock above it at
      `:481-512`, `globToRegExp` at `:863-880`, the manifest drift refusal at `:936-945`, and the
      printed fence block at `:951-959`. This is the mechanism, verbatim in shape
    - `scripts/conversion-manifest.mjs` — `PHASE_42_PATHS` at `:149-183`, and enough of the file
      to confirm it does no work at module scope and calls no `process.exit` (the reason
      `verify-dialogs.mjs:147-155` refuses to import `verify-tokens.mjs` is a real hazard, and
      the check that it does not apply here is a read, not an assumption)
    - `scripts/verify-dialogs.mjs` — the refusal block at `:491-532`, check B's loop at
      `:614-644` with its two `continue` guards, and the header's account of the two declared
      exceptions at `:92-113`
    - `41-CONTEXT.md:24-27` and `:313-314` — the phase's own fence, in the words it was written in
    - `src/components/scanner/ScanFlash.tsx` in full — enough to state, in the docblock you write,
      what it actually is: a status flash with a dismissal timer, not a dialog
  </read_first>
  <action>
    Add an exported constant `PHASE_42_EXEMPT_PATHS` beside the two declared exceptions, holding
    the same three globs the sibling gate and the manifest hold — the scanner route, the
    scanner's components, and the door's second address — each with its reason on the line.

    Copy `globToRegExp` from the sibling gate as a local helper, and build the fence matcher from
    it. Then, before anything is measured, import `PHASE_42_PATHS` from
    `scripts/conversion-manifest.mjs` and compare the sorted globs with the local list. **A
    drift refuses at exit 2** — same message shape as the sibling's — because two lists that
    disagree mean one gate is guarding a path another one scans and neither report says so. A
    failed import also refuses: a fence that could not be cross-checked is a fence nobody
    verified.

    Apply the fence in **check B only**, as a third `continue` beside the primitive and the
    full-bleed viewer, and count what it skipped. **Checks A and C are deliberately not fenced,
    and the docblock says why:** check A reads the primitive itself; check C asks whether a
    dialog reports its outcome invisibly, which is a defect report, and a phase boundary is a
    reason not to *change* a file, never a reason to stay quiet about a silent failure
    (`meta-gates.md`, zero fallimenti silenziosi). Measured today: **no file behind the fence
    imports the primitive**, so check C's numbers are identical either way — record that in the
    docblock as measured, not assumed.

    Print the fence before check B's numbers, in the sibling's shape: the three globs with their
    reasons, the number of walked files that fall behind it, and a closing sentence saying what
    the silence costs. Add a report line — `fenced by path, never measured : N  (Phase 42)` —
    beside the existing exempt line, and make the difference between the two explicit in the
    docblock: the media viewer is a file **declared correct** as what it is; the fenced paths are
    files **nobody measured**. Collapsing an exemption and a fence into one word is how a scope
    boundary turns into an approval.

    Finally, extend the header's *"what a green does not mean"* list with the fence: five files
    at the door are not read by check B, and if a hand-rolled dialog is written there this gate
    will not say so.
  </action>
  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Before editing, the pre-change report was captured verbatim to a file and kept for task 2
    - `node scripts/verify-dialogs.mjs` exits **0** **on the live repository** — not on a probe,
      not on a copy
    - The report prints `REMAINING entries declared     : 14`, `files measured carrying a shell : 14`
      and `REMAINING = 14`, **unchanged from the pre-change report**. The fence must cost nothing
      today: if any number moved, the fence removed a file that was being measured, and that is a
      reconciliation to do before task 2 — not a delta to accept
    - No `REMAINING` entry became STALE (`! B` prints nothing), which is the same property stated
      from the other side
    - The report prints the fence block with all three globs and their reasons, and a count of
      **5** files walked behind it
    - `grep -c 'PHASE_42_EXEMPT_PATHS' scripts/verify-dialogs.mjs` returns at least **3**, and the
      file contains `export const PHASE_42_EXEMPT_PATHS`
    - The docblock states in words that the fence is a scope boundary and not an approval, and
      names `src/components/scanner/ScanFlash.tsx` as the file it keeps silent about
    - Checks A and C are untouched: the report still prints `✓ A` and `✓ C` with the same counts
      as the pre-change report
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The scanner and the door are outside check B by path, the fence is printed as unmeasured rather than approved, it agrees with the manifest or refuses, and it changed no number on this tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Match the rung as a family, bound the tokens, and make the matcher prove itself</name>
  <files>scripts/verify-dialogs.mjs</files>
  <read_first>
    - the file as task 1 left it — especially the header's list of what a green does **not** mean,
      which this task extends again, `OVERLAY_PARTS` and `shellShapes` at `:335-360`, the two
      declared exceptions at `:300-329`, and `REMAINING` with its written discipline at `:362-396`
    - `41-REVIEW.md` WR-09 in full
    - `41-UI-SPEC.md` §10, the z ladder — so the reason the rung is a poor signature is taken
      from the contract rather than from the review's summary of it
    - `scripts/verify-tokens.mjs:535-554` `consumerPattern()` — this repository's existing
      technique for boundary-guarding a utility name, which is what stops a match inside a
      longer word
  </read_first>
  <action>
    Turn `OVERLAY_PARTS` from three substrings into three anchored regexes, and **export** it so
    its behaviour is inspectable rather than inferred:

    - the positioning utility, guarded on both sides so it cannot match inside a longer word —
      the case WR-09 names is the same letters appearing at the end of an ordinary English word
    - the inset utility, guarded the same way
    - the z rung as a **family**: two-or-more digits, or an arbitrary bracketed integer, each
      boundary-guarded

    Assemble every regex source by concatenating fragments, exactly as the incumbent line does,
    so no complete utility literal appears in the file. Then `shellShapes` tests each line with
    `every((re) => re.test(line))` instead of `every((part) => line.includes(part))`.

    **Add a matcher self-check that runs on every invocation** and prints its result in the
    report, before check B's numbers. Three fixed probe strings, each assembled by concatenation
    like the regexes:
    - the three parts present, but with the positioning utility embedded at the end of a longer
      word → expected **no match**
    - the three parts present at a bracketed rung other than the incumbents' → expected **match**
    - the three parts present at a two-digit numeric rung → expected **match**

    If any probe disagrees with its expectation, `refuse()` — exit 2, nothing measured — because
    a matcher that does not behave as its own header describes has not measured the tree, it has
    measured something else. This is the file answering *"which concrete situation would trip
    it?"* on every run instead of once in a plan.

    **The probes are the matcher's self-description, not the acceptance evidence.** They are three
    strings this task wrote; they cannot discover a file this task did not think of. The evidence
    is the live run in the acceptance criteria below — and this exact defect is why: three probes
    of the same shape passed while the widened matcher reddened a correct file at the door.

    Rewrite the `OVERLAY_PARTS` docblock at `:335-343`. Keep the true sentence — each part alone
    is ordinary, and it is the three together on one line that make an overlay — and replace the
    rung claim: the rung is matched as a family because **§10 keeps that rung declared for
    non-modal elements that must clear the navigation**, so it is a property an overlay usually
    has and never the property that makes it one. Say that the boundary guards exist to prevent
    the opposite error, a red on a correct file, and that §0 rule 3 says that is the error that
    gets a gate switched off. Say also, in one sentence, that widening the rung is what made the
    Phase 42 fence necessary, and name the file — so a future reader finds the two changes
    connected rather than coincidental.

    Finally, add the line-splitting limit to the header's list of what a green does not mean.
  </action>
  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-dialogs.mjs` was run **against the actual repository working tree**
      and exits **0**. A pass observed only against the three synthetic probes, or against a
      copied or reduced tree, is **not sufficient evidence** and does not satisfy this criterion:
      the probes are written by the same hand as the matcher and share its blind spots
    - The report prints `REMAINING entries declared     : 14`, `files measured carrying a shell : 14`
      and `REMAINING = 14`, and `! B` prints no stale entry
    - The task-1 report and the task-2 report were diffed, and every difference is recorded —
      expected differences are the self-check block and the docblock text, and nothing else
    - **If the measured number rises, the new file is triaged before anything is edited**, and
      the three outcomes are not interchangeable:
      · it is a dialog, outside the fence → a copy the old matcher was missing. Put it on
        `REMAINING` with its reason and its target surface, record the decision in the SUMMARY,
        re-run
      · it is **not** a dialog (a status layer, a flash, a full-bleed viewer) → it is neither a
        `REMAINING` entry nor a reason to loosen the matcher. `REMAINING`'s header says every
        entry is a dialog without a focus trap, and a false entry is worse than no entry. It
        would need a **third declared exception**, which is a decision this plan does not
        pre-authorise: **stop, record the file and its lines in the SUMMARY, and report it**
      · it is behind the fence → impossible after task 1; if it happens, the fence is not
        applied where task 1 said it was, and that is the bug to fix
      In no case is the matcher loosened to make the number come back
    - The report contains a block headed with the matcher self-check and three rows whose
      verdicts read, in order, no-match, match, match
    - `grep -c 'OVERLAY_PARTS' scripts/verify-dialogs.mjs` returns at least **3**, and the file
      contains `export const OVERLAY_PARTS`
    - No complete utility literal appears in the new code: the fragments are concatenated, the
      same way `:344` already does it today. Inspect the added lines and record that they are
    - `grep -c 'line.includes' scripts/verify-dialogs.mjs` does not include the overlay matcher
      any more — the three parts are tested as regexes
    - Checks A and C are untouched: `git diff` over the file leaves their bodies alone, and the
      report still prints `✓ A` and `✓ C`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>An overlay is recognised by its shape at any rung, a longer word cannot trigger it, the matcher demonstrates both on every run, and the live tree still reconciles at fourteen.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Three mutations — the fence, the list, and the self-check</name>
  <files>scripts/verify-dialogs.mjs</files>
  <read_first>
    - the file as task 2 left it, and its exit-code convention: **1** is a red check, **2** is a
      refusal meaning nothing was measured, and the two are never merged
    - `.claude/rules/ai-engineering.md` — *Gate prova per mutazione*, including the requirement
      that the mutation is asserted to have landed **before** its result is read
    - `41-09-SUMMARY.md` — how the first version of this gate was proven
  </read_first>
  <action>
    Three cycles, none of them touching product code.

    **Mutation A — the fence must be load-bearing.** Comment out or empty the
    `src/components/scanner/**` entry of `PHASE_42_EXEMPT_PATHS`. Note that this trips the
    manifest drift refusal first, which is itself a correct behaviour and worth recording: to
    reach the matcher, mutate the manifest's copy in the same way, and assert **both** lines
    landed by printing them. Run: expect `src/components/scanner/ScanFlash.tsx` to appear under
    check B as an undeclared shell, at exit **1**. Restore both, assert by printing the lines
    again, run: exit **0** with `REMAINING = 14` and no scanner file named anywhere in the
    report. This is the cycle that proves the fence is doing work rather than decorating the
    output — and it is the only proof that the widened matcher would otherwise redden a correct
    file.

    **Mutation B — an undeclared copy must redden.** Remove one entry from `REMAINING` — choose
    one and name it in the SUMMARY. Assert the removal landed by grepping the file for that path
    and confirming zero hits. Run: expect exit **1**, with check B naming that file as a shell
    declared outside the primitive and not on the list. Restore the entry, assert the grep
    returns one hit, run: exit **0** with the count back at 14.

    **Mutation C — the self-check must refuse when the matcher stops matching its description.**
    Break one of the three regexes — the rung family is the one to break, since it is what this
    plan bought — so that one probe disagrees with its expectation. Assert the mutation landed by
    printing the mutated line. Run: expect exit **2**, a refusal, and **no** check-B verdict at
    all, because a matcher that failed its own probes has not measured the tree. Restore, assert,
    run: exit 0.

    Record all three cycles as assert → exit → assert → exit. A mutation whose landing was not
    asserted first is not a proof in either direction: the substitution that silently fails to
    apply is the same one that would certify a dead check as working, and this repository has
    already been bitten by both a `perl` substitution that did not match and a NUL byte that made
    a script binary to `grep`.
  </action>
  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"; git status --porcelain scripts/</automated>
  </verify>
  <acceptance_criteria>
    - Mutation A: both mutated lines (gate and manifest) were printed before the run; the run
      named `src/components/scanner/ScanFlash.tsx` under check B at exit **1**; after restoration
      both lines were printed again, the run exited **0**, printed `REMAINING = 14`, and no path
      under `src/components/scanner/`, `src/app/(admin)/door/` or `src/app/(admin)/**/scanner/`
      appears anywhere in the report except inside the fence block
    - Mutation A is written up in the SUMMARY in one sentence that says what it proves: without
      the fence, task 2's widened matcher reddens a correct, out-of-scope file — and that this is
      a scope boundary, not a judgement that the file is correct or incorrect
    - Mutation B: the removed path's grep count was shown to be **0** before the run; the run
      exited **1** and check B named that path; after restoration the grep count was **1**, the
      run exited **0** and printed `REMAINING = 14`
    - Mutation C: the mutated regex line was printed before the run; the run exited **2** with a
      refusal and printed no check-B tick; after restoration the run exited **0**
    - `git status --porcelain` shows no leftover mutation in `scripts/` and no change under `src/`
    - `npm run build` exits 0
    - The SUMMARY records all three cycles in the order assert → exit → assert → exit, and states
      in one sentence which copy the old matcher would have missed
  </acceptance_criteria>
  <done>The fence has been observed holding a correct file out of a check it would otherwise redden, check B has been observed reddening on an undeclared copy, and the self-check has been observed refusing on a matcher that stopped matching its own description.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → DS-08's mechanical half | check B is the only thing asserting that a second dialog shell has not been written; behaviour is H41-2 and is a person's |
| `REMAINING` → the fourteen copies still standing | the list is edited by decision; a copy that leaves it must have converted, and a copy that joins it must carry its reason |
| the Phase 42 fence → five files at the door | after this plan check B does not read them. They are **unmeasured, not approved**, and the two words are not interchangeable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-58 | Spoofing | a green that missed a copy written at another rung | mitigate | the rung is matched as a family, and the self-check demonstrates it against a fixed probe on every run |
| T-41-59 | Denial of Service | a red on a correct file, after which the gate is switched off | mitigate | both tokens are boundary-guarded; probe 1 asserts on every run that a longer word does not trigger the matcher; and the one correct file the widened rung would have reddened — the door's status flash — is fenced by path before the widening lands, proven by mutation A |
| T-41-60 | Tampering | a nineteenth hand-rolled overlay on a money or venue surface slipping past | mitigate | the shape, not the rung, is the signature; two of the fourteen on `REMAINING` are refund paths and one is `RevealVenueDialog`, the interface of a monotone guard — and this gate is what keeps that count honest |
| T-41-63 | Tampering | **a weakened gate lets a future change ship unnoticed** — a real hand-rolled dialog written behind the fence, on the door, is invisible to check B forever | accept, and stated plainly | this is what a fence costs and the cost is not hidden: the three globs and the five files are printed on every run with the sentence that they were never measured; the header records it among the things a green does not mean; the alternative — a `REMAINING` entry characterising and targeting a scanner surface — would be a false statement (that file is not a dialog) plus new scope in a round whose scope is four items. Phase 42 inherits the obligation, and this plan's SUMMARY is where it is written down |
| T-41-64 | Spoofing | the fence read as an approval of the door's markup | mitigate | the report distinguishes the media viewer (**declared correct** as what it is) from the fenced paths (**nobody measured them**) in the report text and in the docblock; the plan says it, the SUMMARY repeats it |
| T-41-65 | Tampering | this gate's fence and the manifest's drifting apart, so one gate guards a path another scans | mitigate | the globs are compared sorted before anything is measured and a drift refuses at exit 2, the same mechanism `verify-touch-targets.mjs:936-945` uses; a failed import of the manifest also refuses |
| T-41-61 | Repudiation | DS-08 read as closed | accept | recorded here and in the SUMMARY: fourteen copies still stand, the behavioural half is **H41-2**, and assumption **A2** is still open with nothing reported |
| T-41-62 | Information Disclosure | what a role or status can see | accept | the gate reads source files, executes no product code and touches no data path. Honestly thin |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | **no packages** (D-41-20) |
</threat_model>

<verification>
- `node scripts/verify-dialogs.mjs` runs **on the live repository** and exits 0 with
  `REMAINING = 14`, no stale entry, and the fence block printed with three globs and five files
- The task-1 run and the task-2 run each reconcile at 14, and their reports were diffed
- The matcher self-check prints three rows and refuses when any disagrees
- All three mutation cycles observed, each with its mutation asserted present before its result
  was read, and mutation A observed naming the door's status flash only while the fence is removed
- `npm run build` exits 0 and `git status --porcelain` is clean of mutations
</verification>

<success_criteria>
- The scanner and the door are fenced out of check B by path, printed on every run as unmeasured
  rather than approved, and the fence agrees with the conversion manifest or the run refuses
- The overlay matcher is keyed on the shape of an overlay at any z rung, with both tokens bounded
- The matcher proves itself on three fixed probes every run and refuses when it disagrees, and
  the live-tree run — not the probes — is what the acceptance rests on
- Checks A and C are untouched, and the two declared exceptions are unchanged
- The header records the fence and the line-splitting limit among the things a green does not mean
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-16-SUMMARY.md` when done.
Record: the pre-change, post-task-1 and post-task-2 reports with their diffs; the three probes and
their verdicts; all three mutation cycles in full; one sentence stating that the Phase 42 fence is
a scope boundary and not an approval, naming the five files it silences and the one it silences
today; whether the measured count stayed at 14 and, if not, every file added to `REMAINING` with
its reason or reported as needing a decision; and one sentence stating that DS-08 is still PARTIAL
because H41-2 and assumption A2 are unobserved. Cite the door's status flash by path and line —
never by pasting its class string, since `.planning/` is scanned by Tailwind (DEF-41-01).
</output>
