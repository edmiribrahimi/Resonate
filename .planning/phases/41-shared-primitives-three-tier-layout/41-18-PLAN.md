---
phase: 41-shared-primitives-three-tier-layout
plan: 18
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - scripts/verify-dialogs.mjs
  - scripts/conversion-manifest.mjs
autonomous: true
requirements: [DS-08]

must_haves:
  truths:
    - "A REMAINING entry that falls behind the Phase 42 fence stops the run instead of being reported STALE, and the debt count no longer shrinks because the gate stopped looking"
    - "The sentence check B prints about its own rung matcher is true of the regex that matcher actually runs"
    - "Each of the two changes was proven by mutating the trigger its own report names, and the trigger mutated is written down in the SUMMARY"
  artifacts:
    - path: "scripts/verify-dialogs.mjs"
      provides: "a refusal on REMAINING ∩ fence overlap, raised before check B prints anything; a rung family that covers what the report claims"
      contains: "fencedRemaining"
  key_links:
    - from: "REMAINING"
      to: "PHASE_42_EXEMPT_PATHS"
      via: "an overlap refusal raised before measurement"
      pattern: "fencedRemaining"
    - from: "RUNG_FAMILY"
      to: "the printed claim at check B's matcher self-check"
      via: "a matcher whose family is not narrower than the sentence describing it"
      pattern: "RUNG_FAMILY"
---

<objective>
Close two of the six warnings the gap review raised against `scripts/verify-dialogs.mjs`.

**WR-02** — a `REMAINING` path that also matches the Phase 42 fence is skipped before
measurement, then reported `! B … STALE — converted; remove this entry`, and
`REMAINING = measuredShells.size` drops by one. A debt counter falling because the
gate stopped looking is the "proxy goes quiet" defect this phase has already paid for
once, now sitting inside the fence this phase just added. Zero overlap today; live the
moment Phase 42 moves or adds a dialog under `src/components/scanner/**` or
`src/app/(admin)/door/**`.

**WR-03** — the matcher self-check prints *"The rung is matched as a FAMILY, so a copy
at any rung is seen"*, while `RUNG_FAMILY` matches two-or-more digits or a bracketed
integer only. `z-0`…`z-9` and `z-auto` are real utilities and none of them matches.

Purpose: a fence must stay a scope boundary and never read as an approval, and a
printed claim is what a reader believes — this repository's own standard.

Output: `scripts/verify-dialogs.mjs` with an overlap refusal and a rung family that is
not narrower than its own sentence, each proven by mutating the trigger its report names.

**This plan closes no requirement.** DS-08 stays PARTIAL: its runtime half — a dialog
observed opening as a sheet and closing with Escape — is H41-2 and is still owed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-16-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Read from the shipped gate before editing. Do not re-derive these from memory.

- `PHASE_42_EXEMPT_PATHS` — `[glob, reason][]`, exported. Compared sorted against
  `PHASE_42_PATHS` in `scripts/conversion-manifest.mjs` **before anything is measured**;
  a drift is a refusal. Consequence for every mutation in this plan: a fence glob added
  to one copy and not the other trips the drift refusal first and never reaches the code
  under test. This is the exact trap recorded as mutation A in `41-16-SUMMARY.md`.
- `REMAINING` — `[path, reason, target][]`, 14 entries. `declaredPaths` is the Map built
  from it.
- `fenceMatch(relPath)` → `{glob, reason} | null`.
- `measuredShells` — Map of file → shell hits, populated by check B's loop, which
  `continue`s on a fenced file before calling `shellShapes`.
- `stale` — declared paths that exist on disk and are not in `measuredShells`.
- `RUNG_FAMILY`, `OVERLAY_PARTS`, `isOverlayLine`, `MATCHER_PROBES`, `probeRows`,
  `probeDisagreements`, `refuse(message)` → exit 2.

Measured baseline on this tree, 2026-08-12, before any change:
`node scripts/verify-dialogs.mjs` → exit 0, `REMAINING entries declared : 14`,
`REMAINING = 14`, `fenced by path, never measured : 5`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: a REMAINING path behind the fence refuses instead of reporting STALE</name>

  <read_first>
    - `scripts/verify-dialogs.mjs` — the fence declaration and its docblock (the paragraph
      that separates *fenced* from *exempt*), `fenceMatch`, check B's loop, the `stale`
      computation, the fence report block, and the `REMAINING =` line.
    - `scripts/conversion-manifest.mjs` — `PHASE_42_PATHS`, the other half of the
      cross-checked fence.
    - `41-GAP-REVIEW.md` WR-02, including its suggested refusal text.
    - `41-16-SUMMARY.md` mutation A — why a one-sided fence mutation never reaches the
      thing under test.
  </read_first>

  <files>scripts/verify-dialogs.mjs</files>

  <action>
    Add a refusal that fires when any declared `REMAINING` path matches the Phase 42
    fence. Compute it from `declaredPaths` and `fenceMatch`, name it `fencedRemaining`,
    and raise it **before check B measures anything** — before the loop that populates
    `measuredShells`, so no tick and no count can be printed on a run whose lists
    contradict each other. Reuse `refuse()`; the exit stays 2.

    The refusal text says what the review's fix says and says it in this file's own
    register: a fenced path is UNMEASURED, so this gate cannot tell a paid debt from one
    it never opened, and it would report the second as the first. Either the entry leaves
    `REMAINING` as a declared decision or the fence does — both are decisions, and which
    one is a question for a person. End with "Nothing was measured."

    Extend the docblock that already separates *fenced* from *exempt* with one paragraph
    recording why the overlap is a refusal rather than a warning, and naming the arrival
    condition: Phase 42 moving or adding a dialog under the scanner or door globs.

    Do not touch `stale` for the non-overlapping case: a genuinely converted entry stays
    a loud `! B` warning and not a failure (§0 rule 3).
  </action>

  <mutation_contract>
    **Documented trigger, in the file's own words:** the fence report already says *"This
    is a SCOPE BOUNDARY, not an approval… If a hand-rolled dialog is written behind that
    fence, check B is silent about it."* The trigger is a path that is on `REMAINING`
    **and** behind the fence.

    **Mutate that trigger, at the site where a real regression appears — the two fence
    lists, in lockstep.** Choose one existing `REMAINING` entry (record which) and add a
    glob covering exactly that one path to `PHASE_42_EXEMPT_PATHS` **and** to
    `PHASE_42_PATHS` in `scripts/conversion-manifest.mjs`. One-sided mutation trips the
    drift refusal and proves nothing; assert the mutation is present in **both** files by
    grep before reading any result.

    Order of evidence, all four steps recorded:
    1. **Pre-fix behaviour** — apply the lockstep mutation to the *shipped* gate, before
       this task's edit. Expected and asserted: exit 0, a `! B` line naming that path as
       STALE with `→ converted; remove this entry`, and `REMAINING = 13`. Revert.
    2. **Post-fix** — apply this task's edit, re-apply the same lockstep mutation.
       Expected and asserted: exit 2, a FATAL naming the overlapping path, the word STALE
       absent for it, and no `✓ B`.
    3. **Negative control** — revert both fence mutations. Expected and asserted: exit 0,
       `REMAINING = 14`, `fenced by path, never measured : 5`.
    4. **Restoration asserted, not assumed** — `git status --porcelain` empty of the
       mutation in both files; `grep -c` for the injected glob returns 0 in both.

    The SUMMARY records, in one sentence each: which trigger was mutated, that it was the
    trigger the report names, that both fence copies were mutated in lockstep and why,
    and the four exit codes.
  </mutation_contract>

  <acceptance_criteria>
    - `scripts/verify-dialogs.mjs` contains a `fencedRemaining` computation and a
      `refuse()` call site whose line number is **lower** than the line that first writes
      to `measuredShells` — assert by comparing the two line numbers from `grep -n`.
    - `node scripts/verify-dialogs.mjs` exits 0 on the unmutated tree, prints
      `REMAINING = 14` and `fenced by path, never measured  : 5`.
    - With the lockstep fence mutation applied: exit code 2, and the injected path appears
      in the FATAL text.
    - With the lockstep fence mutation applied to the shipped gate (step 1): exit code 0
      and `REMAINING = 13` — the defect reproduced before it was fixed.
    - After revert: `git status --porcelain` prints no line for
      `scripts/conversion-manifest.mjs`.
    - The docblock names the arrival condition (Phase 42 under the scanner or door globs).
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"</automated>
  </verify>

  <done>
    A REMAINING path pulled behind the fence stops the run with a refusal naming it; the
    same path on the shipped gate was observed shrinking the count to 13; the unmutated
    tree still exits 0 at REMAINING = 14; the tree is byte-identical to before the
    mutations.
  </done>
</task>

<task type="auto">
  <name>Task 2: the rung family covers what the report claims it covers</name>

  <read_first>
    - `scripts/verify-dialogs.mjs` — `LEFT_BOUNDARY`, `RIGHT_BOUNDARY`, `POSITION_UTILITY`,
      `INSET_UTILITY`, `RUNG_PREFIX`, `RUNG_FAMILY`, its docblock (*"Two-or-more digits, or
      an arbitrary bracketed integer"*), `OVERLAY_PARTS`, `isOverlayLine`,
      `MATCHER_PROBES`, and the printed sentence under the matcher self-check.
    - `41-GAP-REVIEW.md` WR-03, both options it offers.
    - `41-16-SUMMARY.md` — why the fence had to land before the previous widening, and why
      probes are never the evidence.
  </read_first>

  <files>scripts/verify-dialogs.mjs</files>

  <action>
    **Decision, and this plan states it rather than leaving it to the executor: widen the
    matcher.** The alternative — narrowing the sentence — was weighed and rejected because
    the delta on this tree is measured zero: every overlay line under `src/` that carries
    all three parts uses a bracketed rung (`z-[60]` ×12, `z-[100]` ×4, `z-[70]` ×1); no
    single-digit and no `auto` rung exists anywhere. Widening therefore makes the printed
    claim true at zero cost in red, which is the direction that leaves a reader believing
    something correct.

    Rebuild `RUNG_FAMILY` from concatenated fragments as today — no complete utility as a
    literal, DEF-41-01 — so it covers one-or-more digits, the `auto` keyword, and an
    arbitrary bracketed value with no whitespace inside. Keep both boundary guards
    untouched: they are what stops a longer English word reddening a correct file, and
    that regression cost this phase a plan already.

    Add two probes to `MATCHER_PROBES` — a single-digit rung and the `auto` keyword, both
    expected to match — following the existing record shape exactly, with `verdict` and
    `expected` both present and agreeing. WR-04 (the dead `expected` field) is out of
    scope for this round: do not restructure the probe record.

    Rewrite the printed sentence so it is true of the regex that runs: name what the
    family now covers, and name what it still does **not** see — a rung reached through a
    variable, and a class string built by concatenation. Do not leave the claim wider than
    the matcher, and do not leave it vaguer than the matcher either.

    **If the widened matcher moves the count off 14, do not bend either side.** A delta is
    a copy the previous matcher was missing. Record every newly matched line as
    `file:line`, verbatim, and stop: whether a given line is a dialog shell or a status
    layer is a declared decision (the fence exists because exactly that question already
    had a wrong answer once), and a planner-issued number is not authority to add a
    `REMAINING` entry.
  </action>

  <mutation_contract>
    **Documented trigger, in the review's own words:** *"A nineteenth hand-rolled overlay
    written `fixed inset-0 z-0` — or `z-auto` — is invisible to check B exactly as `z-50`
    was before this change."*

    **Mutate that trigger, at the site where a real regression appears — a live JSX line
    in a scanned file, not a probe string.** Create a temporary component file under
    `src/components/ui/` carrying one element whose class string holds all three parts at
    a single-digit rung, and a second at `auto`. It must not be behind the fence and must
    not be on `REMAINING`, so the correct verdict is an **undeclared** copy. Assert the
    file is present before reading any result. Do not run `npm run build` while it exists
    — the class string is a live Tailwind candidate (DEF-41-01) and this file is
    transient.

    **The temporary file must nevertheless be valid TypeScript that typechecks**, because
    plan 41-19 runs in this same wave and runs `npm run build`: a probe that breaks a
    concurrent build would redden a plan that touched nothing. A default-exported
    component taking no props, importing nothing, returning one element carrying the class
    string, is enough — and it is why the probe lives under `src/components/ui/` rather
    than under `src/app/`, where it would also become a route.

    1. **Pre-fix** — with the shipped matcher, expected and asserted: exit 0, `✓ B`,
       `REMAINING = 14`. The copy is invisible; that is the defect.
    2. **Post-fix** — with the widened family, expected and asserted: exit 1, `✗ B`, the
       temporary file named under "declare a dialog shell and are not on REMAINING", with
       its line number and the matched source.
    3. **Negative control** — delete the temporary file. Expected and asserted: exit 0,
       `✓ B`, `REMAINING = 14`, and `git status --porcelain` empty.

    The SUMMARY records which rung shapes were mutated, that the mutation was a live class
    string and not a `MATCHER_PROBES` entry, and the measured delta on the real tree
    (expected 0 — say so with the number, not with the word "unchanged").
  </mutation_contract>

  <acceptance_criteria>
    - `grep -v '^ \*' scripts/verify-dialogs.mjs | grep -c 'RUNG_FAMILY'` is at least 2
      (declaration and use), counted on comment-stripped lines.
    - `node scripts/verify-dialogs.mjs` exits 0 and prints `REMAINING = 14` on the
      unmutated tree — the count reconciled against the pre-change baseline of 14, with
      any delta reported line by line and the plan stopped rather than the count adjusted.
    - The matcher self-check prints five probe rows, all agreeing (`probeDisagreements`
      empty), including one single-digit rung and one `auto`.
    - The printed sentence names at least one shape the matcher does not see.
    - `MATCHER_PROBES` entries all carry both `verdict` and `expected`, and they agree —
      assert by reading the block, not by running.
    - With the temporary overlay file present: exit 1 and the file named in the `✗ B`
      block. Without it: exit 0.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"; git status --porcelain</automated>
  </verify>

  <done>
    A hand-rolled overlay at a single-digit or `auto` rung is seen by check B and named as
    undeclared; the printed sentence describes the regex that runs, including what it
    still misses; the tree exits 0 at REMAINING = 14 with no product file changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan changes one developer-run script. It runs on a developer machine, reads source files, writes stdout and an exit code. No request, no session, no database row, no rendered surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-D1 | Information disclosure | `verify-dialogs.mjs` report | accept | The report prints repository-relative paths and line numbers only. No member data, no venue, no date, no credential. Unchanged by this plan. |
| T-41-D2 | Elevation of privilege | role/status visibility | accept | Nothing here alters what any role or status can see: no route, no policy, no component, no query is touched. `files_modified` contains two scripts and no file under `src/` or `supabase/`. |
| T-41-D3 | Tampering | **a weakened gate ships a regression unnoticed** | mitigate | The subject of this round. Two mechanisms: (a) the overlap refusal is raised *before* `measuredShells` is written, so no count and no tick can be printed on contradictory lists — asserted by line-number comparison, not by reading; (b) each change carries a mutation contract naming the trigger its own report claims to catch, mutated at a live site, with the restoration asserted. The widening direction was chosen only after the delta was measured at zero, so it cannot redden a correct file on this tree. |
| T-41-D4 | Tampering | transient mutation left behind | mitigate | Every mutation is asserted present before its result is read and asserted absent after: `git status --porcelain` empty and `grep -c` returning 0 in both fence copies. The temporary overlay file is deleted in the same task that creates it. |
| T-41-SC | Tampering | supply chain | accept | No package is installed by this plan. D-41-20 forbids new packages in this phase; the legitimacy gate has nothing to audit here. |
</threat_model>

<verification>
- `node scripts/verify-dialogs.mjs` exits 0, `REMAINING = 14`, `fenced by path, never measured : 5`.
- `git status --porcelain` lists only `scripts/verify-dialogs.mjs`.
- `npm run build` exits 0 (no product file was changed; this is the repository's only
  typecheck and it is run because a temporary file lived under `src/` during task 2).
- The SUMMARY names, per change, the trigger mutated and the four exit codes observed.
</verification>

<success_criteria>
- A `REMAINING` path behind the Phase 42 fence stops the run at exit 2 naming the
  overlap, and the same path on the shipped gate was observed reporting STALE and
  shrinking the count to 13.
- `RUNG_FAMILY` matches one-or-more digits, `auto`, and an arbitrary bracketed value; a
  live overlay line at `z-0` reddens check B and is named as undeclared.
- The printed matcher sentence is true of the regex and names a shape it still misses.
- No requirement is ticked. DS-08 stays PARTIAL.
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-18-SUMMARY.md` when done.
It MUST contain a section titled "The trigger each mutation used" with one row per change:
the trigger named in the check's own docblock or report, the site mutated, the exit code
before, the exit code after, and the restored exit code.
</output>
