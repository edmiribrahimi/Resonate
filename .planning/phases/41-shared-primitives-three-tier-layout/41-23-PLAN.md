---
phase: 41-shared-primitives-three-tier-layout
plan: 23
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-conversion.mjs
  - src/components/ui/PageShell.tsx
autonomous: true
gap_closure: true
requirements: [RESP-01, RESP-02]

# src/components/ui/PageShell.tsx is listed because task 2 mutates it. Every
# mutation is TRANSIENT and restored byte-for-byte; the plan's end state leaves
# `git diff -- src/components/ui/PageShell.tsx` empty. No other plan in wave 1
# may touch that file.

must_haves:
  truths:
    - "Reintroducing CR-01 on ANY line of PageShell's focus branch — not only the outer element, not only the inner one — takes scripts/verify-conversion.mjs to a non-zero exit with no ✓ E printed"
    - "A line inside the focus branch can never be counted as evidence that a navigation property 'still survives elsewhere'"
    - "The single className={FOCUS_ROOT} occurrence is asserted to sit INSIDE the width === \"focus\" branch, not merely somewhere in the file"
    - "The gate still exits 0 on the unmodified tree, with all five checks green"
    - "Every refusal the region read can raise is raised before any verdict line prints"
  artifacts:
    - path: "scripts/verify-conversion.mjs"
      provides: "check E1's focus-branch region: derivation, three refusals, the in-region absence assertion, and the exclusion of the region from propertyReadsElsewhere"
      contains: "FOCUS_BRANCH_OPEN_RE"
    - path: ".planning/phases/41-shared-primitives-three-tier-layout/41-23-SUMMARY.md"
      provides: "the mutation matrix: one row per line of the derived region, its variant, its exit code, its verdict line, and the restore assertion"
      contains: "region line(s) mutated"
  key_links:
    - from: "src/components/ui/PageShell.tsx (every line of the width === \"focus\" branch)"
      to: "scripts/verify-conversion.mjs check E1"
      via: "a bounded region assertion over NAV_PROPERTIES, plus the same region excluded from the 'read elsewhere' evidence"
      pattern: "focusBranchRegion"
---

<objective>
Close GAP-CR-02 on its third attempt, by stopping the fix from being aimed at
the place the defect was last seen.

Round 1 (plan 41-17) asserted on the `FOCUS_ROOT` **constant**; the defect moved
to the render site. Round 2 (plan 41-20) asserted on the **outer element** of the
focus branch; the defect moved to the inner element. Both times the check was
written one level narrower than the defect, and both times the reintroduced line
was counted by `propertyReadsElsewhere` as proof that "the shell still reads both
properties elsewhere" — the defect satisfying the assertion meant to catch it.

Asserting on a POINT while the defect lives in a REGION fails the same way every
time. This plan asserts over the **region**: the whole `width === "focus"` branch,
every line of it, and excludes that region from the evidence that currently
accepts it.

The acceptance criterion matches. It is not "prove by mutation" — that sentence
has already been satisfied twice by a mutation aimed at the wrong element. It is
**one mutation per line of the derived region, enumerated, with the count
recorded, and every one of them red.**

Purpose: the guard against CR-01 has been declared closed twice and found open
twice. A green gate that cannot fail on a realistic reintroduction reads as
guarded when it is not.
Output: `scripts/verify-conversion.mjs` with a region-bounded check E1, and a
SUMMARY carrying the per-line mutation matrix.

**This plan observes no screen.** RESP-01 and RESP-02 stay PARTIAL; RESP-01
closes only after 41.2. H41-1…H41-6 and the thirteen `pending` rows of
`41-CR01-PASS.md` are untouched and remain owed in full.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.claude/rules/meta-gates.md

**The three constraints this repository makes true, restated because each has
bitten inside this phase:**

1. **There is no test runner for the product** (Guardrail 1, D-41-20). No
   acceptance criterion below is "tests pass". The proofs here are exit codes,
   exact stdout strings, and source assertions.
2. **DEF-41-01 — Tailwind compiles class strings from `.planning/` and from
   comments.** Never run `npm run build` while a mutation carrying a utility
   literal is present in a product file. Run it once at the end, with everything
   restored, and **again after the SUMMARY is written**, because `.planning/` is
   scanned.
3. **Line numbers in `PageShell.tsx` have moved twice in this phase** (the render
   site was `:136`, then `:153`). **Every mutation is applied by matching the
   line's TEXT, never its number.** Plan 41-20 recorded a substitution that
   silently did not apply; assert each mutation present by grep before reading a
   result, and absent afterwards.

`src/components/ui/PageShell.tsx` renders `(auth)`'s three surfaces —
`access-gating` primary — and `/payment/callback` — `ticketing-payments`
primary. Every transient mutation of it is restored byte-for-byte with
`git status --porcelain` asserted to carry no line for it.
</context>

<interfaces>
The shape of the branch this plan bounds, as it stands today (line numbers are
indicative only — derive by text):

- `src/components/ui/PageShell.tsx:125` — `const FOCUS_ROOT = "…"` (the constant, read by E1 today)
- `:151` — `if (width === "focus") {` — the branch opener
- `:152` — `return (`
- `:153` — the outer element, `className={FOCUS_ROOT}` — guarded since 41-20
- `:154` — the inner element, a template literal carrying `w-full max-w-sm` and the `className` prop — **the gap**
- `:155` — the outer element's close
- `:156` — `);`
- `:157` — `}` — the branch's closing brace

Existing identifiers in `scripts/verify-conversion.mjs` this plan builds on:
- `SHELL_FILE` (:749), `FOCUS_ROOT_IDENTIFIER` (:786), `NAV_PROPERTIES` (:797)
- `liveLines(relPath)` (:381) — comment-stripped, **line-count preserving**: a
  blanked line is pushed as `''`, so index arithmetic against the real file holds
- `shellLines`, `focusRootLineNo`, `focusRoot` (:1239-1280)
- `FOCUS_BRANCH_RE` (:1301), `focusBranchLines` (:1302)
- `propertiesInFocusRoot` (:1819), `propertyReadsElsewhere` (:1823-1829),
  `propertiesDroppedEntirely` (:1830)
- `refuse(message)` (:298) — exits 2, or 1 when `failures` is non-empty
- first `failures.push(` is at :1485; every refusal that must precede a tick sits below that number
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: check E1 bounds the focus branch as a region, and the region stops feeding assertion 3</name>

  <read_first>
    - `scripts/verify-conversion.mjs:236-320` — the exit-code header (what a 1 and a 2 mean, and the mixed case) and `failures` / `refuse()`, whose invariant task 1 must not break
    - `scripts/verify-conversion.mjs:740-800` — `SHELL_FILE`, `FOCUS_ROOT_IDENTIFIER`, `NAV_PROPERTIES`, and the "a marker beats a heuristic" rule at :786
    - `scripts/verify-conversion.mjs:376-410` — `liveLines`, to confirm it preserves line count when it blanks a comment
    - `scripts/verify-conversion.mjs:1180-1330` — E1's hoisted read: the three declaration refusals, `FOCUS_ROOT_LITERAL_RE`, `FOCUS_BRANCH_RE` and its refusal text (the documented trigger of each)
    - `scripts/verify-conversion.mjs:1802-1930` — E1's comparisons, the printed report block, and the three ✗ E verdicts
    - `src/components/ui/PageShell.tsx` — the whole file; in particular the docblock at :69-79 (why the padding is on the outer element, and the 112px it names) and the branch at :151-157
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-17-SUMMARY.md` — round 1's attempt: it asserted the CONSTANT
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-20-SUMMARY.md` — round 2's attempt: it asserted the OUTER element, and its trigger table shows a proof whose site was one element narrower than the defect
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-VERIFICATION.md` — the `gaps:` block: the reproduction, and the four items under `missing:`
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW-2.md` — critical CR-01, including option (a) at :119-133
  </read_first>

  <files>scripts/verify-conversion.mjs</files>

  <action>
Rewrite check E1's hoisted read so that what it asserts is the focus **branch**,
bounded as a region, instead of one constant and one className.

**1. Derive the region, anchored on the branch — not on the render site.**
Add `FOCUS_BRANCH_OPEN_RE`, a marker matching the branch opener (the
`width === "focus"` comparison, tolerant of whitespace and of either quote
style), scanned over `shellLines`. Anchoring on the opener rather than on the
`className={FOCUS_ROOT}` line is what also closes the third item under the
verification's `missing:` — today `FOCUS_BRANCH_RE` requires exactly one
`className={FOCUS_ROOT}` **anywhere in the file** and never that the occurrence
sits inside the focus branch, so `FOCUS_ROOT` rendered on the default branch
with the focus branch carrying an arbitrary class string satisfies it.

Compute the region's end by **brace balance** from the opener line — counting
`{` and `}` over the comment-stripped lines and taking the line at which the
running balance returns to zero. Do **not** use a bare-closing-brace regex
(`/^\s*\}\s*$/`, the shape sketched in 41-GAP-REVIEW-2.md option (a)): a bare
brace can appear inside the branch, the region would then terminate early, and a
region shorter than the branch is this round's defect for the third time. Record
that reasoning in the docblock, because it is the exact trim a later reader will
be tempted to make.

**2. Three refusals, all raised inside the hoisted read** so task 41-20's
invariant survives — every check-E refusal precedes every tick:
  - the opener is found zero times, or more than once → refuse, nothing measured
  - the balance never returns to zero before end of file → refuse, "the focus
    branch has no readable end, so nothing was measured"
  - the single `className={FOCUS_ROOT}` occurrence falls **outside** the derived
    region → refuse

Each refusal text carries the three things the `FOCUS_BRANCH_RE` refusal already
carries (what was measured and found, why it is a refusal and not a failure, and
what a legitimate change does), because a refusal that fires on correct code
without saying what to do is how a gate gets switched off — §0 rule 3, and
D-41-19's second failure mode. **Name, in the refusal text, the legitimate future
forms that would trip it**: the branch rewritten as a ternary, the branch
extracted into its own component, or the opener written as part of a compound
condition. In each case the instruction is the same one 41-20 wrote: the
assertion **widens in the same commit, carrying the measurement that justified
it** — it does not get deleted, and the branch does not get inlined to make it
quiet.

**3. The assertion the two previous rounds did not make.** Add
`propertiesInFocusBranch`: for every line of the region, and for both entries of
`NAV_PROPERTIES`, collect the line numbers on which the property text appears.
Non-empty → push `E` onto `failures` and print a `✗ E` verdict that names each
property, each line number, and each offending line verbatim, and repeats the
measured consequence the existing CR-01 verdict already states (248px leading
against 24px trailing at and above 768px, a centred card 112px right of the
viewport centre; ~96px of bottom padding below 768px under a card with no bar
beneath it).

Keep the existing `propertiesInFocusRoot` assertion on the constant. It is not
redundant: the constant is declared outside the branch, so the region does not
cover it.

**4. The exclusion — the half that stops the defect certifying itself.** In
`propertyReadsElsewhere`, skip every line whose number falls inside the derived
region, in addition to the `focusRootLineNo` it already skips. A clearance added
anywhere in the branch must never be counted as the clearance surviving
"elsewhere". Assert that `propertiesDroppedEntirely` still evaluates over the
narrowed set: on today's tree the default/wide branch still reads both
properties, so it stays empty and the assertion that the clearance did not leave
the primitive still holds.

**5. The report tells the truth about what it bounded.** The printed evidence
block currently says "outside the focus root". Change it to "outside the focus
**branch**", and print, above the focus-root line: the region's first and last
line numbers, the count of lines in it, the opener line verbatim, the closing
line verbatim, the line at which `FOCUS_ROOT` is rendered, and the count of
navigation properties found inside the region. A silently truncated region must
be visible in the gate's own output — that is the property no previous round's
report had.

Do not touch `MIN_HEIGHT_RE` or `CENTRING_RE`. Their weakness — assertion 2
being satisfied by forms that do not produce what it defends, named in
41-20-SUMMARY.md and in 41-GAP-REVIEW.md CR-02 — is **not subsumed by this
region fix**: the region bounds where the properties may not appear, and says
nothing about what the constant's height and centring utilities actually
produce. It stays open, out of scope this round, and is named here so it is not
lost.
  </action>

  <acceptance_criteria>
    - `node scripts/verify-conversion.mjs` on the unmodified tree exits **0** and prints five ticks ending `CONVERSION_OK — all five checks passed over 8 declared surface(s)`
    - The printed check-E block names the derived region by first line, last line and line count, and the region's line count is **7** on today's tree (the opener through its closing brace); the opener line and the closing line are printed verbatim
    - The printed evidence line for each of the two navigation properties reads "outside the focus **branch**" and lists **only** the two lines of the default/wide branch — the line carrying the inline-start read and the line carrying the block-end read. Neither of the two focus-branch element lines appears
    - `grep -n 'refuse(' scripts/verify-conversion.mjs` — every call site added by this task has a line number **lower** than the first `failures.push(` line number, asserted by comparing the two numbers in the SUMMARY
    - The region end is computed by brace balance, not by a bare-closing-brace match: `grep -n 'focusBranchRegion\|FOCUS_BRANCH_OPEN_RE' scripts/verify-conversion.mjs` returns the derivation, and no `/^\s*\}\s*$/` literal is introduced
    - `npm run build` exits **0**, run with the tree restored (never while a product-file mutation is present)
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>

  <done>The gate exits 0 on the clean tree; check E's report names the bounded region and its two evidence lines are the default/wide branch's only; every new refusal precedes every tick.</done>
</task>

<task type="auto">
  <name>Task 2: the mutation matrix — one variant per line of the region, every one red, the count recorded</name>

  <read_first>
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-VERIFICATION.md` — the "Behavioral Spot-Checks" table: the exact mutation that produced exit 0 with ✓ E, and the two-cycle mutation protocol (`git status --porcelain` clean before and after each)
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-20-SUMMARY.md` — "The trigger each mutation used", and the recorded failure in which a `perl -0pi` substitution silently did not apply, which is why every mutation is asserted present by grep before its result is read
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-17-SUMMARY.md` — the round whose proof mutated the constant the check already read, so the proof did not match the claim
    - `src/components/ui/PageShell.tsx` — the branch, to enumerate its lines by text
    - `scripts/verify-conversion.mjs` — the region derivation written in task 1, to know what the matrix has to cover
  </read_first>

  <files>src/components/ui/PageShell.tsx</files>

  <action>
Prove the region assertion by mutating **every line of it**, not by mutating the
inner element. A single mutation at the inner element would be the third fix
aimed at where the defect was last seen, and round 4 would find it one element
deeper again.

**Derive the region first, by TEXT.** Read the opener line and the closing line
from the file as it stands, record both verbatim and their current line numbers,
and record the total line count N. Line numbers in this file have moved twice in
this phase; every substitution below matches the line's text.

**The matrix. Two classes, both required.**

*Class A — the realistic reintroductions (syntactically valid TSX):*
  - A1, A2 … one variant per line **inside the region that carries a
    `className`**: append the two nav-inset arbitrary-value utilities (the
    inline-start padding and the block-end bottom padding, exactly the pair the
    verification reproduced) into that line's class string. On today's tree this
    is two variants — the outer element and the inner element.
  - A-new … one variant that **adds a third element** inside the branch,
    carrying those two utilities on its own `className`. This is the round-4
    case: the branch grows an element nobody has written yet. It must be red for
    the same reason every other line is.

*Class B — the exhaustive textual cover:*
  - one variant per **remaining** line of the region (the opener, the `return (`,
    the closing tags, the `);`, and the branch's closing brace): insert
    immediately after that line — and, for the region's last line, immediately
    before it — a new line carrying an element whose `className` holds the two
    utilities. These variants need not typecheck. The gate reads text, and the
    property this round is buying is that **no line of the branch is invisible to
    it**, whatever a future edit puts there.

**For every variant, without exception:**
  1. assert the mutated text is present by grep before reading any result
  2. run `node scripts/verify-conversion.mjs`, capture the exit code and stdout
  3. assert exit is **non-zero**, that stdout contains **no** `✓ E`, and **no**
     `CONVERSION_OK`
  4. record which verdict it produced — exit **1** with a `✗ E` (measured and
     wrong) or exit **2** with a FATAL (a form the gate could not read). Both are
     non-green and both are acceptable; which one it is, is information the next
     reader needs, and the outer-element variant is expected to be a refusal
     because appending to `className={FOCUS_ROOT}` breaks the whole-of-exactly-one
     assertion
  5. restore, assert the mutated text absent by grep, and assert
     `git diff -- src/components/ui/PageShell.tsx` is empty

**Do not run `npm run build` while any variant is present.** Each carries live
Tailwind candidates in a product file (DEF-41-01).

**Also exercise the region read's own refusals**, since a refusal branch nothing
can reach is a decoration (D-41-19, first failure mode):
  - rename the opener's compared literal so no opener is found → refuse, nothing
    measured
  - duplicate the opener line so two are found → refuse
  - move the `className={FOCUS_ROOT}` render outside the branch → refuse
Each restored under the same assert-present / assert-absent protocol.

**The negative control.** With everything restored: the gate exits 0 with five
ticks, and the printed evidence lines are back to the default/wide branch's two
lines only.

**Record in the SUMMARY, as a table:** every variant, its class, the line it
targeted with that line's verbatim text, its exit code, its verdict line, and
its restore assertion — plus, stated as a number, **how many lines of the region
were mutated and that all of them reddened**. That count is the acceptance
criterion of this round. Two previous rounds satisfied "prove by mutation" and
left the guard open.

State plainly in the SUMMARY, as 41-20 did: **a gate that can finally fail is
not a surface anyone has seen.** H41-1…H41-6 remain unobserved, H41-4 stays
`human_needed`, `41-CR01-PASS.md`'s thirteen rows stay `pending`, and this plan
upgrades none of them.
  </action>

  <acceptance_criteria>
    - The SUMMARY carries a mutation table with **one row per line of the derived region** plus the A-new row plus the three refusal rows; today's region is 7 lines, so the matrix has **at least 11 rows**
    - The SUMMARY states the number of region lines mutated as a bare number and asserts every one produced a non-zero exit
    - For **every** row: exit code is non-zero, stdout contains neither `✓ E` nor `CONVERSION_OK`, and the recorded verdict is either `✗ E` at exit 1 or a `FATAL` at exit 2
    - The inner-element row (the exact reintroduction the verifier reproduced — appending the inline-start and block-end utilities to the inner container's class string, `FOCUS_ROOT` and the outer element left byte-identical) is present and its exit is **non-zero**. This is the row the previous round did not have
    - After the final restore: `git diff -- src/components/ui/PageShell.tsx` is **0 lines**, and `git status --porcelain` carries **no line** for any file under `src/`
    - `node scripts/verify-conversion.mjs` on the restored tree exits **0**, five ticks
    - `npm run build` exits **0**, run once with everything restored, and **re-run after the SUMMARY is written** (DEF-41-01: Tailwind scans `.planning/`, and the SUMMARY quotes class strings)
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "gate=$?"; git status --porcelain -- src/ | wc -l</automated>
  </verify>

  <done>Every line of the focus branch has been mutated once, each mutation reddened the gate, the count is recorded, and the tree is byte-identical to its starting state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → human reviewer | A green from `verify-conversion.mjs` is read as "this defect cannot come back". That trust is the asset this plan repairs |
| this plan's transient mutations → `src/components/ui/PageShell.tsx` | The file renders `(auth)`'s login, register and set-password, and `/payment/callback`. A mutation left behind ships a layout regression on the product's front door and its payment-outcome screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-23-01 | Tampering | check E1's coverage of the focus branch | mitigate | The round's whole subject: a gate weakened by omission has let this regression class pass twice. The region assertion plus the per-line mutation matrix is the mitigation, and the matrix — not the code change — is what proves it |
| T-41-23-02 | Tampering | `src/components/ui/PageShell.tsx` left mutated | mitigate | Every mutation asserted present by grep before its result is read and absent afterwards; `git diff` on the file asserted 0 lines and `git status --porcelain -- src/` asserted empty at the end of task 2 |
| T-41-23-03 | Denial of service | the gate refusing on correct code | mitigate | D-41-19's second failure mode. Three legitimate future forms (ternary, extracted component, compound condition) are named in the refusal text with the instruction to widen in the same commit. A gate that reddens correct code gets switched off, and that has bitten seven times in this phase |
| T-41-23-04 | Information disclosure | the gate prints source lines from a public repo | accept | It prints paths, line numbers and class strings from files already committed to a public repository. No secret, no venue address, no member data |
| T-41-23-SC | Tampering | package installs | mitigate | None. `package.json` is unchanged by this plan; no package-manager command runs (D-41-20) |

**Does this change alter what any role or status can see?** No.
`PageShell.tsx` renders no role-conditional or status-conditional content, and
`scripts/verify-conversion.mjs` runs only in development. Reachability of the
four focus routes is decided in `src/lib/routes/capability-routes.ts`, the
middleware and the RLS policies — none touched.

**Could a weakened gate let a regression ship unnoticed?** That is the finding.
It already has, twice, on this exact guard. The mitigation is not the assertion —
it is the enumerated per-line mutation matrix that proves the assertion covers
the region rather than a point inside it.
</threat_model>

<verification>
- `node scripts/verify-conversion.mjs` — exit 0, five ticks, on the restored tree
- `npm run build` — exit 0, run with everything restored and again after the SUMMARY is written
- `git status --porcelain -- src/` — empty
- The mutation matrix in the SUMMARY: every row non-zero exit, the region line count stated as a number
- `npm run verify` — record the aggregate verdict verbatim. On a worktree without `.env.local` the expected baseline is exit 2 with `verify:capabilities` REFUSED and `verify:redirects` not run; that is a property of the environment, not of this change, and it must be stated as such rather than reported as a pass
</verification>

<success_criteria>
- Check E1 asserts the absence of both navigation properties on **every line** of
  the `width === "focus"` branch, and excludes those lines from the "read
  elsewhere" evidence
- The single `className={FOCUS_ROOT}` occurrence is asserted to sit inside that
  branch
- Every line of the region has been mutated once and reddened the gate; the count
  is recorded in the SUMMARY
- No requirement is ticked. DS-07, DS-08, DS-09, RESP-01, RESP-02, RESP-03 and
  RESP-04 all remain PARTIAL
- The SUMMARY states that a gate that can finally fail is not a surface anyone
  has seen
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-23-SUMMARY.md` when done.
</output>
