---
phase: 41-shared-primitives-three-tier-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - scripts/verify-tokens.mjs
  - src/app/layout.tsx
autonomous: true
requirements: [DS-07, RESP-03]
must_haves:
  truths:
    - "A control boundary can be written with a token that measures 3:1 or better against every ground"
    - "The height of the navigation and the width of the side column are one declaration each, readable from any component"
    - "A rule can be written that applies only where no coarse pointer exists, and the build emits it"
    - "A visitor can pinch-zoom the product"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--control, the two layout variables, @custom-variant pointer-fine-only"
      contains: "--control: #A493C0"
    - path: "scripts/verify-tokens.mjs"
      provides: "'control' and the two layout names in KNOWN_TOKEN_NAMES"
      contains: "'control'"
    - path: "src/app/layout.tsx"
      provides: "a viewport block without maximumScale and userScalable"
  key_links:
    - from: "src/app/globals.css :root"
      to: "@theme inline"
      via: "--color-control: var(--control)"
      pattern: "--color-control"
    - from: "src/app/globals.css"
      to: "scripts/verify-tokens.mjs KNOWN_TOKEN_NAMES"
      via: "the same commit declares the name in both"
      pattern: "'control'"
---

<objective>
Declare the three things every later plan in this phase reads: the control-boundary
token, the two navigation-clearance variables, and the pointer variant. Remove the
pinch-zoom block in the same pass, because it is one line in the same file family and
D-41-08 assigned it here.

Purpose: nothing else in this phase can be written until these exist. `--control`
closes finding A1 (75 form inputs on a 1.39:1 boundary). The two layout variables are
what make D-41-03's clearance one number with three consumers instead of three
authors. `pointer-fine-only` is the only mechanism §6.3 permits for the one shrink
site, and **zero pointer variants exist in this tree**, so nothing proves it compiles.

Output: `src/app/globals.css` carrying one new colour token, two layout variables and
one custom variant; `scripts/verify-tokens.mjs` with three names added to
`KNOWN_TOKEN_NAMES`; `src/app/layout.tsx` with two viewport lines gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-PATTERNS.md
@CLAUDE.md
@.claude/rules/meta-gates.md
</context>

<interfaces>
The declaration discipline this file already follows, and which every edit below
copies (`globals.css:225-250`): a name is declared in `:root` and mapped one-to-one
in `@theme inline`. `globals.css:146` carries the precedent for a variable that is
**deliberately not** mapped — the two layout variables take that shape.

`scripts/verify-tokens.mjs` exports `KNOWN_TOKEN_NAMES` (`:305-340`),
`UTILITY_PREFIXES` (`:271-284`, twelve colour prefixes), `consumerPattern()`
(`:535-554`), `listScannableFiles()`, `liveLines()`, `refuse()` (`:406-410`).
`findBlock()` (`:460-479`) returns **only the first** `:root` block; check A
(`:719-726`) scans every `var(--x)` **in the token file** and requires the name to be
in that first `:root` or in `@theme`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declare --control, and put its name in the gate's memory in the same commit</name>
  <files>src/app/globals.css, scripts/verify-tokens.mjs</files>
  <read_first>
    - `src/app/globals.css` lines 3-120 (the `:root` colour block) and 225-288 (`@theme inline`)
    - `scripts/verify-tokens.mjs` lines 286-345 (`KNOWN_TOKEN_NAMES` and the discipline written above it)
    - `41-UI-SPEC.md` §5.2 — the arithmetic that forces the token and the reason the name does not begin with `line-`
  </read_first>
  <action>
    Add `--control: #A493C0;` to the top-level `:root` in `src/app/globals.css`, in the
    lines group, with a comment carrying the three computed ratios from UI-SPEC §5.2:
    7.14 on `--ground`, 6.78 on `--surface`, 6.29 on `--raised`, 7.03 on `--sunk`, all
    against WCAG 1.4.11's 3:1 for a non-text control boundary; and the sentence that
    `--control` is `--muted` is `--sem-info` — three names, one value, literal on all
    three sides, which is D-40-05's discipline and the reason separating them later is
    a value change and not a refactor.

    Add `--color-control: var(--control);` to `@theme inline`, next to the `--color-line*`
    group, so `border-control`, `ring-control` and `outline-control` exist as utilities.

    Add `'control'` to `KNOWN_TOKEN_NAMES` in `scripts/verify-tokens.mjs`, in the lines
    group, in the same commit — D-41-14. A name declared but absent from that list is
    invisible to check D and a half-rename passes.

    Do not touch `--muted`, `--sem-info`, or any `--line*` value. This task adds one
    name; it changes none.
  </action>
  <verify>
    <automated>node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-tokens.mjs` exits 0 and its report prints a `known names`
      count one higher than before the edit
    - `grep -c -- '--control: #A493C0' src/app/globals.css` returns 1
    - `grep -c -- '--color-control: var(--control)' src/app/globals.css` returns 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>`border-control` is a usable utility, `verify-tokens` knows the name exists, and the build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the two navigation-clearance variables, top-level first</name>
  <files>src/app/globals.css, scripts/verify-tokens.mjs</files>
  <read_first>
    - `src/app/globals.css` lines 140-160 (the precedent for a declared-but-unmapped variable) and the end of the `:root` block at 209
    - `41-UI-SPEC.md` §3.2 — the two variables, the media query, the four consumers, and the note about `verify-tokens` check A
    - `41-PATTERNS.md` §6.2 — the resolved evidence: `findBlock()` returns only the first `:root`, so the names **must** be declared at top level
  </read_first>
  <action>
    In the top-level `:root` of `src/app/globals.css`, declare both:
    `--nav-inset-block-end: calc(5rem + env(safe-area-inset-bottom));` and
    `--nav-inset-inline-start: 0px;`.

    Then add a `@media (min-width: 48rem)` block containing a second `:root` that sets
    `--nav-inset-block-end: 0px;` and `--nav-inset-inline-start: 14rem;`. 48rem is the
    `md` tier boundary of §2.1 and 14rem is the side column's 224px of §8.2 — write both
    numbers with that reference in the comment, not as bare values.

    **Declare them at top level even though the media query redeclares them.** This is
    not belt-and-braces: `findBlock()` reads only the first `:root`, and a
    media-query-only declaration referenced anywhere in this file makes check A go red
    on a correct file. That is WR-02's exact shape and Phase 40 paid for it once.

    Give them **no `--color-*` mapping** — they are layout variables, not colours, and
    are reached only through arbitrary values (`ps-[var(--nav-inset-inline-start)]`,
    `pb-[calc(var(--nav-inset-block-end)+1rem)]`). Follow the precedent at
    `globals.css:146` and say so in the comment.

    Add `'nav-inset-block-end'` and `'nav-inset-inline-start'` to `KNOWN_TOKEN_NAMES`
    in the same commit (D-41-14). Check D will then correctly assert that
    `bg-nav-inset-block-end` and friends have zero consumers, because no such utility
    exists and none should.

    Write in the comment the value this reproduces: on a phone with no safe-area inset
    the page shell's bottom padding computes to `80 + 0 + 16` = **96px, exactly today's
    `pb-24`**, so the migration is value-preserving on the device it was written for.
  </action>
  <verify>
    <automated>node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-tokens.mjs` exits 0, and its check A line reads `✓ A` — the
      point of the task is that a correct file stays green
    - `grep -c -- '--nav-inset-block-end' src/app/globals.css` returns 2 (top-level and media query)
    - `grep -c -- '--nav-inset-inline-start' src/app/globals.css` returns 2
    - the first occurrence of each is **before** line 209, i.e. inside the top-level `:root`:
      `awk '/^:root/{r=1} /^}/{if(r==1){print NR; exit}}' src/app/globals.css` prints a
      line number greater than the first `--nav-inset` line number
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both variables resolve at every width, `verify-tokens` is green on a correct file, and no Tailwind colour utility was created for either.</done>
</task>

<task type="auto">
  <name>Task 3: Declare pointer-fine-only, prove the build emits it, and remove the pinch-zoom block</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <read_first>
    - `41-UI-SPEC.md` §6.2 — why the query is only ever used to shrink, and why composing two variants would depend on Tailwind's ordering
    - `41-VALIDATION.md` — "must be proven to emit by `npm run build` before any consumer is written"
    - `src/app/layout.tsx` lines 75-95 (the `viewport` block) and `41-PATTERNS.md` §2.13
  </read_first>
  <action>
    Add to `src/app/globals.css`, near the other at-rules:
    `@custom-variant pointer-fine-only (@media (any-pointer: fine) and (not (any-pointer: coarse)));`
    with a comment stating the asymmetry it implements: 44px is the unprefixed default
    and the query only ever **shrinks**, so a wrong answer costs a slightly-too-large
    button and never a too-small one — `checkin-offline.md`'s asymmetry applied to a
    control.

    **Then prove it emits, before any consumer exists.** Temporarily add
    `@source inline("pointer-fine-only:min-h-9");` to `globals.css`, run `npm run build`,
    and grep the emitted stylesheet under `.next/static/css/` for `any-pointer`. If
    `@source inline` is not honoured by the installed Tailwind, fall back to adding
    `pointer-fine-only:min-h-9` to the `<body>` className in `src/app/layout.tsx` for the
    probe build. **Remove the probe and rebuild before committing.** Record both the
    grep output and the removal in the SUMMARY — a proof whose mutation was not asserted
    to have landed is not a proof.

    In `src/app/layout.tsx`, delete the `maximumScale: 1` and `userScalable: false`
    lines from the `viewport` export (D-41-08, `40-REVIEW.md` WR-16). **Do not touch
    `themeColor`** — it is check F's subject in `verify-tokens.mjs` (`GROUND_HEX`), and
    the reversed-glyph check reads a comment line in this same file. Leave `width` and
    `initialScale` alone.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - **During the probe build:** `grep -rl 'any-pointer' .next/static/css` names at
      least one file, and that file also contains `not (any-pointer` — recorded verbatim
      in the SUMMARY
    - **After the probe is removed:** `grep -rc 'pointer-fine-only:min-h-9' src` returns
      0 for every file, and `npm run build` exits 0
    - `grep -cE 'maximumScale|userScalable' src/app/layout.tsx` returns 0
    - `grep -c 'themeColor: "#0A0712"' src/app/layout.tsx` returns 1 — unchanged
    - `node scripts/verify-tokens.mjs` exits 0
  </acceptance_criteria>
  <done>The variant compiles and the emitted CSS was read, not assumed; the probe is gone; pinch-zoom works and `themeColor` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet → every rendered surface | one token file is read by all 181 components; a wrong value is a product-wide wrong value |
| viewport meta → the browser's zoom behaviour | the only user-agent contract this plan edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | `src/app/layout.tsx` `themeColor` | mitigate | the task forbids touching it and the acceptance criteria assert the exact literal survives; `verify-tokens` check F is run in the same commit |
| T-41-02 | Information Disclosure | none | accept | this plan adds no route, no query, no input and reads no user data |
| T-41-03 | Denial of Service | `globals.css` media-query `:root` | mitigate | check A is asserted green in Task 2's criteria; a red gate on a correct file is how a gate gets switched off (`verify-media-strip.mjs:51-62`) |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | **no packages installed** (D-41-20). `41-RESEARCH.md` §Package Legitimacy Audit records that the gate did not run because there is nothing to vet. If any task proposes a package, the audit re-runs and a blocking human checkpoint precedes the install |
</threat_model>

<verification>
- `npm run build` exits 0 after every task
- `node scripts/verify-tokens.mjs` exits 0 after every task, with check A green
- No file under `src/**/scanner/**`, `src/components/scanner/**` or `src/app/(admin)/door/**`
  appears in `git diff --name-only` for this plan
</verification>

<success_criteria>
- `border-control` paints `#A493C0`, and the ratio table is in the file next to it
- `var(--nav-inset-block-end)` computes to `calc(5rem + env(safe-area-inset-bottom))` below 768px and `0px` at and above it; `var(--nav-inset-inline-start)` is the mirror
- `pointer-fine-only:` was observed in built CSS and the probe that observed it is gone
- pinch-zoom is enabled; `themeColor` is byte-identical
- `KNOWN_TOKEN_NAMES` gained exactly three names in the same commits that declared them
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-01-SUMMARY.md` when done.
Record verbatim: the `any-pointer` grep output from the probe build, and the
`verify-tokens` report line showing the new `known names` count.
</output>
