---
phase: 41-shared-primitives-three-tier-layout
plan: 19
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/components/ui/PageShell.tsx
autonomous: true
requirements: [RESP-01, RESP-02]

must_haves:
  truths:
    - "The paragraph that justifies the focus form's exception states a fact that is true of this tree: the root layout wraps every route, route groups included, and mounts no navigation"
    - "Nothing but comment lines moved — the focus root literal, the render site and both nav-inset reads are byte-identical to HEAD"
  artifacts:
    - path: "src/components/ui/PageShell.tsx"
      provides: "the census that justifies the focus form reserving nothing, stated correctly"
      contains: "src/app/layout.tsx"
  key_links:
    - from: "PageShell's docblock census"
      to: "src/app/layout.tsx"
      via: "a claim about what the root layout wraps and what it mounts, verified at the source"
      pattern: "MotionProvider"
---

<objective>
Close **WR-07**. `src/components/ui/PageShell.tsx`'s docblock says that only two route
layouts exist and *"neither covers `(auth)` or `(public)/payment`."* The root layout
covers both — in the App Router it wraps every route, route groups included. The gate's
own report already disagrees with the docblock: it lists `src/app/layout.tsx` as an
ancestor layout of all eight surfaces.

The **conclusion** the paragraph supports is correct, for a different reason: the root
layout mounts only the motion and toast providers and no navigation. The stated
**measurement** is false.

Why this is not tidy-up. Check E1 does not read the docblock, so this sentence is
currently the only thing in the repository holding CR-01 — and it holds it with a false
fact. The next person who adds a navigation to the root layout will read this paragraph,
believe it does not apply to them, and reintroduce 248px of leading padding against 24px
trailing on `/login`, `/register`, `/set-password` and `/payment/callback`. The paragraph
was written expressly so that *"a decision undone without its measurement reads as a slip
to the next person"*; a false measurement is worse than none.

Purpose: the one place a future reader will look tells the truth.
Output: one corrected paragraph, zero behaviour changed, proven.

**This plan closes no requirement.** RESP-01 and RESP-02 stay PARTIAL; RESP-01 closes
only after 41.2, and only by a written human pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-VERIFICATION.md
@CLAUDE.md
</context>

<interfaces>
The two facts the corrected paragraph must rest on, both to be re-read at the source
before a word is written — a derived citation nobody re-opened is the defect this
warning is made of:

- `src/app/layout.tsx` — the **root** layout. It wraps every route in the application,
  route groups included. Its imports are `MotionProvider` and `ToastProvider`; it mounts
  them and nothing else that could be a navigation.
- `src/app/(admin)/admin/(work)/layout.tsx` — mounts `AppNav`, and covers none of the
  four focus routes.
- `NAV_MODULES` in `scripts/verify-conversion.mjs` names the two modules that count as a
  navigation: `src/components/layout/AppNav.tsx` and `src/components/layout/MobileNav.tsx`.

Untouched by this plan, and asserted untouched: `FOCUS_ROOT`'s literal, the focus
branch's render site, and the two `var(--nav-inset…)` reads on the default/wide branch.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: restate the census as what the tree actually is</name>

  <read_first>
    - `src/components/ui/PageShell.tsx` — the whole file; the paragraph to correct is the
      one beginning *"The `focus` form is the exception"*.
    - `src/app/layout.tsx` — what it imports and what it mounts. Cite by line.
    - `src/app/(admin)/admin/(work)/layout.tsx` — its `AppNav` mount. Cite by line.
    - `41-GAP-REVIEW.md` WR-07, including its suggested replacement wording.
  </read_first>

  <files>src/components/ui/PageShell.tsx</files>

  <action>
    Replace the false clause. The corrected paragraph must say all of:

    - §4's focus list is closed at four routes — `/login`, `/register`, `/set-password`,
      `/payment/callback`;
    - two route layouts wrap them: `src/app/layout.tsx`, which wraps **every** route in
      the application, route groups included, and mounts only the motion and toast
      providers; and `(work)`'s, which mounts `AppNav` and covers none of the four;
    - therefore not one of the four mounts a navigation, and the focus form reserves
      nothing.

    The reason must be the one that is true — *the root layout mounts no navigation* —
    and not the one that was false — *the root layout does not cover these routes*. Cite
    the two layout files by path; cite `MotionProvider` and `ToastProvider` by name.

    Do not spell any utility class in the new prose. The docblock already records why
    (DEF-41-01: Tailwind cannot tell a class string in a comment from a use, and a later
    gate counting leftover manual clearance would count this docblock). Name properties
    and components, never the arbitrary-value utilities that read them.

    Nothing outside the comment changes. No line of executable code is edited, added or
    removed, and no other paragraph is rewritten while passing through.
  </action>

  <acceptance_criteria>
    - `grep -c "neither covers" src/components/ui/PageShell.tsx` returns 0.
    - `grep -c "src/app/layout.tsx" src/components/ui/PageShell.tsx` returns at least 1.
    - The new text contains `MotionProvider` and `ToastProvider` — assert by grep.
    - `grep -cE '(ps|pb|pt|px|py|p)-\[' src/components/ui/PageShell.tsx` returns exactly
      the pre-change value: no bracketed utility literal was introduced into prose.
      Record the pre-change value before editing and compare against it.
    - `grep -c 'var(--nav-inset' src/components/ui/PageShell.tsx` returns 2, both on the
      default/wide branch.
  </acceptance_criteria>

  <verify>
    <automated>grep -c "neither covers" src/components/ui/PageShell.tsx; grep -c 'var(--nav-inset' src/components/ui/PageShell.tsx</automated>
  </verify>

  <done>
    The paragraph names the root layout as wrapping every route and mounting no
    navigation, cites both layout files, and supports the same conclusion by a true
    measurement.
  </done>
</task>

<task type="auto">
  <name>Task 2: prove no behaviour moved</name>

  <read_first>
    - `git diff -- src/components/ui/PageShell.tsx` — the whole diff, read line by line.
    - `41-13-SUMMARY.md` — how the previous change to this file proved the four consuming
      routes had zero changed lines. Use the same shape of evidence.
  </read_first>

  <files>src/components/ui/PageShell.tsx</files>

  <action>
    Prove, and record, that this is a comment-only change. `(auth)` is `access-gating`
    primary and `/payment/callback` is `ticketing-payments` primary: a change to the
    component those four surfaces render inside is presentational **only if it is
    demonstrated to be**, and the demonstration is the deliverable of this task, not a
    note at the end of the previous one.

    Produce four pieces of evidence:

    1. `git diff --name-only` lists exactly one path, `src/components/ui/PageShell.tsx`.
    2. Every added and removed line in `git diff -U0` for that file is inside the block
       comment — each one starts with ` *` or is a comment delimiter. Read them; do not
       infer from the count.
    3. The three load-bearing lines are byte-identical to HEAD: the `FOCUS_ROOT`
       declaration, the focus branch's render site, and the default/wide branch's two
       `var(--nav-inset…)` reads. Compare against `git show HEAD:src/components/ui/PageShell.tsx`.
    4. `npm run build` exits 0 — this repository has no test runner, so the build is the
       typecheck and is the only automatic gate over product code.

    If any of the four disagrees with "comment-only", stop and report which: bending the
    evidence to fit the claim is the failure this whole round exists to correct.
  </action>

  <acceptance_criteria>
    - `git diff --name-only` output is exactly `src/components/ui/PageShell.tsx`.
    - `git diff -U0 -- src/components/ui/PageShell.tsx | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -vcE '^[+-]\s*(\*|/\*|\*/)'`
      returns 0 — no changed line outside the block comment.
    - `git show HEAD:src/components/ui/PageShell.tsx | grep -n 'const FOCUS_ROOT'` and the
      same grep on the working file produce identical text.
    - `npm run build` exits 0.
    - No file under `src/app/(auth)/`, `src/app/(public)/payment/` or `supabase/` appears
      in `git status --porcelain`.
  </acceptance_criteria>

  <verify>
    <automated>npm run build; echo "build exit=$?"; git diff --name-only</automated>
  </verify>

  <done>
    Four pieces of evidence recorded in the SUMMARY, each as a command and its output:
    one file changed, no line outside the comment, three load-bearing lines identical to
    HEAD, build exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `(auth)` surfaces | `/login`, `/register`, `/set-password` render inside this component. `access-gating` primary. |
| client → `/payment/callback` | Renders inside this component. `ticketing-payments` primary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-P1 | Elevation of privilege | `PageShell` on the four focus routes | mitigate | The component decides, submits and reports nothing: it returns two nested `div`s. This plan changes only a block comment, and task 2 proves it — one file in `git diff --name-only`, zero changed lines outside the comment, three load-bearing lines byte-identical to HEAD. Nothing about who reaches `(auth)` or what a `pending` user sees is expressible here; that lives in `capability-routes.ts`, the middleware and the RLS policies, none of which is touched. |
| T-41-P2 | Information disclosure | the docblock's own text | accept | The corrected paragraph names route paths, component names and file paths that are already public in this repository. No member data, no venue, no unannounced date. |
| T-41-P3 | Tampering | **a weakened gate ships a regression unnoticed** | mitigate | Inverted here: this plan repairs the only thing currently holding CR-01, since check E1 does not read the docblock. It does not weaken a gate — but it also does not *become* a gate, and the plan says so rather than implying the paragraph now guards anything. The mechanical guard is plan 41-20's render-site assertion; this file's correctness stays enforced by that plan and by H41-1 / `41-CR01-PASS.md`, both still owed. |
| T-41-P4 | Tampering | Tailwind compiling prose | mitigate | DEF-41-01: a utility class written in a comment is a live candidate. The acceptance criteria assert the count of bracketed-utility literals in the file is unchanged from before the edit. |
| T-41-SC | Tampering | supply chain | accept | No package is installed. D-41-20 forbids new packages in this phase. |
</threat_model>

<verification>
- `npm run build` exits 0.
- `git diff --name-only` lists exactly one file.
- `grep -c "neither covers" src/components/ui/PageShell.tsx` returns 0.
- `grep -c 'var(--nav-inset' src/components/ui/PageShell.tsx` returns 2.
</verification>

<success_criteria>
- The census paragraph states a fact that holds on this tree and supports the same
  conclusion by the true reason.
- Comment-only change, proven by four recorded pieces of evidence rather than asserted.
- No requirement ticked; RESP-01 and RESP-02 stay PARTIAL.
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-19-SUMMARY.md` when done.
It MUST contain the four pieces of evidence from task 2 verbatim — command and output —
and must state explicitly that this paragraph is documentation and not a gate.
</output>
