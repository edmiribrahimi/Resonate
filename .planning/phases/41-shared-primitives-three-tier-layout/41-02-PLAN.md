---
phase: 41-shared-primitives-three-tier-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-no-viewport-read.mjs
  - scripts/verify-breakpoints.mjs
autonomous: true
requirements: [RESP-01, RESP-04]
must_haves:
  truths:
    - "A tier difference written in JavaScript instead of CSS fails a check before it is merged"
    - "A file that acquires a fourth breakpoint prefix fails a check, and the files that still carry the third are a written list rather than a memory"
  artifacts:
    - path: "scripts/verify-no-viewport-read.mjs"
      provides: "G7 — matchMedia, useSyncExternalStore and innerWidth have zero occurrences under src/"
      contains: "refuse("
    - path: "scripts/verify-breakpoints.mjs"
      provides: "G6 — no sm:, xl: or 2xl: outside a declared, shrinking list"
      contains: "REMAINING"
  key_links:
    - from: "scripts/verify-breakpoints.mjs"
      to: "the 22 files that carry sm: today"
      via: "a declared [path, count, reason] list that only shrinks"
      pattern: "REMAINING"
---

<objective>
Write the two gates that have no legitimate exception, before any surface converts.
Both are cheap, both are absolute in their end state, and both can be proven red today.

Purpose: G7 keeps §0 rule 6 true — the sheet↔window switch is CSS-only precisely so
the viewport is never read in JavaScript, and a gate is what keeps that from being
re-litigated one `useEffect` at a time. G6 keeps §2.1's two prefixes from becoming
three; the tree has **44 `sm:` uses in 22 files** today, and D-41-05 decided they are
migrated rather than grandfathered — so the gate ships with those 22 as a **written,
shrinking list**, not as an exemption nobody can see.

Output: two `scripts/verify-*.mjs` files in the house shape, each proven able to go
red, each printing what it counted.

**Neither script is registered in `package.json` here.** Plan 41-12 owns that file and
registers all six new gates at once, so no two plans in a wave contend for it. Run them
with `node scripts/verify-<gate>.mjs` until then.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-VALIDATION.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-PATTERNS.md
@CLAUDE.md
@.claude/rules/meta-gates.md
</context>

<interfaces>
`scripts/verify-tokens.mjs` is the model species and already exports what both gates
need: `listScannableFiles(dir)`, `liveLines(text)` (comment blanking before any
counting — a line-shape heuristic, deliberately not a tokeniser, `:203-219`),
`consumerPattern(names)` with its leading and trailing boundary guards (`:535-554`),
and `refuse(message)` → **exit 2** (`:406-410`).

The five-part header every gate opens with, in order: *what it asserts, in one
sentence* → *why a structural check and not a test* → **`── WHAT A GREEN DOES NOT
MEAN ──`** → the enumerated checks → *exit codes*.

`scripts/verify-sunset-gradient.mjs:141-149` is the shape for a declared list,
including the discipline for growing one: *"Adding the first one is a DECISION that
edits this constant — not a diff nobody reads."*
`scripts/verify-routes.mjs:140-152` (`PUBLIC_ALLOW`) is the `[value, reason]` pair
shape — **the reason travels with the entry**, which is the shape to copy here.
`scripts/verify-tokens.mjs:818-822` is the vacuous-check confession, which G7 lands in
immediately because its subject is legitimately empty.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: G7 — the viewport is never read in JavaScript</name>
  <files>scripts/verify-no-viewport-read.mjs</files>
  <read_first>
    - `scripts/verify-tokens.mjs` lines 1-60 (the five-part header), 400-460 (`refuse`, `findBlock`), 570-600 (the refusals taken together before any tick), 810-830 (the vacuous confession), 1035-1058 (the verdict block)
    - `41-UI-SPEC.md` §0 rule 6 and §13's G7 row
  </read_first>
  <action>
    Create `scripts/verify-no-viewport-read.mjs`. It asserts: `matchMedia`,
    `useSyncExternalStore` and `innerWidth` have **zero** occurrences in live lines
    under `src/`. No exemptions — the count is zero today and the gate keeps it zero.

    Use `listScannableFiles` and `liveLines` imported from `./verify-tokens.mjs` — the
    cross-script ESM import is already house style (`verify-capabilities.mjs:145`), and
    `verify-tokens.mjs` already exports both although nothing imports them yet.

    `refuse()` → exit 2 if the walk of `src/` finds no scannable file: *"a vacuous green
    is not a green"* (`verify-tokens.mjs:582`). Put the refusals before any tick, for the
    reason `:586-590` gives.

    The `WHAT A GREEN DOES NOT MEAN` header must say: this reads text, not behaviour. It
    cannot see a viewport read reached through a renamed import, through a dependency,
    or built by string concatenation; and it says nothing about whether the CSS-only
    switch is *correct*, only that no JavaScript is deciding the tier. Name **H41-2** as
    the observation that closes what it cannot see.

    Print what was counted: the number of files walked and the number of occurrences
    found for each of the three names, then the verdict block.
  </action>
  <verify>
    <automated>node scripts/verify-no-viewport-read.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-no-viewport-read.mjs` exits 0 and prints a walked-file count greater than 170
    - **Proven red, with the mutation asserted to have landed:** append a line
      containing `window.innerWidth` to `src/components/motion/MotionProvider.tsx`;
      assert `grep -c 'window.innerWidth' src/components/motion/MotionProvider.tsx`
      returns 1 **before** running the gate; then the gate exits 1 and names that file.
      Revert, assert the grep returns 0, and the gate exits 0 again
    - **Proven not fooled by a comment:** the same string inside a `//` comment leaves the gate at exit 0
  </acceptance_criteria>
  <done>The gate exists, refuses on an empty walk, goes red on a real read and green on a commented one, and its header states what a green does not mean.</done>
</task>

<task type="auto">
  <name>Task 2: G6 — two breakpoint prefixes, and a written list of the files still carrying a third</name>
  <files>scripts/verify-breakpoints.mjs</files>
  <read_first>
    - `41-UI-SPEC.md` §2.1 (the three tiers and the two prefixes), §2.3 (the class-by-class map of all 44 `sm:` uses), §13's G6 row
    - `scripts/verify-routes.mjs` lines 130-160 (`PUBLIC_ALLOW`, the `[value, reason]` shape)
    - `scripts/verify-sunset-gradient.mjs` lines 135-155 (the discipline for growing a declared list)
  </read_first>
  <action>
    Create `scripts/verify-breakpoints.mjs`. It asserts, over live lines under `src/`:

    **Check A — no fourth prefix anywhere.** `xl:` and `2xl:` have zero occurrences,
    with no exemption. Measured zero today; the gate keeps it zero.

    **Check B — `sm:` only where it is declared.** Build `REMAINING` by measuring the
    tree at write time: every file carrying `sm:`, as `[path, count, reason]` triples
    where the reason names the conversion unit that will remove it (for example *"the
    four sheet modals — absorbed by the Dialog primitive, §8.3"*, *"the table
    dual-renders — consolidated onto `md`, §8.8"*). A file **not** on the list carrying
    `sm:` is a failure. A file **on** the list carrying **more** than its recorded count
    is a failure. Removing a file from the list is what converting its surface looks
    like; adding one is a decision that edits the constant.

    Use `consumerPattern`'s boundary-guard technique for the prefix match so `sm:` is not
    found inside another token, and `liveLines` so a docblock quoting `sm:hidden` — and
    several of them do — is not counted as a use.

    Print: files walked, `xl:`/`2xl:` counts, the `REMAINING` length, the summed `sm:`
    count, and the three files with the most remaining. A green that prints *"22 files
    still carry `sm:`"* is doing its job; a green that prints nothing is not.

    The `WHAT A GREEN DOES NOT MEAN` header must say: this counts prefixes, not layouts.
    A file using only `md:` and `lg:` can still be wrong at every width, and **H41-1**
    is the only thing that observes otherwise. It also cannot see a prefix built by
    concatenation.
  </action>
  <verify>
    <automated>node scripts/verify-breakpoints.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-breakpoints.mjs` exits 0 on the tree as it stands, and its
      printed `REMAINING` length equals `grep -rlE '\bsm:' src --include='*.tsx' | wc -l`
    - **Proven red on a new file:** add `sm:hidden` to a className in
      `src/components/ui/Icons.tsx` (not on the list); assert
      `grep -c 'sm:hidden' src/components/ui/Icons.tsx` returns 1; the gate exits 1 and
      names the file. Revert and re-run: exit 0
    - **Proven red on a growing file:** add one `sm:` use to a file already on the list;
      the gate exits 1 citing the count increase. Revert and re-run: exit 0
    - **Proven red on the fourth prefix:** add `xl:hidden` anywhere under `src/`; exit 1. Revert: exit 0
  </acceptance_criteria>
  <done>Two prefixes are the contract, the 22 files still outside it are named in a file rather than remembered, and the list can only shrink without a decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → a developer's confidence | a gate that is green having measured nothing is worse than no gate, because it is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Repudiation | both gates | mitigate | each prints what it counted, not just a verdict (`verify-tokens.mjs:707-717`), so a green states its own scope |
| T-41-05 | Denial of Service | `verify-breakpoints.mjs` | mitigate | the `REMAINING` list exists **before** the first run, so the gate cannot go red on the 22 correct-for-now files and be switched off (`verify-media-strip.mjs:51-62`) |
| T-41-06 | Spoofing | a vacuous green | mitigate | `refuse()` → exit 2 on an empty walk; both gates are proven red by an asserted mutation before they are trusted |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | **no packages** (D-41-20). Both gates are plain Node ESM reading the filesystem |
</threat_model>

<verification>
- Both scripts exit 0 on the unmodified tree
- Both were observed exiting 1 on a mutation whose presence was asserted by grep first
- `npm run build` exits 0 — neither script is imported by the app, but the tree was mutated and reverted, so the build proves the revert landed
- No file under `src/**/scanner/**`, `src/components/scanner/**` or `src/app/(admin)/door/**` is edited
</verification>

<success_criteria>
- `scripts/verify-no-viewport-read.mjs` and `scripts/verify-breakpoints.mjs` exist, in the five-part header shape, each with `refuse()` → exit 2 distinct from exit 1
- G6's `REMAINING` list is a `[path, count, reason]` constant whose length matches a measurement recorded in the SUMMARY
- Every mutation used to prove red was asserted to have landed before its result was read, and reverted after
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-02-SUMMARY.md` when done.
Record: the two gates' full green output, and for each red proof the grep that asserted
the mutation, the exit code observed, and the grep that asserted the revert.
</output>
