---
phase: 41-shared-primitives-three-tier-layout
plan: 08
type: execute
wave: 5
depends_on: ["41-03", "41-04", "41-05", "41-06"]
files_modified:
  - src/components/ui/Skeleton.tsx
  - src/components/ui/Chip.tsx
  - src/app/(public)/gallery/page.tsx
  - src/app/(public)/gallery/GalleryClient.tsx
  - src/app/(public)/gallery/loading.tsx
  - src/app/(admin)/admin/(work)/members/register/page.tsx
  - scripts/conversion-manifest.mjs
autonomous: true
requirements: [DS-07, RESP-01, RESP-02, RESP-03]
must_haves:
  truths:
    - "A loading placeholder is one component, and the component has importers"
    - "The gallery stops widening at 1280px and its grid steps 1 → 2 → 3 columns"
    - "A converted page mounts the navigation that has two tiers, not the phone-locked wrapper"
    - "The membership register reads as a member list on a phone and on a laptop"
    - "Badge exists and is rendered by a surface in the same plan that declares it"
  artifacts:
    - path: "src/components/ui/Skeleton.tsx"
      provides: "SkeletonLine, SkeletonCard, SkeletonAvatar on the §8.9 contract"
      contains: "bg-raised"
    - path: "src/app/(public)/gallery/loading.tsx"
      provides: "the first importer Skeleton has ever had"
    - path: "src/components/ui/Chip.tsx"
      provides: "the Badge export added to the file plan 41-03 wrote, in the plan that first renders it"
      exports: ["Chip", "Badge"]
  key_links:
    - from: "src/app/(public)/gallery/loading.tsx"
      to: "src/components/ui/Skeleton.tsx"
      via: "import"
      pattern: "ui/Skeleton"
    - from: "src/app/(public)/gallery/page.tsx"
      to: "src/components/layout/AppNav.tsx"
      via: "the page-level nav mount, swapped from MobileNav"
      pattern: "AppNav"
---

<objective>
Fix `Skeleton`'s class strings and give it its first consumer in the same plan; convert
`/gallery` and `/admin/members/register` whole.

Purpose: `src/components/ui/Skeleton.tsx` is **DS-07's thesis, already disproved once**.
It exists, its API is correct, and it has **zero importers**, while **102** hand-rolled
`animate-pulse` blocks live in **20** files. D-41-04 exists because of this file.

And the pattern map found the trap inside the easy reading: *"adoption, not creation" is
true of the API and false of the class strings.* Three of the four strings in that file
are retired by this phase — `rounded-lg` does not survive §9, `bg-card-border/50` is a
legacy **line** token used as a **ground**, and `p-5` (20px) is not a step §3.1 names.
A plan that reads §8.9 literally ships the wrong ones. **The file is modified, then
adopted.**

`/gallery` is the smallest surface that exercises the rest at once: it is on §4's closed
`wide` list, it is the best specimen of the incumbent page shell
(`gallery/page.tsx:60-80` shows the page root, the header, the `<h1>` and the nav mount
with its four props inside twenty lines), it has a `loading.tsx`, and it is the first
**page-level** nav mount to move from the phone-locked wrapper to `AppNav`.

Output: `Skeleton` on its contract with importers; two whole surfaces converted; six
surfaces in the manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-PATTERNS.md
@CLAUDE.md
@.claude/rules/media-and-storage.md
@.claude/rules/venue-secrecy.md
@.claude/rules/community-membership.md
@src/components/ui/Skeleton.tsx
@src/app/(public)/gallery/page.tsx
</context>

<interfaces>
`PageShell` (`width="wide"` for `/gallery`, `width="default"` for the register),
`Card`, `PageTitle`, `SectionHeading` and `Button` from plan 41-05; `IconButton` and
`Chip` from plan 41-03; `AppNav` from plan 41-03, whose prop shape is
`role`, `status`, `capabilities` (**required, deliberately**), `liveAssignmentCapabilities`,
plus `form` and `workNav`.

**`Badge` is added by this plan**, to the `src/components/ui/Chip.tsx` that plan 41-03
wrote — which already carries the whole ladder and the sentence that governs both
symbols, and which left `Badge` unexported with a comment naming this plan. Task 3's
`/admin/members/register` stage marks are its first render, and D-41-04 forbids
publishing it any earlier than the wave that renders it.

§8.9's visual contract, and it is the whole of it: `animate-pulse rounded-xl bg-raised`.
`--raised` on `--surface` is **1.08:1**, which is correct for a placeholder — it must
read as absent content, not as content.

The three-tier grid axis (§2.2) is the **only genuinely three-tier axis in the product**:
1 column on a phone, `md:grid-cols-2`, `lg:grid-cols-3`. Everything else is a two-state
change at `md`. A gallery is exactly that axis, so `sm:grid-cols-3` — which skipped its
middle — becomes `md:grid-cols-2 lg:grid-cols-3`.

The `wide` list is closed (§4) and `/gallery` is on it. The register is not, so it is `default`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Skeleton's class strings, corrected — then left alone</name>
  <files>src/components/ui/Skeleton.tsx</files>
  <read_first>
    - `src/components/ui/Skeleton.tsx` in full (45 lines, three exports, one `sm:`)
    - `41-UI-SPEC.md` §8.9 (the contract and the 1.08:1 argument), §9 (three radii), §3.1 (the named steps)
    - `41-PATTERNS.md` §2.9 — the correction that "adoption, not creation" does not mean "no edit"
  </read_first>
  <action>
    Keep the three exports and their API — `SkeletonLine`, `SkeletonCard`,
    `SkeletonAvatar` — unchanged, so no consumer written against them has to move.

    Replace the class strings: `rounded-lg` → `rounded-xl`; `bg-card-border/50` →
    `bg-raised`; `p-5` → `p-6`; the card's shell to `Card`'s contract (`rounded-2xl
    border border-line bg-surface`). Remove the one `sm:` — a placeholder has no tier
    behaviour, and G6 counts it.

    Put in the docblock: `--raised` on `--surface` is **1.08:1**, deliberately, because a
    placeholder must read as absent content and not as content; and the record that this
    file had **zero importers for the whole of its existence** while 102 hand-rolled
    blocks lived in 20 files, so the next author knows what the file is for.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-breakpoints.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; both gates exit 0
    - `grep -cE 'rounded-lg|bg-card-border|p-5\b' src/components/ui/Skeleton.tsx` returns 0
    - `grep -c 'bg-raised' src/components/ui/Skeleton.tsx` is at least 1
    - `grep -cE '\bsm:' src/components/ui/Skeleton.tsx` returns 0
    - the three export names are unchanged: `grep -cE 'export function (SkeletonLine|SkeletonCard|SkeletonAvatar)' src/components/ui/Skeleton.tsx` returns 3
  </acceptance_criteria>
  <done>The API survived, the three retired strings did not, and the file is ready to be imported for the first time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert /gallery whole — shell, grid, nav mount, and Skeleton's first import</name>
  <files>src/app/(public)/gallery/page.tsx, src/app/(public)/gallery/GalleryClient.tsx, src/app/(public)/gallery/loading.tsx, scripts/conversion-manifest.mjs</files>
  <read_first>
    - `src/app/(public)/gallery/page.tsx` (82), `GalleryClient.tsx` (53), `loading.tsx` (38) — all three, in full
    - `41-UI-SPEC.md` §4 (the `wide` list), §2.2 (the three-tier grid axis), §2.3 (the `sm:` map), §8.9
    - `.claude/rules/media-and-storage.md` and `.claude/rules/venue-secrecy.md`
  </read_first>
  <action>
    Convert all three files as one unit.

    `page.tsx` — outermost element becomes `PageShell width="wide"`; the header's
    `px-6 pt-12` is absorbed by the shell; the `<h1>` becomes `PageTitle`; `min-h-dvh
    pb-24` goes. Swap the `MobileNav` mount for `AppNav`, passing the same four props
    unchanged — this is the first **page-level** mount to move, and it is what gives a
    public surface the side column at `md`. `AnimatedSection` stays as it is; it is spine
    and it is already clean.

    `GalleryClient.tsx` — the grid becomes the three-tier axis: one column, `md:grid-cols-2`,
    `lg:grid-cols-3`. Any `sm:` prefix goes (§2.3). Cards become `Card`; every
    interactive element carries `min-h-11`; any `text-[10px]`/`text-[11px]` moves to
    `text-xs`.

    `loading.tsx` — replace the hand-rolled `animate-pulse` blocks with `SkeletonCard`
    and `SkeletonLine`. **This is the first import `src/components/ui/Skeleton.tsx` has
    ever had**, and it is the proof D-41-04 asks for. Keep the placeholder's shape
    matching the loaded layout, so the page does not jump.

    **Media and secrecy, stated rather than assumed.** The gallery renders event media.
    Do not change which rows are fetched, which filter is applied, or what a group's
    label contains. A layout change that un-truncates a caption, surfaces a field, or
    renders a group heading that names a place can show a venue before its reveal, and
    `venue_reveal_sent` is **monotone** — there is no un-revealing. If a caption or a
    group label is currently truncated, it stays truncated to the same length.

    Add `/gallery` to `CONVERTED` with its `pageFile` and `wide`.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-conversion.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-media-strip.mjs && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; all four gates exit 0
    - `node scripts/verify-conversion.mjs` reports `/gallery` walked, and its check C now
      prints a non-zero importer count for `ui/Skeleton.tsx`
    - `grep -rc 'ui/Skeleton' "src/app/(public)/gallery/loading.tsx"` is at least 1
    - `grep -c 'animate-pulse' "src/app/(public)/gallery/loading.tsx"` returns 0
    - `grep -c 'MobileNav' "src/app/(public)/gallery/page.tsx"` returns 0 and `grep -c 'AppNav'` returns at least 1
    - `grep -cE 'card-border|bg-card\b|bg-background|text-foreground' "src/app/(public)/gallery/page.tsx" "src/app/(public)/gallery/GalleryClient.tsx" "src/app/(public)/gallery/loading.tsx"` returns 0 for all three
    - `grep -c 'lg:grid-cols-3' "src/app/(public)/gallery/GalleryClient.tsx"` is at least 1
    - **no data change:** `git diff -U0` over the three files shows no changed line
      containing `from(`, `select(`, `eq(`, `order(`, `limit(` or `truncate` — or each is listed in the SUMMARY with its reason
  </acceptance_criteria>
  <done>A public surface renders at three column counts, stops widening at 1280px, mounts the two-tier navigation, and imports the skeleton that nobody had ever imported.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert /admin/members/register whole</name>
  <files>src/components/ui/Chip.tsx, src/app/(admin)/admin/(work)/members/register/page.tsx, scripts/conversion-manifest.mjs</files>
  <read_first>
    - `src/app/(admin)/admin/(work)/members/register/page.tsx` in full (435 lines, 7 legacy-token utilities, 4 raw palette colours, one `<h1>`)
    - `41-UI-SPEC.md` §4 (this route is **not** on the `wide` list, so it is `default`), §8.5 (`Chip` versus `Badge` — the sentence that decides G5), §8.11 (the empty state is a class contract, not a component), §11 (the empty-state copy rule)
    - `src/components/ui/Chip.tsx` as plan 41-03 left it — the ladder, the governing sentence, and the unexported `Badge` with the comment naming this plan
    - `41-CONTEXT.md` **D-41-04** — `Badge` is published here because this is the wave that renders it
    - `.claude/rules/community-membership.md` — the register is the record of who was let in and by whom
  </read_first>
  <action>
    Convert the file whole. `PageShell width="default"` as the outermost element; the
    `<h1>` becomes `PageTitle`; the card shells become `Card`; the four raw palette
    colours become the declared semantics for their meaning, with `--ground` as the ink
    wherever a semantic is a fill; the seven legacy names become their new names.

**First, add the `Badge` export** to `src/components/ui/Chip.tsx`:
    `rounded-full px-2 py-1 text-xs font-semibold`, **no `min-h`** — 8px and 4px are the
    `sm` and `xs` steps of §3.1, chosen over the tree's incumbent `px-2.5 py-0.5` because
    10px and 2px are not multiples of 4 and not steps this ladder names. Read the size and
    variant maps plan 41-03 already wrote; do not write a second ladder. Delete the
    comment that named this plan as the one that would add it. The resulting badge is
    ~24px against `Chip`'s 44px, so the two stay visually distinct without a `min-h` doing
    it — which is why G5 scans interactive elements only.

    Then: stage and act marks are **text** with a `Badge` — not interactive, therefore no
    `min-h`. If any of them is a `<Link>` or a `<button>`, it is a **`Chip`** at
    `min-h-11`, not a `Badge`. That single sentence is the difference between a correct
    file and a 20px target, and plan 41-11's G5 will read this file.

    Add both new exports to `PRIMITIVES` in `scripts/conversion-manifest.mjs` — one entry
    per symbol — so plan 41-07's G1 check C counts an importer for `Badge` from the moment
    it exists.

    If the page has an empty state, apply §8.11's class contract — `px-6 py-12 text-center`,
    heading `text-base font-semibold text-ink`, body `text-sm text-muted` — and §11's
    copy rule: one sentence naming what is absent in the surface's own noun, one naming
    the next step. Never `No data`, never `Nothing here`, never an error tone. **Do not
    build an empty-state component** — D-41-11 decided against it and the reason is that
    the copy changes per surface.

    **This surface is the membership record and its content is personal data.** Do not
    change which columns are selected, do not add a field "while we are here", and do not
    change how an act is attributed. `community-membership.md` requires that approvals and
    rejections carry **who** and **when**; if the conversion would drop either from the
    rendered row, it does not.

    Add the route to `CONVERTED` with its `pageFile` and `default`.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-conversion.mjs && node scripts/verify-breakpoints.mjs && node scripts/verify-semantic-separation.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; all three gates exit 0
    - `grep -cE '(bg|text|border)-(red|green|blue|amber|yellow|orange|emerald|rose)-[0-9]' "src/app/(admin)/admin/(work)/members/register/page.tsx"` returns 0
    - `grep -cE 'card-border|bg-card\b|bg-background|text-foreground' "src/app/(admin)/admin/(work)/members/register/page.tsx"` returns 0
    - `grep -c '<h1' "src/app/(admin)/admin/(work)/members/register/page.tsx"` returns 0 — the title is `PageTitle`
    - `grep -cE 'export (function|const) Badge' src/components/ui/Chip.tsx` returns 1, and `grep -cE 'px-2\.5|py-0\.5' src/components/ui/Chip.tsx` returns 0
    - `node scripts/verify-conversion.mjs` check C prints an importer count of at least 1 for the `Badge` export specifically, not merely for `Chip.tsx`
    - `node -e "import('./scripts/conversion-manifest.mjs').then(m=>{console.log(m.CONVERTED.length); process.exit(m.CONVERTED.length===6?0:1)})"` exits 0
    - **no data change:** `git diff -U0` shows no changed line containing `select(`, `from(`, `eq(`, `order(` — or each is listed in the SUMMARY with its reason
  </acceptance_criteria>
  <done>Six whole surfaces are declared converted, `Badge` is published in the plan that renders it, the register still says who did what and when, and no column was added because a card had room.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event media → a public page | `/gallery` is public; what a caption or a group label says is published |
| a venue's address → any surface that renders an event | `venue_reveal_sent` is a one-way switch; a layout change that renders earlier or truncates less can precede it |
| the membership record → a work surface | personal data, with its own purpose and retention, distinct from product data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-26 | Information Disclosure | `/gallery` captions and group labels | mitigate | the task forbids changing what is fetched, filtered or truncated; the acceptance criteria diff `from(`, `select(`, `eq(`, `order(`, `limit(` and `truncate`; `verify-media-strip.mjs` runs in the gate |
| T-41-27 | Information Disclosure | a venue rendered before its reveal | mitigate | the same diff assertion plus the explicit instruction that a truncation stays at its current length. `venue-secrecy.md`: the switch is monotone and a mistake here is not reversible |
| T-41-28 | Information Disclosure | the membership register | mitigate | no column added, no field surfaced, no change to attribution; the diff assertion covers the query |
| T-41-29 | Repudiation | who approved or rejected, and when | mitigate | the task states that if the conversion would drop either from the rendered row, it does not |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | **no packages** (D-41-20) |
</threat_model>

<verification>
- `npm run build` exits 0 after every task
- `verify-conversion`, `verify-breakpoints`, `verify-no-viewport-read`, `verify-tokens`, `verify-semantic-separation`, `verify-media-strip` all exit 0
- `git diff --name-only` contains no `scanner/` and no `(admin)/door/` path
- Manual, recorded in the SUMMARY (**H41-1**): `/gallery` and `/admin/members/register` at
  390px, 768px and 1280px. On `/gallery` confirm one, two and three columns respectively,
  and that content stops widening on the third. Confirm the side column at 768px and up
  and the bottom bar below it
</verification>

<success_criteria>
- `Skeleton` carries `animate-pulse rounded-xl bg-raised`, keeps its three export names, and has at least one importer
- `Badge` is exported and rendered inside this plan, and G1 check C counts its importer by symbol
- `/gallery` renders through `PageShell width="wide"` and mounts `AppNav`
- `/admin/members/register` renders through `PageShell width="default"`
- Six surfaces in `CONVERTED`, and `verify-conversion.mjs` green over all six
- No query, filter, ordering or truncation changed on either surface
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-08-SUMMARY.md` when done.
Record: `verify-conversion.mjs`'s full report showing six surfaces and the importer count
for `ui/Skeleton.tsx`; the two data-change diff assertions; the H41-1 observations at the
three widths, with the gallery's column counts.
</output>
