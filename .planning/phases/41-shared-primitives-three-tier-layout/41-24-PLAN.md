---
phase: 41-shared-primitives-three-tier-layout
plan: 24
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-dialogs.mjs
autonomous: true
gap_closure: true
requirements: [DS-08]

must_haves:
  truths:
    - "A REMAINING entry naming a file check B never opens — fenced, the primitive itself, or the declared exemption — refuses, instead of being reported STALE and dropping the debt count by one"
    - "The refusal's reason line distinguishes a fenced path (unmeasured, a scope boundary) from an exempt path (measured and declared correct) from the primitive itself"
    - "The overlay matcher sees a negative rung written out in a class string, and the printed sentence describes what the matcher actually does"
    - "The widening changes nothing on this tree: the same REMAINING count, the same exit 0"
  artifacts:
    - path: "scripts/verify-dialogs.mjs"
      provides: "the never-measured-by-B skip set as one derived list, keyed by the refusal; and the rung family widened to the negative form with a sixth self-check probe"
      contains: "NEVER_MEASURED_BY_B"
    - path: ".planning/phases/41-shared-primitives-three-tier-layout/41-24-SUMMARY.md"
      provides: "the before/after mutation table for both findings, with exit codes and verdict lines"
  key_links:
    - from: "REMAINING ∩ (fence ∪ primitive ∪ exemption)"
      to: "the refusal above check B's loop"
      via: "one derived skip set rather than the fence alone"
      pattern: "NEVER_MEASURED_BY_B"
    - from: "an overlay written at a negative rung"
      to: "check B's shell matcher"
      via: "an optional leading hyphen on the rung token, assembled by concatenation"
      pattern: "RUNG_FAMILY"
---

<objective>
Two findings in `scripts/verify-dialogs.mjs`, both of the shapes this phase has
already paid for.

**WR-01 — a debt counter that falls because the gate stopped looking.** Plan
41-18 closed the *fence* half of the STALE defect: a `REMAINING` entry behind the
Phase 42 fence now refuses. It left the other half open. Check B's loop skips
three categories before `shellShapes()` ever reads a file — the primitive, the
declared full-bleed exemption, and the fenced paths — and the refusal covers only
the third. Put the exemption on `REMAINING` and the gate prints "converted;
remove this entry" about a file it never opened, and the debt number falls. That
file still carries a native dialog element. The refusal's own text names this
failure — *"a debt counter that falls because the gate stopped looking is worse
than no counter"* — and the refusal does not cover it.

**WR-05 — the printed sentence promises a family the matcher does not match.**
The report says a copy is seen at any rung **written out** in the class string,
and names exactly two exclusions: a rung reached through a variable, and a class
string built by concatenation. A negative rung is written out and is neither, and
the left boundary blocks it. Either the matcher covers what the sentence claims,
or the sentence stops claiming it. This plan widens the matcher, which is the
direction 41-18 chose for the same shape one round ago.

Purpose: a check keyed on a proxy goes quiet, and a sentence that over-promises
is how a reader stops reading the numbers.
Output: `scripts/verify-dialogs.mjs`, and a SUMMARY carrying the before/after
measurement for each.

**This plan observes no screen.** DS-08 stays PARTIAL; A2 (scroll lock) stays
open; H41-2 stays unobserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.claude/rules/meta-gates.md

**Constraints that decide how this is proven:**

1. **No test runner exists for the product** (Guardrail 1, D-41-20). Every proof
   below is an exit code, an exact stdout string, or a source assertion.
2. **DEF-41-01 — Tailwind compiles class strings from comments and from
   `.planning/`.** `verify-dialogs.mjs` already assembles every utility token by
   concatenation for this reason; anything added here does the same. `npm run
   build` is not run while any transient file carrying a class-string literal
   exists, and is run again after the SUMMARY is written.
3. **`src/components/ui/PageShell.tsx` is off limits to this plan.** Plan 41-23
   runs in the same wave and mutates it transiently. Nothing here touches it, and
   nothing here touches any tracked file under `src/`.
4. **A fence is a scope boundary (unmeasured); an exemption is measured and
   declared correct.** They must stay apart in prose and in print. That
   distinction is the substance of WR-01's fix, not a wording preference.
</context>

<interfaces>
Existing identifiers in `scripts/verify-dialogs.mjs`:

- `PRIMITIVE_FILE` (:295) — `src/components/ui/Dialog.tsx`
- `FULL_BLEED_VIEWER` (:330) — `src/components/media/Lightbox.tsx`; exempt from check B, deliberately **not** on `REMAINING`, and it still carries a native dialog element
- `PHASE_42_EXEMPT_PATHS` / `fencePatterns` / `fenceMatch(relPath)` (:829)
- `fencedRemaining` and its refusal (:842-858) — the half 41-18 closed; the position above check B's loop is deliberate and stays
- `REMAINING` (:650), `declaredPaths` (:779)
- check B's loop (:969-979) — the three `continue`s that decide what is never measured
- `stale` (:983, :990) — computed as *on disk and not in `measuredShells`*, which is what mis-reports the two uncovered categories
- the printed count block (:1022-1027) and the STALE notice (:1063-1065)
- `LEFT_BOUNDARY` / `RIGHT_BOUNDARY` / `RUNG_PREFIX` / `RUNG_FAMILY` / `OVERLAY_PARTS` (:525-537), `isOverlayLine` (:540)
- `MATCHER_PROBES` and the self-check refusal (:544-…, printed at :997-1009)
- the printed sentence about what the rung family covers (:1001-1009)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: the skip set is derived once, and the refusal is keyed on all of it</name>

  <read_first>
    - `scripts/verify-dialogs.mjs:290-420` — `PRIMITIVE_FILE`, `FULL_BLEED_VIEWER`, the two exceptions and the paragraph at :359 distinguishing a measured-and-declared-correct file from a fenced one, and the WR-02 paragraph at :396-411 explaining why an overlap is a refusal and not a warning
    - `scripts/verify-dialogs.mjs:760-860` — the pre-loop refusals, `fenceMatch`, and `fencedRemaining` with its refusal text (the half 41-18 closed, whose wording this task reuses)
    - `scripts/verify-dialogs.mjs:960-1070` — check B's loop, `stale`/`missing`/`undeclared`, the printed counts, and the STALE notice
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-18-SUMMARY.md` — the round that closed the fence half; its trigger table and the paragraph on why the mutation had to be applied to both files
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW-2.md` — WR-01, with the measured output showing exit 0, the STALE line and `REMAINING = 14`
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-VERIFICATION.md` — the anti-pattern row recording WR-01 as accepted on the review's executed-mutation evidence
  </read_first>

  <files>scripts/verify-dialogs.mjs</files>

  <action>
**First, reproduce the defect on the shipped gate, before editing anything.**
Add `FULL_BLEED_VIEWER` to `REMAINING` on the live tree, run the unmodified gate,
and record the exit code, the `! B … STALE` line verbatim and the printed
`REMAINING = N`. Assert the mutation present by grep before reading the result
and absent afterwards. This is the round's discipline: a finding taken from a
document rather than re-measured has been wrong twice in this phase.

**Then the fix.** Introduce `NEVER_MEASURED_BY_B` — a map from path to the reason
check B never opens it — holding `PRIMITIVE_FILE` ("the primitive itself; check B
measures copies OF it") and `FULL_BLEED_VIEWER` ("declared exempt from check B —
measured and declared correct; a file that will never convert is not a debt").
Drive check B's two `continue` statements from that map rather than from two
hard-coded comparisons, so the loop and the refusal cannot drift apart: the set
that decides what is skipped is the set the refusal is keyed on.

Widen `fencedRemaining` into `unmeasurableRemaining` — declared paths that are
either fenced or in `NEVER_MEASURED_BY_B` — keeping the refusal exactly where it
is, above check B's loop, for the reason 41-18 recorded: every number the gate
prints derives from what that loop collected, so a run whose two lists
contradict each other must not reach a tick, a STALE notice, or a count.

**The reason line per entry is the substance, not decoration.** Each refused
entry prints which of the three it is, in these terms and kept apart: *fenced —
behind that glob, never opened, a scope boundary that says nothing about the
file's markup*; *exempt — measured and declared correct*; *the primitive itself*.
Collapsing them into one sentence would re-lose the distinction this repository
keeps deliberately.

Keep the existing refusal's closing argument — that the gate cannot tell a debt
somebody paid from one it never opened, that a debt counter falling because the
gate stopped looking is worse than no counter, and that either the entry leaves
`REMAINING` or the fence/exemption does, both being decisions for a person.

Update the printed count block so the exempt line and the fenced line stay
legible as two different things, and so a reader can reconstruct the total from
what is printed.
  </action>

  <acceptance_criteria>
    - **Reproduction, before the edit:** `FULL_BLEED_VIEWER` added to `REMAINING` on the shipped gate produces exit **0**, a `! B` STALE line naming it with the text "converted; remove this entry", and a printed `REMAINING` count; all three recorded verbatim in the SUMMARY
    - **After the edit, same mutation:** exit **2**, a FATAL naming the entry, and a reason line reading as the **exempt** case (measured and declared correct), not the fenced one. No `✓ B`, no `DIALOGS_OK`, no STALE notice, no `REMAINING = ` count printed
    - **`PRIMITIVE_FILE` added to `REMAINING`:** exit **2**, reason line reading as the **primitive** case
    - **Regression guard — the half 41-18 closed still holds:** a fenced path (`src/components/scanner/ScanFlash.tsx`) added to `REMAINING` still produces exit **2**, and its reason line names the glob it sits behind and reads as the **fenced** case
    - **Negative control:** with `REMAINING` restored, `node scripts/verify-dialogs.mjs` exits **0**, prints `DIALOGS_OK`, and the printed `REMAINING = N` is the **same N** as the pre-edit baseline, recorded as a number in the SUMMARY
    - `git status --porcelain` carries no line for any file under `src/`
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"</automated>
  </verify>

  <done>A REMAINING entry naming any file check B never opens refuses with the correct one of three reasons; the fenced case still refuses; the debt count on the clean tree is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: the rung family covers the negative form the printed sentence already promises</name>

  <read_first>
    - `scripts/verify-dialogs.mjs:480-600` — the matcher docblock (what the family covers, the two shapes it cannot see, why both tokens are boundary-guarded, and the DEF-41-01 concatenation rule), `LEFT_BOUNDARY`…`RUNG_FAMILY`, `OVERLAY_PARTS`, `isOverlayLine`, and `MATCHER_PROBES` with its refusal
    - `scripts/verify-dialogs.mjs:995-1020` — the printed self-check block and the sentence that over-promises
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-18-SUMMARY.md` — the WR-03 section: the previous widening, the measurement that justified it, and the connection between widening the rung and needing the Phase 42 fence
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW-2.md` — WR-05, including the measured claim that this tree contains no negative rung, so the delta is a measured zero
  </read_first>

  <files>scripts/verify-dialogs.mjs</files>

  <action>
**Measure the delta before changing anything.** Grep the scanned tree for a rung
token written in the negative form and record the hit count. The review measured
zero; re-measure it, because "the delta is a measured zero" is a claim about this
tree on this day and it is the reason the widening is safe.

**Widen the rung token** so an optional leading hyphen is part of the match,
assembled by concatenation like every other fragment in this file — no complete
utility appears as a literal (DEF-41-01, and the header already says the
positioning utility used to be spelled whole and no longer is).

**Add a sixth fixed probe** for the negative form, assembled the same way, with
the expectation `match`. The probes refuse the run when any of them disagrees
with its description, which is what keeps the printed sentence and the regex from
drifting.

**Assess the widening for the failure mode that matters more** (D-41-19, and §0
rule 3 quoted three times in this file family): a red on a correct file. The left
boundary must still block a rung token preceded by a word character or a hyphen,
so an ordinary hyphenated utility ending in those characters cannot redden a
correct file. Keep or add a probe proving the blocked direction, and state in the
SUMMARY which shapes the widened token now hits and which correct shapes it
would wrongly catch — the answer must be "none on this tree", stated with the
grep count that measured it.

**Update the printed sentence to describe what the matcher now does.** The rung
is matched as a family including its negative form; the two exclusions it still
cannot see — a rung reached through a variable, and a class string assembled by
concatenation — stay stated. The sentence must not claim more than the regex
matches; that identity is the finding.
  </action>

  <acceptance_criteria>
    - The self-check prints **six** probes and all six agree with their stated expectation; the run does not refuse
    - A grep of the scanned tree for the negative rung form returns **zero** hits, recorded as a number in the SUMMARY — the measured zero that makes the widening safe
    - `node scripts/verify-dialogs.mjs` on the clean tree exits **0** and prints the **same** `REMAINING = N` as before the widening: a measured zero delta, not an argued one
    - **The widening bites, proven on a real run:** a transient new file is created under `src/components/` (a path that is not the primitive, not the declared exemption, not behind the Phase 42 fence, and not on `REMAINING`) carrying an overlay whose class string uses the negative rung form. The gate then exits **1** with `✗ B` naming that file under "declare a dialog shell and are not on REMAINING". The file is deleted and `git status --porcelain` asserted empty afterwards
    - No tracked file under `src/` is modified at any point; in particular `src/components/ui/PageShell.tsx` is not touched
    - `npm run build` exits **0**, run only after the transient file is deleted, and **again after the SUMMARY is written** (DEF-41-01)
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-dialogs.mjs; echo "exit=$?"; git status --porcelain -- src/ | wc -l</automated>
  </verify>

  <done>The matcher sees a negative rung, the sentence describes what it sees, six probes agree, and the tree's count is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → human reviewer | `REMAINING = N` is read as the size of a real debt. A count that falls because the gate stopped opening a file is a number that lies to the person planning 41.2 |
| the printed sentence → the next author | A sentence claiming a family the regex does not match teaches the next author to trust a coverage that is not there |
| transient file under `src/` → the tree | A file left behind ships an unreferenced component carrying a live Tailwind class string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-24-01 | Tampering | `REMAINING`'s debt count | mitigate | The refusal is keyed on the derived skip set, so any entry check B never opens refuses instead of being reported converted. Proven by mutating both uncovered categories and re-proving the covered one |
| T-41-24-02 | Repudiation | the printed sentence versus the matcher | mitigate | Six self-check probes refuse the run when the description and the regex disagree; the sentence is edited in the same commit as the widening |
| T-41-24-03 | Denial of service | the widened rung reddening a correct file | mitigate | Both tokens stay boundary-guarded; the tree is grepped for the negative form and the zero is recorded. §0 rule 3: a gate that reddens correct code gets switched off |
| T-41-24-04 | Tampering | the transient probe file left in `src/` | mitigate | Deleted before any build runs; `git status --porcelain -- src/` asserted empty |
| T-41-24-SC | Tampering | package installs | mitigate | None. `package.json` unchanged, no package-manager command runs (D-41-20) |

**Does this change alter what any role or status can see?** No.
`scripts/verify-dialogs.mjs` runs only in development and renders nothing. No
product file is modified. The transient probe file is unimported and never
reaches a route.

**Could a weakened gate let a regression ship unnoticed?** Yes, and that is
precisely WR-01: an unconverted hand-rolled dialog is reported as converted, its
row disappears from the debt list, and 41.2 plans against a number that is short
by one. The mitigation is that the gate refuses rather than reports, because it
cannot tell a debt somebody paid from one it never opened.
</threat_model>

<verification>
- `node scripts/verify-dialogs.mjs` — exit 0 on the clean tree, `DIALOGS_OK`, `REMAINING = N` equal to the pre-change baseline
- Four mutation cycles recorded with exit codes and verdict lines: the exemption on `REMAINING` (before and after the edit), the primitive on `REMAINING`, the fenced path on `REMAINING`, and the transient negative-rung file
- `git status --porcelain -- src/` — empty
- `npm run build` — exit 0, run after the transient file is deleted and again after the SUMMARY is written
- `npm run verify` — record the aggregate verdict verbatim; on a worktree without `.env.local` the expected baseline is exit 2 with `verify:capabilities` REFUSED and `verify:redirects` not run. State it as a property of the environment, not as a pass
</verification>

<success_criteria>
- A `REMAINING` entry naming a fenced path, the primitive, or the declared
  exemption refuses, with the correct one of three reason lines
- The clean-tree debt count is identical before and after
- The overlay matcher sees a negative rung, six probes agree, and the printed
  sentence describes what the matcher does
- No requirement is ticked. DS-08 remains PARTIAL and A2 remains open
- The SUMMARY states that a gate that can now fail is not a surface anyone has
  seen: H41-2 stays unobserved
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-24-SUMMARY.md` when done.
</output>
