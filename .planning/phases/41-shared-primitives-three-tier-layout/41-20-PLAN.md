---
phase: 41-shared-primitives-three-tier-layout
plan: 20
type: execute
wave: 2
gap_closure: true
depends_on: [41-19]
files_modified:
  - scripts/verify-conversion.mjs
  - src/components/ui/PageShell.tsx
autonomous: true
requirements: [RESP-01, RESP-02]

must_haves:
  truths:
    - "CR-01 reintroduced at the render site, with FOCUS_ROOT itself untouched, stops the run — the exact mutation this round's verifier used to prove check E green on the full defect"
    - "Every refusal in verify-conversion.mjs is raised before any check prints a verdict, and a refusal raised while a check has already failed reports the failure rather than replacing it"
  artifacts:
    - path: "scripts/verify-conversion.mjs"
      provides: "a render-site assertion tied to the constant check E1 reads, and refusals that cannot launder a failure"
      contains: "FOCUS_BRANCH"
  key_links:
    - from: "check E1's assertions about the FOCUS_ROOT literal"
      to: "the JSX className expression that renders it"
      via: "a refusal when the focus branch's className is not exactly the identifier"
      pattern: "className=\\\\\\{FOCUS_ROOT"
    - from: "refuse()"
      to: "the failures already recorded when it fires"
      via: "an exit code that reports the failure instead of hiding it behind a refusal"
      pattern: "failures.length"
---

<objective>
Close **GAP-CR-02** and the `verify-conversion` half of **WR-01**.

**GAP-CR-02.** Check E1 — plan 41-17's entire purpose, described in its own objective as
*"the check CR-01 did not have"* — reads the `FOCUS_ROOT` constant's declaration and
asserts three things about that literal. Nothing ties any of them to the JSX expression
that actually renders. This round's verifier appended the full CR-01 defect to the render
site, left `FOCUS_ROOT` untouched, and ran the shipped gate: **exit 0, `✓ E`** — and the
reintroduced line was counted *toward* assertion 3 ("still reads both properties
elsewhere"), so the defect fed the check meant to catch it.

**WR-01.** E1's three refusals sit after checks A–D have printed and pushed into
`failures`, against this file's own comment that check E's measurement is taken early
*"so its refusals precede every tick"*. A run in which a check genuinely fails and E1's
read cannot be taken exits 2, not 1 — `REFUSED` in the aggregate, where the summary then
says nothing failed. A failure that becomes a refusal is a failure nobody sees.

Purpose: the assertion lands on the site where a regression appears, and no refusal can
absorb a failure that already happened.

Output: `scripts/verify-conversion.mjs` with E1's whole read hoisted beside E2's, a
render-site assertion inside it, and a `refuse()` that reports a failure it finds already
recorded — each proven by mutating the trigger the check itself names.

**This plan closes no requirement.** RESP-01 and RESP-02 stay PARTIAL. A gate that can now
fail is not a surface anyone has looked at: H41-1 and `41-CR01-PASS.md`'s thirteen pending
rows are unchanged by this plan and stay owed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-shared-primitives-three-tier-layout/41-VERIFICATION.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-17-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
Read from the shipped gate before editing. Line numbers drift; the symbols do not.

- `refuse(message)` — prints `FATAL: …`, exits 2. Defined near the top, before every
  caller.
- `failures` — the array checks A–E push their letter into. Declared **after** `refuse()`
  today; the verdict block at the end reads it.
- `FOCUS_ROOT_IDENTIFIER` — exported, `'FOCUS_ROOT'`. The marker E1 reads the literal by.
- `FOCUS_ROOT_DECL_RE`, `FOCUS_ROOT_LITERAL_RE`, `MIN_HEIGHT_RE`, `CENTRING_RE`.
- `NAV_PROPERTIES` — `['--nav-inset-inline-start', '--nav-inset-block-end']`.
- `shellLines` — `liveLines(SHELL_FILE)`, comment-stripped, including the JSX form
  DEF-41-02 records.
- `propertyReadsElsewhere`, `propertiesDroppedEntirely` — assertion 3's data, the one the
  reintroduction currently feeds.
- The comment marking E2's measurement point: *"check E's measurement, taken here so its
  refusals precede every tick"*. E1's read belongs beside it.
- `EXEMPT_PATHS` — includes `CATALOGUE_FILE`, the format swatch whose brand hexes check A
  would otherwise report. `ORPHANS_DECLARED` — empty today; a duplicated `path::export`
  pair refuses, and that refusal sits **after** checks A and B print. Both are the
  script-only levers this plan's mutations use, so no product class string is needed to
  stage a failure.

Measured baseline on this tree, 2026-08-12, before any change:
`node scripts/verify-conversion.mjs` → exit 0, `CONVERSION_OK — all five checks passed
over 8 declared surface(s), 53 file(s) scanned`, `✓ E`, and the focus root printed as
`FOCUS_ROOT = "flex min-h-dvh items-center justify-center p-6"` with
`--nav-inset-inline-start` read at line 143 and `--nav-inset-block-end` at line 147.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: every refusal precedes every tick, and no refusal absorbs a failure</name>

  <read_first>
    - `scripts/verify-conversion.mjs` — `refuse()`, the `failures` declaration, the comment
      at E2's measurement point, E1's read block (declaration scan, the two count
      refusals, the literal refusal), the `ORPHANS_DECLARED` duplicate refusal, and the
      verdict block.
    - `41-GAP-REVIEW.md` WR-01 — including the run it recorded, four verdicts printed then
      exit 2.
    - `.claude/rules/meta-gates.md` — *zero fallimenti silenziosi*, and why a failure with
      a neutral face is the shape this repository names.
  </read_first>

  <files>scripts/verify-conversion.mjs</files>

  <action>
    Two changes, both in service of one sentence: a failure must not be able to hide
    behind a refusal, in either direction.

    **1. Hoist E1's read.** Move the whole read — the `FOCUS_ROOT` declaration scan, the
    zero-declarations refusal, the multiple-declarations refusal, the literal refusal, and
    the assignment of `focusRoot` / `focusRootLineNo` — up beside E2's measurement, under
    the comment that already claims refusals precede every tick. Leave only the
    *comparisons* (`propertiesInFocusRoot`, `focusRootHasHeight`, `focusRootHasCentring`,
    `propertyReadsElsewhere`, `propertiesDroppedEntirely`) and the report at their current
    position. Extend that comment to say the read now honours it, and that E1's read is
    part of the same measurement rather than a late one.

    **2. A refusal reports a failure it finds already recorded.** Move the `failures`
    declaration above `refuse()` so it is in scope at every call site — a temporal-dead-zone
    reference would throw a `ReferenceError`, which the aggregate cannot tell from a red
    check, and this file's own header says a broken gate must never read as a finding.
    Then, inside `refuse()`, when `failures` is non-empty: print the FATAL exactly as
    today, then a distinct line naming the checks already failed and stating that a
    measurement that happened outranks one that did not, and exit **1** instead of 2.

    Keep the two verdicts distinguishable. The FATAL line is still printed, so a refusal is
    still legible as a refusal; what changes is that the run no longer reports itself to
    the aggregate as "nothing measured" when something was measured and was wrong.
  </action>

  <mutation_contract>
    Two triggers, each mutated at the site where the regression appears, each recorded.

    **Trigger A — the documented one for hoisting**, in this file's own words: *"check E's
    measurement, taken here so its refusals precede every tick"*. The scenario is a run in
    which E1's read cannot be taken. Mutate it the way the review did: rename the constant
    in `src/components/ui/PageShell.tsx` so `FOCUS_ROOT_IDENTIFIER` no longer finds it.
    - **Pre-fix**, on the shipped gate: assert four verdict lines (`✓ A`, `✓ B`, `✓ C`,
      `✓ D`) print **before** the FATAL, exit 2.
    - **Post-fix**: assert **zero** lines matching a verdict mark for checks A–E appear
      before the FATAL, exit 2.
    - **Negative control**: restore the constant name; exit 0, `✓ E`, `git status
      --porcelain` empty.

    **Trigger B — the documented one for the mixed outcome**: a refusal raised after a
    check has already failed. This is not hypothetical and needs no relocation: the
    `ORPHANS_DECLARED` duplicate refusal sits after checks A and B print. Stage both
    conditions with script-only levers, touching no product file:
    - remove the `CATALOGUE_FILE` entry from `EXEMPT_PATHS`, so the format swatch's brand
      hexes reach check A and it fails;
    - add two identical `path::export` triples to `ORPHANS_DECLARED`, so the duplicate
      refusal fires.
    - **Pre-fix** (before change 2 of this task, with change 1 in place): assert `✗ A`
      prints, then the FATAL, exit **2** — a FAILED run laundered into a refusal, WR-01
      reproduced.
    - **Post-fix**: assert `✗ A` prints, the FATAL prints, the new line names check A, and
      the exit code is **1**.
    - **Negative control**: revert both levers; exit 0, five ticks, `git status
      --porcelain` empty.

    Assert every mutation is present by grep **before** reading any result, and assert its
    absence by grep after. The SUMMARY records both triggers by name, the site each was
    mutated at, and the four exit codes per trigger.
  </mutation_contract>

  <acceptance_criteria>
    - `grep -n 'refuse(' scripts/verify-conversion.mjs` — every call-site line number is
      **lower** than the line number of the first `failures.push(` in the file. Assert by
      comparing the two numbers, not by reading.
    - `grep -n 'const failures' scripts/verify-conversion.mjs` returns a line number lower
      than `grep -n 'function refuse' scripts/verify-conversion.mjs`.
    - `node scripts/verify-conversion.mjs` exits 0 on the unmutated tree and prints
      `CONVERSION_OK` with five ticks.
    - With trigger A applied to the fixed gate: exit 2, and
      `node scripts/verify-conversion.mjs | grep -nE '^  [✓✗] [A-E] ' | head -1` prints
      nothing before the FATAL — capture the full output and assert the FATAL line index
      is lower than every verdict line index.
    - With trigger B applied to the fixed gate: exit 1, output contains both `✗ A` and
      `FATAL:`.
    - With trigger B applied before change 2: exit 2 — the defect reproduced before it was
      fixed, recorded.
    - After every revert: `git status --porcelain` lists only
      `scripts/verify-conversion.mjs`.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"; git status --porcelain</automated>
  </verify>

  <done>
    No refusal in this file can be raised after a verdict has printed, asserted by line
    number; a refusal raised while a check has failed exits 1 and names both; the shipped
    tree still exits 0 with five ticks.
  </done>
</task>

<task type="auto">
  <name>Task 2: check E1 asserts the branch that renders, not only the constant it reads</name>

  <read_first>
    - `scripts/verify-conversion.mjs` — the hoisted E1 read from task 1, `FOCUS_ROOT_IDENTIFIER`
      and its docblock (*"a marker beats a heuristic"*), `shellLines`, and the three
      existing E1 assertions with their report.
    - `src/components/ui/PageShell.tsx` — the focus branch's render site and the
      default/wide branch's two `var(--nav-inset…)` reads.
    - `41-VERIFICATION.md` — the reproduction paragraph for GAP-CR-02: the exact mutated
      className, and the observation that the reintroduced line satisfied assertion 3.
    - `41-GAP-REVIEW.md` CR-02 — the suggested `FOCUS_BRANCH_RE` and its refusal text.
  </read_first>

  <files>scripts/verify-conversion.mjs</files>

  <action>
    Add, inside the hoisted E1 read so it precedes every tick, an assertion that the shell
    renders the declared constant as the whole of exactly one `className`.

    Build the pattern from `FOCUS_ROOT_IDENTIFIER` — the same marker the declaration scan
    uses, so the two cannot drift apart — matching `className={<identifier>}` with nothing
    interpolated beside it. Count the matches across `shellLines` (comment-stripped, so a
    documented example cannot satisfy it). Anything other than exactly one is a refusal.

    The refusal text must carry three things, because a refusal that fires on a correct
    file and does not say what to do is how a gate gets switched off (§0 rule 3):
    - what was measured and what was found — the identifier, the count, and the line
      numbers of any matches;
    - why this is a refusal rather than a failure: check E1 asserts against the constant,
      so a focus root assembled from it **plus anything else** is a form this gate did not
      read, and asserting a property's absence from a fragment is how a check goes green
      on a defect it never saw;
    - what a future legitimate change must do: if the focus branch one day needs a second
      class — the `nav` prop D-41-04 deliberately did not write is the named candidate —
      this assertion widens **in the same commit**, with the measurement that justified it,
      and not by deleting the refusal.

    Record in the E1 docblock the fact this assertion exists to close, in one sentence: the
    reintroduction was counted toward assertion 3, so the defect fed the check meant to
    catch it. That sentence is the reason a reader will not remove this as redundant.

    Do not touch `MIN_HEIGHT_RE` or `CENTRING_RE`. Their secondary weakness is real and is
    out of scope for this round; do not narrow, widen or comment on them here.
  </action>

  <mutation_contract>
    **Documented trigger, in the verifier's own words:** *"mutated the render site only
    (left the FOCUS_ROOT constant untouched) to `className={\`${FOCUS_ROOT}
    ps-[calc(var(--nav-inset-inline-start)+1.5rem)]
    pb-[calc(var(--nav-inset-block-end)+1rem)]\`}` — the full CR-01 defect (248px leading
    padding vs 24px trailing, card 112px right of centre at ≥768px) — and ran the SHIPPED,
    UNMODIFIED gate. Result: exit 0, `✓ E`."*

    **Mutate that exact trigger, at that exact site.** Reproduce the className above in
    `src/components/ui/PageShell.tsx`'s focus branch. `FOCUS_ROOT` itself stays byte-identical
    — a mutation of the constant proves nothing here, because the constant is the one thing
    the shipped check already reads. Assert the mutation present by grep before reading any
    result. **Do not run `npm run build` while it is present**: those are live Tailwind
    candidates (DEF-41-01) and this mutation is transient.

    1. **Pre-fix**, on the gate as it stands after task 1: assert exit 0, `✓ E` present,
       and the report's `read at line(s)` for both properties now including the mutated
       render-site line — the defect feeding assertion 3, observed rather than quoted.
    2. **Post-fix**: assert exit 2, a FATAL naming the shell file and the identifier, and
       `✓ E` absent from the output.
    3. **Negative control**: restore the render site byte-for-byte. Assert exit 0, `✓ E`
       present, `read at line(s)` back to the two default/wide lines only, and `git status
       --porcelain` carrying no entry for `src/components/ui/PageShell.tsx`.
    4. **A second direction, cheap and worth it**: replace the render site's `className`
       with a different-but-correct-looking form (the identifier inside a template literal
       with nothing else in it). Assert it also refuses — the assertion is about the shape
       the gate can read, not about the characters that happen to follow. Restore.

    The SUMMARY records that the trigger mutated was the **render site** and not the
    constant, quotes the mutated className verbatim, and gives the four exit codes.
  </mutation_contract>

  <acceptance_criteria>
    - `scripts/verify-conversion.mjs` contains a render-site pattern built from
      `FOCUS_ROOT_IDENTIFIER` — assert the identifier is interpolated, not re-typed, by
      grepping for the identifier constant's use in the pattern's construction.
    - The new `refuse()` call site's line number is lower than the first `failures.push(`
      line number (the task-1 invariant still holds after this addition).
    - `node scripts/verify-conversion.mjs` exits 0 on the unmutated tree with five ticks.
    - With the CR-01 render-site reintroduction present: exit code 2, output contains
      `FATAL:` and does not contain `✓ E`.
    - With the same reintroduction present on the pre-task-2 gate: exit code 0 and `✓ E`
      present — the gap reproduced before it was closed, recorded verbatim.
    - After restoration: `git status --porcelain` has no line for
      `src/components/ui/PageShell.tsx`, and `git diff HEAD -- src/components/ui/PageShell.tsx`
      is empty.
    - The E1 docblock contains one sentence naming the "reintroduction fed assertion 3"
      fact.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"; git diff --stat HEAD -- src/components/ui/PageShell.tsx</automated>
  </verify>

  <done>
    CR-01 reintroduced at the render site stops the run; the same reintroduction on the
    previous gate was observed passing green with the reintroduced line feeding assertion
    3; the shipped tree exits 0 with five ticks and PageShell is byte-identical to HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `(auth)` and `/payment/callback` | `PageShell` is mutated transiently in both tasks. Those four surfaces render inside it; `access-gating` and `ticketing-payments` are primary there. |
| developer → gate output | The exit code of `verify-conversion` is what a reader and the aggregate believe about eight surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-C1 | Tampering | `src/components/ui/PageShell.tsx` left mutated | mitigate | Both tasks mutate product source transiently. Every mutation is asserted present by grep before its result is read, and asserted absent afterwards by `git diff HEAD --` being empty and `git status --porcelain` carrying no line for the file. `npm run build` is explicitly forbidden while a mutation is present, so no live Tailwind candidate from a mutated class string can reach an emitted stylesheet (DEF-41-01). |
| T-41-C2 | Elevation of privilege | who reaches `(auth)` / `/payment/callback` | accept | Nothing in `files_modified` decides, submits or reports anything on those routes: the gate is a developer script, and `PageShell` returns two nested `div`s. Reachability is `capability-routes.ts`, the middleware and the RLS policies — none touched. The transient mutations touch a `className` and a constant name, never a branch that renders different content for a different role or status. |
| T-41-C3 | Tampering | **a weakened gate ships a regression unnoticed** | mitigate | The subject of this plan and of this round. Three mechanisms: (a) the assertion is moved onto the render site, which is where the regression appears — the verifier's own reproduction is the mutation contract, verbatim; (b) every refusal is proven to precede every tick by line-number comparison, so a refusal cannot arrive after a verdict; (c) a refusal raised while a check has already failed exits 1, so the aggregate reports a failure as a failure. Each is proven by mutating the trigger the check names, with a negative control, and the trigger is written into the SUMMARY so a reader can tell whether the proof matched the claim. |
| T-41-C4 | Denial of service | a refusal on correct code | mitigate | The new render-site refusal fires on any focus branch not written as exactly the identifier — including a future correct one. Mitigated by making the refusal text carry the instruction: widen in the same commit, with the measurement. §0 rule 3 is why the instruction is mandatory rather than nice. |
| T-41-SC | Tampering | supply chain | accept | No package is installed. D-41-20 forbids new packages in this phase. |
</threat_model>

<verification>
- `node scripts/verify-conversion.mjs` exits 0, five ticks, `CONVERSION_OK`.
- `npm run build` exits 0 — run once, at the end, with every mutation reverted.
- `git diff HEAD -- src/components/ui/PageShell.tsx` is empty.
- Every `refuse(` call-site line number is lower than the first `failures.push(` line.
- The SUMMARY names, per trigger, the site mutated and the exit codes before and after.
</verification>

<success_criteria>
- The exact reintroduction this round's verifier used — CR-01 appended at the render site,
  constant untouched — moves the gate from exit 0 / `✓ E` to exit 2, and the before-state
  is recorded, not assumed.
- No refusal can be raised after a verdict has printed; a refusal raised while a check has
  failed exits 1 and names both.
- PageShell is byte-identical to HEAD when the plan ends.
- No requirement ticked; RESP-01 and RESP-02 stay PARTIAL, and H41-1 and the thirteen
  pending `41-CR01-PASS.md` rows stay owed.
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-20-SUMMARY.md` when done.
It MUST contain a section titled "The trigger each mutation used", with one row per
trigger: the trigger as the check's own docblock or report words it, the site mutated
(render site, constant, exemption list — say which), the exit code before the fix, the
exit code after, and the restored exit code.
</output>
