---
phase: 41-shared-primitives-three-tier-layout
plan: 25
type: execute
wave: 2
depends_on: [41-23]
files_modified:
  - scripts/verify-conversion.mjs
autonomous: true
gap_closure: true
requirements: [RESP-01, RESP-02]

must_haves:
  truths:
    - "The exit-code header states what is true of this tree: no refusal in this file can fire after a failure today, and the rule is written for the shape that will be reachable"
    - "A correct FOCUS_ROOT declaration carrying a trailing block comment does not take the conversion gate — and through the aggregate, the whole suite — to REFUSED"
    - "An editor tilde backup beside a wrapper file does not take the whole suite to REFUSED"
    - "Neither relaxation becomes a hole: concatenation still refuses, and an unknown wrapper extension still refuses"
  artifacts:
    - path: "scripts/verify-conversion.mjs"
      provides: "an exit-code header narrowed to the measured truth; FOCUS_ROOT_LITERAL_RE tolerant of a trailing block comment; NON_ROUTE_WRAPPER_EXTENSIONS covering the tilde backup"
      contains: "NON_ROUTE_WRAPPER_EXTENSIONS"
    - path: ".planning/phases/41-shared-primitives-three-tier-layout/41-25-SUMMARY.md"
      provides: "the before/after mutation table for all three findings, including the refusals that must still fire"
  key_links:
    - from: "the ORPHANS_DECLARED duplicate refusal"
      to: "the header's claim about reachability"
      via: "a claim narrowed to the measured state, with the line numbers that measured it"
      pattern: "ORPHANS_DECLARED"
---

<objective>
Three gate-integrity warnings in `scripts/verify-conversion.mjs`, all from
`41-GAP-REVIEW-2.md`, all of shapes this phase has already paid for.

**WR-02 — a refusal branch nothing can reach, under a header that says
otherwise.** The header keeps the "a refusal must not absorb a failure" rule
*"reachable rather than decorative"* by pointing at one refusal: the
`ORPHANS_DECLARED` duplicate check. That check compares `orphanDeclared.size`
with `ORPHANS_DECLARED.length`, and the list is empty — the comparison is `0
!== 0`, false on every run for any tree. Every other refusal call site in the
file sits above the first `failures.push`. So today no refusal here can fire
after a failure, and the branch carries a claim that it is not a decoration.

**WR-03 — the literal reader still reddens correct code, one comment syntax
away from where it was just fixed.** Plan 41-20 relaxed the end anchor for a
trailing line comment. A trailing **block** comment is equally ordinary and
equally correct, and still takes the gate to exit 2 — and through
`verify-all.mjs`, the whole suite to REFUSED.

**WR-04 — an editor backup takes the entire suite to REFUSED.** The wrapper
allow-list covers `.orig`, `.rej` and `.bak` but not the trailing-tilde backup
that several editors write beside the file being edited. One `layout.tsx~` and
all sixteen gates report REFUSED.

Purpose: two of these are the same defect the project has been bitten by seven
times — an acceptance oracle that reddens correct code — now inside the gates
themselves. The third is the opposite shape: a claim of reachability that
nothing can reach.
Output: `scripts/verify-conversion.mjs`, and a SUMMARY carrying the before/after
measurement for each, including the refusals that must **still** fire.

**This plan observes no screen.** RESP-01 and RESP-02 stay PARTIAL; RESP-01
closes only after 41.2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.claude/rules/meta-gates.md

**This plan runs after 41-23**, which rewrites check E1's read in the same file.
Rebase on the tree 41-23 left, re-derive every line number before asserting one,
and re-run 41-23's own acceptance check (`node scripts/verify-conversion.mjs`
exit 0, five ticks) before starting, so a regression introduced here is
attributable.

**Constraints that decide how this is proven:**

1. **No test runner exists for the product** (Guardrail 1, D-41-20). Every proof
   is an exit code, an exact stdout string, or a source assertion.
2. **DEF-41-01, DEF-41-02, DEF-41-06.** Comment strippers in this file family
   are incomplete by record: `{/* … */}` is live to strippers that handle `//`,
   `*`, `/*` and `*/` at line start, and a block comment inside a JSX opening tag
   with prose body lines once made a file unscannable past line 65. WR-03 is a
   cousin of both, and the fix must not be a fourth stripper.
3. **`src/components/ui/PageShell.tsx` is mutated transiently by this plan too**
   (WR-03's trigger is a change to the `FOCUS_ROOT` declaration line). Plan 41-23
   is finished by the time this runs. Every mutation is restored byte-for-byte
   and `git status --porcelain -- src/` asserted empty.
</context>

<interfaces>
- `scripts/verify-conversion.mjs:236-259` — the exit-code header, including the paragraph that ends *"is what keeps the rule reachable rather than decorative"* — WR-02's claim
- `:271-315` — `failures` and `refuse()`, whose post-failure branch is the mechanism in question
- `:970` — `export const ORPHANS_DECLARED = []`
- `:1485` — the first `failures.push(`; every `refuse(` call site is at 982…1345 except one at `:1600`, which is the `ORPHANS_DECLARED` duplicate check
- `:1206-1237` — `FOCUS_ROOT_LITERAL_RE` and the docblock arguing why the anchor was relaxed rather than a stripper added
- `:1268-1278` — the refusal raised when the literal cannot be read as a single closed double-quoted literal
- `:849-879` — `NON_ROUTE_WRAPPER_EXTENSIONS` and the closed-list discipline in its docblock (`.mdx` deliberately absent; `layout.mts` must still refuse)
- `:1076-1110` — the wrapper refusal and the `rel.endsWith(ext)` mechanism, which already handles a suffix that is not a dotted extension
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: the header stops claiming a reachability this tree does not have</name>

  <read_first>
    - `scripts/verify-conversion.mjs:236-320` — the exit-code header and `refuse()`, including the paragraph 41-20 added about the mixed FAILED-then-refused outcome
    - `scripts/verify-conversion.mjs:940-975` — the `ORPHANS_DECLARED` docblock and the empty list
    - `scripts/verify-conversion.mjs:1590-1610` — the duplicate check and its refusal
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-20-SUMMARY.md` — deviation 2, where the executor decided **not** to hoist this refusal precisely so the exit-1 rule would stay reachable, and the trigger-B row that exercised the mechanism (duplicate pair + a red check A → exit 1)
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW-2.md` — WR-02, with the arithmetic that makes the branch unreachable
  </read_first>

  <files>scripts/verify-conversion.mjs</files>

  <action>
**The decision, taken here rather than left to the executor: narrow the header to
the truth, and keep the mechanism.** The alternative the review offers — restore
reachability by making a late refusal genuinely conditional on tree state — is
rejected, and the reason is recorded in the header so it is not re-litigated:
inventing a refusal in order to make a sentence true is engineering the gate to
support a claim, which is the same defect family as the claim itself. The
mechanism is correct, was proven by mutation in round 2, and becomes reachable on
its own when 41.1 or 41.2 first populates `ORPHANS_DECLARED`.

Rewrite the header paragraph so it states, as a measurement rather than an
assertion: the first `failures.push` line number; that every `refuse` call site
except one is above it; that the single site below it is the `ORPHANS_DECLARED`
duplicate check; that `ORPHANS_DECLARED` is empty, so the comparison is between
two zeroes and cannot be true; and therefore that **no refusal in this file can
fire after a failure on this tree today**. Then state that the rule is written
for the shape that will be reachable when the list is populated, name the SUMMARY
that recorded the mutation exercising it, and say what a reader should do if the
list is still empty when they next touch this: re-measure rather than trust the
sentence.

Do not delete the branch, do not hoist the `ORPHANS_DECLARED` refusal, and do not
weaken `refuse()`. What changes is the sentence a reader believes, in a file
whose whole method is written evidence.

**Re-prove the mechanism in the same task**, so the narrowed claim is backed by a
run and not by a citation: populate `ORPHANS_DECLARED` with a duplicated
path-plus-export pair and stage a failing check A with a script-only lever
(41-20's `SCRIM_PREFIX` lever, which does make check A fail — its recorded
deviation 1 documents that the plan's original lever did not). Assert exit **1**
with the FATAL printed first and then the line naming the checks already failed.
Restore both levers and assert them absent by grep.
  </action>

  <acceptance_criteria>
    - The header contains no present-tense claim that the post-failure rule is reachable on this tree, and states instead the three measured facts: the first `failures.push` line number, that exactly one `refuse` call site is below it, and that `ORPHANS_DECLARED.length` is 0
    - Those three numbers, as printed in the header, match what `grep -n 'failures.push(' scripts/verify-conversion.mjs | head -1`, `grep -n 'refuse(' scripts/verify-conversion.mjs` and the `ORPHANS_DECLARED` declaration actually say — asserted in the SUMMARY by quoting both the header text and the grep output
    - **The mechanism still works:** with a duplicated `ORPHANS_DECLARED` pair and a script-only lever making check A fail, the run exits **1**, prints the FATAL first, then `CONVERSION_FAIL — 1 check(s) had ALREADY failed`. Both levers asserted present before the result is read and absent after
    - `node scripts/verify-conversion.mjs` on the restored tree exits **0**, five ticks
    - `git status --porcelain -- src/` empty; no product file touched by this task
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"</automated>
  </verify>

  <done>The header states the measured reachability rather than an aspirational one, the mechanism is re-proven by a run, and the branch is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: the FOCUS_ROOT literal reader tolerates a trailing block comment, and still refuses a fragment</name>

  <read_first>
    - `scripts/verify-conversion.mjs:1206-1280` — `FOCUS_ROOT_LITERAL_RE`, the docblock's argument for relaxing the anchor rather than adding a stripper (a `//` inside the double-quoted literal would be indistinguishable from one after it, and truncating there would hand the regex a fragment), and the refusal that fires when the literal cannot be read whole
    - `scripts/verify-conversion.mjs:376-410` — `liveLines`, and which comment shapes it blanks; the FOCUS_ROOT declaration is not a comment line, so it survives the stripper untouched
    - `src/components/ui/PageShell.tsx:109-125` — the FOCUS_ROOT docblock and the declaration, the line the trigger mutates
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW-2.md` — WR-03, with the measured exit 2 and the FATAL text
  </read_first>

  <files>scripts/verify-conversion.mjs</files>

  <action>
**Reproduce first, on the shipped gate.** Append a trailing block comment to the
`FOCUS_ROOT` declaration line in `PageShell.tsx` — matched by the line's text,
never its number — and record the exit code and the FATAL verbatim. Then restore.

**Relax the tail of `FOCUS_ROOT_LITERAL_RE`; do not add a stripper.** The
docblock's argument is unchanged and now covers one more syntax: the capture
group stays bounded by the same two quotes, so the literal read is byte-for-byte
the literal, comment or no comment. Adding a stripper would be a fourth
incomplete stripper in a file family that already records two — DEF-41-02 and
DEF-41-06 — and would have to decide where a comment begins inside a string,
which is exactly what it cannot do.

**The shapes the tail must accept**, and each must be exercised:
  - a trailing line comment (already accepted — a regression guard, not a new case)
  - a trailing block comment that opens and closes on the same line
  - a block comment that opens on that line and does **not** close on it
  - a semicolon present, and a semicolon absent, in each of the above

**The shapes it must still refuse**, and each must be exercised, because
narrowing a refusal to "not this" must not become narrowing it to "nothing":
  - a literal built by concatenation
  - a literal that does not close on the declaration line
  - a single-quoted or backtick literal

**And the property that makes the relaxation safe, exercised directly:** a
declaration whose literal *contains* the characters that open a block comment
must still be read whole, with the printed
`FOCUS_ROOT = "…"` value byte-identical to the value read from the same line
without them. Compare the two printed values and assert equality — that
comparison, not the regex, is the evidence.

Extend the docblock with the one syntax added and the reason it is the same
argument, so a later reader does not re-derive it as a third round.
  </action>

  <acceptance_criteria>
    - **Reproduction, before the edit:** the declaration carrying a trailing block comment produces exit **2** with the FATAL naming the line, recorded verbatim
    - **After the edit:** the same declaration produces exit **0** with five ticks, and the printed `FOCUS_ROOT = "…"` value is **byte-identical** to the value printed with no comment, asserted by comparing the two captured strings
    - The unclosed-block-comment form, the semicolon-absent form, and the trailing-line-comment form each produce exit **0**
    - A concatenated literal, a literal continuing onto the next line, and a backtick literal each still produce exit **2** with the "not a double-quoted literal closing on that line" FATAL
    - A literal whose contents include block-comment characters is read whole, with the printed value equal to the same literal without them
    - The SUMMARY names what the widened tail hits and states which correct file it would wrongly catch — the answer must be "none", with the exercised shapes as the evidence
    - Every mutation asserted present by grep before its result is read and absent afterwards; `git diff -- src/components/ui/PageShell.tsx` is **0 lines** at the end
    - `npm run build` exits **0**, run only with every mutation restored
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"; git diff --stat -- src/components/ui/PageShell.tsx | wc -l</automated>
  </verify>

  <done>A correct declaration with a trailing block comment passes; a fragment still refuses; the value read is proven unchanged by the comment.</done>
</task>

<task type="auto">
  <name>Task 3: the wrapper allow-list covers the editor tilde backup, and stays a closed list</name>

  <read_first>
    - `scripts/verify-conversion.mjs:840-879` — `NON_ROUTE_WRAPPER_EXTENSIONS` and its docblock: the closed-list discipline, why `.mdx` is deliberately absent, and the statement that an extension in neither list still refuses because an unknown extension is the case the refusal was written for
    - `scripts/verify-conversion.mjs:1076-1110` — the wrapper enumeration, the `rel.endsWith(ext)` mechanism (which already handles a suffix that is not a dotted extension), and the refusal text
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-GAP-REVIEW-2.md` — WR-04, with the measured exit 2 and the note that `verify-all.mjs` reports it as REFUSED for all sixteen gates
    - `.planning/phases/41-shared-primitives-three-tier-layout/41-21-SUMMARY.md` — the round that widened this list for `.orig`, `.rej` and `.bak`, and the direction it took
  </read_first>

  <files>scripts/verify-conversion.mjs</files>

  <action>
**Reproduce first, on the shipped gate.** Copy `src/app/layout.tsx` to a
sibling named with a trailing tilde, run the gate, record exit **2** and the
FATAL verbatim, then delete the copy and assert `git status --porcelain` clean.

**Add one entry** to `NON_ROUTE_WRAPPER_EXTENSIONS` for the trailing-tilde
suffix, with a reason line stating what it is — the tilde form of a backup, what
several editors write beside the file being edited — and that Next resolves no
route file at it. The existing `rel.endsWith(ext)` mechanism already matches a
suffix that is not a dotted extension, so nothing else changes and the closed-list
discipline is not weakened.

**State the false-positive analysis in the docblock and in the SUMMARY**, because
this entry makes the gate skip something and every skip is a place a real wrapper
could hide: the entry matches any path under the app directory whose name ends
with a tilde and whose basename is one of the climbed wrapper basenames. Next
resolves no route file at such a name, so no legitimate wrapper can be skipped by
it. That sentence is the entry's justification, and it belongs beside the entry.

**Record, and do not add: the `.json` form.** The review names `layout.json` as
refusing for the same reason. It is outside the six items the owner fixed for
this round, so it is not added here — named in the SUMMARY under what was
deliberately not taken, so it is not lost, exactly as 41-20 named the
`MIN_HEIGHT_RE` weakness it left.

**Prove the list is still closed**, not merely longer: an unknown wrapper
extension must still refuse.
  </action>

  <acceptance_criteria>
    - **Reproduction, before the edit:** a copy of `src/app/layout.tsx` named with a trailing tilde produces exit **2** with the FATAL naming it, recorded verbatim
    - **After the edit:** the same file produces exit **0**, and the gate's report prints it under "skipped as not a route module" with its reason line
    - **The list is still closed:** a wrapper file carrying an extension in neither list (for instance the `.mts` form the docblock already names) still produces exit **2**
    - The tilde copy is deleted; `git status --porcelain` is empty and shows no untracked file under `src/app`
    - The SUMMARY names what the new entry hits, states that no legitimate wrapper can be skipped by it and why, and records the `.json` form as named and deliberately not taken this round
    - `node scripts/verify-conversion.mjs` on the clean tree exits **0**, five ticks
    - `npm run build` exits **0**, run with the tree clean, and **again after the SUMMARY is written** (DEF-41-01)
  </acceptance_criteria>

  <verify>
    <automated>node scripts/verify-conversion.mjs; echo "exit=$?"; git status --porcelain | wc -l</automated>
  </verify>

  <done>An editor tilde backup no longer takes the suite to REFUSED, an unknown extension still does, and the skip's false-positive analysis is written beside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → human reviewer | A REFUSED aggregate reads as "the machine could not measure". Two of these findings make it fire on correct code, and a gate that reddens correct code gets switched off — after which nothing is measured at all |
| the header's claim → the next author | A file whose whole method is written evidence, carrying one sentence its own arithmetic disproves |
| this plan's transient mutations → `src/components/ui/PageShell.tsx` and `src/app/` | Task 2 edits the shell's declaration line; task 3 creates a file beside the root layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-25-01 | Denial of service | `FOCUS_ROOT_LITERAL_RE` and `NON_ROUTE_WRAPPER_EXTENSIONS` reddening correct code | mitigate | Both relaxed, both exercised on the correct shapes, and both exercised on the shapes that must still refuse so the narrowing does not become "anything goes" |
| T-41-25-02 | Spoofing | a real wrapper hidden behind the new skip | mitigate | The false-positive analysis is written beside the entry: the suffix matches only names at which Next resolves no route file, and an unknown extension still refuses |
| T-41-25-03 | Repudiation | the header claiming a reachability the arithmetic denies | mitigate | The claim is narrowed to the three measured facts, the numbers are asserted against grep output, and the mechanism is re-proven by a run rather than by citation |
| T-41-25-04 | Tampering | transient mutations left in `src/` | mitigate | Each asserted present before its result is read and absent afterwards; `git diff` on the shell asserted 0 lines and `git status --porcelain` asserted empty |
| T-41-25-SC | Tampering | package installs | mitigate | None. `package.json` unchanged, no package-manager command runs (D-41-20) |

**Does this change alter what any role or status can see?** No. All three changes
are to a development-only script; the two product-file touches are transient and
restored. Reachability of the four focus routes is decided in
`src/lib/routes/capability-routes.ts`, the middleware and the RLS policies —
none touched.

**Could a weakened gate let a regression ship unnoticed?** Yes, by the route this
round is about: a gate that refuses on correct code is a gate somebody removes
from the pipeline, and a removed gate cannot catch anything. WR-03 and WR-04 are
that shape; WR-02 is its mirror — a rule that reads as enforced while nothing can
reach it.
</threat_model>

<verification>
- `node scripts/verify-conversion.mjs` — exit 0, five ticks, on the clean tree after all three tasks
- Nine mutation cycles recorded with exit codes and verdict lines: the `ORPHANS_DECLARED` duplicate plus a red check A; the four accepted literal shapes; the three refused literal shapes; the tilde backup before and after; the unknown extension
- `git status --porcelain` — empty, with no untracked file under `src/app`
- `npm run build` — exit 0, run with everything restored and again after the SUMMARY is written
- `npm run verify` — record the aggregate verdict verbatim; on a worktree without `.env.local` the expected baseline is exit 2 with `verify:capabilities` REFUSED and `verify:redirects` not run, which is a property of the environment and must be stated as such
</verification>

<success_criteria>
- The exit-code header states the measured reachability, with numbers that match
  the file
- A trailing block comment on the `FOCUS_ROOT` declaration passes; a fragment
  still refuses; the value read is proven unchanged
- An editor tilde backup is skipped with a printed reason; an unknown extension
  still refuses
- No requirement is ticked. DS-07, DS-08, DS-09, RESP-01, RESP-02, RESP-03 and
  RESP-04 all remain PARTIAL
- The SUMMARY states that a gate that can now fail is not a surface anyone has
  seen, and records the `.json` wrapper form as named and not taken
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-25-SUMMARY.md` when done.
</output>
