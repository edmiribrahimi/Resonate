---
phase: 41-shared-primitives-three-tier-layout
plan: 05
type: execute
wave: 3
depends_on: ["41-01", "41-03"]
files_modified:
  - src/components/ui/PageShell.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Typography.tsx
  - src/components/layout/AppNav.tsx
  - src/app/(public)/payment/callback/page.tsx
  - scripts/conversion-manifest.mjs
autonomous: true
requirements: [DS-07, RESP-01, RESP-02]
must_haves:
  truths:
    - "Content stops widening on a large screen because the page shell caps it, not because a page remembered to"
    - "The navigation clearance is owned by the page shell in every tier, and computes to today's 96px on a phone"
    - "Orbitron renders, on the page title and on nothing else — D-41-15"
    - "The plain Button exists and is rendered by a surface in the same plan that declares it"
    - "One converted surface exists, and it is named in a file two gates read"
  artifacts:
    - path: "src/components/ui/PageShell.tsx"
      provides: "the three widths, the gutter, the rhythm and the nav clearance"
      contains: "max-w-5xl"
    - path: "src/components/ui/Button.tsx"
      provides: "the Button export added to the ladder plan 41-03 wrote, in the plan that first renders it"
      exports: ["Button", "IconButton"]
    - path: "src/components/ui/Card.tsx"
      provides: "the one card shell — rounded-2xl border border-line bg-surface p-6"
    - path: "src/components/ui/Typography.tsx"
      provides: "PageTitle (the display role) and SectionHeading"
      exports: ["PageTitle", "SectionHeading"]
    - path: "scripts/conversion-manifest.mjs"
      provides: "the declared spine, Phase 42's excluded paths, and the converted-surface list G1 and G4 both read"
      exports: ["SPINE", "PHASE_42_PATHS", "PRIMITIVES", "CONVERTED"]
  key_links:
    - from: "src/components/ui/PageShell.tsx"
      to: "src/app/globals.css"
      via: "ps-[var(--nav-inset-inline-start)] and pb-[calc(var(--nav-inset-block-end)+1rem)]"
      pattern: "--nav-inset-"
    - from: "src/app/(public)/payment/callback/page.tsx"
      to: "src/components/ui/PageShell.tsx"
      via: "the page's outermost element"
      pattern: "PageShell"
---

<objective>
Build the page shell, the card and the page title, and convert one whole surface onto
them — the single-file `/payment/callback`. Create the conversion manifest in the same
plan, because a manifest with no entry is a `refuse()`, and because the first entry is
what makes criterion 1 mechanical rather than rhetorical.

Purpose: nothing wider than `max-w-lg` exists anywhere in this tree, so every surface is
edge-to-edge at 1920px — that is RESP-02's defect, and D-41-06 puts the cap in one place.
`pb-24` is written 54 times in 49 files as manual clearance for a fixed bottom bar; now
that the bar leaves the bottom edge at `md` (plan 41-03), the shell is what keeps the
number right in both tiers. And `font-display` has zero consumers in `.tsx` today —
Orbitron is loaded **and preloaded** at the root and renders nothing, which is
`40-REVIEW.md` WR-11, a wasted fetch on every page until a page title renders it.

`/payment/callback` is the smallest whole surface in the tree that exercises all three:
one file, no imports of its own, the `focus` width, the card shell written twice, five
`<h1>`s in five mutually exclusive branches, three primary actions carrying
`bg-accent … text-white` (**2.91:1**, finding A2), and six raw palette colours standing
in for declared semantics.

Output: three primitives; one converted surface; one manifest with one entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-UI-SPEC.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-CONTEXT.md
@.planning/phases/41-shared-primitives-three-tier-layout/41-PATTERNS.md
@CLAUDE.md
@.claude/rules/meta-gates.md
@src/app/(public)/payment/callback/page.tsx
</context>

<interfaces>
`IconButton` and `Chip` were published by plan 41-03, which also wrote the **whole
ladder** — four sizes, four variants, one focus expression — as module-level maps in
`src/components/ui/Button.tsx` and `src/components/ui/Chip.tsx`, with `Button` and
`Badge` **deliberately unexported** and each marked with the plan that adds it (D-41-04).

**This plan adds the `Button` export**, because `/payment/callback` is its first render:
the `lg` size (`min-h-12 rounded-full px-8 text-sm font-semibold`) is *"the single primary
action on a `focus` screen"*, and this page has three of them, one per branch. `Badge`
stays deferred to plan 41-08.

The `wide` list, closed and edited by decision rather than by diff (§4):
`/admin/members` · `/admin/finance` · `/admin/events/[id]/sales` ·
`/admin/events/[id]/tickets` · `/admin/events/[id]/guest-list` ·
`/admin/events/[id]/review` · `/admin/events/[id]/analytics` · `/admin/analytics` ·
`/admin/analytics/members` · `/admin/analytics/compare` · `/admin/members/growth` ·
`/gallery`.
The `focus` list, also closed: `/login` · `/register` · `/set-password` ·
`/payment/callback`. Everything else is `default`.

Manifest shape to copy — `verify-routes.mjs:140-152` (`PUBLIC_ALLOW`), entries as
`[value, reason]` pairs so **the reason travels with the entry**; and
`verify-sunset-gradient.mjs:141-149` for the discipline of growing a declared list.
Cross-script ESM import is house style (`verify-capabilities.mjs:145`).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PageShell, Card, PageTitle, SectionHeading — and the Button export</name>
  <files>src/components/ui/PageShell.tsx, src/components/ui/Card.tsx, src/components/ui/Typography.tsx, src/components/ui/Button.tsx, src/components/layout/AppNav.tsx</files>
  <read_first>
    - `41-UI-SPEC.md` §8.1 (the shell's three forms, verbatim class strings), §4 (the three widths and the two closed lists), §8.4 (the card), §7.1 (where the display role lands and where it must not), §7.3 (the section heading's one string and each axis's reason), §3.1 (why `mb-3` is rejected)
    - `41-CONTEXT.md` **D-41-15** — this is the phase where `--font-display` starts rendering. Orbitron is loaded **and preloaded** at the root with **zero utility consumers in `.tsx`**, which `globals.css:330-335` declared as intended and named this phase as the end of. The 52 `<h1>` across 38 files are where it lands, and `PageTitle` is the only thing that carries it
    - `41-CONTEXT.md` **D-41-12** — the section heading is the second of the two divergent families that needed the UI-SPEC to decide before extraction: four axes across ~20 sites, so it is a decision and not a rename. §7.3 made it, and **no incumbent string survives**
    - `41-CONTEXT.md` **D-41-04** — the `Button` export lands here because this plan renders it; see the interfaces block
    - `41-PATTERNS.md` §2.2 — the best specimen, `(public)/gallery/page.tsx:60-80`, and the delta
    - `src/app/globals.css` — the two layout variables declared in plan 41-01
  </read_first>
  <action>
    Create `src/components/ui/PageShell.tsx` taking `width?: "default" | "wide" | "focus"`
    and rendering exactly §8.1's strings:

    default and wide — outer `min-h-dvh ps-[var(--nav-inset-inline-start)]`, inner
    `mx-auto w-full max-w-5xl px-6 pt-12 pb-[calc(var(--nav-inset-block-end)+1rem)]`;
    `wide` swaps `max-w-5xl` for `max-w-7xl` and nothing else changes.
    focus — `flex min-h-dvh items-center justify-center px-6
    ps-[calc(var(--nav-inset-inline-start)+1.5rem)]
    pb-[calc(var(--nav-inset-block-end)+1rem)]` with an inner `w-full max-w-sm`.

    Use `ps-*`, not `pl-*` — logical properties cost nothing and are correct. The inner
    container centres **inside the space the column leaves**, not inside the viewport,
    which is why the padding is on the outer element. **The shell sets no ground**:
    `globals.css:337-342` already binds `body` to `--background`, so a page writing
    `bg-background` on its own root drops it as redundant when it converts.

    Put the arithmetic in the docblock: on a phone with no safe-area inset the bottom
    padding computes to `80 + 0 + 16` = **96px, the value `pb-24` already produces**. The
    migration is value-preserving on the device it was written for, and that sentence is
    what makes 47 future diffs reviewable.

    Create `src/components/ui/Card.tsx` — `rounded-2xl border border-line bg-surface p-6`.
    `border-card-border` becomes `border-line` because a card's content already says
    where the card is; the **control-boundary** half of the same 406-site triage becomes
    `border-control` and lives in the form controls, not here. Say both destinations in
    the docblock: a rule that can only be kept by remembering which one you meant is not
    kept.

    Create `src/components/ui/Typography.tsx` exporting `PageTitle` —
    `font-display text-3xl font-semibold tracking-tight text-ink` — and `SectionHeading` —
    `mb-4 font-mono text-xs font-semibold uppercase tracking-widest text-muted`.

    `PageTitle`'s docblock must carry §7.1's exclusion list, because this is the file a
    future author will copy from: the display role must **not** appear on a dialog title,
    a card heading, a section heading, a table header, a nav entry, a button, a label,
    **any figure or count**, any body prose, or **any format name**. `SunSet`, `RamaDub`,
    `MotionLab` and `re:sonate` are read, not decorated.

    `SectionHeading`'s docblock records the four axes and why each was chosen against the
    plurality: `mb-4` over the plurality's `mb-3` because 12px is not a step Phase 40's
    ladder names; `font-semibold` because 500 does not exist in this system;
    `text-xs` at the label/data role (`--muted` on `--surface` is **6.78:1**);
    `tracking-widest`; and `font-mono`, because the data face renders anything set in
    caps with wide tracking and until now this was set in the interface face by default.
    **Say that no incumbent string survives** — otherwise somebody will "fix" it back to
    the plurality.

    Add the **`Button`** export to `src/components/ui/Button.tsx`, reading the size and
    variant maps plan 41-03 already wrote — do not write a second ladder, and do not
    change a rung. Delete the comment that named this plan as the one that would add it.
    Task 2 renders it three times on `/payment/callback`, which is why it is declared here
    and not in wave 2 (D-41-04). `Badge` stays absent, with its own comment naming 41-08.

    Then replace the inline `Work` heading string in `src/components/layout/AppNav.tsx`
    with the `SectionHeading` import, so no component in this plan ships without a
    consumer (D-41-04).
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `grep -c 'max-w-5xl' src/components/ui/PageShell.tsx` returns 1 and `grep -c 'max-w-7xl'` returns 1 and `grep -c 'max-w-sm'` returns 1
    - `grep -c 'pl-' src/components/ui/PageShell.tsx` returns 0 — logical properties only
    - `grep -c 'font-display' src/components/ui/Typography.tsx` returns 1
    - `grep -c 'mb-3' src/components/ui/Typography.tsx` returns 0
    - `grep -c 'SectionHeading' src/components/layout/AppNav.tsx` is at least 1
    - `grep -cE 'export (function|const) Button\b' src/components/ui/Button.tsx` returns 1, and the file still contains exactly one definition of each size and each variant — `grep -c 'min-h-12' src/components/ui/Button.tsx` returns 1
    - `grep -cE 'export (function|const) Badge' src/components/ui/Chip.tsx` returns 0 — still deferred, still commented with the plan that adds it
    - `grep -rc 'font-display' src/components/ui/Typography.tsx` is the **only** `.tsx` hit tree-wide besides consumers created later: `grep -rl 'font-display' src --include='*.tsx'` lists exactly one file
  </acceptance_criteria>
  <done>Three primitives exist with the contract's exact strings, Orbitron has somewhere to land, `Button` is exported in the plan that renders it, and `SectionHeading` has a consumer the moment it exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert /payment/callback whole</name>
  <files>src/app/(public)/payment/callback/page.tsx, scripts/conversion-manifest.mjs</files>
  <read_first>
    - `src/app/(public)/payment/callback/page.tsx` in full (178 lines) — five branches, five `<h1>`, three primary actions, two hand-written card shells at `:74` and `:168`
    - `41-UI-SPEC.md` §5.3 (the accent fill's ink, computed), §5.4 (the focus expression), §7.1 (one `<h1>` per surface), §11 (the copy contract and the banned error shape), §9 (three radii)
    - `.claude/rules/ticketing-payments.md` if present, and `CLAUDE.md` Operating Principle 4
  </read_first>
  <action>
    Convert the whole file, not part of it. The surface is one file and it either converts
    or it does not.

    Outermost element becomes `PageShell width="focus"`. The two hand-written card shells
    become `Card`. The three `mt-4 inline-block w-full rounded-full bg-accent py-2.5
    text-sm font-medium text-white` links become `Button` at `lg`, variant `primary` —
    which carries `text-ground` at **6.85:1** instead of white at 2.91.

    The six raw palette colours become the declared semantics for their meaning: the
    success mark `--sem-done`, the failure mark `--sem-crit`, the not-found mark
    `--sem-warn`. Where a semantic is a fill, its ink is `--ground` (`40-UI-SPEC.md` §4.3).

    Each branch's `<h1>` becomes `PageTitle`. They are mutually exclusive, so the surface
    still renders exactly one `<h1>` — that is the invariant, not the count in the file.
    The eleven legacy-token utilities become their new names; these are aliases
    (`globals.css:205-208`), so the rename changes no pixel.

    Every remaining interactive element carries `min-h-11`. Anything at `text-[10px]` or
    `text-[11px]` moves to `text-xs`; body is `text-sm`.

    **This is a money surface and the constraint is what it does not change.** The page
    reads the outcome of a SumUp checkout. **Do not change what it fetches, when it
    fetches, how it polls, which status strings it branches on, or any redirect it
    performs.** The rule this project already applies — *always verify via the GET
    checkout API, never trust the announcement* — is not relaxed, weakened or moved by a
    layout change, and no branch is merged with another because two branches now share a
    component. If a branch's copy collapses distinct causes into one message, note it in
    the SUMMARY under §11's banned shape; **do not rewrite the copy in this plan.**

    Add the entry `["/payment/callback", "src/app/(public)/payment/callback/page.tsx", "focus", …]` to the manifest created in Task 3
    (or create the manifest first and add the entry here — the order inside the plan is
    yours; the commit that declares the surface converted must contain both).
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-tokens.mjs && node scripts/verify-semantic-separation.mjs && node scripts/verify-breakpoints.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; all four gates above exit 0
    - `grep -cE '(bg|text|border)-(red|green|blue|amber|yellow|orange|emerald|rose)-[0-9]' "src/app/(public)/payment/callback/page.tsx"` returns 0
    - `grep -cE 'card-border|bg-card\b|bg-background|text-foreground' "src/app/(public)/payment/callback/page.tsx"` returns 0
    - `grep -c 'text-white' "src/app/(public)/payment/callback/page.tsx"` returns 0
    - `grep -c 'PageShell' "src/app/(public)/payment/callback/page.tsx"` is at least 1 and the shell is the outermost returned element
    - **the money path is byte-unchanged:** `git diff -U0 "src/app/(public)/payment/callback/page.tsx" | grep -E '^[-+]' | grep -cE 'fetch|checkout|status|redirect|router\.|searchParams'` returns 0, or every such line is listed in the SUMMARY with the reason it moved
  </acceptance_criteria>
  <done>One whole surface renders through the shell, the card and the title; nothing about how it learns a payment's outcome moved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: The conversion manifest — the artifact criterion 1 rests on</name>
  <files>scripts/conversion-manifest.mjs</files>
  <read_first>
    - `scripts/verify-routes.mjs` lines 130-160 (`PUBLIC_ALLOW`, `[value, reason]` pairs)
    - `scripts/verify-sunset-gradient.mjs` lines 135-155 (growing a declared list is a decision)
    - `41-PATTERNS.md` §4.1 — the recommendation, and why the manifest cannot be a JSON file nobody reviews
    - `41-CONTEXT.md` D-41-01 (the fourteen-file spine) and the Phase Boundary paragraph
  </read_first>
  <action>
    Create `scripts/conversion-manifest.mjs` exporting three declared lists, each entry
    carrying its reason:

    `SPINE` — the fourteen files D-41-01 names, converted in plans 41-03 and 41-04 (the
    six under `src/components/motion/`, `ui/Icons.tsx`, `ui/Skeleton.tsx`, the three under
    `src/components/toast/`, `formats/FormatMarker.tsx`, `layout/AppNav.tsx` with its
    `MobileNav` wrapper, `staff/StaffNav.tsx`). These are **excluded from a converted
    surface's import-closure walk**, with the reason written in the file: the spine has no
    surface of its own, so converting it shows nobody a half-converted page.
    **Mark `ui/Skeleton.tsx` as pending until plan 41-08 lands** — a spine list that
    claims a file is converted before it is, is worse than no list.

    `PHASE_42_PATHS` — `src/app/(admin)/**/scanner/**`, `src/components/scanner/**`,
    `src/app/(admin)/door/**`, excluded by path with the reason in the file: they are
    Phase 42's, they are not opened by this phase for anything beyond a read, and
    `src/components/layout/MobileNav.tsx` exists as a wrapper precisely so this exclusion
    costs nothing (D-41-21).

    `PRIMITIVES` — every component a plan has published, as **`[path, exportName, reason]`
    triples — one entry per exported symbol, not one per file.** Plan 41-07's G1 check C
    asserts each **named export** has at least one importer, which is the mechanical form
    of the `Skeleton` failure this repository already records.

    **Per export and not per file, deliberately.** `Button.tsx` and `Chip.tsx` each carry
    two rungs of one ladder, published one wave apart; a file-level entry would go green
    on `Button.tsx` the moment `IconButton` had an importer and would never see a plain
    `Button` that nobody rendered. That is precisely the orphan D-41-04 exists to prevent,
    and a per-file check would be blind to it.

    Seed it with everything published up to and including this wave, and nothing else —
    an entry for a symbol that does not exist yet is a claim, not a record:
    `IconButton` and `Chip` (plan 41-03), `PageShell`, `Card`, `PageTitle`,
    `SectionHeading` and `Button` (this plan). `Badge`, `Input`, `Textarea`, `Select`,
    `Dialog`, `DataTable`, `Checkbox` and the `Skeleton` exports are added by the plans
    that publish them.

    `CONVERTED` — the surfaces declared converted, as
    `[route, pageFile, width, reason]` quadruples. `route` is for a human reading the
    list; **`pageFile` is the exact path a gate opens**, so no gate has to reimplement
    Next's route-group and dynamic-segment resolution — the surface inventory already
    records that a work surface's files live in two directories that are not nested in
    one another, so a path-prefix resolver would miss half of a surface. `width` is one
    of `default` / `wide` / `focus` and must agree with §4's two closed lists. First
    entry: `/payment/callback`, `src/app/(public)/payment/callback/page.tsx`, `focus`.

    Export a `refuse()`-worthy condition rather than a silent empty pass: a helper that
    consumers call, or a documented invariant that **an empty `CONVERTED` is exit 2, not
    a green** — *"a vacuous green is not a green"* (`verify-tokens.mjs:582`). Plans 41-07
    and later import this module; it must be safe to import and must not exit on import.

    Write, at the top of the file, the sentence that makes it worth reviewing: **a surface
    enters this list when the whole surface has been converted, and adding an entry is a
    decision, not a diff nobody reads.** The list is what turns criterion 1 from a
    sentence into a check.
  </action>
  <verify>
    <automated>node -e "import('./scripts/conversion-manifest.mjs').then(m=>{const n=m.CONVERTED.length; console.log('converted:',n,'spine:',m.SPINE.length); process.exit(n>0?0:1)})"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - the node command above exits 0 and prints `converted: 1`
    - every `CONVERTED` entry's `pageFile` exists on disk, asserted by a loop over `fs.existsSync`
    - `grep -c 'payment/callback' scripts/conversion-manifest.mjs` returns 1
    - every `SPINE` entry is an existing path: a loop asserting `fs.existsSync` for each returns true for all
    - `grep -c 'scanner' scripts/conversion-manifest.mjs` is at least 1 — Phase 42's exclusion is written, not implied
    - importing the module has no side effect: the node command above did not print a gate report
  </acceptance_criteria>
  <done>Two later gates have one file to read, the spine's exclusion is declared with its reason, and the first converted surface is named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SumUp checkout outcome → the page | the page reads a payment's status; the announcement is never the truth, the GET is |
| manifest → two gates | a wrong entry makes a gate assert the wrong thing about the right file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-14 | Tampering | `(public)/payment/callback/page.tsx` | mitigate | the task forbids touching fetch, polling, status branching and redirects; the acceptance criteria diff those exact tokens and require any movement to be listed with its reason in the SUMMARY |
| T-41-15 | Information Disclosure | the same page | accept | it renders a checkout outcome the visitor already owns; no new field is surfaced and no query is added |
| T-41-16 | Spoofing | `scripts/conversion-manifest.mjs` | mitigate | an empty `CONVERTED` is exit 2 in every consumer, so a manifest emptied by accident refuses rather than passes; `SPINE` entries are asserted to exist on disk |
| T-41-17 | Repudiation | a surface declared converted that is not | mitigate | the entry's reason travels with it, and plan 41-07's G1 walks the closure rather than trusting the claim |
| T-41-SC | Tampering | npm/pip/cargo installs | mitigate | **no packages** (D-41-20) |
</threat_model>

<verification>
- `npm run build` exits 0 after every task
- `verify-tokens`, `verify-semantic-separation`, `verify-breakpoints`, `verify-no-viewport-read` all exit 0
- `git diff --name-only` contains no `scanner/` and no `(admin)/door/` path
- Manual, recorded in the SUMMARY (this is **H41-1** for one surface): open `/payment/callback`
  at 390px, 768px and 1280px; nothing clipped, nothing stretched, no horizontal scroll on
  the page body, and the card centred inside the space the column leaves
</verification>

<success_criteria>
- `PageShell` exists with the three widths and is the outermost element of one page
- `Card`, `PageTitle` and `SectionHeading` exist, each with at least one importer
- Orbitron renders on exactly one kind of element
- `/payment/callback` carries no raw palette colour, no legacy token, no white on accent
- `scripts/conversion-manifest.mjs` names one converted surface, the spine, and Phase 42's exclusion — each with its reason
</success_criteria>

<output>
Create `.planning/phases/41-shared-primitives-three-tier-layout/41-05-SUMMARY.md` when done.
Record: the money-path diff assertion output, the H41-1 observation at the three widths,
and the manifest's printed counts.
</output>
