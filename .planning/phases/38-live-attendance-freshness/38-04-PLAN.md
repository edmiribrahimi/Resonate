---
phase: 38-live-attendance-freshness
plan: 04
type: execute
wave: 3
depends_on: ["38-02"]
files_modified:
  - .planning/phases/38-live-attendance-freshness/baseline/38-BASELINE-realtime.post-38.json
autonomous: false
requirements: [LIVE-01, LIVE-06]

must_haves:
  truths:
    - "The production database carries exactly one policy in schema realtime, it is a SELECT policy, and its rendered predicate names has_capability with door.operate"
    - "No INSERT, UPDATE, DELETE or ALL policy exists on realtime.messages — with RLS on and the table grant already held by authenticated, that absence is what makes the database the only sender"
    - "The four triggers exist on the four tables that reach the attendee list, measured against the before-set captured in plan 38-01"
    - "EXECUTE on the fan-out helper is granted to nobody: F1 is closed in production, not only in a file"
    - "Applying the migration moved zero rows — every table reachable by cascade from the four trigger tables holds the same count before and after"
    - "The project's migration history records this migration as a version, because the migrations endpoint was used and not /database/query"
  artifacts:
    - path: ".planning/phases/38-live-attendance-freshness/baseline/38-BASELINE-realtime.post-38.json"
      provides: "The after-figure against pre-38: policies, triggers, the helper's ACL, the row-count snapshot, and the version the endpoint assigned"
      contains: "realtime_policies"
  key_links:
    - from: "supabase/migrations/20260811120000_live_attendance_channel.sql"
      to: "the production database"
      via: "POST /v1/projects/{ref}/database/migrations"
      pattern: "database/migrations"
---

<objective>
`[BLOCKING]` Apply the migration to production, then measure what it actually did.

Purpose: `npm run build` and the typecheck pass **without** this step. No Supabase
client in this repository is parameterised with `Database`, so no column name in
any query is checked by anything, and a migration is not type-checked at all.
Skipping the apply therefore produces a verification state that is green and
false, and every plan after this one would be exercising a channel nobody sends to.

Three of this phase's claims cannot be settled by reading the file, and are
settled here: that the policy exists as **Postgres renders it** rather than as the
file reads; that `EXECUTE` on the fan-out helper reaches nobody, which is finding
**F1**; and that the four triggers landed on the four tables and not three.

**The Supabase CLI is not installed on this machine.** `supabase db push` is not
executable here. The path is the Management API's *migrations* endpoint —
**not** `/database/query`, which applies DDL while writing nothing into the
project's history and is how eighteen entries of drift accumulated before this
phase.

This plan is `autonomous: false`. It writes to production, and in this project
that is an act with a recorded incident behind it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-live-attendance-freshness/38-01-SUMMARY.md
@.planning/phases/38-live-attendance-freshness/38-02-SUMMARY.md

`.planning/STATE.md:161` records phase 36's apply — version `20260810144239`,
through the migrations endpoint, with the note that the eighteen pre-existing
history gaps predate the phase and repairing them is the owner's call (`PUT` on
the same endpoint upserts without applying). `.planning/STATE.md:178` records
phase 37's, version `20260810210214`, and the reason its second migration was
deliberately applied to zero. Read both before the first request.

`.planning/STATE.md:220` records D12: 63 production rows lost during a phase-36
verification, across seven tables, with no PITR. That is why this plan takes a row
snapshot on both sides of a DDL-only change that should move nothing — a
should-move-nothing that is measured is worth more than one that is asserted.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: The owner reads the SQL that is about to reach production</name>
  <files>supabase/migrations/20260811120000_live_attendance_channel.sql</files>
  <read_first>
    - `supabase/migrations/20260811120000_live_attendance_channel.sql` — the whole file
    - `.planning/phases/38-live-attendance-freshness/38-02-SUMMARY.md` — the structural checks it already passed, and the `SECURITY` clause it chose
    - `.planning/phases/38-live-attendance-freshness/38-01-SUMMARY.md` — the emit-path finding that decided that clause
  </read_first>
  <what-built>
    One migration file: a fan-out helper in the `private` schema with its `EXECUTE`
    revoked, four `AFTER` trigger wrappers and their four triggers, and one `SELECT`
    policy on `realtime.messages`. All inside one `BEGIN;`/`COMMIT;`, every `CREATE`
    preceded by its `DROP … IF EXISTS`. Nothing has been sent to production yet.
  </what-built>
  <how-to-verify>
    This is `CLAUDE.md` Critical work — access, RLS and the door — so the approach
    is presented before it acts, not after. Six checks, with your own eyes:

    1. The header: does the numbered list of changes match what you expect this
       phase to do, and nothing else? In particular, are there four triggers and
       one policy, and is `public.attendances` absent?
    2. The policy: is it `FOR SELECT` only? Read the `CASE` and confirm the topic
       regex comes **before** the `::uuid` cast. The other order is a refused
       connection rather than a `false`.
    3. The predicate: does it call `private.has_capability('door.operate', …)` and
       nothing else? A new function that resolves assignment would be a second
       definition of "assigned to this night", which is the failure phase 32 exists
       to prevent.
    4. The revoke: is `REVOKE ALL ON FUNCTION private.notify_attendance_changed(uuid,
       uuid) FROM public, anon, authenticated` present, with **no** matching grant?
       Without it, any signed-in member could call it over the REST RPC path and
       forge "the list changed" on any night whose id they know.
    5. The two triggers on the money tables: read the header sentence that says they
       are `AFTER` triggers, write nothing of their own, and cannot fail a payment
       path because `realtime.send` catches everything and raises a warning. Are you
       satisfied by it?
    6. Confirm no venue, date, line-up or person appears anywhere in the file, and
       that nothing in it says what any format sounds like.

    **What you are authorising, precisely:** one DDL transaction against production.
    It creates functions, triggers and one policy. It writes no row, deletes no row
    and reads no personal data. The authorisation is spent on exactly this, once. If
    the file changes afterwards, the authorisation does not carry over.

    One scheduling constraint: do not apply this during a night. The triggers land
    on the check-in write path, and although they cannot fail it, a DDL transaction
    takes locks on those tables for the moment it runs. There is no night in
    progress requirement to check beyond asking.
  </how-to-verify>
  <action>
    Present the file and the six checks above, plus the sentence naming exactly what
    the authorisation covers. Do not proceed on silence — an unanswered checkpoint
    is not an approval.
  </action>
  <verify>
    <human-check>The owner has read the file and answered, and no night is in progress.</human-check>
  </verify>
  <acceptance_criteria>
    - **(D)** The owner has responded in words; silence is not approval
    - **(D)** Any correction requested is applied to the migration file and the structural checks of plan 38-02 are re-run before task 2 begins
    - **(D)** The scope of the authorisation is stated before it is given — one DDL transaction, zero row writes — and recorded in the SUMMARY with the date it was spent
  </acceptance_criteria>
  <resume-signal>Type "apply" to send it, or describe what to change.</resume-signal>
  <done>A person has read the SQL that is about to change the production schema.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Snapshot, apply through the migrations endpoint, snapshot again</name>
  <files>.planning/phases/38-live-attendance-freshness/baseline/38-BASELINE-realtime.post-38.json</files>
  <read_first>
    - `.planning/STATE.md:161, 178, 276-281` — the recorded form of the request, the endpoint, and the history-drift note
    - `.planning/phases/38-live-attendance-freshness/38-01-SUMMARY.md` — the `pre-38` figures this is measured against
    - `.claude/rules/ai-engineering.md` — gate *un'istantanea prima copre cio' che si tocca, non cio' che si crea*; a cascade is a write path nobody declared, and it is enumerated by reading the constraints
  </read_first>
  <action>
    Three steps, in this order, and the order is the point.

    **Step 1 — the before snapshot.** Enumerate every table reachable by foreign key
    from `public.door_scan_events`, `public.tickets`, `public.guest_list_entries`
    and `public.ticket_refunds` by **reading the constraints**, not from memory:
    query `pg_constraint` for `contype = 'f'` in both directions and walk the graph.
    Phase 37 found the true count was 18 tables from `event_parties` and not 17,
    because `discount_code_tiers` arrives at two hops through two different paths —
    so walk it, do not eyeball it. Then take `select count(*)` on every table in that
    set and store the result under `rowcounts_before`, with the enumerated table list
    beside it. Read-only, through `/database/query` with `read_only: true`.

    **Step 2 — the apply.** Derive the project ref from `NEXT_PUBLIC_SUPABASE_URL`
    and read `SUPABASE_ACCESS_TOKEN` from `.env.local`, inside the process. Neither
    is printed, neither enters a file, neither enters a commit message.

    Send `POST https://api.supabase.com/v1/projects/{ref}/database/migrations` with
    `Authorization: Bearer $SUPABASE_ACCESS_TOKEN`, `Content-Type: application/json`,
    and a body carrying the file's contents as `query` and the migration's name as
    `name`. Send the file **byte for byte** — do not paraphrase it, do not strip the
    comments — so that what is recorded as the version is what is on disk.

    **The migrations endpoint, not `/database/query`.** The second one applies the
    DDL and writes nothing into the project's history. Using it here would add a
    nineteenth gap to the eighteen that already predate this phase, and repairing
    those remains the owner's call.

    If the request fails, read the error before retrying. The transaction is atomic:
    a failure means nothing was applied. A failure naming a missing column is a
    correct refusal and the answer is to find the column, not to loosen the file.

    **Step 3 — the after snapshot.** Re-run step 1's counts on exactly the same
    enumerated table list and store them under `rowcounts_after`. Compare cell by
    cell. A DDL-only migration must move zero rows; if any count differs, stop and
    report it before doing anything else, because in this project that sentence has
    a precedent and it is not a good one.

    Record in the JSON: the version the endpoint assigned, `applied_at`, the
    enumerated cascade table list, both count sets, and a one-line statement of the
    comparison result. Record in the SUMMARY that the drift in the project's
    migration history predates this phase.
  </action>
  <verify>
    <automated>node -e "const b=require('./.planning/phases/38-live-attendance-freshness/baseline/38-BASELINE-realtime.post-38.json'); if(!b.version) throw new Error('the version assigned by the migrations endpoint is not recorded'); const a=b.rowcounts_before,c=b.rowcounts_after; if(!a||!c) throw new Error('row snapshots missing'); const ka=Object.keys(a).sort(),kc=Object.keys(c).sort(); if(ka.join()!==kc.join()) throw new Error('the two snapshots do not cover the same tables'); if(ka.length<7) throw new Error('cascade enumeration looks too small: '+ka.length+' tables'); const moved=ka.filter(k=>a[k]!==c[k]); if(moved.length) throw new Error('rows moved during a DDL-only migration: '+JSON.stringify(moved.map(k=>[k,a[k],c[k]]))); console.log('applied as version '+b.version+'; '+ka.length+' tables identical before and after')"</automated>
  </verify>
  <acceptance_criteria>
    - **(S)** The cascade table set was enumerated by walking `pg_constraint`, and the enumerated list is stored in the JSON — not a remembered list
    - **(S)** `rowcounts_before` and `rowcounts_after` cover the same table set and every count is identical; a single difference fails this task and stops the plan
    - **(S)** The version assigned by the endpoint is recorded
    - **(G)** The endpoint used was `/database/migrations`; `/database/query` appears in this plan only with `read_only: true`
    - **(G)** No project ref, access token or anon key appears in the written JSON — `grep -cE 'supabase\.co|eyJ|sbp_'` returns 0
  </acceptance_criteria>
  <done>The production schema carries the phase, the project's history says so, and nothing moved that should not have.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the boundary as Postgres renders it, not as the file reads</name>
  <files>.planning/phases/38-live-attendance-freshness/baseline/38-BASELINE-realtime.post-38.json</files>
  <read_first>
    - `.planning/phases/38-live-attendance-freshness/38-01-SUMMARY.md` — `realtime_policies` was `[]`, and `source_table_triggers` was the before-set
    - `.planning/phases/38-live-attendance-freshness/38-RESEARCH.md` § *Code Examples* → *Checking the policy admits exactly the right set*
    - `.planning/phases/38-live-attendance-freshness/38-VALIDATION.md` § *Per-Requirement Verification Map* — the LIVE-06 rows and what the **S** class is allowed to conclude
  </read_first>
  <action>
    Five read-only probes, each stored under a named key and each compared against
    its `pre-38` figure. Where `pre-38` had a figure, the SUMMARY states the
    before and the after side by side; a number with no before is not evidence of a
    change.

    - `realtime_policies` — `select policyname, cmd, roles::text, qual from
      pg_policies where schemaname = 'realtime'`. Expected: exactly one row, `cmd`
      = `SELECT`, `policyname` = `realtime_messages_select_door_assigned`. Read the
      `qual` **as Postgres renders it** and confirm it names `has_capability` and
      the literal `door.operate`. This is the only way to catch a predicate that
      reads correctly in a diff and evaluates to something else: phase 36 has the
      precedent, where an unqualified join would have been `USING (true)` in
      disguise.
    - `write_policies` — the same query filtered to `cmd <> 'SELECT'`. Expected
      `[]`. With RLS on and `authenticated` holding the table grant, that empty
      result **is** the proof that no client can send on the topic.
    - `source_table_triggers` — the same query plan 38-01 ran. Expected: the
      before-set plus exactly four new rows, one on each of `door_scan_events`,
      `tickets`, `guest_list_entries`, `ticket_refunds`. State the difference, not
      just the total.
    - `helper_acl` — `select n.nspname, p.proname, p.prosecdef, p.proconfig::text,
      p.proacl::text from pg_proc p join pg_namespace n on n.oid = p.pronamespace
      where p.proname = 'notify_attendance_changed'`. Expected: exactly one row, in
      schema `private`, `prosecdef` true, `proconfig` carrying the empty
      `search_path`, and a `proacl` granting `EXECUTE` to no role other than the
      owner. **This is finding F1 closed in production.** If `proacl` is null, that
      means the default applies — `EXECUTE` to `PUBLIC` — and the revoke did not
      take: report it as a failure, not as an absence of information.
    - `realtime_send_body` — re-read `prosrc` for `realtime.send` and confirm the
      insert is still wrapped in `EXCEPTION WHEN OTHERS THEN RAISE WARNING`. It was
      measured on 2026-08-11 and it is what makes the trigger on the money tables
      safe; re-asserting it after the change costs one query and is the difference
      between a citation and a check.

    Then a sixth, for the day of the first night rather than for today: `GET
    /v1/projects/{ref}/config/realtime` and confirm `suspend` is still `false`.
    Record it with its timestamp; a `suspend: true` at 22:00 makes the whole phase
    a no-op with no error anywhere.

    Write everything into `38-BASELINE-realtime.post-38.json` and paste the
    before/after table into the SUMMARY.

    **What these probes cannot settle, and must be said rather than implied:** the
    Management API bypasses RLS, so none of these queries proves that a real member
    session is refused. That is procedure **P7**, and it stays open until a session
    exercises it. Say so in the SUMMARY instead of letting one `SELECT` policy in
    `pg_policies` read as a closed requirement.
  </action>
  <verify>
    <automated>node -e "const b=require('./.planning/phases/38-live-attendance-freshness/baseline/38-BASELINE-realtime.post-38.json'); const p=b.realtime_policies; if(!Array.isArray(p)||p.length!==1) throw new Error('expected exactly one policy in schema realtime, got '+JSON.stringify(p)); if(p[0].policyname!=='realtime_messages_select_door_assigned') throw new Error('policy name is '+p[0].policyname); if(p[0].cmd!=='SELECT') throw new Error('policy cmd is '+p[0].cmd); if(!/has_capability/.test(p[0].qual)||!/door\.operate/.test(p[0].qual)) throw new Error('rendered predicate does not ask the door question: '+p[0].qual); if(!Array.isArray(b.write_policies)||b.write_policies.length!==0) throw new Error('a write policy exists on realtime.messages'); const h=b.helper_acl; if(!Array.isArray(h)||h.length!==1) throw new Error('expected one notify_attendance_changed, got '+JSON.stringify(h)); if(h[0].nspname!=='private') throw new Error('helper is in schema '+h[0].nspname); if(h[0].proacl===null||h[0].proacl===undefined) throw new Error('proacl is null — the default EXECUTE to PUBLIC applies and F1 is NOT closed'); const acl=String(h[0].proacl); if(/(^|[,{])=X\//.test(acl)) throw new Error("EXECUTE is granted to PUBLIC: "+acl); if(/(^|[,{])(anon|authenticated)=/.test(acl)) throw new Error("EXECUTE reaches a client role: "+acl); const t=b.source_table_triggers.map(r=>r.tbl); for(const tb of ['door_scan_events','tickets','guest_list_entries','ticket_refunds']) if(!t.includes(tb)) throw new Error('no trigger on '+tb); if(!/EXCEPTION/.test(b.realtime_send_body)) throw new Error('realtime.send no longer swallows its own failure'); if(b.realtime_config.suspend!==false) throw new Error('Realtime is suspended'); console.log('boundary measured: 1 SELECT policy, 0 write policies, 4 triggers, helper private and unreachable')"</automated>
  </verify>
  <acceptance_criteria>
    - **(S)** Exactly one policy in schema `realtime`, named `realtime_messages_select_door_assigned`, `cmd` = `SELECT`, and its **rendered** `qual` contains both `has_capability` and `door.operate`
    - **(S)** Zero policies with any `cmd` other than `SELECT` on `realtime.messages`
    - **(S)** The four triggers exist, stated as a difference against the `pre-38` before-set, one per table
    - **(S)** `notify_attendance_changed` exists once, in schema `private`, `prosecdef` true, and its `proacl` is non-null and grants `EXECUTE` to no client role — a null `proacl` is a failure, not a missing datum
    - **(S)** `realtime.send` still wraps its insert in `EXCEPTION WHEN OTHERS`
    - **(S)** `realtime_config.suspend` is `false`, with the timestamp of the reading
    - **(D)** The SUMMARY states plainly that the Management API bypasses RLS, so LIVE-06 is **not** closed by these probes and stays open until procedure **P7**
  </acceptance_criteria>
  <done>The boundary exists in production, measured as Postgres renders it, and what it does not yet prove is written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine → the Management API | `SUPABASE_ACCESS_TOKEN` crosses here and grants project administration; the request in task 2 is the only write this phase makes to production before the door procedures |
| the migration transaction → the four source tables | a DDL transaction takes locks on the tables the door writes to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Repudiation | the project's migration history | mitigate | `/database/migrations`, never `/database/query`, so the applied version is recorded. The eighteen pre-existing gaps are named as predating this phase and are the owner's call to repair |
| T-38-04-02 | Tampering (data loss) | every table reachable by cascade from the four trigger tables | mitigate | Row counts on both sides of the apply, over a table set enumerated from `pg_constraint` rather than remembered. Any difference stops the plan. D12 is the recorded reason this is not optional |
| T-38-04-03 | Elevation of privilege | the fan-out helper's `EXECUTE` | mitigate | `proacl` read from `pg_proc` after the apply. A null `proacl` means the default `EXECUTE` to `PUBLIC` still applies and F1 is open — treated as a failure, not as a missing value |
| T-38-04-04 | Spoofing | a policy that reads correctly and evaluates otherwise | mitigate | The predicate is read from `pg_policies.qual` as Postgres renders it, not from the file. Phase 36 has the precedent for why the two can differ |
| T-38-04-05 | Information disclosure | the access token and the project ref | mitigate | Both derived and read inside the process, never printed, never written to a file, never committed. Asserted by a grep on the written JSON |
| T-38-04-06 | Denial of service on the door | the DDL locks | accept | Applied outside a night, confirmed at the blocking checkpoint. Production holds three nights and no published event today, which is why this is acceptable now and would not be at 22:00 |
| T-38-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed by this phase; any install appearing here blocks |
</threat_model>

<verification>
No `npm run build` in this plan — no product code is touched. The evidence is
entirely **S**: read-only probes against the real database, each with its expected
result and its `pre-38` counterpart.

What remains unproved after this plan, and is written into the SUMMARY rather
than glossed: the Management API bypasses RLS, so no query here can show that a
real member session is refused. LIVE-06 is closed by procedure **P7** in plan
38-07, and by nothing before it.
</verification>

<success_criteria>
- The migration is applied and recorded as a version through the migrations endpoint
- Row counts are identical before and after across the enumerated cascade set
- Exactly one `SELECT` policy in schema `realtime`, zero write policies, four new triggers on the four tables
- `EXECUTE` on `private.notify_attendance_changed` reaches no client role, measured from `proacl`
- The SUMMARY states what these probes do not settle, and names P7 as what does
</success_criteria>

<output>
Create `.planning/phases/38-live-attendance-freshness/38-04-SUMMARY.md` when done.
Record: the assigned version, the before/after table for every probe, the cascade
table list and its size, and the date the owner's authorisation was spent.
</output>
